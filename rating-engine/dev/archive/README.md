# archive/

Bind-mounted stand-in for the Azure Blob `archive` container (rm04-spec D4).
The evidentiary record — 7-year retention (`infra/bicep/modules/rating-engine-storage.bicep`).
rm09 owns the cross-protocol landing-to-archive move; rm04 only makes this
location exist.
