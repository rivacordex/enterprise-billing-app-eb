# logs/

Bind-mounted stand-in for the Azure Blob `logs` container (rm04-spec D4).
Component log files pending sweep (rm06) — 24-month retention
(`infra/bicep/modules/rating-engine-storage.bicep`). rm04 only makes this
location exist.
