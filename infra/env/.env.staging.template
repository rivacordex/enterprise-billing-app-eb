# um30-spec §"Environment templates". See .env.dev.template for the full
# explanation — non-secret values only, secrets added via Key Vault
# references by the pipeline.
NODE_ENV=production
APP_URL=https://ebill-staging-app.azurecontainerapps.io
BETTER_AUTH_URL=https://ebill-staging-app.azurecontainerapps.io
NEXT_PUBLIC_APP_URL=https://ebill-staging-app.azurecontainerapps.io
LOG_LEVEL=info
ENTRA_TENANT_ID=
MICROSOFT_CLIENT_ID=
# NOTE: distribution SFTP config is NOT here — it is engine config, and the
# deployed engine is a separate Container App. It is wired in
# infra/bicep/modules/workflow-engine-container-app.bicep (params +
# enableSftpDistribution), not in these app-container env templates (bm22 §7).
