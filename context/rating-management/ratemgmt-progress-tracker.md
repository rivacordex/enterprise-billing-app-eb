# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- Phase F — completeness and gap detection (rm00-build-plan.md) — rm12 authored this session; **rm13 (ship-gate assembly) not started**

## Current Goal

- rm13: ship-gate assembly (Phase F/G, per `ratemgmt-code-standards.md` §10's table — "rm13 assembles and runs the [guardrail] suite and owns only test #15"). Not started this session; the actual spec file was not read.
- **Deferred by owner decision (2026-09-02): do not deploy `rating-engine/flows/*.yaml` to the local Kestra engine (`kestra flow namespace update rating ...`) until rm13 is complete.** The local engine is confirmed reachable and login-capable (see the dated Completed entry below), but only the 9 built-in tutorial flows are loaded — none of this module's own flows have been pushed yet. Hold off on the deploy step until rm13 ships.

## Previous Goal

- rm12: completeness and gap detection (`specs/rm12-completeness-gap-detection.md`, Phase F) — see Completed for the full delivered summary.

## Earlier Goals

- rm11: stranded-batch recovery (`specs/rm11-stranded-batch-recovery.md`, Phase E) — a startup + scheduled flow that finds `udr_batch` rows stuck at `PROCESSING` beyond a namespace-KV threshold (a worker killed mid-load by a Container Apps relocation, §9.7), resolves each (`status = 'FAILED'`, releasing the `UNIQUE (file_key, batch_run_num)` claim so the file reprocesses cleanly under `batch_run_num = N+1`), and logs + alarms the outcome as the new `BATCH_STRANDED` code (`MAJOR`, component `SCHEDULER`, coordinated into rm02 per D5/ai-workflow-rules §7.3). Depends on rm07 (the claim) and rm09 (the transaction it recovers from — D2: RL's guard+supersede+insert is one transaction, so a strand's rows never committed and the raw file is still in `landing/`).

- rm10: fill rm09's `# STUB: rm10` supersede hook — batch-level supersession by `file_key` across all partitions (`specs/rm10-supersession-reprocessing.md`, Phase E). On a reissue (`batch_run_num = N`), marks every live row of the same `file_key` from a prior run `SUPERSEDED` (status-only, across all partitions), stamps lineage (`superseded_by_batch_id`, `supersede_reason`) once on the retired `udr_batch` rows, detects a cross-period supersede and a shrinking reissue, all inside rl.py's existing one-transaction load. **Authored + build-hygiene-clean this session** (`tsc --noEmit`/ESLint/Prettier green on the new + touched TS test files; the flow YAML re-parses). **`py_compile` was NOT run this session** — no `python3` on this session's `PATH` (unlike rm09's session) — so `rl.py`'s Python syntax is verified by careful manual review only, not executed; flagged in Open Questions as the one thing to re-verify before this is considered done. The DB-gated black-box suite is not exercised — same posture as rm04-rm09.

- rm09: replace the rm06/rm07/rm08 `rl` stub with the guarded transactional loader (`specs/rm09-rl-guarded-transactional-load.md`, Phase D). Consumes rm08's rated manifest (`outputs.rp.uri`); in **one** psycopg transaction runs the pre-load `BILL_APPROVED` guard (refuse the whole batch → `LOAD_BLOCKED_BILLED`, `REFUSED`, zero rows), the `CURRENCY_MISMATCH` assertion (refuse → `REFUSED`), the named supersede-hook (`# STUB: rm10`, no-op in rm09), the bulk `COPY` insert at `RATED`, and reconciliation (`parsed = rated + rejected + discarded`; imbalance → `RECON_IMBALANCE` `CRITICAL`, `FAILED`); then — **only after commit** — archives the raw file `landing/` → `archive/` (copy-then-delete, idempotent, `archive_file_path` set last). Same infra posture as rm04-rm08: authored + build-hygiene-clean; the DB-gated black-box suite and the live-engine run are not exercised this session.

- rm08: replace the rm06/rm07 `rp` stub with event-time price resolution + snapshot + the `FLAT` calculation (`specs/rm08-rp-price-resolution-snapshot.md`, Phase D). Resolves Open item 2 → (b) the SQL as-of predicate. **Authored + build-hygiene-clean; the DB-gated black-box suite and the live-engine run are not exercised this session.**

- rm07: replace the rm06 `prp` **stub** with the real Pre-Rating Processor for the `RAN_USAGE` CSV feed (`specs/rm07-prp-claim-validate-reject.md`, Phase D). **Authored and build-hygiene-clean; PRP pure logic verified via a stubbed-import harness; the DB-gated black-box suite and the live-engine trigger/deploy are NOT run this session — see Completed and Open Questions.**

- rm06: the rating flow **template** (stubbed `prp`/`rp`/`rl`, `# STUB:` markers only, no business logic), the JSON Lines logging contract, the independent idempotent **log-sweep**, and git-based flow deployment (`specs/rm06-flow-template-logging-sweep.md`). **Files authored, `tsc`/ESLint/Prettier clean on the TS test; NOT run against a live engine or a live database this session — see Completed/Open Questions.**

- rm05: put the rating engine's Kestra UI behind Container Apps Easy Auth with Entra — one `Workflow.Admin` app role, assignment required, gated to a Billing Ops group, IP-allow-listed, 7-year sign-in log retention, client secret in Key Vault (`specs/rm05-entra-reverse-proxy.md`). **Files authored and build-hygiene-clean; NOT deployed, no Entra objects provisioned, no accepted-risk sign-off obtained — see Completed/Open Questions.**

## Completed

- rm12 — completeness and gap detection (`specs/rm12-completeness-gap-detection.md`, Phase F). A new scheduled flow (not a filled-in stub) comparing `rating.udr_batch` against a per-`udr_type` expected-cadence config, raising `FILE_NOT_RECEIVED`/`FILE_LATE`, clearing them itself once a later batch for that period lands and completes, and surfacing superseded-never-replaced usage via the rm01 orphan index. **Authored + build-hygiene-clean this session** (`tsc --noEmit`/ESLint/Prettier green on the new test file; the new flow YAML re-parses via `js-yaml`). **`py_compile` was NOT run this session — no `python3` on this session's `PATH`** (same posture as rm10/rm11) — `completeness_check.py` was verified by careful manual review only, including a full hand-trace of the 4-run raise/clear/idempotency sequence the DB-gated test encodes. The DB-gated black-box suite is not exercised — same posture as rm04-rm11. Delivered:
  - `rating-engine/flows/completeness-check.yaml` (new) — `concurrency: limit: 1`; an hourly `Schedule` trigger (`0 * * * *` — lighter than rm11's 5-minute cadence, since a missed/late file is a day-granularity concern); the two KV values render via the task's `env:` block (`RATING_COMPLETENESS_CONFIG`/`RATING_COMPLETENESS_LOOKBACK_DAYS`), not spliced into `commands:` text — a free-form KV payload embedded directly in `commands:` could break out of its surrounding shell quotes, unlike a purely-numeric KV; one task invoking `python3 -m runtime.completeness_check --config "$RATING_COMPLETENESS_CONFIG" --lookback-days "$RATING_COMPLETENESS_LOOKBACK_DAYS" --workflow-execution-id "{{ execution.id }}"`; `errors`/`finally` handlers both invoking `emit_terminal_log --component SCHEDULER`. Review hardening: `parse_config` (`completeness_check.py`) now rejects a repeated `udr_type` in `--config` (fails closed) — `alarm_key` is keyed on `udr_type` alone, so two entries for the same type would silently name one alarm_key with two conflicting deadlines.
  - `rating-engine/worker/runtime/completeness_check.py` (new) — `python3 -m runtime.completeness_check`. Several design decisions made in the absence of an explicit spec algorithm (Implementation §3 states the *what* — "compare the expected deliveries for the period against `udr_batch`" — not a literal SQL predicate, unlike D5's verbatim orphan query) and recorded here per ai-workflow-rules §5.2/§5.5:
    - **KV config format is a delimiter-based scalar, never JSON** (`"UDR_TYPE:HH:MM,UDR_TYPE2:HH:MM"`, parsed by `parse_config`). D2 calls for "expected cadence and window per `udr_type`" in the KV store; a Kestra `commands:` argument is a shell string, and every existing KV value in this codebase (rm11's threshold, the chunk-size KV) is a plain scalar for exactly the reason a JSON blob's embedded double quotes cannot survive that boundary safely. **Cadence is fixed to daily** (the only cadence the spec's own example — "RAN_USAGE daily, expected by 06:00" — illustrates); a non-daily cadence was **not built**, per ai-workflow-rules §3's "no speculative additions" (building multi-cadence support nobody asked for is exactly the over-engineering that rule forbids). If a non-daily cadence is ever needed, that is a spec change, not a gap in this implementation.
    - **A "period" is a UTC calendar day; "received for period D" means `udr_batch.received_at` (UTC) falls on day D** (`find_batches_for_period`), not a content-date parsed from `file_key`. `file_key`'s date-encoding is a per-`udr_type` PRP filename convention (`file_key_rule`, an rm07 flow **variable**, output-affecting config per D2/architecture §3 — not KV, and not this module's to duplicate or re-derive). Using `received_at` keeps rm12 self-contained at the file-arrival boundary D1 describes ("it exists at the landing directory and nowhere else"), rather than needing to know each feed's filename shape.
    - **Clearing is this module's own responsibility, not RL's** — a genuine design question the spec's own D4 wording ("a later successful batch emits a CLEARED row") left open on *who* emits it. RL has no notion of the scheduler's `alarm_key` (unlike rm11's `BATCH_STRANDED`, cleared by the reprocessed batch's own `BATCH_COMPLETE`, which RL already emits for other reasons) — there is no hook by which RL could know a `FILE_NOT_RECEIVED:RAN_USAGE:2026-08-21` alarm exists to clear. So `completeness_check.py` clears its own alarms: on a later run, once a period's alarm is open (raised, no later `CLEARED`) and any batch for that period has reached `COMPLETE`/`PARTIAL` ("lands and completes", D4's literal words), it emits `CLEARED` itself. Resolves rm09's Open Questions note that "the auto-clear `alarm_key` convention... is settled when the clearing feature (rm12) is built."
    - **Two DISTINCT idempotency guards, not one** (`alarm_already_raised` vs `alarm_is_open`) — caught while hand-tracing the test sequence, not obvious from the spec text. A single "is currently open" guard for the RAISE decision is wrong: the condition a raise reacts to (e.g. "this delivery arrived after its deadline") is a fixed historical fact that stays true forever once it happens, so gating the raise on "not currently open" lets a **cleared** alarm re-open on the very next run (the fact is still true, the guard is satisfiable again) — an infinite raise/clear oscillation. `alarm_already_raised` (has this alarm_key/event_code fired **at all**, ever?) gates the raise instead — a dated `alarm_key` (D3's `EVENT:type:period` form) names one specific occurrence, raised at most once, cleared at most once. `alarm_is_open` (raised, no later `CLEARED`) still gates the clear.
    - **Superseded-never-replaced (D5) is surfaced via the task's own stdout** (captured by Kestra), running the spec's literal orphan-index query verbatim — **not** a new `process_log` event_code. No catalogued code exists for this condition, and the rm02 seed's seventeen codes are exhaustive per its own spec; inventing an eighteenth is exactly ai-workflow-rules §5.1's never-guess item ("an event_code's default severity, or whether it is self-clearing"). "Queryable rather than invisible" (verification item 5) is satisfied by the query running and being correct, not by an alarm.
    - **`FILE_LATE`'s severity in the JSON-Lines line is left NULL** (`perceived_severity` resolved later by the sweep, per every other component's convention, §7.2/Inv #14) — this module never hardcodes severity at the call site, same as every other emitter in this codebase.
  - `rating-engine/worker/runtime/__init__.py`, `README.md` — register `completeness_check` as rm12's entry point (an executable module, not eager-imported, like its siblings); a new module-docstring paragraph / README table row.
  - `tests/rating/rm12-completeness-gap-detection.integration.test.ts` (new). A static `describe` (no `DATABASE_URL`) asserts the SQL predicates, the two-distinct-guards structure, the literal orphan query, that only `FILE_NOT_RECEIVED`/`FILE_LATE`/`CLEARED` are ever emitted (never a non-auto-clearing code), and the flow YAML's trigger/concurrency/handler structure — **passes now (7 passed)**. The DB-gated `describe.skipIf(!DATABASE_URL || !pythonReady)` inserts `udr_batch`/`udr_rated` fixture rows **directly via Drizzle** (rm09's precedent for fixture rows, rather than driving the full `prp`→`rp`→`rl` pipeline — this unit's logic is entirely about evaluating existing state against a schedule, not about producing that state, so a full price-resolution graph proves nothing extra) and shells out to the **real** `python3 -m runtime.completeness_check` and `python3 -m runtime.log_sweep` in sequence (the sweep is what moves a raise from the JSON-Lines file into `rating.process_log`, which `alarm_already_raised`/`alarm_is_open` read — so proving idempotency for real requires actually sweeping between runs, not just re-invoking the check). Covers verification items 1 (`FILE_NOT_RECEIVED` on silence), 2 (a `CLEARED` row against the same `alarm_key` once the delivery lands and completes), 3 (`FILE_LATE` on a late arrival), the D4/D6 oscillation fix across a 4-run sequence (raise → idempotent no-op → late-raise + earlier-clear → self-clear → stable no-op), 5 (the orphan query, including the "SUPERSEDED with a live sibling at the exact same natural key is correctly excluded" edge the spec's literal predicate implies), 7 (`FILE_NOT_RECEIVED`/`FILE_LATE`/`CLEARED` all resolve in `event_catalog`, `is_auto_clearing`), an on-time arrival raising nothing, an empty `--config` no-op, and a malformed `--config` entry failing closed — skipped this session (no live test Postgres, and no `python3` on this session's `PATH` at all, not just missing `psycopg`/`polars` — a stricter gate than rm06-rm11's sessions, which at least had `python3` itself).

- rm11 — stranded-batch recovery (`specs/rm11-stranded-batch-recovery.md`, Phase E). A new startup + scheduled flow (not a filled-in stub) that finds `udr_batch` rows stuck at `PROCESSING` beyond a namespace-KV threshold, resolves each (`status = 'FAILED'`, releasing the `UNIQUE (file_key, batch_run_num)` claim), and logs + alarms the outcome. **Authored + build-hygiene-clean this session** (`tsc --noEmit`/ESLint/Prettier green on the new + touched TS files; both new/touched flow YAML files re-parse). **`py_compile` was NOT run this session** — no `python3` on this session's `PATH` (same posture as rm10) — `stranded_reconcile.py` was verified by careful manual review only. The DB-gated black-box suite is not exercised — same posture as rm04-rm10. Delivered:
  - `rating-engine/flows/stranded-batch-reconcile.yaml` (new) — `concurrency: limit: 1`; a `Schedule` trigger (`*/5 * * * *`) carrying a `backfill: { start: "2026-01-01T00:00:00Z" }` clause as D7's "on flow start" half (Kestra OSS has no distinct "on scheduler start" trigger primitive that this session could confirm — the backfill catch-up mechanism is the best-available substitute, explicitly caveated inline as **UNCONFIRMED against the pinned `v1.3.35` engine**, same D0-spike class as every other Kestra trigger/property specific already flagged in this tracker); one task invoking `python3 -m runtime.stranded_reconcile --threshold-seconds "{{ kv('rating_stranded_batch_threshold_seconds') ?? 3600 }}" --workflow-execution-id "{{ execution.id }}"` (D4 — threshold is namespace-KV, operational config, never a flow variable); `errors`/`finally` handlers both invoking `emit_terminal_log --component SCHEDULER` (code-standards §3.9, `SCHEDULER` was already an accepted `--component` choice).
  - `rating-engine/worker/runtime/stranded_reconcile.py` (new) — `python3 -m runtime.stranded_reconcile`. `find_stranded_batches` (D3): `SELECT ... FROM rating.udr_batch WHERE status = 'PROCESSING' AND now() - started_at > %(threshold)s` (threshold passed as a Python `timedelta`, psycopg-adapted to `interval`, no explicit cast). `resolve_stranded_batch` (D3/D6): `UPDATE ... SET status = 'FAILED', error_summary = ..., completed_at = now() WHERE batch_id = %(batch_id)s AND status = 'PROCESSING' RETURNING batch_id` — the `AND status = 'PROCESSING'` guard makes a concurrent double-resolve a silent no-op (`RETURNING` no row) rather than a duplicate alarm, which is what makes the reconcile idempotent (D6) without needing a separate "already resolved" branch. `emit_stranded_event` (D5): one `process_log` line per resolved batch — `event_code="BATCH_STRANDED"`, `component="SCHEDULER"`, `log_level="ERROR"`, `alarm_key=f"BATCH_STRANDED:{file_key}:{batch_run_num}"` (Implementation §3's literal form), `managed_object=file_key`, `additional_info` carrying `batch_run_num`/`started_at`/`threshold_seconds`. `reconcile()` resolves and **commits each candidate independently** (not one all-or-nothing transaction) so a crash partway through a large reconcile pass leaves the remaining strands for the next run (startup or scheduled) rather than rolling back ones already fixed. Writes nothing to `billing.*` (Inv #1) and computes/re-parses nothing about the stranded batch's actual content — recovery is entirely "release the claim, let the trigger's next fire reprocess" (D3), the same altitude as rm09 D9's "recovery is re-running the batch."
  - **Coordinated `BATCH_STRANDED` catalog addition (D5, ai-workflow-rules §7.3)** — a seventeenth code, `MAJOR`, component `SCHEDULER`, `event_type=processingErrorAlarm`, a new locally-defined probable cause `abandonedClaim`, `is_auto_clearing=true`/`clear_event_code=BATCH_COMPLETE` (the eighth code naming `BATCH_COMPLETE` as its clearer). Shipped in the same change set: `context/rating-management/specs/rm02-event-catalog-seed.md` (the register — the new MAJOR-table row, the D4 locally-defined-cause table entry, the D6/verification-11 seven→eight count, and every live sixteen→seventeen / thirty-two→thirty-four count updated in place, per how that spec already reads as current state rather than a diff); `db/seeds/rating-event-catalog.data.ts` (`RATING_EVENT_CODES` + the seed row + the updated eight-clearer comment); `db/seeds/rating-event-catalog.ts` (the stale sixteen/thirty-two comment). **Note on rm11-spec's own phrasing** — its Implementation §5 says the coordinated update touches "`ratemgmt-code-standards.md` §7" too, but §7.11 already states the code *list* lives in rm02 (the register), not code-standards, and nothing there enumerates codes or a count to update — so no code-standards edit was made; flagging the spec's own wording as slightly imprecise rather than silently overriding rm02-spec's already-settled "the register" framing (ai-workflow-rules §5.6 posture, applied to a doc-vs-doc disagreement rather than doc-vs-code).
  - **Cross-unit test touches (same precedent as rm07/rm09 narrowing a prior unit's now-stale assertions):** `tests/rating/rm02-event-catalog-seed.integration.test.ts` items 3 (sixteen→seventeen codes present), 11 (seven→eight `BATCH_COMPLETE` clearers), and 17 (idempotent re-run leaves seventeen rows, not thirty-four) updated to the new count; `rating-engine/worker/runtime/emit_terminal_log.py`'s stale "none of the sixteen catalogued codes" comment now says "none of the catalogued codes" (dropped the number rather than re-pinning a value that will go stale again).
  - `rating-engine/worker/runtime/__init__.py`, `README.md` — register `stranded_reconcile` as rm11's entry point (an executable module, not eager-imported, like `prp`/`rp`/`rl`/`log_sweep`/`emit_terminal_log`); a new module-docstring paragraph / README table row describing what it does.
  - `tests/rating/rm11-stranded-batch-recovery.integration.test.ts` (new) — code-standards §10 test **#14**'s rm11 half ("the stranded batch it resolves"). A static `describe` (no `DATABASE_URL`) asserts the find/resolve SQL predicates and the flow YAML's trigger/concurrency/handler structure — **passes now (5 passed)**. The DB-gated `describe.skipIf(!DATABASE_URL || !pythonReady)` runs the **real** `python3 -m runtime.prp` to create a genuine claimed batch, back-dates `started_at` directly to simulate the killed worker, then shells out to the **real** `python3 -m runtime.stranded_reconcile` — covering verification items 1-3 (stranded shape, resolved to `FAILED`, a corrected `_v2` reissue then claims `batch_run_num = 2` cleanly), 4 (a fresh batch within the threshold untouched), 5 (idempotent re-run resolves nothing), and 6-7 (`BATCH_STRANDED` resolves to `MAJOR` via `event_catalog`, is `is_auto_clearing`/`BATCH_COMPLETE`-clearing) — skipped this session (no live test Postgres, and `psycopg`/`polars` are not on this session's `python3`), same posture as rm06-rm10's DB-gated suites. **Deliberately does not test** a byte-identical redelivery of the same physical file after a strand is resolved — see the Open Questions entry on `is_duplicate_redelivery`'s status-blind checksum match, which this suite works around by using a corrected reissue (new content/filename) rather than deciding that open question by fiat.

- **2026-09-01 — rm04's local dev stack (`docker compose -f docker-compose.dev.yml -f rating-engine/dev/docker-compose.dev.yml up`) run for the FIRST time, end to end, against a real (locally-run) pinned `v1.3.35` engine.** Not the full D0 ACA process-runner spike (still needs a real Azure Container Apps environment — unexecuted, see the standing D0 Open Question), but the first real evidence any of `kestra.yml`'s D0-caveated property names actually work, and it surfaced + fixed three genuine bugs no prior session's `py_compile`/static-suite posture could have caught:
  1. **`db/bootstrap/kestra-db-roles.sql` was missing `GRANT CREATE ON SCHEMA public TO kestra_engine`.** The existing `GRANT CONNECT, CREATE ON DATABASE "kestra"` only lets `kestra_engine` create NEW schemas; since PostgreSQL 15, `CREATE` on the pre-existing `public` schema is no longer granted to `PUBLIC` by default, so Kestra's own Flyway migrations (which create its tables directly in `public`) failed immediately with `permission denied for schema public`. Fixed with a new Step 4, run against a SEPARATE connection to the `kestra` database itself (schemas are per-database, so it cannot run from the billing-database connection Steps 1-3 use) — `db/bootstrap/kestra-db-roles.ts` now splits the SQL file on a `--> statement-breakpoint-kestra-db` marker (matched by a whole-line-anchored regex, not a plain substring split, after a first attempt collided with the marker string appearing in the new step's own explanatory prose) and opens a second `postgres()` connection with the database swapped in the URL.
  2. **`rating-engine/dev/docker-compose.dev.yml`'s `kestra-setup` service silently never set the `kestra_engine` role password.** Its inline ``sh -c "... node -e \"...sql`ALTER ROLE...`...\""`` command nested a JS tagged-template backtick inside a double-quoted shell string; POSIX `sh` evaluates backticks as command substitution regardless of the JS-side escaping, so it ran `ALTER ROLE kestra_engine WITH PASSWORD '...'` as a (failing) *shell command* (`sh: ALTER: not found`) instead of passing it to Node, and the JS `node -e` body it actually received evaluated to `sql.then is not a function`. Fixed by extracting a real committed script, `rating-engine/dev/set-kestra-role-password.mjs`, and simplifying the compose command to `node rating-engine/dev/set-kestra-role-password.mjs`.
  3. **`rating-engine/kestra/kestra.yml`'s Azure storage config used wrong property names AND wrong env var names — two separate, compounding bugs.** (a) `account-name`/`connection-string` (the D0-caveat's original kebab-case guess) don't exist on `io.kestra.storage.azure.AzureStorage$AzureStorageBuilder` — confirmed by extracting the plugin jar (`io_kestra_storage__storage-azure__1_3_2.jar`) from the running image and reading its Lombok/Jackson builder's field names directly out of the compiled class (no `javap`/`jar` in the JRE-only image, so `grep -a` on the raw `.class` bytecode's constant-pool strings): the real fields are `sharedKeyAccountName` and `connectionString` (camelCase, matching the builder's `@JsonPOJOBuilder(withPrefix = "")` convention — there is no plain `accountName`). (b) Even after fixing the YAML keys, the SAME error persisted (`Unrecognized field "connection"`) because the container env vars `KESTRA_STORAGE_AZURE_ACCOUNT_NAME`/`KESTRA_STORAGE_AZURE_CONNECTION_STRING` — used for `${...}` substitution inside the YAML — are ALSO auto-exposed by Micronaut as their OWN config properties (lowercased, `_` split into dotted/nested path segments), independently of the YAML's `${...}` substitution mechanism; a multi-word env var suffix under the `kestra.storage.azure.*` tree therefore creates a bogus *nested* sibling property (`account: {name: ...}`, `connection: {string: ...}`) that the plugin's Jackson binding then rejects — `ENDPOINT`/`CONTAINER` are single-word suffixes so they never collided. Fixed by renaming those two env vars to `RATING_ENGINE_AZURE_ACCOUNT_NAME`/`RATING_ENGINE_AZURE_CONNECTION_STRING` (moving them out of the `kestra.storage.azure.*` tree Micronaut auto-binds into) in `rating-engine/dev/.env.example`, `rating-engine/kestra/kestra.yml`, **and** `infra/bicep/modules/rating-engine-container-app.bicep` (the deployed engine sets the same `ACCOUNT_NAME` var, so the deployed path had the identical latent bug, unexercised since `deployRatingEngine=false`).
  - **Result**: `enterprise-billing-app-rating-engine-1` reaches a clean start — Flyway migrations apply, "Main server is running", "Health endpoint is available", indexer/worker/scheduler/executor all start with no further errors (one harmless `SecretService` warning that `SECRET_RATING_RUNTIME_PASSWORD` isn't base64 — expected and already documented, since that secret is read raw by `db.py`, never through Kestra's own `{{ secret() }}`). The UI (`http://localhost:8085/ui/` locally) serves `200 OK` and confirms `"1.3.35"` — the pinned version. **Still NOT exercised**: the ACA process-runner itself (D0's actual scope — this was a Docker Compose run, not Container Apps), Managed-Identity Blob auth (this run only proved the `connectionString`/Azurite path), and none of the `flows/**` (no flow was triggered/run this session — `ran-usage-rating.yaml`/`log-sweep.yaml` are deployed as files onto the engine but not exercised).
  - Verified: `tsc --noEmit`/ESLint/Prettier clean on the touched TS; `az bicep build` clean on the touched Bicep module; the local stack brought up and Kestra confirmed reachable and correctly versioned as described above.

- rm10 — supersession and reprocessing (`specs/rm10-supersession-reprocessing.md`, Phase E). Fills rm09's `# STUB: rm10` supersede hook in `load_and_reconcile`. **Authored and build-hygiene-clean** (`tsc --noEmit`/ESLint/Prettier green on both the new and the touched TS test files; the flow YAML re-parses as valid YAML). **`py_compile` NOT run this session — no `python3` on `PATH`** (unlike the rm09 session); `rl.py`'s new/changed Python was verified by careful manual trace instead (the SQL text, the control flow through `load_and_reconcile`/`supersede_batch`/`emit_supersede_events`/`main`, and the four consecutive-reprocessing lineage chain worked through by hand) — flagged in Open Questions as the one re-verification still owed. The DB-gated black-box suite is not exercised — same posture as rm04-rm09. Delivered:
  - `rating-engine/worker/runtime/rl.py` — fills the hook (rm10 D1-D9), inside RL's existing one transaction, immediately before the `COPY`:
    - **`supersede_batch`** runs the D3 predicate — `UPDATE rating.udr_rated SET status = 'SUPERSEDED' WHERE is_live AND udr_ref_batch_id IN (SELECT batch_id FROM rating.udr_batch WHERE file_key = $file_key AND batch_run_num < $N) RETURNING partition_period, udr_key, udr_ref_batch_id` — keyed on **`file_key`, never `source_file`** (D3/Inv #5), scoped **across all partitions** (D2), touching **only `status`** (D1). Superseding an already-superseded row is a no-op for free: `is_live` is `NULL` once `SUPERSEDED`, so `WHERE is_live` already excludes it (no extra guard code needed).
    - **Lineage (D1/D3)** — `superseded_by_batch_id`/`supersede_reason` stamped once on the **retired `udr_batch` rows** (`WHERE batch_id = ANY(retired_ids)`, from the `RETURNING`'s distinct `udr_ref_batch_id`s) — never on `udr_rated` (there is no `superseded_by_udr_id` column, rm01 D11). Because each run's predicate only ever matches rows still live at that moment, four consecutive reprocessings naturally chain — batch 1 → superseded_by batch 2, batch 2 → batch 3, etc. — without any special-casing (verification item 5).
    - **Cross-period detection (D2)** — `scan_and_guard` (already reading every chunk for the guard/currency pass) now also collects `incoming_periods: dict[udr_key, set[partition_period]]` as a side product (no extra chunk read, Inv #10 applied to reads too). A superseded row is cross-period when its `partition_period` is not among the run-N successor row(s) sharing its `udr_key` — the corrected-timestamp case. Emits `CROSS_PERIOD_SUPERSEDE` (`WARNING`, already catalogued by rm02, component RL, `isAutoClearing: false`) with a bounded sample.
    - **Shrinking-reissue check (D4)** — compares run-N's `parsed_count` (from the manifest's `Counts`) against run-(N−1)'s `parsed_count`, read directly from `udr_batch` (`WHERE file_key = $file_key AND batch_run_num = $N-1`) — never re-derived. Smaller → `SHRINKING_REISSUE` (`MAJOR`, already catalogued, component RL). `superseded_count` (this run's own count of rows it retired) is stamped on the CURRENT batch inside the existing `stamp_terminal` call (extended with one new parameter) — distinct from the lineage stamped on the *retired* batches.
    - Both alarms are emitted via `emit_supersede_events`, called **after** the load transaction commits (they describe committed fact, matching the terminal-event precedent) but **before** the archive step — neither gates the load; `SHRINKING_REISSUE` does not refuse the batch (the spec's "MAJOR" is a surfaced alarm, not a refusal, unlike `LOAD_BLOCKED_BILLED`/`CURRENCY_MISMATCH`).
    - `load_and_reconcile` and `get_batch_state` gained `file_key`/`batch_run_num` params/columns respectively — `batch_run_num` is read from the DB state probe (authoritative), since neither PRP's nor RP's manifest carries it; `main()` threads it through unchanged otherwise.
    - Module docstring (item 4) and the "Scope boundaries" paragraph updated from "the supersede hook is a `# STUB: rm10` no-op" to describe the real behaviour.
  - `rating-engine/flows/ran-usage-rating.yaml` — the header comment and the `rl` task's inline comment updated from "`# STUB: rm10` no-op" to describe the real batch-level supersession + its two alarms; no task/variable changes (the hook lives entirely inside `rl.py`, not the flow YAML).
  - `rating-engine/worker/runtime/__init__.py`, `README.md` — the rm09 entry no longer claims the supersede-hook is a stub; a new rm10 paragraph (`__init__.py`) / table-row wording (`README.md`) describes what it fills.
  - `tests/rating/rm10-supersession-reprocessing.integration.test.ts` (new) — code-standards §10 tests **#4** (the skipped-supersede proof — a live row the supersede predicate cannot reach still aborts the `COPY` on the unique constraint, Inv #3 backstop), **#7** (supersession scope — a reissue under a NEW filename, same `file_key`, supersedes; `source_file` differs between the two batch rows, proving the predicate never keyed on it), and **#10**'s `SHRINKING_REISSUE` half (a reissue smaller than its predecessor raises the alarm rather than silently losing the un-reissued record — asserted via `additional_info.parsed_count`/`prev_parsed_count` and the dropped record's absence from any live row). Plus: cross-period supersede (a corrected timestamp moving a record to a different monthly partition is still superseded, across the partition boundary, and emits `CROSS_PERIOD_SUPERSEDE` at `WARNING`), the four-consecutive-reprocessings lineage chain (verification item 5 — 4 `SUPERSEDED` + 1 live row, each retired batch pointing at the batch that replaced it, not all at the final run), and structural checks (status-only `UPDATE`, no `superseded_by_udr_id` anywhere in `rl.py`, lineage only on `udr_batch`, supersede runs before the `COPY` inside `load_and_reconcile`). A static `describe` (no `DATABASE_URL`) asserts the predicate/lineage/event-code text in `rl.py` — **passes now (4 passed)**. The DB-gated `describe.skipIf(!DATABASE_URL || !pythonReady)` seeds a single MYR account/offering/subscription and shells out to the **real** `python3 -m runtime.prp` → `runtime.rp` → `runtime.rl`, run multiple times per scenario (fresh reissues under `_v2`/`_v3`/… filenames matching the shared `RAN_USAGE_<date>(_v<N>)?.csv` file-key rule) — **skipped this session** (no live test Postgres, and `psycopg`/`polars` are not on this session's `python3`), same posture as rm06-rm09's DB-gated suites.
  - **Cross-unit test touch (same precedent as rm07/rm08/rm09 narrowing a now-stale prior assertion):** `tests/rating/rm09-rl-guarded-transactional-load.integration.test.ts`'s static check that asserted the literal `# STUB: rm10` marker (and its introductory comment) is updated to assert the *real* supersession instead (`supersede_batch` present, the stub marker gone, `load_and_reconcile`'s doc-comment referencing "batch-level supersession").

- rm09 — RL guarded transactional load (`specs/rm09-rl-guarded-transactional-load.md`, Phase D). Replaces the rm06/rm07/rm08 `rl` stub with the guarded transactional loader. **Authored and build-hygiene-clean** (`py_compile` green on `rl.py`; the flow YAML parses; the pure decision logic — `period_of` UTC bucketing incl. a `+08` event crossing the month boundary, `Counts.discarded` folding `lookup_miss`, the guard/currency/recon branches, `COMPLETE` vs `PARTIAL`, and the archive copy-then-delete + idempotent recovery — verified this session by a stubbed-import harness, **23/23**; `tsc --noEmit`/ESLint/Prettier green on the TS test; the rm09 static suite **3 passed / 7 skipped**). **NOT run against a live database or a live Kestra engine** — same posture as rm04-rm08. Delivered:
  - `rating-engine/worker/runtime/rl.py` (new) — `python3 -m runtime.rl`, the RL entry point. Consumes RP's rated manifest (`outputs.rp.uri`); **no-ops on a non-`PROCESSING` status** (a `DISCARDED` redelivery / lost claim, the rm07/rm08 forward contract — touches no DB, does not archive, exit 0). For a `PROCESSING` batch, in **ONE** psycopg transaction (`db.transaction`, Inv #8, D1): (1) the **`BILL_APPROVED` guard** (D2/Inv #6) — one set-based query over all incoming `(start_datetime, udr_key)` keys (`unnest`, never per-record); any live `BILL_APPROVED` collision raises `BatchRefused` → the whole batch is refused (zero rows, `udr_batch.status = REFUSED`, `LOAD_BLOCKED_BILLED` at `MAJOR` naming the colliding keys + their `billrun_ref_id` in `additional_info`, `managed_object` = the source file per D2); (2) the **`CURRENCY_MISMATCH` assertion** (D3) — the resolved `udr_currency` vs `billing_account.currency` joined via `product_inventory.billing_account_id`, one set-based query over the distinct `(subscriber, currency)` pairs; a mismatch refuses the batch at `MAJOR`; (3) the **supersede-hook** — a named `# STUB: rm10` no-op inside the transaction (D8, `batch_run_num = 1` has nothing to supersede); (4) the **bulk `COPY` insert at `RATED`** (D4) via `db.copy_insert` (never row-by-row, Inv #10), `partition_period` computed per row as the UTC month bucket (`period_of`, mirroring `rating.period_of()` — the `udr_rated_period_matches_check` CHECK is the backstop, Inv #15), `status = 'RATED'`, money parsed back to `Decimal` (never `float`, §5.9), the live-row unique constraint the final backstop (Inv #3); (5) **reconciliation** (D5) — stamp `rated_count` and assert `parsed = rated + rejected + discarded`; an imbalance raises `ReconImbalance` → `RECON_IMBALANCE` at `CRITICAL`, batch `FAILED`, the insert rolled back (zero rows); (6) **terminal status** (D7) — `COMPLETE` (all parsed records rated) or `PARTIAL` (some rejected/discarded within threshold), stamped inside the transaction. **Only after the transaction commits** (Inv #9, D6): the **archive** — copy `landing/<file>` → `archive/<file>` (a **copy**, not a move, so a crash mid-copy leaves landing intact), verify size, delete from landing, then set `udr_batch.archive_file_path` (a URI) **last** in a separate committed statement — so a batch that committed but whose archive did not finish keeps `archive_file_path` NULL and is recoverable. **D6/D9 recovery**: on entry RL probes the batch state — a batch already `COMPLETE`/`PARTIAL` with `archive_file_path` NULL takes the **archive-only** path (never re-loads, which would abort on the live-row constraint); already-archived is a full no-op. A refusal (`REFUSED`) / imbalance (`FAILED`) exits non-zero (the domain alarm is the signal; the file stays in `landing/`, never archived), matching PRP's refuse-exit-non-zero precedent. RL writes nothing to `billing.*` (Inv #1, grant-enforced) and computes no rate.
  - `rating-engine/flows/ran-usage-rating.yaml` — the `rl` `# STUB:` replaced with the real `python3 -m runtime.rl` task (Process runner, D0), passing `--manifest "{{ outputs.rp.uri }}"`, `--udr-type`, `--landing-dir "{{ vars.landing_dir | default('/data/landing') }}"` (the mount holding the raw file to archive, joined with the manifest's `source_file`), `{{ execution.id }}`/`{{ flow.revision }}`. Header comment updated (all three sections `prp`/`rp`/`rl` are now REAL; the supersede-hook inside rl's transaction is the remaining `# STUB: rm10`). `errors`/`finally` handlers unchanged (already `--component RL`).
  - `rating-engine/worker/runtime/__init__.py`, `README.md` — register `rl` as rm09's entry point (an executable module, not eager-imported, like `prp`/`rp`/`log_sweep`); README row + storage `move` owner note updated (rm09 implements the real archive in `rl.py`, copy-then-delete across the landing/archive boundary).
  - `tests/rating/rm09-rl-guarded-transactional-load.integration.test.ts` (new) — code-standards §10 tests **#8** (approved-record guard), **#10** (`RECON_IMBALANCE` half), **#14** (archive ordering: file + rows half). A static `describe` (no `DATABASE_URL`) asserts the flow-YAML contract (real `runtime.rl` task, `outputs.rp.uri` handoff, `--landing-dir`), the `# STUB: rm10` supersede-hook marker, and that `rl.py` uses `copy_insert`/`COPY` and no per-record `INSERT INTO rating.udr_rated` — **passes now (3 passed / 7 skipped)**. The DB-gated `describe.skipIf(!DATABASE_URL || !pythonReady)` seeds the product/ordering/inventory graph (invA/invB on an MYR account, invC on a **USD** account for the mismatch) and shells out to the **real** `python3 -m runtime.prp` → `runtime.rp` → `runtime.rl` (black-box), covering: clean → `COMPLETE` + reconcile + file in `archive/` (archive_file_path a URI, source_file unchanged); the `BILL_APPROVED` guard (a pre-inserted live approved row read from the rated Parquet → `REFUSED`, zero rows, `LOAD_BLOCKED_BILLED` naming the key + `billrun_ref_id`); `CURRENCY_MISMATCH` (invC's USD account vs MYR price); mid-transaction integrity (a pre-inserted live `RATED` collision aborts the COPY → zero rows, file left in `landing/`, batch stays `PROCESSING`); `RECON_IMBALANCE` (a tampered manifest count → `FAILED`, zero rows); the archive-after-commit **recovery** (committed rows + `archive_file_path` NULL + file restored to landing → re-run archives only, never re-loads, count unchanged); and the non-`PROCESSING` no-op forward contract — skipped this session (no live test Postgres, and `psycopg`/`polars` are not on this session's `python3`).
  - **Resolves rm08's forward contracts to RL:** RL no-ops on a non-`PROCESSING` manifest; asserts `CURRENCY_MISMATCH` (rm08 only stamped `udr_currency`); and composes the reconciliation identity including rm08's `lookup_miss_count` (see the count-folding decision in Open Questions).

- rm08 — RP price resolution and snapshot (`specs/rm08-rp-price-resolution-snapshot.md`, Phase D). Replaces the rm06/rm07 `rp` stub with event-time (as-of) price resolution + snapshot + the `FLAT` calculation. **Resolves Open item 2 → (b) the SQL as-of predicate.** Authored and build-hygiene-clean (`tsc`/ESLint/Prettier green on the TS test; the flow YAML parses as valid YAML; `py_compile` green on `rp.py`; the pure `Decimal` rounding + FLAT-rate-detail logic verified by a stubbed-import harness this session — HALF_UP(0.005)=0.01, HALF_EVEN(0.005)=0.00, TRUNCATE(0.005)=0.00, unknown mode + non-`FLAT` variant both rejected). **NOT run against a live database or a live Kestra engine — same posture as rm04-rm07.** Delivered:
  - `rating-engine/worker/runtime/rp.py` (new) — `python3 -m runtime.rp`, the RP entry point. Consumes PRP's manifest (`outputs.prp.uri`); **no-ops on a non-`PROCESSING` status** (a `DISCARDED` redelivery / lost claim — rm07's forward contract — passing the status through in its own manifest, touching no DB, exit 0). For a `PROCESSING` batch: reads each PRP Parquet chunk, runs **one set-based as-of SQL query per chunk** (the `WITH price_windows … lead() … WHERE eff_from <= :t AND (:t < eff_to OR eff_to IS NULL)` `[start, end)` predicate from spec §2, chunk passed as typed `unnest(...)` arrays — never per-record, Inv #10; records keyed by `line_no` since `udr_id` doesn't exist until RL's `core.generate_ulid()` default), resolving through the **pinned** `product_offering` version (the order item's immutable `product_offering_id` FK, D2/§6.1) with `effective_amount = COALESCE(oipo.amount, popp.amount)` (D2). **Snapshots** the resolved inputs onto the row (D4) — `udr_usage_rate`, `udr_price_ref`, `udr_price_effective_date`, `udr_price_override_ref`, `udr_rounding_mode`. Computes **`FLAT`** (D5): quantity **ignored**; `udr_rated_price_raw` at full precision (`format(x,'f')` exact decimal string, no float — §5.9), `udr_rated_price` rounded **once** to 2 dp per `udr_rounding_mode` (`HALF_UP`/`HALF_EVEN`/`TRUNCATE`, mapped to `ROUND_HALF_UP`/`ROUND_HALF_EVEN`/`ROUND_DOWN`, matching `udr_rated_rounding_mode_check`; fail-closed on any other mode). Stamps `udr_currency` from the **resolved price row** (D9 — the `CURRENCY_MISMATCH` assertion is RL's, not here), `udr_rate_type='FLAT'` + the minimal `{"rateType":"FLAT"}` `udr_rate_detail` (a Python mirror of the Zod `flatRateDetailSchema`, validated before carry — non-`FLAT` variants are a `# STUB:`, D6), and the version columns `rated_datetime` / `rating_engine_version` (from the `RATING_ENGINE_VERSION` container env, rm04 D6) / `rating_flow_revision` (D10/Inv #12). Emits a rated Parquet chunk per input chunk (typed UTC `Datetime`, money as exact `Decimal` strings) + a rated manifest (`outputs.rp.uri`) carrying the batch identity, `rated_count`, `lookup_miss_count`, and PRP's own counts forward for reconciliation. A record whose subscriber/offering/price does not resolve — **or whose resolved catalog price carries no scalar `amount` (a `tiered` price, unratable by the v1 `FLAT` calc)** — raises **`LOOKUP_MISS`** (already catalogued `MAJOR`, component RP) as **one summarised `process_log` line** (Inv #11, with a bounded sample of miss line numbers) and is **not rated / not fabricated** (D3). **RP writes nothing to `rating.*`** — it only reads the seven enumerated tables and produces Parquet; the insert is RL's one transaction (Inv #8), and RP does not touch `udr_batch` counts (RL/reconciliation own the final tally).
  - `rating-engine/flows/ran-usage-rating.yaml` — the `rp` `# STUB:` replaced with the real `python3 -m runtime.rp` task (Process runner, D0), passing `--manifest "{{ outputs.prp.uri }}"` + the new output-affecting flow **variables** `rounding_mode` (`HALF_UP`, D7/§5 — the single v1 rounding value) and `subscriber_ref_column` (`PUBLIC_KEY`, D3 — which chunk column carries the `product_inventory_id`; **ASSUMED** for the RAN_USAGE sample whose three keys are opaque, flagged "confirm with upstream" like rm07's `file_key_rule`), plus `{{ execution.id }}`/`{{ flow.revision }}`. Emits `outputs.rp.uri`. The header comment and the `rl` stub comment were updated (rp is now REAL; only `rl` remains stubbed; rl documented to no-op on a non-`PROCESSING` status — the rm07/rm08 forward contract). `rl` remains STUBBED (rm09/rm10).
  - `rating-engine/worker/runtime/__init__.py`, `README.md` — register `rp` as rm08's entry point (an executable module, not eager-imported, like `prp`/`log_sweep`/`emit_terminal_log`).
  - `tests/rating/rm08-rp-price-resolution-snapshot.integration.test.ts` (new) — code-standards §10 test **#13** (price snapshot reproducibility — the headline) plus as-of correctness (#2 pinned version via a decoy branched OFF2 whose 9.9999 price is never resolved, #3 the `[start, end)` boundary → new price), override (#4), currency (#5), FLAT + quantity-ignored + raw/rounded (#6), the three rounding modes (#7 — same 0.0050 amount → HALF_UP 0.01 vs HALF_EVEN/TRUNCATE 0.00), Decimal/no-float (#8), snapshot columns populated (#9), version stamps (#10), `LOOKUP_MISS` (#11 — one summarised line), FLAT-only scope (#12), and the non-`PROCESSING` no-op forward contract. A static `describe` (no `DATABASE_URL`) asserts the flow-YAML contract (real `runtime.rp` task, the `outputs.prp.uri → outputs.rp.uri` handoff, the `rounding_mode`/`subscriber_ref_column` variables, the flow-revision stamp) — **passes now (3 passed / 9 skipped this session)**. The DB-gated `describe.skipIf(!DATABASE_URL || !pythonReady)` seeds the product/ordering/inventory graph via Drizzle (a subscription pinned to OFF1 with a dated usage-price chain P1→P2 and a usage override on a second subscription), then shells out to the **real** `python3 -m runtime.prp` → `python3 -m runtime.rp` (black-box, reading the rated Parquet back via python+polars, not a TS reimplementation) — skipped this session (no live test Postgres, and `psycopg`/`polars` are not on this session's `python3`).

- rm07 — PRP claim/validate/reject (`specs/rm07-prp-claim-validate-reject.md`, Phase D). Replaces the rm06 `prp` stub with the real Pre-Rating Processor for the `RAN_USAGE` CSV feed. **Authored and build-hygiene-clean (`tsc`/ESLint/Prettier green on the TS test; the flow YAML + embedded feed-profile JSON parse; the PRP pure logic — `file_key`, canonical `udr_key`, per-row validation, `DUPLICATE_IN_FILE`, threshold — verified by a stubbed-import harness this session). NOT run against a live database or a live Kestra engine — same posture as rm04/rm05/rm06.** Delivered:
  - `rating-engine/worker/runtime/prp.py` (new) — `python3 -m runtime.prp`, the PRP entry point. **`file_key` from the filename** via a configured regex rule with a named `file_key` group (`FILE_KEY_UNRESOLVED` at `MAJOR` + non-zero exit on no match — never "treat as new", D3/Inv #5); **`hashlib` checksum + `DUPLICATE_BATCH` discard** of a byte-identical redelivery *before* the claim, emitted as a `WARNING` with a `DISCARDED` manifest and exit 0 (D5); the **`COALESCE(max(batch_run_num),0)+1` claim insert** committed *before* parsing so a parse crash leaves a `RECEIVED` row for rm11 reconciliation (D4/Inv #7), with a concurrent `UniqueViolation` loser making no batch (verification item 2); a **config-driven `FeedProfile`** (column mapping + `udr_key` column list + event-time/usage roles, validated fail-loud, D1) parsed from the `--profile` flow variable; the **fixed canonicalisation rule** (sorted key names, trimmed + `casefold`ed values, UTC for any declared timestamp key column, `k=v` joined by `|`, D2) — the measured value is excluded by construction; **per-row validation** to the D6 reason codes (`MALFORMED_ROW`/`BAD_DATETIME`/`BAD_USAGE`/`MISSING_KEY_FIELD`/`OUT_OF_RANGE`/`DUPLICATE_IN_FILE`, plus `UNKNOWN_SUBSCRIBER` **only** when a profile declares a subscriber-ref mapping — wired with a one-query `inventory.product_inventory` batch resolve, never per-record); a **reject file** `error/<file_key>-run<N>-rejects.csv` with `line_no` + reason code(s) + the **original physical row** carried verbatim as a single CSV field (the row's own bytes, including garbled quoting — NOT reserialised from the parsed fields, which would lose the original quoting; the reject file is itself CSV, so recovering the row means unquoting that one field); the **per-`udr_type` reject threshold** (`0` = all-or-nothing → `PARSE_FAILURE` + `REFUSED` + non-zero exit so RP/RL are skipped; below → carry survivors, `BATCH_PARTIAL`); **chunked Parquet** output sized by `--chunk-size` (typed UTC `Datetime` for `start_datetime`; the measured quantity carried as an exact `Decimal` **string** — no `float` path, D8) plus a **manifest** JSON (batch identity, status, ordered chunk URIs) whose file URI is the `outputs.prp.uri` handoff to RP; the **counts** (`parsed_count`/`rejected_count`/`discarded_count`=0) stamped on `udr_batch` via columns all inside `rating_runtime`'s UPDATE grant; and **one** summarised `process_log` line per outcome (never per-record, Inv #11) with `perceived_severity` left for the sweep to resolve (§7.2a) — PRP supplies only the `event_code` + `log_level`.
  - `rating-engine/flows/ran-usage-rating.yaml` — the `prp` `# STUB: rm07` replaced with the real `python3 -m runtime.prp` task (passing the profile/rule/threshold vars + the `{{ kv('rating_ran_usage_chunk_size') }}` chunk size + `{{ trigger.uri }}`/`{{ execution.id }}`/`{{ flow.revision }}`); rm06's manual **Webhook trigger replaced with the `landing/` file trigger** (`io.kestra.plugin.fs.local.Trigger`, `action: NONE` — the DB claim owns the file, never the trigger, Inv #7/#9); output-affecting **flow `variables`** introduced (`feed_profile` single-line JSON, `file_key_rule`, `reject_threshold`, `udr_type`, `landing_dir`) per architecture §3 (chunk size stays namespace-KV, performance-only). `rp`/`rl` remain STUBBED (rm08/rm09).
  - `rating-engine/worker/runtime/__init__.py`, `README.md` — register `prp` as rm07's entry point (not eager-imported, like `log_sweep`/`emit_terminal_log`).
  - `tests/rating/rm07-prp-claim-validate-reject.integration.test.ts` (new) — code-standards §10 test **#9** (two filenames → one `file_key`; two content dates → never the same key; unparseable name → `FILE_KEY_UNRESOLVED` + no batch; byte-identical redelivery → `DUPLICATE_BATCH` + no new batch) and **#12** (N rejects → exactly one summarised `process_log` line, independent of N; counts stamped; the N land in the reject file), plus threshold-0 refuse and chunked-Parquet handoff. A static `describe` asserts the flow-YAML contract (real prp task, landing trigger + `action: NONE`, the output-affecting variables, the chunk-size KV reference) — passes now; the DB-gated `describe.skipIf(!DATABASE_URL || !pythonReady)` shells out to the **real** `python3 -m runtime.prp` (black-box) and is skipped this session (no live test Postgres, and `psycopg`/`polars` are not on this session's `python3`).
  - **Cross-unit test touch (necessary, same precedent as rm01 narrowing a billing guard):** `tests/rating/rm06-flow-template-logging-sweep.integration.test.ts` — rm07 changed the flow's `prp` section (now real, no `# STUB: rm07` marker, `python3 -m runtime.prp` not `python3 -c`) and its trigger (landing file, not Webhook), so rm06's two structural assertions that asserted the old state were updated to the new reality with rm07-attribution comments.
  - **Review hardening (`/code-review high` pass, 7 finder angles + verify):** fixed the surfaced findings by severity. (1) **[critical] Fail-fast on an empty/missing source file** (`prp.py` main step 0) — an empty `--source-file` (e.g. an unresolved trigger-output expression) previously derived a blank `file_key` and then **crashed** the `FILE_KEY_UNRESOLVED` handler on `logemit.line`'s empty-correlation-field guard (never reaching the intended `return 1`); a file removed between trigger and checksum raised an opaque `FileNotFoundError`. Both are now one clear stderr message + exit 1 before any logging/DB work. (2) **[flow] Corrected the trigger caveat** — the `{{ trigger.uri }}` binding is now honestly flagged as UNCONFIRMED (the trigger may emit `files[]`/`uris[]`, not a top-level `uri`) and named the #1 D0-spike item, with fix-#1 above as the fail-safe. (3) **Whitespace-only lines are skipped, not rejected** — a trailing `"   "` line parsed to `[' ']` (truthy), was counted + rejected `MALFORMED_ROW`, and could spuriously tip a good file over the reject threshold into `REFUSED`; now any all-blank line is a non-record. (4) **Reject file preserves the ORIGINAL row + true physical line number** — switched from streaming `csv.reader` over the file (which lost raw bytes and numbered by logical record) to reading physical lines and parsing each (single-line-record contract, made explicit), so a garbled/misquoted row is quarantined verbatim at its real line number (D6 "the original row"). Removed the now-dead `_raw_line` (and its function-local `StringIO` import). (5) **Bounded `DUPLICATE_IN_FILE` memory** — `seen_keys` now holds a 160-bit `blake2b` digest per natural key instead of the full `(isoformat, udr_key)` strings (udr_key up to 512 chars), so a 5M-row file no longer risks a multi-GB in-RAM set that contradicts Inv #10's chunking intent. (6) **Refuse cleanup + crash-orphan altitude** — refuse now `shutil.rmtree`s the whole per-batch work dir (all chunks at once) instead of per-file `unlink(missing_ok=True)` that silently swallowed storage faults; a crash leaves a batch-identifiable work dir for rm11 reconciliation. (7) **Work dir defaults to the system temp dir**, not `/data/work` (the parent of the four mounts, which need not be writable) — the chunks/manifest are ephemeral intra-execution handoff artifacts; `--work-dir` overrides. (8) **Removed speculative dead code** — `udr_key_timestamp_columns` (not a spec-listed profile field, no configured feed uses it) is gone from the identity-critical canonicalisation path; the key-name sort is now done once at profile construction (`sorted_key_columns`) rather than per row. `subscriber_ref` is kept (D1/D6 spec-grounded, profile-gated). Two DB-gated regression tests added (whitespace-skip + raw-fidelity; empty-source-file fail-fast). All re-verified: `py_compile`, stubbed end-to-end harness (whitespace skipped, dup at accurate physical line, original raw preserved, refuse rmtree's the dir, fail-fast returns 1 without touching logemit), `tsc`/ESLint/Prettier clean, static suites **11 passed / 22 skipped**. **Deliberately deferred (lowest severity, cross-cutting):** extracting the ~80-line DB test harness shared with rm06 into `tests/helpers` (touches rm06's passing test — a standalone cleanup), and removing the now-orphaned `RATING_USAGE_WEBHOOK_KEY` secret plumbing (`rating-engine-container-app.bicep` / `dev/.env.example` / `engine-access.md`, a cross-unit rm04 infra touch) — both already flagged in Open Questions.
  - **Review hardening (2nd `/code-review high` pass — caught bugs the FIRST fix pass introduced/left):** (1) **[real] Step-0 fail-fast crashed on a remote/`kestra://` URI** — `storage._local_path` *raises* `ValueError` on a non-`file://` scheme, and step 0 called it before its guards, so for exactly the "unresolved trigger binding" inputs the fix targeted (`kestra://`, `abfss://`) it produced an uncaught traceback instead of the intended clean exit. Now wrapped: an empty value, an unsupported-scheme URI, a no-filename path, and a non-existent/non-file path each `return 1` with a distinct stderr message. (2) **[real regression] Leading-blank-line header bug** — the first fix pass replaced the old `header_seen` flag with `if line_no == 1: continue`, so a file with a blank first line (some exporters emit one) skipped the blank *as* the header and then parsed the real header row as data → spurious `MALFORMED_ROW` + inflated reject count (could tip a small file into `PARSE_FAILURE`). Restored "the header is the first NON-blank line," in both the main loop and `_scan_subscriber_refs`. (3) **[real] Vanished-file after the `is_file()` check** now returns 1 cleanly instead of an opaque `FileNotFoundError` at checksum (the guard's own stated goal — the check narrows but cannot close the TOCTOU window). (4) **[fail-closed] Defended the single-line-record contract** — an odd double-quote count (an unterminated quoted field, i.e. a garbled row or an embedded-newline record this per-line parser does not support) is now a **loud** `MALFORMED_ROW` rather than a silent split into two possibly-corrupt rows (§5.4); this resolves the altitude concern that the per-line parse's RFC-4180 limitation was enforced only by a comment. (5) **Cleanup** — `physical.rstrip("\r\n")` (one char-set pass, identical result), `next(csv.reader([raw]), None)` (drops the dead `list(...)`/`if not decoded` construct). **Deliberately NOT changed, with reason:** the per-row `csv.reader([raw])` allocation (accepted — C-fast, and per-line parsing is what buys raw-byte reject fidelity + true physical line numbers); `blake2b(... isoformat() ...)` (isoformat is the exact canonical instant form — the memory bound is the goal, and a float `.timestamp()` risks identity-key precision loss); the `/tmp` handoff durability + the `-dup`/`-lost` discard dirs (the ephemeral intra-execution handoff on one pod is fine for the stub chain; durable Blob/internal-storage handoff is rm08's, already noted); the D2 "UTC for any timestamp component" removal (documented deferral — no configured feed keys on a timestamp, and there is no signal to guard on without re-adding the profile field). Re-verified: `py_compile`, the stubbed harness (leading-blank tolerated + header correctly skipped, odd-quote → MALFORMED at the accurate physical line with the original preserved, `kestra://`/`abfss://` URIs → exit 1 without touching `logemit`), `tsc`/ESLint/Prettier clean, static suites **11 passed / 22 skipped**.

- rm06 — flow template, logging contract and log sweep (`specs/rm06-flow-template-logging-sweep.md`). **Resolves the rm04-deferred repo-boundary question** (see Architecture Decisions) — asked the user directly rather than guessing, since `ratemgmt-ai-workflow-rules.md` repeatedly says flows live in "a separate repository" and this is the first unit that would actually create one; the user chose to keep flows in `enterprise-billing-app`. Delivered:
  - `rating-engine/flows/ran-usage-rating.yaml` (new) — the template: `prp`/`rp`/`rl` as three named, ordered, `# STUB:`-marked sections naming their owning spec (rm07/rm08/rm09), each a `python3 -c` placeholder emitting a file URI for the next section (D5), a manual `Webhook` trigger (D3 — the `landing/` file trigger is rm07's), `concurrency: limit: 1` (D6), and `errors`/`finally` handlers that both invoke `emit_terminal_log` (D7). Task `commands` are explicitly documented as illustrative (matching the spec's own line 120 caveat) — the binding contract is the markers/order/handoff/concurrency/handlers, not the literal Python.
  - `rating-engine/flows/log-sweep.yaml` (new) — the independent sweep flow: a 5-minute `Schedule` trigger (cadence is an operating default, not namespace-KV config — Kestra's Schedule trigger needs a compile-time cron, flagged as a deviation from rm00's "namespace-KV config" framing), one task invoking `python3 -m runtime.log_sweep`, and its own `errors`/`finally` terminal-outcome handlers (D9 — the sweep can no more sweep its own crash than a flow can).
  - `rating-engine/worker/runtime/log_sweep.py` (new) — the real sweep implementation (D9/D10): parses unmarked `*.jsonl` files under `--dir`, resolves `perceived_severity` via one `LEFT JOIN rating.event_catalog` per line with the `CASE WHEN ec.event_code IS NULL THEN 'INDETERMINATE' ELSE ec.default_severity END` shape (never `COALESCE`), computes `partition_period = rating.period_of(log_datetime)` at insert, and is idempotent by rename-on-completion to `logs/swept/` — one transaction per file (all its lines + one summarised malformed-quarantine row, or nothing). A file whose last line is torn (no trailing newline) is skipped **wholesale** this run, not partially inserted, so a retry can never duplicate a partial load. A malformed line quarantines its raw text to `logs/malformed/<file>` and contributes to one `WARN`/`INDETERMINATE` summary row per file (never one row per bad line) under a deliberately uncatalogued `MALFORMED_LOG_LINE` event_code — see the module's own docstring for why that's a considered choice, not an oversight, and the Open Questions note below on the tension with the "`INDETERMINATE` count must be zero" hygiene metric.
  - `rating-engine/worker/runtime/emit_terminal_log.py` (new) — the `errors`/`finally` terminal-outcome emitter (D7), invoked as `python3 -m runtime.emit_terminal_log`. Uses `STUB_EXECUTION_FAILED`/`STUB_EXECUTION_FINALIZED` event codes, deliberately **not** added to `specs/rm02-event-catalog-seed.md`'s catalog — inventing a new catalog row's severity is a `ratemgmt-ai-workflow-rules.md` §5.1 never-guess item, and rm06 ships no business logic to justify picking one; they resolve via the sweep's own no-row→`INDETERMINATE` path instead. Correlation fields the template can't yet derive (`batch_id`, `source_file` — that's rm07's PRP) fall back to a literal `"UNKNOWN"` rather than being omitted, since `logemit.line()` requires every correlation field non-empty (§7.6).
  - `rating-engine/worker/runtime/__init__.py`, `README.md` — extended to list the two new modules and record that rm06 (not "a later rm06/rm07 unit") is the one that adds the Dockerfile `COPY`.
  - `rating-engine/worker/Dockerfile` — adds `COPY runtime/ /app/runtime/` + `ENV PYTHONPATH=/app`, baking the whole runtime package (db/storage/transform/logemit + rm06's two new modules) into the image as `import runtime` / `python3 -m runtime.<module>`, matching the flows' invocation style. rm04 deliberately left this COPY for "the first flow unit"; this is it.
  - `infra/azure-pipelines.yml` — new `deployRatingFlows` boolean parameter (default `false`, same conservative-gate precedent as `buildRatingEngine`/`deployRatingEngine`) and a `deploy_rating_flows` stage (D2): `kestra flow validate` then `kestra flow namespace update rating ./flows` via the pinned Kestra image's own CLI, gated on `deployRatingFlows=true` **and** `Build.SourceBranch = refs/heads/main` (D2's "on merge to main" — never on a PR validation run), reading the engine's FQDN via `az containerapp show` and the Basic Auth password live from Key Vault (`kestra-basic-auth-password`) rather than a duplicated pipeline secret variable. Carries explicit, un-silenced caveats (see Open Questions) rather than assuming they're solved: CLI flag names are unconfirmed against a real engine, the pipeline's hosted agent has no established network path to the Kestra webserver (every other stage in this pipeline deliberately goes through the ARM control plane instead, for exactly this reason), and the pipeline's own service-connection principal isn't yet granted Key Vault read access anywhere in this repo's Bicep.
  - `infra/docs/engine-access.md` — new "Flow deployment — the UI is read-only in practice (rm06 D2)" section documenting the mechanism and the same three unresolved items as the pipeline comment, so they're recorded in exactly one place a future reader would look first.
  - `tests/rating/rm06-flow-template-logging-sweep.integration.test.ts` (new) — code-standards §10 test #16 (owned by rm06). A static `describe` (no `DATABASE_URL` needed) scans the flow YAML text for the D1/D3-D7 structural contract (STUB markers naming their spec in order, no rate-maths/parsing tokens inside the actual `python3 -c` command bodies specifically — comments naming `BILL_APPROVED`/`landing/` etc. to say what's *not* here don't trip it, `concurrency: limit: 1`, file-URI handoff, both handlers present, no secret literal/`console.*`). A DB-gated `describe.skipIf(!DATABASE_URL || !pythonReady)` shells out to the **real** `python3 -m runtime.log_sweep` (black-box — not a TS reimplementation of its SQL) against a live test Postgres, covering: JSON-Lines round-trip of a `specific_problem` containing quotes/newlines/a pipe; the three-outcome severity resolution (`DB_WRITE_FAILURE`→`CRITICAL`, `BATCH_COMPLETE`→`NULL`, an uncatalogued code→`INDETERMINATE`) with `log_level` passed through unchanged; `partition_period` computed and absent from the line; idempotent re-sweep; malformed-line quarantine + summary row; torn-last-line deferral and later pickup; a "crashed flow" log still sweeping; and the `log_datetime`→`insert_datetime` lag being queryable. **Not run this session** — needs `python3` on `PATH` with the worker's `requirements.txt` installed (psycopg at minimum) in whatever environment runs `npm run test:integration`, which this session doesn't have; `tsc --noEmit`, ESLint and Prettier are clean on the file itself (verified this session — the only pre-existing `tsc` errors in the repo are 45 unrelated ones from an untracked `review_enterprise-billing-app/` directory, not touched by this unit).
  - Checklist items **not** covered by the automated suite, same "flagged not run" posture as rm04/rm05: item 1 (a file moving through the flow to success) and item 16 (a real `deploy_rating_flows` run reaching a real engine) both need a running Kestra engine — rm04 D9's local stack or a real deploy — neither exists yet this session.
  - Review hardening (`/code-review high` pass, follow-up to the CodeRabbit batch): (1) **`log_sweep.run_sweep` now isolates per file** — a file that fails during read (invalid UTF-8) or at INSERT (a value `rating.period_of` rejects, a check-constraint violation, a transient DB error) was previously unhandled, so it aborted the whole sweep and, never being renamed, recurred every run — silently starving `process_log` of every file sorted after it (including a crashed flow's terminal line, defeating D9). Wrapped the per-file body in a `try/except` that rolls back (via `db.transaction`), leaves the poison file un-renamed for inspection, logs to stderr (surfaced by `log-sweep.yaml`'s own handlers — a `process_log` row is unsafe when the failure may *be* the DB), and continues. (2) **`_normalize` now fails closed on a naive `log_datetime`** — an offset-less value would be localized in the DB session timezone (not guaranteed UTC), silently shifting the stored instant and mis-bucketing `partition_period`; it is now treated as malformed (quarantined), matching `logemit.line()`'s own tz-aware guard (§5.4, §7.9). (3) **Corrected the module docstring's false "in one transaction" idempotency claim** — the row inserts are one transaction but the rename to `swept/` is a separate post-commit filesystem step; documented the residual commit→rename window honestly (errs toward duplicate, never toward data loss) rather than over-promising atomicity — see the Open Questions escalation below. (4) `azure-pipelines.yml`'s `deploy_rating_flows` `kestra flow namespace update` now passes **`--no-delete`** (caveated as unconfirmed against v1.3.35, same class as the other CLI flags): the default reconcile DELETES server-side flows absent from the checkout, so a partial/sparse checkout could silently drop a live flow (e.g. `log-sweep`) with a green pipeline; and the Key-Vault-password comment now notes the argv-exposure of `--user user:password` and the stronger env-var form to confirm at the D0 spike. Added two guardrail cases to the rm06 suite (naive-datetime→quarantine, poison-file isolation). **Deliberately not changed**: (a) the per-line `INSERT ... LEFT JOIN event_catalog` is kept (not switched to `db.copy_insert`) — `COPY FROM STDIN` cannot do the per-row severity resolution the spec *prescribes* (D9 / Implementation §4) and guardrail #16 asserts the query shape of, and log volume is bounded by design (§7.4/§7.11); (b) the eight per-task `taskRunner: Process` blocks stay per-task (the spec shows them that way; a flow-level `pluginDefaults` refactor of stub tasks rm07+ will replace is speculative).
  - Review hardening (CodeRabbit batch 2): (1) **`ran-usage-rating.yaml`'s Webhook trigger now carries its REQUIRED `key`** — `io.kestra.plugin.core.trigger.Webhook` mandates a `key`; without it `kestra flow validate` fails (verification item 17). Added `key: "{{ secret('RATING_USAGE_WEBHOOK_KEY') }}"` (§3.8 — referenced, never a literal) and mirrored it in the spec's Implementation §1 example. **Confirmed against the live Kestra schema** (`io.kestra.plugin.core.trigger.Webhook`): `key` is `$required: true` and `$dynamic: true`, and is the URL-embedded secret token ("the only security mechanism to protect your endpoint ... must be considered as a secret") — distinct from the rating domain keys (`file_key`/`udr_key`) and from the request payload, which arrives as `trigger.body`/`headers`/`parameters` (rm07's real input). The `$dynamic` flag confirms the `secret()` expression is supported. **Introduces a new Kestra secret, `RATING_USAGE_WEBHOOK_KEY`, to provision** (like `kestra-basic-auth-password`). (2) **`log-sweep.yaml` gained flow-level `concurrency: limit: 1`** — the 5-minute Schedule can overlap a slow sweep, and rename-on-completion (D10) does not guard two runs reading a file before either renames it, so concurrent sweeps would double-insert. Consistent with D10's recovery model: a *crashed* sweep releases the slot so the next scheduled run still recovers it, and a truly-*hung* run (which would hold the slot) is what D10's stuck-sweep alarm covers. (3) `runtime/README.md`'s "idempotent sweep" claim was qualified to match the honest module docstring (idempotent by rename-on-completion, with the documented residual commit→rename window + per-file poison isolation). **Deliberately not changed / skipped with reason**: (a) spec Implementation §1/§2's *illustrative* YAML (`python3 /runtime/emit_terminal_log.py --outcome …`, stub `print()` handoff) — spec line 120 states task commands are illustrative, and the *actual* flow files already invoke `python3 -m runtime.emit_terminal_log --component … --workflow-execution-id …` and demonstrate the `outputs.prp.uri` handoff shape; making the stubs write real intermediate files / emit real Kestra outputs is rm07's job (D1/D3). (b) `trigger.uri` in the prp stub — an illustrative placeholder with a `?? "n/a"` fallback; the real Webhook-body/landing file input is rm07's (D3), and the fallback means it does not break validation. (c) `concurrency` on `ran-usage-rating` being "per udr_type" — correct as-is: the flow is type-specific (`ran-usage-rating` = the RAN usage flow, code-standards §8.3), so one flow per type each with `limit: 1` *is* the per-udr_type serialization architecture §5 prescribes; Kestra OSS has no keyed/partitioned concurrency and none is needed here. (d) the `kestra flow validate` / `namespace update` `docker run` invocations (redundant-`kestra`-token / `--local`) — unconfirmed against v1.3.35's image entrypoint and CLI; the whole stage is gated pending the D0 spike that verifies exactly these, so guessing an entrypoint/flag change now (§5) is deferred to that spike. (e) the durable processed-file-identity marker for the commit→rename window — still a schema/spec change out of rm06's four-table scope (§5.1, §6), already escalated in Open Questions.
  - Review hardening (`/code-review high` pass on batch 2): (1) **Provisioned the `RATING_USAGE_WEBHOOK_KEY` secret end-to-end** — the batch-2 `key` reference was dangling (referenced nowhere but the flow/spec text), so the deployed engine and the local stack could not resolve it. Wired it the same way the three existing engine secrets are: a `rating-usage-webhook-key` Key Vault secret + a `SECRET_RATING_USAGE_WEBHOOK_KEY` env var in `rating-engine-container-app.bicep` (a deliberate cross-unit touch into rm04's module — same precedent as rm05 appending `rating-engine-client-secret` there), a base64 dummy in `rating-engine/dev/.env.example`, and a provisioning note in `infra/docs/engine-access.md`. **Key correctness detail, confirmed against Kestra docs** (`docs/concepts/secret`, "Environment variables as secrets (OSS)"): Kestra OSS's env secret backend **base64-decodes** `SECRET_<NAME>`, so this KV value / env var must be BASE64-encoded — unlike the pre-existing `SECRET_RATING_RUNTIME_PASSWORD`, which the worker's `db.py` reads RAW via `os.environ` and is therefore *not* a real Kestra `secret()` (the old bicep comment claiming it was resolved via `{{ secret() }}` was corrected). This webhook key is the repo's first genuine `{{ secret() }}` use. Base64/backend behaviour against the pinned `v1.3.35` remains a D0-spike confirmation item (same class as the other kestra-config caveats). (2) **Corrected `log-sweep.yaml`'s concurrency comment** — it claimed overlapping *scheduled* sweeps were the risk, but the Schedule trigger defaults to `allowConcurrent: false` (confirmed via the live schema), so Kestra already won't start a new scheduled run while one is RUNNING. Reframed `concurrency: limit: 1` as a backstop for the paths that serialization does *not* cover — UI/API backfill and `recoverMissedSchedules` catch-up runs — where rename-on-completion still can't guard concurrent file reads. (3) Trimmed the 12-line inline `key` comment to 7 lines and pointed it at where the secret is provisioned.
  - Review hardening (CodeRabbit batch 3 — spec consistency): brought `specs/rm06-flow-template-logging-sweep.md` into line with the shipped, correct code. (1) **Severity-resolution SQL (§4)** now shows `CASE WHEN ec.event_code IS NULL THEN 'INDETERMINATE' ELSE ec.default_severity END` — matching `runtime/log_sweep.py`'s `_INSERT_SQL`. The old example selected `ec.default_severity` directly and hand-waved the INDETERMINATE mapping to a non-existent "emitter mapping" step; as literally written it would have inserted NULL (not INDETERMINATE) for an uncatalogued code, contradicting Inv #14. (2) **Idempotency (§5 + verification item 13)** now records the residual commit→rename crash window honestly: idempotent for a re-run of an already-swept file, but at-least-once across that narrow window (errs toward duplicate, never data loss), with the durable-marker fix still escalated and `code-standards.md` §7.10 kept as the target. (3) **D6** now states explicitly that "per `udr_type`" is one-flow-per-type serialization (§8.3), not keyed concurrency (Kestra OSS has none, and none is needed). **Deliberately not changed**: the spec's Implementation §1 illustrative YAML (file-path `emit_terminal_log.py` handler invocation; stub `print()` handoff) — spec line 120 states those task commands are illustrative and the *actual* flow files already use the module form with `--component`/`--workflow-execution-id` and the `outputs.prp.uri` handoff shape; fixing fragments of a deliberately-simplified whole-flow example would duplicate the real flow into the spec and drift, and building real file-URI outputs is rm07's job (D1/D3).

- rm05 — Entra reverse proxy / Container Apps Easy Auth (`specs/rm05-entra-reverse-proxy.md`). Delivered:
  - `infra/bicep/modules/rating-engine-container-app.bicep` — extended (not a new file, per D2 "rm05 owns the ingress block from here"): `enableEasyAuthIngress`/`corporateIpAllowList` params flip ingress from rm04's internal-only/disabled default to external with `ipSecurityRestrictions` built from the allow-list (D2/D6), and a 4th Key Vault secret ref (`rating-engine-client-secret`) is appended to the `secrets` array only when `enableEasyAuthIngress` is true — so an rm04-only deploy never needs that KV entry to exist.
  - `infra/bicep/modules/easy-auth.bicep` (new) — the `Microsoft.App/containerApps/authConfigs` resource (D1/D3/D4/D5: platform enabled, `RedirectToLoginPage`, Entra registration + client secret setting name + allowed-audience validation), the Container App diagnostic setting (console + system logs), and the three `Microsoft.OperationalInsights/workspaces/tables` retention resources (`SigninLogs`, `ContainerAppConsoleLogs`, `ContainerAppSystemLogs` → 2555 days total retention, D7). Its `corporateIpAllowList` param is required + `@minLength(1)` — deployed in the same pass as the ingress change above, so an empty allow-list fails the whole deployment (verification item 14) even though the ingress module's own copy of the param has no such constraint (ownership split explained inline).
  - `infra/bicep/modules/entra-signin-diagnostics.bicep` (new) — `targetScope = 'tenant'`, exports Entra `SignInLogs` to the shared Log Analytics workspace. Deliberately **not** wired into `main.bicep`/the pipeline — tenant-scoped deployment needs a tenant-level role, a different privilege class than the resource-group Contributor the rest of this repo's Bicep assumes; documented as a manual `az deployment tenant create` step instead (mirrors Implementation §1/§2 treating the app registration/group as Entra-admin, non-Bicep actions).
  - `infra/bicep/main.bicep` — `deployEasyAuth` bool param (default `false`, same D0-style gate precedent as `deployRatingEngine`), `corporateIpAllowList` and `ratingEngineEntraClientId` params, and the `easyAuth` module call. `easyAuth` references `ratingEngineContainerApp`'s output, so `deployEasyAuth=true` with `deployRatingEngine=false` fails the deployment rather than silently deploying nothing — encodes rm05's declared dependency on rm04 as a real Bicep failure, not just a doc note.
  - `infra/docs/engine-access.md` (new) — the provisioning order (Entra app registration → Workflow.Admin role → Billing Ops group → assignment-required → client secret to Key Vault → apply `easy-auth.bicep` → Entra tenant sign-in-log export) matching Implementation §7, the Storage-Account export alternative note (D7), and the accepted-risk record (D10) — **written with an explicit "outstanding" sign-off placeholder**, since no actual approver/date was obtained in this session; the doc records the mechanism, not a real sign-off.
  - `infra/azure-pipelines.yml` — `containerize_rating_engine` stage's lint step extended to `az bicep build` both new files (matches the existing "fail the build on an `az bicep build` error" pattern).
  - **Spec-vs-ARM-schema deviation, recorded in `engine-access.md`:** the spec's Implementation §3 lists `globalValidation.requireAuthentication: true`. That property belongs to App Service's Easy Auth schema, not Container Apps' `authConfigs` API — `az bicep build` rejects it against both the 2023-05-01 and 2024-03-01 type definitions available locally (checked, not assumed). Omitted; `unauthenticatedClientAction: 'RedirectToLoginPage'` is the only mechanism this API exposes for the same intent (verification item 3 is unaffected — it only asserts the redirect behavior, not this specific property).
  - Verified: all five touched/new `.bicep` files build clean via `az bicep build` (one pre-existing-pattern `BCP081` warning on the diagnostic-settings resource — "no local types available," does not block deployment, left unsuppressed since it's informational only). **Not verified**: nothing requiring a live Azure subscription, a real Entra tenant, or the Entra admin actions in Implementation §1/§2 — none of the 14 verification checklist items have been run. No Entra app registration, app role, Billing Ops group, or client secret exist yet; `deployEasyAuth` defaults `false` so this cannot be deployed by accident, mirroring rm04's `deployRatingEngine` gate.
  - Review hardening (CodeRabbit pass, batch 2): (1) `easy-auth.bicep`'s `containerAppDiagnostics` incorrectly put `ContainerAppConsoleLogs`/`ContainerAppSystemLogs` on the containerApps-scoped diagnostic setting — per Microsoft Learn (Container Apps log-options), those log categories are only valid on the `managedEnvironments` diagnostic setting; the containerApps scope only supports the `AllMetrics` metric category (`--logs` is unsupported there). Split into the app-scoped setting (now `AllMetrics` only) and a new `environmentDiagnostics` setting scoped to a new `containerAppsEnvironmentId` param's `existing` managed-environment resource, carrying the two log categories; `main.bicep` now passes `containerAppsEnvironment.id` through. (2) Resolved the rm04 "deliberately not changed" note (line 37 above): now that `rating-engine/worker/runtime/db.py` (`_dsn()`) is landed and demonstrably treats `SECRET_RATING_RUNTIME_PASSWORD` as a bare password (built into a DSN alongside separate `RATING_DB_HOST`/`PORT`/`NAME`/`USER` env vars), the KV secret backing it was still misleadingly named `pg-connection-string-rating-runtime` — indistinguishable from `pg-connection-string-app`/`-migrate`, which genuinely hold full `DATABASE_URL` values. Renamed to `rating-runtime-db-password` (mirrors `kestra-engine-db-password`'s naming for the same bare-password shape) in `rating-engine-container-app.bicep`; the `SECRET_RATING_RUNTIME_PASSWORD` env var name and secretRef lookup are unchanged. **Skipped as out of scope**: a suggested deployment-time preflight validating the rating-engine service principal's `appRoleAssignmentRequired` flag and the Billing Ops group's app-role assignment via Microsoft Graph — this re-opens the already-documented D3/D4/D5 decision (Implementation §1/§2, `engine-access.md`) that Entra app registration/role/group state is a manual admin action outside Bicep's scope, and would need a Graph-permissioned deployment identity that doesn't exist in this repo; not undertaken speculatively.

- rm04 — Kestra deployment and local dev environment (`specs/rm04-kestra-deployment-and-local-dev.md`). **Architecture deviation from the spec's literal repo boundary — see Architecture Decisions: everything lives in `enterprise-billing-app`, not a separate rating repo.** Delivered:
  - `rating-engine/worker/Dockerfile` — Kestra base pinned BY REAL DIGEST (D2): `kestra/kestra:v1.3.35@sha256:0b880e20e403ef635a6bc7e9e72786c7a6dde143858723449dc57500830868e2` (current stable head of the 1.3 line, the WITH-plugins variant so the Azure Blob storage plugin `kestra.storage.type: azure` needs is present; digest resolved via `docker buildx imagetools inspect` on 2026-08-27). The base is Ubuntu 24.04 and already ships CPython 3.12 in a uv-managed venv at `/app/.venv` (+ `uv` at `/usr/bin/uv`), so there is no apt/pip bootstrap — the rating libs install straight into that venv via `uv pip install --require-hashes`. **Built and validated end-to-end this session** (`docker build` clean; `polars`/`pyarrow`/`psycopg`/`azure.identity`/`azure.storage.blob` all import on Python 3.12.3).
  - `rating-engine/worker/requirements.in` + `requirements.txt` — top-level D3 pins live in `requirements.in`; `requirements.txt` is the **fully `uv pip compile --generate-hashes`'d lock** (21 packages incl. every transitive dep — cryptography, msal, azure-core, cffi, urllib3, …), resolved against the exact runtime base (Ubuntu 24.04 / linux / cp312) so the hashes match the wheels that actually install. Top-level versions unchanged (`psycopg[binary]` 3.3.4, `polars` 1.44.1, `pyarrow` 25.0.1, `azure-storage-blob` 12.30.0, `azure-identity` 1.25.3). The `docker build` above proves `--require-hashes` is satisfied.
  - `rating-engine/kestra/kestra.yml` — datasource → `kestra` DB via `kestra_engine`, storage → Azure Blob (`kestra-internal` container, MI auth in the deployed path / Azurite connection-string locally), Basic Auth, default namespace `rating` (D7). Property names follow Kestra OSS's documented schema but are **unconfirmed against a real engine** — flagged inline for the D0 spike to verify, including whether the pinned Azure Blob plugin actually supports Managed Identity auth (if not, D5's "four credentials" becomes five).
  - `infra/bicep/modules/rating-engine-storage.bicep` — Files share `landing`, Blob containers `archive`/`error`/`logs`/`kestra-internal`, lifecycle rules (archive 7yr / error+logs 24mo, D4), `Storage Blob Data Contributor` + `Storage File Data SMB Share Contributor` role assignments for the engine MI. `allowSharedKeyAccess` left at its default (true) — deliberately, since ACA's Files volume mount has no MI option and needs the account key at the platform level only (commented; Blob access stays MI-only).
  - `infra/bicep/modules/rating-engine-container-app.bicep` — the `rating-engine` Container App: `activeRevisionsMode: 'Single'`, min/max replicas 1 (D2), ingress disabled by default / internal-only opt-in (D8), 3 Key Vault secret refs (Basic Auth password, the `rating_runtime` credential, and `kestra-engine-db-password` — a **dedicated `kestra_engine` password**, not a connection string, since `kestra.yml` sets the datasource URL/username separately; the 4th D5 credential, internal storage, is MI-only per the storage module), `RATING_ENGINE_VERSION` env var (D6), Azure Files volume mount for `landing/`, health probes.
  - `infra/bicep/modules/acr.bicep` + `modules/key-vault.bicep` — extended (backward-compatibly, empty-string-default param) with a third Managed Identity's role assignment (`AcrPull` / `Key Vault Secrets User`) for the rating engine, alongside the existing app + migrate identities.
  - `infra/bicep/main.bicep` — a dedicated `ratingEngineManagedIdentity` (created unconditionally, like the app/migrate identities) and a **`deployRatingEngine` bool param defaulting to `false`** — the storage + Container App modules are gated behind it, so **D0's "do not build the rest of this unit until the spike passes" is encoded as an actual deploy gate**, not just a doc note: the Bicep is authored and `az bicep build`-clean but cannot be deployed by a normal `true`-default apply.
  - `rating-engine/dev/docker-compose.dev.yml` + `.env.example` + `landing/archive/error/logs/` (`.gitkeep` + a `README.md` each, no fabricated feed-format fixture — the feed format is still Open item 1) — D9's local stack. Run as a second `-f` file alongside the root `docker-compose.dev.yml` (`docker compose -f docker-compose.dev.yml -f rating-engine/dev/docker-compose.dev.yml up`) so it's the **same** Compose project/network/`db` container, not a duplicate. Azurite backs Kestra's internal storage only (code-standards §3.4, "no local carve-out"); the four named locations are plain bind mounts locally. **No Docker socket mount** (D9, verification item 23). Not yet run — `docker compose config`/`up` needs a Docker daemon this session doesn't have.
  - `infra/azure-pipelines.yml` — new `containerize_rating_engine` stage (parallel to `containerize`): fails the build on an unhashed `requirements.txt` line or a `az bicep build` error, then builds/pushes the worker image to the shared ACR. Does **not** apply Bicep or deploy — no stage in this pipeline applies `main.bicep` for the app either (existing manual/out-of-band convention), and `deployRatingEngine` defaults false regardless.
  - Verified: `tsc --noEmit`, ESLint, Prettier clean on the new TS file; all five touched/new `.bicep` files build clean via `az bicep build` (no linter suppressions — the storage account key is no longer exported; see Review hardening below); `azure-pipelines.yml`, the new compose file, and `kestra.yml` all parse as valid YAML. **Not verified**: nothing requiring a Docker daemon, a live Azure subscription, or a real Kestra engine — that's the entire D0 spike (verification items 1–4) plus most of items 5–26. See Open Questions for the full unresolved list.
  - Review hardening (CodeRabbit pass): (1) `kestra_engine` role now declares explicit `NOSUPERUSER NOCREATEDB NOCREATEROLE NOREPLICATION NOBYPASSRLS` on both the CREATE and `ELSE ALTER` branches (matches rm03's `rating_runtime` hardening — load-bearing on re-run if the role drifted). (2) The ACR/Key-Vault role assignments for the engine MI are now gated on `deployRatingEngine` (empty principalId → the modules' existing `if (!empty(...))` skips the grant), so a `deployRatingEngine: false` apply leaves no unused grant on the shared registry/vault. (3) The datasource password secret was renamed `pg-connection-string-kestra-engine` → `kestra-engine-db-password` to reflect that `KESTRA_DATASOURCES_POSTGRES_PASSWORD` takes a bare password, not a connection string. (4) The internal-ingress branch dropped its `traffic` weights (invalid under `activeRevisionsMode: 'Single'`). (5) The storage module no longer exports the account key — it exposes `storageAccountId`, and the container-app module resolves the Files-mount key inline via an `existing` reference + `listKeys()` (the codebase's own pattern; removes the secret-in-output linter suppression). (6) The local compose `ALTER ROLE … PASSWORD` now escapes Compose interpolation (`$${KESTRA_DATASOURCES_POSTGRES_PASSWORD}`) so the value expands inside the container from `env_file`, not to empty at parse time. **Deliberately not changed**: the Blob retention defaults stay 2555/730 days (D4's stated 7yr/24mo — leap-year padding to 2557/731 would contradict the spec); the Dockerfile Kestra base stayed a `<KESTRA_VERSION>@sha256:<digest>` placeholder at the time of that pass (no registry access then; **since resolved** — see the version-lock note below).
  - Review hardening (`/code-review high` pass): (1) The `containerize_rating_engine` pipeline stage is now gated by a `buildRatingEngine` template parameter (default false) — automatic CI/PR runs skip it, so the placeholder-base Dockerfile no longer turns every build red; queue with `buildRatingEngine=true` after the D0 spike. Mirrors the `deployRatingEngine` gate. (2) The Container App health probes now target port **8081** (Kestra's Micronaut management port), not 8080 — probing the webserver port returned 404 so the revision could never become Healthy; the management port is unauthenticated so Basic Auth does not block them (a D0 note flags verifying the port and the 0.22+ split `/health/liveness`+`/health/readiness` against the pinned release). (3) The Dockerfile drops `--no-deps` from `pip install --require-hashes` — `--no-deps` would install only the five top-level pins and silently ship an image whose `import azure.identity` breaks at runtime; without it, an un-`pip-compile`d requirements.txt fails the build loudly (matching the file's own stated contract). (4) The Python runtime installs into a dedicated venv so pip targets the intended interpreter, not the base image's system python. *(Superseded by the version-lock pass below: the pinned base already ships CPython 3.12 in a uv-managed venv, so install is now `uv pip install` into `/app/.venv` — no apt/venv bootstrap step.)* (5) The local dev DB password collapsed to a single `KESTRA_DATASOURCES_POSTGRES_PASSWORD` env-file variable used by both the kestra-setup ALTER ROLE and the engine's datasource — previously the role password (env_file) and the engine's connect password (Compose interpolation from host/root `.env`) read from different sources and could diverge on a partial override. (6) The `.env.example` header no longer claims a `.env` copy overrides values (both services' `env_file` read `.env.example` directly). **Deliberately not changed**: `SECRET_RATING_RUNTIME_PASSWORD` still resolves the `pg-connection-string-rating-runtime` KV secret — unlike the kestra datasource (separate URL/username env vars ⇒ must be a bare password), the rating_runtime secret feeds rm06+ flows that construct their own connections, where a self-contained DSN may be the right shape; deferred to rm06 rather than changed speculatively.
  - Version lock (a session WITH Docker + registry + PyPI access, unlike the authoring session): resolved and pinned the Kestra base to `kestra/kestra:v1.3.35@sha256:0b880e…` (current stable, with-plugins), rewrote the Dockerfile to install into Kestra's own uv venv, and replaced `requirements.txt` with a full `uv pip compile --generate-hashes` lock (`requirements.in` is now the input). **`docker build` succeeds and every runtime lib imports on py3.12.3** — this clears the Dockerfile/requirements halves of D0 verification item 25 and the "image builds" risk (the process-runner-on-ACA behaviour is still unproven — that remains the D0 spike). The pipeline's requirements-hash lint was updated to an `awk` block that understands uv's multi-line hash format, and the `containerize_rating_engine` stage's `condition` now also requires `succeeded()` so a failed `test_scan` blocks the build.
  - Runtime foundation (`rating-engine/worker/runtime/`) — **format-agnostic plumbing only**, the reusable building blocks the `prp`/`rp`/`rl` components (§3.5) will call from Kestra Python tasks; explicitly NOT rating logic (§8.1) and NOT the usage-feed parser (feed format is Open item 1, undecided). `db.py` (psycopg connect as `rating_runtime` via `SECRET_RATING_RUNTIME_PASSWORD` §3.8, `fetch`/`execute`, `transaction` for RL's one atomic unit Inv #8, `copy_insert` bulk-not-per-record §3.2, money stays `Decimal` §5.9); `storage.py` (file I/O over landing/archive/error/logs, polars fixture readers — real parser is rm07's PRP); `transform.py` (polars `join`/`correlate`/`unmatched`, whole-file/chunk §3.2, fail-loud on missing key); `logemit.py` (JSON-Lines lines matching the `process_log` contract §7.9 exactly — `partition_period` deliberately absent, severity NOT resolved here that's rm06's sweep §7.2). Every deferred-logic spot carries a `# STUB:` naming its owning unit; the package is version-controlled but NOT yet `COPY`'d into the image (rm04 = interpreter + libs only; the COPY lands with rm06/rm07). **Validated this session**: built into the worker image and run against a throwaway Postgres — DB bulk round-trip (money round-trips as `Decimal`), correlate/anti-join + missing-key guard, fixture read, and the JSON-Lines emitter round-tripping quotes/newlines/delimiters with the correlation-set and log-level guards firing (exit 0).
  - Review hardening (`/code-review high` on the runtime package): (1) `logemit.to_json` now passes `default=_json_default` so a `Decimal`/`datetime` in `additional_info` (§7.7 routes amounts there; money is always `Decimal`) serialises to a string instead of raising `TypeError` mid-file and corrupting the `.jsonl` the rm06 sweep ingests. (2) `transform.correlate` now raises if a `keep` column already exists in the primary frame — a left join would otherwise rename the reference column `<col>_right` and silently keep the primary value. (3) `runtime/__init__.py` imports its submodules so `import runtime; runtime.db` works (was `AttributeError`; only `from runtime import db` worked before). (4) `storage.move` uses `shutil.move` (copy+delete fallback) instead of `os.rename`, surviving the cross-filesystem case (landing SMB vs other mounts, `EXDEV`). (5) `storage` exposes a `location(name)` function that reads `RATING_<NAME>_DIR` on call, replacing module-level constants frozen at import (long-lived workers / test overrides). (6) `db.connect`/`execute`/`copy_insert` docstrings now state the commit contract explicitly — the helpers deliberately do NOT commit so RL's guard+supersede+insert compose into one transaction (§3.6, Inv #8); `execute` returns psycopg's `rowcount` (`-1` for DDL). (7) `requirements.txt` recompiled from the committed `requirements.in` (provenance annotations now reference `requirements.in`, not the throwaway `/tmp/req.in`). All re-validated by a rebuild + expanded smoke against Postgres (exit 0). **Kept intentionally** (not removed per the altitude finding): `correlate`/`unmatched` — they are the correlation capability this foundation was asked to provide, now hardened.

- rm03a — `kestra` database + `kestra_engine` role (rm03-spec §Implementation Step 9a; rm04's Depends-on). Not its own spec file — `rm00-build-plan.md`, which both rm03's spec and this tracker cite as rm03a's source, is not present in this repo (same gap noted for other units' plan-doc references). Built from the one paragraph in rm03-spec §Implementation Step 9a plus the D5/D7 tables in `ratemgmt-code-standards.md` and `ratemgmt-architecture.md`. Delivered:
  - `db/bootstrap/kestra-db-roles.sql` — creates the `kestra` database (a bare `CREATE DATABASE`, since it cannot run inside a transaction block/`DO $$` — idempotency is handled in the runner instead), the `kestra_engine` login role (`CONNECTION LIMIT 20` — **placeholder**, no number is stated anywhere in the docs; mirrors `rating_runtime`'s limit pending an operator decision sized to the engine's real replica/worker count), and the mirror-image `REVOKE CONNECT ON DATABASE "kestra" FROM PUBLIC` + explicit `GRANT CONNECT, CREATE TO kestra_engine` (Inv #18's reverse direction — `rating_runtime`/`app_runtime`/`app_migrate` never get an explicit grant, so none can reach `kestra`). Must run **after** `db:bootstrap-rating-roles`'s own `REVOKE CONNECT … FROM PUBLIC` on the billing database.
  - `db/bootstrap/kestra-db-roles.ts` — the `npm run db:bootstrap-kestra-roles` runner, structurally a copy of `rating-db-roles.ts` with one addition: it catches Postgres error `42P04` (duplicate_database) around the `CREATE DATABASE` statement so a re-run is a no-op instead of a crash.
  - `package.json` — added `db:bootstrap-kestra-roles` (not wired into `db:setup` — manual/superuser-only step, same as `db:bootstrap-roles`/`db:bootstrap-rating-roles`).
  - `infra/docs/db-role-verification.md` — extended: provisioning-order step 3a, the `ALTER ROLE kestra_engine PASSWORD` follow-up (never committed), and a new "Verification SQL — the kestra database boundary" section with `has_database_privilege` assertions for all four roles.
  - Verified: `tsc --noEmit`, ESLint, Prettier clean. **Not run against a live database** — no automated test suite ships for rm03a in this pass (the file-org table in `ratemgmt-code-standards.md` §8 places rm03a's assertions inside the existing `tests/rating/grants.integration.test.ts`, not a new file; extending that suite was out of scope for what was asked — flagged in Open Questions).

- rm03 — `rating_runtime` role, grants and the billing boundary (`specs/rm03-rating-runtime-role-grants.md`). Delivered:
  - `db/bootstrap/rating-db-roles.sql` — new bootstrap script (NOT a Drizzle migration; needs `CREATEROLE`, D11). All eleven spec steps: role with `CONNECTION LIMIT 20` (idempotent, `ELSE ALTER` converges the limit), the D6 `REVOKE CONNECT … FROM PUBLIC` + explicit re-grant, schema `USAGE`, column-scoped `rating` table grants on the **parents only** (D3), the six-column `app_runtime` boundary (D1), the D8 sequence/function `EXECUTE`/`USAGE` grants, enumerated cross-schema `SELECT` (D9), the redundant-by-design `billing` write revoke (Inv #1), the D7 `REVOKE EXECUTE … FROM PUBLIC` on the four `SECURITY DEFINER` pgledger functions, `SELECT`-only default privileges (D5), and `app_migrate` schema ownership. Statement-breakpoint markers so the runner and `psql` can both apply it.
  - `db/bootstrap/rating-db-roles.ts` — the `npm run db:bootstrap-rating-roles` runner (copy of `bootstrap-db-roles.ts`), reading `BOOTSTRAP_DATABASE_URL`. Registered in `package.json`.
  - `infra/docs/db-role-verification.md` — extended the provisioning order with step 3 (`db:bootstrap-rating-roles`, after `db:bootstrap-roles`), a "Platform changes" note recording the two revokes (D6 `PUBLIC` `CONNECT`, D7 `PUBLIC` `EXECUTE` — escalations E1/E2) so an operator learns what changed database-wide, and the manual `ALTER ROLE rating_runtime PASSWORD` follow-up (never committed).
  - `tests/rating/grants.integration.test.ts` — live-DB assertion suite, a **connection per role**, asserting both the ACL (`has_*_privilege`) and the real statement it governs. Covers the pg_attribute enumeration (exactly six `app_runtime`-updatable + only `status` for `rating_runtime`), both-direction column refusals, `is_live` (D4), partition-direct refusal + zero child ACLs (Inv #17a), the D8 insert prerequisites, the billing boundary incl. the D7 `permission denied for function` + the standing "no `PUBLIC`-executable `SECURITY DEFINER`" assertion, the CONNECT boundary (D6, no-grant probe refused / three roles admitted / `rolconnlimit = 20`), the D5 default-privilege posture incl. the column-scoped-`ALTER DEFAULT PRIVILEGES` rejection, and idempotency (re-run → identical enumeration, limit still 20).
  - Verified: `tsc --noEmit`, ESLint, Prettier clean; the suite runs **32/32 green** against the disposable test Postgres (`docker-compose.test.yml`, port 5434), never the dev stack. Caught and fixed a self-inflicted bug during verification — a literal `--> statement-breakpoint` in the SQL header comment split the header mid-sentence (precedent avoids the arrow form in prose); reworded to match.
  - Review hardening: Step 1 now sets an explicit `NOSUPERUSER NOCREATEROLE NOCREATEDB NOREPLICATION NOBYPASSRLS` on **both** branches. Redundant on CREATE (these are the defaults, kept as declaration of intent) but load-bearing on the `ELSE ALTER` branch — a pre-existing `rating_runtime` that drifted to `SUPERUSER` would bypass every ACL this unit builds (D6: superusers bypass the ACL entirely), so re-running must converge the attribute set, not just the connection limit. Added grants-suite item 37 (elevate the role, re-run, assert all five attributes stripped + LOGIN/limit preserved).



- rm02 — `event_catalog` seed (`specs/rm02-event-catalog-seed.md`). Delivered:
  - `db/seeds/rating-event-catalog.data.ts` — the sixteen catalog rows, the `RATING_EVENT_CODES` typed constant + `RatingEventCode` type, and the reusable `seedEventCatalog(db)` upsert. Side-effect-free so the verification suite imports it without connecting (the seed-admin.ts / seed-admin.config.ts split precedent). Idempotent as `ON CONFLICT (event_code) DO UPDATE` — not `DO NOTHING` — so a re-run carries a severity re-tune (including back to NULL) to an existing environment; never `DELETE`s or deactivates a code absent from the list.
  - `db/seeds/rating-event-catalog.ts` — the standalone `npm run db:seed-rating` runner wrapping `seedEventCatalog`.
  - `package.json` — added `db:seed-rating`, appended to the `db:setup` chain after `db:seed-billing`.
  - `tests/rating/rm02-event-catalog-seed.integration.test.ts` — live-DB verification suite, items 1–20 + 23 (build-hygiene items 21/22 are the tsc/lint/prettier + empty-DB-seed run, exercised outside the suite; 22 is also covered by the suite's `beforeAll` seeding green from a freshly migrated DB). Includes the §A1 pair (13+14 in one test) that a `COALESCE(default_severity,'INDETERMINATE')` implementation would collapse.
  - The rm01 amendment in §A (`default_severity` nullable + `event_catalog_default_severity_check`) was already shipped by rm01 (see rm01 Open Questions / rm02 O1), so no migration change was needed here — rm02 builds against it and asserts it (items 1–2).
  - Verified: `tsc --noEmit`, ESLint, Prettier all clean; the rm02 integration suite runs 20/20 green against the disposable test Postgres (`docker-compose.test.yml`, port 5434), never the dev stack.

- rm01 — `rating` schema foundation (`specs/rm01-rating-schema-foundation.md`). Delivered:
  - `db/migrations/0034_rating.sql` — hand-authored DDL: `rating` schema, `rating.period_of()`, `udr_batch_seq`, `udr_rated`/`udr_batch`/`process_log`/`event_catalog` with every constraint and index the spec lists, both `*_default` bootstrap partitions.
  - `db/schema/rating/{pg-schema,udr-rated,udr-batch,process-log,event-catalog,index}.ts` — Drizzle typing-only declarations; wired into `db/schema/index.ts`.
  - `validation/rating/udr-rate-detail.schema.ts` — discriminated union on `udr_rate_type`, FLAT only (v1 scope).
  - `db/bootstrap/rating-partman-setup.{sql,ts}` — pg_partman registration (`udr_rated` 7yr / `process_log` 24mo, both DETACH), no second cron job. Retrofitted the pg_partman-v5 preflight assertion into `audit-partman-setup.sql` and `billing-partman-setup.sql` per spec §Implementation §6.
  - `package.json` — added `db:setup-partman-rating`, wired into `db:setup`. `db:setup` now runs `db:setup-partman` (the shared audit-log/pg_partman bootstrap) before `db:setup-partman-billing` and `db:setup-partman-rating`, so a fresh deployment provisions the `audit-log-partman-maintenance` job the billing and rating bootstraps depend on for their maintenance sweep.
  - `tests/rating/rm01-schema.integration.test.ts` — live-DB verification suite (items 1–24, 28). Named `.integration.test.ts` (not the spec's literal `rm01-schema.test.ts`) so it's actually picked up by `vitest.integration.config.ts`'s include glob rather than the DB-free default project — see Open Questions.
  - Fixed a legitimate ship-gate casualty: `tests/services/billing/collect-claim.test.ts`'s bm13 v1-placeholder guard asserted **no** `rating` export existed anywhere in `db/schema` — true only until a real rating schema shipped, and its own comment said as much. Narrowed the assertion to what must still hold on the billing side (collect-claim.ts imports nothing from `db/schema/rating`; no sanctioned rating-schema writer file exists yet).
  - Verified: `tsc --noEmit`, `npm run lint`, `prettier --check` all clean; full non-DB `vitest run` clean (2758/2758, after the collect-claim fix — a handful of unrelated action-test failures are pre-existing stale-hardcoded-date flakiness, confirmed unrelated by re-running in isolation). **Not yet run:** the new `tests/rating/rm01-schema.integration.test.ts` itself, and `tests/db/billing-partman-setup.integration.test.ts` post-retrofit — both need a disposable test Postgres, never the dev stack's `DATABASE_URL` (repo memory: `local-docker-dev-stack`).

## In Progress

- rm11 is file-complete and build-hygiene-clean (see Completed) — the new `stranded-batch-reconcile.yaml` flow + `stranded_reconcile.py` runtime module are authored, the coordinated `BATCH_STRANDED` catalog addition (rm02 spec + seed + `RATING_EVENT_CODES`) is in place, and the new guardrail suite's static half passes. **`py_compile` was NOT run this session** — no `python3` on this session's `PATH` (same as rm10's session) — so `stranded_reconcile.py` is verified by careful manual review only; flagged in Open Questions-adjacent Next Up below. The DB-gated black-box suite (`tests/rating/rm11-stranded-batch-recovery.integration.test.ts`) has not run against a live test Postgres, and the flow has not been validated/deployed against a live Kestra engine (so D7's "on flow start" half — see the Open Questions entry on the `backfill` mechanism — is unverified for real).
- rm10 is file-complete and build-hygiene-clean (see Completed) — the `# STUB: rm10` supersede hook is filled with the real batch-level supersession (predicate, lineage, cross-period + shrinking-reissue detection). `tsc`/ESLint/Prettier are green on the TS side; `rl.py` itself was **not** run through `py_compile` this session (no `python3` on `PATH`) — reviewed by hand instead. Its new DB-gated black-box suite (`tests/rating/rm10-supersession-reprocessing.integration.test.ts`) has not run against a live test Postgres, and the full `prp → rp → rl` reissue chain has not been exercised end-to-end against a live Kestra engine. Same posture as rm04-rm09.
- rm09 is file-complete and build-hygiene-clean (see Completed) — the `rl` stub is replaced with the guarded transactional loader (guard + currency assertion + supersede hook, now filled by rm10 + `COPY` insert in one transaction, reconciliation, archive-after-commit), the static suite passes (3/7), and the pure decision logic is harness-verified (23/23). Its DB-gated black-box suite (`tests/rating/rm09-rl-guarded-transactional-load.integration.test.ts`) has not run against a live test Postgres (needs `psycopg`+`polars` on `python3` and a disposable PG), and the full `prp → rp → rl` chain has not been exercised end-to-end against a live Kestra engine. Same posture as rm04-rm08.
- rm08 is file-complete and build-hygiene-clean (see Completed) — the `rp` stub is replaced, the as-of resolution + snapshot + `FLAT` calc + `LOOKUP_MISS` are authored, and the static flow-wiring suite passes. Its DB-gated black-box suite (`tests/rating/rm08-rp-price-resolution-snapshot.integration.test.ts`) has not run against a live test Postgres (needs `psycopg`+`polars` on `python3` and a disposable PG), and the real `prp → rp` chunk handoff has not been exercised against a live Kestra engine. Same posture as rm04-rm07.
- rm07 is file-complete and build-hygiene-clean (see Completed), but its DB-gated black-box suite (`tests/rating/rm07-prp-claim-validate-reject.integration.test.ts`) has not run against a live test Postgres, and the new `landing/` file trigger + the real `prp` task have not been validated/deployed against a live Kestra engine. rm06 is file-complete on its TS half but its Python sweep has not run against a live database, and the flow YAML has not been validated/deployed against a live engine. rm05 is file-complete but not deployed; no Entra objects provisioned; accepted-risk sign-off (D10) outstanding. rm03a and rm04 remain file-complete, not run against live infrastructure (rm04 gated by `deployRatingEngine = false`).

## Next Up

- rm06: run `tests/rating/rm06-flow-template-logging-sweep.integration.test.ts`'s DB-gated suite against a disposable test Postgres with `python3` + the worker's `requirements.txt` installed (currently skipped everywhere this session ran). Once rm04's local stack (`docker compose -f docker-compose.dev.yml -f rating-engine/dev/docker-compose.dev.yml up`) is actually run end to end, also exercise checklist item 1 (trigger `ran-usage-rating` manually, confirm it reaches success through all three stubs) and `kestra flow validate ./rating-engine/flows` for real, against the pinned `v1.3.35` release.
- rm06: resolve the three flagged gaps in `deploy_rating_flows` (`infra/azure-pipelines.yml` / `infra/docs/engine-access.md`) before ever setting `deployRatingFlows=true` — network reachability from the hosted ADO agent to the Kestra webserver, the pipeline service connection's Key Vault read access, and the `kestra` CLI's actual flag names against the real release.
- rm07: run `tests/rating/rm07-prp-claim-validate-reject.integration.test.ts`'s DB-gated suite against a disposable test Postgres with `python3` + the worker's `requirements.txt` (`psycopg`+`polars`) installed (skipped this session). Then, once rm04's local stack is up, exercise the headline case (the 50,000-record fixture with 37 bad rows → `PARTIAL`, reject file, 49,963 carried) and confirm the `landing/` file trigger actually fires on the pinned `v1.3.35` engine (the `io.kestra.plugin.fs.local.Trigger` plugin id / `{{ kv(...) }}` chunk-size read are D0-spike confirmation items — see Open Questions).
- rm08: run `tests/rating/rm08-rp-price-resolution-snapshot.integration.test.ts`'s DB-gated suite against a disposable test Postgres with `python3` + the worker's `requirements.txt` (`psycopg`+`polars`) installed (skipped this session). Then, once rm04's local stack is up, exercise the real `prp → rp` handoff end to end on the pinned `v1.3.35` engine and confirm the `subscriber_ref_column` build-time mapping against a feed that actually carries a subscriber dimension (the RAN_USAGE sample's is ASSUMED `PUBLIC_KEY`). Also decide whether `emit_terminal_log.py`'s generic `STUB_EXECUTION_*` codes become real per-component catalogued codes once real failure paths exist (rm06 Open Questions).
- rm09: run `tests/rating/rm09-rl-guarded-transactional-load.integration.test.ts`'s DB-gated suite against a disposable test Postgres with `python3` + the worker's `requirements.txt` (`psycopg`+`polars`) installed (skipped this session). Then, once rm04's local stack is up, exercise the full `prp → rp → rl` chain end to end on the pinned `v1.3.35` engine — a clean file reaching `COMPLETE` with the raw file in `archive/`, and a killed-mid-transaction run leaving the file in `landing/` with zero rows (verification items 7-9).
- rm10: **first**, get `python3` + the worker's `requirements.txt` (`psycopg`+`polars`) on `PATH` and run `py_compile` on `rl.py` — this session had no `python3` available, so the rm10 changes were reviewed by hand only, unlike every other Python unit shipped so far. Then run `tests/rating/rm10-supersession-reprocessing.integration.test.ts`'s DB-gated suite against a disposable test Postgres (skipped this session). Then, once rm04's local stack is up, exercise a real reissue end to end on the pinned `v1.3.35` engine — a corrected file superseding its predecessor, and a shrinking reissue actually raising the `SHRINKING_REISSUE` alarm on a live engine (verification items 1-9).
- rm11: **first**, get `python3` + the worker's `requirements.txt` (`psycopg`+`polars`) on `PATH` and run `py_compile` on `stranded_reconcile.py` — this session had none available, so it was reviewed by hand only. Then run `tests/rating/rm11-stranded-batch-recovery.integration.test.ts`'s DB-gated suite against a disposable test Postgres. Then, once rm04's local stack is up, exercise a real strand end to end on the pinned `v1.3.35` engine — kill a worker mid-`rl` task, confirm the reconcile (both on scheduler restart and on its next scheduled tick) resolves the stranded `udr_batch` row and the file reprocesses (verification items 1-8), and separately confirm or refute the Open Questions entry on `is_duplicate_redelivery` for a byte-identical (not just corrected) redelivery.
- General clearing for RL-raised alarms (follow-up to rm12, see the rm12 Open Questions entry below): rm12 shipped, but it clears only the two codes it itself raises (`FILE_NOT_RECEIVED`/`FILE_LATE`). The auto-clearing codes RL raises and names `BATCH_COMPLETE` as their clearer (`PARSE_FAILURE`, `LOOKUP_MISS`, `BATCH_PARTIAL`, `TASK_RETRY_OK`, `DB_WRITE_FAILURE`, `BATCH_STRANDED`) still have no code anywhere that emits their `CLEARED` row — rm09's `BATCH_COMPLETE` emission does not look up and clear a prior open alarm on the same `file_key`/`batch_id`. Not yet scoped to a unit.
- Run `tests/rating/rm01-schema.integration.test.ts` (and re-run `tests/db/billing-partman-setup.integration.test.ts` / `tests/db/audit-*` integration suites, since their `.sql` files were retrofitted) against a disposable test Postgres — not the dev stack.
- rm03a: run `db:bootstrap-kestra-roles` against a real Postgres and verify the `has_database_privilege` assertions in `infra/docs/db-role-verification.md`; decide whether its assertions get folded into `tests/rating/grants.integration.test.ts` (matching the code-standards §8 file-org table) or a separate suite.
- rm04 D0: run the actual process-runner spike on a real Azure Container Apps environment (verification items 1–4) — this is the prerequisite for everything else in the unit being considered done, including flipping `deployRatingEngine` to `true`.
- rm04 follow-ups: the Azure Blob plugin's Managed-Identity support against the pinned Kestra release (`v1.3.35`) is still unconfirmed — the 2026-09-01 local run only exercised the `connectionString`/Azurite path (see the dated Completed note below), never MI/`DefaultAzureCredential`. *(`kestra.yml`'s property names and `docker compose -f docker-compose.dev.yml -f rating-engine/dev/docker-compose.dev.yml up` end to end — previously listed here — are now confirmed working locally; see the rm04 Delivered/Completed entries. The `worker/requirements.txt` transitive-dependency lock and the real Kestra image digest were already done.)*
- rm05: run the Entra admin provisioning steps (app registration, `Workflow.Admin` role, Billing Ops group, assignment-required, client secret to Key Vault — `infra/docs/engine-access.md` steps 1–2), then deploy with `deployRatingEngine=true deployEasyAuth=true` and run all 14 items in `rm05-entra-reverse-proxy.md`'s verification checklist. Obtain and record the actual D10 accepted-risk sign-off (approver + date) in `engine-access.md` — currently a placeholder.
- Phase B — carry E1 forward as a billing-module escalation (own the D7 `SECURITY DEFINER` `FROM PUBLIC` revoke where the pgledger functions live), and the O4 carry-forward into rm12's clearing logic (`CLEARED` is the sixteenth seeded code).

## Open Questions

- rm06 — `deploy_rating_flows`'s network path from the hosted ADO agent to the Kestra webserver is unresolved. Every other pipeline stage in this repo reaches Container Apps through the Azure Resource Manager control plane specifically to avoid this problem (see the `deploy` stage's smoke-test comment); `kestra flow namespace update` has no ARM equivalent and needs a direct HTTP path, which neither the internal-only ingress (rm04 D8) nor the Easy-Auth-gated external ingress (rm05, `RedirectToLoginPage`) obviously provides to a non-interactive CLI call. Needs an infrastructure decision (self-hosted/VNet-integrated agent, or an Easy-Auth exception path) before `deployRatingFlows` can ever be set to `true`.
- rm06 — the pipeline's service-connection principal has no `Key Vault Secrets User` (or equivalent) grant anywhere in this repo's Bicep; every existing KV role assignment targets a Container App's Managed Identity. `deploy_rating_flows` needs one to read `kestra-basic-auth-password` directly.
- rm06 — `kestra` CLI flag names (`flow validate`, `flow namespace update ... --server ... --user user:password`) are the spec's literal text, unconfirmed against the pinned `v1.3.35` release — same caveat class as `kestra.yml`'s D0 property names.
- rm06 — the malformed-line summary row (`log_sweep.py`) deliberately writes `perceived_severity = 'INDETERMINATE'` under an uncatalogued `MALFORMED_LOG_LINE` event_code, per D10's literal "one WARN/INDETERMINATE summary" instruction. This is in tension with code-standards §7.3's framing that the `INDETERMINATE` count "is zero" as a CI hygiene metric — a healthy system with malformed input would show a nonzero count by design here. Read as: §7.3's metric is about catalog *coverage of real component event_codes*, not about data that already arrived corrupt; not silently reconciled — rm13 (which owns the ship-gate assertion mechanism for the no-per-record-fan-out guard) may need to exclude `LOG_SWEEP`-sourced malformed summaries from that count, or this may need revisiting then.
- rm06 — **`log_sweep.py`'s idempotency has a residual commit→rename window that a full fix cannot close in rm06's scope.** The row inserts are one transaction; the rename to `logs/swept/` is a separate filesystem step *after* the commit, and the two cannot be made atomic without a durable "this file was processed" marker committed *with* the inserts. The four-table `rating` schema (`udr_rated`/`udr_batch`/`process_log`/`event_catalog`, code-standards §5.1) has no home for such a marker, and adding a fifth table is a schema/spec change (§6, ai-workflow-rules §3/§5.1), not an in-flow fix. Current behaviour errs deliberately toward a *duplicate* on a crash in that narrow window (worker killed / mount error between commit and rename), never toward a swept-but-unloaded file (data loss); the docstring now states this honestly instead of claiming atomicity, and `run_sweep`'s per-file isolation means one such failure no longer wedges the whole sweep. **This is in mild tension with code-standards §7.10** ("sweeping the same file twice leaves the row count unchanged") for that specific crash edge — §7.10 is satisfied for the normal re-run case (the file is already in `swept/`). Closing the window fully needs a decision on a `rating.processed_log_file` (or equivalent) identity table keyed on file identity/content — raise before rm13's ship gate, or accept the never-lose-data trade-off as the v1 posture. The spec's §5 now also states the **consumer-side** half of the at-least-once contract: `process_log` is telemetry (not financial truth — `udr_rated` is, and the sweep never writes it), a duplicate carries identical `event_code`/correlation fields (only `log_id`/`insert_datetime` differ), alert rules key off `event_code` presence (Inv #14) so an alarm's firing is unaffected, and only count-based consumers must dedupe or tolerate a rare over-count.
- rm06 — `emit_terminal_log.py`'s `errors`/`finally` handlers emit `STUB_EXECUTION_FAILED`/`STUB_EXECUTION_FINALIZED`, deliberately uncatalogued (see Completed) rather than guessing a severity for a new catalog row. Once rm07-rm12 replace the `prp`/`rp`/`rl` stubs with real logic that has its own real failure paths and catalogued codes, whether the flow-level handlers still need this generic marker (or should look up the failing task's own last-known event_code) is an open design question for whichever of those units gets there first — not resolved here.
- rm02-spec §A prescribes the amendment constraint be named `event_catalog_severity_check`, but rm01 shipped it as `event_catalog_default_severity_check` (rm01 Open Questions). The rm02 verification suite asserts against the actually-shipped name rather than renaming a merged constraint for a purely cosmetic match. The behaviour (nullable + the six X.733 values, vocabulary identical to `process_log_severity_check`) is what the spec's items 1–2 actually require, and both pass.
- rm02-spec §Implementation §2 says "define [`RATING_EVENT_CODES`] once, alongside the seed." Shipped it in a sibling `rating-event-catalog.data.ts` (imported by both the runner and the test) rather than in the runner script itself, because the runner ends in `void main().catch(...)` — importing it would connect and seed as a side effect. The data module keeps the constant, the rows and the upsert side-effect-free. Same directory, so "alongside" holds.
- rm01-spec §Implementation §8 names the test file `tests/rating/rm01-schema.test.ts` (no `.integration.` infix). Every other live-DB suite in this repo uses `.integration.test.ts` so it lands in the separate `vitest.integration.config.ts` project (its `include` glob requires that suffix) rather than the DB-free default project. Shipped it as `rm01-schema.integration.test.ts` to match that mechanism; flagging in case the bare name was intentional and the convention should be revisited instead.
- `rating.event_catalog.default_severity` is documented as "CHECK" in rm01-spec §Implementation §5's column notes, but no CHECK SQL is given (unlike every other CHECK in the spec, which is spelled out). Resolved: added `event_catalog_default_severity_check` in `0034_rating.sql` allowing `NULL` or the same six severity values `process_log_severity_check` accepts, honouring the spec's "CHECK" note while preserving the nullable column ("NULL means logged but never alarms").
- rm03-spec §Implementation §4 names the test file `tests/rating/grants.test.ts` (no `.integration.` infix). Shipped it as `grants.integration.test.ts` for the same reason rm01/rm02 did: the DB-free default vitest project would grab a bare `.test.ts` and it would only ever `describe.skipIf` (no `DATABASE_URL`), while `vitest.integration.config.ts`'s include glob requires the `.integration.test.ts` suffix to run it against a live DB. Same standing convention question flagged for rm01.
- rm03 escalation E1 (open): `PUBLIC` held `EXECUTE` on the four `billing` `SECURITY DEFINER` pgledger functions — any login role could post ledger transfers. rm03 closes it (Step 9) but the defect predates rating and lives in the billing module; it should be raised there so the revoke is owned where the functions live and the next `SECURITY DEFINER` function ships with a matching `FROM PUBLIC`. rm03's standing assertion (grants suite item 28) fails the build if a fifth is added without one.
- rm04 D0 remains open — **the ACA process-runner spike itself has not been run.** The Bicep/Dockerfile/compose are authored and encode the gate (`deployRatingEngine = false`), and (2026-09-01) the engine now runs cleanly under Docker Compose locally against the pinned `v1.3.35` image — but nothing here substitutes for actually proving the custom image on the process runner works on a real Container Apps environment specifically (no Docker daemon there, unlike local Compose). Verification items 1–4, and most of 5–26, remain unexecuted. This is still the single biggest open item blocking rm04 from being considered done.
- rm04 — **`kestra.yml`'s `datasources.postgres.*`/`kestra.server.basicAuth.*`/`kestra.storage.azure.*` property names are now CONFIRMED against the real pinned engine (2026-09-01, local Docker Compose run)** — see the dated Completed entry for the two real bugs this surfaced (the `sharedKeyAccountName`/`connectionString` field-name fix and the `RATING_ENGINE_AZURE_*` env-var rename to dodge a Micronaut auto-binding collision). **Still unconfirmed: whether the pinned Azure Blob storage plugin supports Managed Identity auth** — this local run only exercised the `connectionString` (Azurite) path, never `DefaultAzureCredential`/MI. If MI auth isn't supported, D5's "four credentials" becomes five (a connection-string/SAS KV secret joins the three already there); this remains a D0-spike (or a targeted deployed-engine test) item.
- rm03a — `kestra_engine`'s `CONNECTION LIMIT 20` is a placeholder (mirrors `rating_runtime`'s limit) — no document states a number for this role, and connection-limit sizing wasn't treated as one of `ratemgmt-ai-workflow-rules.md` §5.1's explicitly-never-guess items, but it's still an invented number that should be confirmed against the engine's real replica/worker count before a production bootstrap run.
- rm05 — the spec's Implementation §3 names `globalValidation.requireAuthentication: true` on the Container Apps `authConfigs` resource; `az bicep build` rejects it (property doesn't exist on that API's `GlobalValidation` type — it's an App Service Easy Auth property, not Container Apps'). Omitted; `unauthenticatedClientAction: 'RedirectToLoginPage'` covers the same intent per the only mechanism this API exposes. Flagged in `infra/docs/engine-access.md`; not something a D0-style spike would resolve differently since it's a compile-time schema fact, not a runtime unknown.
- rm05 — the Entra tenant sign-in-log diagnostic setting (D7, Implementation §5) needs a tenant-scoped deployment (`Microsoft.aadiam/diagnosticSettings`, `targetScope = 'tenant'`) and tenant-level Security Administrator (or better) — a different privilege class than every other resource in this repo's Bicep, which is resource-group Contributor. Shipped as a standalone `entra-signin-diagnostics.bicep`, deliberately not wired into `main.bicep`, documented as a manual `az deployment tenant create` step in `engine-access.md`. Flagging in case a future unit wants this folded into an automated tenant-level pipeline instead.
- rm03a — `ratemgmt-code-standards.md` §8's file-org table places rm03a's grant/CONNECT assertions inside the existing `tests/rating/grants.test.ts` (shipped as `grants.integration.test.ts`, rm03), not a new file. This pass added the SQL verification steps to `infra/docs/db-role-verification.md` but did not extend the automated integration suite — flagging so the next pass either extends that file or this note is resolved as intentionally deferred.

- rm07 — the `landing/` file trigger's plugin id (`io.kestra.plugin.fs.local.Trigger`), its `action: NONE`/`from`/`interval` properties, and the `{{ kv('rating_ran_usage_chunk_size') }}` namespace-KV read are **unconfirmed against the pinned `v1.3.35` engine** — a D0-spike item, same class as rm04's `kestra.yml` property names and rm06's kestra-CLI flags. The binding contract of rm07 is the prp task's claim/validate/reject behaviour (verified by the black-box suite against a real Postgres, still to run), not the trigger's literal plugin id; if the pinned image lacks a local-file trigger, a Schedule-poll + list is the fallback.
- rm07 — the `RATING_USAGE_WEBHOOK_KEY` secret that rm06 provisioned end-to-end (Key Vault `rating-usage-webhook-key` + `SECRET_RATING_USAGE_WEBHOOK_KEY` in `rating-engine-container-app.bicep` + `dev/.env.example` + `engine-access.md`) is now **unreferenced** — rm07 replaced rm06's manual Webhook trigger with the `landing/` file trigger (D3/Implementation §1), and the flow carries no `{{ secret('RATING_USAGE_WEBHOOK_KEY') }}` any more. Left provisioned (harmless — an unused KV secret/env var) rather than ripping the wiring back out of rm04's bicep module in this pass (a cross-unit infra touch out of rm07's `flows/**`+`worker/**` boundary). Flag for a later cleanup: either remove the secret, or repurpose it if a manual/API reprocess trigger is re-added alongside the file trigger.
- rm07 — **deviation from the spec's "polars streaming / batched reads" (D7/Implementation §3), recorded per ai-workflow-rules §5.5.** `prp.py` parses the CSV with Python's stdlib `csv` (per-line raw text + line number + quoting fidelity) and writes the *survivor chunks* with polars Parquet, rather than reading through polars. The binding requirements all hold — chunked handoff, no per-record task fan-out (Inv #10), `Decimal`/UTC (D8) — but the headline reject-file requirement (write the 37 bad rows with their line numbers + raw text + reason codes) needs per-row raw-line access that `polars.read_csv` does not cleanly give (it errors or truncates on a ragged row instead of yielding it for quarantine). Reject fidelity is the dominant constraint, so `csv`-for-parse + polars-for-chunk-write is the faithful implementation.
- rm07 — **deviation: `udr_usage_quantity` is carried in the Parquet chunk as an exact `Decimal` *string*, not a typed Arrow decimal.** D7 says the Parquet handoff is "typed — preserves `Decimal`". The string round-trip preserves the value exactly with **zero** `float` path (D8/§5.9 is the stronger constraint); a typed Arrow `decimal128` would need a fixed precision/scale chosen at PRP that risks silent coercion. RP (rm08) reads the column back as `Decimal` and casts to `numeric(20,6)`. `start_datetime`/`end_datetime` **are** typed (UTC `Datetime`, full precision). Flag so rm08's RP reads the quantity as `Decimal(str)`.
- rm07 — the feed profile carries `event_time_assumed_tz` (default `UTC`): a naive `DATETIME` (no offset) is localised with this configured zone then converted to UTC (fail-closed, §5.4), because the assumption is output-affecting (it moves `partition_period` and identity). The `RAN_USAGE` sample header (`DATETIME,…`) carries no timezone guidance — **confirm the real feed's timezone convention with upstream** (same "confirm with upstream" class as the D3 filename convention). An offset-aware value is honoured as-is.
- rm07 — `DUPLICATE_LIVE` (a collision with an existing live `udr_rated` row) is **deliberately not implemented here** — it is supersession, which is rm10's, and D6's own note defers it there. rm07 implements `DUPLICATE_IN_FILE` (within-file dedup on `(start_datetime, udr_key)`) only. Likewise `UNKNOWN_SUBSCRIBER` is wired (one-query `inventory.product_inventory` batch resolve) but fires only when a profile declares a `subscriber_ref` mapping; `RAN_USAGE` declares none, so the concrete `inventory` column to match is left as a profile-declared name (unconfirmed until a feed first needs it — §5.1 honoured by making it config, not a hardcoded guess).
- rm07 — exit-code / downstream-skip contract for RP/RL (still stubs): a **refuse** (`PARSE_FAILURE`, threshold exceeded) exits **non-zero** so RP/RL do not run and the flow's `errors` handler reports — which means the generic `STUB_EXECUTION_FAILED` terminal line also fires alongside the domain `PARSE_FAILURE` (acceptable — the `MAJOR` alarm is `PARSE_FAILURE`; interacts with the rm06 `emit_terminal_log` open question). A **`DUPLICATE_BATCH`** discard and a **claim-lost** race exit **0** with a `status: DISCARDED` manifest, so RP/RL (once rm08/rm09 make them real) must **no-op on a non-`PROCESSING` manifest status** — a forward contract recorded here for those units. **rm08 honours this**: `runtime.rp` no-ops on a non-`PROCESSING` PRP manifest (status passthrough, no DB, exit 0).

- rm08 — **subscriber resolution is a deliberate PLACEHOLDER/SHELL; the real design is recorded here (owner's plan, 2026-08-30).** rm08 ships the price-resolution + snapshot + FLAT **shell**; the subscriber-identity → subscription → offering bridge is intentionally **not built yet** (it lands with the real rating logic, per owner decision — `prp`/`rp`/`rl` are placeholders whose details are spelled out later). **Current shell behaviour:** `rp.py` resolves each record from a `--subscriber-ref-column` flow variable naming a chunk column (a PRP key dimension `key__<NAME>`, or a verbatim column), whose value RP joins **directly** to `inventory.product_inventory.product_inventory_id`; the sample test wires `PUBLIC_KEY` = a real `product_inventory_id` so the shell resolves. A value that does not resolve → `LOOKUP_MISS` (MAJOR), not rated. **Intended real design (this use case), deferred:** (1) the external subscriber key (`PUBLIC_KEY`) identifies the **customer/subscriber**, defined under `customer.party_role.party_role_specification` (jsonb) — it points to the SUBSCRIBER, *not* a subscription; (2) a dedicated **subscription lookup table** (to be defined later) holds the `(PUBLIC_KEY, COMMERCIAL_UNIT, SITE)` combination, and rating looks it up by the record's `udr_key` combination to land on the specific subscription/offering to price against — this is what closes the customer→offering **grain gap** the review surfaced (a customer holds many subscriptions, so identity alone cannot pick a price). **Also deferred:** an expression/UNIQUE index on `party_role_specification->>'public_key'` (needed for the 50k-row-per-chunk join + one-customer-per-key safety), and generalising PRP's `SubscriberRef(column, inventory_column)` config to describe a *target table + JSON path* rather than only a `product_inventory` column. The three example keys are a grounding sample; the resolver is config-driven and swappable per use case.
- rm08 — **`udr_rate_detail` is validated in Python, mirroring the Zod union rather than importing it (D6/Implementation §4).** The spec says "import the discriminated union from `validation/rating/udr-rate-detail.schema.ts`; validate the FLAT variant before write." `rp.py` is Python and cannot import a TS Zod schema, so it constructs the minimal `{"rateType":"FLAT"}` FLAT variant and validates it with a small Python mirror of `flatRateDetailSchema` (discriminant is `FLAT`, no band data), carrying it as a JSON string in the rated Parquet. The Zod union remains the source of truth (it types the Drizzle `udr_rate_detail` column for TS consumers); the actual DB write + its final validation are RL's. Flag if a shared cross-language schema is ever wanted.
- rm08 — **`udr_rated_price_raw`: RP now owns the `numeric(18,6)` boundary (RESOLVED, was deferred).** D5 says raw is "`effective_amount` at full precision (`numeric(18,6)`)" and D8 says "rounds **once**". Originally `rp.py` carried `format(effective_amount,'f')` exact and let RL's `numeric(18,6)` cast define storage — which, for a hypothetical catalog amount with >6 dp, would be a *second* silent round at RL, in tension with "round once". A code-review pass flagged this; **fixed** — `rate_record` now **fails closed** (`_exceeds_scale`, mirroring `prp._parse_usage`) when the resolved amount has more than 6 significant fractional digits, so RP will not defer a silent round to RL. A conforming (≤6 dp) amount is still carried exact (`format(x,'f')`, e.g. `0.0050`) and stores losslessly in `numeric(18,6)`, so the raw value matches the snapshot; only a genuinely un-representable amount (a misconfigured catalog/override) stops the batch, which is the right fail-closed posture for financial data.
- rm08 — **a resolved-but-`tiered` catalog price (`amount IS NULL`) is surfaced as `LOOKUP_MISS`, not a distinct code.** v1 computes `FLAT` only (D5), which needs a scalar `amount`; a `usage` price whose `pricing_model='tiered'` (`amount` NULL, `pricing_characteristics` set) resolves a row but yields no FLAT-computable amount, so `rp.py` treats it the same as an unresolved lookup (`LOOKUP_MISS`, MAJOR, with an `additional_info` note) rather than inventing a new event_code (a rm02 spec change, never-guess §5.1). When a non-`FLAT` rate type is implemented, this branch is where its `# STUB:` computation attaches.
- rm08 — **deferred cleanup: shared handoff plumbing duplicated between `prp.py` and `rp.py`, to extract when rm09 (RL) lands.** A `/code-review high` pass flagged real (if thin) duplication across the two entry points: the `_work_dir` root resolver (`root = Path(base) if base else Path(tempfile.gettempdir())/"rating-work"` — byte-identical; the leaf differs intentionally, `{batch_id}` vs `{batch_id}-rp`, to avoid collision), the manifest-write mechanics (`mkdir` + `write_text(json.dumps(..., indent=2))`, divergent payloads), the log-emit mechanic (`logemit.line` → `logs/{component}-{execid}.jsonl`, divergent fields), and the URI-resolve+validate `main()` preamble (`storage._local_path` → empty/bad-scheme/not-a-file → clean exit). Deliberately **not** extracted this pass: (a) each extractable piece is currently thin (a one-line root resolver, thin JSON/log wrappers) because only the *mechanics* repeat while the *content* is legitimately per-stage; (b) rm09's RL will be a **third** consumer of exactly this plumbing, so the abstraction should be designed once against three concrete consumers (rule of three) rather than guessed from two; (c) extracting now re-touches rm07's landed/tested `prp.py` — the same cross-unit "standalone cleanup" the codebase already deferred once (rm07's shared DB-test-harness extraction). Two sub-candidates the finder raised were **rejected** outright as over-engineering: a helper for the single-builtin `format(x,"f")` idiom, and merging `rp._as_utc` (normalise an existing datetime) with `prp._parse_instant` (parse a string with profile-tz) — different operations that only share an aware-UTC tail. **Action for rm09:** extract a shared `runtime` handoff helper (work-root + `write_json` + log-emit + URI-validate) across prp/rp/rl in one scoped change.
- rm08 — **rm08 does not update `udr_batch` counts; the `lookup_miss_count` rides the rated manifest.** rm07 stamped `parsed/rejected/discarded` on `udr_batch`; the reconciliation identity is `parsed = rated + rejected + discarded` (§10.10). rm08's `LOOKUP_MISS` records were parsed+carried by PRP but not rated, a fourth category the spec does not add to `udr_batch`, and rm08's spec scope is the row-level snapshot + the `LOOKUP_MISS` log line, not a batch-count write. So rm08 leaves `udr_batch` to RL and carries `rated_count`/`lookup_miss_count` (+ PRP's counts) in the rated manifest for RL/rm11 to compose the final identity — recorded here as the forward contract for rm09.

- rm09 — **`LOOKUP_MISS` is folded into `discarded_count` for the reconciliation identity (resolves the rm08 fourth-category tension).** rm09-spec D5 states the identity as `parsed = rated + rejected + discarded` with "parsed/rejected/discarded from rm07" and does not mention lookup misses. But a `LOOKUP_MISS` record was parsed by PRP yet is neither rated nor a PRP validation reject, so a naïve reading breaks the identity whenever RP could not resolve a record (e.g. the rm08 fixture's bogus subscriber). RL therefore computes `discarded = prp_discarded_count + lookup_miss_count` (both from the rated manifest) and asserts `parsed = rated + rejected + discarded` against that — a lookup-missed record IS discarded (parsed but not billed). For the clean fixture this is `parsed = rated + rejected + 0`; with one miss, `5 = 4 + 0 + (0+1)`. This is the "RL/rm11 compose the final identity" the rm08 note anticipated, and it is fail-closed (an unaccounted record trips `RECON_IMBALANCE`). `Counts.discarded` in `rl.py` is the single place this folding lives.
- rm09 — **terminal status: `COMPLETE` iff every parsed record rated (`rated == parsed`), else `PARTIAL`.** D7's table lists only "some records rejected within threshold (rm07) → PARTIAL", but a `LOOKUP_MISS` (discarded, not a PRP reject) is also a shortfall from "all records rated". Following D7's own "Clean load, **all records rated** → COMPLETE" wording and the fail-closed principle (surface degradation), RL flips to `PARTIAL` on ANY shortfall — PRP rejects **or** RP lookup-misses — not only on PRP rejects. `RP` already emitted the `LOOKUP_MISS` `MAJOR` alarm; RL's `BATCH_PARTIAL` (`MINOR`) then carries the counts. A batch with a miss is `PARTIAL`, not a misleading `COMPLETE`.
- rm09 — **archive backend: Azure Blob via `azure-storage-blob` (RESOLVED by owner decision, 2026-08-31 code review — B).** The archive is now backend-dispatched in `storage.py` (`copy_to_archive`/`archive_exists`/`archive_uri`): when `RATING_ARCHIVE_BLOB_URL` is set (the deployed engine) it **uploads to Azure Blob** via `azure-storage-blob` + `DefaultAzureCredential` (the worker MI, rm04 D5) and `archive_file_path` is the **Blob URL**; otherwise it falls back to the local `archive/` filesystem (dev/test), where `archive_file_path` is a `file://` URI. `azure-storage-blob`/`azure-identity` are imported **lazily** so `import runtime` still works where they are absent (the DB-free test env, fs backend). The load-bearing D6 behaviour holds on both backends: copy/upload + **verify by size before promoting** (fixes the review's verify-after-replace finding), delete landing **last**, `archive_file_path` set last, idempotent archive-only recovery, URI distinct from `source_file` (D4). **Authored-not-run for the Blob path** (no `azure-storage-blob` on this session's `python3`, no live Blob) — same posture as rm04-rm08; the fs backend is harness-verified. A local Azurite connection string (`RATING_ARCHIVE_BLOB_CONNECTION_STRING` + `RATING_ARCHIVE_BLOB_CONTAINER`) is supported for a future local Blob test.
- rm11 — **possible cross-unit gap: PRP's `DUPLICATE_BATCH` checksum dedup (rm07 D5, `is_duplicate_redelivery`) matches on `(file_key, file_checksum)` regardless of the matched row's `status`.** After the stranded-batch reconcile fails a `PROCESSING` batch and releases its claim, a redelivery carrying the *exact same bytes* as the just-failed run would still resolve `is_duplicate_redelivery(...) == True` and be discarded as `DUPLICATE_BATCH` rather than reprocessed — `is_duplicate_redelivery` has no notion of "that prior attempt never actually loaded anything." Not fixed here: rm11's spec (`specs/rm11-stranded-batch-recovery.md`) never mentions this interaction, and `prp.py`'s dedup policy is rm07's to own, not rm11's to relitigate un-asked (ai-workflow-rules §5.6). What rm11 actually guarantees is narrower and still real: the `udr_batch` row is released from an indefinitely-open `PROCESSING` state, and a **corrected** reissue (new content/filename, e.g. `_v2`, matching the existing RAN_USAGE reissue convention) claims `batch_run_num = N+1` and processes cleanly — proven by the rm11 test suite. Whether a byte-identical resend should also reprocess after a strand (which would mean scoping `is_duplicate_redelivery` to non-terminal-failure prior rows, or to `COMPLETE`/`PARTIAL` only) is an open design question for whoever revisits rm07's dedup check next — raise before treating "the file reprocesses" as fully closed for every redelivery shape, not just a corrected one.
- rm11 — **the flow's "on flow start" trigger half (D7) has no confirmed Kestra OSS mechanism.** `stranded-batch-reconcile.yaml` relies on the `Schedule` trigger's `backfill: { start: <fixed past date> }` catch-up behaviour firing immediately whenever the Kestra Scheduler service (re)starts, on the theory that a worker relocation restarts the scheduler and backfill catch-up substitutes for a literal "on start" trigger, which Kestra OSS does not appear to have as a distinct primitive. The exact property name/semantics are **unconfirmed against the pinned `v1.3.35` release** — same D0-spike class as every other Kestra trigger/property specific already flagged in this tracker (the landing file trigger's plugin id, the Webhook `key`, the `kestra` CLI flags). The 5-minute Schedule cadence is a bounded fallback either way — a strand is never stuck longer than one cron tick past the threshold, even if backfill behaves differently than assumed.
- rm09 — **code-review outcomes (xhigh multi-agent review, 2026-08-31).** Applied fixes: (2) `udr_usage_rate`/all money columns now go through a null-safe `_money(value, column)` that **fails closed** with a clear diagnostic on an unexpected NULL, instead of an opaque `Decimal('None')` crash (owner decision **D**: a rate is **ZERO, never null** — a null is an RP contract violation); (3) the load-state guard now handles a **missing batch row** (loud error, no silent orphan-insert) and treats any non-`PROCESSING` status (incl. `RECEIVED`) as not-loadable → exit 1, replacing the sloppy `not in ('PROCESSING',)`; (4) archive verify now happens **before** the atomic promote (in `storage.copy_to_archive`), so a partial copy never overwrites a prior archive; (10) removed the dead `--work-dir` arg; (13) `find_currency_mismatches` materialises the pair set **once** before splitting (no reliance on two set-iterations aligning); (14) the archive-only recovery path reports `rated_count` from the authoritative committed `udr_batch.rated_count` (added to the `get_batch_state` probe), not the RP manifest. **Deferred by decision:** the auto-clear `alarm_key` convention (correctness #1 / OQ **A**) — **no auto-clear is planned this phase**, so the emitted `alarm_key`s are forward-looking placeholders; the convention (so `BATCH_COMPLETE` pairs with prior `BATCH_PARTIAL`/`LOOKUP_MISS`/`PARSE_FAILURE` raises across PRP+RL) is settled when the clearing feature (rm12) is built. The shared prp/rp/rl handoff-helper extraction remains a standalone follow-up (rule-of-three now met). **Cross-unit test updates (same precedent as rm07 narrowing rm06's assertions):** rm09 making `rl` real broke two prior static assertions — `tests/rating/rm06-*.test.ts` #2 (asserted `# STUB: rm08 owns RP` + `# STUB: rm09 owns RL` and two `python3 -c` bodies; `rm08 owns RP` was already stale debt rm08 left behind) and `tests/rating/rm08-*.test.ts`'s rl-stub assertion — both updated to the now-all-real reality (prp/rp/rl are real modules; the only remaining stub marker is `# STUB: rm10`; zero `python3 -c` bodies). All four rating static suites green (rm06-rm09: 17 passed / 39 DB-gated skipped).
- rm09 — **code-review round 2 (2026-08-31): remaining items triaged by severity, top item fixed.** Six review findings were still open after round 1 (#7 COMPLETE/PARTIAL disposition, #8 exit-code/retry, #9 whole-batch memory, #11 shared-helper duplication, #12 per-row allocation, #15 count source). **Fixed (highest severity — #9, folding in #12+#15): RL now STREAMS the load** — `load_payload`/`Payload` (which materialised every COPY row + all keys before inserting, ~1-2 KB/row × up to 5M rows ⇒ multi-GB heap / worker OOM) are replaced by two chunk-at-a-time passes inside the one transaction: `scan_and_guard` (the `BILL_APPROVED` guard **per chunk** + currency pairs accumulated into a small deduped set, asserted once — guard still covers the whole batch before any insert, D2/Inv #6) then `copy_chunks` (`COPY` one chunk at a time, Inv #10). Peak memory is now **one chunk**, not the whole batch, matching the chunked-handoff design. `rated` is the summed actual `COPY` count (fixes #15 — the terminal event and the `udr_batch` stamp now use the same authoritative number). Harness re-verified **16/16** incl. multi-chunk streaming (one `COPY` per chunk), guard-refusal-across-chunks with zero rows, and empty-chunk skip. **Evaluated and intentionally NOT changed:** (#7) COMPLETE-vs-PARTIAL stays `rated == parsed` — it is spec-faithful (D7 "all records rated → COMPLETE") and adding a disposition field "for rm10/rm11" is exactly the speculative cross-unit scope creep ai-workflow-rules §3 forbids; rm10 owns its own terminal logic in the same transaction it fills the supersede-hook. (#8) exit-non-zero on REFUSED/FAILED is correct and **moot for a retry loop** — automated retry-with-backoff is out of scope (project-overview "Out of scope"), the flow declares no `retry:`, and a re-run of a terminal batch is already handled by the load-state guard (#3). **Deferred (unchanged reasoning):** (#11) the shared prp/rp/rl handoff-helper extraction — a standalone cleanup that re-touches rm07/rm08's landed, tested modules whose DB-gated suites cannot be run this session; do it in its own change when the live-DB suite runs.
- rm09 — **`partition_period`: one source of truth (OQ C, 2026-08-31 decision + risk).** Kept the Python `period_of` (proposes the value) with the DB `udr_rated_period_matches_check` CHECK (`= rating.period_of(start_datetime)`) as the **single authority**: Python and the SQL function are independent implementations, so they cannot silently drift — a Python error is **rejected at insert** (fail-closed), never committed wrong. A new DB-gated test asserts the loaded rows' `partition_period` equals `rating.period_of(start_datetime)` (agreement is provable, not just enforced). **Risk of the literal "one runtime definition" alternative (rejected):** computing it DB-side via `COPY`→temp table→`INSERT … SELECT rating.period_of(...)` would need a `CREATE TEMPORARY` grant for `rating_runtime` (a grant/spec change per code-standards §9.3, and relying on `PUBLIC`'s default TEMP is exactly the anti-pattern Inv #18 warns against) **and** doubles the write I/O at the 100k-5M-row target — for a marginal gain over a CHECK that already makes silent drift impossible. Revisit only if a future need makes the temp-staging pattern worthwhile.
- rm09 — **exit-code posture: `REFUSED` (guard/currency) and `FAILED` (recon) exit non-zero; `COMPLETE`/`PARTIAL` exit 0.** A refusal is a deliberate business outcome, but exiting non-zero makes it a visible flow failure and matches rm07's precedent ("a refuse exits non-zero so downstream does not run and the error handler reports"; the domain alarm — `LOAD_BLOCKED_BILLED`/`CURRENCY_MISMATCH`/`RECON_IMBALANCE` — is the real signal, alongside which the generic `STUB_EXECUTION_FAILED` terminal line also fires, acceptable per the rm06 `emit_terminal_log` note). On `REFUSED`/`FAILED` the file stays in `landing/` (never archived, zero rows). **Known follow-on (out of rm09 scope):** a `REFUSED` file left in `landing/` will be re-picked by the trigger, and PRP's `max+1` claim makes a new `batch_run_num`, so a genuinely-wrong file re-refuses each cycle until an operator removes it — the operator-triggered-recovery model (project-overview "out of scope: automated retry"); rm11/operator owns it.
- rm09 — **a live-row `UNIQUE` violation on `COPY` propagates uncaught (intended).** RL catches only `BatchRefused` (D2/D3) and `ReconImbalance` (D5). A double-live insert — the Inv #3 backstop firing when the guard/supersede is wrong/raced/skipped (test #2/#3) — raises `psycopg.errors.UniqueViolation`, which is deliberately **not** caught: the transaction rolls back (zero rows), RL exits non-zero with the traceback, and the batch is left `PROCESSING` for rm11's stranded-batch reconcile (D6). Catching it would mask the very guarantee Inv #3 exists to provide.
- rm09 — **the `CURRENCY_MISMATCH` assertion is an inner join (per the spec's literal query), so a record whose `product_inventory.billing_account_id` does not resolve to a `billing_account` is NOT flagged.** rm09-spec §3's query is `product_inventory JOIN billing_account … WHERE ba.currency <> udr_currency`; a missing account simply returns no row (no mismatch, no assertion possible), matching the spec exactly. In practice RP already resolved the record (so the inventory row exists); a null/unresolvable `billing_account_id` is a separate data-integrity gap not in rm09's scope. Flag if a later unit wants a fail-closed "no account currency to assert against" branch.
- rm09 — **rm08's "extract a shared `runtime` handoff helper across prp/rp/rl" action is deliberately still deferred.** The rm08 note assigned rm09 the extraction of the thin duplicated plumbing (work-root resolver, `write_json`, log-emit, URI-validate preamble). `rl.py` instead duplicates only the pieces it actually needs (the `main()` URI-validate preamble and the `logemit.line → logs/RL-<execid>.jsonl` mechanic — it has **no** `_work_dir`/manifest-write, since RL emits no chunk handoff). Reason unchanged from rm08's own hesitation: extracting now re-touches rm07/rm08's landed, tested `prp.py`/`rp.py` (a cross-unit "standalone cleanup", against diff-minimal), and the extractable pieces remain thin. The rule-of-three is now genuinely met (prp/rp/rl), so the extraction is a good **standalone** follow-up — do it in its own scoped change that re-runs all three suites, not folded into a feature unit.

- rm10 — **`rl.py`'s changes were not run through `py_compile` this session — no `python3` was available on `PATH`** (the earlier rm04/rm06-rm09 sessions that verified `py_compile`/harness output all had it; this one did not). The new/changed code (`supersede_batch`, the extended `scan_and_guard`/`load_and_reconcile`/`get_batch_state`/`stamp_terminal` signatures, `emit_supersede_events`, and the `main()` threading) was reviewed by hand line-by-line instead — including tracing the four-consecutive-reprocessings lineage chain and the cross-period `incoming_periods` correlation by natural key — but that is not a substitute for actually running the interpreter. **First thing to do before trusting this unit**: get `python3` + `psycopg`/`polars` available and run `python3 -m py_compile rl.py`, then the stubbed-import harness pattern rm09 used, then the new integration suite.
- rm10 — **cross-period detection correlates by `udr_key` alone, not the full `(start_datetime, udr_key)` natural key.** D2's scenario is one corrected record whose timestamp moves it to a different partition; the implementation collects `incoming_periods: dict[udr_key, set[partition_period]]` while scanning the run's chunks (a side product of the existing guard/currency pass) and flags a superseded row as cross-period when its `partition_period` isn't among the periods any run-N row sharing its `udr_key` landed in. If a single reissue legitimately carries **multiple** distinct `start_datetime`s under the same `udr_key` (e.g. an `udr_key` built from stable dimensions only, with several readings in the file), the set-based check still works (it checks membership, not a 1:1 pairing) — but it cannot tell *which* specific successor a given predecessor corrected into, only whether *some* successor shares its partition. This matches what the spec asks for (detect the crossing, not reconstruct the exact predecessor→successor pairing) and needs no `udr_rated` schema change (there is no per-row successor pointer, rm01 D11). Flag if a future need wants the precise pairing.
- rm10 — **`SHRINKING_REISSUE` compares `udr_batch.parsed_count` (run N vs run N−1), not a live-row count.** D4 says "the run-N record count against run-(N−1)"; `parsed_count` is the count already stamped on `udr_batch` by PRP for each run and is available before the insert (unlike a rated/live count, which would need the COPY to have already happened). This also means the check fires even when PRP itself carried the same total but the *content* shrank in a way `parsed_count` wouldn't show (e.g. N-1 had 100 parsed records covering 100 distinct natural keys, N has 100 parsed records but only 90 distinct keys, 10 duplicated) — an edge case D4's literal "record count" wording does not obviously cover either way; not addressed here, flagged for a later pass if it turns out to matter in practice.
- rm10 — **CodeRabbit review pass on the rm10 diff (2026-09-01): five inline findings + one nitpick fixed; verified `tsc`/ESLint/Prettier clean, both touched YAML files re-parse, and the touched integration test file's static describe still runs green (DB/python-gated tests skip as expected in this session's environment).** (1) `incoming_periods` in `rl.py` restructured from `dict[udr_key, set[partition_period]]` to a flat `set[(udr_key, partition_period)]` — same information, one container instead of many small per-key sets; the D2 cross-period lookup and `scan_and_guard`'s return type/docstring updated to match. (2) `zip(udr_keys, start_dts)` in `scan_and_guard` now passes `strict=True` (both lists are derived from the same chunk frame so are already guaranteed equal length; this makes a future refactor that breaks that guarantee fail loudly instead of silently truncating). (3) `db/bootstrap/kestra-db-roles.sql`'s header comment corrected — it claimed the whole file could run through one `psql` invocation, which stopped being true once Step 4 (the 2026-09-01 fix, see Completed) was added: Step 4 needs a separate connection to the `kestra` database itself, so the header now documents Steps 1-3 and Step 4 as two separate invocations. (4) The progress tracker's own Completed entry #2 had a single-backtick Markdown span whose content contained literal backticks (the nested JS-tagged-template/shell snippet), which breaks Markdown code-span parsing; switched to a double-backtick-delimited span. (5) **`kestra.yml`'s `connectionString: ${RATING_ENGINE_AZURE_CONNECTION_STRING:-}` removed from the shared/deployed config — this was a real bug, not just style.** `:-` is bash syntax, not Micronaut's placeholder-default syntax (`${VAR:default}`, single colon); with the dash, an unset env var resolves the default to the literal string `"-"`, not empty. Worse, even a *correctly* empty-defaulted value would still break the deployed engine: the Azure storage plugin's `AzureClientFactory` (`kestra-io/storage-azure`, checked via GitHub) takes the `connectionString` branch on any **non-null** value, including `""`, so any value at all on this key (not just a wrong one) prevents the intended Managed Identity fallback. Fixed by omitting the key entirely from the shared `kestra.yml` and instead injecting it **local-dev-only** via `KESTRA_CONFIGURATION` (Kestra's documented Docker override env var — a YAML string layered on top of the `--config` file) in `docker-compose.dev.yml`, referencing the same `RATING_ENGINE_AZURE_CONNECTION_STRING` `.env.example` value already used elsewhere (no duplicated literal). **Not verified against a live engine this session** (no Docker run performed) — unlike the three bugs in the dated 2026-09-01 Completed entry, which WERE confirmed live. Specifically unconfirmed: that `KESTRA_CONFIGURATION` YAML-merges the way Kestra's docs describe on this pinned `v1.3.35` image, and that the `$$`-escaped placeholder in `docker-compose.dev.yml` resolves correctly through Micronaut at container start. **First thing to do before trusting this fix**: re-run the local Compose stack and confirm the engine still starts clean and the Azurite archive/storage path still works, exactly as the 2026-09-01 session did for the three property-name bugs.

- rm12 — **`completeness_check.py` was not run through `py_compile` this session — no `python3` was found on this session's `PATH` at all** (stricter than rm10/rm11's sessions, which at least had a `python3` binary even without `psycopg`/`polars` — this session's `python3`/`python`/`py` all resolve to the Windows Store alias stub, not a real interpreter). The module was verified by careful manual review only, including a full hand-trace of the 4-run raise/idempotent-noop/late-raise-and-clear/self-clear/stable-noop sequence the DB-gated test encodes. **First thing to do before trusting this unit**: get a real `python3` + `psycopg`/`polars` on `PATH` and run `python3 -m py_compile completeness_check.py`, then the DB-gated integration suite.
- rm12 — **period semantics are a genuine design decision, not given by the spec, and are recorded so a later session does not silently second-guess them.** Implementation §3 states only "compare the expected deliveries for the period against `udr_batch`" — no literal predicate (unlike D5's verbatim orphan query). This session decided: (a) a "period" is a UTC calendar day; (b) "received for period D" means `udr_batch.received_at` (not a content-date parsed out of `file_key`) falls on day D; (c) cadence is fixed to daily only (the sole example the spec gives — a non-daily cadence is unimplemented, not broken, per ai-workflow-rules §3's "no speculative additions"). If a future `udr_type` needs a non-daily cadence, or needs "period" to mean the file's *content* date rather than its arrival date, that is a spec change to `rm12-completeness-gap-detection.md`, not a bug fix here.
- rm12 — **clearing ownership: `completeness_check.py` clears its own alarms; RL is never involved.** This resolves rm09's Open Questions note that "the auto-clear `alarm_key` convention... is settled when the clearing feature (rm12) is built" — but only for `FILE_NOT_RECEIVED`/`FILE_LATE`. RL-raised auto-clearing codes that name `BATCH_COMPLETE` as their clearer (`PARSE_FAILURE`, `LOOKUP_MISS`, `BATCH_PARTIAL`, `TASK_RETRY_OK`, `DB_WRITE_FAILURE`, `BATCH_STRANDED`) still have **no code anywhere that actually emits their `CLEARED` row** — rm09's `BATCH_COMPLETE` emission carries no logic that looks up and clears a prior open alarm on the same `file_key`/`batch_id`. rm12's spec scope is file-level absence + orphan usage, not "build the general clearing mechanism for every RL-raised code" — flagging this as still-open rather than assuming rm12 silently closed it for codes it never touches.
- rm12 — **KV config value format chosen as a delimiter-based scalar (`"UDR_TYPE:HH:MM,..."`), never JSON, and this is a real constraint, not a style preference.** A Kestra `commands:` argument is a shell string; a JSON blob's embedded double quotes cannot survive the existing double-quote-wrapped `"{{ kv(...) ?? default }}"` pattern this codebase's flows already use (rm11's threshold, the chunk-size KV) without either breaking the shell parse or requiring a different quoting scheme this session did not attempt to design. If a future unit wants richer per-`udr_type` config than "one deadline time", either a new KV key per `udr_type` (following rm07's per-flow-variable precedent) or a deliberately-designed quoting scheme is needed — not a JSON blob through this same mechanism.

## Architecture Decisions

- **2026-08-28 — flow definitions (`flows/**`) also live in `enterprise-billing-app`, resolving the question the 2026-08-27 decision below deliberately left open.** rm06 is the first unit that ships real Kestra flow definitions, and both `specs/rm06-flow-template-logging-sweep.md`'s header ("Repo: rating repo") and `ratemgmt-ai-workflow-rules.md` (§ "Read this first", §1.2, §2.2, §2.6, §6) repeatedly frame flows as living in a genuinely separate repository from the app — stronger and more repeated language than rm04's infra-only framing. Rather than either build a new repo no one had asked for yet, or silently reuse the rm04 precedent without re-confirming it now applies to business-logic flows too, the user was asked directly (`AskUserQuestion`): create a real separate repo, or keep flows here. **Chosen: keep them here.** Landed as `rating-engine/flows/` (a new sibling of the existing `rating-engine/{worker,kestra,dev}/` subtree), and `infra/azure-pipelines.yml` gained a `deploy_rating_flows` stage rather than a second repo's own pipeline. This does not retroactively resolve every reference to "the rating repo" throughout the rm0x specs — later units should keep reading "rating repo" as "the `rating-engine/` subtree of this repo," matching how `rating-engine/worker/Dockerfile` and `rating-engine-container-app.bicep` were already read that way for rm04/rm05.
- **2026-08-27 — rm04's infra lives in `enterprise-billing-app`, not a separate "rating repo."** The spec (`rm04-spec` header table, D1) and `ratemgmt-code-standards.md` §8/§1.2 both frame `worker/**`/`infra/**`/`dev/**` as a separate rating-repo boundary, kept apart from the app repo because rating *flow logic* must never live here (`ratemgmt-ai-workflow-rules.md` §1.2). Asked directly, the user clarified: their notion of "rating repo" is specifically the future home of Kestra **flow definitions** (the actual business logic, rm06+), and that repo does not need to exist yet — flows aren't being built in this pass. The Kestra **engine deployment** (worker image, Bicep, local compose) is infrastructure for standing up the app's own Kestra instance, so it belongs in the existing app codebase. Landed as a `rating-engine/` subtree (`worker/`, `kestra/`, `dev/`) plus new modules under the existing `infra/bicep/modules/` (rather than a parallel `infra/` tree), keeping one Bicep deployment graph that references the same shared ACR/Key Vault/Container Apps Environment/Postgres as the rest of `main.bicep` (D1's "shared platform footprint"). **This is a deviation from the literal spec text**, recorded here per `ratemgmt-ai-workflow-rules.md` §5.5 since `_newmodule-rating-engine-plan.md` (the code-standards §7 owning document for "design decision and its reasoning") is not present in this repo, mirroring the already-known `rm00-build-plan.md` gap. When flow work actually starts, a real separate rating repo (or a decision to keep flows here too) needs to be made then — this decision does not resolve that, only defers it honestly instead of building a repo no one asked for yet.
- **2026-08-27 — D0 encoded as a deploy gate, not just a doc note.** Rather than either refusing to write any rm04 code (honoring D0 "do not build the rest of this unit" with maximal literalness) or writing it and hoping someone remembers not to deploy it, `infra/bicep/main.bicep`'s `deployRatingEngine` param defaults to `false` and gates every rm04-specific resource (storage account, Container App). The files exist, compile, and are ready, but a normal `az deployment group create` against these parameter files does not stand up the engine — someone has to deliberately flip the flag, which is the natural point to also confirm the spike passed.

## Session Notes

- rm01 ripple: adding the first `rating.*` migration means every `tests/**/*.integration.test.ts` file's full-migration `beforeAll`/`afterAll` now creates the `rating` schema. Added `DROP SCHEMA IF EXISTS "rating" CASCADE` immediately before each existing `DROP SCHEMA IF EXISTS "core" CASCADE` call across all 71 affected files (140 insertions), mirroring the `customer`/`billing` schema precedent (see repo memory `new-pgschema-integration-test-ripple`).
- Migration is `0034_rating.sql` (next number after `0033_customer_bill_finalization_guard.sql`); journal entry added by hand in `db/migrations/meta/_journal.json` with no matching snapshot file, matching the existing gap for every other hand-authored partitioned-table migration (0018–0020, 0027–0033 also have no snapshot).