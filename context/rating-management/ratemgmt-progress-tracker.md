# Progress Tracker

Update this file after every meaningful implementation change. Git history is authoritative for *what* changed and *when*; this file exists for *why*, *what's still open*, and *what a fresh session must not silently re-decide*. Keep entries dense — a fact worth recording is worth recording once, tersely.

## Current Phase

- Phase F/G — ship gate (`rm00-build-plan.md`) — rm13 authored and CodeRabbit-hardened (commits `072a37f`, `8e10aad`). No unit's DB-gated or live-Kestra-engine-gated pieces have ever run against real infrastructure in any session — see In Progress/Next Up.

## Current Goal

- No next unit identified. `rm00-build-plan.md` (which would say what comes after ship-gate assembly) is not present in this repo — don't assume rm13 is the last unit without confirming with the owner.
- Flow deployment to the local Kestra engine is unblocked (rm13 shipped, the prior deferral condition) but not yet done — the engine is confirmed reachable/login-capable, but only the 9 built-in tutorial flows are loaded.
- **Trust `git log`/`git show` over this file's own narrative if they disagree.** A concurrent session's commit (`ddd71e9`) once silently reverted this file's rm13 sections after another commit (`072a37f`) had already authored them — almost certainly a stale-checkout collision between two sessions editing the same file (sequential commits don't merge-conflict the way `git merge` would). No code was lost, only this document briefly misrepresented it; restored from `git show ddd71e9`. Full account in Session Notes.

## Completed

- **rm13 — ship gate** (`specs/rm13-ship-gate.md`, Phase F/G). Assembles the code-standards §10 guardrail suite into CI; writes only test #15 (no-per-record fan-out) plus one new end-to-end journey test (D3), and otherwise changes only CI config. Delivered: `tests/rating/rm13-no-fan-out.integration.test.ts` — test #15, settling D2's open assertion mechanism as Kestra's REST API (`GET /api/v1/executions/{id}` → `taskRunList.length`) rather than guessing internal DB tables; static half (fixed-task-count bound, computed by text-slicing the flow YAML, not a real YAML parse — no new dependency) passes 2/2, the live-engine half has never run (no Kestra engine reachable in any session, and no product/inventory fixture is even needed for it — every record deliberately becomes `LOOKUP_MISS`). `tests/rating/rm13-e2e-journey.integration.test.ts` — composes rm07→rm08→rm09→rm10→rm12 in one black-box run (63 good + 37 bad rows → `PARTIAL` → rated → loaded at `RATED` + archived → reissue → rm10 supersedes → completeness check runs clean); DB/python-gated, never run. `db/checks/rating-migration-boundary.ts` + CLI runner (D4 — flags a rating migration (filename-matched `*rating*.sql`) that writes schema-qualified `billing.*` outside a comment; 5/5 unit tests pass, no DB needed). `infra/azure-pipelines.yml` — installs the rating-engine Python runtime in `test_scan` (a hard failure as of the CodeRabbit follow-up, not `continueOnError`), runs the migration-boundary check, adds Semgrep `p/python`, and two new off-by-default stages: `rating_fanout_gate` (test #15 against a disposable local Kestra stack spun up in CI) and `zap_scan_rating_engine` (an unauthenticated DAST baseline against the deployed engine's Easy-Auth login surface, since there is no login form to script). Build-hygiene clean throughout. **CodeRabbit-fix follow-up (`8e10aad`)**: reworded the spec/checklist so "all 16 run" doesn't contradict test #15 being a non-blocking design guard; widened `BILLING_REFERENCE` to catch quoted-identifier/whitespace-around-dot forms; removed `continueOnError` from the pip-install step (judged the lock proven-clean enough via the Docker build); hardened test #15's live-engine half (refuses Basic Auth over non-HTTPS/non-loopback, a non-`SUCCESS` terminal state now throws instead of silently passing); removed an accidentally-committed `changes.zip`. See Open Questions for the one still-open judgment call (the filename-based migration detection).

- **2026-09-01 — rm04's local dev stack run for the first time, end to end, against the real pinned `v1.3.35` engine** (not the full D0 ACA process-runner spike — that remains open). Surfaced and fixed three real bugs no static review could catch: (1) `kestra-db-roles.sql` was missing `GRANT CREATE ON SCHEMA public TO kestra_engine` (Postgres 15+ no longer grants that to `PUBLIC`) — fixed with a new Step 4 against a separate `kestra`-database connection. (2) The `kestra-setup` compose service's inline `sh -c "node -e ..."` nested a JS template backtick inside a shell string, so POSIX `sh` ran it as a shell command instead of passing it to Node — silently never set the `kestra_engine` password; fixed by extracting `rating-engine/dev/set-kestra-role-password.mjs`. (3) `kestra.yml`'s Azure storage config used wrong property names (real fields, confirmed by reading the plugin jar's bytecode, are `sharedKeyAccountName`/`connectionString`) **and** its env var names collided with Micronaut's own auto-bound nested properties — fixed by renaming to `RATING_ENGINE_AZURE_*` everywhere including the deployed Bicep module (same latent bug there). Result: the engine starts cleanly; the UI is reachable and confirms `v1.3.35`. Still unexercised: the real ACA process runner, Managed-Identity Blob auth, and any flow trigger/run (no flow has ever executed).

- **rm12 — completeness and gap detection** (`specs/rm12-completeness-gap-detection.md`, Phase F). A new scheduled flow (`completeness-check.yaml`, hourly) comparing `rating.udr_batch` against a per-`udr_type` expected-cadence KV config, raising `FILE_NOT_RECEIVED`/`FILE_LATE` and self-clearing them once a later batch for that period lands and completes; also surfaces superseded-never-replaced usage via the rm01 orphan index (via stdout, not a new event code). Delivered `completeness_check.py` + the flow + `tests/rating/rm12-completeness-gap-detection.integration.test.ts` (static half 7/7; DB-gated half never run — no `python3` at all this session, reviewed by hand only including a full 4-run raise/clear/idempotency trace). Key decisions (reasoning in Open Questions): KV config is a delimiter scalar, never JSON; a "period" is a UTC calendar day keyed on `received_at`, not a content-date; cadence is fixed daily; clearing is this module's own job, not RL's; two *distinct* idempotency guards (`alarm_already_raised` for the raise, `alarm_is_open` for the clear) prevent an infinite raise/clear oscillation. CodeRabbit follow-up: `parse_config` now rejects a duplicate `udr_type`; the two KV values pass through the task's `env:` block instead of being spliced into `commands:` text.

- **rm11 — stranded-batch recovery** (`specs/rm11-stranded-batch-recovery.md`, Phase E). A startup + scheduled flow (`stranded-batch-reconcile.yaml`, every 5 min + a `backfill` clause standing in for an unconfirmed "on flow start" Kestra primitive) that finds `udr_batch` rows stuck at `PROCESSING` past a namespace-KV threshold, resolves each to `FAILED` (releasing the `(file_key, batch_run_num)` claim so a reissue reprocesses under `batch_run_num = N+1`), and alarms `BATCH_STRANDED` (a coordinated 17th event code added to rm02's seed). Delivered `stranded_reconcile.py` + the flow + `tests/rating/rm11-stranded-batch-recovery.integration.test.ts` (static half 5/5; DB-gated half never run). Deliberately does not test a byte-identical redelivery after a strand resolves — see Open Questions.

- **rm10 — supersession and reprocessing** (`specs/rm10-supersession-reprocessing.md`, Phase E). Fills rm09's `# STUB: rm10` hook inside RL's one transaction: `supersede_batch` retires every live row sharing the reissue's `file_key` (never `source_file`) across all partitions, status-only; lineage stamped once on the retired `udr_batch` row; detects a cross-period supersede (`CROSS_PERIOD_SUPERSEDE`, WARNING) and a shrinking reissue (`SHRINKING_REISSUE`, MAJOR, via `udr_batch.parsed_count` run-N vs N-1). `tests/rating/rm10-supersession-reprocessing.integration.test.ts` static half 4/4; DB-gated half never run; `rl.py` reviewed by hand only (no `python3`). CodeRabbit pass fixed a real bug in `kestra.yml`'s `connectionString: ${VAR:-}` (bash-only syntax; worse, ANY value including `""` disables the intended Managed-Identity fallback) — moved the local-only connection string into `docker-compose.dev.yml`'s `KESTRA_CONFIGURATION` override instead; not re-verified live.

- **rm09 — RL guarded transactional load** (`specs/rm09-rl-guarded-transactional-load.md`, Phase D). Replaces the `rl` stub: inside ONE psycopg transaction — the `BILL_APPROVED` guard (`LOAD_BLOCKED_BILLED`/`REFUSED` on any live collision), the `CURRENCY_MISMATCH` assertion, the then-stubbed supersede hook, a streamed chunk-at-a-time `COPY` insert at `RATED` (post-review: never whole-batch-in-memory), and reconciliation (`parsed = rated + rejected + discarded`, imbalance → `RECON_IMBALANCE`/`FAILED`). Only after commit: copy-then-delete archive, `archive_file_path` set last, idempotent archive-only recovery. `py_compile` + a 23/23 stubbed-import harness passed; the DB-gated suite (3/7 static) never ran live. Load-bearing decisions in Open Questions: `LOOKUP_MISS` folds into `discarded_count`; terminal status is `PARTIAL` on any shortfall, not just PRP rejects; a live-row `UNIQUE` violation on `COPY` is deliberately left uncaught; `partition_period` has one source of truth (the DB CHECK).

- **rm08 — RP price resolution and snapshot** (`specs/rm08-rp-price-resolution-snapshot.md`, Phase D). Replaces the `rp` stub: one set-based as-of SQL query per chunk resolves each record through the order item's PINNED `product_offering` version, snapshots every resolved input onto the row, computes `FLAT` (quantity ignored, raw `numeric(18,6)` + rounded-once `numeric(18,2)`, fails closed above 6 significant fractional digits). An unresolved or non-scalar-priced (`tiered`) record is `LOOKUP_MISS`, not rated or fabricated. `py_compile` + a stubbed rounding harness passed; DB-gated suite (3/9 static) never ran live. **Subscriber resolution is a deliberate placeholder** (owner's 2026-08-30 plan) — the real customer→subscription→offering bridge is deferred; see Open Questions.

- **rm07 — PRP claim/validate/reject** (`specs/rm07-prp-claim-validate-reject.md`, Phase D). Replaces the `prp` stub: derives `file_key` from the filename (never content), discards a byte-identical redelivery as `DUPLICATE_BATCH` before claiming, commits the `max(batch_run_num)+1` claim before parsing, validates each row to six reason codes, quarantines rejects verbatim (original bytes + true physical line number), emits chunked Parquet + a manifest. A stubbed-import harness passed; DB-gated suite (11/22 static) never ran live. Two review-hardening passes fixed real bugs (fail-fast on an empty/remote source file without crashing the log path, a leading-blank-line header regression, bounded `DUPLICATE_IN_FILE` memory via `blake2b` digests, a loud `MALFORMED_ROW` on an unterminated quoted field instead of a silent bad split). Deviations in Open Questions: parses with stdlib `csv` (not polars — reject fidelity needs raw-line access); the usage quantity rides Parquet as an exact `Decimal` string, not a typed Arrow decimal.

- **rm06 — flow template, logging contract, log sweep** (`specs/rm06-flow-template-logging-sweep.md`). Resolved the rm04-deferred repo-boundary question directly with the user: flows stay in `enterprise-billing-app` (see Architecture Decisions). Delivered the `prp`/`rp`/`rl` `# STUB:`-marked flow template, the independent `log-sweep.yaml`/`log_sweep.py` (idempotent by rename-on-completion, per-file crash isolation, `INDETERMINATE` resolution for uncatalogued codes via `LEFT JOIN` — never `COALESCE`), `emit_terminal_log.py`, and `deploy_rating_flows`'s CI wiring. Test #16 (owned here) passes its static half; DB-gated half never run. Several review passes fixed real bugs (a missing required Webhook `key`, later superseded by rm07's file trigger; naive-datetime lines now quarantine instead of mis-bucketing `partition_period`; per-file sweep isolation so one poison file no longer starves the rest). Open, unclosed: the sweep's commit→rename window isn't fully atomic (see Open Questions), and `deploy_rating_flows`'s network path is unresolved.

- **rm05 — Entra reverse proxy / Easy Auth** (`specs/rm05-entra-reverse-proxy.md`). Puts the Kestra UI behind Container Apps Easy Auth: one `Workflow.Admin` Entra app role (assignment required), a Billing Ops group, IP allow-list, 7-year sign-in log retention, client secret in Key Vault. Delivered `easy-auth.bicep` (new), extended `rating-engine-container-app.bicep`/`main.bicep`, a standalone tenant-scoped `entra-signin-diagnostics.bicep` (deliberately not wired into the pipeline — needs tenant-level Security Administrator), and `infra/docs/engine-access.md`. All Bicep builds clean; **nothing deployed** — no Entra objects provisioned, `deployEasyAuth` defaults `false`, D10's accepted-risk sign-off is a placeholder. Deviation: `globalValidation.requireAuthentication` doesn't exist on the Container Apps API (it's an App Service property) — `unauthenticatedClientAction: RedirectToLoginPage` covers the same intent.

- **rm04 — Kestra deployment and local dev environment** (`specs/rm04-kestra-deployment-and-local-dev.md`). Architecture deviation from the spec's literal repo boundary (see Architecture Decisions). Delivered the pinned-by-digest worker Dockerfile (built + validated end-to-end with a full `uv pip compile --generate-hashes` lock), `kestra.yml`, the storage/container-app/ACR/Key-Vault Bicep modules (all gated behind `deployRatingEngine=false`), the local dev compose stack, `containerize_rating_engine`'s CI stage, and the format-agnostic `runtime/` foundation (`db.py`/`storage.py`/`transform.py`/`logemit.py`) later units build on. Several passes fixed real infra bugs (role hardening on re-run, gated MI grants, health probes on the correct Micronaut management port, a JSON-serialization crash on `Decimal`/`datetime`). **D0's actual scope — the process runner on a real ACA environment — has never been run**; the single biggest thing blocking this unit from done.

- **rm03a — `kestra` database + `kestra_engine` role** (rm03-spec §Implementation Step 9a; no dedicated spec file). Delivered `kestra-db-roles.sql`/`.ts` (idempotent `CREATE DATABASE`, the mirror-image `REVOKE CONNECT ... FROM PUBLIC` + explicit re-grant) and provisioning-order docs. Clean build; **no automated test suite ships for this unit** — its assertions were meant to fold into `grants.integration.test.ts` and never did.

- **rm03 — `rating_runtime` role, grants, the billing boundary** (`specs/rm03-rating-runtime-role-grants.md`). Delivered `rating-db-roles.sql`/`.ts` (connection-limited role, the `PUBLIC` CONNECT/EXECUTE revokes, column-scoped parent-table-only grants, the six-column `app_runtime` boundary, enumerated cross-schema `SELECT`, `SELECT`-only default privileges) and `tests/rating/grants.integration.test.ts` — 32/32 green against the disposable test Postgres, including role-attribute hardening that converges even a drifted `SUPERUSER` role on re-run.

- **rm02 — `event_catalog` seed** (`specs/rm02-event-catalog-seed.md`). Delivered `rating-event-catalog.data.ts` (16 rows + `RATING_EVENT_CODES`, side-effect-free, `ON CONFLICT DO UPDATE`) + the `db:seed-rating` runner. `tests/rating/rm02-event-catalog-seed.integration.test.ts` runs 20/20 green, including the pair a naive `COALESCE(default_severity,'INDETERMINATE')` would collapse.

- **rm01 — `rating` schema foundation** (`specs/rm01-rating-schema-foundation.md`). Delivered the hand-authored `0034_rating.sql` (schema, `rating.period_of()`, `udr_batch_seq`, all four tables + constraints + bootstrap partitions), Drizzle typing-only declarations, the FLAT-only `udr-rate-detail` discriminated union, and `rating-partman-setup.{sql,ts}` (7yr/24mo retention, DETACH). Narrowed one stale billing test guard. Full non-DB `vitest run` clean (2758/2758); the new DB-gated suite has never run against a disposable test Postgres.

## In Progress

Every unit rm03a–rm13 is **file-complete and build-hygiene-clean** (`tsc`/ESLint/Prettier, and `py_compile`/a stubbed-import harness wherever `python3` was available) but has **never been exercised against live infrastructure**: no session has had a disposable test Postgres + `python3`+`psycopg`+`polars` together, and no session has ever triggered a real Kestra flow execution. rm04's local dev stack was proven reachable once (2026-09-01, engine only — no flow run). rm05 is undeployed (no Entra objects provisioned, D10 sign-off outstanding). This is the standing gap across the whole module, not a per-unit issue.

## Next Up

- **The standing gap**: get `python3`+`psycopg`+`polars` and a disposable test Postgres into one session, then run every `tests/rating/*.integration.test.ts` DB-gated suite for the first time — this alone validates rm06-rm13's Python logic via more than manual review.
- Once rm04's local stack is up with flows deployed, exercise each unit's real end-to-end path: rm06 (trigger `ran-usage-rating` manually), rm07 (the 50k/37-bad-row headline case + the landing trigger), rm08 (the real `prp→rp` handoff + confirm `subscriber_ref_column="PUBLIC_KEY"` against a real feed), rm09 (`prp→rp→rl` reaching `COMPLETE` + a killed-mid-transaction recovery), rm10 (a real reissue superseding + a real `SHRINKING_REISSUE`), rm11 (kill a worker mid-`rl`, confirm both the startup and scheduled reconcile paths), rm13 (queue `runRatingFanoutGate=true` — the first-ever live Kestra execution for this whole module; expect first-draft bugs in the CI mechanism itself).
- rm13: confirm the `pip install --require-hashes` step in `test_scan` actually succeeds on a real hosted agent (`continueOnError` was removed on the strength of the Docker build alone, not a real ADO run); once a real engine is deployed, queue `scanRatingEngineDast=true`.
- rm04 D0: run the actual process-runner spike on a real Azure Container Apps environment — the prerequisite for `deployRatingEngine=true` and everything downstream.
- rm05: run the Entra admin provisioning steps, deploy with `deployRatingEngine=true deployEasyAuth=true`, run all 14 verification items, and obtain the real D10 accepted-risk sign-off (currently a placeholder).
- rm06: resolve the `deploy_rating_flows` gaps before ever setting `deployRatingFlows=true` — ADO-agent-to-webserver network reachability, the pipeline's Key Vault read grant, and the `kestra` CLI's real flag names.
- rm03a: verify `db:bootstrap-kestra-roles` against a real Postgres; decide whether to fold its assertions into `grants.integration.test.ts` or a new file.
- General RL-alarm clearing (rm12 follow-up, not yet scoped to a unit): six auto-clearing codes RL raises (`PARSE_FAILURE`, `LOOKUP_MISS`, `BATCH_PARTIAL`, `TASK_RETRY_OK`, `DB_WRITE_FAILURE`, `BATCH_STRANDED`) still have no code anywhere that emits their `CLEARED` row.
- Phase B: carry escalation E1 forward as a billing-module issue (the `SECURITY DEFINER` `FROM PUBLIC` revoke belongs where the pgledger functions live).

## Open Questions

- **rm13** — the migration-boundary checker identifies "a rating migration" by filename (`/rating/i`), not by parsing which schema a statement targets. Works for every migration shipped so far and the established naming convention, but would miss a hypothetically differently-named future one. Not touched by the CodeRabbit-fix pass (which widened the separate `BILLING_REFERENCE` regex, not this).
- **rm12** — (1) Clearing ownership: `completeness_check.py` clears only its own two codes; six RL-raised auto-clearing codes still have no `CLEARED` emitter (re-checked 2026-09-02, still open, not yet scoped to a unit). (2) KV config's injection-safety is resolved (values pass through `env:`, not spliced into `commands:` text) but the delimiter-scalar *format* itself remains a real limitation if a future unit needs richer per-`udr_type` config than one deadline time.
- **rm11** — (1) Possible cross-unit gap: PRP's `DUPLICATE_BATCH` dedup matches on `(file_key, checksum)` regardless of the matched row's status, so a byte-identical redelivery after a stranded batch resolves would still be discarded rather than reprocessed. rm11 only guarantees a *corrected* reissue claims cleanly; whether a byte-identical resend should also reprocess is open for whoever next revisits rm07's dedup check. (2) The "on flow start" trigger half has no confirmed Kestra OSS mechanism — relies on a `Schedule` `backfill` catch-up as a substitute, unconfirmed against v1.3.35; the 5-minute cadence bounds the worst case regardless.
- **rm10** — (1) `rl.py`'s rm10 changes were never run through the interpreter (no `python3`) — reviewed by hand only; run `py_compile` + the harness + the suite before fully trusting it. (2) Cross-period detection correlates by `udr_key` alone, not the full natural key — correct for "detect the crossing," not "reconstruct the exact predecessor→successor pairing." (3) `SHRINKING_REISSUE` compares `udr_batch.parsed_count` run-N vs N-1, not a live-row count — can't distinguish "fewer total records" from "same total, fewer distinct keys" (a duplicated-key edge case).
- **rm09** — (1) `LOOKUP_MISS` folds into `discarded_count` for the reconciliation identity — resolves the rm08 fourth-category tension. (2) Terminal status is `PARTIAL` on *any* shortfall (PRP reject or RP lookup-miss), not only PRP rejects. (3) Archive backend is Azure Blob (via MI) when `RATING_ARCHIVE_BLOB_URL` is set, else local filesystem — the Blob path is authored-not-run. (4) `partition_period` has one source of truth: Python's `period_of` proposes it, the DB CHECK is the sole authority — a Python bug is rejected at insert, never committed wrong. (5) `REFUSED`/`FAILED` exit non-zero, file stays in `landing/`; a genuinely-wrong file re-refuses every trigger cycle until an operator removes it (by design — no automated retry). (6) A live-row `UNIQUE` violation on `COPY` is deliberately left uncaught (Inv #3's backstop firing; catching it would mask the guarantee). (7) `CURRENCY_MISMATCH` is an inner join (per the spec) — a record whose `product_inventory.billing_account_id` doesn't resolve is silently not flagged; a separate data-integrity gap. (8) The shared prp/rp/rl handoff-helper extraction (rm08's action item) is still deferred — the rule-of-three is now met; do it as its own scoped change.
- **rm08** — (1) Subscriber resolution is a deliberate placeholder/shell (owner's 2026-08-30 plan): the current shell joins `--subscriber-ref-column` straight to `product_inventory.product_inventory_id`; the real design is a dedicated subscription lookup table keyed on `(PUBLIC_KEY, COMMERCIAL_UNIT, SITE)` under `party_role_specification`, closing the customer→offering grain gap (a customer can hold many subscriptions) — deferred, not built. (2) `udr_rate_detail` is validated by a Python mirror of the Zod union, not an import (Python can't import TS); the Zod union stays the source of truth for the Drizzle column type. (3) `udr_rated_price_raw` fails closed above 6 significant fractional digits (resolved — was originally deferred to RL's cast, which would have been a second silent round). (4) A resolved-but-`tiered` price (no scalar amount) surfaces as `LOOKUP_MISS`, not a new event code.
- **rm07** — (1) The landing file trigger's plugin id/properties and the chunk-size KV read are unconfirmed against v1.3.35 — same D0-spike class; a Schedule-poll+list is the fallback if the pinned image lacks a local-file trigger. (2) `RATING_USAGE_WEBHOOK_KEY` (provisioned by rm06) is now unreferenced since rm07 replaced the Webhook trigger with the file trigger — left as harmless dead wiring. (3) Deviation: parses with stdlib `csv`, not polars — reject fidelity needs raw-line access polars doesn't cleanly give. (4) Deviation: the usage quantity rides Parquet as an exact `Decimal` string, not a typed Arrow decimal. (5) `event_time_assumed_tz` defaults UTC for naive timestamps — confirm the real feed's convention with upstream. (6) `DUPLICATE_LIVE` is deliberately not this unit's (that's rm10's supersession); `UNKNOWN_SUBSCRIBER` only fires when a profile declares a subscriber-ref mapping. (7) A refuse exits non-zero so RP/RL never run; `DUPLICATE_BATCH`/a lost-claim race exits 0 with a `DISCARDED` manifest — RP/RL must no-op on any non-`PROCESSING` status (rm08 honors this).
- **rm06** — (1) `deploy_rating_flows`'s network path from the hosted ADO agent to the Kestra webserver is unresolved — every other stage goes through ARM instead; needs an infra decision before `deployRatingFlows=true`. (2) The pipeline's service-connection principal has no Key Vault read grant anywhere in Bicep. (3) The `kestra` CLI's flag names are the spec's literal text, unconfirmed against v1.3.35. (4) `log_sweep.py`'s malformed-line summary resolves to `INDETERMINATE` under an uncatalogued code — in tension with the "`INDETERMINATE` count is zero" hygiene metric; read as that metric being about catalog coverage of real component codes, not already-corrupt data — may need excluding from that count, or revisiting at rm13 (not done). (5) The sweep's commit→rename idempotency has a residual, un-closeable-in-scope window — errs toward duplicate, never data loss; fully closing it needs a durable "processed" marker table (a schema change). (6) Whether `emit_terminal_log.py`'s generic `STUB_EXECUTION_*` codes should become real per-component codes now that every stage has real failure paths is still open.
- **rm05** — (1) `globalValidation.requireAuthentication` doesn't exist on the Container Apps `authConfigs` schema (App Service property) — omitted, `RedirectToLoginPage` covers the same intent. (2) The Entra tenant sign-in-log export needs a tenant-scoped deployment + Security Administrator — shipped standalone, not wired in, documented as a manual step.
- **rm04** — (1) D0's actual process-runner spike has never been run — the single biggest open item. (2) `kestra.yml`'s Postgres/BasicAuth/Storage property names are CONFIRMED (2026-09-01 local run); Managed-Identity Blob auth is still unconfirmed (only the connectionString/Azurite path was proven) — if MI isn't supported, a 5th credential joins the deployed engine's secret set.
- **rm03a** — (1) `kestra_engine`'s `CONNECTION LIMIT 20` is an unconfirmed placeholder. (2) `grants.integration.test.ts` was never extended with rm03a's own assertions (only the docs got the SQL) — flagged, not resolved.
- **rm03** — escalation E1 (open): `PUBLIC` held `EXECUTE` on billing's four `SECURITY DEFINER` pgledger functions — any login role could post ledger transfers. rm03 closes it, but the defect predates rating and should be raised in the billing module too (own the revoke where the functions live).
- **rm01/rm02/rm03 naming quirks** (cosmetic, behavior unaffected): rm02-spec names the amendment constraint `event_catalog_severity_check`; shipped as `event_catalog_default_severity_check` (rm01's actual name). `RATING_EVENT_CODES` lives in a sibling `.data.ts` file, not the runner itself (which has a `void main().catch()` side effect) — still "alongside the seed." rm01/rm03-spec name test files without the `.integration.` infix; shipped with it so vitest's integration project (not the DB-free default) actually picks them up — flagging in case the bare name was intentional instead.

## Architecture Decisions

- **2026-08-28 — flow definitions (`flows/**`) also live in `enterprise-billing-app`.** rm06 is the first unit shipping real Kestra flows, and both the spec and `ratemgmt-ai-workflow-rules.md` repeatedly frame flows as living in a genuinely separate repository — stronger language than rm04's infra-only framing. Asked the user directly rather than guessing; chosen: keep them here, as `rating-engine/flows/`. `infra/azure-pipelines.yml` gained `deploy_rating_flows` instead of a second repo's own pipeline. Later units should keep reading "rating repo" as "the `rating-engine/` subtree of this repo."
- **2026-08-27 — rm04's infra lives in `enterprise-billing-app`, not a separate "rating repo."** Asked directly, the user clarified their notion of "rating repo" specifically means the future home of Kestra *flow definitions* (rm06+), and that repo doesn't need to exist yet. The Kestra *engine deployment* (worker image, Bicep, local compose) is infrastructure for the app's own Kestra instance, so it belongs here — landed as a `rating-engine/` subtree plus new modules under the existing `infra/bicep/modules/`, sharing one Bicep deployment graph (ACR/Key Vault/Container Apps Environment/Postgres) with the rest of `main.bicep`. A deviation from the literal spec text, recorded per ai-workflow-rules §5.5.
- **2026-08-27 — D0 encoded as a deploy gate, not just a doc note.** `main.bicep`'s `deployRatingEngine` param defaults to `false` and gates every rm04-specific resource — the files exist, compile, and are ready, but nothing deploys the engine by accident. Someone has to deliberately flip the flag, which is the natural point to also confirm the spike passed.

## Session Notes

- **2026-09-02 — tracker-repair incident.** While addressing rm12's open items, `git log` showed three commits this session had no memory of (`072a37f` rm13, `ddd71e9` rm12 CodeRabbit fixes, `8e10aad` rm13 CodeRabbit fixes) — a concurrent session or the user had pushed this work directly. `ddd71e9`'s own diff showed it reverting this file's "Current Phase"/"Current Goal" to a pre-rm13 state and deleting rm13's entire Completed/In-Progress/Next-Up/Open-Questions content, even though `072a37f` (immediately prior) had already authored it — most likely a stale-checkout collision (sequential commits don't merge-conflict). **No code or test content was lost** — only this markdown file's narrative was briefly wrong; restored from `git show ddd71e9`'s diff. **Lesson**: in a repo other sessions might also be touching, check `git log`/`git status` before trusting this file's "Current Goal" at face value, especially after any gap — this file's edits aren't commits, so nothing stops two concurrent editors from clobbering each other this way.
- **rm01 ripple**: adding the first `rating.*` migration means every `tests/**/*.integration.test.ts` file's full-migration `beforeAll`/`afterAll` now creates the `rating` schema. Added `DROP SCHEMA IF EXISTS "rating" CASCADE` before each existing `core` drop across all 71 affected files, mirroring the `customer`/`billing` precedent (repo memory: `new-pgschema-integration-test-ripple`).
