# User Management — Progress Tracker

## Status

| Unit | Name                                                          | Status |
| ---- | ------------------------------------------------------------- | ------ |
| um01 | Project Scaffold & Themed App Shell                           | Done   |
| um02 | Database Foundation & Better-Auth Identity Schema             | Done   |
| um03 | Local Sign-in + Seeded Break-glass Admin + Audit Foundation   | Done   |
| um04 | Custom Per-Account Lockout (LOCAL)                            | Done   |
| um05 | RBAC Schema + Registry/Role Seed + Admin Grant                | Done   |
| um06 | Authorization Enforcement Core                                | Done   |
| um07 | Users list/detail (READ)                                      | Done   |
| um08 | Create user (EDIT)                                            | Done   |
| um09 | Forced first-login password change (`/set-password`)          | Done   |
| um10 | Entra SSO sign-in + email-match linking                       | Done   |
| um11 | Edit user details (EDIT)                                      | Done   |
| um12 | Assign / revoke roles (EDIT)                                  | Done   |
| um13 | Disable / re-enable user (EDIT) + instant revocation          | Done   |
| um14 | Reset LOCAL password (EDIT)                                   | Done   |
| um15 | Unlock locked account (EDIT)                                  | Done   |
| um16 | Switch auth_method SSO ↔ LOCAL (EDIT) + session revocation    | Done   |
| um17 | Tombstone-delete user (DELETE) + `DeleteUserDialog`           | Done   |
| um18 | Roles list/detail (READ)                                      | Done   |
| um19 | Create & edit role (EDIT)                                     | Done   |
| um20 | Map role → permission levels (EDIT)                           | Done   |
| um21 | Delete role (DELETE)                                          | Done   |
| um22 | System Config view (READ)                                     | Done   |
| um23 | System Config edit (EDIT)                                     | Done   |
| um24 | Audit Log viewer (READ)                                       | Done   |
| um25 | Password Complexity (config + validation + temp-password gen) | Done   |
| um26 | Logout (sidebar sign-out button)                              | Done   |
| um27 | Audit Log → ULID + Range Partitioning (pg_partman) (INFRA)    | Done   |
| um28 | Side Panel & System Config (chrome + branding + locale)       | Done   |
| um29 | Configurable Business Timezone (display + boundary)           | Done   |
| um30 | Deployment, Secrets & Security Gates (INFRA)                  | Done   |

**Next:** all specced units implemented. um29 automated gates green (`tsc`, ESLint, `vitest run`, `next build`); manual `/qa` runtime pass (set `APP_TIMEZONE=Asia/Kuala_Lumpur`, click through Users/Roles/System Config/Audit Log, confirm +08 display + correct local-day filter across UTC midnight) still pending.

## Conventions (apply to every unit; specs often contradict these — codebase wins)

- **Validation files:** `validation/<entity>-<operation>.schema.ts` (not the specs' `validation/users.ts`).
- **Repositories:** every function takes the `db | tx: Database` handle as explicit first param (callers compose into their own transaction). Files never import `db/client` directly. Export style is per-file: plain named functions (`appuser`, `audit`, `lockout`, `roles`) vs namespace object (`roleAssignRepository`, `rolePermissionAssignRepository`) — each file keeps whatever its first export established.
- **Services:** read before-snapshot ahead of the transaction; run the mutation + `insertAuditEvent(...)` atomically in one `db.transaction`. Reads aren't audited.
- **Actions:** `requirePermission → schema.safeParse → service → revalidatePath('/administration/users')`; any thrown error (incl. `redirect()`'s `NEXT_REDIRECT`, gated via `isRedirectError`) maps to `{ ok:false, code:'FORBIDDEN' }` — actions are RPC-invoked, never navigations.
- **Errors:** `AppError` codes are `INTERNAL` / `FORBIDDEN` / `SERVER_ERROR` (no `INTERNAL_ERROR`). Repository "should never happen" guards throw plain `Error`.
- **Tests:** `tests/<area>/*.test.ts`; DB tests use the `*.integration.test.ts` suffix (separate `vitest.integration.config.ts`, `fileParallelism: false`, `describe.skipIf(!databaseUrl)` so missing `DATABASE_URL` loud-skips). Not the specs' `tests/unit`/`tests/integration`. Running integration suites DROPs `core`/`drizzle` — re-run `db:seed && db:seed-rbac` before manual smoke-testing on the same container.
- **Config:** `lib/config.ts` exposes fields under their raw env key (`config.DATABASE_URL`), Zod-validated, frozen, server-only. `BOOTSTRAP_ADMIN_*` live in a separate `db/seeds/seed-admin.config.ts` loader (seed-only), not `lib/config.ts`. SSO vars (`MICROSOFT_*`, `ENTRA_TENANT_ID`) are optional; `entraConfig`/`isSsoConfigured` derived from them.
- **Telemetry sink is `lib/logger.ts`** (specs' `lib/telemetry.ts` doesn't exist). Sole sanctioned `console.*` site; future GlitchTip/OTel hook.
- **`server-only` imports:** aliased to `tests/mocks/server-only.ts` (no-op) in both vitest configs.
- **React:** `react-hooks/set-state-in-effect` is enforced — never `useEffect(() => setState())`. Reset all state via a `key` prop on the parent; reset partial state by tracking the previous prop value and adjusting during render.
- **jsdom:** `tests/setup.ts` polyfills `hasPointerCapture` + `scrollIntoView` (Radix `Select` needs them).

## Stack & architecture facts

- **DB:** postgres.js + Drizzle, single `core` pgSchema (declared once in `db/schema/identity.ts`, imported by siblings). Schema files are **flat** in `db/schema/` (no `core/` subfolder, no `.schema.ts` suffix). 10 tables: `appuser`, `account`, `session`, `verification`, `audit_log`, `roles`, `permissions`, `role_permission_assign`, `role_assign`, `system_config` (um22). PK property is `id` in Drizzle (Better-Auth hardcodes `id`, not remappable); SQL columns stay snake_case. Better-Auth FK columns are `text`, not `uuid` (its id format) — `system_config.modified_by` is `text` referencing `appuser.id` for the same reason, despite um22-spec's literal SQL showing `UUID`. RBAC PKs are `uuid` `defaultRandom()`.
- **Migrations:** `0000_core.sql`, `0001_audit.sql`, `0002_rbac.sql`, `0003_roles_name_ci_unique.sql`, `0004_system_config.sql` (um22 — `npx drizzle-kit generate` named this one correctly on its own, no rename needed; the seed `INSERT` for the `app_name` row was appended by hand after the generated DDL). **um27 rewrote `0001_audit.sql` in place** (fresh-install approach — no `ALTER`/backfill; the table is born `PARTITION BY RANGE (created_datetime)`, ULID-keyed, composite-PK `(audit_id, created_datetime)`, `audit_id DEFAULT core.generate_ulid()`, plus a `pgcrypto` `CREATE EXTENSION` for `gen_random_bytes` and a single `audit_log_default` bootstrap partition). Its meta snapshots (`0001`–`0004`, each carries the `audit_log` table) were hand-edited to match — `drizzle-kit generate` reports "No schema changes" after the rewrite. The pg_partman/pg_cron provisioning (extensions, `create_parent`, retention, daily cron) is **not** a migration — it lives at `db/bootstrap/audit-partman-setup.sql` and runs via `npm run db:setup-partman` (`db/bootstrap/audit-partman-setup.ts`, reads `BOOTSTRAP_DATABASE_URL`, mirrors the role-bootstrap pattern), once per env AFTER migrate. The least-privilege role bootstrap is **not** a migration — it lives at `db/bootstrap/bootstrap-db-roles.sql` and runs via `npm run db:bootstrap-roles` (`db/bootstrap/bootstrap-db-roles.ts`, reads `BOOTSTRAP_DATABASE_URL`, a superuser/owner conn, never the app's `DATABASE_URL`). It was originally committed as `0005_bootstrap_db_roles.sql` but that placed a superuser-only, role-creating step inside the sequence the automated `migrate` stage runs as the (lower-privilege, self-created) `app_migrate` role — it only ever passed because um30 verified it as the `postgres` superuser against throwaway Docker; against the least-privilege role it `42501`-fails. Pulled out post-um30: deleted the `0005` migration + `meta/0005_snapshot.json`, removed the `idx:5` `_journal.json` entry (`drizzle-kit generate` still reports "No schema changes" — schema == `0004_snapshot.json`). Latest migration is now `0004_system_config`. `meta/_journal.json` `tag`s must match the filename or `db:migrate` breaks. `db:setup` = `db:migrate && db:seed && db:seed-rbac`. Seeds use a dedicated `postgres({max:1})` client, are idempotent, and write no `AUDIT_LOG` row (bootstrap exception).
- **Better-Auth (installed v1.6.19, explicit `@better-auth/core` dep):**
  - Field mapping in `auth/index.ts` only remaps fields whose default name differs from the Drizzle property (`createdAt`→`createdDatetime`, etc.).
  - Status gate (`ACTIVE || PENDING`) lives in `databaseHooks.session.create.before` (runs after password check). `DISABLED`/`DELETED` rejected. PENDING is allowed because first-login activation requires signing in pre-activation.
  - Lockout + SSO branch live in top-level `hooks.before`/`after` matched on `ctx.path` (the only point before password verification). Lockout bookkeeping try/catches and never rethrows; the lock-check (`before`) does propagate.
  - **Session payload carries no `status`/`force_password_change`** — pages needing those do their own `findUserById` lookup.
  - SSO: native account-linking resolves the email match itself before `account.create.before` fires; our hook only validates (reject LOCAL/DELETED). `disableSignUp: true` blocks JIT; `accountLinking.trustedProviders:['microsoft']` required (Entra omits `email_verified`); `mapProfileToUser` prefers `oid` and maps `email: email ?? preferred_username ?? upn`. No auto GET signin route — `app/api/auth/signin/microsoft/route.ts` bridges to `signInSocial(asResponse:true)`.
    - **Email-claim fallback (fix):** Better-Auth's Microsoft provider maps email from the `email` claim alone, which Entra omits for mailbox-less accounts (e.g. `*.onmicrosoft.com` test users). Without it, native email→APPUSER matching fails and `disableSignUp` rejects with the generic "not authorized" message even though a valid SSO APPUSER exists. `mapProfileToUser` now falls back to `preferred_username`/`upn` (the UPN), which is what such users are provisioned under; identity key stays `oid`.
- **AuthZ:** `auth/resolver.ts` builds the effective-permission map (highest `LEVEL_RANK` grant per `PermissionName`, no cache). `auth/guard.ts` exports `requireAuthenticated`/`requirePermission`/`resolveForcePasswordChangeSession` (all redirect, never return a Response). `app/page.tsx` is the one place that bypasses the guard layer (redirect-loop risk) and imports `db` directly — has its own `root-page` ESLint boundary carve-out. 4 permissions seeded (`users`, `roles`, `system_config`, `audit_log`), ADMIN-only matrix.
- **ESLint boundaries:** inward-only import graph (`eslint-plugin-boundaries` v6, `mode:"full"`). Client-safe carve-outs exist for single files: `auth/client.ts`, `auth/permission-constants.ts`, `auth/lockout.ts` (um15, lets `services/**` reuse `isCurrentlyLocked` instead of inlining the check). `components → actions`, `actions → services`, `services → db` are allowed.
- **UI / design tokens (`app/globals.css`):** full token system on `:root`; only a curated shadcn subset is re-exposed via `@theme inline`. Scale tokens (status/cyan/primary/neutral) have **no** Tailwind utility — reference them as `bg-[color:var(--color-success-50)]`. Scales define only `-50`/`-500`/`-700` steps (no `-300`/`-600`). The shadcn `--accent` slot maps to the magenta brand accent — override list/focus hover to `bg-muted`. shadcn CLI is v4.11 "Nova": no `Form` component (use `Field`/`FieldError` + RHF `register`), `components.json` has no `style` field. Vendor `components/ui/*` files: token wiring/consumer-side `className` overrides only, never hand-edited.

## Open items

- **ZAP PR13v2 fix (2026-07-02, branch `dev1`, IN PROGRESS):** `context/zap-reports/ZAP-PR13v2-fix-plan.md`. Re-scan after v1 cleared all original findings; two actionable alerts remain.
  - **Fix 1 (Medium, rule 10055) — `style-src 'unsafe-inline'`:** IN PROGRESS. Deliverables 1.1–1.4: replace the `audit-log-table.tsx` category swatch inline style with a Tailwind class map; move the 4 Toaster CSS custom properties from `sonner.tsx`'s `style` prop into a `.toaster {}` rule in `globals.css`; remove `'unsafe-inline'` from `style-src` in `next.config.ts`; add an ESLint `no-restricted-syntax` rule banning the `style` JSX prop.
  - **Fix 2 (Low, rule 10110) — `eval(` in a static chunk:** Deliverable 2.4 (Dockerfile `eval(` grep guard) implemented alongside Fix 1. Deliverables 2.1–2.3 require staging investigation (fetch the flagged chunk, compare BUILD_ID, decide fix-or-suppress); the working hypothesis is a stale dev build since `grep -rl "eval(" .next/static` on the current local build returns zero matches and `script-src 'self'` (no `'unsafe-eval'`) already blocks any runtime exploitation.
- **ZAP PR13 fix (2026-07-02, branch `dev1`):** `context/zap-reports/ZAP-PR13-fix-plan.md`'s 3 Medium + 7 Low findings against the ZAP baseline scan. Not a numbered um-unit — the plan spans app-wide headers plus the `/login` form, so it's tracked here rather than in the units table.
  1. **Headers (rules 10038/10020/10035/10021/10063/10037/90004), `next.config.ts`:** a single `headers()` block (source `/(.*)`) now sets `Content-Security-Policy`, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Strict-Transport-Security`, `Permissions-Policy`, `Cross-Origin-Embedder-Policy`, `Cross-Origin-Opener-Policy`, `Cross-Origin-Resource-Policy`; `poweredByHeader: false`. No nonce/`proxy.ts` involvement for CSP — the app has zero inline `<script>`, so `script-src 'self'` (plus `'unsafe-eval'` only when `NODE_ENV!=="production"`, matching Next's own CSP guide's dev-mode note) is sufficient without forcing every route into dynamic rendering. `style-src` keeps `'unsafe-inline'` because `audit-log-table.tsx` sets one inline `style` attribute (category-color swatch) — matches the plan's own suggested starting policy.
  2. **Anti-CSRF token (rule 10202), double-submit pattern:** new `lib/csrf.ts` (`generateCsrfToken`/`readCookieValue`/`csrfTokensMatch`, `node:crypto` only — no new dependency, per workflow §5.6/code-standards §1.17-"installed dependency? use it", so the plan's suggested `next-csrf` package was not added). New root `proxy.ts` — **this Next.js version (16.2) renamed `middleware.ts` to `proxy.ts`** (confirmed via `node_modules/next/dist/docs/.../file-conventions/proxy.md`, exactly the kind of breaking change AGENTS.md's header warns about) — matches on `/login` only and mints a fresh token per view: sets it as an httpOnly/`SameSite=Lax` `csrf_token` cookie on the response and forwards it as an `x-csrf-token` request header (Next's documented nonce-injection pattern) since **cookies can't be set during Server Component rendering** (only in a Server Function or Route Handler — confirmed via `node_modules/next/dist/docs/.../functions/cookies.md`), ruling out setting it directly in `login/page.tsx`. The page reads the forwarded header and passes it into `LoginForm` as `csrfToken`, which renders a visible `<input type="hidden" name="_csrf">` (what ZAP's passive rule actually checks for) and resends the same value as an `x-csrf-token` header via `authClient.signIn.email`'s second (fetch-options) argument. `auth/index.ts`'s existing `hooks.before` (already matched on `ctx.path === "/sign-in/email"` for the um04 lockout check) now compares the submitted header against the cookie _first_, throwing `AUTH_ERROR_CODES.INVALID_CSRF_TOKEN` (new code, `types/auth.ts`) on mismatch — same request-hook vantage point already documented there for running ahead of password verification. This is additive to Better-Auth's own built-in origin-check/Fetch-Metadata CSRF defense (`node_modules/better-auth/dist/api/middlewares/origin-check.mjs`, on by default) — that defense doesn't satisfy ZAP's rule 10202 because it's not a token visible in the HTML form, which is what the passive scan pattern-matches on.
  3. Verified: `tsc --noEmit` clean, ESLint clean (incl. boundaries — `lib/csrf.ts` fits the existing `lib/**` element with no new carve-out; root `proxy.ts` isn't a declared boundary element, same as `next.config.ts`/`drizzle.config.ts`, so it's unrestricted), Prettier clean, full unit suite green (934 of 934 — 6 new in `tests/lib/csrf.test.ts`), `next build` succeeds and lists `ƒ Proxy (Middleware)`. Headers confirmed via `curl -D-` (all 8 present on `/api/health`, `/login`, and the sign-in API route; no `X-Powered-By`).
  4. **Golden-path verified end-to-end (2026-07-02)** against `docker-compose.dev.yml`'s local Postgres 17 (`.env` now points at `localhost:5432`, not the old shared Tailscale dev DB): `GET /login`'s rendered `<input type="hidden" name="_csrf">` value byte-matches the `csrf_token` cookie; `POST /api/auth/sign-in/email` with a correct `Origin` header returns `403 {code:"INVALID_CSRF_TOKEN"}` when the `x-csrf-token` header is missing or doesn't match the cookie, and passes through to Better-Auth's own password check (`INVALID_EMAIL_OR_PASSWORD` for a wrong password — proving the CSRF gate itself isn't the blocker) when the token matches. A real successful sign-in wasn't attempted (no known-good credentials for the admin row already seeded in that Docker volume from an earlier session). **Unrelated hiccup hit along the way, not caused by this fix:** `docker-compose.dev.yml`'s one-shot `setup` service's `db:setup-partman` step failed (`part_config_parent_table_pkey` duplicate key) because the reused `pgdata` volume already had pg_partman provisioned — `db/bootstrap/audit-partman-setup.ts` isn't idempotent the way `db:migrate`/`db:seed` are. Worked around by starting the `app` service directly (`--no-deps`, migrate+seed had already succeeded); flagged here for whoever next touches that script, out of scope for the ZAP fix itself.
- **Post-um29/um30 cleanup (2026-06-30, branch `dev3`):** four follow-ups closed in one pass.
  1. **DST now handled** (reverses um29-spec §2.2's accepted single-offset limitation). `lib/timezone.ts`'s `localDayToUtcBounds` now resolves each bound via a new `wallClockAsUtcToInstant` two-pass helper (sample the zone offset at the wall-clock-as-UTC guess, apply it, re-sample at the refined instant for the authoritative offset) — zero new deps, still `Intl`-only. So `America/New_York`/`America/Los_Angeles`/`Australia/Sydney` audit day boundaries are correct on transition days (the old code was up to 1h off — clearest case: Sydney spring-forward day-start, whose true local midnight sits on the other side of the 02:00 switch from the guess). `getZoneOffsetMinutes` was already correct (returns the offset at the given instant); only its _usage_ was wrong. Comments in `lib/timezone.ts`/`lib/locale.ts` updated; `tests/lib/timezone.test.ts` now asserts DST transition days (Sydney spring-forward/fall-back, NY spring-forward) + a DST-reflecting `getZoneOffsetMinutes` case.
  2. **`lib/password-policy.ts` removed** (dead since `b24ffc3` decoupled `validation/password.ts` from `lib/config` — nothing imports it; grep for `from "@/lib/password-policy"` is empty). Deleted the file, its `lib-password-policy` ESLint boundary element + source rule (the `validation` boundary is now `["types","validation"]`), and the five stale `vi.mock("@/lib/password-policy", …)` calls. `tests/actions/set-password.action.test.ts` was additionally **made deterministic**: it imports `passwordPolicy` straight from `@/lib/config` (the action does too, since `b24ffc3`), so the now-removed `lib/password-policy` mock never shielded it — it only passed in the full suite when a sibling file happened to set `process.env` first (order-dependent flake). Replaced with `vi.mock("@/lib/config", …)` providing `passwordPolicy`, realizing the original "never depends on lib/config's eager env validation" intent.
  3. **SSO non-secret IDs wired into IaC** (`MICROSOFT_CLIENT_ID`, `ENTRA_TENANT_ID`). These are **public identifiers, not credentials** (the only SSO secret, `microsoft-client-secret`, stays in Key Vault), but to keep the concrete tenant/client IDs **out of git** they are deploy-time parameters, not committed values: `main.bicep` → `container-app.bicep` get `entraTenantId`/`microsoftClientId` params (empty default; emitted as plain `value` env vars only when non-empty, so an unconfigured env leaves them absent → `lib/config`'s `?? null` keeps SSO off rather than present-but-empty); the pipeline `deploy` stage appends them to `--set-env-vars` from the `um30-infra` variable group, conditionally via a bash array so an empty group value omits the var (mirrors the Bicep). The committed `*.bicepparam` deliberately do **not** hold the IDs — supply them at `az deployment group create` time / via the variable group. `bicep build` + lint/format/typecheck/tests all clean.
  4. **Entra callback URI (manual, NOT done here):** add `<APP_BASE_URL>/api/auth/callback/microsoft` to the Entra app registration's Redirect URIs or Microsoft sign-in fails with a redirect-mismatch — an Azure-portal action, can't be code-changed.
- **um13:** one live disable call returned `SERVER_ERROR` in a browser against a _shared_ dev server; an isolated direct service call returned `{ ok:true }`. Unit/integration green. Re-check on an isolated dev server.
- **um14:** test-count baseline discrepancy (measured 345 on `dev2` vs um13's quoted 328) left unreconciled — verify next unit.
- **Locked-account message discloses** ("temporarily locked") rather than um04-spec's non-disclosing wording — deliberate um03 UX, user-confirmed. `auth/index.ts` throws `code: USER_LOCKED` with that message.
- **`/administration/system-config`** (um10's Entra-ID-only page) was extended in um22 with a DB-sourced "Configuration Parameters" `ConfigTable` section above the existing Entra section; the Entra section itself is unmodified. Entra values stay env-only; only the new `system_config` table is DB-sourced.
- **`.env` was deleted from the working tree during um18** (user confirmed they have a backup copy) to avoid integration tests running destructively against the shared Tailscale dev DB (`100.112.98.112`) it pointed at. Restore it before running `next dev` or any `db:*`/`npm test` (full, incl. integration) script again.
- **Pre-existing "(um25)" comment clash — resolved in um30:** `lib/config.ts`, `.env.example`, and `db/migrate.ts` had `// ... (um25)` comments from um02/um03 anticipating that um25 would be the "secrets sourced from Azure Key Vault via Managed Identity" unit. um30 is that actual unit, so those comments were updated to reference `(um30)` instead.
- **Resolved during um29** (were the two long-standing pre-existing failures): both were stale tests contradicting deliberate source, fixed by aligning the test to the codebase ("codebase wins"). (1) `tests/app/no-access-page.test.tsx` asserted the old "any modules yet" copy after `app/(admin)/no-access/page.tsx` was changed (uncommitted, pre-dates um25) to "this module yet" — test updated to match. (2) `tests/app/admin-layout.test.tsx` asserted the whole footer is omitted when `getCurrentUserIdentity()` resolves `null`, but um28's `admin-sidebar.tsx` (`b516ac9`) deliberately renders the Sign-out action **always** (only the identity strip is gated) — test updated to assert sign-out present + identity strip absent. Full unit suite now **920/920 green**.
- **Pre-existing integration drift from um27's partitioning (surfaced during um28's throwaway-Docker run):** `tests/db/migration.integration.test.ts`'s exact-table-list assertion (`expect …toEqual([10 tables])`) and the appuser-FK-count assertion (`expect(fks).toHaveLength(6)`) fail on a fresh PG16 `migrate` because um27's `audit_log_default` DEFAULT partition appears in `information_schema.tables` (→ 11 tables) and its FK to `appuser` is duplicated on the partition (→ 13 FKs). Not caused by um28 (0005 adds no table and no appuser FK — those two queries return identical results with/without 0005); left unfixed (out of scope per workflow §2.5). Fix when next touching the audit/migration tests: exclude partition children (e.g. `AND c.relispartition = false`, or filter out `audit_log_default`) from both assertions.
- **`.env` still points at the shared Tailscale dev DB** (`100.112.98.112`) — unchanged since um18. um30's DB work (bootstrap-roles migration, integration suite re-run) was verified against throwaway local Docker Postgres containers created and destroyed for this unit only; the shared dev DB was never touched.

## Per-unit specs

All under `context/user-management/specs/`. um01–um15 implemented; deviation/verification detail recoverable from git history if needed.

- um01 `um01-spec.md` — tooling + themed shell (Next 16.2, React 19, Tailwind v4, shadcn, ESLint boundaries, Vitest, `infra/azure-pipelines.yml`).
- um02 `um02-db-foundation-identity-schema.md` — Drizzle + 4 identity tables.
- um03 `um03-local-signin-admin-seed-audit.md` — credential provider, `/login`, `audit_log`, break-glass admin, `LOCAL_LOGIN` audit.
- um04 `um04-lockout.md` — per-account lockout state machine (5 fails → 15 min lock + `USER_LOCKED`).
- um05 `um05-rbac-schema-seed.md` — 4 RBAC tables, registry/role seed, ADMIN grant.
- um06 `um06-spec.md` — permission resolver, page guards, `/no-access`, root redirect.
- um07 `um07-spec.md` — `/administration/users` read (table/detail, badges, `AdminNav`).
- um08 `um08-spec.md` — create user (+ Rev 1: Auth Method dropdown, field reorder).
- um09 `um09-spec.md` — `/set-password` + PENDING→ACTIVE activation.
- um10 `um10-spec.md` — Entra SSO + email-match linking + minimal system-config page.
- um11 `um11-spec.md` — inline edit user name/phone.
- um12 `um12-spec.md` — assign/revoke roles + last-ADMIN guard.
- um13 `um13-spec.md` — disable/enable + instant session revocation + last-admin guard.
- um14 `um14-spec.md` — admin LOCAL password reset + one-time temp-password reveal.
- um15 `um15-spec.md` — admin unlock locked account (`USER_UNLOCKED`) + `Unlock` button/dialog on `UserDetail`.
- um16 `um16-spec.md` — switch `auth_method` SSO↔LOCAL (`USER_AUTH_METHOD_CHANGED`) + instant session revocation; SSO→LOCAL reveals one-time temp password, LOCAL→SSO clears credential + lockout. `Switch to …` control in the `UserDetail` Auth Method row + confirm dialog with self-switch warning.
- um17 `um17-spec.md` — tombstone-delete DISABLED users behind `users:DELETE` (`USER_DELETED`); atomically sets `status=DELETED`, strips `role_assign` + `account` rows + residual sessions, preserves the `appuser` row (no physical delete), frees email/Entra identity for reuse via the um02 partial unique index. New `DeleteUserDialog` (standalone, `AlertDialog`) + filled-danger "Delete user" header button on `UserDetail`; muted "· Deleted" header for DELETED users. Deviations from spec (codebase wins): repo file is `appuser.repository.ts` and `getUserRoleNames(db, userId)` takes the `db` handle (no module-`db`); audit writer is `insertAuditEvent`; validation is `validation/delete-user.schema.ts`; action maps redirect→FORBIDDEN via `isRedirectError` (no NEXT_REDIRECT re-throw); button/Alert use existing tokens (`--color-danger-500/700`, no `-600`; warning Alert via inline classes, no `variant="warning"`); dialog error-reset uses reset-during-render (tracks prev `isOpen`), not a `useEffect` (enforced `react-hooks/set-state-in-effect`); `isDeleted` cross-user reset relies on the page `key` remount. No migration needed — partial index already in `0000_core.sql`. Repository unit coverage folded into the integration test (codebase has no mocked-db repo tests).
- um18 `um18-spec.md` — read-only `/administration/roles` (`RoleTable` + `RoleDetail`, URL-driven via `?roleId`), `PermissionLevelTag` chip (READ/EDIT/DELETE → info/warning/danger tokens), permission matrix in the detail panel. Deviations from spec (codebase wins): `services/roles/roles-read.service.ts` already existed (um07/um08's `listRoles()`) — `getAllRolesWithMappings`/`getRoleWithMappings` were added to that file, not a new one; `rolesRepository.findById` reuses the existing `findRoleById` (already did exactly this, from um12) and only `findAll` (full `Role[]`, ordered) was added alongside the existing partial-projection `findAllRoles`; `rolePermissionAssignRepository.findMappingsForRole` added without touching um06's `findGrantsByRoleIds`, per spec. No separate `tests/integration/app/roles-page-guard.test.ts` — mirrored um07's split instead: a mocked guard-call assertion in `tests/app/roles-page.test.tsx`, real route×level redirect behavior already covered by `tests/auth/guard.integration.test.ts` (which already exercises `PERMISSIONS.ROLES` at all three levels). No Tooltip component is installed — the disabled "Add Role" stub button uses a native `title` attribute for "Feature coming soon". `AdminNav`'s "Roles" link (added in um07) needed no changes — it already pointed at `/administration/roles`. Test paths follow the codebase convention (`tests/components/`, `tests/services/`, `tests/db/`, `tests/app/`, `.integration.test.ts` suffix), not the spec's `tests/unit`/`tests/integration` tree. Verified: `tsc --noEmit` clean, ESLint clean, full unit suite green (527 tests), full integration suite green (168 tests, run against a throwaway local Docker Postgres since `.env`'s `DATABASE_URL` pointed at the shared Tailscale dev container).
- um19 `um19-spec.md` — create-role dialog (`CreateRoleDialog`, mirrors um08's `CreateUserDialog` pattern, no success screen — closes immediately, toasts, navigates to `?roleId=<new>`) and inline edit-role flow on `RoleDetail` (mirrors um11's edit-user pattern: `mode: 'view'|'edit'` toggle, Permissions matrix stays read-only and visible in both modes). Shared `RoleForm` (Role Name + optional Description). `findRoleByName` (case-insensitive `ilike`), `insertRole`, `updateRoleNameDescr` added to `roles.repository.ts`; new `services/roles/roles-write.service.ts` (`createRole`/`updateRole`, no-op short-circuit on unchanged edit); new `actions/roles/create-role.action.ts` / `update-role.action.ts`. Deviations from spec (codebase wins): validation files are `validation/create-role.schema.ts` / `validation/update-role.schema.ts` (not the spec's single `validation/roles.ts`), with `editRoleFieldsSchema = updateRoleSchema.omit({roleId:true})` mirroring `editUserDetailsFieldsSchema`; `RoleForm` uses the project's `Field`/`FieldError`/`FieldGroup` + RHF `register` (no shadcn `Form` component, per ui-context); actions gate `FORBIDDEN` via `isRedirectError` (matches `assignRoleAction`/`disableUserAction`, not `updateUserDetailsAction`'s bare-catch outlier); `RoleDetail`'s edit-mode/error-state reset on role-change relies on the page's `key={selectedRoleId ?? "none"}` remount (enforced `react-hooks/set-state-in-effect` forbids a reset-on-prop-change `useEffect`), mirroring um17's `isDeleted` reset — not the spec's implied per-prop `useEffect`. Added `components/ui/textarea.tsx` via `npx shadcn add textarea` (none existed yet) for the Description field. Verified: `tsc --noEmit` clean, ESLint clean, full unit suite green (602 tests), full integration suite green (181 tests, run against a throwaway local Docker Postgres since `.env`'s `DATABASE_URL` pointed at the shared Tailscale dev container).
- um20 `um20-spec.md` — interactive `PermissionMatrixEditor` on `RoleDetail` (per-permission `—`/READ/EDIT/DELETE button group, `useOptimistic` + per-row saving state, immediate save on click via `setPermissionMappingAction`) replacing the um18 read-only matrix whenever the actor has `roles:EDIT`; `audit_log` READ-max enforced at three layers (disabled+titled buttons, action passthrough, service guard first-check). `permissionsRepository.findByName` implemented (was a stub since um06); `rolePermissionAssignRepository` extended with `upsertRolePermission`/`deleteRolePermission` (`onConflictDoUpdate` on the `role_permission_assign_role_permission_unique` constraint); `roles-write.service.ts` extended with `setRolePermissionLevel` (audit_log guard → role lookup → permission lookup → before-snapshot → no-op short-circuit → transactional write + `PERMISSION_MAPPING_CHANGED` audit event); new `actions/roles/set-permission-level.action.ts`. Deviations from spec (codebase wins): validation lives in its own `validation/set-permission-level.schema.ts` (not the spec's single `validation/roles.ts`, matching the create-role/update-role file-per-schema convention); all repository functions (incl. `permissionsRepository.findByName`) take the `db`/`tx: Database` handle as an explicit first param, per the established repository convention, rather than the spec's no-handle/`tx`-only signatures; `actions/roles/set-permission-level.action.ts` masks the internal `PERMISSION_NOT_FOUND` service code to `SERVER_ERROR` exactly as specified. `RoleDetail`'s existing read-only matrix branch is preserved unchanged for `roles:READ`-only actors; `PermissionMatrixEditor` mounts instead of it (never both) whenever `roles:EDIT` is granted, independent of the um19 name/description edit-mode toggle. No new `PERMISSIONS` rows, no schema migrations — both RBAC tables and the unique constraint already existed from um05. Verified: `tsc --noEmit` clean, ESLint clean, full unit suite green (652 tests), full integration suite green (198 tests, run against a throwaway local Docker Postgres since `.env` is currently absent from the working tree — see Open Items).
- um21 `um21-spec.md` — `roles:DELETE`-gated role deletion: ghost danger "Delete" button in `RoleDetail`'s header (between Edit and ×), disabled+titled for seeded roles (defense-in-depth — the action-level guard also blocks them); `DeleteRoleDialog` (`AlertDialog`, mirrors um17's `DeleteUserDialog` pattern — parent-owned open state, reset-during-render on the `isOpen` false→true edge, plain `useState`/`async`+`try/finally`, not `useTransition`) shows an inline destructive `Alert` for `ROLE_IN_USE` (pluralized assigned-count message)/`ROLE_NOT_FOUND`/`SEEDED_ROLE`/`FORBIDDEN`/`SERVER_ERROR` and stays open; on success calls `onOpenChange(false)` then `onSuccess`, and `RoleDetail`'s `onSuccess` does `router.push('/administration/roles')` + `toast.success('Role deleted.')` (the role row is hard-deleted, so the panel can't stay open showing it — unlike um17's tombstone, where the panel stays and shows a muted "Deleted" header). New `isSeededRole` pure helper in `types/rbac.ts` (alongside the already-present `SEEDED_ROLE_NAMES`); `rolesRepository.deleteRoleById`, `rolePermissionAssignRepository.deleteMappingsForRole`, `roleAssignRepository.countByRoleId` (counts _all_ `role_assign` rows for a role, unlike um12's status-filtered `countNonDeletedUsersWithRole`) added to the existing namespace-object repositories; `roles-write.service.ts` extended with `deleteRole` (not-found → seeded → in-use checks run ahead of the transaction, mirroring `updateRole`; mapping-delete-then-role-delete + `ROLE_DELETED` audit write run inside one `db.transaction`, FK order); new `actions/roles/delete-role.action.ts`. Deviations from spec (codebase wins, consistent with um18-20's documented pattern): no separate `DrizzleTransaction` type was introduced — the existing `Database` union type (`db/client.ts`) already covers both the pool client and a `tx` handle, so no repository signatures changed; `deleteRole`'s result type (`DeleteRoleResult`) is defined and exported directly in `roles-write.service.ts` (matching `CreateRoleResult`/`UpdateRoleResult`/`SetRolePermissionLevelResult`), not in `types/roles.ts`, and excludes `FORBIDDEN`/`SERVER_ERROR` — those are action-boundary concerns added only to `DeleteRoleActionResult`, with `SERVER_ERROR` produced by letting service errors propagate to the action's `try/catch` (no internal `try/catch`+`SERVER_ERROR` in the service itself); validation is its own `validation/delete-role.schema.ts` file (not the spec's single `validation/roles.ts`); the danger-ghost button styling reuses the existing `--color-danger-500`/`-700`/`-50` ghost-button pattern already established for "Disable" in `UserDetail` rather than the spec's literal classes, which referenced a nonexistent `--color-danger-200` token (the danger scale only defines `-50`/`-500`/`-700`) and the neutral `--border-default` token; `DeleteRoleDialog` is always mounted in `RoleDetail` (Radix `AlertDialog` itself unmounts its content when `open=false`) rather than spec's conditional-mount-on-`isDeleteOpen`, matching `DeleteUserDialog`'s established pattern. Test paths follow codebase convention (`tests/types/rbac.test.ts`, `tests/validation/delete-role.schema.test.ts`, `tests/services/roles-write.service.test.ts` extended, `tests/actions/delete-role.action.test.ts` + `.integration.test.ts`, `tests/components/delete-role-dialog.test.tsx`, `tests/components/role-detail.test.tsx` extended, `tests/db/roles-repository.integration.test.ts` extended with a `um21:` describe block covering the two new repos). No separate `roles-delete.service.integration.test.ts` or `roles-delete-guard.test.ts` — folded into the single `delete-role.action.integration.test.ts`, mirroring um18's analogous split-test deviation. Verified: `tsc --noEmit` clean, ESLint clean, full unit suite green (696 tests), full integration suite green (212 tests, run against a throwaway local Docker Postgres container since `.env` is still absent from the working tree — see Open Items).
- um22 `um22-spec.md` — adds the `system_config` table (just-in-time migration `0004_system_config.sql` + a single seeded `app` / `app_name` row) and a `system_config:READ`-gated "Configuration Parameters" section on the existing `/administration/system-config` page (um10), above the unmodified Entra ID Settings section. New `ConfigTable` (groups rows by `config_group`, group-header `<tr colSpan={4}>`, value truncation >80 chars + `title`, URI values in `font-mono`, RETIRED rows `opacity-60`, centered empty-state card with a `Settings` icon) and `ConfigStatusBadge` (ACTIVE/DRAFT/RETIRED → success/info/neutral tokens, mirrors `PermissionLevelTag`'s `cva` pattern) in `components/system-config/`; `db/schema/system-config.ts` (`systemConfig` Drizzle table); `db/repositories/system-config.repository.ts` (`findAllNonSecret` — left-joins `appuser` for `modifiedByName`, excludes `is_secret=TRUE`, all statuses included); `services/system-config/system-config-read.service.ts` (`getSystemConfigParams`, thin delegation); `groupConfigRows`/`formatRelativeTime` added to `lib/formatters.ts`; `types/system-config.ts` (`ConfigStatus`, `SystemConfigDisplayRow`, `SystemConfigGroup`). Deviations from spec (codebase wins): schema file is `db/schema/system-config.ts`, not `system-config.schema.ts` (no `.schema.ts` suffix anywhere in `db/schema/`); `system_config.modified_by` is `text` referencing `appuser.id`, not `uuid` as the spec's literal SQL showed — Better-Auth ids are `text`, mirroring `role_assign.assigned_by`; `groupConfigRows`/`formatRelativeTime` went into the already-existing `lib/formatters.ts` (the spec's `lib/format.ts` doesn't exist in this codebase) rather than a new `lib/group-config-rows.ts`; no `PageHeader`/breadcrumb component exists anywhere in the codebase (Users/Roles pages have no page-level title either) — the page renders a plain `<h1>` instead of inventing that component for this one caller; no mocked-db repository unit tests (`tests/db/system-config-repository.integration.test.ts` is the sole repository coverage), matching the um17/um18-documented "codebase has no mocked-db repo tests" precedent; the page guard test stays guard-level-only (asserts `requirePermission` + that `getSystemConfigParams` is/isn't called), matching um07/um18/um21's precedent over the spec's fuller render-assertion list. `npx drizzle-kit generate` named the migration `0004_system_config` correctly on its own (no rename needed, unlike um02-04's auto-name renames); the seed `INSERT` was appended by hand after the generated `CREATE TABLE`/FK/index DDL. Extended `tests/db/migration.integration.test.ts`'s exact-table-list and appuser-FK-count assertions to include `system_config`/`system_config.modified_by SET NULL`. Verified: `tsc --noEmit` clean, ESLint clean, Prettier clean, full unit suite green (721 tests), full integration suite green (219 tests, run against a throwaway local Docker Postgres container since `.env` is still absent from the working tree — see Open Items).
- um23 `um23-spec.md` — `system_config:EDIT`-gated value editing via a new Actions column on `ConfigTable` (each row gets a ghost `Pencil` icon button opening `ConfigEditDialog`, a `Dialog`-based Client Component leaf with a read-only Group/Key block, a `font-mono` `Textarea`, and Cancel/Save changes footer). `systemConfigRepository` extended with `findById` (same join shape as `findAllNonSecret`, no `is_secret` filter — returns any row) and `updateValue` (sets `config_value`/`modified_by`/`last_modified_datetime`); new `services/system-config/system-config-write.service.ts` (`updateConfigValue`: not-found → secret-row guard → before-snapshot → transactional value write + `SYSTEM_CONFIG_CHANGED` audit event, mirroring `updateRole`'s shape); new `actions/system-config/update-config.action.ts` (`requirePermission` → `safeParse` → service → `revalidatePath`, mirroring `updateRoleAction`/`deleteRoleAction`'s `isRedirectError`-gated `FORBIDDEN` mapping). Deviations from spec (codebase wins, consistent with um18-22's documented pattern): the service-level result type (`UpdateConfigResult`, `{ok:true} | NOT_FOUND | SECRET_ROW`) is defined and exported directly in `system-config-write.service.ts` — not as a `FORBIDDEN`/`SERVER_ERROR`-inclusive type in `types/system-config.ts` as the spec's §23.1 literally shows — matching `DeleteRoleResult`'s documented um21 precedent (`types/roles.ts` never holds result types either); `FORBIDDEN`/`VALIDATION_ERROR`/`SERVER_ERROR` are action-boundary concerns added only to `UpdateConfigActionResult` in the action file, with `SERVER_ERROR` produced by letting a thrown repository error propagate to the action's `try/catch` (no internal `try/catch` in the service); the action checks `requirePermission` _before_ `safeParse` (spec's §23.5 pseudocode parses first) — matches the tracker's documented `requirePermission → schema.safeParse → service → revalidatePath` convention used by every other action; validation is its own `validation/update-config.schema.ts` file (not the spec's single `validation/system-config.ts`); `targetEntity` on the audit event is `"SYSTEM_CONFIG"` (uppercase), matching the established `"ROLES"`/`"APPUSER"`/`"ROLE_ASSIGN"` convention, not the spec's lowercase `'system_config'`; `ConfigEditDialog` uses the project's `Field`/`FieldLabel`/`FieldError` + RHF `register` (no shadcn `Form` component, per ui-context, matching `RoleForm`) and a plain `useState`/`async`+`try/finally` submit flow (matching `CreateRoleDialog`/`DeleteRoleDialog`) rather than the spec's `useTransition` — no other component in the codebase uses `useTransition`; the inline error `Alert` uses `variant="destructive"` (the only two variants `Alert` defines are `default`/`destructive`) rather than the spec's literal `--color-danger-50/200/700` classes, which (per um21's already-documented finding) reference a nonexistent `--color-danger-200` token; `ConfigTable`'s new `canEdit` prop is optional with a `false` default (not the spec's required `canEdit: boolean`) so the pre-existing um22 test calls that omit it keep compiling — additive, non-breaking. Repository coverage for `findById`/`updateValue` was folded into the existing `tests/db/system-config-repository.integration.test.ts` (no mocked-db repo tests, matching the um17/um18/um22 precedent). No new `PERMISSIONS` rows or schema migrations — `system_config:EDIT` already existed as a grantable level from um05, and `SYSTEM_CONFIG_CHANGED` was already present in `types/audit.ts`'s `AUDIT_EVENT_TYPES`. Verified: `tsc --noEmit` clean, ESLint clean, Prettier clean, full unit suite green (764 tests), full integration suite green (230 tests, run against a throwaway local Docker Postgres container since `.env` is still absent from the working tree — see Open Items).
- um24 `um24-spec.md` — read-only `audit_log:READ`-gated `/administration/audit-log` page: filter bar (event type `<select>` with `<optgroup>` per category, actor `<select>`, From/To date inputs, Apply/Clear) driving server-side filtering + offset pagination (fixed 50/page) via URL search params; `AuditLogTable` renders a category-colored left-border strip + `AuditEventCategoryBadge` pill per row, with an expandable detail row showing formatted Before/After JSON. New `db/repositories/audit-log.repository.ts` (`auditLogRepository.findFiltered` — `count()` + a left-joined, offset-paginated select sharing one `and()`-combined WHERE clause; `findActors` — `selectDistinct` left-joined for the Actor dropdown, `asc(userName)` relying on Postgres's default NULLS-LAST to put tombstoned actors last); `services/audit-log/audit-log-read.service.ts` (`getAuditLog` converts the validated date strings to UTC day-boundary `Date`s before calling the repository; `getAuditLogActors` thin delegation); `validation/audit-log-filters.schema.ts` (`auditLogSearchParamsSchema`, every field `.catch()`s to its default — `null`/`1` — so a tampered URL never throws); `components/audit-log/` (`AuditEventCategoryBadge` Server Component, `AuditLogFilters`/`AuditLogTable`/`AuditLogPagination` Client Components). Deviations from spec (codebase wins, consistent with um18-23's documented pattern): `types/audit-log.ts` reuses the existing `AuditEventType`/`AUDIT_EVENT_TYPES` from `types/audit.ts` (only adding `AuditEventCategory`/`AUDIT_EVENT_CATEGORY_MAP`/row types) rather than the spec's literal §24.1 redefining its own 20-member union from scratch; `AuditLogRow.actorUserId`/`targetEntity`/`targetId` are typed `string | null` (not the spec's non-null `string`) to match the schema's actual nullability (`audit_log.actor_user_id` has `onDelete: "set null"`, `target_entity`/`target_id` have no `.notNull()`) even though every current write path supplies non-null values; validation file is `validation/audit-log-filters.schema.ts` (not the spec's single implied `validation/audit-log.ts`), matching the file-per-schema convention; `auditLogRepository`/`AuditLogFilters`'s `EVENT_TYPE_OPTIONS` derive the category→events grouping programmatically from `AUDIT_EVENT_CATEGORY_MAP` rather than hand-listing all 20 event types a second time, so the two structures can't drift out of sync; page metadata title is `"Audit Log — Enterprise Billing"` (the established `"<Page> — Enterprise Billing"` pattern from every other admin page) not the spec's literal `"Audit Log | Administration"`; the page renders a plain `<h1>` (no `PageHeader`/breadcrumb component exists anywhere in the codebase, per um22's already-documented finding). AdminNav's "Audit Log" link (added back in um07) needed no changes — it already pointed at `/administration/audit-log`. No mocked-db repository unit tests — `tests/db/audit-log-repository.integration.test.ts` is the sole repository coverage, matching the um17/um18/um22/um23 precedent; the page guard test stays guard-level-only (`tests/app/audit-log-page.test.tsx`), matching um07/um18/um21/um22's precedent. No new `PERMISSIONS` rows, no schema migrations, no Server Actions — `audit_log` is READ-max (Invariant #11) and was seeded in um05; `AUDIT_LOG` itself was created in um03. Verified: `tsc --noEmit` clean, ESLint clean, Prettier clean, full unit suite green (816 tests), full integration suite green (240 tests, run against a throwaway local Docker Postgres container since `.env` is still absent from the working tree — see Open Items).
- um25 `um25-password-complexity.md` — env-configurable LOCAL password policy (`PASSWORD_MIN_LENGTH` default 15, `PASSWORD_REQUIRE_{UPPERCASE,LOWERCASE,NUMBER,SPECIAL}` default `true`, `PASSWORD_SPECIAL_CHARS` default OWASP-style set), enforced uniformly across first-login change (um09), admin reset (um14), create-user temp password (um08), and SSO→LOCAL switch temp password (um16). `lib/config.ts` parses the six `PASSWORD_*` vars (boolean coercion via a `z.enum(["true","false"])` helper so a malformed value throws loud, not silently defaults) and exports a frozen `passwordPolicy: PasswordPolicy` (`types/password.ts`); `validation/password.ts` exports `buildPasswordSchema(policy)` (`.min()` + one `superRefine` collecting every failing rule's issue in a single pass — verified empirically against the installed Zod v4 that a base `.min()` failure does not abort the chained `superRefine`, so all violations surface together) and `defaultPasswordSchema = buildPasswordSchema(passwordPolicy)`; `services/password.ts` exports `generateTempPassword(policy)` (one char from each enabled required pool + fill to `minLength` from the union + Fisher-Yates shuffle, `crypto.randomInt` exclusively, no `Math.random`). Three existing temp-password call sites in `services/users/users-write.service.ts` (`createUser`, `resetLocalPassword`, `switchAuthMethod`) now call `generateTempPassword(passwordPolicy)` instead of the old fixed-format `lib/temp-password.ts` generator (removed — that file now exports only `hashTempPassword`, unchanged). `validation/set-password.schema.ts`'s `newPassword` field is now `defaultPasswordSchema` (the old ad-hoc `.min(12).max(128)` check is gone, per the spec's explicit "replace the ad-hoc check" instruction — no separate max-length guard re-added). `reset-password`/`create-user`/`switch-auth-method` needed **no** inbound-schema change: confirmed by reading their actual `validation/*.schema.ts` files that the temp password is always server-generated on those three flows (never admin-supplied), matching the spec's documented fallback ("if um08 auto-generates it, no inbound validation is needed"). `SetPasswordForm` now sets `criteriaMode: "all"` on `useForm` and flattens RHF's `error.types` (or a server `VALIDATION_ERROR`'s full message array, set via `setError(field, { types: { server: messages } })`) into `FieldError`'s existing multi-message array-render path, so every violated rule shows at once from both client and server validation; an optional policy-rules hint (`formatPasswordPolicyHints`, `lib/formatters.ts`) is passed from the server-rendered `/set-password` page as a `passwordPolicyHints` prop. Deviations from spec (codebase wins, consistent with um18-24's documented pattern): the generator lives in `services/password.ts` per the spec's literal file path even though `lib/temp-password.ts` previously held an equivalent function — required adding a `"services" -> "services"` self-import boundary rule (`eslint.config.mjs`) so `users-write.service.ts` can import it, mirroring the precedent set by `auth/lockout.ts`'s `"auth" -> "auth"` self-import; similarly `validation/password.ts` needed `passwordPolicy` from `lib/config.ts`, which the existing `"validation" -> ["types"]`-only boundary rule disallows (validation files are deliberately zod-only/framework-agnostic) — resolved with a new narrow carve-out element `lib/password-policy.ts` (pattern-matched ahead of the general `"lib"` type, mirroring the `auth-permission-constants` carve-out shape) that only re-exports `passwordPolicy`, plus a `"validation" -> "validation"` self-import rule so `set-password.schema.ts` can import `defaultPasswordSchema`. Every unit test that transitively imports the real `users-write.service.ts` or the real `validation/password.ts` chain now needs an explicit `vi.mock("@/lib/config", ...)` or `vi.mock("@/lib/password-policy", ...)` (mirroring the established `vi.mock("@/db/client", ...)` pattern used to dodge `lib/config`'s eager env-validation-at-import) — added to `tests/services/users-write.service.test.ts`, `tests/app/users-page.test.tsx`, `tests/validation/set-password.schema.test.ts`, `tests/components/set-password-form.test.tsx`, and `tests/actions/set-password.action.test.ts`. No new `PERMISSIONS` rows, no schema migrations — this is a pure env/validation/generation unit, no DB or RBAC surface. Verified: `tsc --noEmit` clean, ESLint clean (incl. the two new boundary self-import rules), Prettier clean, full unit suite green (838 of 839 tests — the one failure, `tests/app/no-access-page.test.tsx`, is pre-existing/unrelated, see Open Items). Integration suite not re-run this unit (no DB/schema changes; `.env` still absent from the working tree — see Open Items).
- um26 `um26-spec.md` — sidebar footer (identity strip + sign-out) pinned to the bottom of the existing um07 admin `<aside>`: a `border-t` divider, a read-only `user_name`/`user_email` strip (both `truncate`), a second divider, then the sign-out control. `AdminNav` is now wrapped in a `flex-1 overflow-y-auto` div so the `mt-auto` footer stays pinned. Plus (user request, beyond spec) a **128-character cap on the password change input**: `.max(128, "Password must be at most 128 characters.")` added to `buildPasswordSchema`'s base string chain in `validation/password.ts` (the password policy's natural home for length bounds; the chained `.superRefine` still runs after a `.max` failure in Zod v4, so an over-long value surfaces every complexity violation alongside it — and server-generated temp passwords at `minLength` are unaffected) + `maxLength={128}` on both `<Input>`s in `SetPasswordForm`. Deviations from spec (codebase wins): **Option B, not the spec-preferred Option A** — the existing `components/sign-out-button.tsx` is a shadcn `Button` wrapper whose `variant` prop is already shadcn's `VariantProps`, so the spec's `variant?: 'light' | 'nav'` union would be a conflicting breaking change; instead added a separate dark-surface `components/nav-sign-out-button.tsx` (raw `<button>`, `LogOut` icon, `aria-label="Sign out"`, "Signing out…" pending label + `Loader2`, same `authClient.signOut()` + `/login` redirect mechanism), leaving the light `/no-access` `SignOutButton` untouched (zero regression). The spec assumed `(admin)/layout.tsx` already resolves the current user for a permission guard — **it does not** (each child page guards itself; the layout ran no auth check), so identity is resolved via a new read-only `getCurrentUserIdentity()` helper in `auth/guard.ts` that returns `{ userName, userEmail } | null` (never redirects, so it can't change the layout's redirect behavior) — this keeps `db/**` out of `layout.tsx` (the checklist's real intent) but does add one DB read the spec's "no new DB query" line assumed away (unavoidable given the layout had no resolved user); the layout is now an `async` Server Component, footer rendered only when identity resolves. Used codebase token forms over the spec's literals: `border-[color:var(--text-on-brand)]/10` (not `border-white/10`), `text-[color:var(--color-primary-300)]`/`hover:bg-[color:var(--color-primary-700)]` via the `[color:var(--…)]` arbitrary-value form AdminNav already uses (both primary steps exist in `globals.css` — the tracker's "no -300 step" note is about other scales, not `primary`), and `focus-visible:[box-shadow:var(--focus-ring)]` (matching sibling AdminNav links) rather than the spec's `ring-2 ring-[--border-focus]`. No new Server Actions, `PERMISSIONS` rows, migrations, or audit event (`USER_LOGOUT` is intentionally not in the event set). Test paths follow codebase convention: new `tests/components/nav-sign-out-button.test.tsx` (default render, dark-token classes, pending state, sign-out→`/login`) and `tests/app/admin-layout.test.tsx` (footer name/email, truncate, sign-out button, all four nav links, footer omitted when identity is `null`) — not the spec's `tests/unit/…` tree; max-length coverage added to `tests/validation/password.test.ts` (reject 129 / accept exactly 128). Verified: `tsc --noEmit` clean, ESLint clean, Prettier clean, affected suites green (37 tests across the 5 touched/related files). The pre-existing unrelated `tests/app/no-access-page.test.tsx` failure (see Open Items) is untouched. No DB/schema changes, so the integration suite was not re-run (`.env` still absent — see Open Items).
- um27 `um27-audit-log-ulid-partitioning.md` — converts `core.audit_log` to a `PARTITION BY RANGE (created_datetime)`, ULID-keyed table and stands up pg_partman + pg_cron so future monthly partitions are pre-created and out-of-retention (7-year) partitions are dropped, with zero manual partition DDL. **Fresh-install rewrite, no data to preserve.** `db/schema/audit.ts`: `auditId` default is now `core.generate_ulid()` (was `.defaultRandom()`), PK is now composite `primaryKey({ columns: [auditId, createdDatetime] })` (Postgres requires the partition key in every PK on a partitioned table); partitioning itself is annotated, not declarable in Drizzle. `db/migrations/0001_audit.sql` **rewritten in place** (not supplemented): a `core.generate_ulid()` plpgsql function (48-bit ms `clock_timestamp` prefix + 80-bit `gen_random_bytes(10)`, `encode(...,'hex')::uuid`), the `PARTITION BY RANGE` parent with composite PK `audit_log_pkey (audit_id, created_datetime)`, the FK + 3 secondary indexes on the parent, and a single `audit_log_default` DEFAULT bootstrap partition. New provisioning script `db/bootstrap/audit-partman-setup.sql` (+ `.ts` runner, `npm run db:setup-partman`): `CREATE EXTENSION pg_partman/pg_cron`, `partman.create_parent` (monthly range, premake 4), `UPDATE part_config` retention `'7 years'`/`retention_keep_table=false`/`infinite_time_partitions=true`, one `run_maintenance_proc()`, and a daily `cron.schedule('audit-log-partman-maintenance','0 3 * * *', …)`. `infra/bicep/modules/postgres.bicep` gains two `configurations` child resources (`azure.extensions` allow-list + `shared_preload_libraries=pg_cron`, the latter a static param ⇒ one-time restart) with parameterized full-replacement defaults; new `infra/docs/audit-partman-setup.md` documents the restart + provisioning order + verification SQL. Repositories unchanged (`insertAuditEvent` omits `audit_id`, relying on the db default — confirmed no code path inserts a literal `audit_id`; reads/filters already key off `created_datetime`/id). Deviations from spec (codebase wins): **added `CREATE EXTENSION IF NOT EXISTS pgcrypto` to `0001_audit.sql`** — the spec's literal SQL omits it, but `gen_random_bytes()` (unlike `gen_random_uuid()`) is **not** in core Postgres; it ships in pgcrypto, so the generator cannot run without it (also added `PGCRYPTO` to the Bicep `azure.extensions` default for Azure). Meta snapshots were **hand-edited** (`0001`–`0004`, each carries the `audit_log` table: `audit_id` → `primaryKey:false`/`default:"core.generate_ulid()"` + a `compositePrimaryKeys` entry named `audit_log_audit_id_created_datetime_pk`, drizzle's auto-name — the physical DDL uses `audit_log_pkey`, but drizzle-kit only diffs TS-schema↔snapshot and never introspects, so the name mismatch is harmless) rather than via a new migration, because the rewrite-in-place approach must not emit an `ALTER` migration; the exact JSON form was captured by running a throwaway `drizzle-kit generate --name=_um27_probe`, copying its 0005 snapshot block into 0001–0004, then deleting the probe migration/snapshot/journal entry — `npx drizzle-kit generate` afterward confirms **"No schema changes"**. Provisioning script + runner mirror the established `bootstrap-db-roles.sql`/`.ts` + `db:bootstrap-roles` pattern (own `BOOTSTRAP_DATABASE_URL`, `--> statement-breakpoint` split) rather than inventing a new shape; doc lives at `infra/docs/audit-partman-setup.md` next to `db-role-verification.md`. Test paths follow codebase convention — composite-PK + `audit_id`-not-standalone-PK coverage added to `tests/db/audit-schema.test.ts` (via `getTableConfig`), not the spec's `tests/unit`/`tests/integration` tree. Verified: `tsc --noEmit` clean, ESLint clean, Prettier clean, full unit suite green (853 of 854 — the one failure, `tests/app/no-access-page.test.tsx`, is pre-existing/unrelated, see Open Items), `drizzle-kit generate` "No schema changes", and a **live throwaway Docker Postgres 16** run confirmed: all migrations apply clean, `relkind='p'` (partitioned), PK = `audit_log_pkey (audit_id, created_datetime)`, `core.generate_ulid()` returns valid time-ordered UUIDs (ms-prefix non-decreasing across calls), and `audit_id` auto-generates on insert (omitted by the insert path). **Not verified (no extensions/Azure in this env, acknowledged like um30's Bicep):** pg_partman/pg_cron themselves aren't in the stock Postgres image, so premake/retention/cron-job/partition-routing-to-monthly-partitions and the Azure server-param/restart flow were authored to spec but not executed — on the stock image both test rows correctly land in `audit_log_default` (the safety-net partition) since no monthly partitions exist until `db:setup-partman` runs. The shared Tailscale dev DB was never touched; the throwaway container was destroyed after.
- um28 `um28-side-panel-system-config.md` — collapsible left admin panel (icon-rail ↔ full width, persisted via a non-`HttpOnly` `sidebar_collapsed` cookie), an admin-configurable company logo on the login page + sidebar header (falls back to the "Enterprise Billing" wordmark), five new editable `system_config` rows + a read-only `description` column, and locale/currency thread-through so date/money formatting is configuration-driven. Shipped as **one unit** (the sidebar collapse toggle and the logo share the sidebar header block). **Schema (the unit's only schema change):** added nullable `description text` to `core.system_config` (`db/schema/system-config.ts`); migration `0005_admin_chrome_config.sql` was `drizzle-kit generate`d (emits the `ALTER TABLE … ADD COLUMN "description" text;` + its `meta/0005_snapshot.json` + `_journal.json` idx:5 entry — `0005` was the correct next index, no renumbering) then **hand-appended** with six seed `INSERT`s (`app/app_logo_path` blank, `app/locale` `en-MY`, `app/default_currency` `MYR`, `company/company_name`/`company_address`/`company_billing_pic` blank — each non-secret/ACTIVE/`config_version=1`/`modified_by=NULL`, each with its seeded description) plus an `UPDATE` setting `app_name`'s description; `npx drizzle-kit generate` confirms "No schema changes" after. **Repository/types:** `description` added to both `findAllNonSecret`/`findById` projections; new `findActiveValue(db, group, key)` (selects the non-secret ACTIVE value, `ORDER BY config_version DESC LIMIT 1` — load-bearing for deterministic latest-active-version-wins given the `(config_group, config_version, config_key)` unique index); `SystemConfigDisplayRow` gains `description: string | null`; new `BrandingLogo` type. **lib:** new `lib/locale.ts` (`SUPPORTED_LOCALES`=`en-MY,ms-MY,en-SG,en-GB,en-US` / `DEFAULT_LOCALE`=`en-GB`; `SUPPORTED_CURRENCIES`=`MYR,SGD,USD,EUR,GBP` / `DEFAULT_CURRENCY`=`MYR`), new `lib/sidebar.ts` (`SIDEBAR_COOKIE="sidebar_collapsed"`); `lib/formatters.ts` — `formatDatetime(date, locale, fallback?)` (locale now a **required 2nd positional**; `timeZone:"UTC"` stays hardcoded — display timezone deferred) + new `formatMoney(amount, locale, currency)`. **Service (new):** `services/system-config/app-config-read.service.ts` — `getBrandingLogo()` (validates each path is a same-origin `/brand/…` string via `startsWith("/brand/")` + no `..`, returns `{src, markSrc?, alt} | null`), `getAppLocale()`, `getAppCurrency()` (allow-list + fallback), all `React.cache`d for per-request dedupe. **Chrome components:** new `components/brand-logo.tsx` (pure presentational logo-or-wordmark on a light plate, `login`/`nav`/`nav-collapsed` variants, plain `<img>` not `next/image`, monogram "EB" fallback for the collapsed rail); new `components/admin-sidebar.tsx` (`"use client"`, owns `collapsed` state seeded from the `defaultCollapsed` prop at `useState` init — never via `useEffect` — writes the cookie on toggle, no Server Action; header = logo + `PanelLeftClose`/`PanelLeftOpen` toggle, footer = identity strip + sign-out, collapsed drops the identity strip); rewrote `components/admin-nav.tsx` (per-item lucide icon — Roles → `ShieldHalf` to avoid the SSO `ShieldCheck` badge collision, Users/`Users`, System Configuration/`Settings`, Audit Log/`ScrollText`; `collapsed` prop; a single unified `<Link>` structure whose label fades via `max-width`+`opacity` rather than being conditionally unmounted, collapsed active = centered light square not the left-border pill, "Administration" caption hidden when collapsed); `components/nav-sign-out-button.tsx` gained a `collapsed` prop (icon-only + `title`). `app/(admin)/layout.tsx` reads the collapse cookie + identity + `getBrandingLogo()` and renders `<AdminSidebar …/>` in place of the inline `<aside>`; `app/(auth)/login/page.tsx` swaps the wordmark `<span>` for `<BrandLogo variant="login" …/>`. **Locale thread-through:** `getAppLocale()` added to the `users`/`roles` pages' read blocks and passed as a `locale` prop to `UserTable`/`UserDetail`/`RoleTable`/`RoleDetail`; all 8 `formatDatetime` call sites updated (user-table ×1, user-detail ×4, role-table ×1, role-detail ×2). **Config-table UI:** `description` rendered as a muted `--text-caption` second-line sublabel inside the existing Key cell (not a new column — `colSpan` unchanged); blank ⇒ no sublabel; `ConfigEditDialog` untouched. **Assets:** committed placeholder `public/brand/logo.svg` (no `logo-mark.svg` — `app_logo_mark_path` is not seeded, so the collapsed rail uses the monogram fallback, exactly the six INSERTs §3.1 lists). Decisions per spec sign-off (2026-06-28): logo in scope, locale/currency wired now, all five rows seeded, additive `description` column, `/brand/`-only logo paths, plain `<img>`, non-`HttpOnly` cookie, default first-visit = expanded, `en-MY`/`MYR` seeds, `timeZone:"UTC"` hardcoded. No new RBAC permission, audit event, or Server Action (the existing `system_config:EDIT`-gated `ConfigEditDialog` edits the new rows). Test paths follow codebase convention: new `tests/services/app-config-read.service.test.ts` (logo path validation incl. external/`data:`/`//host`/`..` rejection, locale/currency fallback), `tests/components/brand-logo.test.tsx`, `tests/components/admin-nav.test.tsx`, `tests/components/admin-sidebar.test.tsx` (toggle flips `w-64`↔`w-16`/`aria-expanded`, writes the cookie, collapsed drops the identity strip); extended `tests/lib/formatters.test.ts` (the locale wiring proven live — `en-MY`===`en-GB`===`"09 Mar 2026, 07:05"` **and** `ms-MY`⇒`"Mac"` differs — plus `formatMoney` with the exact U+00A0 NBSP for `RM 1,234.56`), `tests/components/config-table.test.tsx` (description sublabel present / absent), `tests/db/system-config-schema.test.ts` (+`description` column), `tests/db/migration.integration.test.ts` (+`description` column, +six seeded rows, app_name description), `tests/db/system-config-repository.integration.test.ts` (`findActiveValue` determinism — highest `config_version` wins; null for missing/RETIRED/secret); the three page/login tests (`users`/`roles`/`login`) and `tests/app/admin-layout.test.tsx` gained `getAppLocale`/`getBrandingLogo`/`cookies` mocks. Deviations from spec (codebase wins): `app_logo_mark_path` is **not** seeded (§3.1 lists exactly six INSERTs and omits it), so no `logo-mark.svg` is committed and the collapsed rail always uses the monogram fallback; `BrandLogo` uses a single unified `AdminNav` `<Link>` (label fade) reconciling §2.1's "labels fade not pop" with §2.5's distinct collapsed square. Verified: `tsc --noEmit` clean, ESLint clean (incl. the `@next/next/no-img-element` line-disables on the plain `<img>`s and import-boundary rules — the new service imports only `react`/`db`/`lib`/`types`, staying framework-agnostic), Prettier clean, full unit suite green (890 of 891 — the one failure, `tests/app/no-access-page.test.tsx`, is the documented pre-existing/unrelated copy-text drift, untouched here). Migration + `findActiveValue` verified against a **live throwaway Docker Postgres 16** (destroyed after; shared Tailscale dev DB untouched): all migrations apply clean, the `description` column is born nullable, all six rows seed with correct group/secret/status/non-null descriptions, `app_name` got its description, and `findActiveValue` returns the highest `config_version` (null for missing/RETIRED/secret). **Two pre-existing integration failures observed (NOT caused by um28):** `migration.integration.test.ts`'s exact-table-list and appuser-FK-count assertions fail because um27's `audit_log_default` partition surfaces in `information_schema.tables`/the FK catalog views (table list returns 11 incl. `audit_log_default`; appuser FKs return 13) — um28's 0005 adds no table and no appuser FK, so those two queries return identical results with or without 0005; left untouched (out of scope, §2.5), flagged in Open Items. `npm run build` not run (`.env` absent — see Open Items; consistent with um25/um27/um30's verification approach).
- um30 `um30-deployment-secrets-security-gates.md` — Dockerfile (3-stage: `deps`→`builder`→`runner`, non-root `nextjs` UID 1000, `next.config.ts` now sets `output: "standalone"`); `app/api/health/route.ts` (no DB query, `BUILD_VERSION` from env); `infra/azure-pipelines.yml` restructured from its um01 single-job shape into five gated stages (`build`→`test_scan`→`containerize`→`migrate`→`deploy`) plus a `zap_scan` stage template (`infra/zap-scan-stage.yml` + `infra/zap/zap-context.xml`/`rules.tsv`); `infra/bicep/` (main.bicep + 5 modules + 3 `.bicepparam` files); `db/migrations/0005_bootstrap_db_roles.sql` (`app_runtime`/`app_migrate` roles + grants, idempotent via `DO` blocks) + matching `meta/_journal.json`/`0005_snapshot.json` entries; `infra/docs/db-role-verification.md`; `infra/env/.env.{dev,staging,prod}.template`. Deviations from spec (codebase wins, consistent with um18-25's documented pattern): pipeline stays at `infra/azure-pipelines.yml` (not the spec's `infra/pipelines/azure-pipelines.yml` — um01 already established this path); SAST stays Semgrep, not CodeQL (um01 never wired CodeQL); `CREATE ROLE IF NOT EXISTS` isn't valid Postgres syntax (only `CREATE TABLE`/`CREATE INDEX` support it) — the bootstrap migration uses `DO $$ IF NOT EXISTS (SELECT FROM pg_roles ...) THEN CREATE ROLE ... END IF; END $$;` instead, and contains no password literal at all (the spec's literal `'<from Key Vault>'` would otherwise become the actual stored password) — see `infra/docs/db-role-verification.md` for the manual, never-committed `ALTER ROLE ... PASSWORD` follow-up; `GRANT CONNECT/CREATE ON DATABASE` use `EXECUTE format(..., current_database())` inside the `DO` block so the file isn't hardcoded to one DB name across dev/staging/prod; table is `core.audit_log` (lowercase, per `db/schema/audit.ts`), not the spec's literal `core."AUDIT_LOG"`; only schema `core` exists today, so the product/customer/billing/accounting repetition the spec describes is deferred until those schemas ship; the Entra secret is consumed as `MICROSOFT_CLIENT_SECRET` (Key Vault secret `microsoft-client-secret`), not the spec's `ENTRA_CLIENT_SECRET` — `lib/config.ts` has read `MICROSOFT_CLIENT_SECRET` since um10, and the spec's literal name is not an env var the app reads; the migration Container Apps Job runs `node --import tsx db/migrate.ts` (the codebase's actual `db:migrate` entrypoint, now reused by the runner image) rather than the spec's literal `npx drizzle-kit migrate` — `tsx` was moved from `devDependencies` to `dependencies` for this reason, and the Dockerfile's runner stage additionally copies the full `deps`-stage `node_modules` (overlaying standalone's traced/pruned subset) plus `db/migrate.ts`, `db/migrations/`, `lib/config.ts`, `lib/errors.ts`, `lib/logger.ts`, `types/password.ts`, and `tsconfig.json` so the same image/tag can run both `node server.js` and the migration script, per the spec's "same image" requirement for both the `deploy` and `migrate` stages; root `.env.example` stays the single canonical env-var reference (Zod-validated by `lib/config.ts`) rather than spec's `infra/env/.env.example` — only the three non-secret per-environment templates were added under `infra/env/`; `.gitignore`'s existing blanket `.env*` pattern needed a `!infra/env/*.template` exception so those new template files aren't accidentally untracked; a `validate:env` npm script (`node --env-file=.env.example --import tsx -e "import('./lib/config.ts')..."`) was added as the build stage's "Zod validation smoke" step (the spec names the check but not an implementation). The Bicep modules and pipeline YAML are infra-as-code authored to spec; they were not run against a real Azure subscription (none exists in this environment) — only the SQL bootstrap migration was actually executed and verified (against a throwaway local Docker Postgres container, immediately destroyed after). Verified: `tsc --noEmit` clean, ESLint clean, Prettier clean, full unit suite green (840 of 841 tests — the one failure, `tests/app/no-access-page.test.tsx`, is pre-existing/unrelated, see Open Items), full integration suite green (240 tests, run against a throwaway local Docker Postgres container — the shared Tailscale dev DB in `.env` was never touched), `npx drizzle-kit generate` confirmed "No schema changes" after adding the bootstrap migration/snapshot, and the full `core.audit_log`/DDL/DML privilege matrix in the verification checklist was manually confirmed via `psql` (see `infra/docs/db-role-verification.md`). **Post-um30 fix:** the role bootstrap was pulled out of the migration sequence — see the Migrations facts line above. Root cause: `npm run db:migrate` against the shared dev DB (role `ebadmin`, no `CREATEROLE`) `42501`-failed on `0005`; the step is superuser-only and self-creates the very `app_migrate` role the automated migrate stage runs as, so it can't live in that sequence. Now `db/bootstrap/bootstrap-db-roles.sql` + `db:bootstrap-roles`. Also hardened `db/migrate.ts`'s error log to print the Postgres `code`/`detail`/`where` (the original failure was truncated to `message` only). Correct provisioning order is **migrate → bootstrap** (the grants/revokes reference existing tables, and the schema's tables must be owned by the superuser/owner — not `app_migrate` — for the `audit_log` REVOKEs to bite). Full flow (migrate → bootstrap → verification SQL → idempotent re-run → missing-env guard) re-verified against a throwaway local Docker Postgres 16, container destroyed after; shared Tailscale dev DB untouched except a non-destructive `db:migrate` (now clean, nothing pending).
