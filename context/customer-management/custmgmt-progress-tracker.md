# Customer Management — Progress Tracker

Update this file after every meaningful implementation change.

## Status

| Unit | Name                                                                  | Status                     |
| ---- | ---------------------------------------------------------------------- | --------------------------- |
| cm01 | DB foundation (`customer` schema, migration, seed, permission registry) | Done (committed `4426342`) |
| cm02 | Validation schemas + read repositories + read services                 | Done (committed `3347d04`) |
| cm03 | Nav ("Customer" `NAV_SECTIONS` entry + locked-item `AdminNav` state)    | Done (committed `47c0f68`) |
| cm04 | View search page                                                        | Done (committed `d37c875`) |
| cm05 | View detail page                                                        | Done (committed `b1d0d8a`) |
| cm06 | Manage search page                                                      | Done (committed `53f9a62`) |
| cm07 | Create customer                                                         | Done (committed `9665074`) |
| cm08 | Edit page + update organization                                         | Done (committed `ff2dbc4`) |
| cm09 | Transition organization status                                         | Done (committed `9aae48c`) |
| cm10 | Transition customer status                                             | Done (committed `00a865a`) |
| cm11 | Add contact                                                             | Done (uncommitted)          |
| cm12 | Update contact                                                          | Done (uncommitted)          |
| cm13 | Delete contact                                                         | Done (uncommitted)          |
| cm14 | Set preferred contact                                                   | Spec written, not started  |
| cm15 | Set preferred contact method                                            | Spec written, not started  |
| cm16 | Authz guardrail sweep (ship gate)                                       | Spec written, not started  |

**Docs completed:** `custmgmt-project-overview.md`, `custmgmt-architecture.md`, `custmgmt-code-standards.md`, `custmgmt-ai-workflow-rules.md`, `custmgmt-ui-context.md`, and `specs/cm00-build-plan.md` (16 dependency-ordered build units, DB foundation through ship-gate guardrail sweep) — all 16 unit specs (`cm01`–`cm16`) are written; implementation proceeds in dependency order.

---

**cm01 implemented per `specs/cm01.md`** — DB foundation: `db/schema/customer.ts` (the `customer` schema, 3 tables — `organization`, `party_role`, `contact_medium` — 3 sequences, all CHECK/UNIQUE/FK constraints), migration `db/migrations/0009_customer.sql`, `db/seeds/customer.ts` (the `customers` permission row + `CUSTOMER_SEARCH_RESULT_LIMIT` seed), `types/customer.ts` (domain unions), and permission registry wiring. The `core.system_config` column set matched the spec's cross-referenced shape exactly (`db/schema/system-config.ts` confirmed against it) — no correction needed.

**Deviations from the spec's stated diff scope:** the composite `party_role_contact_medium_fk` (§2.2, `DEFERRABLE INITIALLY DEFERRED`) is hand-authored directly in the migration SQL, not declared in `db/schema/customer.ts` — drizzle-kit's `foreignKey()` builder can't express `DEFERRABLE`, and declaring it in schema.ts creates a genuine mutual-type-dependency between `party_role` and `contact_medium` that `tsc` can't resolve (TS7022/TS7024, circular inference). Migration table-creation order was hand-reordered to organization → party_role → contact_medium per spec §3.3 (drizzle-kit emits `contact_medium` first alphabetically; all FKs are separate `ALTER TABLE` statements so this was cosmetic, not functionally required, but kept for spec fidelity). Adding the `customers` permission triggered a large, spec-undersold ripple (see `[[permission-name-addition-ripple]]` memory) — beyond the `EffectivePermissionMap` fixture updates, every one of the 28 pre-existing `tests/**/*.integration.test.ts` files needed a `DROP SCHEMA IF EXISTS "customer" CASCADE` added to their beforeAll/afterAll (none of them knew about the new schema, and `customer` now holds FKs into `core`, so omitting the drop breaks the next test file's `CREATE SCHEMA "customer"` with "already exists").

**Verified this session:** `npm run typecheck`, `npm run lint`, `npm run format:check`, `vitest run` (unit), `vitest run --config vitest.integration.config.ts` (31/31 files, 281/281 tests) against the local Docker Postgres.

**Committed** as `4426342` ("customer data layer: schema, migration 0009, seed, permission registry (cm01)").

**Retroactive correction (post-`cm08`, live-app verification):** live-testing `cm08`'s edit page surfaced that `admin@example.com` (role `ADMIN`) got redirected to `/no-access` clicking "View Customer" — `db/seeds/customer.ts` only ever granted `customers:EDIT`/`customers:READ` to `MANAGER`/`USER`, with no `ADMIN` grant, unlike Product Management's seed (`db/seeds/product.ts`), which explicitly grants `ADMIN` the `products` permission. This was cm01's original design, not a regression — `cm01-spec §3.6` and its guardrail test (`tests/db/customer-seed.integration.test.ts`) explicitly asserted "**exactly** MANAGER→EDIT and USER→READ," with no ADMIN row ever considered. Confirmed with the user this was an oversight relative to Product's pattern, not intentional, and fixed by mirroring Product's approach: added `ADMIN → customers:EDIT` (not `DELETE` — `customers` has no DELETE level, architecture §4 unchanged) to `db/seeds/customer.ts`'s `grants` array, updated `cm01-spec.md` §3.6/§3.7/§5 and `custmgmt-architecture.md` §4 to document the three-role grant set as current, and updated `tests/db/customer-seed.integration.test.ts`'s three assertions (grant count 2→3, added `ADMIN`/`EDIT` check, idempotency-rerun count 2→3). Verified: `npm run typecheck`/`lint`/`format:check` clean; `tests/db/customer-seed.integration.test.ts` + `create-customer.integration.test.ts` + `optimistic-lock-conflict.integration.test.ts` all green against the dev container. Dev stack re-provisioned (`docker compose -f docker-compose.dev.yml down -v && up -d --build`) after each schema-wiping integration run; final state confirmed directly in `psql` — `core.role_permission_assign` now has `MANAGER→EDIT`, `USER→READ`, `ADMIN→EDIT` for `customers`, `admin@example.com` holds role `ADMIN`, `/login` serves 200. Not yet re-verified by clicking through the live UI as `admin@example.com` — DB-level and guard-logic verification only.

---

**cm02 implemented per `specs/cm02.md`** — §3.1–§3.12 in full: `validation/customer/{transitions,specification,organization,party-role,contact-medium}.schema.ts` (new); three read-only repositories `db/repositories/{organization,party-role,contact-medium}.ts` (new, finders only); `services/customer/{search-customers,get-customer-detail}.ts` (new); `types/customer.ts` extended with the read-model interfaces (`OrganizationDetail`, `CustomerRoleDetail`, `ContactAddress`, `ContactRow`, `CustomerDetail`, `CustomerSearchResult`, `CustomerSearchResults`). Confirms the two resolved design decisions: (1) search results and the `[id]` route param are keyed by `party_role_id`, not `organization_id` (an org can have >1 party_role over its history per Module Inv. #3) — proven by an integration test where one organization's CLOSED + ACTIVE party_roles surface as two distinct rows; (2) corrected `pm03-spec.md`'s `appuser.display_name` reference to `user_name` (the actual identity-schema column per `um02` and every later UM spec) — used directly via `findUserById` from `db/repositories/appuser.repository.ts` (no `identityRepository` wrapper object exists in the codebase, contrary to the spec's phrasing — the named export is used directly).

**Deviations from the spec's stated diff scope:** none in the shipped files — diff is exactly the read models, the five new validation schema files, the three new repositories, the two new services, and the new/edited test files (§5 diff-hygiene checklist). One implementation choice flagged at the time, not treated as a deviation: `searchCustomers`/`getCustomerDetail` took `db: Database` as an explicit first parameter (verbatim per spec's code block), unlike Product Management's `services/product/*` which import the `db` singleton internally. **Superseded by cm04 (see below):** this turned out to be the actual bug, not a deliberate choice — cm04 corrected both functions to accept only `query`/`partyRoleId` and import the `db` singleton internally, matching every downstream spec's assumed single-argument call. The contract documented here is stale; the corrected signature is current as of cm04.

**Verified this session:** `npm run typecheck`, `npm run lint`, `npm run format:check` all clean; `vitest run` (unit config) — 127 files / 1144 tests green; `vitest run --config vitest.integration.config.ts` against the local Docker Postgres dev container — 32 files / 293 tests, all green, including the new `tests/db/customer-repositories.integration.test.ts`.

**Committed** as `3347d04` ("customer validation schemas + read repositories + read services (cm02)").

---

**cm03 implemented per `specs/cm03.md`** — the "Customer" `NAV_SECTIONS` entry (View Customer → `/customers/view`, `Building2`; Manage Customer → `/customers/manage`, `UserCog`) inserted between `Products` and `Administration` in `components/admin-nav.tsx`, plus the new greyed/locked `AdminNav` capability: `NavItem.requiredPermission` (only "Manage Customer" sets it, `{ name: 'customers', level: 'EDIT' }`), a new optional `AdminNavProps.permissionMap`, and a `hasLevel`-backed `locked` branch rendering `<span role="link" aria-disabled="true">` (dimmed, `Lock` icon, non-navigating, "Requires MANAGER access" tooltip in expanded mode, plain-label tooltip collapsed) — fails closed when `permissionMap` is omitted. Collapsed-rail dividers now render for 3 sections (2 hairlines).

**§2.3.5 resolved:** `app/(app)/layout.tsx` only had `getCurrentUserIdentity()` → `{ userName, userEmail }` in scope, no full map and no `userId` either. Extended `getCurrentUserIdentity` (`auth/guard.ts`) to also return `userId` (it already loads the full `user` row internally), then added one `resolveEffectivePermissions(identity.userId)` call in the layout (only when identity resolves) and threaded the result through `AdminSidebar` (new optional `permissionMap` prop, `components/admin-sidebar.tsx`) into `AdminNav`. No `db/**` import added to `layout.tsx` — both calls stay behind `auth/`.

**Deviations from the spec's stated diff-hygiene file list:** the checklist named only `admin-nav.tsx`, `admin-nav.test.tsx`, and conditionally `layout.tsx`. Two more files needed a touch, both required to actually wire the map end-to-end rather than optional: `components/admin-sidebar.tsx` (the actual `<AdminNav>` caller sitting between the layout and the nav — it needed the pass-through prop) and `auth/guard.ts` (`getCurrentUserIdentity` didn't expose `userId`, only `userName`/`userEmail`, so `resolveEffectivePermissions` had nothing to call with). `tests/app/admin-layout.test.tsx` also needed its `getCurrentUserIdentity` mock fixtures updated (`userId` added) and a new `@/auth/resolver` mock (`resolveEffectivePermissions` stubbed to an all-null map) so the layout test suite doesn't hit the DB. Fixed the `custmgmt-code-standards.md` §8 Route-column typo across all five rows (search/detail/edit/add-new) plus its two prose cross-references, not just the two search-page routes cm03 itself depends on — same root typo, left consistent rather than half-fixed.

**Verified this session:** `npm run typecheck`, `npx eslint` (touched files), `npx prettier --check` all clean; `vitest run tests/components/admin-nav.test.tsx tests/app/admin-layout.test.tsx` (20/20 new+existing assertions green); full `vitest run` — 127 files / 1151 tests green (was 127/1144 before this unit's +7 new tests). Integration suite and dev-server manual verification (§ "Behavior" checklist items) not re-run this session — no `db/**`/`services/**`/`actions/**` touched, so the integration suite is unaffected by this unit's diff.

**Committed** as `47c0f68` ("customer nav entry + locked AdminNav state (cm03)").

---

**cm04 — spec written, not yet implemented.** `specs/cm04.md`: `app/(app)/customers/view/page.tsx`: READ guard, `q`-only search, `CustomerSearchPanel` + `CustomerResultsTable`, `OrganizationStatusBadge`/`CustomerStatusBadge`.

---

**cm04 implemented per `specs/cm04.md`** — `validation/customer/search-params.schema.ts` (new, lenient `.catch("")`-defaulted `q`); `app/(app)/customers/view/page.tsx` + `loading.tsx` + `error.tsx` (new) — READ guard as line 1 of the body, `q`-only search (no sort/pagination — capped + hinted per the overview); `components/customers/customer-search-panel.tsx` (client, Apply/Enter/Clear + `useTransition`) + `customer-results-table.tsx` (server component — no interactivity needed since row click is a real navigation, not a `?id=` selection); `organization-status-badge.tsx`/`customer-status-badge.tsx` (new, first consumers), colors/icons taken verbatim from `custmgmt-ui-context.md` §1–§2. New tests: `tests/app/customers-view-page.test.tsx`, `tests/components/{customer-search-panel,customer-results-table,organization-status-badge,customer-status-badge}.test.tsx`.

**Deviation from the spec's stated diff scope — a real fix to `cm02`, not a `cm04`-local workaround:** `cm02`'s `searchCustomers`/`getCustomerDetail` took `db: Database` as an explicit first parameter (a "deliberate choice" per `cm02`'s own tracker entry above). Wiring cm04's page to call `searchCustomers(db, parsed.q)` as the spec's code block literally shows tripped the `boundaries/dependencies` ESLint rule (`app/**` has no allowed edge to `db/**` — only `root-page`, `services`, `auth`, and `db` itself do). Checked every downstream spec that calls these services (`cm05`, `cm06`, `cm08`) — all of them already assume a single-argument call (`searchCustomers(parsed.q)`, `getCustomerDetail(idResult.data)`), matching Product Management's `services/product/*` convention (import the `db` singleton internally). So the `db`-as-parameter shape was the actual bug, only surfaced once cm04 became the first real consumer. Fixed at the source: `services/customer/search-customers.ts` and `services/customer/get-customer-detail.ts` now import `db` from `@/db/client` internally and dropped the parameter; updated all existing call sites accordingly (`tests/services/search-customers.service.test.ts`, `tests/services/get-customer-detail.service.test.ts`, `tests/db/customer-repositories.integration.test.ts` — 24 call sites across three files, argument-count only, no assertion changes). `cm05`/`cm06`/`cm08` need no further adjustment — their specs already match the corrected signature.

Also added `/customers/view` to `tests/app/route-manifest.test.ts`'s frozen `ROUTE_MANIFEST` (pm01's rename-invariance guardrail enumerates every `app/**/page.tsx`-derived route; a new route trips it by design until registered).

**Verified this session:** `npm run typecheck`, `npm run lint`, `npm run format:check` all clean; `vitest run` — 132 files / 1182 tests green (was 132/1182 total after adding this unit's new test files and fixing the route-manifest guardrail); `vitest run --config vitest.integration.config.ts` against the local Docker Postgres dev container — 32 files / 293 tests green, including `tests/db/customer-repositories.integration.test.ts` exercising the corrected service signatures against the real database.

**Committed** as `d37c875` ("customer view search page (cm04)"). `cm06` (Manage Customer search page) reuses `search-params.schema.ts` and `CustomerResultsTable` unchanged.

---

**cm05 implemented per `specs/cm05.md`** — `app/(app)/customers/view/[id]/page.tsx` (new): READ guard first, `[id]` parsed against `partyRoleIdSchema` before any DB call (malformed IDs never reach `getCustomerDetail`), one `"Customer not found"` state for both the malformed-ID and unknown-ID paths, the three fixed-order sections composed underneath an `InconsistencyBanner` shown only when `isStatusInconsistent` fires. New `components/customers/{organization-section,customer-role-section,contact-details-section,organization-type-badge,preferred-indicator,inconsistency-banner}.tsx` — the last exports the authoritative `isStatusInconsistent` rule verbatim per spec §2.2 (Rule 1: `ACTIVE` customer + non-`ACTIVE` organization; Rule 2: `DISSOLVED`/`MERGED` organization + non-`CLOSED` customer), now recorded here as authoritative per workflow §4.4 — `custmgmt-project-overview.md`'s existing "e.g." example already covers Rule 1's worked case, so the overview itself was not edited. `CustomerRoleSection`'s specification renders read-only `JSON.stringify(spec, null, 2)` in a `<pre>`, not the edit-only `SpecificationEditor`. `--preferred-fg` and the three `--banner-warning-*` tokens added to `globals.css` — all four are semantic aliases onto already-existing `--color-accent-500`/`--color-warning-{500,700,50}` values (confirmed no literal-hex duplication, per spec §3.5/§3.7's "confirm before adding a duplicate"); `--color-cyan-*` also already existed, no addition needed. `/customers/view/[id]` added to `tests/app/route-manifest.test.ts`'s frozen `ROUTE_MANIFEST`.

**Deviation from the spec's literal code block:** `InconsistencyBanner`'s JSX used an inline `style` prop for the three `--banner-warning-*` tokens (spec §3.7) — this repo has a project-wide ESLint rule banning inline `style` props (`no-restricted-syntax`, ZAP rule 10055 / CSP `style-src` without `unsafe-inline`), which the spec's own preamble already flagged as a blind spot ("no live-repo mount this session"). Fixed by using Tailwind arbitrary-value classes (`border-[color:var(--banner-warning-border)]` etc.) instead — same tokens, same visual result, matching how every other badge/status component in this module already reads its CSS custom properties.

**Verified this session:** `npm run typecheck`, `npm run lint`, `npm run format:check` all clean; `vitest run` — 139 files / 1210 tests green (was 132/1182 before this unit's +7 new test files / +28 new tests). Integration suite not re-run — no `db/**`/`services/**`/`actions/**` touched, so it's unaffected by this unit's diff (all new/edited files are `app/(app)/customers/view/[id]/**`, `components/customers/**`, `app/globals.css`, and the corresponding test files).

**Committed** as `b1d0d8a` ("customer view detail page (cm05)"). `cm08` will reuse `OrganizationTypeBadge`/`PreferredIndicator`/`InconsistencyBanner` built here.

---

**cm06 implemented per `specs/cm06.md`** — `app/(app)/customers/manage/page.tsx` + `loading.tsx` + `error.tsx` (new): `requirePermission(PERMISSIONS.CUSTOMERS, LEVELS.EDIT)` as the first statement of the page body, otherwise line-for-line the same shape as `cm04`'s View page (`firstValue`, the guard-first ordering, the `results !== null` empty-query short-circuit). `CustomerSearchPanel`/`CustomerResultsTable` imported and used unchanged, only `basePath`/`baseHref` swapped to `/customers/manage`. The "Add new customer" CTA (`--action-cta-bg`, first consumer of that token, per ui-context §7) links to `/customers/manage/new` (`cm07`, accepted interim 404) and is always rendered regardless of search state, paired with the non-blocking nudge caption per spec §2.2 decision 3. `loading.tsx`/`error.tsx` are copies of `cm04`'s View equivalents (spec §3.2 — "no new pattern"). New test: `tests/app/customers-manage-page.test.tsx`, mirroring `customers-view-page.test.tsx`'s guard/empty-query/deep-link/tampered-q/array-q cases plus this unit's own guardrail entry (EDIT passes, `/no-access` propagates for a non-EDIT session) and three CTA-presence assertions (empty query, populated results, zero results) per spec §3.3.

**Deviation from the spec's stated diff scope:** `tests/app/route-manifest.test.ts`'s frozen `ROUTE_MANIFEST` needed `/customers/manage` added — not mentioned in cm06's own diff-hygiene checklist, but the same guardrail `cm04`'s tracker entry already flagged trips by design for any new `app/**/page.tsx` until registered. No other file outside the stated scope touched; no edit to any `cm04`/`cm05` file.

**Verified this session:** `npm run typecheck`, `npm run lint`, `npm run format:check` all clean; `vitest run` — 140 files / 1220 tests green (was 139/1210 before this unit's +1 new test file / +10 new tests). Integration suite not re-run — no `db/**`/`services/**`/`actions/**` touched, so it's unaffected by this unit's diff. Live dev-server manual verification (§5 "Behavior" checklist — MANAGER nav flow, USER direct-navigation redirect, CTA click landing on the interim 404) not run this session.

**Committed** as `53f9a62` ("customer manage search page (cm06)").

---

**cm07 implemented per `specs/cm07.md`** — the module's first mutation unit, landing the action → service → repository shape (`{ok:true;value}|{ok:false;code}`) reused by `cm08`–`cm15`. `organizationRepository.insert`/`findSimilarNames` and `partyRoleRepository.insert` (extending `cm02`'s finder-only files, exactly the JIT arrival both files' own comments anticipated); `validation/customer/create-customer.schema.ts` (new, `organizationFieldsSchema.extend({ specificationRaw, confirmed })`); `services/customer/create-customer.ts` (new) — the two-step similar-name confirm per spec §2.2 (`confirmed:false` checks `findSimilarNames` before any write; a match returns `SIMILAR_NAMES_FOUND` with nothing written; `confirmed:true` skips the check unconditionally), one `db.transaction` writing `organization` → `ORGANIZATION_CREATED` audit → `party_role` → `CUSTOMER_CREATED` audit, and `isUniqueViolation` (new `lib/db-errors.ts`, first needed here, checking Postgres `PostgresError.code === "23505"` + `constraint_name`) catching the real, non-skippable `registration_number` uniqueness constraint on every submission. `actions/customer/create-customer.ts` (new) mirrors `um11`'s action shape exactly — guard, `safeParse`, one service call, `revalidatePath('/customers/manage')`. `app/(app)/customers/manage/new/{page,loading,error}.tsx` (new) — thin guard-then-form page, `loading`/`error` copied from `cm06`'s Manage page pattern. `components/customers/specification-editor.tsx` (new, first consumer of `party_role_specification`'s one editor per code-standards §4.4) and `new-customer-form.tsx` (new) — `react-hook-form` + `zodResolver(createCustomerSchema.omit({confirmed:true}))`, `confirmed` as local state per spec §3.7, a `useEffect` on the watched `name` field resetting `confirmed`/clearing the warning when the name changes after `SIMILAR_NAMES_FOUND`, dismissible (non-`confirmed`-clearing) warning banner, submit button relabeling "Create anyway" with a distinct ring treatment (still `--action-cta-bg`) while a warning is active. New tests: `tests/services/create-customer.service.test.ts`, `tests/actions/create-customer.action.test.ts`, `tests/components/{specification-editor,new-customer-form}.test.tsx`, `tests/db/create-customer.integration.test.ts` (written, not run this session — see below).

**Deviations from the spec's stated diff scope — two real ripples, not local workarounds:** (1) the spec's `insertAuditEvent`/`writeAuditEvent` naming and the `AuditEventType` union: the actual helper is `insertAuditEvent` (`db/repositories/audit.repository.ts`, already correctly used), but `ORGANIZATION_CREATED`/`CUSTOMER_CREATED` didn't exist in `types/audit.ts`'s `AUDIT_EVENT_TYPES` — added both, which per the `[[permission-name-addition-ripple]]` pattern rippled into `types/audit-log.ts`'s `AUDIT_EVENT_CATEGORY_MAP` (a `Record<AuditEventType, AuditEventCategory>` — both new types added as `"Additive"`, matching `USER_CREATED`/`ROLE_CREATED`) and `tests/components/audit-log-filters.test.tsx` (event-type count 20→22, option count 21→23, plus explicit optgroup assertions for the two new types). (2) `tests/db/customer-repository-exports.test.ts` was a structural guardrail asserting `organizationRepository`/`partyRoleRepository` "export no mutation function" — exactly the finder-only invariant `cm02` shipped and this unit's own repository-file comments named cm07 as the JIT unit that would end it. Removed the now-obsolete assertions for those two repositories (kept `contactMediumRepository`'s, since it remains finder-only until `cm11`), with a comment explaining the graduation rather than a silent deletion.

**Integration suite run against the dev container's `DATABASE_URL` (user-confirmed, per `[[local-docker-dev-stack]]`'s "ask before running" guidance), then re-provisioned.** First pass caught a real bug: `isUniqueViolation` (`lib/db-errors.ts`) checked `err.code`/`err.constraint_name` directly, but Drizzle wraps the raw driver error in `DrizzleQueryError`, exposing the original `PostgresError` on `.cause` — so the duplicate-`registration_number` path fell through to the generic `throw err` instead of returning `DUPLICATE_REGISTRATION_NUMBER`. The mocked-repository unit test couldn't catch this (it never touches Drizzle's real error wrapping); only the real-DB integration test surfaced it. Fixed by checking both `err` and `err.cause`; saved as `[[audit-event-type-addition-ripple]]`-adjacent — actually a new memory, `isUniqueViolation`-checks-`.cause`, folded into `lib/db-errors.ts`'s own comment rather than a separate memory file since it's fully explained in the code. Re-ran: full integration suite (33 files / 295 tests) green, full unit suite (144 files / 1240 tests) green, `typecheck`/`lint`/`format:check` clean. Dev stack re-provisioned via `docker compose -f docker-compose.dev.yml down -v && up -d --build` — migrations + all four seeds (admin, RBAC, product, customer) + partman setup ran clean; `/login` verified serving 200 again.

**Verified this session:** `npm run typecheck`, `npm run lint` (0 errors; 1 pre-existing-pattern warning on `new-customer-form.tsx`'s `watch()` call — React Compiler skips memoizing that component, not a functional issue), `npm run format:check` all clean; `vitest run` (unit config) — 144 files / 1240 tests green; `vitest run --config vitest.integration.config.ts` — 33 files / 295 tests green, including the new `tests/db/create-customer.integration.test.ts`. Live dev-server manual click-through (§5's full "Behavior" checklist beyond the login-page smoke check) not exercised this session.

**Committed** as `9665074` ("customer create action/service/repository + add-new page (cm07)") — this tracker row previously read "Done (uncommitted)" past the point the commit actually landed; corrected retroactively, no code change. `cm08` (edit page + optimistic lock) is next; it reads back the customer this unit creates and is the first unit to exercise `party_role.last_modified_datetime`'s compare-and-bump lock.

---

**cm08 implemented per `specs/cm08.md`** — the module's first optimistic-lock implementation. `partyRoleRepository.compareAndBumpLock` (extends `cm02`'s finder-only `db/repositories/party-role.ts`) — one atomic `UPDATE party_role SET last_modified_datetime = now() WHERE party_role_id = $id AND last_modified_datetime = $expected RETURNING ...`, zero rows matched (stale lock or unknown ID) returns `null`, the single place Module Invariant #6 is implemented; every mutation service `cm08`–`cm15` calls this exact function. `organizationRepository.update` (extends `cm07`'s `db/repositories/organization.ts`) — no `status` field in its input, so a plain edit can never accidentally overwrite it. `validation/customer/update-organization.schema.ts` (new) — `organizationFieldsSchema.extend({organizationId, partyRoleId}).merge(optimisticLockSchema)`. `services/customer/update-organization.ts` (new) — `before` loaded outside the transaction (plain read); inside the transaction, `compareAndBumpLock` runs first and a `null` short-circuits to `{ok:false, code:'CONFLICT'}` before `organizationRepository.update` or the audit write ever run (the transaction still commits as a harmless no-op); on success returns the bumped `lastModifiedDatetime`. `actions/customer/update-organization.ts` (new) — guard → `safeParse` → service call → on success `revalidatePath` for both the literal edit-page path (`/customers/manage/${partyRoleId}`) and `/customers/manage` (an org name change can affect search results there too). `app/(app)/customers/manage/[id]/page.tsx` + `loading.tsx` + `error.tsx` (new) — EDIT guard first, `partyRoleIdSchema.safeParse` before any DB call, one `getCustomerDetail` call feeding both the page header and `OrganizationForm`'s `lastModifiedDatetime` prop (sourced from `customerRole`, not `organization`, per §2.3.2), the same "Customer not found" card shape as `cm05`'s view page (not yet extracted — two call sites isn't worth it), and two trailing JSX comments naming `cm10`/`cm11` as the seams that extend this same page file. `components/customers/organization-form.tsx` (new, `'use client'`) — same fields as `cm07`'s `NewCustomerForm` minus specification/status, `organizationId`/`partyRoleId` as real hidden inputs, `lastModifiedDatetime` tracked as component state (not a registered form field, since a `Date` doesn't round-trip through a text `<input>`) and merged into the submitted payload — functionally still "submitted verbatim, never edited by the user" per spec intent. A `CONFLICT` result renders `components/customers/optimistic-lock-conflict-banner.tsx` (new, shared component — reuses `InconsistencyBanner`'s warning tokens verbatim) and disables the save button until a reload; a successful save threads the returned bumped timestamp back into local state so a fast same-session second save doesn't flash a spurious conflict against its own write.

**Retroactive `cm01` fix (§2.1) confirmed a non-issue in this codebase:** `cm01` as actually implemented already declares every `timestamptz` column in the `customer` schema at explicit millisecond precision (`precision: 3` on all six timestamp columns across `organization`/`party_role`/`contact_medium` in `db/schema/customer.ts`) — no follow-up migration was needed for this unit, exactly as cm01's own tracker row anticipated.

**Deviations from the spec's stated diff scope — two real ripples, following the established pattern:** (1) `ORGANIZATION_UPDATED` didn't exist in `types/audit.ts`'s `AUDIT_EVENT_TYPES` — added it, which rippled into `types/audit-log.ts`'s `AUDIT_EVENT_CATEGORY_MAP` (`"Change"`, alongside `USER_UPDATED`/`ROLE_UPDATED`) and `tests/components/audit-log-filters.test.tsx` (event-type count 22→23, option count 23→24, plus an explicit "Change" optgroup assertion), same shape as `[[audit-event-type-addition-ripple]]`. (2) `tests/app/route-manifest.test.ts`'s frozen `ROUTE_MANIFEST` needed `/customers/manage/[id]` added — same guardrail every new `app/**/page.tsx` trips by design, flagged in `cm04`'s and `cm06`'s tracker entries; not called out in `cm08`'s own diff-hygiene checklist but the same root cause. No change was needed to `tests/db/customer-repository-exports.test.ts` — `organizationRepository`/`partyRoleRepository` already graduated out of the finder-only guardrail as of `cm07`, and `compareAndBumpLock` doesn't match that test's mutation-name pattern (`insert|create|update|delete|remove|set`) regardless.

**Integration suite run against the dev container's `DATABASE_URL` (user-confirmed), then re-provisioned.** New `tests/db/optimistic-lock-conflict.integration.test.ts` — creates a customer via `createCustomer` directly, loads `lastModifiedDatetime` once (simulating two editors opening the same customer), applies editor A's update successfully (lock bumps), then attempts editor B's update using the original, now-stale timestamp — asserts `CONFLICT`, asserts the organization row reflects only editor A's change, asserts exactly one `ORGANIZATION_UPDATED` audit row exists. Passed clean on the first run — no repeat of `cm07`'s `isUniqueViolation`/`.cause` surprise, since that fix was already in place and reused unchanged (`organization_registration_number_unique`). Full integration suite: 34 files / 296 tests green (was 33/295 after `cm07`). Dev stack re-provisioned via `docker compose -f docker-compose.dev.yml down -v && up -d --build` — migrations + all four seeds + partman setup ran clean; `/login` verified serving 200 again.

**Verified this session:** `npm run typecheck`, `npm run lint` (0 errors; the same pre-existing `new-customer-form.tsx` React Compiler `watch()` warning from `cm07`, unrelated to this unit's diff), `npm run format:check` all clean; `vitest run` (unit config) — 146 files / 1249 tests green (was 144/1240 before this unit's +2 new test files / +9 new tests). Live dev-server manual click-through of the edit page's own "Behavior" checklist (concurrent-edit UI walkthrough beyond the login-page smoke check) not exercised this session.

**Committed** as `ff2dbc4` ("customer edit page + update-organization (cm08); grant ADMIN customers:EDIT; rename spec files") — this tracker row previously read "Done (uncommitted)" past the point the commit actually landed; corrected retroactively, no code change. `cm09` (transition organization status) is next; it reuses `compareAndBumpLock` unchanged for the first status-transition mutation.

---

**cm09 implemented per `specs/cm09.md`** — the module's first status-transition mutation, and the first consumer of `StatusTransitionControl`, the one component that ever renders a status dropdown in this module (code-standards §4.2), built to be reused unchanged by `cm10` for the customer entity via its `entityKind` prop. `organizationRepository.updateStatus` (extends `cm07`'s/`cm08`'s `db/repositories/organization.ts`) — a narrow update touching only `status`/`status_reason`/provenance, kept separate from `cm08`'s field-only `update` so neither can accidentally touch the other's columns. `validation/customer/transition-organization-status.schema.ts` (new) — `{organizationId, partyRoleId, targetStatus}` merged with `cm02`'s `statusTransitionInputSchema` (`{statusReason, lastModifiedDatetime}`); `targetStatus` is only checked against the `OrganizationStatus` enum here, the actual edge-validity check is the service's job. `services/customer/transition-organization-status.ts` (new) — `ORGANIZATION_TRANSITIONS[before.status]` checked in-memory *before* the transaction opens (an invalid edge never needs a DB round trip or a lock check); inside the transaction, `compareAndBumpLock` (cm08) still runs first exactly as `updateOrganization` does, a `null` short-circuiting to `CONFLICT` before the status write or audit ever run. `actions/customer/transition-organization-status.ts` (new) — same guard/safeParse/service-call/revalidate shape as `cm08`'s `update-organization` action. `components/customers/status-transition-control.tsx` (new, `'use client'`) — renders the current-status badge (`OrganizationStatusBadge`/`CustomerStatusBadge` picked by `entityKind`) plus, when `nextStates` is non-empty, a shadcn `Select` whose `SelectItem`s carry a leading color-swatch dot (`STATUS_SWATCH_COLOR`, one combined map keyed by the union of `OrganizationStatus`/`CustomerStatus` since the component is entity-agnostic) matching each target status's badge family (ui-context §1–§2); selecting a target reveals a mandatory `Reason` textarea and an `Apply` button gated on both being non-empty; a `CONFLICT` result swaps in `OptimisticLockConflictBanner` (cm08) in place of the control; any other non-ok result (`INVALID_TRANSITION`/`VALIDATION_ERROR` — both defense-in-depth paths the rendered control can never trigger itself, since its own options are always valid edges and Apply is reason-gated) renders a generic inline error. `components/customers/organization-form.tsx` (extended) — renders `<StatusTransitionControl>` below the "Save changes" button, `nextStates={ORGANIZATION_TRANSITIONS[organization.status]}` computed in the component (still server-sourced data — the transition map itself is never sent to the client, only the precomputed array is), wrapped by a local `handleStatusTransition` adapter that calls the new action, threads a successful bump into the same `currentLastModifiedDatetime` state `cm08` already tracks, and collapses `ORGANIZATION_NOT_FOUND`/`FORBIDDEN` into the control's generic `VALIDATION_ERROR` bucket (paths a real UI session can't hit). New tests: `tests/services/transition-organization-status.service.test.ts` (every `ORGANIZATION_TRANSITIONS` edge accepted via `it.each`, every non-edge pair rejected with `INVALID_TRANSITION` before any lock/transaction call, `compareAndBumpLock` returning `null` ⇒ `CONFLICT` with no write/audit, happy path persists `status_reason` on the row and in the audit `afterData`), `tests/validation/transition-organization-status.schema.test.ts` (blank/whitespace-only `statusReason`, malformed IDs, out-of-enum `targetStatus`, missing lock all rejected), `tests/components/status-transition-control.test.tsx` (terminal state renders no dropdown, selecting a target reveals the gated reason/Apply, each option's swatch color asserted, `CONFLICT` swaps in the banner and calls `onConflict`).

**Deviations from the spec's stated diff scope — following the established ripple pattern, not local workarounds:** (1) `ORGANIZATION_STATUS_CHANGED` didn't exist in `types/audit.ts`'s `AUDIT_EVENT_TYPES` — added it, rippling into `types/audit-log.ts`'s `AUDIT_EVENT_CATEGORY_MAP` (`"Change"`, alongside `ORGANIZATION_UPDATED`) and `tests/components/audit-log-filters.test.tsx` (event-type count 23→24, option count 24→25, plus an explicit "Change" optgroup assertion for the new type), same shape as `[[audit-event-type-addition-ripple]]`. (2) `tests/components/organization-form.test.tsx` needed a new `vi.mock("@/actions/customer/transition-organization-status", ...)` added alongside its existing `update-organization` action mock — `OrganizationForm` now imports the new action directly, and without the mock the test's real import chain reaches `db/client.ts`'s eager env validation and throws (the same failure mode `cm07`'s tracker entry already documented for this codebase's `lib/config.ts`). No new integration test file was added: the spec's §3.6 "Integration" bullet (every valid transition submitted without `statusReason` is rejected) is already covered at the schema layer by the new schema test — Zod rejects a blank `statusReason` before the action ever calls the service, matching how `cm08` itself drew this same line.

**Integration suite run against the dev container's `DATABASE_URL` (user-confirmed), then re-provisioned.** Full integration suite: 34 files / 296 tests green — unchanged file/test count from `cm08`'s baseline, confirming no new integration file was needed. Dev stack re-provisioned via `docker compose -f docker-compose.dev.yml down -v && up -d --build`; `/login` verified serving 200 again.

**Verified this session:** `npm run typecheck`, `npm run lint` (0 errors; the same pre-existing `new-customer-form.tsx` React Compiler `watch()` warning from `cm07`/`cm08`, unrelated to this unit's diff), `npm run format:check` all clean; `vitest run` (unit config) — 149 files / 1293 tests green (was 146/1249 before this unit's +3 new test files / +44 new tests, accounting for the `it.each` edge/non-edge expansion). Live dev-server manual click-through of the status-transition UI (concurrent-edit walkthrough, terminal-state rendering in the browser) not exercised this session — DB-level and component-test verification only.

**Committed** as `9aae48c` ("transition organization status + StatusTransitionControl (cm09)"). `cm10` (transition customer status) is next; it reuses `StatusTransitionControl` unchanged for the customer entity, only different bound values (`entityKind="customer"`, `CUSTOMER_TRANSITIONS`, `CustomerRoleForm`).

---

**cm10 implemented per `specs/cm10.md`** — reuses `StatusTransitionControl` (cm09) unchanged for the customer entity, only different bound values (`entityKind="customer"`, `CUSTOMER_TRANSITIONS`), proving the component's "one source" claim by actually reusing it for a second entity kind. `db/repositories/party-role.ts` extended with `compareAndUpdateStatus`/`compareAndUpdateSpecification` (§2.2) — a same-row refinement of `cm08`'s `compareAndBumpLock` pattern: since this unit's two mutations write directly to `party_role`, the same row the lock lives on, the compare-check and the actual data write collapse into one atomic `UPDATE` rather than bumping the lock and then issuing a second `UPDATE` against the row just touched. `compareAndBumpLock` itself is untouched, still used as-is by every other mutation. `validation/customer/transition-customer-status.schema.ts` + `update-party-role-specification.schema.ts` (new, verbatim per spec §3.2); `services/customer/transition-customer-status.ts` + `update-party-role-specification.ts` (new) — `transitionCustomerStatus` checks `CUSTOMER_TRANSITIONS[before.status]` in-memory before the transaction opens (no skipping `VALIDATED` falls out of the map's shape, no special-case code); `updatePartyRoleSpecification` mirrors it exactly, substituting `parseSpecificationInput` for the transition-map check and writing no `status_reason`-style field (a specification edit isn't a lifecycle transition). `actions/customer/transition-customer-status.ts` + `update-party-role-specification.ts` (new) — same guard/safeParse/service-call/revalidate shape as every prior mutation action; the specification action only revalidates the edit page (unlike status, the specification never appears in `CustomerSearchResult`, so `/customers/manage` has nothing to refresh). `components/customers/customer-role-form.tsx` (new, `'use client'`) — the second section added to `cm08`'s edit-page container, resolving its `{/* cm10 adds <CustomerRoleForm /> here */}` seam comment. Read-only fields (`partyRoleId`, `account ?? '—'`, `lastModifiedByName`, `lastModifiedDatetime`) render first, followed by two independently-submitted areas in the same card: the `StatusTransitionControl` and a `SpecificationEditor` (cm07) + its own "Save specification" button. Each area tracks its **own** local `lastModifiedDatetime` state, both seeded from the same `customerRole.lastModifiedDatetime` prop but never shared after a save — so a save in one area doesn't disturb the other, but if one lands first, the other's next attempt correctly sees its own now-stale value and gets `CONFLICT`, scoped only to that area's banner (Module Inv. #6, one shared lock column serializing all edits in the customer's scope). `app/(app)/customers/manage/[id]/page.tsx` extended — fetches `getAppLocale`/`getAppTimezone` (previously only the View page did) to format `CustomerRoleForm`'s read-only `lastModifiedDatetime`, and resolves the `cm10` seam comment; the `cm11` seam comment is left in place.

**Deviation from the spec's stated diff scope — the established audit-event-type ripple, not a local workaround:** `CUSTOMER_STATUS_CHANGED` and `PARTY_ROLE_SPECIFICATION_UPDATED` didn't exist in `types/audit.ts`'s `AUDIT_EVENT_TYPES` — added both, rippling into `types/audit-log.ts`'s `AUDIT_EVENT_CATEGORY_MAP` (both `"Change"`, alongside `ORGANIZATION_STATUS_CHANGED`/`ORGANIZATION_UPDATED`) and `tests/components/audit-log-filters.test.tsx` (event-type count 24→26, option count 25→27, plus two new "Change" optgroup assertions), same shape as `[[audit-event-type-addition-ripple]]`. No change needed to `tests/db/customer-repository-exports.test.ts` — `partyRoleRepository` already graduated out of the finder-only guardrail as of `cm07`. New tests: `tests/services/transition-customer-status.service.test.ts` (every `CUSTOMER_TRANSITIONS` edge accepted via `it.each`, every non-edge rejected with `INVALID_TRANSITION` before any lock/transaction call, an explicit `INITIALIZED → ACTIVE` assertion, `compareAndUpdateStatus` returning `null` ⇒ `CONFLICT`, happy path persists `status_reason` and audits correctly), `tests/services/update-party-role-specification.service.test.ts` (malformed JSON and top-level array ⇒ `INVALID_SPECIFICATION` with no transaction, not-found ⇒ `PARTY_ROLE_NOT_FOUND`, stale lock ⇒ `CONFLICT`, valid object ⇒ saved and audited as `PARTY_ROLE_SPECIFICATION_UPDATED`), `tests/components/customer-role-form.test.tsx` (status and specification areas submit independently — calling one never calls the other's action — and a `CONFLICT` in either area shows the banner scoped to that area only), `tests/validation/transition-customer-status.schema.test.ts` (blank/whitespace `statusReason`, malformed `partyRoleId`, out-of-enum `targetStatus`, missing lock all rejected — this closes code-standards §9.3's "both maps" reason requirement for the customer side, same as cm09 did for the organization side, without needing a new integration test file).

**Integration suite run against the dev container's `DATABASE_URL` (user-confirmed), then re-provisioned.** Full integration suite: 34 files / 296 tests green — unchanged file/test count from `cm09`'s baseline, confirming no new integration file was needed (the schema-layer test above already covers the "every edge without `statusReason`" requirement) and no regression from the repository/audit-type changes. Dev stack re-provisioned via `docker compose -f docker-compose.dev.yml down -v && up -d --build`; `/login` verified serving 200 again.

**Verified this session:** `npm run typecheck`, `npm run lint` (0 errors; the same pre-existing `new-customer-form.tsx` React Compiler `watch()` warning from `cm07`–`cm09`, unrelated to this unit's diff), `npm run format:check` all clean; `vitest run` (unit config) — 153 files / 1332 tests green (was 149/1293 before this unit's +4 new test files). Live dev-server manual click-through of the new status/specification UI (concurrent-edit walkthrough beyond the login-page smoke check) not exercised this session — DB-level and component-test verification only.

**Committed** as `00a865a` ("transition customer status + update party role specification (cm10)"). `cm11` (add contact) is next; it reuses `contact-mutations.ts`/`ContactManagerPanel` and is the first mutation to prove Module Inv. #6's "even a contact-only edit" clause using the original cross-table `compareAndBumpLock` (not this unit's same-row shortcut, since a contact mutation touches `contact_medium`, not `party_role`, directly).

---

**cm11 implemented per `specs/cm11.md`** — the module's first contact mutation and the first real exercise of `cm01`'s composite deferrable FK. `contactMediumRepository.insert` (extends `cm02`'s finder-only `db/repositories/contact-medium.ts` — this repository graduates out of the finder-only guardrail here, the last of the module's three to do so) and `partyRoleRepository.setPreferredContact` (extends `cm08`'s `db/repositories/party-role.ts`, no lock predicate per spec §2.5 — called only after `compareAndBumpLock` already succeeded earlier in the same transaction). `validation/customer/add-contact.schema.ts` (new) — `contactFieldsSchema.extend({partyRoleId}).merge(optimisticLockSchema)`, verbatim per spec §3.3. `services/customer/contact-mutations.ts` (new) — the one shared home for all contact-mutation logic (code-standards §7.3; `cm12`–`cm15` each add one more export here); `addContact` implements both independently-tracked invariants (Module Inv. #4/#5) without conflating them: the preferred-*contact* pointer auto-sets only when `existingContacts.length === 0` (checked before the transaction opens), while `resolvePreferredMethod`'s fixed `PHONE → EMAIL → ADDRESS` priority (spec §2.3, now authoritative) evaluates every contact add regardless of first/subsequent. Transaction order fixed per spec §2.4: lock-check → insert contact → (if first) point `party_role.contact_medium` at it — one `db.transaction`, `CONTACT_CREATED` audited unconditionally, `PREFERRED_CONTACT_CHANGED` audited only on the first-contact branch. `actions/customer/add-contact.ts` (new) — same guard/safeParse/service-call/revalidate shape as every prior mutation action. `components/customers/contact-manager-panel.tsx` (new, `'use client'`) — the third and final section added to `cm08`'s edit-page container, resolving its `{/* cm11 adds <ContactManagerPanel /> here */}` seam; composes (not forks) `cm05`'s `ContactDetailsSection` per-contact visual pattern (name/role, phone/email/address rows with icons, `PreferredIndicator` at contact- and method-level) alongside an "Add contact" button (`--action-cta-bg`, ui-context §7) revealing an inline `contactFieldsSchema` form with Save/Cancel; a `CONFLICT` result renders `OptimisticLockConflictBanner` (cm08) in place of the form, matching every other mutation UI in the module. `app/(app)/customers/manage/[id]/page.tsx` — `cm11` seam resolved.

**Deviations from the spec's stated diff scope — the established audit-event-type ripple, plus one structural-guardrail retirement:** (1) `CONTACT_CREATED` and `PREFERRED_CONTACT_CHANGED` (both names used verbatim in the spec's own service code block) didn't exist in `types/audit.ts`'s `AUDIT_EVENT_TYPES` — added both, rippling into `types/audit-log.ts`'s `AUDIT_EVENT_CATEGORY_MAP` (`CONTACT_CREATED` → `"Additive"` alongside `ORGANIZATION_CREATED`/`CUSTOMER_CREATED`; `PREFERRED_CONTACT_CHANGED` → `"Change"` alongside the other per-entity change events) and `tests/components/audit-log-filters.test.tsx` (event-type count 26→28, option count 27→29, plus explicit optgroup assertions for both new types), same shape as `[[audit-event-type-addition-ripple]]`. (2) `tests/db/customer-repository-exports.test.ts` — the structural no-mutation guardrail whose only remaining assertion covered `contactMediumRepository` (`organizationRepository`/`partyRoleRepository` had already graduated as of `cm07`) — was deleted outright rather than edited down to an empty `describe` block, since `cm11` is exactly the JIT unit the file's own comment anticipated ending it for the last of the module's three repositories; no repository in this module remains finder-only after this unit.

**Verified this session:** `npm run typecheck`, `npm run lint` (0 errors; the same pre-existing `new-customer-form.tsx` React Compiler `watch()` warning from `cm07`–`cm10`, unrelated to this unit's diff), `npm run format:check` all clean; `vitest run` (unit config) — 152 files / 1340 tests green (was 153/1332 before this unit: −1 file for the deleted exports guardrail, +2 new test files, +9 net new assertions across the two, plus the audit-log-filters count bump). **Integration suite run against the dev container's `DATABASE_URL` (user-confirmed), then re-provisioned** — first `docker compose up -d --build` without a prior `-v` down hit the known `[[local-docker-dev-stack]]` "setup-partman isn't idempotent" failure against a stale volume; redone as `down -v && up -d --build` for a clean volume, which succeeded. New `tests/db/add-contact.integration.test.ts`: adding a contact to a brand-new customer (zero contacts) results in `party_role.contact_medium` pointing at the new row; a direct attempt to point a *different* party role's preferred-contact pointer at another customer's contact row (via `partyRoleRepository.setPreferredContact` directly, bypassing the service — no code path in this module ever constructs this) is rejected by the composite deferrable FK, re-confirming `cm01`'s constraint end to end through the actual repository function the service calls. Full integration suite: 35 files / 298 tests green (was 34/296 after `cm10`). Dev stack re-provisioned via `down -v && up -d --build`; `/login` verified serving 200 again.

**Live-verified via headless Chromium (Playwright) against the running dev stack** — logged in as `admin@example.com` (confirming `cm01`'s retroactive `ADMIN → customers:EDIT` grant works end-to-end for Manage Customer, not just at the DB/guard level), created a customer, and on its edit page: the "Customer – Contact Details" section renders with an "Add contact" button; the inline form exposes all `contactFieldsSchema` fields (Name/Role/Phone/Email/Address Line 1&2/City/State/Postal/Country) plus Save/Cancel; submitting a name-only contact succeeded, showing the pink preferred-contact star and "No contact method on file" (cm05's reused empty-method rendering); adding a second contact with a phone number left the first contact's star untouched and gave the second contact its own preferred-*phone* star, not a contact-level one — visually confirming Module Inv. #4/#5 are independently tracked, not conflated. Dev stack re-provisioned once more afterward to clear the test data created during this click-through.

**Committed** as `0d80df8` ("add contact + auto-preferred contact/method (cm11)"). `cm12` (update contact) is next; it extends this same `contact-mutations.ts` file and `ContactManagerPanel` with `resolveUpdatedPreferredMethod` (preserve-unless-cleared preferred-method rules).

---

**cm12 implemented per `specs/cm12.md`** — extends `cm11`'s `contact-mutations.ts`/`ContactManagerPanel` with the update-contact mutation. `contactMediumRepository.findById`/`update` (extends `cm02`'s/`cm11`'s `db/repositories/contact-medium.ts`) — `update` is a field-only write (contact fields + `preferredContactMethod` + `lastModifiedBy`), no `refPartyRole`/`contactMediumId` in its settable data. `validation/customer/update-contact.schema.ts` (new) — `contactFieldsSchema.extend({contactMediumId, partyRoleId}).merge(optimisticLockSchema)`, verbatim per spec §3.2. `services/customer/contact-mutations.ts` (extended) — `resolveUpdatedPreferredMethod` implements the three-way case set verbatim per spec §2.1 (nothing preferred yet → same first-populated priority as `cm11`'s create path; still-populated → untouched, no reassignment logic runs; cleared while another remains populated → `{ok:false}`, Module Inv. #5; cleared to zero → `null`, allowed); `updateContact` reads `before` outside the transaction, resolves the preferred-method case **before** the transaction opens (a blocked resolution never touches `compareAndBumpLock`), then inside the transaction: lock-check → field update → `CONTACT_UPDATED` audit (no separate `PREFERRED_METHOD_CHANGED` event for the "nothing preferred yet" auto-assignment case, same economy as `cm11`). `actions/customer/update-contact.ts` (new) — same guard/safeParse/service-call/revalidate shape as every prior mutation action. `components/customers/contact-manager-panel.tsx` (extended) — each `ContactCard` gains an "Edit" affordance (hidden once a lock conflict has occurred — nothing in the panel is safe to resubmit until reload) that swaps the card for an inline `ContactEditForm`, pre-populated from the `ContactRow` via `contactRowToFormValues`; a `PREFERRED_METHOD_STILL_POPULATED` result surfaces as a field-level message ("Set a different preferred method before clearing this one.") next to whichever field corresponds to the contact's current `preferredMethod`, while a `CONFLICT` result bubbles up to the panel's existing shared `OptimisticLockConflictBanner` slot, same placement as every other mutation UI in the module. The 10-field form block itself was factored into a shared `ContactFieldsFieldset` sub-component reused by both the add form and the new edit form — a genuine, non-premature dedup (same fields/errors wiring appearing twice verbatim), not a new abstraction layer.

**Deviation from the spec's stated diff scope — the established audit-event-type ripple, not a local workaround:** `CONTACT_UPDATED` didn't exist in `types/audit.ts`'s `AUDIT_EVENT_TYPES` — added it, rippling into `types/audit-log.ts`'s `AUDIT_EVENT_CATEGORY_MAP` (`"Change"`, alongside `PARTY_ROLE_SPECIFICATION_UPDATED`/`PREFERRED_CONTACT_CHANGED`) and `tests/components/audit-log-filters.test.tsx` (event-type count 28→29, option count 29→30, plus an explicit "Change" optgroup assertion for the new type), same shape as `[[audit-event-type-addition-ripple]]`.

**Verified this session:** `npm run typecheck`, `npm run lint` (0 errors; the same pre-existing `new-customer-form.tsx` React Compiler `watch()` warning from `cm07`–`cm11`, unrelated to this unit's diff), `npm run format:check` all clean; `vitest run` (unit config) — 152 files / 1358 tests green (was 152/1340 before this unit's +18 new assertions across the extended `contact-mutations.service.test.ts`, `contact-manager-panel.test.tsx`, and `audit-log-filters.test.tsx`). **Integration suite run against the dev container's `DATABASE_URL` (user-confirmed), then re-provisioned** — 35 files / 298 tests green, same file/test count as `cm11`'s baseline, confirming no new integration test file was needed (per spec §3.6, this unit's coverage is unit-test-only).

**Live-verified via headless Chromium (Playwright) against the running dev stack** — logged in as `admin@example.com`, created a customer, added a contact with both a phone and email (phone auto-preferred per `cm11`'s priority), then: (1) edited the contact clearing only the phone field while the email remained populated — the inline "Set a different preferred method before clearing this one." message appeared and the edit was rejected; (2) editing again to also clear the email — cleared to zero populated methods successfully, contact reverted to the "No contact method on file" state. Both outcomes matched `resolveUpdatedPreferredMethod`'s spec'd behavior end to end through the real UI, not just mocked-repository unit tests. Dev stack re-provisioned once more afterward to clear the test data created during this click-through.

**Committed:** not yet — this tracker entry reflects the implementation as it stands in the working tree. `cm13` (delete contact) is next; it extends this same `contact-mutations.ts` file and `ContactManagerPanel` with the module's one physical delete.

---

**cm13 implemented per `specs/cm13.md`** — the module's one physical delete, extending `cm11`/`cm12`'s `contact-mutations.ts`/`ContactManagerPanel`. `contactMediumRepository.deleteById` (extends `db/repositories/contact-medium.ts`) — a plain `DELETE FROM contact_medium WHERE contact_medium_id = $1` with no guard of its own, per code-standards §6.7. `validation/customer/delete-contact.schema.ts` (new) — `{contactMediumId, partyRoleId}` merged with `optimisticLockSchema`, no contact fields since nothing is written to them. `services/customer/contact-mutations.ts` (extended, `deleteContact`) — verbatim per spec §2.1: `contactMediumRepository.findById` (⇒ `CONTACT_NOT_FOUND` if missing) then `partyRoleRepository.findById` checking whether `partyRole.contactMedium === input.contactMediumId` (⇒ `CANNOT_DELETE_PREFERRED_CONTACT`, Module Inv. #4's one enforcement point) — both checks run *before* the transaction opens; inside the transaction, `compareAndBumpLock` (⇒ `CONFLICT` on stale lock) then `deleteById` then a `CONTACT_DELETED` audit with the full pre-delete row as `beforeData` and `afterData: null` (the sole historical trace of the row, since this is a hard delete). `actions/customer/delete-contact.ts` (new) — same guard/safeParse/service-call/revalidate shape as every prior mutation action. `components/customers/contact-manager-panel.tsx` (extended) — `ContactCard` gains a destructive-styled "Delete" button (`Trash2` icon) for **non-preferred** contacts only; the preferred contact's row shows no delete control, instead a caption ("Make another contact preferred to delete this one") so the constraint is visible rather than only discoverable by failure. Clicking Delete opens `DeleteContactDialog` (new, built on `AlertDialog` not `Dialog` — same reasoning as `components/users/delete-user-dialog.tsx`'s tombstone confirm: the one genuinely irreversible action in the module deserves the one confirmation dialog that can't be backdrop-dismissed). A `CONFLICT` result from the dialog bubbles up to the panel's existing shared `OptimisticLockConflictBanner`, same placement as every other mutation UI in the module; both the Edit and Delete affordances are hidden once a conflict has occurred, matching cm12's "nothing is safe to resubmit until reload" rule.

**Deviations from the spec's stated diff scope — the established audit-event-type ripple, plus one test-layer reinterpretation:** (1) `CONTACT_DELETED` didn't exist in `types/audit.ts`'s `AUDIT_EVENT_TYPES` — added it, categorized `"Removal"` in `types/audit-log.ts`'s `AUDIT_EVENT_CATEGORY_MAP` (alongside `USER_DELETED`/`ROLE_DELETED`, the module's only other physical-delete-adjacent events) and `tests/components/audit-log-filters.test.tsx` (event-type count 29→30, option count 30→31, plus an explicit "Removal" optgroup assertion), same shape as `[[audit-event-type-addition-ripple]]`. (2) The spec's §3.6 "Integration" bullet reads "a direct `deleteContactAction` call ... is still rejected server-side" — taken literally this means the Server Action, but no integration test anywhere in this module calls an `actions/customer/*` function directly (`requirePermission` needs a real session this suite doesn't construct, the same limitation noted in `cm12`'s tracker entry for its own Integration bullet). Followed the module's established testing boundary instead: `tests/db/delete-contact.integration.test.ts` (new) calls the `deleteContact` **service** directly against a real DB — creating a customer, adding its first (auto-preferred) contact, and asserting a direct call to delete it is rejected with `CANNOT_DELETE_PREFERRED_CONTACT` with the row surviving; a second test adds a non-preferred contact, deletes it, and asserts the row is physically gone and a `CONTACT_DELETED` audit row exists with the full pre-delete data. This still proves the spec's point (the UI's button-omission is a UX nicety, not the actual boundary) at the same layer every other mutation integration test in this module operates at.

**Structural guardrail (§3.3):** `tests/structure/contact-medium-delete-callers.test.ts` (new) — walks `actions/`, `app/`, `components/`, `db/`, `services/` and asserts the string `contactMediumRepository.deleteById` appears nowhere but `db/repositories/contact-medium.ts` (its own definition) and `services/customer/contact-mutations.ts` (`deleteContact`, the one sanctioned caller). Same `node:fs`/`node:path` recursive-scan shape as `tests/guardrails/product-module-boundaries.test.ts`, not `fast-glob` (not a dependency of this repo).

**Verified this session:** `npm run typecheck`, `npm run lint` (0 errors; the same pre-existing `new-customer-form.tsx` React Compiler `watch()` warning from `cm07`–`cm12`, unrelated to this unit's diff), `npm run format:check` all clean; `vitest run` (unit config) — 153 files / 1372 tests green (was 152/1358 before this unit's new/extended test files). **Integration suite run against the dev container's `DATABASE_URL` (user-confirmed), then re-provisioned** — first pass caught a real test bug, not a product bug: the new `CONTACT_DELETED` audit-row query had no filter on `eventType`, so it non-deterministically matched the contact's earlier `CONTACT_CREATED` row instead; fixed by filtering on both `targetId` and `eventType`. Re-ran clean: full integration suite 36 files / 300 tests green (was 35/298 after `cm12`). Dev stack re-provisioned via `docker compose -f docker-compose.dev.yml down -v && up -d --build`; `/login` verified serving 200 again. Live dev-server manual click-through (non-preferred delete, preferred-contact caption, confirm-dialog Cancel/Delete) not exercised this session — DB-level and component-test verification only.

**Committed:** not yet — this tracker entry reflects the implementation as it stands in the working tree. `cm14` (set preferred contact) is next; it gives the MANAGER the actual reassignment path — the escape hatch for this unit's delete-blocked case.

---

**cm14 — spec written, not yet implemented.** `specs/cm14.md`: explicit reassignment among existing contacts, reusing `cm11`'s pointer-write function; the escape hatch for `cm13`'s delete-blocked case.

---

**cm15 — spec written, not yet implemented.** `specs/cm15.md`: the last of the module's nine mutation actions. Explicit reassignment of a contact's preferred *method* among its currently-populated methods only — deliberately no "clear" verb (a method's preference only ever goes to `null` as a side effect of `cm12`'s update-contact clearing the last populated field). Narrow `updatePreferredMethod` repository function kept separate from `cm12`'s broader `update` so neither scope bleeds into the other.

---

**cm16 — spec written, not yet implemented.** `specs/cm16.md`: the module's ship gate (tests-only boundary, following Product Management's `pm09` precedent). Extends `guard.integration.test.ts` with the 5-route × USER/MANAGER matrix plus (new relative to `pm09`, since this module has 9 mutation actions vs. Product's 0) a direct-Server-Action USER-denial loop across all `actions/customer/*`. Adds one genuinely new core-flow E2E test (no prior unit owned the chained flow, only individual steps) and two new static guardrail-sweep tests (module-boundary invariants + audit-completeness — one `writeAuditEvent` call per mutation, unique `eventType`s). All eight code-standards §9 guardrails ledgered as inherited-and-verified vs. net-new-to-cm16. Cannot be meaningfully executed until `cm01`–`cm15` are implemented and merged — it is the ship gate, not a spec-writing dependency.

## Open Questions

- None yet.

## Architecture Decisions

- Soft delete only via status (Organization → DISSOLVED, Customer → CLOSED); no physical deletes except contact rows
- `contact_medium` is flattened (one row per contact person, inline phone/email/address) rather than normalized child tables
- Preferred contact and preferred contact method enforced at the database level (composite deferrable FK / constraints), not by operator discipline
- Reuses existing platform as-is: better-auth RBAC (USER/MANAGER), `system_config`, pg_partman audit schema, conventions from User Management and Product Management

## Per-unit specs

| Unit | Spec file       | Summary                                                                                                                                                                                                                                                              |
| ---- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| cm01 | `specs/cm01.md` | DB foundation: `customer` schema (3 tables, 3 sequences, all constraints), migration `0009_customer.sql`, seed (`customers` permission + `CUSTOMER_SEARCH_RESULT_LIMIT`), permission registry wiring, `types/customer.ts` domain unions, guardrail tests. |
| cm02 | `specs/cm02.md` | Validation schemas (transitions, specification, organization, party-role, contact-medium) + three finder-only repositories + `services/customer` (`searchCustomers`, `getCustomerDetail`) + read-model types. No `app/**`, `actions/customer/`, or UI. |
| cm03 | `specs/cm03.md` | "Customer" `NAV_SECTIONS` entry (View/Manage) + new `AdminNav` locked/greyed-item capability for a permission the viewer lacks. |
| cm04 | `specs/cm04.md` | `app/(app)/customers/view/page.tsx`: READ guard, `q`-only search, `CustomerSearchPanel` + `CustomerResultsTable`, `OrganizationStatusBadge`/`CustomerStatusBadge`. |
| cm05 | `specs/cm05.md` | `app/(app)/customers/view/[id]/page.tsx`: three read-only sections (organization/role/contacts), `OrganizationTypeBadge`/`PreferredIndicator`/`InconsistencyBanner`, authoritative `isStatusInconsistent` rule. |
| cm06 | `specs/cm06.md` | `app/(app)/customers/manage/page.tsx`: EDIT guard, reuses `cm04`'s search panel/table, "Add new customer" CTA → `cm07`. |
| cm07 | `specs/cm07.md` | First mutation unit: create-customer action/service/repository shape, two-step similar-name confirm, registration-number uniqueness DB check, `SpecificationEditor`. |
| cm08 | `specs/cm08.md` | Edit-page container + first optimistic-lock implementation (`compareAndBumpLock`), reused through `cm15`. Implemented. |
| cm09 | `specs/cm09.md` | `StatusTransitionControl` (reused by `cm10`); `transition-organization-status` action/service. |
| cm10 | `specs/cm10.md` | Reuses `StatusTransitionControl` for the customer entity; `CustomerRoleForm`; folds in `update-party-role-specification`; `compareAndUpdateStatus`/`compareAndUpdateSpecification`. Implemented. |
| cm11 | `specs/cm11.md` | First contact mutation: `contact-mutations.ts`, `ContactManagerPanel`, first-contact-auto-preferred + phone>email>address priority rules. |
| cm12 | `specs/cm12.md` | `resolveUpdatedPreferredMethod` — preferred-method preservation/clearing rules on contact update. Implemented. |
| cm13 | `specs/cm13.md` | The module's one physical delete (contact), with a convention + structural-test-enforced precondition. Implemented. |
| cm14 | `specs/cm14.md` | Explicit set-preferred-contact reassignment; escape hatch for `cm13`'s delete-blocked case. |
| cm15 | `specs/cm15.md` | Explicit set-preferred-contact-method reassignment among currently-populated methods. |
| cm16 | `specs/cm16.md` | Ship gate: authz-matrix route × role matrix, Server-Action USER-denial loop, chained core-flow E2E, module-boundary + audit-completeness guardrail sweeps. |
