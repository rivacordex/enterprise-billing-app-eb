# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- Phase 1 — Data core (ac00-build-plan.md unit 1 of 17)

## Current Goal

- ac09 — Credit/Debit Notes (CRN/DBN) — next unit per the build plan.

## Status

8 of 17 units implemented (ac01–ac08). Module is mid-build — data core, seeds, onboarding, read pages, and the document-posting core (PAY + DEP) are done; CRN/DBN/ADJ, the reversal workbench, period close, GL journal export, and Accounts Settings config pages remain.

| Unit | Name                                                                                      | Status                                    |
| ---- | ----------------------------------------------------------------------------------------- | ------------------------------------------ |
| ac01 | pgledger Foundation (vendored fork, transform pipeline, ULID helpers, `billing.*` migration) | `0f19c95`                                   |
| ac02 | Module Tables & Views (10 `billing.*` tables, 3 composition views, repository skeletons)   | `f85367e`                                   |
| ac03 | Seed Set + GL Health (sys accounts, CoA, GL mappings, reason codes, bill cycles)           | `fe022d5` (+ review fixes `97b46dc`)        |
| ac04 | Customer Onboarding Wizard (atomic FA/BAN/ledger/binding creation on VALIDATED)            | `0eaa888` (+ review follow-ups `79ec7eb`)   |
| ac05 | Accounts Nav + Overview (search, FA/BAN detail, live balances, URL context strip)          | `06e3deb` (+ CodeRabbit fixes `a1942be`)    |
| ac06 | Ledger Explorer (account picker, transfers grid, two-leg drawer, zero-sum strip)           | `856fe3a`                                   |
| ac07 | Document Core + PAY (`money.ts`, state machine, atomic posting, capture + allocation)      | `8225a66`                                   |
| ac08 | Deposit Operations — DEP (capture, reverse-to-account, refund)                            | uncommitted (working tree)                  |

## Completed

- **ac03 — Seed Set + GL Health** (`context/accounting-management/specs/ac03-seed-set-and-gl-health.md`). Started and finished 2026-07-26. Not yet committed to git. Shipped: six `sys.*` MYR pgledger accounts (`sys.cash`, `sys.revenue`, `sys.revenue_adj`, `sys.write_off`, `sys.rounding`, `sys.tax_payable` — no `sys.deposit_movement`, deposit legs steer to `sys.cash`); 11-node CoA in `billing.gl_account` (1000/2000 summaries, 9 postable leaves); nine `billing.gl_mapping` rows (three role selectors with `currency IS NULL`, six system-account selectors with `currency = MYR`); ten `billing.reason_code` rows (four at `auto_post_limit = 0`); two `billing.bill_cycle` rows; three `core.system_config` wizard-default rows (`ACCOUNTS_DEFAULT_BILL_CYCLE` = BCY000001, `ACCOUNTS_DEFAULT_CURRENCY` = MYR, `ACCOUNTS_DEFAULT_CREDIT_LIMIT` = NULL). One idempotent seed script (`db/seeds/accounts/seed-accounts.ts`) composed of six helpers (`seed-sys-accounts.ts`, `seed-coa.ts`, `seed-gl-mappings.ts`, `seed-reason-codes.ts`, `seed-bill-cycles.ts`, `seed-wizard-defaults.ts`), `db:seed-accounts` script and `db:setup` wiring, `ledgerRepository.createAccount` implemented (`db/repositories/accounts/ledger.repository.ts`). V5 test (`tests/accounts/v05-gl-resolution.integration.test.ts`) green: 0 unmapped after seeding, 0 unmapped after onboarding fixture FA/BAN with three bindings, catalog integrity assertions, idempotency. `typecheck`/`lint`/`format:check` all clean (1 pre-existing lint error + 32 pre-existing warnings, unchanged), 1615 unit tests still green.
  - **`deposit_movement`/no-sys-account (§2.2 resolution):** `POSTING_NATURES` includes `deposit_movement` but deposit legs steer to `sys.cash` — no `sys.deposit_movement.MYR` account seeded. Flagged for ac08 (deposit ops) to steer to `sys.cash`, matching Q16's `deposits ↔ sys.cash` story. Additive if later finance needs a dedicated line.
  - **Credit-limit default (§2.6 resolution):** `ACCOUNTS_DEFAULT_CREDIT_LIMIT` seeded with `configValue = NULL` (no pre-fill). ac04 reads the key and falls back gracefully; ac15 can set a non-null value. If a hard default is wanted, only the seeded value changes, not the schema.
  - **pgledger name non-uniqueness (codebase finding):** `pgledger_create_account` has no UNIQUE constraint on `name` — the seed's pre-check (`pgledger_accounts_view WHERE name = ?`) is what prevents duplicates on re-run. The V5 idempotency test verifies count stays at 6, not by calling the function twice. Documented in V5 test comment.

- **ac01 — pgledger Foundation** (`context/accounting-management/specs/ac01-pgledger-foundation.md`). Started and finished 2026-07-25. Not yet committed to git (uncommitted working tree — commit is the user's call). Verified locally: `npm run pgledger:transform` idempotent (clean re-run), transform unit tests green (7/7, `tests/pgledger/transform.test.ts`), fresh-DB migrate + scratch + V1 zero-sum + engine guardrail tests green (`tests/accounts/v01-zero-sum.integration.test.ts`, run against a throwaway Postgres 16 container — never against the shared dev-stack `DATABASE_URL`), full existing test suite still green (1586 unit + 356 integration tests across 38 integration files, including the 37 files that got the new `billing` schema drop), `typecheck`/`lint`/`format:check` all clean, `db:generate` confirmed a no-op ("No schema changes, nothing to migrate").
- **ac02 — Module Tables & Views** (`context/accounting-management/specs/ac02-module-tables-and-views.md`). Started and finished 2026-07-26. Not yet committed to git (uncommitted working tree). Shipped: 10 `billing.*` Drizzle tables (`db/schema/billing/{accounts,catalogs,documents,ledger-binding,periods}.ts`) with their sequences/ID-prefix defaults, CHECK constraints, cross-schema FKs (`customer.party_role`, `core.appuser`), and the UNIQUE constraints (`ledger_binding` triple, `document_line` transfer + `(ref_document_id,line_no)`, `gl_mapping` triple, `bill_cycle.name`); the 3 composition views (`account_view`, `gl_resolution_view`, `gl_journal_view`) as hand-appended raw SQL (`db/schema/billing/views.sql`) declared `.existing()` for typed reads (`db/schema/billing/views.ts`); one migration `db/migrations/0012_billing_module_tables.sql` (drizzle-kit-generated tables + hand-appended views, `CREATE SCHEMA` corrected to `IF NOT EXISTS` since ac01 already created `billing`); `billing` added to `drizzle.config.ts`'s `schemaFilter` plus the `tablesFilter: ["!pgledger_*"]` exclusion (verified: `db:generate` re-run is a no-op, pgledger tables untouched); `types/accounts.ts` (the 8 code-standards §2.1 unions + `TmfAccountRef`/`TmfRelatedParty` + re-exported row types); JSONB shape guards in `validation/accounts/{contact,mode-ref,metadata}.schema.ts`; repository skeletons under `db/repositories/accounts/*.ts` (typed signatures, `throw new Error("not implemented (acNN)")` bodies) plus a working `account-view.repository.ts` (`findByAccountId`/`search`) and trivial `findById` readers on `financial-account`/`billing-account` repositories. Verified locally: new unit test `tests/db/billing-schema.test.ts` (29 tests: column sets, nullability, CHECK/UNIQUE presence, no stored-balance columns) and new integration test `tests/db/billing-schema.integration.test.ts` (8 tests: schema/table/view existence, `FIN`/`BAN` id-format, per-type document-sequence prefixes, the `account_view` fixture headline test with composed `relatedParty[]`, CHECK/UNIQUE/cross-schema-FK rejections) both green against a throwaway Postgres 16 container; full existing suite still green (1615 unit + 364 integration tests across 39 integration files); `typecheck`/`format:check` clean, `lint` clean of new errors (35 pre-existing-pattern `no-unused-vars` warnings on the skeletons' intentionally-unused params, non-blocking; one unrelated pre-existing `error` in `components/customers/organization-form.tsx` not touched by this unit). No seeds, no services, no pages, no repository method bodies beyond `account_view`/`findById` — as scoped.

## In Progress

- None.

## Completed (recent)

- **ac07 — Document core + PAY** (`context/accounting-management/specs/ac07-document-core-and-payment.md`). Started and finished 2026-07-29. Shipped the module's posting spine: `money.ts` extended with `sum(...)` (the file itself already existed from ac05 — no duplicate created, resolving the spec's codebase-verification note (a)); `types/accounts.ts`'s `DOC_TRANSITIONS` map and `AssignedItem` type; `services/accounts/document-state-machine.ts` (`submitDocument` — threshold routing Q20, `approveDocument` — approver ≠ creator Inv. #6, `cancelDocument`, plus `submitDocumentStandalone`/`approveDocumentStandalone` wrappers that open their own transaction for actions, since `actions/**` may not import `db/client` directly — boundaries lint); `services/accounts/post-document.ts` (`postDocument` — the sole `pgledger_create_transfers` caller, guard/balanced-doc/period-validation/nature-steering/audit per §2.3, plus the `postExplicitLegs` explicit-leg primitive required by spec §3.3); `services/accounts/leg-templates.ts` (PAY templates for `capture`/`allocation`/`release`/`refund`, keyed by `(doc_type, line_kind)`); `services/accounts/{capture-payment,allocate-payment,refund-payment}.ts`; the `accounts_transactions` permission migration (`db/migrations/0017_accounts_transactions_permission.sql` — the typed constant and `PERMISSION_NAMES` entry already existed from ac04's dual-permission wizard check; only the DB row + role grants were missing) with MANAGER/USER/ADMIN `:EDIT` grants added to `seed-accounts-permissions.ts`; five validation schemas (`document-base` shared Q29 fields + `capture-payment`/`allocate-payment`/`refund-payment`/`submit-document`/`approve-document`) and five actions; repository write bodies filled (`document.repository` — per-type-sequence `insert` + `compareAndUpdateState` + `findPendingApprovalsForFinancialAccount`; new `document-line.repository.ts` — `insert`/`findById`/`findByDocumentId`/`setTransferId`/`setReversedByLineId`/`findAllocationsForBillingAccount`; `reason-code.repository`/`accounting-period.repository` filled; `ledger.repository.pgledgerCreateTransfers` batch caller; `billing-account.repository.updatePaymentStatus`); UI (`doc-state-badge.tsx`, `capture-payment-panel.tsx`, `allocate-payment-panel.tsx`, `payment-refund-panel.tsx`, `pending-approvals-list.tsx`, `/accounts/transactions` page, nav entry, `route-manifest.test.ts` updated). `DOCUMENT_POSTED` added to `AUDIT_EVENT_TYPES`/`AUDIT_EVENT_CATEGORY_MAP` (Additive) with the matching `audit-log-filters.test.tsx` count ripple (43→44 event types). Tests: `money.test.ts` (unit), `v11-document-state-machine.integration.test.ts`, `v03-balance-equals-ledger.integration.test.ts` (new — the real posting path, `v03-live-balances.integration.test.ts` from ac05 left untouched), `v08-payment-status.integration.test.ts`, `refund-payment.integration.test.ts` (overpayment / cash-refund-of-settled / convert-to-advance, all four-eyes), `route-level-transactions.test.ts` (structural + grep-gates). Verified against a throwaway Postgres 16 container: all 7 accounts integration files touched/added this unit pass (44 tests); full unit suite green (typecheck/lint/format:check all clean).
  - **Design decision — `release` registered as a genuine leg template, not routed through `postExplicitLegs`:** the refund workbench's allocation-reversal leg (`fa.{FIN}.unapplied_cash → ban.{BAN}.receivables`, the exact opposite of `allocation`'s leg) is fully deterministic from `(doc_type, line_kind, FA, BAN)` alone — no different in kind from `capture`/`allocation`/`refund`. Registering it in `leg-templates.ts` keeps `approve-document`'s posting path fully generic (it must stay reusable, unmodified, by ac08–ac10 per the ai-workflow-rules), rather than special-casing `PAYMENT_REFUND` documents inside the generic approval flow. `postExplicitLegs(tx, documentId, actorId, legs)` is still implemented and exported in `post-document.ts` exactly to spec §3.3's shape, ready for ac11's *true* arbitrary reversals (where the leg direction must be read from an existing transfer's actual accounts, not a fixed per-kind shape) — but ac07's own refund flow does not call it. A deliberate deviation from a literal reading of "reversal legs post via `postExplicitLegs`," recorded here rather than by editing the spec.
  - **Allocation's reason code is hardcoded to `CUST_PAYMENT`** (not user-selected) — there is no dedicated "allocate" reason code in the ac03 seed set, and applying already-received cash isn't itself a new external money movement. `refSettledDocumentId` is optional/nullable (a general application against a BAN's outstanding receivables is valid without pointing at one specific charge) since DBN document creation doesn't exist until ac09.
  - **Period-absence resolution (spec §"Note on codebase verification" placeholder "or absent-and-policy-closed"):** no period-close policy toggle exists in this unit (ac14 owns the full period open/close workflow) — an absent `accounting_period` row is treated as **open**; only an existing row with `state = 'closed'` rejects a post. V11's closed-period test seeds an explicit closed row, matching this resolution.
  - **Two real bugs found and fixed during verification, not spec issues:** (1) `document.repository.ts`'s per-type sequence lookup used `sql\`nextval(${sql.raw(seqName)})\`` — `sql.raw()` inlines the sequence name as a bareword, which Postgres parsed as `billing.document_pay_seq` (schema-qualified column reference), not the string literal `nextval(regclass)` expects, failing with "missing FROM-clause entry for table billing"; fixed by binding it as a normal parameter cast `${seqName}::regclass`. (2) `ledger.repository.ts`'s `pgledgerCreateTransfers` passed its `metadata` object straight through `${metadata}::jsonb` in a `drizzle-orm` `sql\`\`` template consumed via `tx.execute()` on the `postgres-js` driver — unlike `postgres.js`'s *own* tag (which ac06 documented as auto-serializing, such that pre-stringifying there double-encodes), drizzle's `sql` template does **not** serialize a plain object for a jsonb-cast bound parameter, and fails at bind time (`TypeError: ... argument must be of type string ... Received an instance of Object`); fixed by `JSON.stringify(metadata)` before interpolating. Both reproduced and fixed against a throwaway Postgres 16 container — the fix is the opposite direction from ac06's jsonb finding, so the rule is per-code-path, not universal: `db.execute(sql\`...\`)` (drizzle template) needs `JSON.stringify()`, a raw `postgres.js` tag does not.
  - **Pre-existing environment quirk reconfirmed, not an ac07 bug:** `v02-binding-integrity`, the pre-existing `v03-live-balances`, and `v07-onboarding-atomicity` integration tests still fail in this session's environment with `expectedLastModifiedDatetime.getTime is not a function` — the same `migrate()`-poisons-its-own-connection issue ac06 documented and worked around. Every ac07 test file uses ac06's two-connection pattern (schema-drop + `migrate()` on a short-lived connection, closed, then a fresh connection for the rest of the test) and passes cleanly; the three pre-existing files were **not** touched (out of scope, same precedent as ac06).
  - **Pre-existing `drizzle-kit generate` drift (not ac07's bug):** a plain `db:generate` re-run reproducibly proposes dropping and recreating `accounting_period_period_format_check` (a snapshot-tracking quirk from migration 0016's `VALIDATE CONSTRAINT` statements, present before this unit's changes) — discarded both times it was generated; not part of the `0017` migration.

- **ac08 — Deposit Operations (DEP: capture, reverse-to-account, refund)** (`context/accounting-management/specs/ac08-deposit-operations.md`). Started and finished 2026-07-29. Pure reuse of ac07's posting core, no schema/permission change. Shipped: `leg-templates.ts` extended with `DEP` entries (`capture`: `fa.deposits → sys.cash`; `release`: `fa.unapplied_cash → fa.deposits`, distinct from `(PAY, release)` despite sharing the `release` line_kind since the map is keyed on `doc_type` too; `refund`: reuses `PAY_LEG_TEMPLATES.refund` directly per spec §3.1's noted sharing) and its `LegTemplateContext` gained `financialAccountDepositsId`; `services/accounts/{capture-deposit,reverse-deposit,refund-deposit}.ts` (thin, one document + one line each, delegating to `submitDocument`); three validation schemas + three actions (`accounts_transactions:EDIT`); three DEP panels (`capture-deposit-panel.tsx`, `reverse-deposit-panel.tsx`, `refund-deposit-panel.tsx`) wired into `/accounts/transactions`, greyed until `?fa` (no `?ban` needed, Q4/Q1). `tests/accounts/v14-deposit-lifecycle.integration.test.ts` (5 tests): capture 10,000 posts directly (USER, ≤ 50,000 limit) → reverse (always four-eyes, self-approval rejected, MANAGER posts) → allocate 6,000 against a fixture A/R via ac07's unmodified allocation → refund the 4,000 remainder (always four-eyes, self-approval rejected, MANAGER posts) → asserts `deposits = 0`, `unapplied = 0` (Q11 closure-eligible), V1 zero-sum throughout. `route-level-transactions.test.ts` extended with the three new panels/services in its structural + grep-gate checks. Verified against a throwaway Postgres 16 container: V14 (5/5) plus the full `tests/accounts/*.integration.test.ts` suite (8/11 files, 51/51 non-skipped tests) pass — the other 3 failures (`v02-binding-integrity`, `v03-live-balances`, `v07-onboarding-atomicity`) are the pre-existing `migrate()` connection-poisoning quirk documented in ac06/ac07's notes below, unrelated to this unit and not touched, same precedent. Full unit suite (1702 tests) green; `typecheck`/`lint`/`format:check` all clean.
  - **Design decision — `post-document.ts`'s nature→sys-account resolution changed from a `Set<PostingNature>` membership check to a `Record<PostingNature, string>` name map:** ac03 seeded no `sys.deposit_movement` account (flagged in ac03's own progress-tracker entry above, "Flagged for ac08 ... to steer to `sys.cash`") — `deposit_movement` needs a sys-account leg (DEP capture/refund) but must resolve to the *existing* `sys.cash.{ccy}` account, not a same-named one. `NATURE_SYS_ACCOUNT_NAME` now maps `deposit_movement → "cash"` (every other nature maps to itself), preserving Module Inv. #8 ("posting nature steers the counter-account") exactly — only the mapping is no longer always the identity function. This is the one required edit to ac07's posting core for ac08's reuse; `postDocument`'s control flow, guard order, and every other service (`capture-payment.ts`, `allocate-payment.ts`, `refund-payment.ts`, `document-state-machine.ts`) are untouched.
  - **`refund-deposit.schema.ts`'s optional `payment_mode`/`mode_ref` pair (§2.5's "decide per finance need") modelled as a plain nullable field pair with a `superRefine` shape-check, not a discriminated union:** capture's `payment_mode`/`mode_ref` are mandatory (a true discriminated union works cleanly there, matching `capture-payment.schema.ts`); refund's pair is optional as a whole (no payout reference is a valid refund), which a `z.discriminatedUnion` can't express directly without an artificial `payment_mode: null` branch. The `superRefine` still enforces the same per-mode jsonb shape (code-standards §6.5) whenever a mode is chosen.
  - **`refund-deposit.ts` reuses `refund-payment.ts`'s exported `getUnappliedCashAvailable`** rather than duplicating the balance read — both compute the identical "held magnitude of `fa.{FIN}.unapplied_cash`" figure (Module Inv. #2 live read), and DEP refund pays out of the same pot per the reconciled V14 direction.

- **ac06 — Ledger Explorer** (`context/accounting-management/specs/ac06-ledger-explorer.md`). Started and finished 2026-07-29. Shipped: `LedgerKindChip` (`components/accounts/ledger-kind-chip.tsx`) and `BalanceCheckStrip` (`components/accounts/balance-check-strip.tsx` — full-bleed `--acct-balance-ok`/`bg-destructive`, V1 surfaced live); `LedgerAccountPicker` (native-GET search over `pgledger_accounts_view.name`), `LedgerTransfersGrid` (client component — `event_at` range + metadata search, URL-param sort, server pagination, matching `offering-table.tsx`'s pattern), `TransferDetailDrawer` (inline detail panel, both signed legs + previous→current balances, matching `offering-detail-region.tsx`'s selection-via-URL convention rather than a floating overlay); `services/accounts/ledger-explorer.ts` (`searchLedgerAccounts`, `listTransfersForAccount`, `getTransferLegs`, `zeroSumByCurrency`, `resolveLedgerAccountLabel`, `resolveReceivablesAccountForBan`); `ledger.repository.ts` extended with `searchAccountsByName`/`findAccountById`/`listTransfersForAccount`/`findTransferById`/`findEntriesByTransferId`/`zeroSumByCurrency` (still the only `pgledger_*_view` caller); `ledgerBindingRepository.findByPgledgerAccountId` (reverse lookup for `resolveLedgerAccountLabel`); `validation/accounts/ledger-explorer-search-params.schema.ts` (`?account/transfer/from/to/q/sort/page/acctq`, lenient `.catch()` parsing); `app/(app)/accounts/ledger/page.tsx` (`force-dynamic`, `accounts_view:READ`, honors both its own URL state and the shared `?ban/fa/party` context — arriving with `?ban` pre-picks that BAN's receivables account); nav entry ("Ledger Explorer", `Compass` icon). Ledger-explorer domain types (`LedgerAccountKind`/`LedgerAccountLabel`/`LedgerAccountSearchResult`/`LedgerAccountSummary`/`LedgerTransferListItem`/`LedgerTransferLeg`/`LedgerTransferDetail`/`ZeroSumRow`) live in `types/accounts.ts`, not the service file — `eslint-plugin-boundaries` forbids `components → services` imports, only `components → types`. Tests: `tests/accounts/route-level-ledger.test.ts` (structural guardrails) + `tests/accounts/ledger-explorer.integration.test.ts` (10 tests: kind-chip resolution, picker search, end-to-end trace with correct signed legs + running balances, `event_at`/metadata filters, sort round-trip, server pagination, V1-in-UI baseline + a deliberately-imbalanced test-only fixture flipping the strip to broken). `tests/app/route-manifest.test.ts`'s frozen route allowlist updated with `/accounts/ledger`. All 1675 unit tests green; `typecheck`/`lint`/`format:check` clean. No writes, no new permission (reuses `accounts_view`).
  - **Two real bugs found and fixed during verification, not spec issues:** (1) `Date` objects interpolated directly into a `drizzle-orm` `sql\`\`` template consumed via `db.execute()` on the `postgres-js` driver fail at bind time (`TypeError: ... Received an instance of Date`) — fixed by calling `.toISOString()` before interpolating (`ledger.repository.ts`'s `listTransfersForAccount`). (2) `postgres.js`'s own `sql\`\`` template auto-serializes a value for a `::jsonb`-cast parameter — pre-stringifying with `JSON.stringify()` before interpolating double-encodes it into a jsonb **string** (so `->>'key'` silently returns `NULL`, not an error); fixed in the test fixture by using `sql.json(value)` instead. Neither is an ac06 app-code path (both were in test/fixture code), but worth remembering for any future raw-SQL test fixture that binds a `Date` or jsonb value.
  - **Environment finding (not an ac06 or app-code bug):** `drizzle-orm`'s `migrate()` running many raw DDL statements over a `postgres.js` connection leaves *that connection's* built-in type-OID cache poisoned — every later `timestamp(tz)` column read back on the *same* connection returns a raw string instead of a parsed `Date` (reproduced independently against a throwaway container; also broke the pre-existing `v02`/`v03`/`v07` integration tests in this session's environment, unrelated to ac06's diff). Worked around locally in `ledger-explorer.integration.test.ts` by running schema-drop + `migrate()` on a short-lived connection, closing it, then opening a fresh connection for the rest of the test. The shared `v02`/`v03`/`v07` tests were **not** touched (out of this unit's scope) — if this resurfaces, the same two-connection pattern is the fix.

- **ac05 — Accounts nav + Overview page** (`context/accounting-management/specs/ac05-accounts-nav-and-overview.md`). Started and finished 2026-07-28. Shipped: `accounts_view` permission migration (`db/migrations/0015_accounts_view_permission.sql`); idempotent seed helper (`db/seeds/accounts/seed-accounts-permissions.ts`) granting READ to MANAGER/USER/ADMIN, `ACCOUNTS_SEARCH_RESULT_LIMIT = "5"` in `system_config`; CSS design tokens for the Accounts module (`app/globals.css`); "Accounts" nav section with `Landmark` icon (`components/admin-nav.tsx`); `parseAccountsContext` URL parser (`validation/accounts/parse-accounts-context.ts`); `money.ts` bigint-sen arithmetic with `stringToSen`/`senToString`/`add`/`subtract`/`compare`/`openReceivable`/`isHeldLiability`; `ledgerRepository.balanceByLedgerAccountId` + `sumReceivablesForFinancialAccount`; `financialAccountRepository.searchForOverview`; services `searchAccounts`, `getFinancialAccountDetail`, `getBillingAccountDetail` (with pure `deriveOverdue`); shared components `AmountCell`, `PaymentStatusBadge`, `ContextStrip`; server-only Overview page (`app/(app)/accounts/overview/page.tsx`, `force-dynamic`, native GET form, no write affordance). Tests: 10 parse-context unit tests, 9 overdue-derivation unit tests, 6 route-level guardrail tests, V3 live-balance integration test. All 1661 unit tests green. TypeScript clean.

- **ac04 — Customer Onboarding Wizard** (`context/accounting-management/specs/ac04-customer-onboarding-wizard.md`). Started and finished 2026-07-26. One atomic `db.transaction`: (1) `compareAndUpdateStatus → VALIDATED`, (2) FA insert (`billing.financial_account`), (3) BAN insert (`billing.billing_account`), (4) `pgledger_create_account` ×3 → `fa.{FIN}.unapplied_cash`, `fa.{FIN}.deposits`, `ban.{BAN}.receivables`, (5) three `ledger_binding` rows, (6) `ACCOUNTS_ONBOARDED` audit event. Dual-permission check (`accounts_transactions:EDIT` + `customers:EDIT`). Wizard dialog injected into `customer-role-form.tsx`'s VALIDATED-intercept path via deferred Promise. Returning-customer gate shows prior FAs for same org. `services/accounts/term-resolution.ts` (pure `resolveTerm`). New type registrations: `ACCOUNTS_ONBOARDED` audit event, `accounts_view`/`accounts_transactions`/`accounts_config` permission names + display labels. `EffectivePermissionMap` made intersection with optional accounts permissions; `meetsLevel` handles `undefined`. Tests: `tests/services/onboard-customer-accounts.service.test.ts` (16 unit tests — all green), `tests/accounts/v02-binding-integrity.integration.test.ts` (V2 invariant), `tests/accounts/v07-onboarding-atomicity.integration.test.ts` (V7 invariant). Full suite: 175/175 files, 1632/1632 tests green. `typecheck` clean.

## Next Up

- ac09 — Credit/Debit Notes (CRN/DBN) — the next operation set over the same posting core.

## Open Questions

- None — Q1–Q28 in `_newmodule-account-plan.md` are all resolved; ac01 raised no new Q.

## Architecture Decisions

- None yet beyond the plan's Q1–Q28.

## Session Notes

- **Code-review fixes applied 2026-07-29** (three-commit batch: ac06/ac07/ac08). All changes are minimal and targeted; see git diff for details. Fixed findings:
  - **All 6 server actions** (`approve-document`, `capture-payment`, `submit-document`, `capture-deposit`, `reverse-deposit`, `refund-deposit`): Bare `catch {}` replaced with `catch (e) { if (!isRedirectError(e)) throw e; return FORBIDDEN; }` so DB/session infrastructure errors are not silently swallowed as permission denials.
  - **All 7 component panels** (`allocate-payment`, `capture-payment`, `payment-refund`, `pending-approvals-list`, `capture-deposit`, `reverse-deposit`, `refund-deposit`): `handleSubmit`/`handleApprove` wrapped in `try/catch/finally` so `setSubmitting(false)` / `setBusyId(null)` always runs.
  - **`today()` helper** in `allocate-payment-panel` and `payment-refund-panel`: Changed from `toISOString().slice(0,10)` (UTC date) to local-date-component derivation so default dates use the user's local calendar day.
  - **`post-document.ts` `finalizePosting`**: CAS failure after ledger writes now throws instead of returning soft CONFLICT so the enclosing `db.transaction` rolls back (prevents orphaned ledger entries).
  - **`allocate-payment.ts`**: `refSettledDocumentId` validation now also checks that the settled document belongs to the supplied BAN (ownership guard).
  - **`refund-payment.ts`**: Duplicate `allocationLineId` detection added before the resolve loop.
  - **`capture-deposit.ts`**: Sentinel pattern added so typed `submitDocument` failures roll back the transaction (no orphaned draft docs on PERIOD_CLOSED etc.).
  - **`reverse-deposit.ts`** and **`refund-deposit.ts`**: Balance checks moved inside `db.transaction` using the `tx` handle to close TOCTOU race; sentinel pattern added for `submitDocument` failures.
  - **`route-level-transactions.test.ts`**: Number() grep regex updated to use negative lookbehind `(?<![.\w])` so `toNumber`/`parseNumber` identifiers are not false-positives.
  - **`ledger-account-picker.tsx`**: `sort: undefined` added to account-selection `buildUrl` override; hidden inputs added to the GET search form to preserve existing URL params (`party`, `fa`, `ban`, `from`, `to`, `sort`, `q`).
  - **`ledger-explorer-search-params.schema.ts`**: `from`/`to` fields now reject calendar-invalid dates (e.g. `2024-13-45`) via `.refine(isCalendarDate)` before the `.catch(null)` fallback.
  - **`approve-document.schema.ts`** and **`refund-payment.schema.ts`**: Deprecated `ZodObject.merge()` replaced with `...schema.shape` spread.
  - **`transactions/page.tsx`**: `listPendingApprovals` and `getRefundWorkbenchData` are now conditioned on `canEdit` (skip DB round-trips for read-only users).
  - **`balance-check-strip.tsx`**: `role` now resolves to `"status"` when all OK and `"alert"` when the invariant is broken.
  - **`ac08-deposit-operations.md`**: Wrong `*.action.ts` filenames in boundary list corrected to `*.ts`.
  - Skipped (with reason): timestamp precision in `document.repository.ts` (schema already has `precision: 3`); `text-mono` in `transfer-detail-drawer.tsx` (valid custom Tailwind class in `globals.css`); `getUnappliedCashAvailable` throw (consistent with codebase); batch label resolution in `ledger-explorer.ts` (performance, not correctness); `v14` test Number() helpers and action-level approval (test-only, service-level coverage exists); receivables-binding throw in `allocate-payment.ts` (same pattern as rest of codebase); VALIDATION_ERROR field display (complex UI wiring, separate concern); `ledger-transfers-grid.tsx` row accessibility (complex refactor); `ledger.repository.ts` deduplication (refactor); `get-billing-account-detail.ts` due-date logic (outside-diff, complex business change).

- **ac01 codebase-verification findings** (spec's "Note on codebase verification", both now resolved and corrected here rather than by editing the spec's placeholder text):
  1. **Next free migration index: `0011`** (`db/migrations/0000`…`0010` already exist through `product_offering_family`). Generated via `drizzle-kit generate --custom --name=billing_pgledger`, producing `db/migrations/0011_billing_pgledger.sql` + the journal entry + `meta/0011_snapshot.json` (a no-op diff vs `0010` — `billing` is not yet in `drizzle.config.ts`'s `schemaFilter`, so nothing to diff).
  2. **An existing Postgres ULID generator does exist in `core`** (`core.generate_ulid()`, added by um27, returns `uuid`) — but it is **not** a substitute for what this unit needs, so §3.3's vendored `ulid.sql` was **not** dropped. Reasoning: upstream `pgledger.sql` already contains its own id-generation logic (`pgledger_uuidv7()` + `pgledger_generate_id(prefix)`) and depends on exactly one external symbol, `uuid_to_ulid(uuid) → text`, vendored upstream itself from `scoville/pgsql-ulid` (BSD-3-Clause) at `pgr0ss/pgledger`'s own `vendor/scoville-pgsql-ulid/uuid-to-ulid.sql`. `core.generate_ulid()` has a different signature/purpose (generates a whole new ULID-as-uuid from scratch; pgledger needs a uuid→ULID-**text** converter applied to a uuid it already generated internally). Swapping in `core.generate_ulid()` would require hand-editing `pgledger_generate_id`'s body — forbidden by module inv. #14 ("changes namespaces, never behaviour"). So `db/pgledger/ulid.sql` vendors `uuid-to-ulid.sql` verbatim (qualified into `billing`), matching pgledger's own upstream dependency exactly.
- **Upstream identity confirmed:** `pgledger` = `pgr0ss/pgledger` (Paul Gross, MIT License). `UPSTREAM_COMMIT` = `43240dbdfc291eca5380cbcee7dfe594922c67d6` (2026-02-06, GPG-verified, tip of `main` at implementation time). The vendored ULID helper (`db/pgledger/ulid.sql`) traces to `scoville/pgsql-ulid` (BSD-3-Clause), vendored transitively via pgledger's own `vendor/scoville-pgsql-ulid/uuid-to-ulid.sql` — both licenses are permissive and compatible with vendoring into this proprietary codebase; provenance recorded in each file's header for future license-audit purposes.
- **Test-suite ripple applied:** per the platform pattern (every prior module's pgSchema needs this), added `await sql.unsafe('DROP SCHEMA IF EXISTS "billing" CASCADE');` to the `beforeAll`/`afterAll` of all 37 existing `*.integration.test.ts` files that do the full-schema drop+migrate cycle, immediately before their existing `"customer"` drop line — otherwise a later integration test's `migrate()` hits `CREATE TABLE billing.pgledger_accounts` on a non-empty `billing` schema left behind by an earlier test file and fails with "already exists".
- `billing` is **not** added to `drizzle.config.ts`'s `schemaFilter` in this unit (still `["core", "product", "customer"]`) — ac02 adds it alongside the module's Drizzle schema files, together with the `tablesFilter` exclusion for `pgledger_*` called out in spec §2.5/§3.5 (todo for ac02, not rediscovered here since `billing` isn't filtered in yet, so `db:generate` cannot touch it either way).
- **ac02 codebase-verification findings** (spec's "Note on codebase verification", all three resolved and corrected here rather than by editing the spec's placeholder text):
  1. **Next free migration index: `0012`** (`0011` is ac01's pgledger fork). Generated via `drizzle-kit generate --name=billing_module_tables`, producing `db/migrations/0012_billing_module_tables.sql` + the journal entry + `meta/0012_snapshot.json`; the generated `CREATE SCHEMA "billing"` was hand-corrected to `CREATE SCHEMA IF NOT EXISTS "billing"` since ac01's migration already creates the schema (drizzle-kit doesn't know that — `billing` wasn't in `schemaFilter` when 0011 was generated).
  2. **Cross-schema id columns confirmed exactly as the spec assumed:** `customer.party_role.party_role_id` (`db/schema/customer.ts`) and `core.appuser.id` (Drizzle property `id`, physical column `user_id` — Better-Auth's adapter hardcodes lookups of a literal `id` field, `db/schema/identity.ts`). No correction needed.
  3. **View mechanism: raw-SQL views hand-appended to the migration** (spec's assumed option), authored once in `db/schema/billing/views.sql` and pasted into `db/migrations/0012_billing_module_tables.sql` after the generated table DDL. Drizzle **can** express a `pgSchema('billing')` view for *typed reads* via `.view(name, columns).existing()` (`db/schema/billing/views.ts`) — confirmed working (`db:generate` re-run stays a no-op, the `.existing()` views are never diffed/managed) — but `.existing()` only gives read typing, it cannot author the `CREATE VIEW` DDL itself (no query-builder path can express the `pgledger_*` joins), so the raw-SQL half of the plan still stands.
  4. **`gl_resolution_view` currency-precedence gap identified and closed defensively, not by the spec's own wording:** a plain equality join on `gl_mapping (selector_type, selector, currency)` can't rule out a selector having *both* a currency-specific row and an all-currencies (`currency IS NULL`) row simultaneously — the table's UNIQUE constraint only guarantees at most one row per exact triple, not "at most one match across the OR'd currency condition." Implemented with a `LEFT JOIN LATERAL … ORDER BY currency NULLS LAST LIMIT 1` so a currency-specific mapping always wins over an all-currencies one when both exist, keeping resolution deterministic (Module Inv. #10) even though seeding (ac03) is what actually decides whether that overlap ever occurs in practice.