# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- Phase 1 — Data core (ac00-build-plan.md unit 1 of 17)

## Current Goal

- ac04 — onboarding wizard (reads `ACCOUNTS_DEFAULT_BILL_CYCLE`/`ACCOUNTS_DEFAULT_CURRENCY` config and creates the real bindings ac03's V5 test proves resolve).

## Completed

- **ac01 — pgledger Foundation** (`context/accounting-management/specs/ac01-pgledger-foundation.md`). Started and finished 2026-07-25. Not yet committed to git (uncommitted working tree — commit is the user's call). Verified locally: `npm run pgledger:transform` idempotent (clean re-run), transform unit tests green (7/7, `tests/pgledger/transform.test.ts`), fresh-DB migrate + scratch + V1 zero-sum + engine guardrail tests green (`tests/accounts/v01-zero-sum.integration.test.ts`, run against a throwaway Postgres 16 container — never against the shared dev-stack `DATABASE_URL`), full existing test suite still green (1586 unit + 356 integration tests across 38 integration files, including the 37 files that got the new `billing` schema drop), `typecheck`/`lint`/`format:check` all clean, `db:generate` confirmed a no-op ("No schema changes, nothing to migrate").
- **ac02 — Module Tables & Views** (`context/accounting-management/specs/ac02-module-tables-and-views.md`). Started and finished 2026-07-25/26. Not yet committed to git (uncommitted working tree — commit is the user's call). Delivered: `db/schema/billing/{schema,accounts,catalogs,documents,ledger-binding,periods,index}.ts` (the 10 module tables — `financial_account`, `billing_account`, `bill_cycle`, `reason_code`, `document`, `document_line`, `ledger_binding`, `gl_account`, `gl_mapping`, `accounting_period` — with their sequences, CHECKs, cross-schema FKs, and the UNIQUE constraints per Inv. #7/#9), `db/schema/billing/views.sql` (raw SQL for `account_view`/`gl_resolution_view`/`gl_journal_view`, hand-appended into the migration), `db/migrations/0012_billing_module_tables.sql` + journal + `meta/0012_snapshot.json`, `types/accounts.ts` (the 8 code-standards §2.1 unions + TMF composition types + re-exported row types), `validation/accounts/{contact,mode-ref,document-metadata}.schema.ts` (JSONB shape guards), `db/repositories/accounts/*.ts` (9 skeleton repos + `ledger.repository.ts` + a fully working `account-view.repository.ts` — the sole `TmfAccountRef` producer, backed by an `.existing()` Drizzle view read handle), `tests/db/billing-schema.test.ts` (39 unit tests: columns/nullability/CHECK-presence/structural), `tests/db/billing-schema.integration.test.ts` (8 integration tests: table/view existence, ID formats incl. all 5 per-type document sequences, the `account_view` fixture headline test, CHECK/UNIQUE/cross-schema-FK rejections). `drizzle.config.ts` updated in the same change: `billing` added to `schemaFilter`, `tablesFilter: ["!pgledger_*"]` added (per the ac01 note). Verified locally against a throwaway Postgres 16 container (never the shared dev-stack `DATABASE_URL`): fresh migrate clean, all 10 tables + 3 views present, full suite green (1625 unit + 364 integration tests, including this unit's own 39+8), `typecheck`/`lint`/`format:check` clean, `db:generate` confirmed a no-op after hand-finishing the migration.
- **ac03 — Seed Set + GL Health** (`context/accounting-management/specs/ac03-seed-set-and-gl-health.md`). Started and finished 2026-07-26. Not yet committed to git (uncommitted working tree — commit is the user's call). Delivered: `db/seeds/accounts/{seed-sys-accounts,seed-coa,seed-gl-mappings,seed-reason-codes,seed-bill-cycles,seed-wizard-defaults,seed-accounts}.ts` (one transaction, skip-if-present per catalog, composed by `seed-accounts.ts`), `package.json` (`db:seed-accounts` script + appended to the `db:setup` chain), `tests/accounts/v05-gl-resolution.integration.test.ts` (9 integration tests: sys-account presence/no-binding, 0-unmapped seeded state, 0-unmapped after a fixture FA/BAN + 3 bindings, unambiguous-resolution, reason-code/bill-cycle/config/CoA/mapping catalog integrity, re-run idempotency). Also filled in `db/repositories/accounts/ledger.repository.ts`'s `createAccount` (real, wraps `pgledger_create_account` via `tx.execute(sql\`...\`)`) and added `findByName` (wraps a `pgledger_accounts_view` lookup) — this seed is the ledger's first real account-creation caller, ahead of document posting; `createTransfer(s)` remain stubs for that later unit. Catalog rows (`gl_account`/`gl_mapping`/`reason_code`/`bill_cycle`/`system_config`) are inserted directly via their Drizzle schema tables in the seed, not through the `db/repositories/accounts/*` skeleton `.insert()` methods — those stubs are explicitly owned by later units (Chart of Accounts CRUD, Accounts Settings) and still throw; this matches the existing `db/seeds/{product,customer}.ts` convention of seeds writing straight to schema tables with no repository indirection. Verified locally against a throwaway Postgres 16 container (never the shared dev-stack `DATABASE_URL`): fresh `db:setup` (migrate → all 5 seeds including this one) completes clean, re-running `db:seed-accounts` alone is idempotent (counts unchanged: 6 sys accounts, 11 `gl_account`, 9 `gl_mapping`, 10 `reason_code`, 2 `bill_cycle`, 3 `accounts` config rows, 0 unmapped throughout), full test suite green (1625 unit + 373 integration tests across 40 integration files, including this unit's own 9), `typecheck`/`lint`/`format:check` clean (the one pre-existing lint error/warning in `components/customers/*` noted by ac02 is still the only lint output — unrelated to this unit), `db:generate` confirmed a no-op (seed-only change, no schema touched).

## In Progress

- None — ac04 not yet started.

## Next Up

- ac04 — onboarding wizard.

## Open Questions

- None — Q1–Q28 in `_newmodule-account-plan.md` are all resolved; ac01 raised no new Q.

## Architecture Decisions

- None yet beyond the plan's Q1–Q28.

## Session Notes

- **ac01 codebase-verification findings** (spec's "Note on codebase verification", both now resolved and corrected here rather than by editing the spec's placeholder text):
  1. **Next free migration index: `0011`** (`db/migrations/0000`…`0010` already exist through `product_offering_family`). Generated via `drizzle-kit generate --custom --name=billing_pgledger`, producing `db/migrations/0011_billing_pgledger.sql` + the journal entry + `meta/0011_snapshot.json` (a no-op diff vs `0010` — `billing` is not yet in `drizzle.config.ts`'s `schemaFilter`, so nothing to diff).
  2. **An existing Postgres ULID generator does exist in `core`** (`core.generate_ulid()`, added by um27, returns `uuid`) — but it is **not** a substitute for what this unit needs, so §3.3's vendored `ulid.sql` was **not** dropped. Reasoning: upstream `pgledger.sql` already contains its own id-generation logic (`pgledger_uuidv7()` + `pgledger_generate_id(prefix)`) and depends on exactly one external symbol, `uuid_to_ulid(uuid) → text`, vendored upstream itself from `scoville/pgsql-ulid` (BSD-3-Clause) at `pgr0ss/pgledger`'s own `vendor/scoville-pgsql-ulid/uuid-to-ulid.sql`. `core.generate_ulid()` has a different signature/purpose (generates a whole new ULID-as-uuid from scratch; pgledger needs a uuid→ULID-**text** converter applied to a uuid it already generated internally). Swapping in `core.generate_ulid()` would require hand-editing `pgledger_generate_id`'s body — forbidden by module inv. #14 ("changes namespaces, never behaviour"). So `db/pgledger/ulid.sql` vendors `uuid-to-ulid.sql` verbatim (qualified into `billing`), matching pgledger's own upstream dependency exactly.
- **Upstream identity confirmed:** `pgledger` = `pgr0ss/pgledger` (Paul Gross, MIT License). `UPSTREAM_COMMIT` = `43240dbdfc291eca5380cbcee7dfe594922c67d6` (2026-02-06, GPG-verified, tip of `main` at implementation time). The vendored ULID helper (`db/pgledger/ulid.sql`) traces to `scoville/pgsql-ulid` (BSD-3-Clause), vendored transitively via pgledger's own `vendor/scoville-pgsql-ulid/uuid-to-ulid.sql` — both licenses are permissive and compatible with vendoring into this proprietary codebase; provenance recorded in each file's header for future license-audit purposes.
- **Test-suite ripple applied:** per the platform pattern (every prior module's pgSchema needs this), added `await sql.unsafe('DROP SCHEMA IF EXISTS "billing" CASCADE');` to the `beforeAll`/`afterAll` of all 37 existing `*.integration.test.ts` files that do the full-schema drop+migrate cycle, immediately before their existing `"customer"` drop line — otherwise a later integration test's `migrate()` hits `CREATE TABLE billing.pgledger_accounts` on a non-empty `billing` schema left behind by an earlier test file and fails with "already exists".
- `billing` is **not** added to `drizzle.config.ts`'s `schemaFilter` in this unit (still `["core", "product", "customer"]`) — ac02 adds it alongside the module's Drizzle schema files, together with the `tablesFilter` exclusion for `pgledger_*` called out in spec §2.5/§3.5 (todo for ac02, not rediscovered here since `billing` isn't filtered in yet, so `db:generate` cannot touch it either way).
- **ac02 codebase-verification findings** (spec's "Note on codebase verification", resolved and corrected here):
  1. **Next free migration index: `0012`** (`db/migrations/0000`…`0011` already exist through `billing_pgledger`). `drizzle-kit generate` produced `db/migrations/0012_billing_module_tables.sql` (renamed from its auto-generated slug) + the journal entry + `meta/0012_snapshot.json`.
  2. **Cross-schema id column names confirmed exactly as the spec assumed**: `customer.party_role.party_role_id` and `core.appuser.id` (TS property; physical column `user_id`) — no correction needed.
  3. **View mechanism confirmed: raw SQL**, per spec §"Note" (c) — Drizzle can't declare a `pgSchema` view that joins `billing.pgledger_*` (outside its schema surface) in one step; the 3 views are hand-appended SQL (`db/schema/billing/views.sql` → migration). For the *read* side, `account-view.repository.ts` declares the view as a Drizzle `.view(name, columns).existing()` read handle — a typed query surface with zero DDL of its own, so drizzle-kit never tries to diff/manage it.
  4. **Generated-migration gotcha (new, worth flagging for any future unit that shares a schema across migrations): `drizzle-kit generate` emits a bare `CREATE SCHEMA "billing";`** because its own prior snapshot (0011) has no knowledge of `billing` (ac01's raw-SQL migration never touched Drizzle's schema-file surface, so `billing` was diffed as "new" the first time it entered `schemaFilter`). The physical schema already exists from migration 0011 — applying the generated statement verbatim would fail on a real environment that has already run 0011. Fixed by hand-editing the generated statement to `CREATE SCHEMA IF NOT EXISTS "billing";` before committing (documented inline in the migration file) — this is a one-time gotcha specific to a schema's *first* entry into `schemaFilter` after an earlier raw-SQL-only migration already created it; a normal `schemaFilter` addition for a schema Drizzle originated would not hit this.
  5. **`gl_resolution_view` resolution-order note**: both branches (`ledger_role` and `system_account`) resolve via a scalar subquery ordered `currency NULLS LAST, LIMIT 1` so a currency-specific `gl_mapping` row wins over a currency-agnostic (`currency IS NULL`) one for the same selector, without ever fanning a pgledger account out to more than one row. Worth keeping in mind for ac03's seed design (a role can be seeded once with `currency = NULL` for "all currencies" or per-currency — both compose correctly against this view).
  6. **Test-suite ripple**: `tests/accounts/v01-zero-sum.integration.test.ts`'s table/view assertions were exact-set (`toEqual`) against `billing`'s full table/view list — this broke the moment ac02 added its own tables to the same schema (not a bug in either unit, just an over-scoped assertion once a second unit started sharing the schema). Loosened to `expect.arrayContaining([...])` so it asserts only the 3 pgledger tables/views it owns are present; the exact-set assertion for the module's own 10 tables + 3 views now lives in this unit's own `tests/db/billing-schema.integration.test.ts`. Any later unit adding more `billing.*` objects should not need to touch either test again.
  7. **Pre-existing, unrelated lint failure**: `npm run lint` reports one `error` (React Compiler `set-state-in-effect` in `components/customers/organization-form.tsx`) and one `warning` (`react-hooks/incompatible-library` in `components/customers/new-customer-form.tsx`) — both pre-date this session (confirmed via `git log`/`git status`, no uncommitted changes to either file) and are outside the Accounts module's scope. `npm run lint` therefore exits non-zero overall even though every file this unit touched is lint-clean; flagging so it isn't mistaken for an ac02 regression.
- **ac03 codebase-verification findings** (spec's "Note on codebase verification", resolved and corrected here):
  1. **Script name/ordering confirmed as assumed**: `db:seed-accounts`, run after `db:migrate`, appended to the end of the `db:setup` chain (`... && npm run db:seed-customer && npm run db:seed-accounts`) — position among the other module seeds doesn't matter (§2.7, no cross-module seed dependency).
  2. **Acting-user convention confirmed**: `last_edited_by = NULL` / `modified_by = NULL` for every seeded row (`gl_account`, `gl_mapping`, `reason_code`, `bill_cycle`, `core.system_config`) — all four catalog tables declare `last_edited_by` nullable (unlike `financial_account`/`billing_account`, which are `NOT NULL`), matching the `cm01`/Product seed precedent the spec assumed.
  3. **§2.5's "Nine rows" is a spec typo**: the reason-code table directly below it lists ten rows, and both the V5 test description (§3.3) and the verification checklist (§5) say "ten reason codes" — implemented as ten (`CUST_PAYMENT` … `ROUNDING_ADJ`); recorded here rather than silently reconciled.
  4. **`ledger.repository.ts`'s real method is `createAccount`, not `pgledgerCreateAccount`** — the spec's prose (§3.1) names the latter descriptively; the file already had a skeleton `createAccount` (ac02) with the exact params this unit needs, so that's what got filled in (plus a new `findByName` for the idempotent-by-name pre-check, §2.2/§2.7). No rename — would have rippled needlessly.
  5. **Catalog rows are seeded via direct Drizzle table inserts, not the `db/repositories/accounts/{gl-account,gl-mapping,reason-code,bill-cycle}.repository.ts` `.insert()` skeletons** — those throw "not implemented", explicitly deferred to later units (Chart of Accounts CRUD / Accounts Settings) by their own comments. Going through them would have meant either implementing someone else's unit early or leaving the seed broken; direct table inserts also match this codebase's existing seed convention (`db/seeds/{product,customer}.ts` write straight to schema tables, no repository indirection).
  6. **`bill_cycle_id` is DB-generated** (`BCY` + sequence), so the wizard-default config's `ACCOUNTS_DEFAULT_BILL_CYCLE` value is read back off the insert/lookup rather than hard-assumed as `"BCY000001"` — correct either way, but robust if some other future seed ever consumes the sequence first.
  7. **Module Inv. #4 / code-standards §6.3** ("repositories in `db/repositories/accounts/` are the only callers" of pgledger functions/views) applies to *application* code, i.e. the seed script — it does **not** restrict the V5 *test*'s fixture setup, which calls `billing.pgledger_create_account` directly via the raw `postgres` client, matching the established test-only precedent in `tests/db/billing-schema.integration.test.ts` and `tests/accounts/v01-zero-sum.integration.test.ts`.