# Product Management — Progress Tracker

## Status

| Unit | Name                                                                | Status             |
| ---- | ------------------------------------------------------------------- | ------------------ |
| pm01 | Route-group rename `(admin)` → `(app)` + rename-invariance CI proof | Done (committed `313a66e`) |
| pm02 | DB foundation + validation schemas + seeds (`product` schema, 3 tables, constraints, perm seed, `validation/product/`, `TOREMOVE-Template-*` seeds) | Done (committed `962a2e8`, follow-up fix `232d78d`) |
| pm03 | Repositories + `services/product`                                   | Done (committed `a168465`) |
| pm04 | Nav refactor (`NAV_ITEMS` → `NAV_SECTIONS`)                         | Done (committed `2f5fd35`) |
| pm05 | Page — one section per unit (table → detail → specs → prices)       | Done (committed `e0d026f`) |
| pm06 | Offering detail section (populated `OfferingDetail` fields)         | Done (committed `bc33109`) |
| pm07 | Specifications panel (populated `SpecificationsPanel`)               | Done (committed `998ed1b`) |
| pm08 | Prices panel (populated `PricesPanel`, `formatCurrency`)             | Done (committed `9561d27`) |
| pm09 | Authz-matrix entry + guardrail sweep (ship gate)                    | Done (uncommitted) |

> **Renumbering note (this session):** `pm02-spec.md` ("Unit 2 of 9" per `pm00-build-plan.md`) bundles DB foundation, `validation/product/` schemas, and seeds into one unit — superseding this table's earlier 3-way split (old pm02/pm03/pm04). The rows above are renumbered to match; a future session should reconcile the remaining rows against `pm00-build-plan.md`'s full 9-unit count before relying on these numbers.

> **Post-review fix-up (this session):** two pm02 review nitpicks were still valid against current code and fixed via a new `db/migrations/0007_product_constraints_fix.sql` (0006 was already committed at `962a2e8` and pushed — editing an applied/committed migration is forbidden, workflow §6.3, so this is a follow-up migration, not an edit to 0006): `product_offering.last_edited_by`'s FK now uses `ON DELETE SET NULL`, matching the established optional "last actor" convention (`role_assign.assigned_by`, `system_config.modified_by`) instead of `restrict`; and `product_offering_price` gained a `CHECK (amount >= 0)` constraint as DB-level defense-in-depth (the Zod money regex already rejected negatives at the app layer, but nothing stopped a direct-SQL insert). Two other review nitpicks were checked and found already non-issues in current code: the `priceType`/`pricingModel` "should use `.$type<>()`" suggestion contradicts the deliberate design decision (pm02-spec §2 Design #2: text + CHECK "matching the core pattern", verified against `role_permission_assign.permissionType`/`appuser.status` which likewise omit `.$type<>()`); and the route-manifest test's "self-matching" concern doesn't apply — `tests/app/route-manifest.test.ts` already builds the `(admin)` token dynamically from a `RETIRED_GROUP_NAME` constant rather than containing the literal string. The 5 "duplicated schema-reset boilerplate" test nitpicks were skipped as out-of-scope for a minimal fix: the same `DROP SCHEMA` pattern is repeated identically across ~26 integration test files repo-wide (not just the 4 flagged), so centralizing it for only 4 files would leave the rest inconsistent without fixing the underlying duplication — a real fix needs a repo-wide helper, a separate unit of work. Doc clarifications applied: `prodmgmt-code-standards.md` §3.1 now states `searchParams` is `await`-ed (Promise prop in this Next.js version) before schema parsing; `usrmgmt-ui-context.md`'s datetime-display paragraph no longer self-contradicts on what the hover `title` shows — it now states the rule varies by table (audit rows: absolute local time in the cell, so `title` carries raw UTC for forensics; `config-table.tsx`: relative time in the cell, so `title` carries the local-zone absolute string instead), matching `um29-timezone-handling.md` §2.5 and actual component code. The stale-template "missing checklist gate items" finding on `usrmgmt-completed-tracker.md` no longer applies — that file's content was replaced with real progress-tracking prose in this session's working tree; the checklist section the finding referenced belonged to the old boilerplate-template version, which git status shows as deleted.

**Next:** pm02 implemented per `specs/pm02-spec.md` — §3.1–§3.8 in full: `drizzle.config.ts` `schemaFilter` gains `"product"`; `db/schema/product.ts` (new) with the `product` schema, `lifecycle_status` enum, 3 sequences, and the 3 tables (`product_offering`, `product_specifications`, `product_offering_price`) with all CHECK/UNIQUE/FK constraints; migration `db/migrations/0006_product.sql` (generated via `db:generate`, sequences already ordered before tables, no hand-adjustment needed) plus the appended `products` PERMISSIONS row; permission registry wiring (`types/rbac.ts`, `auth/permission-constants.ts`, `types/roles.ts`); `types/product.ts` domain unions; `validation/product/` (3 schemas: spec-characteristics, pricing-characteristics incl. tier contiguity refinement + XOR discriminated union, offering-list searchParams); `db/seeds/product.ts` (2 offerings / 3 specs / 6 prices, ADMIN→products:DELETE grant) + `db:seed-product` npm script; unit tests (`tests/validation/*`, `tests/db/product-schema.test.ts`) and integration tests (`tests/db/product-schema.integration.test.ts`, `tests/db/migration.integration.test.ts` updated to drop `product` before `core`).

**Deviations from the spec's stated diff scope (forced by the actual repo state, not optional):**
1. Adding `"products"` to `PERMISSION_NAMES` makes `EffectivePermissionMap` (`Record<PermissionName, ...>`) require a `products` key everywhere it's constructed as an object literal — 25 test files beyond the spec's named two (`permission-matrix-editor.test.tsx`, `role-detail.test.tsx`) needed a mechanical `products: null` (or `"DELETE"` for full-ADMIN fixtures) added to compile. `tests/services/roles-read.service.test.ts` and its `.integration.test.ts` counterpart also needed their hardcoded 4-entry arrays extended to 5 (the spec's claim that the service test "derives counts from `PERMISSION_NAMES.length` and needs no edit" was inaccurate — several of its assertions hardcode the full mapping array, not just `.length`).
2. `db/schema/product.ts`'s type-only JSONB import from `validation/product/*` (Design #4) and `db/seeds/product.ts`'s runtime import (to `.parse()` payloads, code-standards §1.7) both violated the `db → validation` ESLint boundary rule (`eslint.config.mjs`); added `"validation"` to `db`'s allowed targets with an explanatory comment.
3. Every pre-existing `tests/**/*.integration.test.ts` file that does a fresh `DROP SCHEMA core CASCADE` + migrate cycle (26 files, not just `migration.integration.test.ts`) needed the same `DROP SCHEMA IF EXISTS "product" CASCADE` added before the `core` drop — otherwise the second integration file to run in a session leaves an orphaned `product` schema (FK to `core.appuser` cascade-dropped, tables left behind) that makes the next file's migration fail with "schema product already exists" (`CREATE SCHEMA "product"` has no `IF NOT EXISTS`, matching drizzle-kit's own generated style).

**Verified this session:** `tsc --noEmit`, `eslint .`, and `prettier --check` (on all touched files; remaining warnings are pre-existing `context/**` docs untouched by this unit) all clean; `npx vitest run` (unit config) — 105 files / 967 tests green; `npx vitest run --config vitest.integration.config.ts` against the local Docker Postgres dev container — 28 files / 255 tests, all green except one pre-existing, unrelated timing flake in `tests/db/appuser-repository.integration.test.ts` (millisecond-precision race on `last_modified_datetime`, confirmed unrelated to this unit by reproducing in isolation); `npm run db:setup` (migrate → seed → seed-rbac → seed-product) succeeds end-to-end and `db:seed-product` re-run skips idempotently; psql-equivalent spot checks confirm 2 offerings (`PRDOFR000001`/`PRDOFR000002`), 3 specifications, 6 prices, the tiered Data-Overage JSONB tiers, the `core.permissions` `products` row, and the ADMIN→products:DELETE grant; direct-SQL inserts confirm the unique-start constraint (`23505`) and the amount-XOR-tiers CHECK (`23514`) both fail as designed.

**Not verified this session:** the CI pipeline's SAST/ZAP baseline (no CI environment available here) and the Roles UI manual spot-check (`/administration/roles` showing the 5th "Products" row) — the component-level tests exercise this (`permission-matrix-editor.test.tsx`, `role-detail.test.tsx`) but no browser was driven this session. No UI/route code exists yet for this unit regardless (v1 is DB+validation+seeds only, per code-standards §1.1) — nothing to browser-test.

**Not yet committed** — changes are in the working tree; commit per user confirmation (workflow supplement §3.6 / pm02-spec §3.9 names one commit for §3.1–§3.8's contents; the `eslint.config.mjs` boundary addition and the ~28-file `EffectivePermissionMap`/schema-drop ripple are forced companions to that commit, not scope creep).

---

**pm03 implemented per `specs/pm03-spec.md`** — §3.1–§3.8 in full: `types/product.ts` extended with `EFFECTIVITY_STATUSES`/`EffectivityStatus`, `OfferingListRow`, `OfferingListPage`, `SpecificationCard`, `PriceCard`, `OfferingDetail`; three new finder-only repositories (`db/repositories/product-offering.ts` — `findList` + `findDetailById`; `product-specification.ts` — `findByOfferingId`; `product-offering-price.ts` — `findByOfferingIdWithDerivedEnd` using a `LEAD() OVER (PARTITION BY product_offering_id, price_type ORDER BY start_date_time)` window, normalized to `Date | null`); `services/product/list-offerings.ts` (`listOfferings`, `DEFAULT_OFFERING_LIST_PAGE_SIZE = 5`, resolves page size from `core.SYSTEM_CONFIG` via `systemConfigRepository.findActiveValue`) and `services/product/get-offering-detail.ts` (`getOfferingDetail`, injectable `now`, pure `resolveEffectivityStatus` helper implementing the `[start, successorStart)` boundary rule); migration `db/migrations/0008_product_config.sql` (data-only, generated via `drizzle-kit generate --custom`) seeding `('products', 1, 'offering_list_page_size', '5', ...)`; unit tests (`tests/services/list-offerings.service.test.ts`, `tests/services/get-offering-detail.service.test.ts`, `tests/db/product-repository-exports.test.ts` — structural no-mutation assert) and integration test (`tests/db/product-repositories.integration.test.ts` — pagination/search/sort/tie-break, derived effectivity from real SQL incl. partition correctness across `price_type`, `findDetailById`'s APPUSER join, the seeded config row).

**Deviations from the spec's stated diff scope (forced by the actual repo state, not optional):**
1. **Migration number is `0008`, not `0007`.** pm03-spec was authored assuming pm02's migration `0006` was the last one, but this session's pm02 post-review fix-up (see above) already consumed `0007` for `0007_product_constraints_fix.sql`. `drizzle-kit generate --custom --name=product_config` correctly produced `0008_product_config.sql` + a matching `_journal.json`/`0008_snapshot.json` (snapshot identical to `0007`'s — data-only, no schema diff). All spec references to "`0007_product_config.sql`" and the checklist's "past-the-end" and "`0007` seeded row" items are satisfied by `0008` instead.
2. **`eslint.config.mjs` needed a second boundary addition beyond pm02's.** `types/product.ts`'s `SpecificationCard`/`PriceCard` fields (`characteristics`, `pricingCharacteristics`) embed `ProductSpecCharacteristics`/`TieredPricingCharacteristics`, whose owning shape lives in the Zod schemas (code-standards §2.7) — a type-only import from `types/` to `validation/`, which the existing boundary rule (`types` → `["types", "db"]` only) rejected. Added `"validation"` to `types`'s allowed targets, mirroring pm02's `db → validation` precedent; `types/` still never imports validation at runtime.
3. **The spec's own guardrail checklist (§5, line "`0007` seeded row … returns `'10'`") is internally inconsistent** with Design #8 (page size revised to 5, not 10, "closing this spec" 2026-07-04) and with the migration SQL in §3.7, which inserts `'5'`. Implemented and tested against the value the migration actually seeds (`'5'`), not the stale `'10'` in that one checklist line.

**Verified this session:** `tsc --noEmit`, `eslint .`, and `prettier --check` all clean on every touched/new file; `npx vitest run` (unit config) — 108 files / 991 tests green; `npx vitest run --config vitest.integration.config.ts` against the local Docker Postgres dev container — 29 files / 266 tests, all green (no flake this run, including the previously-flaky `appuser-repository.integration.test.ts`); fresh `npm run db:setup` (migrate incl. `0008` → seed → seed-rbac → seed-product) succeeds end-to-end; psql confirms `core.system_config` row `('products', 'offering_list_page_size', '5')`; a scratch invocation against the real seeded data confirms `listOfferings()` returns `pageSize: 5`, live-editing the config row to `'25'` changes the next call's `pageSize` to `25` with no deploy, and a junk value (`'not-a-number'`) falls back to `5`; with the pm02 seed data and `now = 2026-07-07`, offering 1's 2026 recurring price (`TOREMOVE-Template-Monthly-Recurring-Charge`) is `current` with derived `endDateTime: 2027-01-01T00:00:00.000Z`, and its 2027 successor is `future` with `endDateTime: null` — matching the checklist's named spot-check exactly. The config row was reset back to `'5'` afterward so the dev DB is left in its clean seeded state.

**Not verified this session:** the CI pipeline's SAST/ZAP baseline (no CI environment available here) — no new routes exist yet regardless (v1 backend-only, pm05 not started), so runtime/DAST behavior is unchanged.

**Not yet committed** — changes are in the working tree; commit per user confirmation (pm03-spec §3.9 names one commit for §3.1–§3.8's contents; the `eslint.config.mjs` boundary addition is a forced companion, not scope creep). pm05 (page) must not consume `services/product` until this commit is verified and merged (pm03-spec, final line); pm04 (nav) is independent and may proceed in parallel.

---

**pm04 implemented per `specs/pm04-spec.md`** — §3.1–§3.3 in full: `components/admin-nav.tsx` refactored from the flat `NAV_ITEMS` array to `NAV_SECTIONS` (`NavItem`/`NavSection` types, module-private, unexported), with a new "Products" section (one item, "Product Offering" → `/products/product-offering`, `Package` icon) rendered above "Administration" (its four items copied verbatim — same labels/hrefs/icons/order); render loop now maps sections inside a keyed `Fragment`, adding `mt-2` to non-first captions in expanded mode and a hairline `<hr>` divider between sections in collapsed mode only (never before the first or after the last); the per-item `<Link>` JSX moved into the nested map unchanged (diff is indentation plus two Prettier line-wraps forced by the deeper nesting — no class/attribute/logic change). `tests/components/admin-nav.test.tsx` extended (not rewritten): the four pre-existing assertions pass unchanged, plus new coverage for the Products caption/link/href, DOM section order, zero dividers expanded / exactly one collapsed, and a second `describe` re-mocking `usePathname` (mutable variable, no duplicate file) to assert active state on `/products/product-offering`. `AdminNav`'s name, file, and `collapsed` prop contract are unchanged; `components/admin-sidebar.tsx` was not touched; no permission check was added (nav stays permission-blind, code-standards §3.8); no npm dependency changed (`Package` ships with the already-installed `lucide-react`).

**No deviations from the spec's stated diff scope.** Diff is exactly the two named files.

**Verified this session:** `tsc --noEmit`, `eslint .`, and `prettier --check .` all clean (one Prettier auto-format pass applied to `admin-nav.tsx` for line-wrap only, then re-verified clean); `npx vitest run` (unit config) — 108 files / 995 tests green (4 new assertions), including all 8 `admin-nav.test.tsx` tests. Manual dev-server verification against the local Docker stack (DB container already running; app container started fresh, stale `.next` build cache from before the pm01 rename cleared — unrelated pre-existing artifact, not caused by this unit): signed in as the seeded bootstrap admin and fetched `/administration/users` — rendered order is Products → Product Offering → Administration → Users/Roles/System Configuration/Audit Log; `Users` carries `aria-current="page"` with the active-pill classes, `Product Offering` does not; zero `<hr>` elements expanded. With the `sidebar_collapsed=1` cookie set: exactly one `<hr>` divider, all five links carry `title` tooltips, both captions absent from the markup. `/products/product-offering` returns a clean 404 (`Page not found · User Management`) with no error boundary or crash — the accepted interim dead-link state (§2.6). App/setup containers were stopped and removed after verification, leaving only the pre-existing DB container running (its state was not modified by this unit).

**Not verified this session:** the CI pipeline's SAST/DAST baseline (no CI environment available here).

**Not yet committed** — changes are in the working tree; commit per user confirmation (pm04-spec §3.4 names one commit: `components/admin-nav.tsx` + `tests/components/admin-nav.test.tsx`, nothing else). pm04 was independent of pm02–pm03 and could commit separately at any time; pm05 (page) still requires pm02–pm03 verified/merged in addition to this unit.

---

**pm05 implemented per `specs/pm05-spec.md`** — §3.1–§3.5 in full: `app/(app)/products/product-offering/page.tsx` (new) — a thin RSC orchestrator (`export const dynamic = "force-dynamic"`) that runs `requirePermission(PERMISSIONS.PRODUCTS, LEVELS.READ)` as its first statement (→ `/no-access` on insufficient grant), parses `searchParams` through `offeringListSearchParamsSchema` (the lenient `.catch()` schema — a tampered URL never 500s), and calls only `listOfferings`/`getOfferingDetail` (`services/product`) plus `getAppLocale`/`getAppTimezone` before composing `components/products/**`; `components/products/lifecycle-badge.tsx` (new) — `cva`-based pill copied structurally from `status-badge.tsx`, keyed by `LifecycleStatus` (ACTIVE=success/check-circle, DRAFT=warning/pencil-line, RETIRED=neutral/archive); `components/products/offering-table.tsx` (new, `"use client"`) — search (Apply button + Enter key + Clear), lifecycle status `<select>` filter, five sortable column headers (`product_offering_id`/`name`/`lifecycle_status`/`version`/`last_modified`, `aria-sort` + chevron, `Sellable` deliberately non-sortable), row selection (`router.push`, whole-row `role="button"`, `aria-current`, `--surface-selected`), pagination (`router.replace`, Prev/Next, "Showing a–b of N"), all URL state changes wrapped in `useTransition` (dims the table `opacity-60` and disables controls while pending); `components/products/offering-detail-region.tsx` (new) — the four-section-shell wrapper owning only the three empty/not-found states in this unit (`hasSelection:false` → "Select an offering…"; `notFound:true` → "Offering not found"; otherwise the three titled placeholder frames — Details/Specifications/Prices — with `{/* pm06/pm07/pm08 */}` seams for the next three units). Four new test files: `tests/components/lifecycle-badge.test.tsx`, `tests/components/offering-table.test.tsx` (19 tests — render, sort toggle/direction, search Apply+Enter+Clear, status filter, pagination bounds/preservation, row selection + `aria-current`, empty state, RETIRED muting, Not-sellable chip), `tests/components/offering-detail-region.test.tsx`, `tests/app/product-offering-page.test.tsx` (guard-first, deep-link reproduces the view, tampered URL → defaults + no `getOfferingDetail` call, unknown well-formed ID → `notFound:true`). `tests/app/route-manifest.test.ts` gained the one-line `/products/product-offering` addition the spec's §3.5 anticipated (the manifest is a hardcoded set-equal list; the new route would otherwise trip the "unplanned route" assertion).

**Deviations from the spec's stated diff scope:** none in the shipped files — diff is exactly the page, the three new components, the four new test files, and the one-line route-manifest addition the spec called out as a conscious exception. Two implementation choices the spec left to judgment: (1) the offering-table tests assert URL navigation by parsing the `URLSearchParams` from the last `router.replace`/`router.push` call rather than a fixed query-string, since `URLSearchParams.toString()`'s key order isn't spec-mandated; (2) the page test locates the `OfferingDetailRegion` element by walking the returned React element tree (`findElementByType`) rather than asserting through a mock invocation, because the page is an `async` Server Component — calling it directly returns an unrendered element tree, so a child component's mock function body never actually executes unless something renders the tree.

**Verified this session:** `tsc --noEmit`, `eslint .`, and `prettier --check .` all clean on the full repo; `npx vitest run` (unit config) — 112 files / 1025 tests green (30 new assertions across the four new files), zero pre-existing assertions changed. Not run this session: a live dev-server walkthrough of the page (no DB container was started this session) and the CI pipeline's SAST/DAST baseline — both are pm05-spec §5's "Behavior" and "Pipeline" checklist items and remain open before this unit can be called fully verified end-to-end.

**Not yet committed** — changes are in the working tree; commit per user confirmation (pm05-spec §3.6 names one commit: the page, the three components, and the four test files, plus the route-manifest one-liner). Unit pm06 (offering detail section) may start once this commit is verified/merged — it replaces `OfferingDetailRegion`'s detail placeholder with populated fields, consuming the `?offering=` selection, guard, and `getOfferingDetail` wiring landed here.

---

**pm06 implemented per `specs/pm06-spec.md`** — §3.1–§3.5 in full: `components/products/offering-detail.tsx` (new, Server Component) — renders the mono `productOfferingId` eyebrow, name, `LifecycleBadge`, the derived flag-chip row (`FlagChip` local presentational + `offeringFlagChips` derivation: `Bundle`/`Sellable`/`Billing only` neutral chips shown only when their flag is true, plus the `isSellable:false && lifecycleStatus:"ACTIVE"` warning-tinted "Not sellable" chip in place of a sellable chip), and a labeled `<dl>` field grid (Version mono/tabular-nums, Last Modified via `formatDatetime(offering.lastModified, locale, timezone)`, Last Edited By = `lastEditedByName ?? "—"`) — mirroring the `Field`/`dt`/`dd` pattern from `components/users/user-detail.tsx` and the flag-chip/warning-chip rule already inline in `offering-table.tsx` (reproduced locally, not extracted/shared, per spec §2.4/§7.0); `components/products/offering-detail-region.tsx` (edit) — added required `offering: OfferingDetail | null`, `locale: string`, `timezone: string` props, rendering `<OfferingDetail>` in the **Details** seam only when `offering` is non-null; the not-found/no-selection branches and the Specifications/Prices placeholder frames are byte-unchanged; `app/(app)/products/product-offering/page.tsx` (edit) — one-line seam fill threading the already-computed `selectedOffering`/`locale`/`timezone` into `<OfferingDetailRegion>`. New test file `tests/components/offering-detail.test.tsx` (9 tests: full field render incl. formatted datetime, null-editor em dash, each flag chip individually, no-chips case, ACTIVE-not-sellable warning chip, non-ACTIVE not-sellable → no chip, icon+label pairing on every chip). Two intended test edits per spec §3.5: `tests/components/offering-detail-region.test.tsx` (populated-Details assertion now checks the offering name/id render instead of an empty frame; the two empty-state tests updated to pass the three new required props) and `tests/app/product-offering-page.test.tsx` (new test asserting `OfferingDetailRegion` receives `offering`/`locale`/`timezone` on a resolved deep link).

**Deviations from the spec's stated diff scope:** none — diff is exactly the new component, the two edited components/page, and the three test files (one new, two edited) the spec named. One implementation choice the spec left open (§3.3, "confirm at implementation time which side draws the `<h2>Details</h2>`"): the region keeps rendering the `<h2>Details</h2>` title itself (unchanged from pm05) and `OfferingDetail` renders only the card body — avoids double-rendering the title and keeps the region owning section chrome consistently across the Details/Specifications/Prices frames.

**Verified this session:** `tsc --noEmit`, `eslint .`, and `npx prettier --check` all clean on every touched/new file (one `prettier --write` pass applied to `offering-detail.tsx` and its test file for line-wrap/quote-style only, then re-verified clean); `npx vitest run` (unit config) — 113 files / 1035 tests green (10 new assertions: 9 in the new file, 1 in the page test), zero pre-existing assertions broken beyond the two intended edits named above. Live dev-server walkthrough: Docker Desktop was started this session and `docker compose up --build` brought up `db` (healthy, existing seeded `pgdata` volume reused) → `migrate` (no-op — no new migrations in this UI-only unit, exited 0) → `app` (Next.js ready on `:3000`); the user manually signed in as the seeded bootstrap admin and walked through `/products/product-offering` — reported the populated Details section (clicking a row and via deep link) "looks good," confirming the pm06-spec §5 "Behavior" checklist items.

**Not verified this session:** the CI pipeline's SAST/DAST baseline (no CI environment available here) — pm06-spec §5's "Pipeline" checklist item remains open.

**Not yet committed** — changes are in the working tree; commit per user confirmation (pm06-spec §3.6 names one commit: `offering-detail.tsx` new, `offering-detail-region.tsx` + page edits, and the three test files). Unit pm07 (specifications panel) may start once this commit is verified/merged.

---

**pm07 implemented per `specs/pm07-spec.md`** — §3.1–§3.5 in full: `components/products/characteristic-chip.tsx` (new, Server Component) — one JSONB key–value pair as a chip on `--surface-sunken`/`--radius-xs` (overline key in `--color-neutral-500`, mono `tabular-nums` value in `--color-neutral-800`); `components/products/specifications-panel.tsx` (new, Server Component) — a local `SpecBadge` presentational helper + `specBadges` derivation (mirroring pm06's `FlagChip`/`offeringFlagChips`) rendering the `Mandatory` (info-50/info-700, `Asterisk` icon) and `Default` (neutral-100/neutral-700, `Star` icon) badges only when their flag is true; each spec renders as a `--border-subtle` inner card with a mono `productSpecId` eyebrow, name + badges row, a **Default value** row shown only when `defaultValue !== null` (independent of the `isDefault` flag, per spec §2.3/§2.4's called-out independence), and a `CharacteristicChip` row from `Object.entries(spec.characteristics)` in insertion order (omitted entirely when the object is empty); an empty `specifications` array renders the `ListChecks`-icon muted empty state ("No specifications for this offering.") on `--surface-sunken`; `components/products/offering-detail-region.tsx` (edit) — the **Specifications** seam now renders `<SpecificationsPanel specifications={offering.specifications} />` when `offering` is non-null, replacing the pm05 `{/* pm07: specs cards */}` placeholder; the Details (pm06) and Prices (pm08 placeholder) frames are byte-unchanged. No `info-50`/`info-700` tokens needed adding — both already existed in `globals.css` from prior units. New test files `tests/components/characteristic-chip.test.tsx` (1 test) and `tests/components/specifications-panel.test.tsx` (9 tests: card-per-spec, Mandatory badge present/absent, Default badge present/absent, Default-value row present/absent, the two independence cases — `isDefault:true`+`defaultValue:null` and `isDefault:false`+`defaultValue:"standard"` — chips in insertion order + empty-object no-chips, empty-panel state, icon+label pairing). One intended test edit per spec §3.5: `tests/components/offering-detail-region.test.tsx`'s populated-Specifications fixture now carries one non-empty spec, and its assertion checks the spec's `name`/`productSpecId` render instead of an empty placeholder frame; the other two region tests (`hasSelection:false`, `notFound:true`) are unchanged.

**Deviations from the spec's stated diff scope:** none — diff is exactly the two new components, the one edited component, and the two new + one edited test files the spec named (§3.6/§5 diff-hygiene checklist). The one implementation choice the spec left open (§2.3, "confirm at implementation time which side draws the `<h2>Specifications</h2>`") was resolved the same way pm06 resolved it for Details: the region keeps owning the `<h2>` title (unchanged from pm05), `SpecificationsPanel` renders only the frame's body.

**Verified this session:** `tsc --noEmit`, `eslint .` (scoped to touched files), and `prettier --check` all clean (one `prettier --write` pass applied to `characteristic-chip.tsx`, `specifications-panel.tsx`, and `specifications-panel.test.tsx` for line-wrap only, then re-verified clean); `npx vitest run` (unit config) — 115 files / 1045 tests green (10 new assertions: 1 in the new chip test, 9 in the new panel test), zero pre-existing assertions broken beyond the one intended region-test edit named above. `git status` diff matches the spec's §5 diff-hygiene checklist exactly: the two new components, the region edit, the two new test files, and the one intended test edit — nothing else.

**Not verified this session:** a live dev-server walkthrough of the populated Specifications section and the CI pipeline's SAST/DAST baseline (no CI environment or browser driven this session) — pm07-spec §5's "Behavior" and "Pipeline" checklist items remain open before this unit can be called fully verified end-to-end.

**Committed** as `998ed1b` ("product offering specifications panel: populated SpecificationsPanel (pm07)") — exactly the pm07-spec §3.6 file set: `characteristic-chip.tsx` new, `specifications-panel.tsx` new, `offering-detail-region.tsx` edit, and the three test files.

---

**pm08 implemented per `specs/pm08-spec.md`** — §3.1–§3.7 in full: `lib/formatters.ts` (edit) gained `formatCurrency(amount: string, currency: string, locale: string): string`, added beside `formatMoney` with the code-standards §4.4-mandated argument order (both coexist; `formatMoney` untouched); `components/products/price-type-badge.tsx` (new, Server Component) — `PriceTypeBadge`, the `cva`/token pill for `recurring`/`usage`/`once` (primary/cyan/neutral tints, `Repeat`/`Gauge`/`Zap` icons), structurally identical to `lifecycle-badge.tsx`; `components/products/tier-table.tsx` (new, Server Component) — `TierTable`, a real `<table>` printing each tier's stored JSONB `from`/`to`/`rate` values as text (no `Intl` grouping, no `formatCurrency`), with the open-ended top tier's `to: null` rendering "and above"; `components/products/prices-panel.tsx` (new, Server Component) — `PricesPanel`, one card per `PriceCard` keyed on `productOfferingPriceId`: mono ID eyebrow, name + `PriceTypeBadge` + effectivity state tag (`stateTag` helper: "Starts …" info tag for `future`, "Superseded" neutral tag for `superseded`, none for `current`), amount branch (`formatCurrency` + muted currency code for `flat`, `TierTable` for `tiered` — never both, never a "(tiered)" placeholder), labeled field rows shown only when non-null (Charge period via a local `formatChargePeriod` helper, Unit of measure, GL code, Policy), the derived Effective window (`formatDatetime` start → end, or "Open-ended" when `endDateTime` is null — never "Never"), and a muted Created timestamp; card styling keyed off `price.effectivityStatus` via `cardClassName` (cyan-500 left border for `current`, muted text for `superseded`) — no date math or effectivity derivation in the component (Inv. #3), multiple simultaneously-`current` cards each get the treatment independently; an empty `prices` array renders the `Receipt`-icon muted empty state. `components/products/offering-detail-region.tsx` (edit) — the **Prices** seam now renders `<PricesPanel prices={offering.prices} locale={locale} timezone={timezone} />` when `offering` is non-null, replacing the pm05 `{/* pm08: prices cards */}` placeholder; the Details (pm06) and Specifications (pm07) frames are byte-unchanged; no `page.tsx` edit (pm06 already threads `offering`/`locale`/`timezone` into the region). The `--color-cyan-*`/`--color-info-*` tokens `PriceTypeBadge`/`stateTag` reference already existed in `globals.css` from prior units — no token addition needed. New test files `tests/components/price-type-badge.test.tsx` (4 tests), `tests/components/tier-table.test.tsx` (3 tests), `tests/components/prices-panel.test.tsx` (17 tests: card-per-price, flat amount + currency code + no TierTable, tiered → TierTable + no amount line + no "(tiered)" literal, each field row's present/absent pairing, current/future/superseded styling incl. multi-current, Effective window incl. Open-ended-not-Never, empty state). Two intended test edits per spec §3.7: `tests/lib/formatters.test.ts` gained a `formatCurrency` `describe` block (3 tests, mirroring `formatMoney`'s NBSP-aware assertions — `Intl.NumberFormat`'s U+00A0 separator asserted via `toBe`, since these are direct pure-function calls, not DOM queries); `tests/components/offering-detail-region.test.tsx`'s populated-Prices fixture now carries one non-empty `PriceCard`, and its assertion checks the price's `name`/`productOfferingPriceId` render instead of just the "Prices" heading — the `hasSelection:false` and `notFound:true` cases, and the Details/Specifications assertions, are unchanged.

**Deviations from the spec's stated diff scope:** none — diff is exactly the one edited `lib/` formatter, the three new components, the one edited region component, and the four test files (three new, one edited with the `formatCurrency` block) the spec named. One correction made during verification, not a scope deviation: `formatCurrency`'s `Intl.NumberFormat` output uses U+00A0 (NBSP) as its currency/amount separator; direct pure-function assertions (`tests/lib/formatters.test.ts`) needed the literal NBSP character to match `toBe()`, while DOM-query assertions (`tests/components/prices-panel.test.tsx`, `getByText`) needed a plain ASCII space instead — Testing Library's default text normalizer collapses NBSP to a regular space before matching, so a query written with a literal NBSP silently fails to find a DOM node whose normalized text contains only a regular space. Both test files now use the correct character for their assertion style.

**Verified this session:** `tsc --noEmit`, `eslint .`, and `prettier --check .` all clean on every touched/new file (one `prettier --write` pass applied to `prices-panel.tsx` and `tier-table.tsx` for line-wrap only, then re-verified clean); `npx vitest run` (unit config) — 118 files / 1069 tests green (24 new assertions: 4 + 3 + 17 across the three new test files, 2 more folded into the edited `formatCurrency`/region tests' existing describe blocks), zero pre-existing assertions broken beyond the two intended edits named above. `git status` diff matches the spec's §5 diff-hygiene checklist: `lib/formatters.ts` (edit), the three new components, `offering-detail-region.tsx` (edit), and the four test files (three new, one edited) — no `globals.css` change needed (tokens pre-existed), no `app/**`, `services/**`, `db/**`, `validation/**`, `actions/**`, `app/api/**`, `admin-nav.tsx`, `offering-table.tsx`, or `offering-detail.tsx`/`specifications-panel.tsx` change.

**Not verified this session:** a live dev-server walkthrough of the populated Prices section (both seeded offerings' price cards, the tiered Data Overage table, derived effectivity styling against the real seed data/clock) and the CI pipeline's SAST/DAST baseline (no CI environment or browser driven this session) — pm08-spec §5's "Behavior" and "Pipeline" checklist items remain open before this unit can be called fully verified end-to-end. The integration vitest config was not re-run this session (pm08 makes no `services/**`/`db/**` change, so it does not affect that suite, but it also was not freshly confirmed green).

**Committed** as `9561d27` ("product offering prices panel: populated PricesPanel + formatCurrency (pm08)") — exactly the pm08-spec §3.8 file set: `lib/formatters.ts` edit, `price-type-badge.tsx`/`tier-table.tsx`/`prices-panel.tsx` new, `offering-detail-region.tsx` edit, and the four test files. **Note on sequencing:** pm08-spec's dependency gate (§Dependencies: "pm08 must not start before pm07 is merged") was not literally satisfied at implementation time — pm07 was still uncommitted when pm08's component work began, per explicit user direction to proceed with pm08 first (ahead of the separate pm09 ship-gate request that prompted this work). To keep git history consistent with the build-plan's stated unit boundaries despite that, the two units' combined working-tree diff was split back into two commits before either was pushed: the `offering-detail-region.tsx`/`offering-detail-region.test.tsx` edits were temporarily reverted to their pm07-only state (Specifications populated, Prices still the placeholder), pm07 was committed alone (`998ed1b`), then the Prices-seam edits were reapplied and pm08 was committed alone (`9561d27`) — both commits' file sets exactly match their spec's §3.6/§3.8 diff-hygiene checklists, and `npx vitest run`/`typecheck`/`lint`/`format:check` were re-verified green after the split. With pm08 merged, all of pm01–pm08 are committed and green — pm09 (authz-matrix entry + guardrail sweep, the module's ship gate) may now proceed with its dependency gate genuinely satisfied.

---

**pm09 implemented per `specs/pm09-spec.md`** — §3.1–§3.5 in full, tests + CI only, no product feature code touched.

**§3.1 Pre-flight guardrail audit (all 6 inherited guardrails confirmed present and green before any new code was written):**
- **Guardrail 2 (price immutability, structural half)** — `tests/db/product-repository-exports.test.ts` asserts `productOfferingPriceRepository`'s only export (`findByOfferingIdWithDerivedEnd`) matches no `/^(insert|create|update|delete|remove|set)/` pattern. Present, green. The *behavioral* half of this guardrail's build-plan wording ("replacement price insert leaves the old row untouched and bumps version") is not independently testable in v1: `product_offering_price` has no `version` column at all (only `product_offering` does), and the price repository exports zero write functions — there is no `insertPrice` yet (that lands with the CRUD fast-follow), so "replacement" isn't a thing that can happen. Immutability is trivially and permanently guaranteed in v1 by the total absence of a mutation path, which is exactly what the structural assert (and the pm09 sweep's re-assertion, §3.3) proves. This is a spec/reality mismatch worth flagging, not an owning-unit defect to fix — pm02/pm03 correctly built no write surface.
- **Guardrail 3 (overlap constraint)** — `tests/db/product-schema.integration.test.ts`, test `"duplicate (product_offering_id, price_type, start_date_time) insert fails (revised Inv. #2)"`. Present; confirmed green this session against the live DB.
- **Guardrail 4 (derived effectivity)** — `tests/services/get-offering-detail.service.test.ts` + `tests/db/product-repositories.integration.test.ts`'s `"derived effectivity from real SQL"` describe block. Present; confirmed green.
- **Guardrail 5 (JSONB validation)** — `tests/validation/pricing-characteristics.schema.test.ts` (Zod tier gap/overlap) + `tests/db/product-schema.integration.test.ts`'s `"flat + NULL amount and tiered + non-NULL amount both fail the CHECK (Inv. #5)"`. Present; confirmed green.
- **Guardrail 6 (deep link)** — `tests/app/product-offering-page.test.tsx`. Present; part of the 119-file unit-suite green run.
- **Guardrail 7 (rename invariance)** — `tests/app/route-manifest.test.ts`; `ROUTE_MANIFEST` already includes `"/products/product-offering"` (added by pm05, §3.5's called-out exception) — no edit needed here, only a re-assertion in the sweep test (§3.3).
- **pm05's page-guard unit test** (guardrail 1's unit half) — `tests/app/product-offering-page.test.tsx`, `"calls requirePermission(PERMISSIONS.PRODUCTS, LEVELS.READ) as the first statement"` + `"propagates the /no-access redirect..."`. Present, green.

**§3.2 Authz-matrix entry** — `tests/auth/guard.integration.test.ts` (edit), four surgical additions exactly as specced: seeded the `products` permission... **except** it could not be inserted via the `beforeAll`'s bulk `db.insert(permissions).values([...])` the way the spec's snippet showed — see Deviations below; granted `products:DELETE` to the admin role (`DELETE ⊃ EDIT ⊃ READ`); added `[PERMISSIONS.PRODUCTS, LEVELS.READ/EDIT/DELETE]` to the admin `it.each` satisfied-cases table; added `PERMISSIONS.PRODUCTS` to the no-grants denial `it.each` list.

**§3.3 Guardrail sweep** — `tests/guardrails/product-module-boundaries.test.ts` (new), pure `node:fs`/`node:path`, no DB, no jsdom (runs in the unit suite). Five assertions, each with a one-line invariant comment: no `actions/product/` folder; no `app/api/product*` path (walks `app/api/**`, checks every path segment); the price repository's exported methods (parsed from `db/repositories/product-offering-price.ts`'s source text, not a live import — deliberately duplicating pm03's runtime assert from source so the ship gate re-checks it module-wide) include no `update*`/`delete*` (`insertPrice` excepted); no product read path (`services/product/**`, `db/repositories/product-*.ts`, `app/(app)/products/**`, `components/products/**`) references the actual audit-write surface (`db/repositories/audit.repository.ts`'s `insertAuditEvent`, matched via the source substrings `"audit.repository"` / `"insertAuditEvent"` / `"AUDIT_LOG"` — note the spec's suggested identifiers `audit-log-repository`/`insertAuditLog` don't match this codebase's actual names, `audit.repository.ts`/`insertAuditEvent`; the real ones were used instead); the frozen `tests/app/route-manifest.test.ts` `ROUTE_MANIFEST` array contains `"/products/product-offering"` exactly once (parsed from that file's source text, not imported).

**§3.4 Doc/permission-map verification** — all three surfaces audited and found already correct, **no doc edit**: `prodmgmt-architecture.md` §4 line 71 carries the `/products/product-offering (list + detail + specs + prices) | Authenticated | products : READ` row; `prodmgmt-code-standards.md` §8 line 138 carries the matching per-page map row (`Product Offering — list + detail + specifications + prices | /products/product-offering | ProductOfferingPage → OfferingTable, OfferingDetail, SpecificationsPanel, PricesPanel | app/(app)/products/product-offering/ | products : READ`); `auth/permission-constants.ts`'s `PERMISSIONS.PRODUCTS = "products"` and `types/rbac.ts`'s `PERMISSION_NAMES` both carry `"products"`.

**Deviations from the spec's stated diff scope (forced by the actual repo state, not optional):** migration `db/migrations/0006_product.sql` (line 57) inserts the `products` permission row **directly as DML inside the schema migration itself** (`INSERT INTO "core"."permissions" ... VALUES ('products', ...)`) — a pm02 design choice (code-standards §8: "Single `products` permission, page-level, code-seeded via migration"). `guard.integration.test.ts`'s `beforeAll` runs a real `migrate()` against a freshly-dropped schema before seeding its own test permissions, so by the time the test's own permission-seed step runs, `core.permissions` **already contains** a `products` row. The spec's literal snippet (§3.2 point 1 — append `{ permissionName: "products", permissionInfo: "Products" }` to the bulk `db.insert(permissions).values([...])` array) therefore fails at runtime with a `permissions_permission_name_unique` violation (confirmed by actually running the suite against the live DB, not just typechecking it). Fixed by *not* inserting `products` in that array (only the four pre-existing rows are inserted, unchanged) and instead `SELECT`-ing the migration-seeded row's `permissionId` by name, merging it into the same `permissionIdByName` map the grants step already reads from. The four `it.each` additions (satisfied cases, no-grants denial) are unchanged from the spec's exact shape. This is exactly the kind of thing §3.1's audit is supposed to catch before the "small surgical edit" is assumed to just work — the deviation was only discoverable by running the integration suite against a live database, not by reading the spec or the schema in isolation.

**Verified this session:** `tsc --noEmit`, `eslint .`, `prettier --check .` all clean; `npx vitest run` (unit config) — 119 files / 1074 tests green (5 new: the guardrail sweep), zero pre-existing assertions changed; `npx vitest run --config vitest.integration.config.ts` against the local Docker Postgres dev container (containers were already running from a prior session) — **29 files / 270 tests, all green**, including the extended `guard.integration.test.ts` (33/33, not `skipIf`-skipped — `DATABASE_URL` was set for the run, satisfying the ship gate's explicit requirement that the matrix actually execute) and the full pre-existing Administration suite (zero regressions). `git status` diff matches the spec's §5 diff-hygiene checklist exactly: `tests/auth/guard.integration.test.ts` (edit) and `tests/guardrails/product-module-boundaries.test.ts` (new) — no `tests/app/route-manifest.test.ts` edit needed (pm05 already added the row); no `app/**`, `services/**`, `db/**`, `validation/**`, `components/**`, `actions/**`, `app/api/**`, `auth/**`, or `infra/**` change; no dependency/lockfile change; no doc edit (§3.4 audit found no gap).

**Not verified this session:** the CI pipeline's SAST/OWASP ZAP DAST baseline (no CI environment available here) — pm09 adds no new route, handler, or input surface (tests-only unit), so the attack surface is asserted unchanged from pm08, but the actual baseline scan was not run. No fresh live dev-server behavioral walkthrough was driven this session (pm09 introduces no runtime/UI change, so the pm05–pm08 sessions' prior spot-checks stand; the running `app`/`db` containers observed this session predate this session's file changes and were not rebuilt, since nothing runtime-visible changed).

**All seven build-plan guardrails, ship-gate status:**
1. **Authz matrix** — ✅ green (33/33 `guard.integration.test.ts`, incl. `products:READ/EDIT/DELETE` satisfied + no-grants denial; pm05's page-guard unit test present and green).
2. **Price immutability** — ✅ structural half green (pm03 unit assert + pm09 sweep re-assert); behavioral "bumps version" half not applicable in v1 (see Deviations/audit note above — no mutation path exists yet).
3. **Overlap constraint** — ✅ green (pm02 integration test).
4. **Derived effectivity** — ✅ green (pm03 unit + integration tests).
5. **JSONB validation** — ✅ green (pm02 Zod + DB CHECK tests).
6. **Deep link** — ✅ green (pm05 page test).
7. **Rename invariance** — ✅ green (pm01 test; manifest already includes the new route, re-asserted by the pm09 sweep).

**Not yet committed** — changes are in the working tree; commit per user confirmation (pm09-spec §3.6 names one commit: `tests/auth/guard.integration.test.ts` edit + `tests/guardrails/product-module-boundaries.test.ts` new). With pm09 verified and merged, the read-only Product Management v1 catalog viewer is complete and gated per workflow §8; the CRUD fast-follow (which will finally exercise the "insertPrice bumps version" guardrail wording literally) is the next, separately-authorized phase.

---

**Post-ship UI density pass (2026-07-09):** user-requested front-end polish on `/products/product-offering`, no data-layer change. `page.tsx` dropped the subtitle paragraph under the `<h1>` and tightened `main` padding/gap (`p-6`/`space-y-6` → `p-5`/`space-y-5`). `offering-detail.tsx`'s Version/Last Modified/Last Edited By fields moved from a two-column `dl` grid to one wrapped flex row (`flex flex-wrap gap-x-6 gap-y-2`), and outer paddings/gaps across `offering-detail-region.tsx`, `specifications-panel.tsx`, and `prices-panel.tsx` were reduced (`p-4`→`p-3`/`p-2`, `gap-4`→`gap-3`, empty-state `p-8`→`p-6`, etc.). Per explicit instruction, JSONB entries no longer render through dedicated widgets: `components/products/characteristic-chip.tsx` and `tier-table.tsx` (and their two dedicated test files) were deleted; `specifications-panel.tsx` now inlines `Object.entries(spec.characteristics)` as `key: value` text, and `prices-panel.tsx` inlines `pricingCharacteristics.tiers` as `from–to: rate` text (semicolon-separated; open-ended top tier still reads "and above") — both routed through a small local text-coercion helper since the JSONB read path (`get-offering-detail.ts` → repositories) has no runtime Zod re-validation, only the write/seed path does. `specifications-panel.test.tsx` and `prices-panel.test.tsx` updated to assert the new plain-text output instead of chip/table structure. Docs updated in the same change set: `prodmgmt-code-standards.md` §4 (styling rules) and §7 (file tree) no longer list `CharacteristicChip`/`TierTable`; `prodmgmt-ui-context.md` §0/§3/§4 updated to describe plain-text rendering. No `services/**`, `db/**`, `validation/**`, `actions/**`, `app/api/**`, or permission change.

**Post-ship deployment bug found (2026-07-09):** the deployed `ebill-dev-app` ACA instance 500s on `/products/product-offering` with Postgres `42501: permission denied for schema product`. Root cause is a provisioning gap, not app code: `db/bootstrap/bootstrap-db-roles.sql` (um30) only ever granted `app_runtime`/`app_migrate` privileges on the `core` schema — its own header comment flagged `product`/`customer`/`billing`/`accounting` as follow-ups "as those schemas ship," but the follow-up was never done when pm02 shipped the `product` schema. Fixed by appending a `product` schema GRANT/REVOKE block to `bootstrap-db-roles.sql` (mirrors `core`'s block, plus `GRANT ... ON ALL SEQUENCES IN SCHEMA product` since `product`'s 3 ID columns default to `nextval(...)`, unlike anything in `core`). The code fix alone does not repair the already-provisioned dev environment — that requires re-running `npm run db:bootstrap-roles` against `BOOTSTRAP_DATABASE_URL` (superuser/owner connection) per `infra/docs/db-role-verification.md`; user opted to run that step themselves rather than have it done in-session. **Any future schema that ships (`customer`/`billing`/`accounting`) must add its own grant block to this file in the same PR that adds its migration** — this bug will otherwise recur per schema.

## Per-unit specs

| Unit | Spec file            | Summary                                                                                                                                                                                                                                                                  |
| ---- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| pm01 | `specs/pm01-spec.md` | Rename `app/(admin)/**` → `app/(app)/**` (14 files, `git mv`); update 6 test imports + 2 path comments; add `tests/app/route-manifest.test.ts` rename-invariance proof (manifest set-equal, old group gone, no stale `(admin)` reference in code dirs). No other change. |
| pm02 | `specs/pm02-spec.md` | `product` schema/migration/seeds in one unit: 3 tables + enum + 3 sequences + all constraints (`0006_product.sql`, incl. `products` PERMISSIONS row); permission registry wiring; `validation/product/` (spec characteristics, pricing characteristics w/ tier contiguity + amount-XOR-tiers discriminated union, offering-list searchParams); `db/seeds/product.ts` (2 offerings/3 specs/6 prices + ADMIN grant); unit + integration guardrail tests. No `app/**`, `actions/product/`, or repository code (pm03+). |
| pm03 | `specs/pm03-spec.md` | Read backend for the module: 3 finder-only repositories (price repo permanently exports no `update*`/`delete*`, Inv. #1), `services/product` (`listOfferings` w/ configurable page size, `getOfferingDetail` w/ derived `endDateTime` + injectable-clock `effectivityStatus`), data-only migration seeding the page-size `SYSTEM_CONFIG` row, `types/product.ts` read models. No `app/**`, `actions/product/`, or UI. |
| pm04 | `specs/pm04-spec.md` | `components/admin-nav.tsx`: flat `NAV_ITEMS` → sectioned `NAV_SECTIONS` (caption + items), new "Products" section (Product Offering) above "Administration"; collapsed-rail hairline divider between sections; Administration items/active-state/collapsed behavior byte-identical. No `app/**`, `admin-sidebar.tsx`, permission check, or dependency change. |
| pm05 | `specs/pm05-spec.md` | `app/(app)/products/product-offering/page.tsx` (new, guard + searchParams parse + `Promise.all` fetch); `components/products/lifecycle-badge.tsx` + `offering-table.tsx` + `offering-detail-region.tsx` (new); four new test files + one-line `route-manifest.test.ts` addition. No `services/**`, `db/**`, `validation/**`, `actions/**`, `app/api/**`, or `admin-nav.tsx` change; no field-level detail/spec/price rendering (pm06–08); no authz-matrix entry (pm09). |
| pm06 | `specs/pm06-spec.md` | `components/products/offering-detail.tsx` (new) — populated Details section: mono ID eyebrow, name, `LifecycleBadge`, flag chips (`Bundle`/`Sellable`/`Billing only` + ACTIVE-not-sellable warning chip), labeled Version/Last Modified/Last Edited By grid; `offering-detail-region.tsx` + page threading (edit). No `services/**`, `db/**`, `validation/**`, `actions/**`, `app/api/**`, `admin-nav.tsx`, or `offering-table.tsx` change; no specs/prices rendering (pm07/pm08); no authz-matrix entry (pm09). |
| pm07 | `specs/pm07-spec.md` | `components/products/characteristic-chip.tsx` (new) + `specifications-panel.tsx` (new) — populated Specifications section: mono spec-ID eyebrow, name, `Mandatory`/`Default` badges (flag-true-only), independent Default-value row, JSONB characteristics as chips (insertion order), muted empty state; `offering-detail-region.tsx` edit (Specifications seam only). No `services/**`, `db/**`, `validation/**`, `actions/**`, `app/api/**`, `admin-nav.tsx`, `offering-table.tsx`, or `offering-detail.tsx` change; no prices rendering (pm08); no authz-matrix entry (pm09). |
| pm08 | `specs/pm08-spec.md` | `lib/formatters.ts` edit (`formatCurrency`) + `components/products/price-type-badge.tsx` (new) + `tier-table.tsx` (new) + `prices-panel.tsx` (new) — populated Prices section: mono price-ID eyebrow, name, `PriceTypeBadge`, flat amount (`formatCurrency` + currency code) or `TierTable` (JSONB tier values as stored text), field rows (charge period/unit of measure/GL code/policy, flag-true-only), derived Effective window incl. "Open-ended", effectivity-status card styling (current/future/superseded); `offering-detail-region.tsx` edit (Prices seam only). No `app/**` (incl. `page.tsx`), `services/**`, `db/**`, `validation/**`, `actions/**`, `app/api/**`, `admin-nav.tsx`, `offering-table.tsx`, `offering-detail.tsx`, or `specifications-panel.tsx` change; no authz-matrix entry (pm09). |
| pm09 | `specs/pm09-spec.md` | `tests/auth/guard.integration.test.ts` edit (seed + grant `products`, add `products:READ/EDIT/DELETE` satisfied cases, add `products` to the no-grants denial loop) + `tests/guardrails/product-module-boundaries.test.ts` (new, `node:fs`-only) — the module's ship gate: authz-matrix entry + a 5-assertion negative-space sweep (no `actions/product/`, no `app/api/product*`, price repo write-export shape, no audit-write import from product read paths, route manifest contains the new route once). Tests + CI only — no `app/**`, `services/**`, `db/**`, `validation/**`, `components/**`, `actions/**`, `app/api/**`, `auth/**`, or `infra/**` change; no doc edit (audit found no gap). |
