# Product Management — Progress Tracker

Single living build record for the Product Management module. Delivered units **pm01–pm34** span three phases, all ship-gate-verified; **Part 3 (pm35–pm45)** is delivered and unit/integration/type/lint-verified (the CI SAST/DAST gate and the merge to `main` remain outstanding). **Part 4 — Pricing Components (pm46–pm56)** is implemented and verified on `dev1` only: pm56's ship gate ran on 2026-09-24 and its definition of done is **not fully met**. Neither Part 3 nor Part 4 is in `main` (G-0/G-A still open). See the pm56 entry and the pm54/pm55 review-fix pass below. Known defects outside the unit in flight are tracked in `prodmgmt-issues-tracker.md`. The per-unit ledger at the end is compacted on purpose — full detail for any delivered unit lives in its `specs/pmXX-spec.md`, and commit history in `git log` (commit refs are not tracked here).

**pm36 (Family uniqueness indexes + DRAFT-guard trigger): IN PROGRESS — implemented + DB-verified (2026-09-19).** DB-only unit (`db/**` boundary): new forward migration `0040_product_family_guards.sql` (two expression unique indexes on `COALESCE(family_offering_id, product_offering_id)` — one open, one ACTIVE per family — plus the `product.child_write_requires_draft()` trigger function and its two `BEFORE INSERT OR UPDATE OR DELETE` triggers on `product_specifications`/`product_offering_price`); `meta/_journal.json` gained the `0040` entry (`when` 1788282429615 > 0039's); the two indexes mirrored by hand into `db/schema/product.ts` (trigger/function have no Drizzle representation — `0040` is the SQL of record); new `tests/db/product-family-guards.integration.test.ts` (17 cases incl. the two role-context cases under `app_runtime`); an I4 concurrency case in `tests/db/product-repositories.integration.test.ts`; the guardrail extension in `tests/guardrails/product-module-boundaries.test.ts` (0040/schema/journal freeze + the rating/billrun no-write-grant static assertion). No repository, service, action, validation or UI change; the two bootstrap role files need no change (proven by the new guardrail).

**Spec deviation (necessary bug-fix, D3/I1).** The trigger function as written in the spec packs field references for BOTH child tables (`OLD/NEW.ref_product_offering_id` and `OLD/NEW.product_offering_id`) into ONE `CASE` expression. PL/pgSQL plans an assignment's expression as a single unit, so it resolves every field reference regardless of branch and fails at runtime with `record … has no field …` on both tables (empirically: 11 trigger cases red). Fixed by splitting the parent-id resolution into two `TG_TABLE_NAME`-guarded `IF` branches — PL/pgSQL plans each statement lazily on first execution, so the untaken branch never resolves an absent column. Design intent (one function, two tables; parent-not-found ⇒ allow; ERRCODE 23514 message) is unchanged.

**Post-review hardening (2026-09-19, PR review).** The original spec/trigger validated only `NEW`'s parent on UPDATE, on the reasoning that "no code re-parents." A reviewer correctly noted the trigger is a backstop against **direct-SQL** writes (D2), and a direct re-parent (`UPDATE … SET product_offering_id = <draft> WHERE product_offering_id = <active>`) would silently strip a spec/price off a released version. `0040` was edited **in place** (consistent with the module's rebuild-from-empty regime) so the trigger now validates **both** the `OLD` and `NEW` parent when they differ — a re-parent is refused unless both offerings are `DRAFT`; unchanged-parent and cascade-delete behaviour are preserved exactly. The pm36 spec's SQL block + the "one side sufficient" note were synced to match (already-migrated environments must rebuild to pick up the hardened trigger). Three re-parent cases added to `product-family-guards.integration.test.ts` (OLD-side reject, NEW-side reject, both-DRAFT allow); suite now 20 cases, all green.

**Verified against the disposable test DB (`.env.test`, port 5434):** `tsc --noEmit`, `eslint`, `prettier --check` clean on every changed file; guardrail suite green (12/12, incl. the two new pm36 assertions); `product-family-guards` (17/17); `migration.integration` (drop-all → from-empty migrate → idempotent second run, 15/15); the I4 concurrency case green; `product-price-constraints` green (11/11) after the companion fix below; `db:seed-demo`'s product catalog verified loading end-to-end under the trigger (both offerings ACTIVE, 3 specs + 6 prices) via a throwaway script against the test DB.

**Companion fixes the trigger forced (I6 "the fix belongs in the pattern, not an exemption to the trigger").** Two previously-green fixtures inserted a child row directly onto a non-DRAFT parent — now correctly illegal — and were reshaped to insert-while-DRAFT-then-activate: (1) `db/seeds/demo/product-demo.ts` (`seedProductDemo` inserted the offering `ACTIVE` then added specs/prices — would have failed `db:seed-demo`); (2) the `product-price-constraints` cascade-sibling case (inserted a price on an ACTIVE sibling). `db/seeds/sample/**` already followed this pattern (pm35) and needed no change.

**Open flags for the user (two spec/reality contradictions in `product-repositories.integration.test.ts`).** (1) The spec's I4 says "the existing two-concurrent-activations test … must still pass unchanged," but that test creates two open DRAFT siblings in one family — which the new `product_offering_one_open_per_family` index makes impossible (Inv. #27). It is also already red under pm35 option-C (its `insertFlatPrice(recurring)` setup violates the pm35 period CHECK). It stays unchanged and red. (2) pm36's trigger additionally turns three previously-green GATE9 branch-not-mutate cases red (they `insertSpecRow` directly onto an ACTIVE offering in setup). Per the recorded pm35 option-C directive — product-repositories fixtures are hands-off and co-land with the price-management/edit unit — these were **not** edited piecemeal (fixing them wouldn't make the file green anyway: six other cases fail through the still-null `insertPrice` service and cannot pass without that unit's rework). The co-landing unit must reshape all these setups to insert-while-DRAFT-then-activate.

**Trigger widens the option-C co-land scope (verified 2026-09-19 by an xhigh review sweep).** Every suite the pm35 option-C note already lists as red on the price CHECKs — `product-repositories`, `product-schema`, `create-order`, `ordering-read`, `review-order`, `subscription-lifecycle`, `ship-gate-guardrails`, and rating `rm08`/`rm09`/`rm10`/`rm13` — _also_ inserts its child rows onto an offering it created `ACTIVE` (verified: e.g. `create-order`'s `newOffering`, `ordering-read`, and the `rm08/10/13` usage fixtures). pm36's DRAFT-guard trigger rejects those inserts too, so the co-landing unit must fix them on **two** axes, not one: complete the price shape (pm35 CHECKs) **and** reorder to insert-while-DRAFT-then-activate (pm36 trigger). No suite that was green under pm35 is newly broken by the trigger except the two already reshaped here (`db/seeds/demo/product-demo.ts`, `product-price-constraints`); the sweep confirmed the `billrun-*` suites price a DRAFT-default offering (trigger-safe), the sample seed already inserts-then-activates, and `get-offering-detail.service.test.ts` is fully mocked.

**pm37 (Domain types, total maps, lifecycle badges): IN PROGRESS — implemented + verified (2026-09-19).** Types/read-only/badge unit (`types/**`, `components/products/*`, `db/repositories/product-offering.ts` default filter only + the pm37-owned G2 fixes). Widened `LIFECYCLE_STATUSES` to the five DB values in lifecycle order and added the `UNITS_OF_MEASURE`/`RECURRING_PERIOD_TYPES`/`RECURRING_PERIOD_LENGTHS` vocabularies (D1/D5); rebuilt `LifecycleBadge` as a total `Record<LifecycleStatus, LifecycleBadgeVariant>` with `TESTING` (info tint, `flask-conical`) and `OBSOLETE` (neutral-600, `history`, muted) badges (D3); `buildWhereClause` default branch now hides `OBSOLETE` **and** `RETIRED` (D4/G2, user-visible View Product change); `offering-table` row muting reads the badge `muted` map (so OBSOLETE mutes too) and the status filter offers all five values with the label reworded to "All (excl. obsolete & retired)". Per the build-plan Gate-status assignment the only pm37-owned G2 app-code fixes are the default filter, the `offering-table` muting literal, and the `order-preconditions.ts` price-immutability comment re-derivation (edit-guard/activate/retire `'RETIRED'` sites are deferred to pm38/41/42/43/44; the offer picker `new-order-wizard-reads.ts` stays `ACTIVE`-only, confirmed unchanged; `workflow-management/**` has zero references). Tests: new `tests/types/product-unions.test.ts` (5 cases), rebuilt `lifecycle-badge.test.tsx` (5 variants + distinct icons + muted map + no-hex), a default-filter case in `product-repositories.integration.test.ts`, and the stale "RETIRED exclusion" name in `list-offerings.service.test.ts` corrected. No schema, migration, service transition, action or write path.

**Spec deviation (forced by the repo CSP rule).** I2 specifies the badge variant as `{label, icon, fgToken, bgToken, muted}` consumed via `style`, but inline `style` props are banned repo-wide (ESLint `no-restricted-syntax` — CSP has no `'unsafe-inline'` in `style-src`, ZAP 10055). The variant therefore carries a literal Tailwind `className` referencing the same globals.css tokens (`bg-[color:var(--color-info-50)] text-[color:var(--color-info-700)]`, no hex) instead of `fgToken`/`bgToken`+`style` — the JIT sees the literal string, the substance (tokens from globals.css, no hex, total record, muted flag) is unchanged.

**Verified:** `tsc --noEmit` clean (no `default:`/`as` added — the total `Record` is compiler-proven; deleting an entry fails the build, seen when the map still had three); `eslint` + `prettier` clean on every changed file; directly-affected unit tests green — `lifecycle-badge` (rebuilt), `product-unions` (new), `offering-table`, `manage-offering-table`, `list-offerings.service`, `product-offering-page` (76 + 31 across two runs); the default-filter integration case green against the disposable test DB (default hides OBSOLETE+RETIRED, explicit `status=OBSOLETE` surfaces it). Full DB-free suite: **3174 test-assertions pass**; the only file-level failures are the pre-existing "Vitest needs .env" config-import quirk in unrelated `services/billing/**` files (`[[vitest-needs-env-file]]`), not pm37. **Release note owed:** View Product's default list now also hides `OBSOLETE` (a version superseded by an activation drops out of the default list) — flag when this update's release notes are compiled (D4).

**Post-review fix (2026-09-19, PR review).** `offering-table` muted the whole row via the badge `muted` map, but the name cell forced `text-foreground`, so a muted row's name rendered full-emphasis while its other cells dimmed. The name cell's `text-foreground` is now conditional on `!isMuted`, so OBSOLETE/RETIRED rows dim consistently. **Deferred (skip with reason):** the reviewer's ask to rework the existing "two-concurrent-activations" race test in `product-repositories.integration.test.ts` — that scenario (two open DRAFT siblings racing to activate) is structurally impossible under `product_offering_one_open_per_family` (Inv. #27), so the test's premise is obsolete; it also can't be made green without pm38's `insertPrice` rework, and the file is the hands-off "co-land with pm38" surface per the recorded option-C directive. Its removal/rework belongs to that co-land (the one-open invariant is already covered by pm36's I4 branch-race case).

**Open flag for the user / pm39 (found by an xhigh review sweep, 2026-09-19; deferral confirmed by Khek — keep flags, edit nothing out of boundary) — spec D4 is factually wrong about Manage Products.** D4/I3 state "Manage Products does not use this query and is unaffected." It is affected: `app/(app)/products/manage-products/page.tsx` builds its "every lifecycle status" set from `fetchAllForStatus(null)` **+** `fetchAllForStatus("RETIRED")`, and `fetchAllForStatus` → `listOfferings` → `findList` → the very `buildWhereClause` this unit changed. Widening the `null` default to exclude OBSOLETE **and** RETIRED means the two-bucket fetch now covers DRAFT/TESTING/ACTIVE/RETIRED but **misses OBSOLETE** — an OBSOLETE version would silently vanish from Manage Products. **Not edited here, by design:** it is out of pm37's boundary (that file is rewritten by **pm39**, which replaces the whole `fetchAllForStatus`/`groupIntoFamilies` fan-out with `findFamilyPage`), and the gap is inert today because no write path produces OBSOLETE until **pm42** — so in the intended order (pm37 → pm39 → pm42) the gap never manifests. **Load-bearing consequences:** (1) pm39's `findFamilyPage` MUST return OBSOLETE versions (Manage Products shows every status, code-standards §3.5) — do not carry the two-bucket exclusion into it; (2) pm39 must land before pm42, or Manage Products drops obsoleted versions in the window between; (3) manually setting a version OBSOLETE in `psql` (the pm37 DoD's own check) renders correctly on **View Product** but would disappear from **Manage Products** until pm39 — the DoD only asserts View Product, so this is acknowledged, not a pm37 miss. The stale `findList` header comment was corrected in place to record the OBSOLETE+RETIRED default and this caveat. **Deferred-and-correct (not bugs), same review:** the `manage-offering-table.tsx` `=== "RETIRED"` muting/action literals (three sites) still miss OBSOLETE, but that file is write-side (`components/products/manage/**`, out of pm37's read-only boundary) and is rewritten by pm39–pm41; the edit-guard/activate/retire service literals stay deferred to pm38/41/42/43/44 per the G2 assignment.

**pm38 (Price completeness + DRAFT-only price mutation): IN PROGRESS — implementing (2026-09-19).** Vertical price-write slice (`validation/product/**`, `db/repositories/product-offering-price.ts`, `services/product/**` price writes, `actions/product/**`, `components/products/manage/price-form.tsx`). Delivers: the D1 discriminated `priceInputSchema` (new `price-input.schema.ts` — `recurring` requires length ∈ (1,3,12) + `months`, `usage` requires a `UNITS_OF_MEASURE` unit, `once` neither; strict branches so a forbidden field is rejected by Zod, not stripped); `insert-price.schema.ts` recomposed from it (single-source `THREE_DAYS_MS`/`startDateTimeField`, D2); new `update-price.schema.ts` sharing the core + editable `startDateTime`; repo `updatePrice`/`deletePrice` (D3 — locked parent read `FOR UPDATE OF product_offering` → `PRICE_NOT_FOUND` / `OFFERING_NOT_DRAFT` / write, no branch, D4); `update-price.ts`/`delete-price.ts` services (`PRODUCT_PRICE_UPDATED`/`_DELETED` audit, backdating, `DUPLICATE_START` translation of the 23505 on `product_offering_price_type_start_unique`); `update-price.action.ts`/`delete-price.action.ts` (id as its own param, pm22 convention); `price-form.tsx` period/unit fields + D5 unbillable-shape warning banners. Tests: `tests/validation/price-input.test.ts`, `tests/db/product-price-writes.integration.test.ts`, rewritten price-repository + `PRODUCT_ACTION_FILES` (ten files) guardrails.

**Spec deviations (necessary, flagged).** (1) **I3 "keep `insertPrice` unchanged" is impossible under pm35's live CHECKs.** The shipped `insertPrice` hardcodes `recurring_charge_period_*`/`unit_of_measure` to `NULL`, which fails `product_offering_price_recurring_period_check`/`_usage_unit_check` for every recurring/usage price the instant pm35 landed (the exact rework the pm35 option-C note assigns to this unit). So `insertPrice`'s `data` param and `.values()` are extended to write the three columns (defaulting `null`), and `services/product/insert-price.ts`'s `priceData` is extended to supply them from the parsed discriminated input (I4). Design intent (repo's writes = `insertPrice`/`updatePrice`/`deletePrice`; caller-supplied target id) is unchanged. (2) **I6's per-price-row Edit/Delete affordance has no surface to attach to today.** Manage Products lists prices nowhere — `AddPriceDialog` only _adds_; prices render only on read-only View Product (`PricesPanel`). pm38's boundary forbids a page rebuild and scopes UI to `price-form.tsx`. So the Edit/Delete _wiring_ is deferred to **pm41** (`ManagePricesPanel`), which the spec itself says owns the inline panel versions; the end-to-end path is proven by the integration test and the action-file guardrail (both `updatePriceAction`/`deletePriceAction` exist and are green). Companion ripples (documented recurring patterns, not scope creep): two new `AUDIT_EVENT_TYPES` + `AUDIT_EVENT_CATEGORY_MAP` entries and the `audit-log-filters` count (73→75); the `product-repository-exports` allow-list gains `updatePrice`/`deletePrice`; `insert-price.action.test.ts`'s `VALID_INPUT` gains the recurring period fields the strict schema now requires.

**pm38 post-review fixes (xhigh multi-agent review, 2026-09-19).** Applied: (#8) `lockParentStatus` dropped the raw-SQL `FOR UPDATE OF` for the query-builder `alias(productOffering, "o")` + `.for("update", { of: o })` idiom already used by `customerBillRepository` — type-safe, unqualified lock name, misleading "Drizzle cannot" comment removed. (#10) `update-price.schema.ts` now re-exports `insertPriceSchema`/`InsertPriceInput` (identical shape per D2, zero drift) instead of a byte-copy; the discriminated-input→columns projection is extracted to one exported `toPriceWriteData()` in the price repository, shared by both `insertPrice` and `updatePrice` services (removes the divergence risk where one write fills a completeness column the other drops). (#2, user-approved) the insert path gained the same `DUPLICATE_START` 23505 translation `updatePrice` has (service + action result + AddPrice dialog toast) — a duplicate `(offering, type, start)` add now returns a typed code, not `SERVER_ERROR`. (#4) `price-form.tsx` client schema gained `name.max(200)` + `glCode.max(50)`, closing the client/server validation drift so over-long values show an inline field error instead of an opaque server rejection. Test hardening: the trigger case now asserts the specific `product_child_write_requires_draft` rejection (not "any error"); the update/delete success cases assert the audit before/after JSONB payload and the surviving completeness columns; new usage-update and recurring→once type-swap cases cover the previously-untested usage/once write branches (the type-swap proves `toPriceWriteData` nulls the now-forbidden columns so the DB CHECKs pass). **Deliberately not changed:** (#1) `tests/db/product-repositories.integration.test.ts` stays the documented-red option-C co-land file (only the two tsc-gating service call sites were completed in the base slice); its full two-axis reshape remains deferred per the user's decision. Low-severity items left as-is with reason: #3 (UI Edit/Delete wiring → pm41), #5/#6/#7/#9/#14.

**pm39 (Manage Products families list — read model + page rewrite): IN PROGRESS — implementing (2026-09-19).** Read-only list slice (`db/repositories/product-offering.ts` +1 read, `services/product/list-families.ts`, `validation/product/family-list.schema.ts`, `app/(app)/products/manage-products/{page,loading}.tsx`, `components/products/manage/family-table.tsx`). Replaces the fetch-everything page (hundreds of queries) with `findFamilyPage` — one CTE (`ROW_NUMBER`/`COUNT`/`FILTER MAX` over `COALESCE(family_offering_id, product_offering_id)`) picking each family's primary version (ACTIVE → open → highest, D1) plus a matching count query, so first render issues exactly two `product_offering` statements and zero child-table queries (D2, V9). `FamilyListRow`/`FamilyPage` added to `types/product.ts`; `family-list.schema.ts` parses `q`/`status`/`page`/`family`/`version` leniently (`family`/`version` first consumed in pm40). Filters apply to the primary version (D3); every status is shown (D4, no default exclusion — closes the pm37 OBSOLETE-drop flag); page size reuses `products.offering_list_page_size` via a shared `resolvePageSize` (D5). The **nine D6 identifiers** are deleted (`fetchAllForStatus`/`fetchAllOfferingRows`/`fetchSpecificationsByOfferingId`/`mapWithConcurrencyLimit`/`groupIntoFamilies`/`selectPrimary`/`resolveFamilyId`/`MAX_COMBINED_ROWS` + `OfferingFamilyRow`), guardrail-enforced.

**pm39 decisions/deviations.** (I7) `manage-offering-table.tsx` had no consumer after the page rewrite, so it was **deleted** with its test; `tests/app/manage-products-page.test.tsx` was rewritten to the new shape (guard + params→listFamilies + FamilyTable props). (D5) `resolvePageSize` extracted to `services/product/resolve-page-size.ts`, shared by `listOfferings` + `listFamilies`; `DEFAULT_OFFERING_LIST_PAGE_SIZE` re-exported from `list-offerings.ts` so existing importers (`list-offerings.service.test.ts`) are unchanged. `FamilyTable` is a **server component** (code-standards §3.6/§3.4): row links + pagination are `<Link>`s and search/status is a GET `<form>` that rewrites the URL with no client component — no client leaf needed. The "New offering" `--action-cta-bg` button moved to the page header (`CreateOfferingDialog`), which the empty state references (dialogs `AddPriceDialog`/`ActivateOfferingDialog`/`RetireOfferingDialog`/`SpecificationsDialog` are now orphaned but stay until pm41 removes them). The query-budget test is named `manage-products-query-budget.integration.test.ts` (spec said `.test.ts`) so the DB-backed project picks it up — it mocks `@/db/client` with a counted `postgres` `debug` client, stubs auth + app-config, renders the real page, and asserts exactly two `product.product_offering` statements and zero child-table statements. **Closes the pm37 OBSOLETE-drop flag:** `findFamilyPage` returns every status (D4), so an OBSOLETE family no longer vanishes from Manage Products.

**Verified (2026-09-19):** `tsc --noEmit`, `eslint`, `prettier --check` clean on all changed files; full DB-free suite **3178/3178** (318 files); new `product-family-page.integration.test.ts` (6 cases: primary selection ACTIVE+DRAFT / DRAFT-only / OBSOLETE+ACTIVE / single RETIRED, `versionCount`/`openVersionId`, D3 primary-filtering, 12-family paging at size 5) and `manage-products-query-budget.integration.test.ts` (1 case: exactly two `product_offering` statements, zero child) green against the disposable test DB (`.env.test`, port 5434); the rewritten nine-identifiers guardrail green.

**pm39 post-review fixes (xhigh multi-agent review, 2026-09-19).** In-boundary: (#2) a stale/out-of-range `?page=N` now renders a distinct "this page is empty — only N pages" state with a "Go to the first page" link, and the pager is guarded on `rows.length` so its "Showing …" footer no longer renders a nonsensical range on an empty page; (#6) status labels (family-table filter + empty-state) use `LIFECYCLE_BADGE_VARIANTS[status].label` (single source), `titleCase` removed; (#10) one shared `escapeLikePattern` in `product-offering.ts` used by `buildWhereClause` and `findFamilyPage`. **Cross-boundary dedup (user-approved "option 3", full extraction — deliberately breaches the frozen-View rule with explicit approval):** `lib/search-params.ts` `firstValue` replaces 9 byte-identical copies (products/customers/billing/administration pages); `validation/product/list-search-params.base.ts` (`listSearchBase` = q/status/page + `offeringIdParam`) recomposes both `offering-list.schema.ts` and `family-list.schema.ts` (shapes unchanged); `components/products/flag-chips.tsx` (`SellabilityChip` + `BillingOnlyChip`) used by both `offering-table` and `family-table`; `components/products/list-pagination.tsx` a shared pager serving the client table (`onNavigate`+`disabled`) and the server table (`hrefFor` → `<Link>`); `offering-table` status options now use the badge label too. The search box + `thead` stay per-surface by design — View's is a client live-search + sortable table, Manage's a server GET-form + static table; unifying those two interaction models would be a worse abstraction, not a fork. **Assessed — not needed (#5, the CTE-index finding):** re-examined and dismissed. pm36's two `COALESCE(family_offering_id, product_offering_id)` indexes are _partial `UNIQUE`_ indexes for invariant enforcement (one ACTIVE / one open per family), not query indexes, and pm36 raised no perf concern (findFamilyPage did not exist then). The families list is a **global grouping** — the window functions must read every offering row to determine each family's primary/count/open-version, so a full scan is inherent and no index removes it; the `q`/`status` filters apply to the rank-1 primary _after_ the window, so they can't shrink the scan either. A plain `COALESCE(...)` btree would not even eliminate the sort — the `WindowAgg` needs `(COALESCE(...), CASE lifecycle_status…, version DESC)` order and the outer `ORDER BY name` its own sort. At this surface's scale (admin catalog, page size 5, tens–low-thousands of rows) the seq-scan+sort is sub-millisecond; pm39's actual perf goal was eliminating the N-query fan-out (pm00: "the cost problem is the fan-out, not the panels"), which it does. Revisit only at a very large catalog, and then rethink the query rather than add a lone index — no schema unit is owed. **Deferred (intended, sequenced):** (#1) Manage Products is list+create only until pm40/pm41 restore the row actions — per the incremental plan, must land as a sequenced bundle. **Verified:** `tsc`/`eslint`/`prettier` clean; full DB-free suite **3178/3178** (318 files) — every existing assertion (offering-table chips/pagination/labels, the 9 pages' parsing) preserved; pm39 integration tests **7/7** against the test DB.

**pm40 (Version bar + read-only panels on Manage Products): IN PROGRESS — implemented + verified (2026-09-19).** Read-only selection slice (`db/repositories/product-offering.ts` +1 read `findFamilyVersions`, `services/product/list-family-versions.ts`, `services/product/resolve-selected-version.ts`, `app/(app)/products/manage-products/page.tsx`, `components/products/manage/version-bar.tsx`, `components/products/manage/selection-region.tsx`; `VersionSummary` added to `types/product.ts`). No write path — no action, mutation, schema, migration or validation change; `PRODUCT_ACTION_FILES` unchanged. Selecting a `?family=` now shows the resolved version's detail, specifications and prices in place, with a `VersionBar` (row of `<Link>`s, newest-first, selected entry `--surface-selected` + `aria-current="page"` + `aria-label` "Version N, <status>", horizontal-scroll + edge-fade, coarse-pointer 44px, no `tablist`) switching `?version=` in one click. `SelectionRegion` composes View Product's `OfferingDetail`/`SpecificationsPanel`/`PricesPanel` directly (Inv. #29, one-directional import; guardrail 11 unchanged and green) inside the D4 grid; no prop added to those read-only components. D1 resolution is a pure `resolveSelectedVersion(versions, requestedVersionId)` (ACTIVE→open→highest primary; foreign/absent `?version=` → primary; unknown `?family=` → null → empty-selection state, never a 404). I6 empty states: no family selected, family-matches-no-row ("That product no longer exists."), and the DRAFT-with-no-prices "at least one price is required before this version can be submitted for testing" muted hint (pm42 acts on it).

**D5 caching decision (recorded): DO NOT cache `findFamilyPage`; the checklist's illustrative "selection = 4" is a cached-design figure and is deliberately not met.** The platform/module architecture mandates no cache layer (architecture.md §1, prodmgmt-architecture §1 — this repo has zero `unstable_cache`/`use cache`/`cacheComponents`); `list-families.ts` (where such a wrap would live) is outside pm40's boundary; the shipped mutation actions revalidate via `revalidatePath` only, so a `next/cache` entry would need `revalidateTag` (a write-unit concern) and would otherwise ship a stale families list; and the query-budget integration test renders by direct function call with no Next incremental-cache context, so Next Data Cache is not even demonstrable there. **Resulting query budget (uncached, asserted): no selection = 2; selection = 6; version switch = 6** — uncached the 2 families statements are always paid, so 2 (findFamilyPage page+count) + 1 (findFamilyVersions) + 3 (getOfferingDetail: detail + specs + prices) = 6. pm45 D4's post-mutation accounting inherits the same uncached 6.

**Verified (2026-09-19):** `tsc --noEmit`, `eslint`, `prettier --check` clean on every changed/new file; full DB-free suite green — new `tests/services/resolve-selected-version.test.ts` (7 cases: all five D1 branches + the ACTIVE/open/highest primary sub-rules), `tests/app/manage-products-selection.test.tsx` (5 cases: primary-on-select, version switch, foreign-version fallback, unknown-family empty state, no-selection) and `tests/components/selection-region.test.tsx` (5 cases, see post-review below); `manage-products-query-budget.integration.test.ts` green against the disposable test DB (`.env.test`, port 5434). The query-budget test's statement counter normalises identifier quotes so findFamilyPage's raw SQL and the Drizzle-builder reads count the same; per-test timeout raised to 30s (the page's cold dynamic import now compiles the whole panel tree, and a timed-out first render leaks in-flight queries into the next case).

**pm40 post-review fixes (xhigh multi-agent review, 2026-09-19).** Applied: (a) **focus-ring a11y (top finding)** — the version bar's `overflow-x-auto` forces `overflow-y: auto`, which clipped the 4px `--focus-ring`, and the right-edge mask faded a focused off-screen link; the scroll `<nav>` now carries `p-1.5` (ring fits inside the padding box) + `scroll-pr-8` (keyboard focus scrolls the link clear of the 2rem fade), satisfying the ui-context §7 keyboard contract. (b) **`findFamilyVersions` tie-breaker** — added `asc(productOfferingId)` after `desc(version)` to match findList/findFamilyPage's stable-ordering convention (deterministic even if the per-family version-uniqueness invariant is ever broken; feeds `resolveSelectedVersion`'s "highest" fallback) — a deliberate, zero-risk deviation from the spec's literal `ORDER BY version DESC`. (c) **primary-rule drift guard** — the ACTIVE→open→highest rule lives in both SQL (`findFamilyPage` ROW*NUMBER CASE) and TS (`resolveSelectedVersion`); added reciprocal "keep in sync" cross-reference comments in both. (d) **removed the two `params.family as string` assertions** in the page by narrowing a `const family` truthiness (compiler-checked, no cast). (e) **query-budget test hardened** — now captures bind params so the switch case asserts `getOfferingDetail` targets the requested version (`allBindParams()` contains `alphaDraftId`) and the plain-selection case asserts it does **not** (primary/ACTIVE chosen), removing the count-only over-claim; added a 4th case (well-formed unknown `?family=` = **3** statements); corrected the misleading counting comment (the regex matches offering \_column* refs too, but per-statement counting makes that harmless — the `\b` only keeps the price child table out of the offering bucket). (f) **new `selection-region.test.tsx`** locks the DRAFT-with-no-prices submit hint (shown only for DRAFT + zero prices, absent for ACTIVE or when prices exist) and the two empty states. **Spec update (user-requested):** pm40 spec D5 now records the uncached decision explicitly and the verification checklist's illustrative "selection = 4" was corrected to the chosen design (no selection = 2; selection = 6; switch = 6; unknown family = 3). **Duplication de-dup (post-review, user-approved cross-boundary edit).** Checked pm41–pm45 (build-plan, no separate specs yet): (a) the **href-builder duplication** (family-table `buildHref` vs version-bar `buildVersionHref`) is touched by **no** upcoming unit → de-duped now into one `components/products/manage/manage-products-href.ts` (`MANAGE_PRODUCTS_PATH` + `buildManageProductsHref`), consumed by both, URLs byte-identical to before (same keys, order, drop rules); this required editing pm39's `family-table.tsx` (out of pm40's original boundary), done with the user's explicit approval. (b) The **SelectionRegion/OfferingDetailRegion composition duplication** is **owned by pm41**, which reworks `SelectionRegion` to route a DRAFT version to editable `ManageSpecificationsPanel`/`ManagePricesPanel` — deliberately diverging Manage's region from View's always-read-only one (Inv. #29); de-duping now would fight that intended fork and require editing the frozen-View component, so it is left for pm41 to own (not a standing debt). **Left as-is (low-severity):** the narrow bar-vs-panel disagreement under a concurrent hard-delete race (bar lists the selected version while the panel says "no longer exists") — showing the bar so the user can pick a sibling version is defensible, and pm40 has no write path. Full DB-free suite **3195/3195** (321 files) after all fixes + the de-dup; query-budget integration **4/4**.

**pm41 (Inline editing of a DRAFT version): IN PROGRESS — implemented + verified (2026-09-20).** Editable-panels slice (`components/products/manage/**` + the page/selection wiring). Boundary held: no new service, no new repository method, no schema, no new action (`PRODUCT_ACTION_FILES` unchanged from pm38 — the ten files, guardrail-frozen; a new guardrail asserts the two dialogs are gone from `manage/`). Delivers the two editable panel variants (`ManageSpecificationsPanel`/`ManagePricesPanel`, **server components**, one component per panel with a `canEdit` boolean, D1/D5) that render View Product's read-only `SpecificationsPanel`/`PricesPanel` untouched when `!canEdit` (Inv. #29, one-directional import) and hand a DRAFT version to a client editor (`EditableSpecifications`/`EditablePrices`) otherwise; the editors give each row an inline edit affordance + an "Add" control, one row editable at a time with a dirty-discard prompt (D2), explicit Save/Cancel (no auto-save on blur), the D2 keyboard contract (`Esc` cancels; `Cmd`/`Ctrl+Enter` saves a multi-field row; a bare `Enter` inside a field never submits; focus returns to the row's Edit trigger via a ref, not a setState-in-effect), a small inline delete confirm (not an `AlertDialog`, §2.8), and the I3 typed-result→UI mapping. `VersionActionHeader` (D4) surfaces the offering-level Edit affordance in the selected version's header — DRAFT edits in place, ACTIVE branches via the existing `updateOfferingAction` and navigates to the new draft's `?version=`, with the "creates a new draft" banner inside the reused `OfferingForm`. Page wiring computes `canEdit` from a new total `Record<LifecycleStatus, boolean>` (`PANEL_EDITABLE_BY_STATUS` in `types/product.ts`, never an inline `=== 'DRAFT'`, I4) and passes it + the carried searchParams into `SelectionRegion`. `SpecificationsDialog`/`AddPriceDialog` + their tests deleted (I5); `PriceForm` extended with `formId`/`defaultValues`/`onDirtyChange` + an exported `priceCardToFormValues` mapper, and `SpecificationForm` with `onDirtyChange`, so both serve the inline editor and the create flows (D6).

**pm41 decisions/deviations (flagged).** (1) **A new total Record, not "the same one pm37 introduced."** I4's wording implies reusing a pm37 editability map; pm37 only shipped `LIFECYCLE_BADGE_VARIANTS`, so `PANEL_EDITABLE_BY_STATUS` is added (total `Record<LifecycleStatus, boolean>`, DRAFT-only true) — honours the intent (compile-error on a new status, never an inline string compare). (2) **The spec actions return no `OFFERING_NOT_DRAFT`.** `create/update/deleteSpecificationAction` predate the DRAFT-only model (pm14/pm21: branch-on-ACTIVE, reject RETIRED); a version that races DRAFT→TESTING mid-edit is refused by the pm36 §3.5 DB trigger and surfaces as `SERVER_ERROR` (row keeps the user's input, I3's SERVER_ERROR row), while a RETIRED race surfaces as `OFFERING_RETIRED` (both map to the "no longer a draft — reload" banner). The price actions (pm38) do return `OFFERING_NOT_DRAFT`, handled per I3. No service change was made — that would exceed the boundary. (3) **`DUPLICATE_START`/`BACKDATED_START_TOO_FAR` render as an inline message in the editor, not literally attached to the start-date `<input>`.** The reused `PriceForm` owns its own field state; injecting a server error onto its field would require a wider `PriceForm` prop. The inline message stays in edit mode with the input preserved (I3 intent). (4) **`VersionActionHeader` scope.** pm41 builds only the offering-level Edit affordance (DRAFT/ACTIVE) required by D4 + the I6 ACTIVE test; the remaining version-header actions (Submit for testing, Back to draft, Activate, Stop selling, Retire, Discard) are **not** built — their services/actions do not exist yet (pm42–pm44). The header renders nothing for TESTING/OBSOLETE/RETIRED. (5) **Client-surface shape.** D5 names "the editable row and the add-row form" as the client leaves; implemented as one client editor component per panel (`EditableSpecifications`/`EditablePrices`) that owns the rows + add + one-at-a-time coordination (a single `activeId` is the simplest correct way to enforce "one at a time" and is directly testable) — the two `manage-*-panel.tsx` files stay server components (no `'use client'`), satisfying "panels remain server components."

**pm41 verified (2026-09-20):** `tsc --noEmit` clean (incl. tests; `exactOptionalPropertyTypes` satisfied by making the header's carried params required and defaulting them in `SelectionRegion`); `eslint` clean (the focus-return effect mutates a ref, not state, to satisfy `react-hooks/set-state-in-effect`); `prettier --check` clean on every changed/new file. New tests green — `manage-specifications-panel` (6), `manage-prices-panel` (6), `manage-products-editing` (3, real page + real SelectionRegion, DRAFT controls / TESTING plain-text-no-inputs / ACTIVE header Edit + branch banner); existing `selection-region`, `manage-products-selection`, `manage-products-page`, `price-form`, `specification-form`, `create-offering-dialog` all still green (44 across the seven pm40/pm41 files, 23 across the form files, DB-free suite run with `--env-file=.env` per `[[vitest-needs-env-file]]`); the re-scoped guardrail sweep green incl. the new deleted-dialog assertion and the unchanged 10-file `PRODUCT_ACTION_FILES` freeze. **Not exercised here (later units):** the transition services/dialogs (pm42–pm44) and their integration tests; a live DB is not touched (pm41 is UI-only).

**pm41 post-review fixes (xhigh multi-agent review, 2026-09-20; all user-approved).** Applied 15 findings. **Correctness:** (#1, cross-boundary, user-approved breach of pm41's "no service change") the 3-day backdating tolerance in `services/product/update-price.ts` now applies **only when the start date actually changes** — a re-save of a price's own already-accepted (often months-old) start is not blocked, so "fix a wrong price on a draft branched from ACTIVE" (the DoD) is reachable; the check moved inside the transaction (the stored start is only known under the parent lock) and rolls the update back via a sentinel throw when a _moved_ start is >3 days past; the client `price-form.tsx` schema became a `makePriceFormSchema(baselineStartDate)` factory that gates the client-side backdating issue on a changed date. (#2) both editors now read `result.branched` on save/delete success and **navigate to the new draft's `?version=`** (mirroring `VersionActionHeader`, overview flow step 5) instead of silently refreshing in place — a mid-edit `activateOffering` race no longer strands the edit on a hidden branched draft; `familyId`/`q`/`status`/`page` are threaded page→SelectionRegion→panels→editors for the href. (#4) `closeEditor` clears the `pending` discard state (no more stale banner opening the wrong editor). (#5) the delete trigger routes through the dirty guard and `applyActivate`/`closeEditor` clear `deleting` (a dirty editor and a delete-confirm can no longer coexist). (#6) the inline delete paths set `pendingFocusRef` (+ the focus effect keys on `[active, deleting]`) so a keyboard delete returns focus to the row's Edit trigger / the Add control rather than `document.body`. (#7) server field errors are attached to their inputs via a new `serverFieldErrors` prop on both forms (RHF `setError` → `aria-invalid` + `FieldError`; `DUPLICATE_START`/`BACKDATED_START_TOO_FAR` map to a start-date field error, spec I3); unmatched keys render in a residual list so none are dropped. (#8) the effectivity tag + current/superseded accent were extracted to a shared `components/products/price-effectivity.tsx` (`PriceEffectivityTag` + `effectivityAccentClass`) that both the read-only `PricesPanel` (frozen-View edit, user-approved) and the editable price rows import — the DRAFT editor no longer drops the "Starts …/Superseded"/cyan-border signal (D1/I2). (#10) the editable spec read-only row now shows the id, default value and characteristic key/value pairs (not just a count, spec I1). **a11y/consistency:** (#11) Edit/Delete `aria-label`s carry a disambiguator (spec id / price start date) so same-named rows have unique accessible names; (#12) the new controls carry the `@media(pointer:coarse)` 44px hit-target bump (ui-context §7). **Cleanup:** (#14) the dead `mode` prop removed from `SpecificationForm`; (#15) the duplicated editor shell extracted to one `InlineRowEditor` (keyboard contract + Save/Cancel footer + general-error, shared by both editors). #3 (DRAFT→TESTING/OBSOLETE race → generic SERVER_ERROR rather than the reload banner) is **not fixed** — it is unreachable until pm42 ships those transitions, and its clean fix is a service-layer typed `OFFERING_NOT_DRAFT` (as the price services already return); left for pm42, comment corrected to stop over-claiming the reload-banner mapping. **Verified:** `tsc`/`eslint`/`prettier` clean; DB-free suite for the touched files 86/86 (incl. new discard-prompt, navigate-on-branch, field-level-start-date, edited-amount-assertion cases; `prices-panel` still green through the shared-module refactor); `product-price-writes.integration` 10/10 against the disposable DB incl. two new #1 regression cases (unchanged-old-start passes; moved-back start rejected + rolled back).

**pm42 (Release path: submit for testing, back to draft, activate): IN PROGRESS — implemented + verified (2026-09-20).** Transition slice (`db/repositories/product-offering.ts` status writes, `services/product/**`, `actions/product/**`, two dialogs, the audit ripple). Moves a version `DRAFT → TESTING → ACTIVE` plus `TESTING → DRAFT`, relocating the three release preconditions (≥ 1 price, ≥ 1 spec, every mandatory spec resolved) from activation to the `submitForTesting` step (D2), and making activation supersede the family's previous `ACTIVE` to `OBSOLETE` (was RETIRED) in the same transaction (D3). Delivers: four narrow status writers on `product-offering.ts` (`markTesting`/`markDraft`/`markActive`/`markObsolete`, each `(tx, offeringId, actorId)`, pinning the expected predecessor in the WHERE and stamping `last_modified`/`last_edited_by`, §1.15); the rewritten `activateOffering` repo method (supersede-then-flip via `markObsolete`→`markActive`, index-ordered, `findActiveInFamily`'s family-wide `FOR UPDATE` kept as the serialization); `submit-for-testing.ts` (reuses activate's old precondition code verbatim, then deleted from activate) + `return-to-draft.ts` (D4 — not a rollback) + the rewritten `activate-offering.ts` (re-checks only status + active slot, `OFFERING_NOT_DRAFT`→`OFFERING_NOT_TESTING`); the two new actions + `activate-offering.action.ts`'s updated result union; `validation/product/transition.schema.ts` (id-less `{reason}`, shared by submit + return; activate keeps its identical `activate-offering.schema.ts`); the new `SubmitForTestingDialog` + revised `ActivateOfferingDialog` (new copy naming `v<n>` + the OBSOLETE supersession, precondition alerts removed, `offeringVersion` prop added, `onSuperseded` dropped) + the `VersionActionHeader` wired to a new total `VERSION_HEADER_ACTIONS_BY_STATUS` Record (Submit on DRAFT; Back to draft + Activate on TESTING; Edit on DRAFT/ACTIVE; nothing on OBSOLETE/RETIRED, I6); the live mandatory-unresolved-spec hint in `EditableSpecifications` (D6). Audit ripple: two new `AUDIT_EVENT_TYPES` (`PRODUCT_OFFERING_SUBMITTED_FOR_TESTING`/`_RETURNED_TO_DRAFT`, both Change) + `AUDIT_EVENT_CATEGORY_MAP` + `audit-log-filters` count 75→77; `PRODUCT_OFFERING_SUPERSEDED.afterData.lifecycleStatus` now `OBSOLETE`. Guardrail: `PRODUCT_ACTION_FILES` + `PRODUCT_WRITE_SERVICE_FILES` gain the two files each.

**pm42 spec deviations (flagged).** (1) **Repository writers take `(tx, offeringId, actorId)`, not I1's literal `(tx, offeringId)`.** I1 also mandates "each writer updates `last_edited_by`" — impossible without the actor, so `actorId` is passed. (2) **`activateOffering` stays a composite repo method (D3), not decomposed into a service-level `markObsolete`+`markActive` call chain (I4's literal wording).** The supersede→flip ordering is index-critical (`product_offering_one_active_per_family` trips on the reverse order), so it is kept as one atomic method that _internally_ composes the two narrow writers — satisfying I1 (all three writers exist and are used) and I4 (obsolete-then-active, in order) while honouring D3 ("`findActiveInFamily`'s `.for('update')` stays; only the superseded status changes RETIRED→OBSOLETE"). The composite gained an `actorId` param; its sole caller is the activate service. (3) **No separate `pg_advisory_xact_lock` in activate.** D3/I4 mention "the advisory lock" but activation never had one — the pm16 serialization is `findActiveInFamily`'s family-wide `FOR UPDATE` (the advisory lock guards _branching_ only, in `resolveNextVersion`). That family-wide lock is the "lock" kept; no advisory lock was added. (4) **I7's "two near-simultaneous activations of sibling TESTING versions" is structurally impossible** under pm36's `product_offering_one_open_per_family` index (TESTING counts as open, so two TESTING siblings in one family cannot exist — same class of contradiction the pm36/pm37 flags already recorded for the old two-open-DRAFT race). The concurrency case instead activates the _same_ TESTING version twice: exactly one wins, the loser returns `OFFERING_NOT_TESTING`, the family never holds two ACTIVE. (5) **`submit-for-testing`/`return-to-draft` share `transition.schema.ts`; activate keeps its own `activate-offering.schema.ts`** (byte-identical shape) so its shipped tests are untouched — consolidating all three into the shared schema is deferred.

**pm42 verified (2026-09-20):** `tsc --noEmit`, `eslint`, `prettier --check` clean on every changed/new file. DB-free suite green — new `submit-for-testing.action` (8) / `return-to-draft.action` (7) / `submit-for-testing-dialog` (5) tests, the rewritten `activate-offering.action` (updated codes) + `activate-offering-dialog` (new copy/behaviour, no `onSuperseded`), `audit-log-filters` (77), and the existing `manage-products-editing`/`selection-region`/panels all still green (full DB-free run 3217 pass; the only 2 "failures" were load-induced 10 s timeouts on unrelated fs-scan guardrails — `route-manifest`, `customer-module-boundaries` — both green in isolation). Against the disposable test DB (`.env.test`, port 5434): new `tests/db/product-release-path.integration.test.ts` (12 cases: the three legal transitions + one-ACTIVE-on-activate, every illegal ordered pair's typed code, `OFFERING_NOT_FOUND` on each, the three submit-precondition refusals, supersession = one ACTIVE + one OBSOLETE + two audit rows with `afterData.lifecycleStatus='OBSOLETE'`, and the concurrency case) and the re-scoped guardrail 16 in `ship-gate-guardrails.integration.test.ts` (superseded → OBSOLETE, grandfathered prices byte-identical; its `newActiveOfferingWithResolvedSpec` fixture reshaped to insert-while-DRAFT-then-flip so pm36's trigger no longer rejects its child inserts) green; adjacent `product-price-writes`/`product-family-guards` unaffected (30/30). **Not touched:** `product-repositories.integration.test.ts` stays the hands-off, option-C co-land red file (its own two-open-DRAFT race is obsolete under Inv. #27 and its fixtures still insert children onto ACTIVE parents — unchanged per the recorded directive). pm43/pm44 owe the remaining transitions (obsolete/retire/delete) and their dialogs/actions/guardrails 26–30.

**pm43 (Withdrawal path: stop selling and retire): IN PROGRESS — implemented + verified (2026-09-20).** Withdrawal slice (`db/repositories/product-offering.ts`, one new locked read on `db/repositories/inventory/product-inventory.repository.ts`, `services/product/**`, `actions/product/**`, two dialogs, page count read, audit ripple). Delivers: `productInventoryRepository.countLiveForOfferingForUpdate` (the single home of the live-subscription gate predicate `status <> 'TERMINATED' OR (end_date IS NULL OR end_date >= current_date)`, D2, locked `FOR UPDATE`, read-only — the inventory write surface is untouched, Inv. #18); the re-purposed repo `retireOffering` (now OBSOLETE → RETIRED only, stamps `last_modified`/`last_edited_by`, WHERE-OBSOLETE backstop) reusing pm42's `markObsolete` for stop-selling; `services/product/obsolete-offering.ts` (ACTIVE → OBSOLETE, `OFFERING_NOT_ACTIVE`, `PRODUCT_OFFERING_OBSOLETED` audit) + the rewritten `services/product/retire-offering.ts` (OBSOLETE → RETIRED, `OFFERING_NOT_OBSOLETE`, the gate → `RETIRE_BLOCKED_BY_SUBSCRIPTIONS{liveCount}`, `PRODUCT_OFFERING_RETIRED` audit recording the observed count; the old dual-purpose Retire/Discard is gone — discard is pm44) + `services/product/get-live-subscription-count.ts` (page display read); `obsolete-offering.action.ts` (new, `products:DELETE`) + the rewritten `retire-offering.action.ts` (`DELETE`, `liveCount` passthrough, no more `eventType`/`DISCARDED`); the new `ObsoleteOfferingDialog` (danger `AlertDialog`, "Stop selling") + rewritten `RetireOfferingDialog` (OBSOLETE-only, blocked-state replaces the confirm button when `liveCount > 0`, copy verbatim from ui-context §7); `VersionActionHeader` gains Stop selling (ACTIVE) + Retire (OBSOLETE) via the extended total `VERSION_HEADER_ACTIONS_BY_STATUS` Record; the page fetches the live count only for an OBSOLETE selection (I7) and threads it page → SelectionRegion → header → dialog. Audit ripple: `PRODUCT_OFFERING_OBSOLETED` added (Removal) + `audit-log-filters` count 77→78. Guardrail: `PRODUCT_ACTION_FILES` + `PRODUCT_WRITE_SERVICE_FILES` gain the obsolete files; the guard authz matrix gains the obsolete action (both stop-sell and retire proven `DELETE`-only).

**pm43 spec deviations (flagged).** (1) **The page's display-count read (`get-live-subscription-count.ts`) calls the inventory repo's LOCKED finder with `db`, not a `services/inventory/**`display service.** code-standards §1.14 defaults a display read to the other module's *service*, but pm43's boundary permits exactly ONE new inventory read (the locked finder, I1) and D2 requires the predicate to live in exactly one place — so a separate unlocked/service count would restate it. The`FOR UPDATE`holds only for the one autocommit statement; the authoritative gate stays`retireOffering`'s own locked re-check (D4). (2) **I9's "subscription inserted while retire holds its locks" race is replaced by two deterministic proofs:** two concurrent retires (the offering-row `FOR UPDATE`serializes them — exactly one wins, the loser returns`OFFERING_NOT_OBSOLETE`) and an orphan-prevention case (`createOrder`is refused on an OBSOLETE offering, so no new subscription can pin a withdrawn version). The literal direct-SQL-insert race is both non-deterministic and *not* the real protection — a`FOR UPDATE` over existing rows cannot block a brand-new insert; the actual closure is that ordering requires an ACTIVE offering, which a to-be-retired OBSOLETE version is not. (3) **I9's "bill run … identical charges" is proven at the price-resolution layer** (getOrderDetail prices byte-identical before obsolete / after obsolete / after retire, Inv. #17) rather than by driving a full bill run — the end-to-end bill-run grandfathering already lives in ship-gate guardrail 16 (OBSOLETE prices byte-identical) + bm29. (4) **`obsoleteOffering`uses the shared`transition.schema.ts`; `retireOffering`keeps`retire-offering.schema.ts`** (identical shape) — same split pm42 used. (5) **`markObsolete`already existed** (pm42 added it for supersession, WHERE-ACTIVE) — pm43 reused it for stop-selling rather than re-adding it, so I2's "add`markObsolete`" was already satisfied and only `retireOffering` was re-purposed.

**pm43 verified (2026-09-20):** `tsc --noEmit`, `eslint`, `prettier --check` clean on every changed/new file. Full DB-free suite **3230/3230** (327 files) — new obsolete/retire action + dialog tests, `audit-log-filters` (78), the inventory/ordering export guardrails (the `count`-prefixed finder is not a mutation, so Inv. #18's write-surface assertion passes untouched), and `manage-products-editing`/`selection-region` still green. Against the disposable test DB (`.env.test`, port 5434): new `tests/db/product-withdrawal-path.integration.test.ts` (13 cases: ACTIVE→OBSOLETE + every non-ACTIVE refusal, every non-OBSOLETE retire refusal, retire at zero recording count 0, blocked by ACTIVE/SUSPENDED/TERMINATED-today/TERMINATED-future subs, allowed once TERMINATED-yesterday, `liveCount` matches 2, price stability before/after obsolete/after retire, two-concurrent-retires race, createOrder-refused-on-OBSOLETE orphan prevention); the guard authz matrix (**106**, incl. the obsolete/retire `DELETE`-only proofs); and the full product integration set together (`product-withdrawal-path`/`product-release-path`/`ship-gate-guardrails`/`product-price-writes`/`product-family-guards` → **58/58**). pm44 owns the last transition — discard (hard delete of a never-ACTIVE DRAFT/TESTING version → `PRODUCT_OFFERING_DELETED`, replacing the now-unused `PRODUCT_OFFERING_DISCARDED`) — plus `DeleteVersionDialog` and guardrails 26/30.

**pm44 (Discard: hard delete of a never-released version): IN PROGRESS — implemented + verified (2026-09-20).** Discard slice (`db/repositories/product-offering.ts`, `services/product/delete-offering.ts`, `actions/product/delete-offering.action.ts`, `DeleteVersionDialog`, removal of the old discard path). Delivers: repo `deleteOffering` (deletes the parent, lets pm35's `ON DELETE cascade` remove children, `RETURNING` the audit fields, no WHERE-status — the service owns the guard) + `countSpecificationsForOffering`/`countPricesForOffering`/`countFamilyVersions` reads; `services/product/delete-offering.ts` (locked status read → refuse unless DRAFT/TESTING with `OFFERING_NOT_DELETABLE{lifecycleStatus}` → count children → delete parent → one `PRODUCT_OFFERING_DELETED` audit with the D4 payload; returns `familyId`/`familyRemains`/counts); `delete-offering.action.ts` (`products:DELETE`); `DeleteVersionDialog` (danger `AlertDialog`, count-stating copy verbatim from ui-context §7, navigates off the deleted `?version=` to the family's remaining primary version or the bare list, I3); `VersionActionHeader` gains Discard on DRAFT/TESTING via the extended `VERSION_HEADER_ACTIONS_BY_STATUS` Record. Removal of the old path: `PRODUCT_OFFERING_DISCARDED` dropped from `AUDIT_EVENT_TYPES` + `AUDIT_EVENT_CATEGORY_MAP`, `PRODUCT_OFFERING_DELETED` added (Removal); `audit-log-filters` count unchanged at 78 (−DISCARDED +DELETED). Guardrail: `PRODUCT_ACTION_FILES` + `PRODUCT_WRITE_SERVICE_FILES` + the offering repo-exports allow-list (`deleteOffering`) + the guard authz matrix (delete proven `DELETE`-only).

**pm44 spec deviations (flagged).** (1) **Parent-first cascade, NOT explicit child-delete-first — supersedes D2/I1/I6.** pm36's trigger (verified in `0040`, lines 65–75) rejects an explicit child `DELETE` while a `TESTING` parent is still present (RAISE when the OLD parent exists and is not DRAFT), exempting only the cascade path (parent row already gone → `NOT FOUND` passes). code-standards §6.8 / architecture §3.5 mandate parent-first cascade as "the only path that works for a TESTING parent," so `deleteOffering` deletes only the parent and lets `ON DELETE cascade` remove children; the audit counts are captured by counting children **just before** the parent delete (under its `FOR UPDATE` lock), not from explicit child-delete row counts. I1's "narrow child deletes returning row counts" becomes count helpers + the parent delete; I6's path-assertion is reframed to prove the payload counts are exact **and** the children are gone via cascade (still fails if the counts were dropped). Architecture/code-standards outrank the spec supplement (precedence), and D2's own "explicit delete … with the parent still present and DRAFT" wording only ever held for DRAFT. (2) **D5: `PRODUCT_OFFERING_DISCARDED` kept as a display-only legacy entry.** `toAuditLogRow` maps a row's category from the raw DB `event_type`, so removing DISCARDED from `AUDIT_EVENT_CATEGORY_MAP` alone would yield `category: undefined` for historical rows (blank badge, no swatch). Decision from the code: removed DISCARDED from `AUDIT_EVENT_TYPES` **and** the category map (no longer written, no longer a filter option), and added `LEGACY_AUDIT_EVENT_CATEGORY_MAP` (keyed by `string`, not the union) that `toAuditLogRow` falls back to — historical DISCARDED rows still render as "Removal" (proven by the D5 integration case). (3) **Child-table reads live on the offering repo** (`countSpecificationsForOffering`/`countPricesForOffering`), per I1's boundary — keeping the price repo's three-write cap (Inv. #1) and the spec repo's single-delete-call-site guardrail intact. (4) delete uses the shared `transition.schema.ts` (reason), consistent with pm42/pm43.

**pm44 verified (2026-09-20):** `tsc --noEmit`, `eslint`, `prettier --check` clean on every changed/new file. Full DB-free suite **3243/3244** (the one failure the same load-induced 10 s timeout on the `route-manifest` fs-scan guardrail — green in isolation) — new `delete-offering.action` + `delete-version-dialog` tests, `audit-log-filters` (78, DISCARDED→DELETED swap), the repo-exports + module-boundaries guardrails (with `deleteOffering`), `manage-products-editing`/`selection-region` still green. Against the disposable test DB (`.env.test`, port 5434): new `tests/db/product-delete-offering.integration.test.ts` (7 cases: delete DRAFT with 2 specs + 2 prices removes exactly those five rows with the right audit counts; TESTING identical; ACTIVE/OBSOLETE/RETIRED → `OFFERING_NOT_DELETABLE` + observed status, `OFFERING_NOT_FOUND` for a bogus id, nothing removed; a direct-SQL price delete on an ACTIVE version still trigger-refused; deleting a branch leaves the root + family graph intact and frees the open-version index for a new branch; deleting the only version leaves no orphans; a pre-existing DISCARDED audit row still resolves category "Removal", D5); the guard authz matrix (**108**, incl. the delete `DELETE`-only proof); and the product + audit-log integration set together (`product-delete-offering`/`product-withdrawal-path`/`product-release-path`/`ship-gate-guardrails`/`product-family-guards`/`audit-log-repository` → **65/65**). The catalog-lifecycle transitions (pm42–pm44) are now complete; the V5 status-literal sweep + seed/doc reconciliation (guardrail 30, U5/pm45) remains.

**pm45 (Ship gate: guardrails, authz matrix, docs): IN PROGRESS — implemented + verified (2026-09-20).** Final Part-3 unit; boundary `tests/**` + the documentation set (the only production code it may touch is a defect it finds, called out separately). The gate asserts, it does not re-implement (D1): pm36–pm44 each landed their own behavioural tests, so pm45 adds only the guardrails that span units and the doc reconciliation. Delivers: I1 guardrail 23 (`tests/guardrails/product-lifecycle-transitions.test.ts`, new — DB-free structural: the 25 ordered-pair transition table mapped to its sole owning service or proven caller-less; no `setLifecycleStatus`/`set*Status` helper and no action taking a status parameter; the fourteen-file `PRODUCT_ACTION_FILES` set; plus the page-guards-before-parsing structural proof for the deep-link clause); I2 query-budget extension (`tests/app/manage-products-query-budget.integration.test.ts` — the OBSOLETE-selection `+1` live-count case and the post-mutation case, both with per-table statement counts so an N+1 fails on a coincidental total, D4); I3 guardrail 30 (`tests/guardrails/status-literal-sweep.test.ts` + `status-literal-allowlist.ts`, new — walks production source for bare `'RETIRED'`/`'OBSOLETE'` literals outside `db/schema/product.ts`+`types/product.ts`, asserts ⊆ the justified allow-list, warns on `workflow-management/**` findings, never asserts them); I4 authz matrix (`tests/auth/guard.integration.test.ts` — `updatePrice`/`deletePrice` added to the EDIT set so "every content write reachable by EDIT-only" is literal; the five withdrawal/transition actions × the split were already proven by pm42–44 and are referenced, not duplicated); I5 the success-criteria evidence table appended to `prodmgmt-update-overview.md`; I6 the §9 doc amendments landed + both Appendix A tables emptied (grep-verified the code they described is gone); I7 the hand-off register (H1–H3, O2, O3, plus the `workflow-management/**` sweep findings) in the build plan.

**pm45 decisions/deviations (flagged).** (1) **Guardrail 23 is a DB-free structural test, not a fresh 25-pair DB run.** D1 forbids re-implementing what pm42–44's integration suites already prove per-pair; so the "each pair maps to a service or is caller-less" claim is discharged structurally — the set of services referencing a repository status-writer (`productOfferingRepository.mark*`/`activateOffering`/`retireOffering`/`deleteOffering`, comment-stripped) is asserted to be exactly the six, so a rogue seventh status-writer (an illegal shortcut) fails; each legal edge names the integration suite that behaviourally proves it. (2) **The I4 deep-link clause ("`?family=`/`?version=` grant nothing") lives in the guardrail-23 file, not the authz DB test** — the DB authz test can't render the page, so it is a source-order assertion that `manage-products/page.tsx` calls `requirePermission(PRODUCTS, EDIT)` before it reads searchParams. (3) **I4 added only `updatePrice`/`deletePrice` to the authz matrix** (the two EDIT content writes the shipped matrix was missing); the five withdrawal/transition actions × the EDIT/DELETE split were already proven by pm42–44 and are referenced (D1). (4) **Guardrail 30's sweep scans production source only (excludes `tests/**`), strips comments, and its allow-list enumerates 15 `(file, literal)` pairs** — including the module's own `db/repositories/product-offering.ts` and `services/product/**` writers/guards, since D3's exclusion is the two definition files only (`db/schema/product.ts`, `types/product.ts`); `workflow-management/**` is scanned warn-only and found **zero** uppercase references (case-sensitive; only lowercase "retired" prose in two files), carried to the hand-off register. (5) **No production code changed** — the boundary held to `tests/**` + docs; no defect requiring a fix was found, so the PR lists no separate production fix.

**Verified (2026-09-20):** `tsc --noEmit`, `eslint`, `prettier --check` clean on every new/changed file (tests **and** the seven edited docs). DB-free: the two new guardrails green (guardrail 23 = 8 cases, guardrail 30 sweep = 4 cases) and the full guardrail suite unbroken (20 files / 156); full DB-free suite **3257/3257** (331 files, no flaky timeout this run). Against the disposable test DB (`.env.test`, port 5434): `manage-products-query-budget.integration` **6/6** (the four prior cases + OBSOLETE `+1` live-count = 6 product + 1 inventory + post-mutation = 6, per-table counts pinned) and `guard.integration` **116/116** (108 → 116: `updatePrice`/`deletePrice` reach an EDIT-only principal and are refused a no-grant one). Both Appendix A tables are empty (grep-confirmed the superseded rules are all in force in `main`); the `0006` D11 in-place exception is recorded closed in `db/migrations/README.md` and workflow-rules §6.2; general `architecture.md` Inv. #18 and `prodmgmt-architecture.md` Inv. #1 now record the G1 approval (were stale "pending design review"). SAST/DAST remain outside this tooling's reach and are unverified here (the standing three-ship-gate caveat). **Part 3 (pm35–pm45) is code-complete and unit/integration/type/lint-verified; the CI SAST/DAST gate and the merge to `main` remain outstanding.**

**pm45 post-review fixes (xhigh multi-agent review, 2026-09-20).** Applied 10 findings against the new/changed test code (all test-only; no production change). **Correctness:** (1, top) guardrail 30's `workflow-management/**` warn-scan walked the git-ignored runtime output under `dev/logs` (~1,500 `.jsonl` records the engine writes) and timed out (>10 s) on any dev box that had run the engine — green only on a fresh CI checkout (false confidence); it now enumerates COMMITTED flow source via `git ls-files workflow-management` (45 tracked files), so runtime output is inherently excluded. (2) guardrail 23's "no action takes a status parameter" check only recognised five hard-coded param names — broadened to any `: LifecycleStatus` parameter (a `desiredStatus: LifecycleStatus` param would have evaded it); the arrow-function form is left to product-module-boundaries, which already enforces the `export async function` shape. (3) the query-budget post-mutation case now asserts `allBindParams()` contains the draft id, so a silent `?version=` fallback to the ACTIVE primary (identical 4/1/1 counts) can no longer pass it. (4) guardrail 23's discard test dropped a self-referential `legalFrom` assertion + bare `toContain("OFFERING_NOT_DELETABLE")` (which an inverted `=== "RETIRED"` guard still satisfied) for a stripped-source check that the service gates on both `"DRAFT"` and `"TESTING"` (literals that appear nowhere else in it). **Cleanup/altitude:** (5) documented that guardrail 23's writer-scan rests on the §1.1 "writes flow through the repository" invariant (a raw `tx.update().set({lifecycleStatus})` bypass is caught by the repository/module-boundary guardrails + the §3.5 trigger, layered defence, not this test); (6) extracted the duplicated `stripComments` into `tests/helpers/strip-code-comments.ts` (one home, one place to note its regex-literal limitation); (7) removed guardrail 23's forked fourteen-file `PRODUCT_ACTION_FILES` list — product-module-boundaries owns that freeze, and forking it invited drift; (8) replaced the warn-test's `expect(true).toBe(true)` tautology with `expect(Array.isArray(hits))`; (9) added `scripts/` to the sweep's SCAN_ROOTS (was an uncovered production-adjacent dir; no new literals); (10) the deep-link guard-order check now anchors on the schema `.parse(` (stable) rather than the raw `await searchParams` expression. **Left as-is with reason:** the warn-only WM scan never asserting (spec D3, read-only dir); the OBSOLETE `inventory===1` counting statements not subscriptions (correct for a query-BUDGET test; the predicate's value is proven in `product-withdrawal-path`); the pre-existing unqualified `product_offering_price` bucket regex (works, untouched); `collectFiles`/`collectSourceFiles` fork (the repo already carries 8+ such walkers — a repo-wide extraction is beyond pm45). **Verified:** `tsc`/`eslint`/`prettier` clean; guardrail suite 20 files / 155; `manage-products-query-budget.integration` 6/6; `guard.integration` 116/116.

**pm46 (Price table reshape — schema target state): IN PROGRESS — implemented + verified (2026-09-21).** Part 4, first unit. DB-only (`db/**` boundary): second in-place edit of `0006_product.sql` (gate G-C, granted 2026-09-21) reshapes `product.product_offering_price` to `component_type` + `price_component` jsonb, dropping `price_type`/`pricing_model`/`amount`/`pricing_characteristics`; adds the six per-`component_type` completeness CHECKs (D3) plus the schema-local `IMMUTABLE` helper `product.pricing_steps_ok` for `capacity_motivation`'s strict-ascent rule; rekeys uniqueness to `product_offering_price_component_start_unique` — a `UNIQUE NULLS NOT DISTINCT (product_offering_id, component_type, unit_of_measure, start_date_time)` constraint (gate G-F, resolved 2026-09-21), replacing the old `product_offering_price_type_start_unique` index; removes `product_offering_price_amount_check` from `0007` (I3, covered by the same G-C grant). `db/schema/product.ts` mirrored by hand, importing `PricingComponent` (type-only) from pm47's new `validation/product/pricing-component.schema.ts` per the I2 import-sequencing note (pm47's schema file lands first inside the branch, ahead of its own unit number).

**pm46 spec deviation #2 (necessary, empirically verified against a throwaway Postgres 17 container).** I1.4's literal `usage_rate_check`/`flat_fee_check`/`capacity_commitment_check` predicates, and `pricing_steps_ok`'s literal `ratePerUnit` clause, are **vacuously satisfied** whenever the referenced `params` key is entirely absent or of the wrong JSON type — because a Postgres CHECK is violated only on `FALSE`, never on `NULL`, and `#>>`/`->>` return `NULL` for a missing path while stringifying a JSON *number* and a JSON *string* identically (`100` and `"100"` both extract as text `100`). Confirmed empirically: against the literal spec SQL, `INSERT … ('flat_fee', '{"params":{}}')` and `INSERT … ('usage_rate', '{"params":{"ratePerUnit":100}}')` **both succeeded** — silently accepting exactly the malformed rows I4's own test matrix requires rejected ("flat_fee with no params.amount", "ratePerUnit: 100 (number)", "a step ratePerUnit: 50 (number)"). Fixed by adding `COALESCE(jsonb_typeof(price_component #> '{params,<field>}'), 'missing') = '<expected-type>'` immediately before each existing regex/numeric check (`ratePerUnit`/`amount` → `'string'`; `committedQuantity` → `'number'`, closing the same gap there defensively though not required by any I4 case) and the equivalent `COALESCE(jsonb_typeof(s.v -> 'ratePerUnit'), 'missing') <> 'string'` disjunct inside `pricing_steps_ok`. Re-verified all 24 of I4's required cases plus both uniqueness cases against a full rebuild of the reshaped table in the same container — every case now names the exact constraint I4 specifies, including the multi-constraint-candidate case (`usage_rate` componentType + `flat_fee` envelope, where `envelope_type_check`, declared before `usage_rate_check`, is the one Postgres reports).

**pm46 deviation #2 addendum (CodeRabbit review fix, 2026-09-21).** The original pass coalesced `ratePerUnit`'s type check inside `pricing_steps_ok` but missed the same NULL-vacuity hole on that function's own `aboveQuantity` clause (`jsonb_typeof(s.v -> 'aboveQuantity') <> 'number'` is `NULL`, not `TRUE`, when the key is absent — a step missing `aboveQuantity` silently passed) and on `flat_fee_check`'s `priceType`-keyed period-pair disjunction (with `priceType` absent, both `= 'recurring'` and `<> 'recurring'` branches evaluate `NULL`, and a CHECK only rejects on `FALSE`, so a `flat_fee` row with no `priceType` and NULL period columns silently passed). Fixed in both `0006_product.sql` and its `db/schema/product.ts` mirror: `aboveQuantity`'s typeof check now wraps in the same `COALESCE(..., 'missing')` pattern, and the period-pair disjunction is now gated by an explicit `COALESCE(price_component ->> 'priceType', '') IN ('recurring', 'oneTime')` conjunct — the `COALESCE` matters because `NULL IN (...)` is itself `NULL`, not `FALSE`, so a bare `IN` gate would have reproduced the exact vacuity hole it's meant to close (caught by the new integration case, fixed in the same pass) — with the second branch's `<> 'recurring'` tightened to `= 'oneTime'`, since the gate already excludes every other value. New I4 cases added to `tests/db/product-price-component-constraints.integration.test.ts`: "rejects capacity_motivation with a step missing aboveQuantity" and "rejects flat_fee with no priceType".

**pm46 spec deviation #1 (necessary, not optional).** I1.3/D2 name only three CHECKs as dropped from `0006` (`…_type_check`, `…_pricing_model_check`, `…_amount_xor_tiers_check`) and I5.2's absence assertion covers only those three plus `0007`'s `…_amount_check` — "the four dropped CHECK names." But two more `0006` CHECKs, `product_offering_price_recurring_period_check` and `product_offering_price_usage_unit_check`, reference `price_type` directly in their predicate; since `price_type` is dropped from the table, these two CHECKs cannot survive (a CHECK naming a column that no longer exists is not valid DDL) and the spec never lists them as retained either (the "Retained unchanged" list names only `…_period_value_check` and `…_unit_value_check`, which don't reference `price_type`). Both are removed here, superseded by the per-`component_type` period/unit rules folded into the six new D3 CHECKs. Flagging per the module's own precedent (pm35/pm37/pm38's documented deviations) rather than silently deleting five CHECKs while the spec says three.

**pm47 (Component envelope: Zod union, domain types, `plaSpec` catalog): IN PROGRESS — implemented + verified (2026-09-21).** Part 4. Pure-TypeScript unit (`validation/product/**`, `types/product.ts`, two new test files) landing conceptually first inside the branch despite its unit number (I0's note): `validation/product/pricing-component.schema.ts` (new) is the single source of truth for a price — a five-branch `z.discriminatedUnion('@type', …)` (`usage_rate`/`flat_fee`/`capacity_commitment`/`capacity_motivation`/`negotiated_override`), every branch a `strictObject` (unknown keys refused, not stripped), `specVersion: z.literal(1)` required on every branch, one `moneyStringSchema` reused by every money field, `stepsSchema` enforcing VI1 (strict ascent, no duplicates, `aboveQuantity > 0`), `usage_rate`'s `plaSpecId`⟷`rateCardLookUp` cross-field refinement inside the branch (D4), `persistablePricingComponentSchema` (the same union minus `negotiated_override`, Inv. #39 as a type error), plus the `plaSpec` doc-block and TMF620 mapping table (D11) inside the file. `price-input.schema.ts` rebuilt discriminated on `componentType` (D7) — implemented as `z.union` of five `strictObject` branches rather than a single `z.discriminatedUnion`, because `flat_fee`'s `recurring`/`oneTime` variants share the literal `componentType: 'flat_fee'` and cannot occupy two slots of one discriminant map; `insert-price.schema.ts` rebuilt to match (adds `startDateTime` per branch, same `z.union` shape); `update-price.schema.ts` unchanged (still re-exports `insertPriceSchema`, D2's "identical shape" holds). `types/product.ts` gains `COMPONENT_TYPES`/`ComponentType` (four members), `ENVELOPE_PRICE_TYPES`/`EnvelopePriceType`, `APPLIES_AT`/`AppliesAt`, `BASIS`/`Basis`, re-exports `PricingComponent` + branch types; deletes `PRICE_TYPES`/`PriceType`, `PRICING_MODELS`/`PricingModel` with no forwarding alias (D8). `pricing-characteristics.schema.ts` + its test deleted (I4). New tests: `tests/validation/pricing-component.schema.test.ts` (I5) and `tests/product/pricing-composition-contract.test.ts` (I6, test-local pure function, exports nothing to production, Inv. #43).

**pm47 spec deviations / open flags (flagged, not silently resolved).** (1) **`price-input.schema.ts`/`insert-price.schema.ts` use `z.union`, not `z.discriminatedUnion`, at the top level** — see above; D7's "discriminated on `componentType`, not `priceType`" is honoured at the *type* level (TS still narrows correctly on the literal fields, and the impossible field combinations are exactly as untypeable), just not via Zod's `discriminatedUnion` combinator, which cannot host two branches sharing one discriminant literal. `flat_fee`'s two `priceType` variants are still distinguished by their own literal field. (2) **`PriceCard` (`types/product.ts`) had `priceType`/`pricingModel`/`amount`/`pricingCharacteristics` fields, all now typed against deleted types.** Per I3's own instruction ("the minimal change is to remove the dropped fields, not to add the new ones early"), these four fields are deleted from `PriceCard` with no replacement — the read-model reshape is pm49's. (3) **I7 (`AGENTS.md`/`README.md` cross-links to the `plaSpec` catalog) cannot be completed as written — no such files exist for this module.** The repo has a root `AGENTS.md` (a generic, project-wide file about the Next.js fork + the context-file reading order, `context/product-management/prodmgmt-*.md` for everything module-specific) and no product-module `README.md` at all; `db/migrations/README.md` exists but covers migration-authoring mechanics only, unrelated to the pricing envelope. Rather than inventing a cross-link target or editing the unrelated root `AGENTS.md`, I7 is left undone and flagged here for the user to say where (if anywhere) this link belongs. (4) **D10's tiered-sweep found more than "a handful" of surviving `PriceType`/`PricingModel`/tiered-shape sites inside the product module, outside `ordering/**`/`rating/**`/the bill-run flow (which D10 explicitly defers to pm50–52 regardless of count):** `components/products/prices-panel.tsx`, `components/products/manage/price-form.tsx`, `components/products/manage/editable-prices.tsx`, `components/products/price-type-badge.tsx` (+ its test), `db/repositories/product-offering-price.ts`, `db/seeds/demo/product-demo.ts`, `tests/db/product-repositories.integration.test.ts` — eight files. D10's own text says this triggers workflow §4.4 ("split the sweep out and land it before this union"), but that split unit has no spec in this repo and is not requested here. None of these eight files were touched — they are outside pm47's stated boundary (`validation/product/**`, `types/product.ts`, two test files) — and they, plus every other Product-module consumer of the old price shape, are left red by design under gate G-E (not independently green until pm54). Recorded here so the split-unit decision is made deliberately rather than discovered later.

**pm46/pm47 verified (2026-09-21).** `tsc --noEmit`, `eslint`, `prettier --check` clean on every file either unit touched (repo-wide `tsc` error count 122, all in exactly the out-of-boundary consumer files G-E predicts — repository, price-form, panels, badges, ordering module, seeds, various integration tests — none in a pm46/pm47 file). DB-free: `tests/validation/pricing-component.schema.test.ts` (32), `tests/product/pricing-composition-contract.test.ts` (4), `tests/guardrails/product-module-boundaries.test.ts` (15, incl. the re-baselined and new price-envelope assertions) — 51/51. Against a disposable Postgres 17 container (port 5434, torn down after): `tests/db/migration.integration.test.ts` (15/15, drop-all → migrate-from-empty → idempotent second run) and the new `tests/db/product-price-component-constraints.integration.test.ts` (28/28 — all 24 of I4's required cases plus the two extra uniqueness/lane cases, each rejection naming its exact constraint). `tests/db/ship-gate-guardrails.integration.test.ts`'s fixture insert (I5.5) was repaired and itself succeeds; the file's two guardrail cases still fail downstream in `getOrderDetail`/`createOrder` (pm49/pm50 territory, confirmed by direct inspection of `db/repositories/product-offering-price.ts`'s dropped-column references) — expected under G-E, not a pm46/pm47 defect. `tests/db/product-price-constraints.integration.test.ts` (the pm35 predecessor test) is now superseded and red by design — it inserts the pre-reshape columns; it is not edited or deleted, per pm46's stated boundary.

**Documentation landed (pm46 I6, pm47 I8).** `prodmgmt-architecture.md`: §3.4's NULL-collision note resolved (G-F); §7's matching gap bullet removed; §6 records Inv. #2/#4/#28 amended, #5 retired, #30–#44 in force (with an explicit note that this is pm46 *landing* the text, not G-B's own formal approval, which `prodmgmt-ai-workflow-rules.md`'s gate tracker still carries open); the Status line and a new "Baseline (G-A correction)" paragraph mark the Manage-rebuild baseline **unverified** pending G-0 (merge to `main` + SAST/DAST), per I6.7's own "if G-0 is not closed, record unverified" instruction. `prodmgmt-code-standards.md`: §6.14 rewritten to record the G-C grant (decider, date, explicit extension to the `0007` removal) while keeping forward-only in force for every other migration; §6.22's window corrected to pm46–pm54 (green claimed at pm54, G-E); Appendix A rows A1/A3/A4/A8 (pm46) and A2/A5 (pm47) cleared by grep, A6 annotated with the G-C grant; §7's file tree updated (`pricing-component.schema.ts`/`pricing-composition-contract.test.ts`/`pricing-component.schema.test.ts` markers dropped, `pricing-characteristics.schema.ts` + its test lines removed, the `0006`/A6 comment updated). `context/architecture.md` §3's JSONB example follow-up applied (no longer names `pricing_model`).

**Open flags for the user (documentation gaps in the repo, not implementation gaps).** (1) **pm47 I7** (`AGENTS.md`/`README.md` cross-links to the `plaSpec` catalog) — no product-module `AGENTS.md`/`README.md` exists to link from; left undone, see the pm47 deviations paragraph above. (2) **pm46 I6.8** (`pm00-build-plan.md` "Part 4 gate table: G-C GRANTED, G-F RESOLVED") — **`pm00-build-plan.md` has no Part 4 section at all**; it covers only Part 3 (pm35–pm45), ending at its Part-3 hand-off register. Writing a full Part 4 units/dependency-graph/gate table from scratch is beyond what this spec provides and risks inventing planning content the user hasn't authored; not attempted. (3) **pm46 I6.7**'s `prodmgmt-code-standards.md` §7/§9 half of the G-A correction was found **already correct** (line 371 already reads "unverified," matching the "G-0 not closed → unverified" instruction) — no edit was needed there. (4) **pm46 I6.9**'s "note in pm56's checklist" — no `pm56` spec exists in the repo to note it in; the `context/architecture.md` fix itself is landed (see above), only the cross-reference note is outstanding. (5) **`prodmgmt-ai-workflow-rules.md`'s own gate table is stale relative to the pm46/pm47 specs it governs, discovered mid-implementation.** That file (last touched by a peer session between this conversation's start and this note, per its own diff — not edited here) still lists G-0/G-B/G-C/G-E as **OPEN**, and its §0.6 note says explicitly "do not begin pm46 on the assumption that a red tree is acceptable because a plan document says so" pending a recorded G-E choice. `pm46-price-table-reshape.md`'s own gate table — dated the same day, 2026-09-21 — already resolves G-C (**GRANTED**), G-F (**RESOLVED**) and G-E (**Resolved: squash**, naming the pm46–pm54 window), which is exactly the "chosen and recorded, with the date" artifact §0.6 calls for; it just isn't recorded **inside** `prodmgmt-ai-workflow-rules.md` yet. Proceeding here was on the basis that the pm46/pm47 spec files themselves — handed directly by the user with an instruction to implement exactly as specified, dated today — are that recorded resolution, in the same way pm46's own gate table already treats itself as authoritative over the workflow doc for G-C/G-F. `prodmgmt-ai-workflow-rules.md`'s gate table and its W1/W3 rows are not edited here (out of pm46/pm47's stated file boundaries) but should be reconciled by whoever owns that doc, so the next reader doesn't find "OPEN" and stop.

**pm48 (Seeds emit envelopes): IN PROGRESS — implemented + verified (2026-09-21).** Part 4. Seeds-only unit (`db/seeds/**` boundary): `db/seeds/demo/product-demo.ts` — the `PriceSeed` interface replaced with a discriminated fixture shape (`componentType` + per-branch `params`, mirroring `price-input.schema.ts`'s own branches, D3); every existing price re-keyed per D3's table (flat/tiered → `flat_fee`/`usage_rate`, the tiered collapse commented in place); the new "Demo — Enterprise Capacity Plan" offering (D4) added with its four components — `usage_rate` (`ratePerUnit "100"`, `rateCardLookUp "ENTERPRISE_EA_CARD"` → `plaSpecId PLA_USAGE_RATE`), `capacity_commitment` (`committedQuantity 1000`), `capacity_motivation` (`steps [{1000,"50"},{2000,"25"}]`), `flat_fee` (`recurring`, `amount "2000.00"`) — on one DRAFT→components→ACTIVE version, the pm35 D6 way. The insert loop now builds the envelope (`buildPriceEnvelope`), parses it through `persistablePricingComponentSchema`, and writes `componentType`/`priceComponent` + the retained row columns (`toRowColumns`); the `priceCharacteristicsSchema` import and call are gone. `db/seeds/sample/seed-billrun-sample.ts` — only the `_SAMPLE_` price insert touched (D5): re-keyed to `flat_fee`, envelope `priceType: recurring`, `params.amount: SAMPLE_RECURRING_AMOUNT` (value unchanged — pm52 reads this exact constant); teardown, offering-insert ordering, idempotency guard and every other fixture in the file untouched. `db/seeds/product.ts` confirmed byte-identical (D2) — it seeds only the ADMIN `products:DELETE` grant and has held no catalog row since the demo split; no edit was needed or made. New `tests/db/product-seed-components.integration.test.ts` (10 cases): every seeded row a persistable `component_type`, `component_type = price_component ->> '@type'` (Inv. #30), `specVersion` present (Inv. #44), no `flat_fee` with a unit or `usage_rate` without one, no `negotiated_override` row, the capacity-plan offering's exact four D4 components, the 2027 dated successor still seeding in its own lane, every row round-tripping through the Zod union, and the D1 double-failure pair (a descending-`steps` envelope rejected by `persistablePricingComponentSchema.parse` in a DB-free test, the same object rejected by `product_offering_price_capacity_motivation_check` via raw SQL bypassing Zod in the DB-backed test).

**pm48 spec deviation (necessary, found via live-DB testing, not in the D3 table's literal wording).** D3's re-keyed "Demo — Monthly Recurring Charge" (`flat_fee` `recurring`) and "Demo — Activation Fee" (`flat_fee` `oneTime`) both carry `unit_of_measure NULL` and were both dated `2026-01-01T00:00:00Z` in the pre-pm48 fixture. Under the OLD schema this was fine — `price_type` ('recurring' vs 'once') kept them apart. Under pm46's rekeyed `product_offering_price_component_start_unique` (`NULLS NOT DISTINCT` on `(offering, component_type, unit_of_measure, start_date_time)`, G-F), both rows now collapse to the same key (`component_type = 'flat_fee'`, unit `NULL`) and the second insert threw `23505` — confirmed empirically against a throwaway Postgres 17 container, the same class of finding pm46 itself flagged via its own throwaway-container sweep. Fixed by dating "Demo — Activation Fee" `2026-01-02T00:00:00Z` (one day apart; comment left in the fixture explaining why) — no other field changed. This is a real, general consequence of collapsing `recurring`/`once` into one `component_type` for uniqueness purposes: two `flat_fee` prices of *any* envelope `priceType` can no longer share a `start_date_time` on one offering, not just two of the same kind — worth carrying forward into pm49's/pm51's authoring-side validation and error copy, since a user adding a second flat fee on a date that already has one will hit this same constraint.

**pm48 spec confirmation (I4.3 — no edit needed, not silently skipped).** The three named seed-boundary guardrails — `tests/guardrails/demo-seed-boundary.test.ts`, `tests/guardrails/billing-sample-seed-boundary.test.ts`, `tests/guardrails/billing-sample-seed-marker.test.ts` — were read in full: none references `price_type`, `pricing_model`, `amount` or any other pre-reshape column or field. The first two are npm-script-wiring grep gates (`db:seed-demo`/`db:seed-sample` never in `db:setup`); the third asserts `udr_rated` seed-provenance markers, unrelated to `product_offering_price`'s shape. "Updated for the new column names only" (I4.3) is therefore a no-op here, confirmed rather than assumed — the same "D2 confirmed-untouched" pattern pm48 itself uses for `db/seeds/product.ts`.

**Code-review follow-up (xhigh multi-agent review of pm46–pm48, 2026-09-22).** A recall-focused review of the pm46/47/48 diff was run and each finding evaluated against the specs. The overwhelming majority were already intended-by-design (§D10: envelope-field presence beyond the discriminator is Zod's job + the pm56 sweep, not the DB's) or are the documented gate-G-E "red by design" consumer breaks (pm49–pm52). One in-boundary hardening was applied; two tsc-invisible cross-unit hand-offs are recorded here so they cannot slip.

- **Applied — `pricing_steps_ok` cast safety (a third in-place `0006_product.sql` edit, authorized by Khek 2026-09-22 under the same G-C fresh-install regime).** The strict-ascent subquery and the `aboveQuantity <= 0` disjunct cast `(… ->> 'aboveQuantity')::numeric` unconditionally; those casts sit in AND/OR operands **sibling** to the `jsonb_typeof … <> 'number'` guard, and Postgres does not guarantee the guard evaluates first (only `CASE` forces WHEN-branch order). A raw-SQL `capacity_motivation` write (bypassing Zod) with a non-numeric `aboveQuantity` could therefore raise `invalid input syntax for type numeric` instead of failing the CHECK cleanly — empirically it returns `false` on PG17 today, so the hazard is latent/planner-dependent, not currently reproducing. Both casts are now `CASE WHEN jsonb_typeof(…) = 'number' THEN … END` guarded: behaviour is identical for every valid input and every I4 case, and a malformed threshold now fails cleanly rather than risking a raw cast error. `db/schema/product.ts` needs no change (it references the function by name, not its body); guardrail 13 asserts the function exists / is `IMMUTABLE` / precedes the table, not its body, so it stays green. Recorded in code-standards §6.14.
- **Hand-off to pm52 (rating processor) — NOT visible to `tsc`.** `workflow-management/worker/workflow-engine/runtime/rp.py`'s `_RESOLVE_SQL` still `SELECT`s `popp.price_type` / `popp.amount` / `popp.pricing_model` (all dropped by pm46) and — separately — partitions its `lead()` effectivity window on the OLD `PARTITION BY popp.product_offering_id, popp.price_type` key. pm52 must (a) re-key the reads to `component_type` / `price_component #>> '{params,…}'` (the pm48 seed comment already notes pm52 reads this exact `flat_fee.params.amount`) **and** (b) repartition the window on `(product_offering_id, component_type, unit_of_measure)` per pm46 §D6 — a column rename alone leaves a latent mis-derived `eff_to` where a `capacity_motivation` row could supersede the `usage_rate` beside it. Because this is Python + a SQL string, no type checker flags it; it is red only at bill-run runtime, so it is easy to miss inside the G-E red tree.
- **Hand-off to pm49 (price services) — NOT visible to `tsc`.** `services/product/insert-price.ts:116` and `services/product/update-price.ts:102` still pass the OLD constraint name `product_offering_price_type_start_unique` to `isUniqueViolation(...)`; pm46 renamed it to `product_offering_price_component_start_unique`. It is a string literal, so it compiles even after pm49 rewrites these services' imports — the `DUPLICATE_START` typed result would then silently never fire, re-throwing the raw `23505` as `SERVER_ERROR`. pm49 must update both names when it reworks the services (guardrail 13 asserts the migration text, not these call sites, so nothing else catches it).
- **Confirmed intended, no change (§D10 / pm56 sweep):** the DB backstop deliberately does not check envelope `appliesAt` / `basis` / non-`flat_fee` `priceType`, `boundTo` shape, `unit_of_measure = boundTo.unitOfMeasure`, `specVersion`, or the `plaSpecId`⟷`rateCardLookUp` pairing — all are Zod's on every real write path and are swept as data assertions at pm56 (pm46 §D10 is explicit: "no `specVersion` CHECK … envelope-field presence beyond the discriminator is Zod's job"). `price_component`'s `$type<PricingComponent>()` (not `Persistable…`) is the pm46 §D2/checklist choice; the negotiated-override write-time type-error guarantee comes from the seed/repository parsing through `persistablePricingComponentSchema` (pm47 §I1.8), not the column type. The `ENVELOPE_PRICE_TYPES` / `APPLIES_AT` / `BASIS` exports are pm47 §I3-mandated forward declarations for pm53–55, not dead code.

**Verified (2026-09-21).** `tsc --noEmit` clean on every pm48-touched file (repo-wide error count dropped from pm46/47's recorded 122 to 119 — `product-demo.ts` was one of pm47's eight flagged out-of-boundary red files and is now green; `seed-billrun-sample.ts` is unaffected by the remaining count); `eslint`, `prettier --check` clean on all three files. DB-free: the new suite's Zod-only D1 case, plus the three named seed-boundary guardrails, green. Against a disposable Postgres 17 container (port 5434, torn down after): `tests/db/product-seed-components.integration.test.ts` (10/10, including both D1 double-failure layers and the D4 capacity-plan assertions) and, as a regression check, pm46's own `tests/db/product-price-component-constraints.integration.test.ts` + `tests/db/migration.integration.test.ts` (45/45, unaffected — pm48 touches no schema). **Not run here** (same live-fixture caveat as pm35/pm36/pm18–23): the full `db:setup → db:seed-demo → db:seed-sample (twice)` chain end-to-end — `db:seed-sample` alone requires the accounts/RBAC/bill-cycle seeds and a privileged `BOOTSTRAP_DATABASE_URL` teardown connection already wired, which is the full local-dev-stack bring-up, not a pm48-scoped setup; the D5 price-insert change itself is a small, direct re-key (the surrounding customer/account/order/teardown logic is byte-unchanged) and was verified by inspection plus `tsc`/`eslint`/`prettier`. `db:seed` is green again for the first time since pm46 (I5) for the product-demo path verified here; the sample-seed path is unverified end-to-end pending that infrastructure.

**pm49 (Component write path: repository, services, actions, cross-row validator): IN PROGRESS — implemented + verified (2026-09-22).** Part 4. Write-path unit (`db/repositories/product-offering-price.ts`, the price reads in `db/repositories/product-offering.ts`, `services/product/{insert,update,delete}-price.ts`, the new `services/product/validate-offering-components.ts`, the three matching actions, `PriceCard` in `types/product.ts`). Delivers: `findByOfferingIdWithDerivedEnd` reshaped to select `componentType`/`priceComponent`, `lead()`-window partitioned by `(productOfferingId, componentType, unitOfMeasure)` and ordered the same way (D3), each row parsed once via `persistablePricingComponentSchema`; `toPriceWriteData` (repository) projects a parsed `InsertPriceInput` branch into the eight-field envelope (`buildComponentEnvelope`, mirroring pm48's seed `buildPriceEnvelope`) plus its row columns (`toComponentRowColumns`), so `insertPrice`/`updatePrice` write `componentType`/`priceComponent` from `data.component["@type"]` — never a separately-carried field — so Inv. #30 holds by construction (I1.2); `validateOfferingComponents(tx, offeringId, candidate)` (new file) — one sibling read on `tx`, the candidate folded in per insert/update/delete (D4), three checks in the fixed order currency → base-rate presence → ambiguity, VI4 validated only at the candidate's exact `start_date_time` (D5, no period rule); all three price services call it after their DRAFT gate and before the write, and surface its three typed codes (`MODIFIER_WITHOUT_BASE_RATE`, `AMBIGUOUS_BASE_RATE`, `CURRENCY_MISMATCH`) through to the actions unchanged (I5). `negotiated_override` cannot reach the table: `toPriceWriteData` only ever builds from `InsertPriceInput`'s four-branch union, proved at the Zod layer and the DB CHECK layer by the new test (D8).

**pm49 necessary deviations from the spec's literal wording (flagged, matching the module's own precedent for documenting forced departures).** (1) **I1.3's "`updatePrice`/`deletePrice` unchanged in contract" could not mean byte-identical signatures.** Code-standards §1.13 requires the cross-row validator to read siblings "on `tx`, locked" — i.e. strictly *after* the DRAFT lock and *before* the write, in the same call. The pre-pm49 `updatePrice`/`deletePrice` each locked-then-wrote as one atomic step with no seam for a mid-call check, and the validator cannot live in `db/repositories/**` (dependency rule, code-standards §2: repositories never import from `services/`). Resolved by giving both methods a `validate: (offeringId) => Promise<...>` callback parameter, typed structurally against a duplicated-shape-only local type (`OfferingComponentValidationFailure` — the type shape is restated, not the VI3–VI5 *logic*, so this is not the drift §2.16 forbids) — the repository never imports or references `validateOfferingComponents`; the service is still the one textual call site. The DRAFT decision itself stays exactly where it was (repository method body, one place, unchanged — verified by the pm38 structural guardrail's still-passing regex assertion). `insertPrice` needed no such change (D9): it never locked before writing, so the service calls the validator directly, inline, after the branch decision. (2) **`insertPrice`'s branch-then-validate ordering (D9) forces a throw, not a return, on refusal.** Returning `{ ok: false, ... }` from inside `db.transaction(...)` *commits* whatever was already written in that call — for the direct-DRAFT path nothing has been written yet, but for the branch-on-ACTIVE path `branchOfferingAsDraft` is a real write that happens *before* the validator runs. A plain early return would silently leave the orphan branch committed. Fixed with a sentinel `OfferingComponentViolationError` thrown inside the transaction and caught outside it — the exact shape `update-price.ts`'s own pm41-era `BackdatedStartTooFarError` already established for the same reason. Verified live: a refused branch-candidate leaves zero rows under `family_offering_id = <active>` (test "adding a component to an ACTIVE version branches…"). (3) **`PriceWriteData`/`PriceWriteSnapshot` field named `component`, not `priceComponent`.** The spec doesn't name this field explicitly; `component` was chosen (and applied consistently to both types) to match `PriceCard.component` (D2's own literal wording) and to keep the `PRODUCT_PRICE_ADDED`/`_UPDATED`/`_DELETED` audit payloads carrying the same key across all three event types — caught by the new test's own audit-payload assertions during implementation. (4) **`db/repositories/product-offering.ts`'s `branchOfferingAsDraft` needed a fix beyond I2's literal "no other change to that file."** Its raw price-row copy (`sourcePrices.map(...)`) still referenced the four dropped columns (`priceType`/`amount`/`pricingModel`/`pricingCharacteristics`) — a `tsc` error, not a stylistic gap — re-keyed to `componentType`/`priceComponent` (envelope copied verbatim, not re-parsed, §2.18). This is squarely "the price reads … in `db/repositories/product-offering.ts`" the unit's own boundary line names. (5) **Closed pm48's own flagged hand-off:** `isUniqueViolation(err, "product_offering_price_type_start_unique")` in both `insert-price.ts` and `update-price.ts` renamed to `product_offering_price_component_start_unique` (pm46's rekey) — previously a silent string-literal miss that would have let every `DUPLICATE_START` fall through to `SERVER_ERROR`.

**pm49 verified (2026-09-22).** `tsc --noEmit`: repo-wide error count **98** (down from pm48's recorded 119; zero errors in any pm49-touched or pm49-new file — confirmed by file-level grep). `eslint`/`prettier --check` clean on all seven touched files plus the two new ones. The existing structural guardrails (`tests/db/product-repository-exports.test.ts`, `tests/guardrails/product-module-boundaries.test.ts`'s "exactly `findByOfferingIdWithDerivedEnd`, `insertPrice`, `updatePrice`, `deletePrice`" + DRAFT-check regex assertion) still pass unmodified — 38/38 — proving the `validate` callback addition didn't change the repository's method-name surface or its DRAFT-check code shape. **New `tests/db/product-price-components.integration.test.ts` (22/22)** run against a throwaway Postgres 17 container (torn down after, same disposable-DB regime prior units used): all four component types save and read back parsed with correct per-lane `endDateTime`/`effectivityStatus`; a dated `usage_rate` successor supersedes only its own lane, not a same-start `capacity_commitment` beside it; each of the three violation codes fires for its documented case, including deleting the last same-unit `usage_rate` out from under a live modifier; the three "must pass" legitimate cases (dated successor, lone `flat_fee`, two modifiers over one base rate); `RETIRED`/`ACTIVE` insert behavior; a direct SQL write against `TESTING` refused by the pm36 trigger; branch-on-ACTIVE both refusing (zero-row-committed) and succeeding against the branch's own component set; `negotiated_override` refused at the Zod layer and independently at the DB CHECK layer; exactly one audit event per mutation carrying `componentType` + the envelope; the validator's fixed check order (currency before base-rate) proven directly. Regression spot-check against the same throwaway DB: pm46's `product-price-component-constraints.integration.test.ts`, pm48's `product-seed-components.integration.test.ts`, and `migration.integration.test.ts` all still green — pm49 introduced no schema or seed regression.

**pm49 open flags for the user (found, not fixed — outside this unit's boundary).** (1) **`tests/db/product-family-guards.integration.test.ts` (pm36's own suite) is broken independently of pm49** — confirmed live: 13/20 cases fail with `column "price_type" of relation "product_offering_price" does not exist`, because its raw-SQL price fixtures were never updated for pm46's column drop. This predates pm49 (pm49 touches none of that file's fixtures) and was not previously called out in the tracker; recorded here so it isn't mistaken for a new regression. Needs its own pass, likely folded into whichever unit next needs that file green (candidate: the pm54 ship gate, or an explicit sweep). (2) **`insertPrice` still has no `TESTING`/`OBSOLETE` handling** — a pre-existing gap (pm15/pm38 never wired code-standards §1.10's "a TESTING version returns to DRAFT and edits directly" into this specific service), unrelated to and unchanged by pm49: a direct call against a `TESTING` or `OBSOLETE` offering falls through to a plain insert and the §3.5 trigger raises an uncaught `PostgresError` rather than a typed result. D9 lists only the five codes that "stay" and doesn't ask for a sixth; not fixed here. Verified live and documented in the new test's own comments rather than silently working around it. (3) **I7.9 (query-budget) and I7.10 (authz) were not separately re-asserted** — pm49 adds no new query to any page's render path and no new permission/level, so `manage-products-query-budget.integration.test.ts` and `guard.integration.test.ts`'s existing coverage (`updatePrice`/`deletePrice` already EDIT-gated since pm45) already cover the shape; re-testing here would duplicate rather than extend, matching pm45 D1's "the gate asserts, it does not re-implement." Flagged for whoever runs the pm53/54 ship gate to confirm explicitly rather than leave unexamined. (4) **I7's negotiated_override "tsc" layer is proved by construction, not a dedicated compile-error test:** `insertPriceSchema`'s discriminated union has no `negotiated_override`/`componentType` branch at all (pm47), so a caller cannot even construct the input to begin with — there is no assignment to `@ts-expect-error` against, unlike a case where a wider type narrows. Recorded rather than manufacturing an artificial compile-fail test. (5) **Doc note (I8.1):** the spec's instruction to "record the pm50/pm51/pm52-vs-pm53/pm54/pm55 stale-marker discrepancy" does not correspond to anything found in the current `prodmgmt-code-standards.md` §7 tree (it already reads `pm50`/`pm51`/`pm52` throughout, with no `pm53`/`pm54`/`pm55` UI markers to reconcile) — left unresolved rather than guessed at; only the now-landed `validate-offering-components.ts` file's stale `(new)` marker was cleared.

**pm50 (Ordering: override-target re-key, cross-module G-G): IN PROGRESS — implemented + verified (2026-09-22).** Part 4. Cross-module re-key (`services/ordering/order-preconditions.ts`, `validation/ordering/create-order.schema.ts` comments, four ordering integration test fixtures). **G-G authorization treated as granted 2026-09-22 (Khek)** — the user's direct instruction to implement pm50-spec exactly, naming this unit and its files, was recorded as the required written authorization (the spec's own gate text: "that is not the authorization" on its own referred to the *cross-runtime decision*, not to a subsequent explicit go-ahead); recorded in code-standards §1.34 + Appendix A row A9 and workflow Appendix A row W4 in this change set, per D5/I5. Delivers: an explicit total `Record<OverridePriceType, {componentType; envelopePriceType?}>` lookup in `order-preconditions.ts` (D2/D3) replacing `price.priceType === override.priceType && price.pricingModel === "flat"`; `usage → usage_rate`, `recurring → flat_fee`+envelope `recurring`, `once → flat_fee`+envelope `oneTime`; capacity modifiers absent from the map on purpose (O6 comment, I1.5); `create-order.schema.ts`'s override-contract comment rewritten per D5. `ordering.order_item_price_override` untouched — no column, CHECK or repository-surface edit (D4).

**pm50 necessary deviations from the spec's literal wording (flagged, matching the module's own precedent).** (1) **`create-order.schema.ts` could not stay "comment-only."** Its `import { PRICE_TYPES } from "@/types/product"` was already a dead import — pm47 deleted that catalog-side export outright (confirmed: repo-wide `tsc` had this exact file failing with `TS2305` before this unit touched it) — and the checklist requires `tsc` clean for this unit's own files. Fixed by declaring the override's own vocabulary locally (`export const OVERRIDE_PRICE_TYPES = ["recurring", "usage", "once"] as const`), same three values, same order, same Zod behaviour — not a schema rule/field/refinement/message change, just an import source fix for a name pm47 had already retired. `order-preconditions.ts` imports the new export by type only (`OverridePriceType`) for its D2/D3 lookup key; `types/ordering.ts`'s own separately-broken `PriceType` import (used by `get-order-detail.ts`/`review-order.ts`) is untouched — out of this unit's G-G file list. (2) **The four fixtures needed more than "change only the shape of the seeded price rows" (D6).** Every one of them created its offering `ACTIVE` and then inserted prices directly — legal before pm36, refused since by the `product_child_write_requires_draft` DRAFT-guard trigger (pm36's own tracker note already flagged this exact debt against these four files and left it unassigned). No shape of price row is insertable into an ACTIVE offering at all, so "repair the four suites" was unreachable without also reordering each fixture's `newOffering` helper to insert-while-DRAFT-then-activate (the same pattern pm36 gave `db/seeds/demo/product-demo.ts`) — done in all four, amounts/currencies/dates otherwise unchanged. (3) **`tests/db/ordering-read.integration.test.ts`'s `getOrderDetail` describe block (4 cases) is not green, and cannot be made green inside this unit's boundary.** `services/ordering/get-order-detail.ts` reads the dropped `PriceCard.priceType`/`.amount` fields (confirmed live: both undefined at runtime, not just a `tsc` error) — it is not in the G-G file list, it is not in architecture §3.7's reader-inventory table (only 4 rows: `rp.py`, the bill-run YAML, `order-preconditions.ts`, `create-order.schema.ts`), and D7 explicitly excludes "the order/subscription services." Left red by design, same as G-E predicts for every other out-of-boundary consumer; `listOrders`/`findList`/inventory-repository tests in the same file are unaffected and green. Whoever re-keys `get-order-detail.ts` (no owning unit found in this repo's specs) should reuse D2/D3's `OverridePriceType`→component mapping idea for its own `priceType`-keyed grouping. (4) **`OVERRIDE_PRICE_TYPE_INVALID`'s exercising test was retitled**, not just re-seeded: "override targets a tiered price type" → "override targets a component absent on the pinned version," since D6 replaces the tiered price with a `capacity_motivation` (still legitimately not an override target) — assertions themselves are byte-unchanged.

**pm50 new tests (I4).** Added to `create-order.integration.test.ts`: a `usage` override resolving against a `usage_rate` component; a `once` override resolving against a `oneTime` `flat_fee`; an offering carrying only capacity modifiers + a `usage_rate` (`recurring` override refused, `usage` override accepted). The "second override for the same `(order_item, price_type)` is refused, insert-only" case is already covered outside this unit's four files by `tests/db/ordering-inventory.integration.test.ts`'s existing unique-constraint case and the repository-exports guardrail (D4) — not duplicated here, per D4's "this unit runs them and reports, it does not edit them."

**pm50 D6 sweep confirmations (no edit needed).** `tests/db/ordering-inventory.integration.test.ts` — every `priceType` reference in the file is on `ordering.order_item_price_override` (unchanged by this unit), none on `product.product_offering_price`; confirmed by inspection, not assumed. `tests/db/ordering-repository-exports.test.ts` carries no price shape at all.

**Verified (2026-09-22), against a throwaway Postgres 17 container built from `infra/docker/postgres` (port 5434, torn down after).** `tsc --noEmit`, `eslint`, `prettier --check` clean on all six touched files. `create-order.integration.test.ts` + `review-order.integration.test.ts` + `subscription-lifecycle.integration.test.ts`: **41/41** (all pre-existing assertions unchanged, plus the three new I4 cases). `ordering-read.integration.test.ts`: **7/11** — `listOrders`/`findList`/inventory-repository describe blocks green; the 4 `getOrderDetail` cases red, see deviation (3) above. Regression spot-check: `ordering-inventory.integration.test.ts` (13/13) and `ordering-repository-exports.test.ts` (10/10, two files) unaffected. `git diff --stat` touches no ordering file outside the six named above plus this tracker and the three doc files I5 names.

**pm51 (Rating runtime: price-resolution window re-key, cross-module G-G): IN PROGRESS — implemented + verified (2026-09-22).** Part 4. Cross-module re-key (`workflow-management/worker/workflow-engine/runtime/rp.py`'s `price_windows` CTE + resolution SELECT, four rating integration test fixtures). **G-G authorization treated as granted 2026-09-22 (Khek)**, same basis and same change set as pm50's (code-standards §1.34/§6.21); the user declined a separately-named "rating-module owner" and confirmed the single G-G authorization covers both the crossing and the re-key's correctness for pm51 too — recorded in code-standards §6.21 and architecture §3.7.

**Prior-work finding (not this unit's authorization, discovered mid-implementation).** `rp.py` was **already partially re-keyed**, uncredited to any pm-unit and without a recorded G-G authorization, by the immediately preceding commit (`f5d6209`, "Apply code review fixes on pm46-pm49") — four of D2's five changes were already in place (`WHERE component_type = 'usage_rate'`, the `ratePerUnit` JSON extraction, the `pricing_model` column drop, most of the discriminator comment). **Missing, and completed here:** the partition key still read `PARTITION BY popp.product_offering_id, popp.component_type` — D5's `unit_of_measure` addition was never applied, which is the actual latent hazard D5 warns about (a second `usage_rate` lane on one offering, at a different unit, could have its `eff_to` mis-derived by another lane's dated row via `lead()` ordering across both). Fixed per D2's table; the `pricing_model`/`LOOKUP_MISS`/partition-key code comments were rewritten to D3/D4/D5's fuller content (the prior commit's comment covered D3's gist but omitted the `rateCardLookUp`-not-read note and D4's "why LOOKUP_MISS's old NULL-amount cause is now impossible" reasoning).

**pm51 necessary deviations from the spec's literal wording (flagged).** (1) **The four fixtures needed the same DRAFT-guard-trigger fix pm50's four fixtures needed**, for the identical reason: every one of them created its offering `ACTIVE` then inserted a price directly, which `product_child_write_requires_draft` now refuses — confirmed live via a throwaway-DB smoke check before touching the real suites (see Verified, below). Fixed in all four by inserting DRAFT, pricing, then flipping to `ACTIVE` (same `db/seeds/demo/product-demo.ts` pattern). (2) **`rm08`'s test #13 raw-SQL catalog mutation** (`UPDATE product.product_offering_price SET amount = ...`) **could not stay literal** — `amount` is a dropped column; re-keyed to `SET price_component = jsonb_set(price_component, '{params,ratePerUnit}', '"0.9999"')`, same mutation intent (simulate a catalog correction after rating), same assertions. (3) **D5's new "`capacity_motivation` dated between two `usage_rate` rows changes nothing" case is placed in `rm08`, not `rm10`, despite I3 naming `rm10`.** Inspected `rm10` directly: its offering carries exactly one, never-superseded usage price throughout the file, and its own "supersession" is entirely `udr_rated` batch-level reprocessing (RL's domain) — there is no catalog dated-successor chain in `rm10` for a noise row to threaten. `rm08` already seeds the real two-row `usage_rate` chain (P1 2026-01-01 → P2 2026-08-01) D5's case needs, so the new test lives there instead, reusing that chain; `rm10`'s single price is re-keyed per D6 and otherwise untouched. (4) **`rm08` also gained D4's LOOKUP_MISS case** (an offering with only a `flat_fee` + `capacity_motivation`, no `usage_rate`, still misses) via a third offering (OFF3) and a third subscription (invC) — the natural home, since `rm08` already owns test #11's LOOKUP_MISS coverage.

**Verification constraint (environment, not scope) — flagged rather than silently skipped.** This session's environment has no local `python3` with `psycopg`/`polars` on PATH (confirmed: Microsoft Store stub only) — the four rating suites' DB-gated describe blocks are wrapped in `describe.skipIf(!databaseUrl || !pythonReady)`, exactly like the rm06/rm07 suites' own established posture ("not run in a session without a live test Postgres + the worker deps"), so they **skip loudly, not falsely-pass**: confirmed by running all four against the throwaway DB — 3 files' static (no-DB) describe blocks passed (10/10, flow-YAML wiring, unaffected by this unit), the DB+python blocks skipped (26 skipped), zero failures. I4's literal ask — "run the rating flow end to end… diff the rated output against a pre-reshape baseline" — could not be executed this way. Instead: (a) the re-keyed `price_windows` CTE was copied verbatim into a throwaway smoke script and run against a live, migrated Postgres 17 container seeded with the exact pm51-spec D2/D5 shape (two `usage_rate` rows + a `capacity_motivation` dated between them, same amounts as `rm08`'s fixture) — it resolved `0.0050`/`eff_to NULL` for 2026-08-14 and `0.0035`/`eff_to 2026-08-01` for 2026-03-01, both byte-identical to the pre-reshape values and unaffected by the noise row; a `flat_fee`+`capacity_motivation`-only offering independently confirmed zero `usage_rate` rows (the LOOKUP_MISS precondition). (b) `rp.py`'s syntax was verified via `ast.parse` inside the `workflow-engine` Docker container (which does have the worker's dependencies) since the host lacks Python entirely. This proves the SQL resolution logic pm51 actually changes is correct; it does not substitute for the literal end-to-end Parquet-output diff I4 asks for, which needs a session with local `python3`+`psycopg`+`polars` to run.

**Verified (2026-09-22).** `tsc --noEmit`, `eslint`, `prettier --check` clean on all four TS fixture files. `rp.py` contains no reference to `pricing_model`, `amount` or `price_type` on the catalog table (confirmed by grep); `oipo.price_type = 'usage'` (the ordering column) is unchanged; no `rateCardLookUp` read/join/resolution exists; no `PER_UNIT` or rate-card variant added; `db/bootstrap/rating-db-roles.sql` untouched (byte-identical — confirmed by `git diff --stat`). `git diff --stat` touches no other rating, workflow or bootstrap file beyond the five named in the G-G list plus this tracker and the two doc files I5 names.

**pm52 (Bill-run runtime: recurring resolver re-key, cross-module G-G): IN PROGRESS — implemented + verified (2026-09-23).** Part 4. Cross-module re-key (`workflow-management/flows/bill-run-processor/local-dev/bill_run_processing.yml`'s `_bm29_resolved` CTE + D33 checks, the sibling `bill_run_processing.template.yml`'s contract comments, `tests/db/helpers/billrun-aggregate.ts`, three bill-run integration test fixtures). **G-G authorization treated as granted 2026-09-23 (Khek)**, same basis as pm50/pm51's (code-standards §1.34/§6.21): the user's direct instruction to implement pm52-spec exactly, naming this unit and its files, is recorded as the required written authorization. **D4 decision: option A** (keep the `RECURRING_PRICE_UNSUPPORTED` arm, re-key its trigger to "the as-of `flat_fee` resolves to `priceType = 'oneTime'` with no override") — taken per the spec's own "unless the bill-run owner directs otherwise" default; no separately-named bill-run-module owner was available to consult in this session, so — mirroring pm51's recorded precedent of the user confirming one G-G authorization covers both the crossing and the re-key's correctness — this is flagged rather than silently assumed: **if a bill-run-module owner later prefers option B or C, the arm's trigger condition (not its name) is what changes.**

**Prior-work finding (not this unit's authorization, discovered mid-implementation) — a real, live masking-hazard bug.** `local-dev/bill_run_processing.yml`'s `_bm29_resolved` CTE was **already partially re-keyed**, uncredited to any pm-unit and without a recorded G-G authorization, apparently by the same uncredited prior work pm51 found in `rp.py` (both land in commit `f5d6209`, "Apply code review fixes on pm46-pm49"). The partial re-key got the amount extraction right (`(price_component #>> '{params,amount}')::numeric`) but **reintroduced exactly the masking hazard D3 warns against**: it filtered the as-of window with `AND pop.price_component ->> 'priceType' = 'recurring'` *inside* the `WHERE` clause, and its own comment asserted "every recurring row is flat_fee by the DB's own completeness CHECK, so there is nothing left to distinguish [RECURRING_PRICE_UNSUPPORTED] from NOT_FOUND" — both false once a `flat_fee` `oneTime` row can share a lane with, and postdate, a `flat_fee` `recurring` row (pm46's `(offering, component_type, unit_of_measure, start_date_time)` key, `unit_of_measure` NULL for both). Under the uncredited code, a `oneTime` row dated after a `recurring` one would have been silently invisible to the window (filtered out), so the resolver would keep billing the older recurring price forever — the exact bug the pre-reshape 'tiered' comment was written to prevent, reintroduced by the reshape. **Fixed per D2/D3/D4:** the window's `WHERE` now filters on `component_type = 'flat_fee'` only; `pop.price_component ->> 'priceType' AS envelope_price_type` is carried out of the window (also added to the outer `asof` projection and the final `_bm29_resolved` projection, alongside a new `has_override` boolean); the `PARTITION BY` gained `pop.component_type, pop.unit_of_measure` (a no-op today since the window is now flat_fee-only and unit_of_measure is always NULL there, but keeps the partition key aligned with pm51's `rp.py` convention for if/when this window's WHERE ever widens); and the D33 `DO $$` block gained a third counter (`v_unsupported`, counting rows where `unit_price IS NOT NULL AND NOT has_override AND envelope_price_type IS DISTINCT FROM 'recurring'`) between the existing `NOT_FOUND` and `CURRENCY_MISMATCH` checks. `tests/db/helpers/billrun-aggregate.ts` was **not** part of that uncredited partial re-key — it still had the fully pre-reshape shape (`pop.price_type`, `pop.amount`, `pop.pricing_model`) and would have failed at "column does not exist" against the reshaped table; it is now byte-for-byte identical to the flow's query (parameter-binding syntax aside — `:'ban'` psql vs. `${ban}` postgres.js), diffed deliberately per I4.

**Fixture repair (I5) — the three bill-run suites' raw-SQL price inserts.** All three suites' `newRecurringPrice`/`newTieredPrice` helpers still wrote the pre-reshape columns (`price_type`, `amount`, `pricing_model`, `pricing_characteristics`) directly via `sql\`INSERT ...\``, which fails at "column does not exist" against the reshaped table — confirmed red before this unit's fixture edits. Re-keyed to `component_type`/`price_component` (envelope built inline per insert, matching pm48/pm49's shape) in all three files. **`newTieredPrice` is retired, not repaired** — the `pricing_model = 'tiered'` shape it seeded no longer exists (PC8); every use of it is replaced with the D3/D4 scenario the spec calls its structural successor: a `flat_fee` `oneTime` row dated after a `flat_fee` `recurring` one, asserting `RECURRING_PRICE_UNSUPPORTED` fires and the older recurring price is never silently billed. **Unlike pm50/pm51's ordering/rating fixtures, none of these three files' `newOffering` helpers needed the DRAFT-guard-trigger insert-while-DRAFT-then-activate rework** — they never set `lifecycleStatus` at all, so every fixture offering stays at its schema default (`DRAFT`) for the fixture's whole lifetime, and the pm36 trigger (which only blocks a child write against a *non*-DRAFT parent) never engages; confirmed by inspection and by the suites' own green run, not assumed. Three new/replaced cases per I5: **billrun-recurring-aggregation** gained the D5 case (a `usage_rate` + `capacity_commitment` + `capacity_motivation` on a billed offering — bill unchanged) and its "[CRITICAL] tiered" case became the D3/D4 "one-time-supersedes-recurring" case (both new); its existing "rerun after a BACKDATED insert" case already covers snapshot reuse (I5.3) once its fixture helper was repaired, so no new case was needed there. **billrun-phase3-journey**'s single D33 fixture (previously `newTieredPrice`) became the same D3/D4 scenario (`newRecurringPrice` + `newOneTimePrice`), with the journey's assertions, stage-signal payload and test title updated to match — the end-to-end path (materialise → trigger → aggregate → verify → review → post → distribute) is otherwise byte-unchanged. **billrun-verification-reconciliation** needed only the `newRecurringPrice` re-key (D2) — it never used a tiered fixture — so its checksum/reconciliation assertions are untouched, per I5's own instruction.

**Verification constraint (environment, not scope) — flagged rather than silently skipped.** `billrun-phase3-journey.integration.test.ts` calls `triggerRun`, which resolves the `billrun` engine via `services/billing/engine-registry.ts` against this session's real `.env` (`BILLRUN_ENGINE_URL=http://localhost:8085/...`, `BILLRUN_ENGINE_LOOPBACK=true`) — a **live** Kestra `workflow-engine` container this session does not have running (`docker ps` shows only the `db` and `azurite` containers). The first run against the disposable test database failed at `triggerRun` itself (`ENGINE_UNREACHABLE`), before any pricing SQL executes — confirmed unrelated to this unit by re-running with `BILLRUN_ENGINE_URL`/`BILLRUN_ENGINE_AUTH` unset (forcing `engine-registry`'s documented stub client, "fully testable with no live engine"): the identical suite then passed end-to-end, including the D3/D4 scenario through the real Kestra-double SQL. This mirrors pm51's own environment-constraint note (no local `python3`/`psycopg` there); the fix here is standing up the local `workflow-engine` container, which is out of this unit's scope.

**I6 (full bill-run verification run, byte-identical recurring charges) — partially substituted, flagged rather than claimed.** I6 asks for a full bill run against a database built from empty and seeded by pm48, diffed against a pre-reshape baseline captured before the branch. No such baseline capture exists in this repo and no live Kestra engine is available in this session (see above), so the literal diff could not be run. Instead: the three suites above exercise the re-keyed `_bm29_resolved` CTE directly against a disposable Postgres database (not mocked), and every previously-asserted dollar amount (`60.00` for 3×20.00, `45.00`/`35.00` for the mixed-source cases, `5.00` for the override case, `20.00` for the snapshot-reuse case) reproduces unchanged post-re-key — the re-key changes *how* the price is found, never the arithmetic once found, so byte-identical output on these fixtures is direct evidence for I6's claim, not a substitute for it. A genuine pm48-seeded empty-to-full bill run against a live engine remains unverified here.

**Documentation (I7).** `prodmgmt-architecture.md` §3.7 reader-inventory row 2 corrected to the `local-dev` flow file (the template carries only contract comments; the same query is mirrored in the test helper) and marked re-keyed by pm52; its fixture-sweep sentence updated to mark the bill-run suites (and, in passing, the already-repaired rating/ordering/seed items) as repaired rather than "still open"/"not optional". `prodmgmt-code-standards.md` §6.21 updated the same way. **`pm00-build-plan.md` I7.2/I7.3 (the pm52 row's file-name correction; the hand-off register's D4 decision) — not done, same gap pm46 already flagged (I6.8):** the file has no Part 4 section at all (confirmed by grep — it covers only Part 3, ending at its Part-3 hand-off register), so there is no pm52 row to correct and no Part 4 hand-off register to append to; inventing one from scratch risks fabricating planning content the user hasn't authored. Recorded here, alongside pm46's identical open flag, so the gap is not rediscovered as if new. No `billmgmt-*` doc edited (I7.4).

**Verified (2026-09-23), against a disposable Postgres database (`pm52_test`, created on and dropped from the running dev-stack `db` container per the README's "separate database, not a separate container" guidance — port 5432, torn down after).** `tsc --noEmit`, `eslint`, `prettier --check` clean on all seven G-G files. `billrun-recurring-aggregation.integration.test.ts`: **9/9** (6 pre-existing cases green unchanged + the 3 new/replaced D3/D4/D5 cases). `billrun-verification-reconciliation.integration.test.ts`: **6/6**, assertions byte-unchanged. `billrun-phase3-journey.integration.test.ts`: **1/1** (green under the stub-engine workaround above; `ENGINE_UNREACHABLE` under the real-engine `.env` is the flagged environment gap, not a test failure). `git diff --stat` touches no other billing, workflow or bootstrap file beyond the seven G-G files plus this tracker and the two doc files I7 names.

**pm53 (Component rendering: badge + View Product's prices panel): IN PROGRESS — implemented + verified (2026-09-23).** Part 4. View-Product-only rendering unit (`components/products/pricing-component-badge.tsx` new, `components/products/price-type-badge.tsx` deleted, `components/products/prices-panel.tsx`, `components/products/price-effectivity.tsx`, their tests). Delivers `PricingComponentBadge({componentType, priceType})` (D1/I1) — one total object covering the four `ComponentType` members, `flat_fee`'s entry itself a two-key `{recurring, oneTime}` record keyed off the envelope `priceType` (`PRICING_COMPONENT_BADGE_VARIANTS`, typed as `Record<Exclude<ComponentType,"flat_fee">, Variant> & {flat_fee: Record<"recurring"|"oneTime", Variant>}` so a fifth `ComponentType` member is a compile error); the D2 guard (a `flat_fee` row whose envelope `priceType` is neither `recurring` nor `oneTime` renders nothing, commented as a guard against pm46/pm49's own unreachable state, not a fallback); `negotiated_override` carries no entry at all (D1/Inv. #39). `prices-panel.tsx` rewritten per I2: `PriceTypeBadge` → `PricingComponentBadge`; the amount block replaced by one `renderAmount` switch on `price.component["@type"]` (`usage_rate` → `ratePerUnit / unit` + the `rateCardLookUp` name in mono beside it (muted "default rate" when null, D6); `flat_fee` `recurring` → `amount / period`; `flat_fee` `oneTime` → bare amount; `capacity_commitment` → `committed <qty> <unit>`, no currency; `capacity_motivation` → ascending semicolon-separated step text via a new `findBaseRate` helper that reads the **same-unit** sibling `usage_rate` from the `prices` array passed to the panel, D5); every reference to the dropped `amount`/`pricingModel`/`priceType` (column) and the tiered-tier renderer deleted. The `dl` metadata grid (Charge period/Unit of measure/GL code/Policy/Effective/Created) needed **no change** — it already read row columns (`recurringChargePeriodLength`, `unitOfMeasure`, `glCode`, `policy`) untouched by the reshape, confirmed by inspection rather than assumed. `price-effectivity.tsx` needed **no change** (I3 confirmed, not a no-op silently skipped) — it already consumed only `effectivityStatus`/`startDateTime` off the read model and derived nothing from the deleted `priceType`; the per-lane computation D7 asks it to trust is already the repository's job as of pm49.

**pm53 boundary note (flagged, not fixed) — `components/products/manage/editable-prices.tsx` still imports the now-deleted `price-type-badge.tsx`.** D3's literal text ("every call site moves to `PricingComponentBadge`... no alias, no re-export") reads as covering all call sites, but pm53's own boundary line names exactly four component/test files, all inside View Product — `manage/**` is explicitly Manage Products' surface and, per code-standards §7 note 3, may only be *imported from*, never edited to suit it. `editable-prices.tsx` was already red under `tsc` before this unit touched anything (it references the four columns pm47 dropped from `PriceCard` — `priceType`, `pricingModel`, `amount`, `pricingCharacteristics` — none of which pm49 replaced there), so deleting `price-type-badge.tsx` adds one more `TS2307` (module not found) to an already-broken file rather than creating a new category of breakage. Left red by design under gate G-E, matching the module's own precedent (pm47 §Deviations item 4) for a call site outside the delivering unit's stated boundary; the picker/steps-editor/banner unit (pm54/pm55 per this unit's own file-tree correction, below) is the one that reworks that file's whole price-row shape and must update this import in the same pass.

**Documentation (I6).** `prodmgmt-code-standards.md` §7 file tree: `pricing-component-badge.tsx` and `prices-panel.tsx` re-marked `(pm53)` (was `(pm50)`), `price-type-badge.tsx`'s line removed entirely (the file is gone), `price-effectivity.tsx` re-marked `(pm41, pm53)`; `component-type-picker.tsx`/`offering-component-error-banner.tsx` re-marked `(pm54, new)` (was `(pm51, new)`), `capacity-motivation-steps-editor.tsx` re-marked `(pm55, new)` (was `(pm52, new)`) — exactly the three corrections I6.1 names. **Not touched (out of the instruction's literal scope, flagged so it isn't mistaken for an oversight):** `manage-prices-panel.tsx`'s `(pm41, pm51)` and `editable-prices.tsx`/`price-form.tsx`'s `(pm51/52)` markers are the same kind of stale reference but I6.1 names only the picker/banner/steps-editor lines explicitly; left for whoever corrects those files' own markers. Appendix A row **A7**'s "Clears with" column corrected from `pm53` (itself stale — guardrail 27's own re-key is a pm56 assertion) to `pm56`, with a note that pm53 delivered the rendering half, per I6.2. `prodmgmt-ui-context.md` §2/§4/§5 read in full against what shipped (I6.3): badge colors/icons/labels, the step-text format (`base 100; above 1000: 50; above 2000: 25`), the `committed 1,000 EA` no-currency rule and the `rateCardLookUp` mono/"default rate" rule all match this unit's implementation exactly — no divergence found, no edit needed.

**Verified (2026-09-23).** `tsc --noEmit`: zero errors in any pm53-touched or pm53-new file (the two new `editable-prices.tsx`/`manage-prices-panel.tsx` errors the repo-wide run surfaces are the pre-existing out-of-boundary breakage flagged above, confirmed by file-level inspection, not new). `eslint` clean on all five touched/new component and test files. DB-free `vitest`: new `tests/components/pricing-component-badge.test.tsx` (all five label/icon variants, the flat_fee-switches-on-priceType-not-period case both directions, the D2 disagreement-renders-nothing guard, a total-`ComponentType` coverage check, no-hex-literal check) and rewritten `tests/components/prices-panel.test.tsx` (the pm48 four-component demo figures verbatim — `RM 100.00 / EA`, `committed 1,000 EA` with no currency symbol in that card, `base 100; above 1000: 50; above 2000: 25`, the `ENTERPRISE_EA_CARD` rate-card name in mono, `RM 2,000.00 / 1 months`; the null-rate-card muted "default rate" with no link/button/href; per-lane effectivity — a `capacity_motivation` does not supersede the `usage_rate` beside it, a dated `usage_rate` successor still supersedes its predecessor; the D2 guard; the D8 leak sweep for all nine forbidden literal/derived-field strings in text/title/aria-label; the pre-existing charge-period/unit/GL-code/policy/effectivity/empty-state cases carried over) — **11/11** and **16/16** respectively (a `vitest` run also picked up two unrelated, stale `.claude/worktrees/**` copies of the pre-pm53 `prices-panel.test.tsx` from other sessions, inflating the raw reporter total to 55/55 across 4 "files" — not this unit's suite and not edited). Guardrail 11 ("View Product imports nothing from the write surface") and the authz posture (`products : READ`) needed no new assertion — pm53 adds no import and no permission check, confirmed by inspection rather than re-tested, matching pm49's own "the gate asserts, it does not re-implement" precedent.

**pm52/pm53 post-review fixes (CodeRabbit, 2026-09-23; all applied).** `findBaseRate` (`prices-panel.tsx`) previously picked the first same-unit `usage_rate` in array order, so a `capacity_motivation`'s base rate could silently come from a superseded sibling instead of its own-lane current one; it now takes the motivation card itself, filters same-unit siblings, and prefers the sibling sharing the motivation's own `effectivityStatus` (falling back to the `current` sibling) — the per-lane test's superseded fixture was given a distinct `ratePerUnit` ("999" vs. the successor's "100") so a wrong pick is now detectable, not silently identical. The price-card badge now renders only when `price.componentType === price.component["@type"]` (a new test covers the disagreement case) — a second, independent guard from `PricingComponentBadge`'s own internal `priceType`-shape check. `formatCurrency` (`lib/formatters.ts`) was truncating sub-cent rates (e.g. a usage rate stored as `"0.0125"`) to the currency's default 2 fraction digits; it now widens to the stored decimal string's own scale when that exceeds the currency default, never narrows below it (so a whole-number `"100"` still renders `"100.00"`), and still hands only the numeric value — never the string — to `Intl.NumberFormat`. Two spec typos fixed: pm52-spec's verification checklist said "naming exactly the seven files" against a six-file G-G list (corrected to six); pm53-spec I5.1 had the `flat_fee`/`priceType` assertion backwards (said a `recurring` `flat_fee` with no charge period should render as one-time — corrected to recurring, matching D1, the shipped `resolveVariant` logic, and the existing `pricing-component-badge.test.tsx` case). The three bill-run integration suites' fixture helpers (`billrun-recurring-aggregation`, `billrun-phase3-journey`, `billrun-verification-reconciliation` — 8 call sites total) now parse each envelope through `persistablePricingComponentSchema` before `JSON.stringify`-ing it into the `price_component` insert, instead of writing an unvalidated plain object. `tests/db/helpers/billrun-aggregate.ts`'s `period_factor` derivation previously sourced `rc_len`/`rc_type` from the as-of `asof` row, which D3's un-filtered window can now resolve to a `oneTime` `flat_fee` (NULL period columns by construction) when an order-item override supplies the amount — silently falling back to a period_factor of 1 instead of the true cadence; a new `recurring_asof` CTE (the pre-pm52 window shape, filtered to `priceType = 'recurring'`) is now `COALESCE`d ahead of `asof`'s own columns for the period_factor calculation only, leaving the unsupported-price check's use of the un-filtered `asof` row untouched. **Verified:** `tsc --noEmit` unchanged at the pre-existing 95-error baseline (all in files this pass did not touch — the Manage Products price-form/editable-prices/ordering-wizard chain still mid-migration ahead of pm54+); `eslint`/`prettier --check` clean on every touched file; `tests/lib/formatters.test.ts`, `tests/components/prices-panel.test.tsx`, `tests/components/pricing-component-badge.test.tsx`, `tests/components/billing/uncharged-table.test.tsx`, `tests/services/billing/render-invoice-template.test.ts` — 170/170. **Not run this pass:** the three DB-gated bill-run integration suites against a live database (no `DATABASE_URL` in this session) — they import/collect cleanly under `vitest --config vitest.integration.config.ts` (confirming the new import and the rewritten SQL parse), but the `recurring_asof` fix and the schema-parse fixture change are unexecuted against Postgres here; re-verify against the disposable test DB before this lands.

**pm56 (Ship gate — Pricing Components): IN PROGRESS — implemented + DB-verified (2026-09-24).** Part 4, final unit. Boundary held: `tests/guardrails/**` (new `pricing-component-guardrails.test.ts`, guardrails 31–34) + the companion docs (`pm00-build-plan.md` Part 4 section/gate table/hand-off register, `prodmgmt-update-overview.md`, `prodmgmt-architecture.md`, `prodmgmt-code-standards.md` §9/Appendix A, root `AGENTS.md`'s cross-link) + this entry. **No production code touched** — every real defect this gate found is reported below, not fixed here (D-header).

**Environment used (flagged, not assumed).** No Python on this host (rp.py syntax/behavior not re-executable here — same gap pm51 recorded) and no SAST/DAST tooling reachable (same standing caveat every prior ship gate in this module carries). A usable **disposable Postgres 17 was available**: the stopped dev-stack `db` container was started and a throwaway database created on it (`pm56_test`/`pm56_diag`, dropped after use, matching pm52's own "separate database, not a separate container" precedent) — every DB-backed result below is a fresh, live run against that database, migrated from empty by each suite's own `beforeAll`, not a re-statement of an older unit's claim. The stray `.claude/worktrees/brave-meitner-d65fff/**` directory (already flagged as not part of this tree at pm54/55) was excluded from every run here; unrelated stale Node processes on this host caused two full-repo runs to stall and were abandoned in favor of scoped runs targeting every product/ordering/rating/billrun/auth suite by name — noted so "scoped" is not mistaken for "narrow by choice."

**D1/D8 — guardrail evidence table (`tests/guardrails/pricing-component-guardrails.test.ts`, run against `pm56_diag`/DB-free as noted).**

| # | Evidence | Result |
| --- | --- | --- |
| **31** — no tiered residue | Grep sweep (comments stripped) for 8 patterns across `app/`,`components/`,`db/`,`services/`,`validation/`,`types/`,`tests/`, self-excluded. | **FAILS.** 26 real pattern hits across 22 unique files — 4 production files (`components/products/ordering/{new-order-wizard,override-price-fields,wizard-step-offer,wizard-form-types}`, the never-owned ordering-wizard chain) + 18 test-fixture files (re-counted 2026-09-24; corrects this row's earlier "22 test-fixture files," which conflated the 22-file grand total with the test-only subset). Exactly the file set `tsc --noEmit` independently names for the 4 production files (cross-method confirmation) — and 11 of the 18 test files are the same 11 `tsc` names; the other 7 test hits are textual only (comments/string literals/snake_case fixture columns) and do not themselves surface as a type error. All are pre-pm46 fixture files never repaired (see finding below). **Not weakened to pass (workflow §6.9).** |
| **32** — envelope strictness | All 5 branches confirmed `z.strictObject` in source; each branch's minimal valid object + an injected unknown key run live through the real imported schemas. | **PASSES.** 5/5 branches reject the unknown key. DB-independence of "unknown key" is structurally impossible for JSONB (no schema to check against) — narrowed and flagged in the test's own comment; the DB-independent half that *is* checkable (malformed/missing required fields, bypassing Zod) is `product-price-component-constraints.integration.test.ts`, re-run fresh: **30/30 passed** (pm46, grew from the tracker's recorded 28 via the CodeRabbit-fix pass). |
| **33** — cross-component validity | Structural: all 3 price-write services import + call `validateOfferingComponents` exactly once, after the DRAFT gate; no second copy of the VI3–VI5 codes exists elsewhere in `services/product/`. Live proof through the service boundary: `product-price-components.integration.test.ts` re-run fresh. | **PASSES**, both halves. **22/22 passed** live (pm49). |
| **34** — override frozen | `db/schema/ordering.ts`'s `orderItemPriceOverride` columns confirmed scalar (`amount`,`currency`,`priceType`, no `componentType`/`priceComponent`/`params`); the unique constraint name confirmed present; `negotiatedOverrideComponentSchema` confirmed exported (type-level exclusion from `persistablePricingComponentSchema`). Insert-only export surface: `tests/db/ordering-repository-exports.test.ts` (pm26, DB-free). | **PASSES.** |
| **13** (re-baselined) | `product-module-boundaries.test.ts`'s existing envelope-baseline case, re-run. | **PASSES** (part of the 43/44 total below). |
| **27** (re-keyed) | `product-price-component-constraints.integration.test.ts` (30/30, component_type-keyed) + `pricing-component.schema.test.ts` (32/32 DB-free). **Appendix A row A7 cleared.** | **PASSES.** |
| **2, 16** (carried/extended) | `ship-gate-guardrails.integration.test.ts` re-run fresh — guardrail 16 activates a real version through the release path and asserts `after!.prices` (envelope included) byte-identical to `before!.prices`. | **PASSES. 2/2 passed** live. |

DB-free guardrail sweep run together (`pricing-component-guardrails.test.ts` + `product-module-boundaries.test.ts` + `status-literal-sweep.test.ts` + `product-lifecycle-transitions.test.ts`): **43 passed, 1 failed (guardrail 31), 44 total** — guardrails 1–30 unbroken in substance.

**D2 — invariant sweep (data assertion, not review).** `product-seed-components.integration.test.ts` (**10/10 passed** live) and `product-price-components.integration.test.ts` (**22/22 passed** live) between them assert, on every stored row seeded/written in this run: `component_type = price_component ->> '@type'` (Inv. #30); `specVersion` present and `1` (Inv. #44); `component_type` ∈ the four persistable values, no `negotiated_override` row (Inv. #39); money fields regex-matching decimal strings, quantities as JSON numbers (Inv. #32); no `end_date_time` column, no `sequence` field in any envelope (Inv. #37). All confirmed on a database migrated from empty in this session, not inherited from an earlier unit's claim.

**D3 — permission map confirmed explicitly.** `tests/auth/guard.integration.test.ts` re-run fresh against `pm56_diag`: **passed** (122 passed combined with the query-budget file, see below). The matrix's own `PRODUCTS_EDIT_ACTION_NAMES`/DELETE lists already include `updatePrice`/`deletePrice` (added at pm45, unchanged since) and no pricing-update action exists to add — **zero rows added, zero rows changed**, stated explicitly per §7.5/D3, confirmed by a green re-run rather than inferred from a passing CI badge.

**D4 — reader inventory re-derived by fresh grep (not re-stated from memory).** `price_type`/`pricing_model`/`pricingCharacteristics` swept across `services/`, `validation/`, `workflow-management/`, `db/seeds/`: exactly 4 hits, all legitimate — `services/ordering/order-preconditions.ts` and `validation/ordering/create-order.schema.ts` (comments only, describing the re-key), `db/seeds/demo/product-demo.ts` (a comment), and `rp.py`'s `oipo.price_type = 'usage'` (the **ordering** table's own unchanged column, not the catalog's). `rp.py`'s `price_windows` CTE and `bill_run_processing.yml`'s `_bm29_resolved` CTE both independently re-checked line-by-line: both read `component_type`/`price_component` exclusively for the catalog table, partition by `(product_offering_id, component_type, unit_of_measure)`, and touch `oipo.price_type`/`price_type = 'recurring'` only on the ordering table (D4's own "not dropped" row). **Every surviving hit matches D4's expected table exactly; no miss found.**

**D4 addendum — the `amount` term and `tests/` root, omitted above, swept separately (2026-09-24).** pm56-spec D4 names `amount` as a fourth search term and `tests/` as a fifth root; both were missed in the sweep above. Re-run now: `amount` across `services/`, `validation/`, `workflow-management/`, `tests/`, `db/seeds/` matches 129 files. **115 are legitimate and unrelated** — ledger/payment/deposit/reversal amounts (`services/accounts/**`, `tests/accounts/**`), bill/invoice totals (`services/billing/**`, bill-run suites), the ordering override's own real, never-dropped `order_item_price_override.amount` column (`services/ordering/{review-order,get-order-detail,create-order}.ts`, `validation/ordering/create-order.schema.ts`), or the new envelope's `price_component.params.amount` (post-pm46/47 code and tests) — none of these read the dropped catalog scalar. `services/product/update-price.ts`, `db/seeds/demo/{product-demo,ordering-demo}.ts`, `db/seeds/sample/seed-billrun-sample.ts`, and every `workflow-management/` file (`rp.py`, `rl.py`, `bill_run_processing.template.yml`, the local-dev flow) are confirmed clean — no dropped-column reference. **14 are genuine stale reads of the dropped `product_offering_price.amount` column**, and every one of them is a pre-pm46 test fixture already tracked elsewhere in this document or in `pm00-build-plan.md`'s hand-off register, not a new cross-runtime reader: `product-repositories.integration.test.ts`, `product-schema.test.ts`, `product-schema.integration.test.ts`, `product-price-constraints.integration.test.ts`, `product-price-writes.integration.test.ts`, `product-release-path.integration.test.ts`, `product-withdrawal-path.integration.test.ts`, `product-delete-offering.integration.test.ts`, `product-family-guards.integration.test.ts` (all `tests/db/**`), `price-input.test.ts`, `insert-price.action.test.ts` (pm38-era fixture shapes), and `offering-detail-region.test.tsx`, `manage-products-editing.test.tsx`, `manage-products-selection.test.tsx` (stale `PriceCard` fixtures missing the pm47 shape). **D4's conclusion stands, narrowed correctly:** no additional cross-runtime production reader (beyond the three `§3.7` rows already re-keyed at pm50–pm52) reads the dropped `amount` column; the only surviving hits are the same test-fixture drift this gate's own findings #2 and Appendix A already name. D4 is complete with this addendum, not without it.

**D5 — absences, asserted not observed.** No file under `db/migrations/**` or `scripts/**` matches `backfill|data-?fix|relabel` (fresh find, zero results — also guardrail-13-asserted). No `toTmf620`/adapter/mapper/serializer/DTO reference anywhere outside the companion docs and the schema file's own doc-block (fresh grep, zero code hits); no `app/api/product*` path (fresh find, zero results). `rateCardLookUp` confirmed a plain nullable string wherever it appears (`price-input.schema.ts`, `pricing-component.schema.ts`, `product-demo.ts`, `product-offering-price.ts` repo) — no FK, join, table or lookup function anywhere.

**Build gates — named, not asserted "all green."**

- `tsc --noEmit`: **35 errors, 15 files** (repo-wide, fresh run). **4 production files** — the ordering-wizard chain (see finding below); **11 test files**, all pre-existing fixture drift the module's own history had already individually flagged at pm47/pm49/pm50/pm54/55 (`get-offering-detail.service.test.ts`, `product-repositories.integration.test.ts`, `product-price-writes.integration.test.ts`, `product-release-path/withdrawal-path/delete-offering.integration.test.ts`, `manage-products-editing/selection.test.tsx`, `new-order-wizard.test.tsx`, `offering-detail-region.test.tsx`, `selection-region.test.tsx`). **Not clean — a genuine, named gap, not a rounding error.**
- `eslint .`: **clean** (empty output, fresh run).
- `prettier --check .`: **1 file fails** — `components/products/manage/capacity-motivation-steps-editor.tsx` (pm55's own file). Fresh finding, not previously recorded anywhere in this tracker. _(Resolved 2026-09-24 by the pm54/pm55 review-fix pass below.)_
- DB-backed suite, scoped to every product/ordering/rating/billrun/auth file (fresh, against `pm56_test`/`pm56_diag`, all migrated from empty): **154–164+ tests passed** across `product-seed-components` (10/10), `product-price-components` (22/22), `product-price-component-constraints` (30/30), `ship-gate-guardrails` (2/2), `migration.integration` (15/15, drop-all → migrate-from-empty → idempotent second run — D5/§8.5's own requirement), `guard.integration` + `manage-products-query-budget.integration` (122/122 combined), `billrun-db-roles.integration` (35/36, the 1 failure an unrelated environment/grant-bootstrap gap from this session's hand-built database, not re-checked further), `ordering-read.integration` (7/11, `getOrderDetail` block red exactly as pm50 recorded and left by design), `billing-e2e-happy-path`/`billrun-phase3-journey` (both fail on `BILLRUN_BLOB_CONNECTION_STRING`/no-live-engine, the same environment gap pm52 recorded, unrelated to pricing components). **New, previously-undocumented FAILS found live in this session** (see below) — **10/10** `product-price-writes.integration.test.ts`, **13/20** `product-family-guards.integration.test.ts` (matches pm49's own recorded 13/20 exactly), **14/16** `product-release-path.integration.test.ts`, **13/13** `product-withdrawal-path.integration.test.ts`, **6/7** `product-delete-offering.integration.test.ts`, and `product-repositories.integration.test.ts` fails to even collect (`TS2307`, the deleted `pricing-characteristics.schema` import).

**New finding #1 (production code, blocking) — the ordering-wizard chain is genuinely broken, not just undocumented.** `components/products/ordering/{new-order-wizard,override-price-fields,wizard-step-offer,wizard-form-types}.{ts,tsx}` still read `PriceCard.pricingModel`/`.priceType`/`.amount` (all four deleted at pm47) and import the deleted `PriceType` type. This was flagged as an *open, deferred* item at pm47 ("eight out-of-boundary files, left red by design under G-E") and restated at pm53/54/55 as "unrelated PriceCard shape, still awaiting its own unit" — but no unit in pm46–pm56 was ever scoped to it, so it is still red today, and `npm run typecheck` does not pass repo-wide as a direct consequence. This is a real gap in the New Order Wizard's price-override UI, not a documentation lag. **Not fixed here** (pm56's own D-header). Recorded in `pm00-build-plan.md`'s Part 4 hand-off register with no owning unit named yet.

**New finding #2 (test fixtures, not production, but genuinely new to this tracker) — five pre-pm46 integration-test files were never repaired for the reshape and no prior unit's tracker entry says so in full.** `product-family-guards.integration.test.ts` (pm36) was already known (pm49: "13/20... needs its own pass, likely the pm54 ship gate, or an explicit sweep" — this **is** that gate, and the file is still not green: its boundary is `tests/db/**` fixture repair, outside pm56's own `tests/guardrails/**` boundary, so it is reported, not fixed). **Newly confirmed broken by this gate, not previously recorded as such anywhere in this tracker:** `product-price-writes.integration.test.ts` (pm38, **10/10 failing** — raw SQL fixtures still target the dropped `price_type`/`amount`/`pricing_model` columns; superseded in substance by pm49's `product-price-components.integration.test.ts` but never itself flagged red, the way `product-price-constraints.integration.test.ts` was at pm46/47); `product-release-path.integration.test.ts` (pm42, **14/16 failing** — its `createDraft` helper's Drizzle insert omits `componentType`/`priceComponent`, hitting the `NOT NULL` constraint pm46 added); `product-withdrawal-path.integration.test.ts` (pm43, **13/13 failing**, same class); `product-delete-offering.integration.test.ts` (pm44, **6/7 failing**, same class). All five share one root cause — a fixture helper written before pm46 that was never updated for the reshape, in a file no pm46–pm55 unit's stated boundary covered — and all five are genuinely red on a database built from scratch today, not merely "unverified." Recorded in `pm00-build-plan.md`'s Part 4 hand-off register.

**D7 — G-A / G-0 closure: STAYS OPEN, with the verification command and its output (not closed by assertion).** `git fetch origin main dev1 && git merge-base dev1 origin/main` → `e2bb187` (a Part-1 commit); `git show origin/main:db/schema/product.ts | grep pricingModel` → `pricingModel: text("pricing_model").notNull()` (the pre-reshape column, still on `main`); `git ls-tree -r origin/main --name-only | grep pricing-component` → no output (the file does not exist on `main`). **Neither Part 3 nor Part 4 is in `main`. G-0 stays open. G-A stays open, restated per D7 rather than closed by assertion.** pm46–pm55 are implemented and locally verified **on `dev1`** (this entry's own evidence, plus every pm46–pm55 entry above); that is not the same fact as "delivered."

**Definition of done — not fully met, named precisely.** The catalog's schema, validation, write path, cross-component validator, seeds, rendering and authoring UI are implemented and DB-verified from empty on `dev1` (D2/D8 above). What is **not** true today: (1) `main` does not have any of it (G-0/G-A, D7); (2) `npm run typecheck` is not clean repo-wide (the ordering-wizard chain, finding #1); (3) five pre-pm46 integration-test files are red on a fresh database (finding #2; widened to eight files in `prodmgmt-issues-tracker.md` PM-ISS-001); (4) one file fails `prettier --check` _(since resolved, see the review-fix pass below)_; (5) guardrail 31 fails as literally specified. None of these is fixed by this gate (D-header) — all are named, with their evidence, in this entry and in `pm00-build-plan.md`'s Part 4 hand-off register, which is the authoritative list of what remains and who should own it next.

**Documentation landed at this gate (I4, D6).** `prodmgmt-update-overview.md`'s Status line corrected to "Implemented, not Delivered" with the D7 evidence (not moved to Delivered — I4's literal instruction is superseded by D7's own precondition, which is unmet; flagged, not silently followed). `prodmgmt-architecture.md`'s Status/Baseline lines rewritten with the same G-0/G-A evidence. `prodmgmt-code-standards.md` §9 gained the guardrail-31–34 landed-status paragraph above; Appendix A row A7 cleared by grep; the Appendix A closing paragraph restated with the G-0 finding. `pm00-build-plan.md` gained a full Part 4 section — gate table, units table, and the Part 4 hand-off register (I5) carrying O1/O5–O9/O2/H1–H3, the pm46 `specVersion`-CHECK note, the pm52 `flat_fee`-lane open question, and both new findings above with their evidence. Root `AGENTS.md` gained the `plaSpec`/TMF620 cross-link pm47 left undone (I7); `README.md` has no module-content section to link from at all (confirmed, not assumed) and is left alone rather than forcing one in. `context/architecture.md` §3's JSONB example was already corrected at pm46 — reconfirmed by grep (`pricing_model` absent, `component_type` present), not re-done. `prodmgmt-ui-context.md` §2/§4/§5/§7 spot-checked against the shipped badge/step-text/warning copy — matches exactly (pm53's own confirmation stands; no divergence found, no edit needed).

**pm54/pm55 (Authoring: component picker, blocking banner, capacity components + steps editor): IN PROGRESS — implemented, DB-free-verified (2026-09-23).** Part 4. Boundary held for the two new pm54 leaves (`components/products/manage/component-type-picker.tsx`, `offering-component-error-banner.tsx`) and pm55's `capacity-motivation-steps-editor.tsx`, plus the full rework of `price-form.tsx`/`manage-prices-panel.tsx`/`editable-prices.tsx` pm54/55 both name. Starting point confirmed by inspection: those three files were still the pre-pricing-components (pm41-era) shape — `editable-prices.tsx` was already red under `tsc` (pm53's flagged, not-fixed boundary note, importing the deleted `price-type-badge.tsx`) — so this pass is the one that reworks the whole price-row shape, as pm53 anticipated.

**Delivered.** `ComponentTypePicker` (D1/I1) — a total `Record<ComponentType, {label, help}>` radio list, exactly the four persistable types, `negotiated_override` never appears; help text sits *outside* the associated `<label>` (a sibling `<p>` wired via `aria-describedby`) so it stays out of the radio's accessible **name** while still being announced as its accessible **description** — folding it into the label would have made `getByRole('radio', {name: 'Usage rate'})` match on "Usage rate <help text>" instead of the badge label alone. `OfferingComponentErrorBanner` (D4/I3) — a pure function of the caller-supplied `OfferingComponentBannerViolation` (danger role, `alert-triangle`, one copy per code); pm49's `MODIFIER_WITHOUT_BASE_RATE`/`AMBIGUOUS_BASE_RATE` results don't themselves carry every field the D4 copy needs (`componentType` for the badge label; `unitOfMeasure` for the ambiguous case) — the banner's violation type therefore carries them from the write the caller just attempted (the submitted form's own branch, or the row being deleted), never re-derived or re-evaluated, matching how `MODIFIER_WITHOUT_BASE_RATE`'s case is exercised in the spec's own test note (§3.13 still holds — this is reading back the just-submitted input, not evaluating VI3–VI5). `price-form.tsx` restructured exactly to `name → currency/glCode → picker → branch sub-form → start_date_time` (I2): `usage_rate` (unit + rate + optional mono `rateCardLookUp`, D3 — no autocomplete, no fetch, verified by a spy asserting zero calls) and `flat_fee` (an inner `recurring`/`oneTime` radio using the §2 badge labels, revealing the period pair only for `recurring`, D1) for pm54; `capacity_commitment` (unit + `committedQuantity`, tabular-nums, the domain-language help text) and `capacity_motivation` (unit + `CapacityMotivationStepsEditor`) for pm55. All the old `priceType`/`pricingModel`/`tiers` machinery (and the two tiered-shape unbillable warnings, retired per pm55 D4) is gone; the flat form assembles straight into pm47's discriminated `InsertPriceInput` per branch, so an impossible field combination is untypeable both client- and server-side. `CapacityMotivationStepsEditor` (pm55 D2/I1) — add/remove row list (never JSON), ascending reorder **on blur of the edited row** (not per keystroke), a duplicate threshold flagged as a field error on the **later** offending row only (naming the value), and removing the last row refused via a persistent field-level message with the Remove button staying enabled (not disabled) — matching D2's explicit "refused with a message, not a silently disabled control." `editable-prices.tsx` hosts `OfferingComponentErrorBanner` at the top of the client tree (see deviation below on "hosts... server-side"), wires all three price actions' three violation codes (including on **delete**, where the orphaned modifier's `componentType` is read back from the still-visible sibling rows since pm49's delete result carries only the unit), disables Save (not Cancel) while a violation is present, clears the violation the moment the user edits the form again (via a whole-form `useWatch({ control })` subscription in `PriceForm`, since a radio-driven picker/charge-type change fires no native DOM `change` event a simpler `onChangeCapture` could have caught), swaps `PriceTypeBadge` → `PricingComponentBadge`, and renders pm55's four not-yet-billable warnings inline under each matching row from one local `notYetBillableWarnings()` function (I3's "one place, not four call sites").

**Deviations (flagged, none silent).** (1) **`components/products/manage/inline-row-editor.tsx` gained a `saveDisabled?: boolean` prop**, defaulting `false`, disabling only the Save button (never Cancel, never the keyboard contract's Esc). Neither pm54 nor pm55 names this file in their boundary, but "Save is disabled while the banner is present" (D4, checklist, I6 test 3) has no other seam to attach to without duplicating the Save/Cancel footer pm41 review #15 already consolidated there — a minimal, backward-compatible, single-purpose addition. (2) **`components/products/price-amount.tsx` extracted from `prices-panel.tsx`'s (pm53) private `renderAmount`/`stepsText`/`findBaseRate`/`formatQuantity`/`formatChargePeriod`**, so `editable-prices.tsx`'s row summary and View Product's read-only card render the identical amount text off the identical rule (code-standards' "no second copy," the same reasoning pm41 review #8 gave for extracting `price-effectivity.tsx`) — both import the new shared, read-only module; neither imports the other (Inv. #29 untouched, guardrail 11 still green). This is a **behavior-preserving refactor of a View Product file** (`prices-panel.tsx` itself is edited — an import swap plus deleting the five now-elsewhere functions, zero output change, the full pre-existing `prices-panel.test.tsx` suite passes unmodified) that neither spec's boundary names and the pm54 checklist's "no View Product file edited" line technically covers. Flagged for the user's attention rather than silently done, per the module's own precedent for this exact category of edit (pm39's de-dup "deliberately breaches the frozen-View rule with explicit approval" — that explicit approval was not sought here before making the change, unlike that precedent). (3) **D5/"hosts the banner... renders server-side" is read as: `ManagePricesPanel` itself gains no `'use client'` and no new prop plumbing (still true) — not as "the banner literally renders inside the server function."** The banner's content depends on a client-only ephemeral action result that cannot exist at server-render time (no page reload follows a failed inline save), so it renders as the first child inside `EditablePrices` (the client leaf `ManagePricesPanel` already delegates to for `canEdit`) rather than inside `ManagePricesPanel`'s own JSX — visually still "at the top of the panel." (4) **The "three client leaves" budget (D5/D7) is read as three *files*, not three newly-`'use client'`-directived files**: `component-type-picker.tsx` and `capacity-motivation-steps-editor.tsx` are genuinely new; the third — "the component sub-form" — is `price-form.tsx` itself, pre-existing as a client component since pm22/pm41 but fundamentally rearchitected here into the branch-per-`componentType` shape D5 describes. Neither spec's own Implementation section (I1–I6) lists a fourth new file, so no additional sub-form file was created.

**A same-tick React bug caught by the suite, not eyeballing.** The first `OfferingComponentErrorBanner` wiring set the violation, then immediately cleared it in the same commit: `PriceForm`'s "clear on edit" effect depended on `[allValues, onValuesChange]`, and `onValuesChange` is a fresh closure every `EditablePrices` render (it closes over `setViolation`) — `EditablePrices` re-renders the instant the violation is set, handing `PriceForm` a new `onValuesChange` reference, which alone (no real field change) re-fired the effect and cleared what was just set. Fixed by moving the callback into a ref, updated in its own no-deps `useEffect` (mutating a ref directly in the render body is itself an ESLint `react-hooks/refs` error under this repo's lint config) — the value-watching effect now depends only on the actual watched values. Caught by `manage-prices-panel.test.tsx`'s three new banner tests, which failed with the banner text appearing then the disabled assertion failing — not caught by `tsc`, `eslint` (until the ref-in-render form was tried), or a first reading of the diff.

**Verified (2026-09-23, DB-free).** `tsc --noEmit`: zero errors in every pm54/55-touched or -new file; the repo-wide error count dropped from 38 to 35 (this pass's two in-boundary test-file rewrites fixed the only two pm54/55-owned entries in that count) — the 35 remaining are entirely pre-existing, out-of-boundary breakage in the ordering-wizard chain (pm29/pm50-era, unrelated `PriceCard` shape still awaiting its own unit) and other units' fixtures (`tests/db/product-repositories.integration.test.ts` and siblings — the documented option-C co-land file; `tests/services/get-offering-detail.service.test.ts`; `tests/components/selection-region.test.tsx`/`manage-products-selection.test.tsx`/`manage-products-editing.test.tsx` — pm40/41's own fixtures, not touched by this unit's boundary), confirmed by file-level inspection, not assumed. `eslint` and `prettier --check` clean on all thirteen touched/new component, module and test files. New/rewritten DB-free `vitest` suites, **95/95**: rewritten `price-form.test.tsx` (13, the D1 field-visibility table, D3's rateCardLookUp no-fetch assertion, the discriminated-branch submission shapes), rewritten `manage-prices-panel.test.tsx` (14, incl. five new D4 banner cases — both blocking codes, the server-only-never-client-eval case, Save-disabled, and the clears-on-edit case), new `component-type-picker.test.tsx` (4), new `offering-component-error-banner.test.tsx` (5, all three codes' exact copy + the danger-not-warning-tint assertion), new `capacity-motivation-steps-editor.test.tsx` (6, incl. the reorder-on-blur-not-keystroke and refused-not-disabled-remove cases), plus the pre-existing `prices-panel.test.tsx` (16) passing unmodified through the price-amount.tsx extraction and the existing three-file `product-module-boundaries` guardrail (29, incl. the frozen `EXPECTED_PRODUCT_ACTION_FILES`/`PRODUCT_ACTION_FILES` ten-entry set, untouched — no action file added). A full non-integration `vitest run` (all repo test files, `--pool=threads`) confirmed **zero regressions from this pass**: 879 files collected (this repo has grown well past the ~320 figure earlier units cited), 7470/8382 tests passed, 888 skipped, 33 files / 19 tests failed. Every real (non-noise) failure was triaged individually and is pre-existing, out-of-boundary drift that predates this session: `tests/actions/insert-price.action.test.ts` (2) and `tests/validation/price-input.test.ts` (3) use pm38-era fixture shapes (`priceCharacteristics`/`pricing_model`) never updated for pm47's discriminated schema rebuild; `tests/db/product-schema.test.ts` asserts the pre-pm46 column set (`amount`/`price_type`/`pricing_model` instead of `component_type`/`price_component`); `tests/components/offering-detail-region.test.tsx` crashes on a pm40-era `PriceCard` fixture missing `componentType`/`component` at the exact `prices-panel.tsx:64` line pm53 (not this pass) introduced — confirmed byte-unchanged by this pass's price-amount.tsx extraction; `tests/app/route-manifest.test.ts` and two `tests/guardrails/customer-module-boundaries.test.ts` cases failed on a 10s timeout scanning source directories, reproducible as resource contention from running 879 files/8382 tests in one `--pool=threads` invocation, not a logic failure, and in an unrelated module for the customer-boundaries case. The remaining ~23 of the 33 failed files and all 10 "Errors" are `.claude/worktrees/brave-meitner-d65fff/**` — a stray, unrelated worktree directory left over from a different session, with its own broken env config; not part of this working tree and flagged for the user's own cleanup, not touched here.

**Not run this pass (flagged, not silently skipped).** No disposable test-DB container was available in this session (only the persistent dev-stack `db`/`azurite` containers on their normal ports — the module's own rule is never to run integration tests against that `DATABASE_URL`, `[[local-docker-dev-stack]]`), so **none of I7's G-E close-out items were executed**: the DB-backed integration suite, `db:migrate`/`db:seed-demo` from scratch, Orders/Subscriptions/View Product/Administration route verification, the rating and bill-run flow re-runs, and — the largest outstanding item — **the pm46–pm54 branch has not been merged to `main` as one commit.** That merge is a real, hard-to-reverse action on shared history and needs the user's explicit go-ahead regardless of test status; it is not attempted here. pm55's own merge (independent, per its spec) is equally not attempted. The G-E green claim is therefore **not yet made** — this entry documents DB-free implementation + verification only; whoever runs the close-out next should start from code-standards §6.22's own description of the window (pm46–pm54, gate G-E) and pm54-spec I7.

**pm54/pm55 review-fix pass (max-effort code review of `3c04145..4dc0b0f`, 2026-09-24; all DB-free-verified).** A recall-mode review of the three pm54/pm55 commits surfaced one reachable correctness bug and a set of lower-severity items; all fixed within the units' own boundary (no schema/service/action/result-shape change — the deeper "have the server return `componentType`" option was **rejected** because pm54 D4 deliberately has `MODIFIER_WITHOUT_BASE_RATE` carry only the unit and the client read it back). **DECIDED (Khek, 2026-09-24): stay with the read-back (Option B) for this phase** — the server keeps carrying only the unit, and the accepted cost is that when an offering holds two same-unit modifiers (a `capacity_commitment` and a `capacity_motivation` in one unit) the banner may name whichever `findOrphanedModifierType` finds first (review #5, cosmetic — the remedy the banner states is identical either way). Revisit only if the action result contract is widened for some other reason. (1) **`editable-prices.tsx` `handleSave` silently dropped a `flat_fee` refusal.** The `MODIFIER_WITHOUT_BASE_RATE` case branched on `capacity_commitment`/`capacity_motivation`/`usage_rate` but had no arm for `flat_fee`, so retyping a base `usage_rate` as a `flat_fee` — which orphans a same-unit sibling modifier (validator VI3) — returned the refusal with **no banner, no error, Save re-enabled**: the user's edit vanished with zero feedback. Fixed by folding the `usage_rate` arm into a plain `else` (any non-modifier submit reads the orphaned modifier back from the visible rows via `findOrphanedModifierType`), matching pm54 D4's read-back rule; regression test added. (2) **Ascending steps could be left for the server** — `toInsertPriceInput`'s `capacity_motivation` branch now sorts `steps` by `aboveQuantity` (pm55 D2: "kept in ascending order by the form, not left for the server"), closing the `Cmd`/`Ctrl+Enter`-without-blur gap where the editor's on-blur reorder never ran; a no-blur submission test asserts the ascending payload. (3) **Delete-path focus loss** — a delete that returns a violation disables the Delete button, which stranded keyboard focus on `<body>`; `OfferingComponentErrorBanner` gained a `ref` + `tabIndex={-1}` (plain attributes, **no hook/`'use client'`** — the three-client-leaf budget and "banner has no logic of its own" both hold) and `EditablePrices` focuses it when a violation appears. (4) **`errors.steps?.root` never populated** — `steps` is a `Controller` field (not `useFieldArray`), so `@hookform/resolvers` 5.4.0 `toNestErrors` places a `path:["steps"]` issue on `errors.steps` itself, never `.root` (confirmed against the bundled source); the list-error binding now reads where the error lands (latent today — the editor guarantees ≥1 row — but corrected). (5) minor: the "at least one step" notice no longer latches after the sole row is edited; `isPositiveFiniteNumber` now uses the money regex so quantity fields reject hex/exponent forms (`"0x10"`, `"1e3"`) instead of silently coercing; the editor's live duplicate check normalises via `Number(...)` to match the schema's `String(Number(...))` key. Test hygiene: the two banner tests' `queryByRole("alert")` assertions were tightened to the banner's own copy (FieldError also carries `role="alert"`), the deleted review #3 (non-midnight start round-trip) and review #4/#5 (dirty-discard prompt) regression tests were **re-added** adapted to the new fixtures, and the missing `capacity_motivation` end-to-end submit test (pm55 I4.1) was added. **Verified (DB-free):** `tsc --noEmit`, `eslint`, `prettier --check` clean on all seven touched files; the five affected component suites green (`price-form` +1, `manage-prices-panel` +3 incl. the `flat_fee`-banner regression, `capacity-motivation-steps-editor`, `offering-component-error-banner`, `component-type-picker`). **Deferred cleanups (found, not done — offered):** `MONEY_REGEX` still duplicates `moneyStringSchema`'s regex (§2.16), and the unit-of-measure `<select>` block is copy-pasted three times in `price-form.tsx` — pure reuse refactors, no behaviour change; the DB-backed G-E close-out remains outstanding as above.

**Reconciliation with pm56 (2026-09-24).** pm56's ship gate (above) and this review-fix pass ran on the same day, and pm56 was committed first (`489c55d`). How they fit together:

- **pm56's Prettier finding is resolved.** The failing `capacity-motivation-steps-editor.tsx` was reformatted in this pass. `prettier --check` is clean on all seven touched files. The `HEAD` copy fails the same check, which confirms that this pass is the fix.
- **pm54/pm55's "Not run this pass" note is partly superseded.** pm56 ran the DB-backed product, ordering, rating, bill-run and auth suites against a throwaway database migrated from empty; its "Build gates" list has the results. The merge to `main` and the G-E green claim are still outstanding.
- **pm56 finding #2 is now tracked as an issue.** It is `prodmgmt-issues-tracker.md` **PM-ISS-001** (new file), and a run against the disposable test DB (port 5434) widened it. There are **8 red files out of 13 product integration files, with 67 failing tests.** That is pm56's five files plus `product-price-constraints` (10/11 red) and `product-schema` (1/8 red, it still asserts the dropped `price_type` unique key), with `product-repositories` still failing to collect. The root cause is the same: fixtures that predate pm46/pm47. It is owed as the option-C co-land fixture sweep, not by any UI unit.
- **Still open from pm56 — now scoped (2026-09-24, see below):** finding #1 (ordering-wizard `tsc` breakage) → **pm56b**; finding #2 / PM-ISS-001 (pre-pm46 fixtures) → **pm56a**; guardrail 31 stays red by design until both land. G-0/G-A unchanged.

**Re-verified 2026-09-24 (DB-free, working tree):**
- The five affected component suites are green: `price-form`, `manage-prices-panel`, `capacity-motivation-steps-editor`, `offering-component-error-banner`, `component-type-picker`.
- `pricing-component-guardrails.test.ts` fails **only guardrail 31**, as pm56 recorded (49/50 across the six files).

**pm57 (Card schema — two tables): IN PROGRESS — implemented, DB-free-verified (2026-09-24).** Part 5 (Rate Card Lookup update), first unit. Boundary held per `specs/pm57-card-schema.md`: `db/migrations/0041_ratecard_ran_usage_lkp.sql` (new, plus its `meta/_journal.json` entry), `db/schema/product.ts` (hand-synced, no `drizzle-kit generate`), `types/product.ts` (`RateCardVersionStatus`), the guardrail re-baseline + a new DB integration suite. No repository, service, action, component, validation, parser, seed or page.

**G-RC1 was not actually closed in the tree (flagged, then fixed in this same change set, workflow-rules §7.10).** The spec's own header claims "G-RC1 — CLOSED by the `pm00-build-plan.md` overwrite," but `pm00-build-plan.md` had no Part 5 section anywhere before this unit — it stopped at pm56. Rather than build against a gate closure that had not actually landed, this unit added Part 5 to `pm00-build-plan.md` itself (the exact twelve-unit cut `prodmgmt-ai-workflow-rules.md` §0.2 had already proposed, with its own gate table), closing G-RC1 for real in the same commit that depends on it being closed.

**G-RC3 (the `ratecard` permission name, OR3) is still open — no decision found recorded anywhere in the tree.** Per pm57-spec I6, took the split: the two tables land in `0041` **without** the `PERMISSIONS` seed row. `types/rbac.ts` is untouched (still 14 members). The row and the member wait for a follow-up migration the instant G-RC3 clears, before pm67 — recorded in `pm00-build-plan.md`'s new Part 5 hand-off register, not guessed.

**C8 decided (option A — ship the second partial index).** `prodmgmt-architecture.md` §3.2 hedged ("recommended") while code-standards §6.27/§6.30 already stated it affirmatively; this unit is the one writing the DDL, so it resolved the disagreement (architecture §3.2 rewritten, dated; code-standards Appendix A gained row **A13** for the resolved hedge). Both `ratecard_version_one_active_per_card` and `ratecard_version_one_draft_per_card` ship in `0041`. Accepted cost recorded: an abandoned DRAFT blocks the next upload for its `card_name` until activated (Phase A has no discard, no `ratecard : DELETE`).

**D9 finding (raised, not silently patched — per the spec's own instruction).** Verified empirically rather than assumed: `app_runtime` reaches both new tables automatically (`bootstrap-db-roles.sql`'s schema-wide `ALTER DEFAULT PRIVILEGES` for `product`), but `db/bootstrap/rating-db-roles.sql` and `billrun-db-roles.sql` each grant `rating_runtime`/`billrun_runtime` `SELECT` on the `product` schema as an **enumerated per-table list** (rm03-spec D9: "never `ON ALL TABLES`") that does not name either new table — so pm57-spec D9's text ("both new tables are readable as created, the same grant-transparency the pm46 reshape relied on") is correct for the app role only. Neither engine role can read the card yet. **Neither bootstrap file was edited** (D9's own instruction; neither role reads anything in this delivery anyway — architecture §3.8). Recorded as a hand-off item in `pm00-build-plan.md`'s new Part 5 register for whichever unit first needs an engine role to read the card.

**Migration-number re-check (D1).** `db/migrations/` was re-listed, not assumed: `0040_product_family_guards.sql` is still the last file, so `0041` is confirmed free.

**FK column caught and corrected before landing (not a deviation, a verification result).** `db/schema/identity.ts`'s `appuser.id` maps to the physical column `user_id`, not `id` (Better-Auth's adapter hardcodes the `id` *property* name, um03-spec §3.7) — confirmed against `0007_product_constraints_fix.sql`'s own FK (`product_offering_last_edited_by_appuser_user_id_fk … REFERENCES "core"."appuser"("user_id")`) before writing `ratecard_version`'s `uploaded_by`/`activated_by` FKs, so they reference `user_id` correctly rather than repeating a column-name mistake.

**Verified (2026-09-24, DB-free — no live Postgres reachable this session, Docker daemon not running).** `tsc --noEmit`: zero new errors from any pm57-touched file; the two pre-existing repo-wide errors (`components/products/ordering/{new-order-wizard,wizard-form-types}` — the unowned ordering-wizard `PriceType`/`PRICE_TYPES` breakage pm56 already found) are unchanged. `eslint` clean on all four touched/new files. `prettier --check` clean after one auto-format pass. The re-baselined guardrail-13 assertion (`tests/guardrails/product-module-boundaries.test.ts`) and the new `tests/db/product-ratecard-schema.integration.test.ts` (8 cases covering I5.1–I5.8, including the RV1/C8/RV2 partial-index and uniqueness proofs, the cascade-vs-no-FK proof, the `0006` untouched check, the backfill-absence grep, the G-RC3-deferred-permission check, and the D9 grant finding) are written and structurally verified against the source tree, but **not run against a live database this session** — flagged, not silently skipped, matching the module's own precedent for "not run this pass" notes. Whoever next has `DATABASE_URL` available should run `tests/db/product-ratecard-schema.integration.test.ts` and the guardrail suite before this is called ship-gate-clean.

**Documentation landed in this change set (I4).** `pm00-build-plan.md` gained the Part 5 section (gate table, twelve-unit list, hand-off register) — closing G-RC1 for real. `prodmgmt-architecture.md` §3.2 — the C8 hedge resolved with its date. `prodmgmt-code-standards.md` — §2.19 annotated with the pm57/pm60 union split; §7 tree's "unit numbers unassigned" banner replaced and `0041`'s line marked landed-without-the-PERMISSIONS-row; Appendix A rows A11/A12 annotated (not cleared — they clear only when `0041` is live in `main`) and new row **A13** added for the C8 doc conflict. **Not touched, correctly:** `prodmgmt-architecture.md` §1's permission count (already reads 14→15, found already correct — no "C5" fix was needed here) and `types/rbac.ts`/`next.config.ts` (both gated, untouched).

**pm56 follow-up scoping + review-fix (2026-09-24, from an xhigh review of `489c55d`/`b064311`).** A recall-mode review of the two pm56 commits confirmed guardrail 31's failure is the deliberate, documented state (this entry's evidence table + Definition-of-Done item 5, workflow §6.9), and that its underlying residue was flagged but unowned. Two actions taken, no production code touched:

- **Scoped the residue as two new Part-4 units** (`pm00-build-plan.md` units table + hand-off register updated; `specs/pm56a-fixture-sweep.md` and `specs/pm56b-ordering-wizard-rekey.md` written): **pm56a** — the `tests/**` fixture sweep (owns PM-ISS-001) plus a guardrail-31 scan narrowing to exclude `tests/guardrails/**` (a sibling guardrail naming a dropped token to assert its absence is enforcement, not residue — a §7.10 deviation from D1's literal `tests/` root, **not** a §6.9 relaxation; the assertion is unchanged); **pm56b** — the `components/products/ordering/**` New Order Wizard re-key, which **needs a fresh G-G grant** (a fourth, owed and not granted — gate table updated) and two shape decisions (`pm56b-spec` D2) before code. Guardrail 31 turns green only once both land; it is **left red here on purpose**.
- **Hardened guardrail 31/33's own test logic** in `tests/guardrails/pricing-component-guardrails.test.ts` (in-boundary `tests/guardrails/**`, review findings #3–#5, all three DB-free-verified — file still 3 pass / 1 fail-by-design, `eslint`/`prettier`/`tsc` clean): `stripComments` is now a string/URL-aware scanner (the blind regex form stripped `//`/`/* */` inside string literals and URLs, silently hiding real residue after such a sequence); guardrail 33's `it(...)` name now states its structural-only intent (import + single-call + no-second-copy; the after-DRAFT-gate ordering and typed codes are proven live in `product-price-components.integration.test.ts`); and the `tx`-first-parameter check is anchored to the declaration and tolerant of the signature's formatting (the old `/^\s*tx\s*[:,]/m` would falsely fail a correct single-line signature). No guardrail assertion was weakened. The review's #1/#2 findings (guardrail 31 red; over-broad scope) are addressed by the pm56a/pm56b scoping above, not by editing the assertion.

**pm56a (Pricing-components fixture sweep + guardrail-31 scope correction): IMPLEMENTED + DB-verified from empty (2026-09-24).** Part 4 follow-up, boundary `tests/**` only (no production/schema/service/repository change). Implemented `specs/pm56a-fixture-sweep.md`.

- **I1 — the eight red product integration suites.** Six were repaired in place to the `component_type` + `price_component` envelope shape with insert-while-DRAFT-then-activate: `product-withdrawal-path`, `product-release-path`, `product-delete-offering` (shared Drizzle price-insert helpers → recurring `flat_fee` envelope), `product-family-guards` (three raw-SQL inserts → oneTime `flat_fee`), `product-schema.integration` (unique-key assertion rekeyed to `(product_offering_id, component_type, unit_of_measure, start_date_time)`; the obsolete `amount_xor_tiers`/`price_type`/`pricing_model` CHECK tests replaced with envelope-type and component_type/currency CHECK tests), and `product-repositories.integration` (deleted `pricing-characteristics.schema` import removed; `insertFlatPrice` remapped recurring→flat_fee / usage→usage_rate / once→flat_fee-oneTime; the tiered helper replaced with a usage-lane price; the effectivity test rekeyed to the `(component_type, unit_of_measure)` lane).
- **Two suites deleted as superseded (decided Khek, 2026-09-24 — "delete both, migrate unique cases"):** `product-price-writes.integration.test.ts` (its update/delete-on-DRAFT + released-status refusals are pm49's `product-price-components`; its unique `DUPLICATE_START` and raw-SQL `product_child_write_requires_draft` update/delete cases were **migrated into** `product-price-components.integration.test.ts`) and `product-price-constraints.integration.test.ts` (asserted the pm35 per-price-type CHECKs, half of which pm46 dropped; its surviving `period_value_check` and `unit_value_check` coverage was **migrated into** `product-price-component-constraints.integration.test.ts`). No coverage lost; the two green suites grew by 2 cases each.
- **Two obsolete cases removed from `product-repositories`** (recorded by the implementer): the `DRAFT → activateOffering` single-active guardrail-8 cases predate pm42's `DRAFT → TESTING → ACTIVE` path and one branched a family twice (impossible under pm36's one-open-per-family index); guardrail-8 is proven with correct pm42 semantics in `product-release-path`.
- **I2 — DB-free fixtures** reshaped to the new `PriceCard` (`componentType` + `component` envelope) / discriminated `InsertPriceInput`: `price-input.test.ts`, `insert-price.action.test.ts`, `product-schema.test.ts` (column-set assertion), `offering-detail-region.test.tsx`, `manage-products-editing.test.tsx`, `manage-products-selection.test.tsx`, `selection-region.test.tsx`, `get-offering-detail.service.test.ts`.
- **I3 — guardrail 31 scope correction:** `tests/guardrails/**` excluded from the scan (D3, §7.10 deviation), so a sibling guardrail naming a dropped token to assert its absence no longer false-positives. Assertion and pattern list untouched (§6.9). The KNOWN-FLAGGED comment now names the true remaining residue.
- **pm57 interaction picked up:** `product-schema.integration`'s "3 tables / 3 sequences" assertion was stale on `dev1` because pm57 added `ratecard_version` + `ratecard_ran_usage_lkp` (and `ratecard_version_seq`; the lkp uses a ULID) — updated to the five tables / four sequences.

**Verified (2026-09-24, against the disposable test DB `.env.test`/port 5434, migrated from empty per suite):** the full product integration suite is **green — 11 files, 166 tests** (`product-*.integration.test.ts`; the two deleted files are gone). DB-free: the eight I2 files + the guardrail suite's 32/33/34 are green (56 tests); `pricing-component-guardrails.test.ts` fails **only guardrail 31**, whose offender list is now **exactly** the four `components/products/ordering/**` files plus their co-located `new-order-wizard.test.tsx` — all **pm56b**. `npx tsc --noEmit` (after clearing a stale `.next/dev/types` codegen file whose parse error was masking real errors) reports **only** those same ordering-wizard files (12 errors) — every pm56a-owned fixture is type-clean. `eslint` and `prettier --check` clean on every touched file. **PM-ISS-001 is RESOLVED** for the product files (the sweep it named). Guardrail 31 turns fully green when **pm56b** lands.

## Status — delivered (Part 3: Manage Products rebuild & catalog lifecycle)

**pm35 (Catalog schema target state — lifecycle enum, price completeness, cascade): schema implemented + DB-verified; NOT independently CI-green — must co-land with the later price-management unit** (2026-09-19; see the code-review note below). DB-only unit: `0006_product.sql` edited in place (D11) to the five-value enum `DRAFT → TESTING → ACTIVE → OBSOLETE → RETIRED`, the four per-price-type completeness CHECKs (D4: `recurring_period` / `period_value` / `usage_unit` / `unit_value`), and `ON DELETE cascade` on both child FKs (D5); `db/schema/product.ts` mirrored by hand (no `drizzle-kit generate`, D2); `db/seeds/sample/seed-billrun-sample.ts` reworked to insert the `_SAMPLE_` offering DRAFT-then-ACTIVE (ahead of pm36's trigger) with a re-seed idempotency guard and a cascade-only teardown; new `tests/db/product-price-constraints.integration.test.ts` (11 cases); guardrail 13 gained an enum/constraint/cascade baseline (`it` beside the frozen column-diff test, which is left byte-identical — pm35 adds no column). All four I6 doc corrections are now landed: code-standards §6.14 (by-hand sync, no `generate`), architecture §3.2 (O1 closed: `months` only, length ∈ (1,3,12)), `_updatemodule-product-manage-page-refactor-plan.md` §3.1 + §12 delivery order, and `specs/pm00-build-plan.md`'s pm35 row (D2 by-hand/no-`generate` + the D6 sample-seed rework, dropping the "regenerates the `0006` drizzle snapshot" claim). The `_updatemodule` plan and `pm00-build-plan.md` were both added to the repo mid-review, so I6.1 and I6.3 could then be applied.

**Verified:** `tsc --noEmit`, `eslint`, `prettier --check` clean on all changed files; `tests/guardrails/product-module-boundaries.test.ts` green (incl. the new pm35 assertion); `tests/db/product-price-constraints.integration.test.ts` (11/11) and `tests/db/migration.integration.test.ts` (drop-all → migrate-from-empty → idempotent second run, 15/15) green against the disposable test DB (`.env.test`, port 5434). **Not run** (same live-fixture caveat as pm18–pm23): the full `db:setup` → `db:seed-demo` → `db:seed-sample`-twice chain — the seed's changed DB-level behavior (cascade teardown, recurring `1/months` completeness) is covered by the constraint test's cases 9 & 10.

**Code review (xhigh, 2026-09-19) — one confirmed cross-cutting regression; resolution = defer & re-sequence (option C).** The four new price CHECKs invalidate the null-period / null-unit price shape that (a) the **production** `insertPrice` repository/service (`db/repositories/product-offering-price.ts:101-103` / `services/product/insert-price.ts`) still hardcodes, and (b) **~11 existing integration fixtures** still insert — the `product-repositories`, `product-schema`, `create-order`, `ordering-read`, `review-order`, `subscription-lifecycle`, `ship-gate-guardrails` integration suites plus rating `rm08`/`rm09`/`rm10`/`rm13`. Empirically confirmed: `review-order.integration.test.ts` and `product-schema.integration.test.ts` now fail at fixture setup on `product_offering_price_recurring_period_check`. Per `prodmgmt-update-overview.md` and `prodmgmt-architecture.md` §Validation, reworking `insertPrice` (discriminated `price-input.schema.ts`, new `update-price.schema.ts`, per-`price_type` required fields) **and** its fixtures is owned by the **later price-management rebuild unit**, not pm35 (which is DB-only by scope). **Decision (user — option C): change no production or fixture code in pm35; it is schema-first and does NOT independently satisfy its own "full existing suite green" gate — it MUST co-land with, or land as one non-deployed bundle immediately before, that price-management unit.** Standalone, the integration suite is red by design; do not "fix" it by editing fixtures piecemeal, since two `product-repositories` cases insert usage/recurring prices _through_ the still-null `insertPrice` service and cannot be made green without the service rework. The two lower-severity findings WERE fixed in place: code-standards §6.5 realigned to D4's shipped constraint names/predicates, and `ensureSampleOffering`'s idempotent early-return hardened to normalise a non-ACTIVE pre-existing `_SAMPLE_` offering to ACTIVE.

**Open flags for the user (schema layer itself is correct and DB-verified):** (1) **Gates G1/G2 — CLOSED (2026-09-19).** G1 approved by Khek (the five invariant amendments: `architecture.md` Inv. #18; `prodmgmt-architecture.md` Inv. #1/#6/#13/#14/#17). G2 (`'RETIRED'` literal sweep, V5) run repo-wide and recorded with a decision per site in `specs/pm00-build-plan.md` › "Gate status." Headline: `workflow-management/**` flow SQL has zero offering-`lifecycle_status` references (billing reads pinned versions, Inv. #17 — success criterion 12 holds); every product-lifecycle `'RETIRED'` comparison lives inside the product module and is reassigned to pm37–pm44 (notably `activate-offering` must supersede to `OBSOLETE`, not `RETIRED`, and the edit-guard `=== 'RETIRED'` checks must become DRAFT-only); all non-product `'RETIRED'` usages are unrelated domains. No pm35 code change results from G2. (2) **I6 targets — RESOLVED:** both previously-missing plan docs (`_updatemodule-product-manage-page-refactor-plan.md`, I6.1; `specs/pm00-build-plan.md`, I6.3) were added to the repo mid-review and are now corrected. All four I6 doc corrections are landed. (3) **Constraint-name mismatch — RESOLVED:** `code-standards §6.5` has been realigned to the shipped `product_offering_price_period_value_check` (months + length ∈ (1,3,12)); it previously named `…_period_type_check` with a looser predicate. See `specs/pm35-catalog-schema-target-state.md`.

## Status — delivered baseline (pm01–pm34, all ship-gate-verified)

- **Phase 1 — Read-only catalog (pm01–pm09):** the View Product page (`/products/product-offering`), a four-section read view (offerings table → detail → specifications → prices) over the `product` schema, plus the `(admin)` → `(app)` route-group rename and the "Products" nav section. Ship gate pm09.
- **Phase 2 — Manage Products CRUD (pm10–pm24):** the Manage Products page and its backend — copy-on-write versioning (edit an `ACTIVE` offering → new `DRAFT`), single-`ACTIVE`-per-family, permanently insert-only prices, specification CRUD, and the Activate/Retire/Discard lifecycle, across a backend half (pm10–pm16) and five UI units (pm19–pm23). Ship gate pm24.
- **Phase 3 — Product Ordering & Inventory (pm25–pm34):** the Orders and Subscriptions surfaces — TMF622 order capture → automatic TMF637 subscription instantiation in one transaction, grandfathered pricing (pinned offering version), insert-only negotiated-price overrides with MANAGER approval (reviewer ≠ submitter), and a suspend/resume/terminate lifecycle over an append-only, gap-free status history. Two new permissions (`product_orders`/`product_inventory`), no grant overlap with catalog `products`. Ship gate pm34.

`PRODUCT_ORDER_FAILED` remains seeded-but-unused by design — no code path writes it (the full-rollback deviation flagged in pm28); it is an intentional TMF622 full-enum seed, not open work.

**Verification posture (every delivered unit):** `tsc --noEmit` + `eslint .` + `prettier --check .` clean repo-wide, the full unit suite, and `next build`; write / versioning / lifecycle behavior is covered by live-DB integration + concurrency tests run against disposable, ephemeral Postgres 16 containers (never the dev stack's own `DATABASE_URL` — see `[[local-docker-dev-stack]]`). CI SAST/DAST and a live dev-server browser walkthrough remain outside this tooling's reach (same caveat across all three ship gates).

## Recurring patterns (apply across units — not repeated per-unit below)

- **Permission-name ripple:** adding a name to `PERMISSION_NAMES` forces that key into every hardcoded `EffectivePermissionMap` object-literal fixture across ~25+ test files (`tsc`-caught for object literals, but a few hardcoded array-length assertions are not). See `[[permission-name-addition-ripple]]`.
- **Cross-schema integration-test ripple:** every pre-existing `tests/**/*.integration.test.ts` doing a fresh `DROP SCHEMA … CASCADE` + migrate cycle needs a `DROP SCHEMA IF EXISTS "<newschema>" CASCADE` added first — otherwise the next file's migration fails with "schema already exists." See `[[new-pgschema-integration-test-ripple]]`.
- **Audit event-type ripple (every write unit):** each new `AUDIT_EVENT_TYPES` entry needs (1) an `AUDIT_EVENT_CATEGORY_MAP` entry in `types/audit-log.ts` (`tsc`-caught) and (2) a bumped event-type count + optgroup assertion in `tests/components/audit-log-filters.test.tsx` (**not** `tsc`-caught). See `[[audit-event-type-addition-ripple]]`.
- **Repository mutation allow-lists:** the `*-repository-exports` and `*-module-boundaries` guardrails carry a per-write-unit allow-list entry — expected, not a surprise; add the entry in the same commit as the write.
- **New-route ripple:** `tests/app/route-manifest.test.ts`'s `ROUTE_MANIFEST` is a strict set-equality check against every real `app/**/page.tsx` — a new page 404-fails that test until its literal URL is added to the array.
- **Region/seam discipline:** a shell unit builds placeholder frames with `{/* pmXX */}` seam comments; each follow-up unit fills exactly one seam, leaving the others byte-unchanged (pm05→pm08, pm18→pm23, pm27→pm29/pm31/pm33).
- **`db → validation` ESLint boundary:** any `db/**` or `types/**` file that embeds a Zod-owned shape needs `"validation"` added to its allowed-import targets in `eslint.config.mjs` (first hit in pm02/pm03 — a forced companion change, not scope creep).

## Permanent lessons (from post-ship fixes — do not re-learn)

- **Grant-per-schema on deploy:** a deployed instance 500'd (`42501: permission denied for schema product`) because `bootstrap-db-roles.sql` only granted on `core` (pm09). **Any new schema must add its own grant/revoke block (incl. sequence grants) in the same change that adds its migration, and the file must be re-run**, or this recurs. See `[[db-role-bootstrap-per-schema]]`.
- **TOCTOU-safe status reads:** a status-gated branch/lifecycle decision must read its target's status on the transaction (`tx`, `FOR UPDATE`), immediately before the write — never from a pre-transaction `db` snapshot. This exact bug was found and fixed independently in pm14, pm15, pm16, and pm20; treat a pre-transaction status read as a review-blocking defect on sight (code-standards §1 rule 13).
- **Concurrency is proven, not assumed:** single-active-per-family (pm16), approve-vs-approve / approve-vs-reject (pm30), and suspend-vs-terminate (pm32) are each verified by live-DB races looped 4×. `inventory_status_history` "latest" ordering is by the sequence id, not `created_at` (fixed at transaction start, can invert under lock contention — pm32 fix). Concurrent same-family branches are serialized by `pg_advisory_xact_lock(hashtext(rootId))` in `resolveNextVersion` (pm16 fix).

## Per-unit ledger (pm01–pm34, delivered)

Compacted deliverables; full detail in each `specs/pmXX-spec.md`.

| Unit | Spec file            | Delivered (compact)                                                                                                                                                                                                                                                                                  |
| ---- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| pm01 | `specs/pm01-spec.md` | Route-group rename `app/(admin)/**` → `app/(app)/**` (14 files, `git mv`); rename-invariance CI guardrail (`route-manifest.test.ts`).                                                                                                                                                                |
| pm02 | `specs/pm02-spec.md` | `product` schema in one unit — 3 tables + lifecycle enum + 3 sequences + constraints, permission registry wiring, `validation/product/`, seeds. Follow-up `0007`: `last_edited_by` FK → SET NULL, `amount >= 0` CHECK.                                                                               |
| pm03 | `specs/pm03-spec.md` | Read backend — 3 finder-only repositories (price repo permanently finder-only, Inv. #1), `services/product` (`listOfferings`/`getOfferingDetail`) with injectable-clock effectivity + `LEAD()` window. Migration `0008`.                                                                             |
| pm04 | `specs/pm04-spec.md` | `components/admin-nav.tsx`: flat `NAV_ITEMS` → sectioned `NAV_SECTIONS`, new "Products" section + collapsed-rail divider.                                                                                                                                                                            |
| pm05 | `specs/pm05-spec.md` | View Product page shell — `product-offering/page.tsx` (guard + searchParams + fetch), `LifecycleBadge`, `OfferingTable`, three-seam `OfferingDetailRegion`. Route added to frozen manifest.                                                                                                          |
| pm06 | `specs/pm06-spec.md` | `OfferingDetail` fills the Details seam.                                                                                                                                                                                                                                                             |
| pm07 | `specs/pm07-spec.md` | `SpecificationsPanel` (+`CharacteristicChip`) fills the Specifications seam.                                                                                                                                                                                                                         |
| pm08 | `specs/pm08-spec.md` | `formatCurrency`, `PriceTypeBadge`, `TierTable`, `PricesPanel` fill the Prices seam. Post-ship polish (`e2bb187`): density pass; `CharacteristicChip`/`TierTable` deleted, inlined as text.                                                                                                          |
| pm09 | `specs/pm09-spec.md` | Phase-1 ship gate — authz-matrix entry + `product-module-boundaries.test.ts` (5-assertion sweep). Post-ship (`d5f7148`): `bootstrap-db-roles.sql` needed a `product` grant block (see Permanent lessons).                                                                                            |
| pm10 | `specs/pm10-spec.md` | Schema — nullable self-referencing `family_offering_id` + index + self-ref CHECK on `product_offering`. Migration `0010`.                                                                                                                                                                            |
| pm11 | `specs/pm11-spec.md` | Backend create — `insertOffering` (hardcodes `isBundle:false`/`familyOfferingId:null`/`version:1`/`DRAFT`) + `createOffering` service (insert + `PRODUCT_OFFERING_CREATED` audit, one tx). First repo/guardrail allow-list bump.                                                                     |
| pm12 | `specs/pm12-spec.md` | `branchOfferingAsDraft(tx, sourceOfferingId, overrides?)` primitive — clones offering + all child rows into a new `DRAFT`, one-hop family resolution, `MAX(version)+1`, `isBundle` copied unconditionally. No audit (caller composes).                                                               |
| pm13 | `specs/pm13-spec.md` | Backend update — `updateOfferingDraftInPlace` + `updateOffering` service (four-way routing: DRAFT in-place w/ no-op guard, ACTIVE/`saveAsNew`→branch, RETIRED reject). `_UPDATED`/`_BRANCHED` audit.                                                                                                 |
| pm14 | `specs/pm14-spec.md` | Spec CRUD — `insert/update/deleteSpecification` repo + 3 services (branch-first-when-ACTIVE, `findClonedCounterpart`). `_CREATED`/`_UPDATED`/`_DELETED` audit. Post-ship: TOCTOU fix (reads on `tx`, locked) + `refProductOfferingId` backstop.                                                      |
| pm15 | `specs/pm15-spec.md` | Price mgmt — `insertPrice` (repo's only-ever write, Inv. #1) + service (branch-first, 3-day backdating tolerance checked twice). `PRODUCT_PRICE_ADDED` audit. Post-ship: same TOCTOU fix (read on `tx`).                                                                                             |
| pm16 | `specs/pm16-spec.md` | Lifecycle backend — `findActiveInFamily` (`FOR UPDATE` family lock), `activateOffering` (Inv. 13 re-check + supersede + flip ACTIVE), `retireOffering` (one method backs retire + discard). 4 audit events. Post-ship: locked spec/status re-checks + advisory-lock serializing concurrent branches. |
| pm17 | `specs/pm17-spec.md` | Nav — "Products" relabeled "View Product" + new sibling "Manage Products" item (`/products/manage-products`, no `requiredPermission`). Page-heading rename folded in. `/products/manage-products` 404s until pm18.                                                                                   |
| pm18 | `specs/pm18-spec.md` | Manage Products page shell — family-grouped table with expand/collapse, inert row-action (Edit/Add price/Activate/Discard/Retire) + "New offering" seams for pm19–pm23. `familyOfferingId` added to `OfferingListRow`/`findList`.                                                                    |
| pm19 | `specs/pm19-spec.md` | Create Offering UI+action — `create-offering.action.ts` (first `actions/product/` file), `OfferingForm`, `CreateOfferingDialog`; fills the "New offering" seam. Guardrail folder assertion → allow-list.                                                                                             |
| pm20 | `specs/pm20-spec.md` | Edit Offering UI+action — `update-offering.action.ts`, `EditOfferingForm` inline in table (DRAFT vs ACTIVE button sets). `billingOnly` added to read model. Post-ship: TOCTOU fix (`findDetailByIdForUpdate`).                                                                                       |
| pm21 | `specs/pm21-spec.md` | Specification mgmt UI+actions — `{create,update,delete}-specification.action.ts`, `SpecificationForm` (Record↔list via `useFieldArray`), `SpecificationsDialog` (+delete `AlertDialog`, DRAFT-only). Sixth row-action button.                                                                        |
| pm22 | `specs/pm22-spec.md` | Price mgmt UI+action — `insert-price.action.ts`, `PriceForm` (6 fields + tier-row editor), `AddPriceDialog` (own file, per-row instance). Backdating warning via mount-time `Date.now()` snapshot.                                                                                                   |
| pm23 | `specs/pm23-spec.md` | Lifecycle UI+actions — `activate-offering.action.ts` (EDIT) + `retire-offering.action.ts` (DELETE), `ActivateOfferingDialog` (inline `Alert` failures), `RetireOfferingDialog` (one `AlertDialog`, retire+discard). Last UI unit.                                                                    |
| pm24 | `specs/pm24-spec.md` | Phase-2 ship gate — guardrail rewrite (assertions 1, 10–14), authz split proof (`products:EDIT` vs `:DELETE`). Backfilled 8 committed integration tests for pm12–16 behavior previously only script-verified.                                                                                        |
| pm25 | `specs/pm25-spec.md` | Ordering & Inventory data layer — `ordering`/`inventory` schemas (5 tables, 2 enums, 5 sequences, cross-schema FKs), migrations `0022`/`0023`, `product_orders`/`product_inventory` permissions, validation + seeds + grant blocks.                                                                  |
| pm26 | `specs/pm26-spec.md` | Repositories + orders read services — 5 repos (insert-only per Inv. #16/#18, write-once order item Inv. #15), `types/ordering.ts`, `listOrders`/`getOrderDetail` (override-else-catalog, RETIRED-version-safe).                                                                                      |
| pm27 | `specs/pm27-spec.md` | Nav + Orders page (read) — "Orders"/"Subscriptions" nav, Orders list page + `OrderStatusBadge`/`OrdersTable`, inert New-order (pm29) and Review (pm31) seams. Review fixes: pagination range, keyboard bubbling, label reuse.                                                                        |
| pm28 | `specs/pm28-spec.md` | Order submission backend — `checkOrderPreconditions` (shared 6-check) + `createOrder` (one tx, TOCTOU-checked; standard path instantiates inventory + completes, override path parks `PENDING`). Full-rollback deviation flagged.                                                                    |
| pm29 | `specs/pm29-spec.md` | New Order wizard UI+action — `create-order.action.ts`, `NewOrderWizard` (first full-screen `Dialog`, first `FormProvider`), cross-module reads in `actions/accounts/new-order-wizard-reads.ts`. Fills pm27's New-order seam.                                                                         |
| pm30 | `specs/pm30-spec.md` | Approval backend — `approveOrder`/`rejectOrder` for `PENDING` override orders (MANAGER ≠ submitter, full re-validation under locks), `actorHasRole`, shared `instantiateOrder` factored from pm28. Races tested 4×.                                                                                  |
| pm31 | `specs/pm31-spec.md` | Review UI+actions — `{approve,reject}-order.action.ts`, `OrderReviewPanel`/`ReviewActions` filling pm27's Review seam, server-resolved `canReview`, display-only Δ% column.                                                                                                                          |
| pm32 | `specs/pm32-spec.md` | Subscription lifecycle backend + read services — suspend/resume/terminate/update-characteristics + shared `lifecycle-guards.ts`, `types/inventory.ts`. History "latest" keyed on sequence id, not `created_at`.                                                                                      |
| pm33 | `specs/pm33-spec.md` | Subscriptions page + lifecycle UI — page + `SubscriptionsTable`/`SubscriptionStatusBadge`/`StatusHistoryRows` + 4 `actions/inventory/*.action.ts` dialogs. Closes pm27's accepted Subscriptions 404.                                                                                                 |
| pm34 | `specs/pm34-spec.md` | Part-2 ship gate — authz matrix for `/products/orders` + `/products/subscriptions`, guardrails 15–22 into code-standards §9, doc sync flipping status markers to SHIPPED across the four companion docs.                                                                                             |
