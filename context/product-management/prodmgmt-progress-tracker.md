# Product Management — Progress Tracker

## Status

Phase 1 (all 9 units) implemented and committed. Module is ship-gate-verified (pm09) at the DB/test level; CI SAST/DAST baseline is a pipeline step outside this tooling.

Phase 2 (CRUD fast-follow) implemented and ship-gate-verified: pm10–pm17 implemented and locally verified via local tests and live-DB checks. pm18 implemented, verified via local tests and build only (no live-DB fixture walkthrough — see pm18 note). pm19 implemented, verified via local tests and build only (no live-DB fixture walkthrough — see pm19 note). pm20 and pm21 implemented, verified via local tests and build only (no live-DB fixture walkthrough — see pm20/pm21 notes). pm22 implemented, verified via local tests and build only (no live-DB fixture walkthrough — see pm22 note). pm23 implemented, verified via local tests and build only (no live-DB fixture walkthrough — see pm23 note); this completed all five UI units (pm19–pm23). **pm24 (ship gate) now complete — see note below.** CI SAST/DAST and the manual dev-server walkthrough remain outside this tooling (same caveat as pm09).

Phase 3 (Product Ordering & Inventory update) in progress: **pm30 (Approval Backend) — implemented and verified**: `services/ordering/review-order.ts`'s `approveOrder`/`rejectOrder` for `PENDING` override orders (MANAGER-role holder of `product_orders:EDIT`, reviewer ≠ submitter; approve re-runs the shared `checkOrderPreconditions` under locks then instantiates via the newly-factored shared `instantiateOrder`, reject is terminal with no inventory), the module's first role-*name* check (`roleAssignRepository.findRoleNamesByUserId` + `auth/roles.ts`'s `actorHasRole`, live per request, with an `auth-roles` ESLint carve-out so `services/**` may import it), and a new live-DB integration/concurrency suite. Backend only (no Server Action/UI; `actions/ordering/` stays pinned to `create-order.action.ts`); audit ripple was pre-satisfied by pm28. Verified via `tsc`/`eslint .`/`prettier --check` repo-wide + full unit suite (204 files / 2173 tests) + `next build`; no live-DB fixture walkthrough (no Postgres in-session, same gap as pm25/pm27/pm29). **pm29 (New Order Wizard UI + Action) — implemented and verified**: `actions/ordering/create-order.action.ts` (the only file in `actions/ordering/`, pinned there by pm29's own `EXPECTED_ORDERING_ACTION_FILES` guardrail) and the full 3-step `NewOrderWizard` (customer → BAN → offer/qty/dates/characteristics/price), filling pm27's "New order" seam. Cross-module wizard reads live in `actions/accounts/new-order-wizard-reads.ts` (disclosed placement call — see pm29 note). Verified via `tsc`/ESLint/Prettier clean **repo-wide** + full unit suite (203 files / 2158 tests) + `next build`; no live-DB fixture walkthrough or browser session performed (see pm29 note). **pm28 (Order Submission Backend) — implemented and verified**: `createOrder` + the shared `checkOrderPreconditions` (the update's core write transaction — first one spanning `customer`+`billing`+`product`+both new schemas), two disclosed additive locked finders on `party-role.ts`/`accounts/billing-account.repository.ts`, six-order-event + inventory-created audit ripple. Verified via `tsc`/ESLint/Prettier + full unit suite (200 files / 2138 tests) + a new 13-test live-DB integration suite green against an ephemeral Postgres 16 container — see pm28 note for a flagged deviation (the spec's "FAILED-persists compensating step" was superseded by the stronger, doubly-corroborated full-rollback precedent). **pm27 (Nav + Orders Page — Read Surface) — implemented and verified**: "Orders"/"Subscriptions" nav entries, the guarded `/products/orders` list page, `OrderStatusBadge`, `OrdersTable`, and inert seams for New order (pm29) and Review (pm31) — the first visible UI of the update. Verified via `tsc`/ESLint/Prettier + full unit suite (200 files / 2138 tests) + `next build`, all clean; no live-DB fixture walkthrough performed (no Postgres available in-session — see pm27 note for how the permission-separation checklist item was proven without one). **pm26 (Repositories + Orders Read Services) — implemented and verified** (2026-08-10): the five pm25-table repositories (`db/repositories/ordering/**` + `db/repositories/inventory/**`, insert-only where Inv. #16/#18 mandate) and the framework-agnostic orders read services (`services/ordering/list-orders.ts`, `get-order-detail.ts`) returning `OrderListRow`/`OrderDetail` read models. Verified via `tsc`/ESLint/Prettier + full unit suite (197 files / 2105 tests) **and the full integration suite (62 files / 552 tests) green against an ephemeral Postgres 16 container** — the first Phase-3 unit to run the live-DB integration suite (pm25 never did). No `db/schema/product.ts` / action / UI change. **pm25 (Ordering & Inventory Data Layer) — implemented; verified via `tsc`/ESLint/Prettier + full unit suite (2097 tests) green, `drizzle-kit generate` reports no drift. No live-DB fixture walkthrough performed** (same gap as pm18–pm23 — the integration guardrail suite and seed need a real Postgres; they `describe.skipIf(!DATABASE_URL)` locally). This is the first unit of the planned Ordering update (`prodmgmt-update-overview.md`, architecture Inv. #15–22); it touches only the two new `ordering`/`inventory` schemas and their data layer, never `db/schema/product.ts`. pm25–pm30 are spec'd and built; the remaining Phase-3 work — subscription lifecycle actions, the remaining Server Actions (approve/reject wiring, `actions/inventory/**`), the Review screen, the Subscriptions page, and the authz matrix — is not yet spec'd; see "Not yet implemented" under Phase 3 below.

**Commit references are no longer tracked in this file** (per-unit notes below stand on their own; use `git log` for commit history).

| Unit | Name                                                                 |
| ---- | --------------------------------------------------------------------- |
| pm01 | Route-group rename `(admin)` → `(app)` + rename-invariance CI proof   |
| pm02 | DB foundation + validation schemas + seeds (`product` schema)         |
| pm03 | Repositories + `services/product`                                     |
| pm04 | Nav refactor (`NAV_ITEMS` → `NAV_SECTIONS`)                            |
| pm05 | Page — one section per unit (table → detail → specs → prices)         |
| pm06 | Offering detail section (populated `OfferingDetail` fields)           |
| pm07 | Specifications panel (populated `SpecificationsPanel`)                |
| pm08 | Prices panel (populated `PricesPanel`, `formatCurrency`)              |
| pm09 | Authz-matrix entry + guardrail sweep (ship gate)                      |
| pm10 | Schema: `family_offering_id` version-lineage column (Phase 2, unit 1) |
| pm11 | Backend: Create offering (`insertOffering`, `createOffering`) (Phase 2, unit 2) |
| pm12 | Backend: Branch-as-draft primitive (`branchOfferingAsDraft`) (Phase 2, unit 3) |
| pm13 | Backend: Update offering (`updateOfferingDraftInPlace`, `updateOffering`) (Phase 2, unit 4) |
| pm14 | Backend: Specification management (Phase 2, unit 5)                   |
| pm15 | Backend: Price management (Phase 2, unit 6)                           |
| pm16 | Backend: Activation & Retirement/Discard (Phase 2, unit 7)            |
| pm17 | Nav: Relabel + "Manage Products" entry (Phase 2, unit 8)               |
| pm18 | Manage Products page shell (Phase 2, unit 9)                           |
| pm19 | UI: Create Offering (Phase 2, unit 10)                                 |
| pm20 | UI: Edit Offering (Phase 2, unit 11)                                    |
| pm21 | UI: Specification Management (Phase 2, unit 12)                        |
| pm22 | UI: Price management (Phase 2, unit 13)                                 |
| pm23 | UI: Lifecycle Actions — Activate/Retire/Discard (Phase 2, unit 14) |
| pm24 | Ship gate: Guardrails, authz matrix, full sweep (Phase 2, unit 15 of 15) |

**Renumbering note:** `pm02-spec.md` bundles DB foundation + `validation/product/` + seeds into one unit, superseding an earlier 3-way split. Numbers above match `pm00-build-plan.md`'s 9-unit count for Phase 1 (pm01–pm09); Phase 2 is 15 units (pm10–pm24 — 14 implementation units pm10–pm23 plus the pm24 ship gate).

## Recurring patterns (apply across units — not repeated per-unit below)

- **Permission-name ripple:** adding `"products"` to `PERMISSION_NAMES` forced `products: null`/`"DELETE"` into every hardcoded `EffectivePermissionMap` object-literal fixture across ~25+ test files (`tsc`-caught for object literals, but a few hardcoded array-length assertions in `roles-read.service.test.ts` were not). See `[[permission-name-addition-ripple]]`.
- **Cross-schema integration-test ripple:** every pre-existing `tests/**/*.integration.test.ts` doing a fresh `DROP SCHEMA core CASCADE` + migrate cycle needed `DROP SCHEMA IF EXISTS "product" CASCADE` added first (26 files) — otherwise the next file's migration fails with "schema product already exists." See `[[new-pgschema-integration-test-ripple]]`.
- **`db → validation` ESLint boundary:** both `db/schema/product.ts` (type-only JSONB import) and `types/product.ts` (`SpecificationCard`/`PriceCard` embedding Zod-owned shapes) needed `"validation"` added to their allowed-import targets in `eslint.config.mjs` — a forced companion change in pm02 and pm03, not scope creep.
- **Region-seam pattern:** pm05 built `OfferingDetailRegion` with three placeholder frames (Details/Specifications/Prices) carrying `{/* pmXX */}` seam comments; pm06/pm07/pm08 each filled exactly one seam, leaving the other two frames byte-unchanged, and each left the `<h2>` section title owned by the region (not the child component).
- **Audit event-type ripple (every Phase 2 write unit):** each new `AUDIT_EVENT_TYPES` entry needs (1) an `AUDIT_EVENT_CATEGORY_MAP` entry in `types/audit-log.ts` (`tsc`-caught) and (2) a bumped event-type count + optgroup assertion in `tests/components/audit-log-filters.test.tsx` (**not** `tsc`-caught). See `[[audit-event-type-addition-ripple]]`.
- **Repository mutation allow-lists:** `tests/db/product-repository-exports.test.ts` and `tests/guardrails/product-module-boundaries.test.ts`'s `PRODUCT_WRITE_SERVICE_FILES` both need a new entry per write unit — expected, flagged once by pm11, not a surprise since.
- **Verification baseline per unit:** `tsc --noEmit` + `eslint .` + `prettier --check` clean, plus `npx vitest run` (unit config) and a live-DB backend-correctness pass via a disposable `tsx` script (real dev DB, distinctive name/id prefix, cleaned up and re-confirmed zero rows afterward — never checked in). CI SAST/DAST and a live dev-server browser walkthrough remain unverified (no CI/browser in-session) unless noted per-unit.
- **New-route ripple (any unit adding a page.tsx):** `tests/app/route-manifest.test.ts`'s `ROUTE_MANIFEST` does a strict set-equality check against every real `app/**/page.tsx` — a new page 404-fails that test until the literal URL is added to the array (pm18 first hit this since pm01–pm17 added no new route).

## Per-unit notes

**pm01** — Renamed `app/(admin)/**` → `app/(app)/**` (14 files, `git mv`), updated 6 test imports + 2 path comments, added `tests/app/route-manifest.test.ts` as the rename-invariance guardrail.

**pm02** — `db/schema/product.ts` (`product` schema: `lifecycle_status` enum, 3 sequences, 3 tables, full constraints), migration `0006_product.sql` + `products` permission row, permission registry wiring, `types/product.ts`, `validation/product/`, `db/seeds/product.ts`. Follow-up fix (`232d78d`, migration `0007`): `product_offering.last_edited_by` FK → `ON DELETE SET NULL`; `CHECK (amount >= 0)` added to `product_offering_price`.

**pm03** — Read backend: three finder-only repositories (price repo permanently exports no mutation, Inv. #1), `listOfferings` + `getOfferingDetail` (injectable-clock `resolveEffectivityStatus`, `LEAD() OVER` window for derived `endDateTime`). Migration `0008` (not `0007` — pm02's follow-up consumed it).

**pm04** — `components/admin-nav.tsx`: flat `NAV_ITEMS` → sectioned `NAV_SECTIONS`, new "Products" section above "Administration." Manually verified live: render order, active-state, collapsed-mode divider, clean 404 pre-route.

**pm05** — `app/(app)/products/product-offering/page.tsx`, `LifecycleBadge`, `OfferingTable`, `OfferingDetailRegion` (three-seam shell). Added route to the frozen manifest.

**pm06** — `OfferingDetail` fills the Details seam. User-verified live ("looks good").

**pm07** — `CharacteristicChip` + `SpecificationsPanel` fill the Specifications seam.

**pm08** — `formatCurrency`, `PriceTypeBadge`, `TierTable`, `PricesPanel` fill the Prices seam. Component work sequenced ahead of pm07's commit (explicit user direction); diff later split into two clean commits before push.

**pm09 (ship gate)** — Tests + CI only. Confirmed all 6 inherited guardrails green; flagged "price immutability bumps version" as untestable in v1 (no mutation path yet — correct for that phase). Added authz-matrix entries + `tests/guardrails/product-module-boundaries.test.ts`. Real deviation: `products` permission is migration-seeded DML, so the spec's "insert in bulk seed array" text would violate the unique constraint — fixed by `SELECT`-ing the seeded row instead.

**Post-ship polish (2026-07-09, `e2bb187`)** — UI density pass; deleted `CharacteristicChip`/`TierTable` per explicit instruction, inlined as plain text instead.

**Post-ship deployment bug (2026-07-09, `d5f7148`)** — deployed instance 500'd (`42501: permission denied for schema product`) because `bootstrap-db-roles.sql` only ever granted privileges on `core`. Fixed by appending a `product` grant/revoke block (incl. sequence grants). **Any future schema must add its own grant block in the same change that adds its migration, or this recurs.**

---

## Phase 2 (CRUD Fast-Follow)

**pm10 — Schema: `family_offering_id`.** Added nullable, self-referencing `familyOfferingId` (`ON DELETE RESTRICT`) + index + self-reference CHECK to `productOffering`. Migration `0010` (schema-diff generated). Two notes: self-referencing FK needed an explicit `(): AnyPgColumn =>` return-type annotation (Drizzle inference gap, compile-time only); FK constraint name truncated by Postgres's 63-char limit to `product_offering_family_offering_id_product_offering_product_of`. One in-scope test fix: `product-schema.test.ts`'s exact-column-set assertion needed the new column added.

**pm11 — Backend: Create offering.** `create-offering.schema.ts` (no `isBundle` field, ever), `insertOffering` (hardcodes `isBundle: false`/`familyOfferingId: null`/`version: 1`/`DRAFT`), `createOffering` service (insert + `PRODUCT_OFFERING_CREATED` audit, one transaction, no uniqueness pre-check). Forced companion fixes (first occurrence of the now-recurring pattern): `AUDIT_EVENT_CATEGORY_MAP` entry, `audit-log-filters.test.tsx` count bump, and both `product-repository-exports.test.ts`/`product-module-boundaries.test.ts` relaxed from blanket read-only prohibitions to named allow-lists (Phase 2 architecture explicitly permits offering-repo writes; price repo stays permanently finder-only sans `insert*`). Live-DB verified: FK-enforced atomicity (bad actorId rolls back the offering insert too), real insert produces correct defaults + exactly one audit row.

**pm12 — Backend: Branch-as-draft primitive.** `branchOfferingAsDraft(tx, sourceOfferingId, overrides?)` clones an offering + all spec/price rows into a new `DRAFT`; `isBundle` always copied from source, never `overrides` (grep-confirmed no read path, even via `as any`). One-hop family resolution inlined, not extracted (explicit "not yet" call). No audit write — caller composes it. Live-DB verified via a script that deliberately threw to force rollback (byte-diffed before/after): version/family resolution (root and branch-of-branch), status always `DRAFT` regardless of source, override `??` semantics, full child-row cloning, zero-children case, source untouched.

**pm13 — Backend: Update offering.** `update-offering.schema.ts` (`name`/`isSellable`/`billingOnly`/`saveAsNew`), `updateOfferingDraftInPlace` (DRAFT-guarded in-place update, never touches `version`/`familyOfferingId`/`isBundle`), `updateOffering` service (four-way routing: in-place DRAFT save w/ no-op short-circuit, `branchAndAudit` shared by ACTIVE-target and `saveAsNew`, `OFFERING_RETIRED`/`NOT_FOUND` guards). Two new audit types (`_UPDATED` Change, `_BRANCHED` Additive) + the two repository/guardrail allow-list bumps. Live-DB verified via real commits against `PM13VERIFY-`-prefixed rows, fully cleaned up after: in-place edit, no-op guard, `saveAsNew` still branches even on identical values, ACTIVE-target branch with correct family/version, not-found/retired guards.

**pm14 — Backend: Specification management.** `create-specification.schema.ts`/`update-specification.schema.ts` (field-identical), `product-specification.ts` gains `insertSpecification`/`updateSpecification`/`deleteSpecification`, three services with uniform branch-first-when-ACTIVE routing + a `findClonedCounterpart` content-matching helper (duplicated once between update/delete, per spec's explicit judgment call). Three new audit types (`_CREATED`/`_UPDATED`/`_DELETED`). One undiscovered-until-test-run gap: `product-repository-exports.test.ts`'s spec-repository assertion needed the same allow-list treatment pm11 gave the offering repository (not named in pm14-spec's own diff list). Live-DB verified against `PM14VERIFY-`-prefixed rows plus the real seeded ACTIVE offering (to test `findClonedCounterpart` picking the right row among multiple candidates); one real snag: price/spec rows are `ON DELETE RESTRICT` against offering, so cleanup had to delete children before parents. **Post-ship review fix (2026-07-22):** all three services read `offering.lifecycleStatus` (and, for update/delete, the target spec's current content) via `db` *before* opening the transaction, then used that stale snapshot to make the branch-first decision inside the transaction — a TOCTOU window. Fixed by moving both reads onto `tx`, immediately before the branch decision, inside the same transaction as the write. Also added a `refProductOfferingId` backstop condition to `updateSpecification`/`deleteSpecification`'s `WHERE` clause (defense-in-depth, same shape as `updateOfferingDraftInPlace`'s status backstop) — callers now pass the target offering id alongside the spec id. `product-repository-exports.test.ts` extended: `MUTATION_NAME_PATTERN` now recognizes a `branch` prefix and `branchOfferingAsDraft` (pm12, previously unguarded by this test) is in `ALLOWED_OFFERING_MUTATIONS`.

**pm15 — Backend: Price management.** `insert-price.schema.ts` + `product-offering-price.ts` gains `insertPrice` (repo's only-ever write, Inv. #1), `insertPrice` service (branch-first-when-ACTIVE, 3-day backdating tolerance checked twice — schema fast-fail + injectable-`now` service check, needed separate time sources since schema's `superRefine` uses real `Date.now()`). One new audit type (`PRODUCT_PRICE_ADDED`, Additive). Live-DB verified: direct/branch-first paths, backdating boundary, not-found/retired guards. **Post-ship review fix (2026-07-22):** `insertPrice` read `offering.lifecycleStatus` via `db` before the transaction opened, then used that stale snapshot for the branch-first decision inside the transaction — the same TOCTOU pattern fixed in pm14's services. Fixed by moving the `findDetailById` read onto `tx`, immediately before the branch decision.

**pm16 — Backend: Activation & Retirement/Discard.** `activate-offering.schema.ts`/`retire-offering.schema.ts` (both single-field `reason`), `product-offering.ts` gains `findActiveInFamily` (`.for("update")` row-locks the whole family by immutable identity, first use of locking in this codebase), `activateOffering` (transactional Inv. 13 re-check + retire-sibling + flip-to-ACTIVE), `retireOffering` (unconditional flip to `RETIRED`, one method backs both retire and discard per code-standards §1 rule 11). Four new audit types (`_ACTIVATED`/`_SUPERSEDED`/`_RETIRED`/`_DISCARDED`). Live-DB verified via disposable script: all precondition/guard rejections, automatic supersession, **Inv. 13 under real concurrency run 4× — exactly one family member ended up `ACTIVE` every time**, retire/discard confirmed to share the identical repository method. **Post-ship review fix (2026-07-22):** two related gaps closed. (1) `activateOffering`'s `SPECIFICATIONS_NOT_RESOLVED` precondition was only checked once, pre-transaction — a mandatory spec's `defaultValue` could be cleared (or the spec deleted, pm14) between that read and the write. Added `productSpecificationRepository.findByOfferingIdForUpdate` (`FOR UPDATE` variant of `findByOfferingId`) and re-check the precondition, locked, inside the transaction, immediately before calling `activateOffering`. `NO_PRICE_ROWS` was left as a pre-transaction-only check (prices are insert-only, Inv. #1 — that result can't regress). (2) `retireOffering` derived its `eventType` (`_RETIRED` vs `_DISCARDED`) from the same pre-transaction status read, which could go stale the same way (e.g. a concurrent activation) — `retireOffering`'s repository method has no status WHERE-backstop of its own. Added `productOfferingRepository.findLifecycleStatusForUpdate` (single-row, `FOR UPDATE`, no join — unlike `findDetailById`) and re-derive `eventType` from a locked re-read inside the transaction; an already-RETIRED result now aborts with `OFFERING_RETIRED` instead of writing a stale-eventType audit row. Separately, `db/repositories/product-offering.ts`'s `resolveNextVersion` (pm12) had no protection against two concurrent `branchOfferingAsDraft` calls on the same family both computing the same `MAX(version)` and allocating duplicate version numbers — row locking doesn't help here (the contended "row" is the next insert, which doesn't exist yet in either transaction's snapshot), so fixed with a `pg_advisory_xact_lock(hashtext(rootId))` acquired at the top of `resolveNextVersion`, serializing concurrent branches of the same family; auto-released at transaction end. Typecheck/lint/format clean and full mocked suite green (155 files / 1414 tests) after this round. Live-DB re-verified once the Tailscale dev DB became reachable again (2026-07-22, `PMRACEVERIFY-`-prefixed rows, cleaned up and re-confirmed zero rows afterward): two concurrent `branchOfferingAsDraft` calls on the same family produced three distinct, sequential versions (advisory lock holds); `activateOffering` succeeds with the locked spec re-check in place; `retireOffering` derives `RETIRED`/`DISCARDED` correctly from the locked re-read for ACTIVE/DRAFT sources respectively and rejects a second retire on an already-RETIRED offering; `insertPrice`'s tx-based re-fetch works on both the direct-DRAFT and branch-first-ACTIVE paths. (Dev DB also needed a `db:migrate` catch-up first — it was missing the pm10 `family_offering_id` migration.)

**pm17 — Nav: Relabel + "Manage Products" entry.** `components/admin-nav.tsx`'s "Products" section relabeled to "View Product" + new sibling "Manage Products" item, deliberately with no `requiredPermission` (nav-renders-regardless-of-permission convention, pm17-spec §2.2). Data-only diff, zero render-loop/type changes. §2.6 folded in: `product-offering/page.tsx`'s `metadata.title`/`H1` → "View Product," text only. Tests extended per §3.5, all pre-existing Customer-section assertions pass unmodified. `/products/manage-products` 404s until `pm18`. Full suite green (155 files / 1414 tests).

**pm18 — Manage Products page shell.** Confirmed pm17 already resolved §3.8's "View Product" heading rename (folded into pm17, not deferred) — this unit touches nothing under `product-offering/`. The one flagged read-model exception (§2.2): `familyOfferingId` added to `OfferingListRow` (`types/product.ts`) and to `findList`'s `SELECT` (`db/repositories/product-offering.ts`) — one column, no new repository method, no new service export. New: `app/(app)/products/manage-products/{page,loading,error}.tsx`, `components/products/manage/manage-offering-table.tsx`. Page-local private helpers (`fetchAllForStatus`/`fetchAllOfferingRows`/`resolveFamilyId`/`selectPrimary`/`groupIntoFamilies`) page-loop the existing `listOfferings` twice (`status: null`, `status: "RETIRED"`) and group by resolved family root, matching `firstValue`'s page-local-helper precedent. Every row action (Edit/Add price/Activate/Discard/Retire) and the "New offering" CTA render fully wired-looking but inert, each with a `pm19`–`pm23` seam comment, per the pm05→pm08 seam discipline. One deviation from the spec's literal code, flagged: ui-context-phase2's `--text-secondary`/`--text-danger` row-action color tokens don't exist in `app/globals.css` (grepped repo-wide — zero other references) and adding them was out of this unit's file scope, so quiet actions use the existing `text-muted-foreground` token and danger actions (Discard/Retire) use the existing `text-destructive` token instead — same semantic roles, no new CSS custom properties introduced. Ripple (additive, expected per pm18-spec §5): `tests/components/offering-table.test.tsx` and `tests/services/list-offerings.service.test.ts` fixtures needed `familyOfferingId: null` added to their `OfferingListRow` literals; `tests/app/route-manifest.test.ts`'s frozen `ROUTE_MANIFEST` needed `/products/manage-products` added (strict set-equality against real `page.tsx` files). Full suite green (157 files / 1426 tests), `tsc`/`eslint`/`prettier`/`next build` all clean. No live-DB fixture walkthrough performed (needs pm13/pm16 update/activate services or a throwaway multi-version fixture script to exercise the expand affordance and RETIRED-primary case per §5's fixture-data note).

**pm19 — UI: Create Offering.** `actions/product/create-offering.action.ts` (new, first file in `actions/product/`; guard → `safeParse` → delegate to pm11's `createOffering` → `revalidatePath` both product pages, no `NAME_CONFLICT`-style branch since pm11's service has none), `offering-form.tsx` (new, create-mode-only `OfferingForm`, no `isBundle` control, `Controller`+`Checkbox` pattern reused from `user-form.tsx`), `create-offering-dialog.tsx` (new, mirrors `create-role-dialog.tsx`; success path is `router.refresh()` not `router.push()` since Manage Products has no selected-row URL-state concept). Filled pm18's inert "New offering" CTA seam in `manage-offering-table.tsx` (one import + one JSX wrap, nothing else touched). Guardrail assertion updated in this same commit, not deferred to pm24 (Design §2.5): `product-module-boundaries.test.ts`'s "has no `actions/product/` folder" assertion replaced with an `EXPECTED_PRODUCT_ACTION_FILES` allow-list check — pm20–pm23 must each append their own action file to that array rather than assuming the rewrite is still pm24's job alone. One undocumented-in-spec ripple: `tests/setup.ts` needed a `ResizeObserver` polyfill added — jsdom doesn't implement it, and pm19 is the first suite to render a `Checkbox` with a non-empty parent (Radix's size-measuring indicator needs it to mount). Verified: `tsc`/`eslint`/`prettier`/`vitest run`/`next build` all clean on the pm19-specific files; the one pre-existing lint error (`organization-form.tsx`) and one pre-existing flaky test timeout (`customer-role-form.test.tsx`) are both unrelated Customer-module issues, unchanged by this unit. No live-DB fixture walkthrough performed (same gap as pm18 — needs a throwaway script or live browser session to confirm the real `PRODUCT_OFFERING_CREATED` audit row and table refresh end-to-end).

**pm20 — UI: Edit Offering.** `actions/product/update-offering.action.ts` (new) — guard → `safeParse` → delegate to pm13's `updateOffering` → `revalidatePath` both product pages. `offering-form.tsx` gains `EditOfferingForm`, composed inline in `manage-offering-table.tsx` (Design §2.1 — no separate `edit-offering-dialog.tsx` file); two independent `handleSubmit` calls, one per save outcome, instead of one native submit disambiguated after the fact. Footer buttons depend on `currentStatus`: DRAFT gets Cancel/"Save as new draft"/"Save"; ACTIVE gets Cancel/"Create new draft" only, behind the `--bg-warning` banner. Flagged read-model exception (same shape as pm18 §2.2): `billingOnly` added to `OfferingListRow`/`findList`'s `SELECT` to prefill the dialog. `editOfferingFieldsSchema` (`.omit`-derived) appended to pm13's `update-offering.schema.ts`. Fills both pm18 Edit seams; a branched save auto-expands its family. Guardrail `EXPECTED_PRODUCT_ACTION_FILES` appended with `update-offering.action.ts`. Ripple: `billingOnly: false` added to `OfferingListRow` fixtures across three test files; `manage-offering-table.test.tsx`'s no-op-loop test now excludes Edit; new `describe("Edit offering (pm20-spec §3.8)")` block. Full suite green (160 files / 1455 tests), `tsc`/`eslint`/`prettier`/`next build` all clean (one pre-existing unrelated `organization-form.tsx` lint error). No live-DB fixture walkthrough performed. **Post-ship review fix (2026-07-23):** `services/product/update-offering.ts`'s `updateOffering` read `offering.lifecycleStatus` via `db` *before* opening the transaction, then used that stale snapshot to route between the ACTIVE-branch/DRAFT-in-place/RETIRED-reject paths inside the transaction — the same TOCTOU pattern already fixed in pm14/pm15/pm16's services. Fixed by moving the read onto `tx` via `findDetailByIdForUpdate` (row-locked), wrapping the entire routing decision and both the branch and in-place-update paths in one `db.transaction`, immediately before any write. `manage-offering-table.test.tsx` gained a test confirming an expanded sibling row's own Edit button populates the dialog with the sibling's values (not the primary's) and submits the sibling's id; `create-offering.action.test.ts`/`update-offering.action.test.ts` each gained a test covering `requirePermission` rejecting with a non-redirect error (`SERVER_ERROR`, service never called). Full suite green (168 files / 1528 tests), `tsc`/`eslint`/`prettier` all clean.

**pm21 — UI: Specification Management.** `actions/product/{create,update,delete}-specification.action.ts` (new) — guard → `safeParse` (create/update only; delete has no schema, matching `update-offering.action.ts`'s plain-parameter precedent) → delegate to pm14's three services → `revalidatePath` both product pages. `specification-form.tsx` (new, `SpecificationForm`) — the first form editing a `Record<string, string>` field: `useFieldArray` needs an array, so a UI-only `characteristicsList: {key,value}[]` shape is translated to/from the wire `Record` at the form's own boundary (`recordToList`/`listToRecord`). `specifications-dialog.tsx` (new, `SpecificationsDialog`) — one `Dialog` swapping between a list view and a form view (not separate dialog files), plus a nested `AlertDialog` (reusing `delete-role-dialog.tsx`'s shape) for hard-delete, UI-gated to `DRAFT` targets only even though pm14's service also supports an `ACTIVE`-target branch-first delete (dead code from this unit's UI, deliberately). **Two design calls flagged for pm22/pm23:** (1) a **direct** success keeps the dialog **open** + refreshes rather than closing — a deliberate departure from pm19/pm20's "always close," fitted to this dialog's "add several specs in one sitting" shape; a **branch-producing** success still closes/toasts/auto-expands exactly like pm20. (2) The read-model gap is closed via a new page-local `fetchSpecificationsByOfferingId` helper (reuses `getOfferingDetail` unmodified, one call per displayed offering) passed as a sibling `specificationsByOfferingId` prop — not by widening `OfferingListRow` or adding a read Server Action. Disclosed cost: `getOfferingDetail` also computes prices, discarded here, doubling per-row query cost on page load — accepted for now, same trade pm18 made. Sixth row-action button (`ListChecks`, `--text-secondary`, `DRAFT`/`ACTIVE` only) added to the table matrix and to both `prodmgmt-code-standards-phase2.md` §4/§7 and `prodmgmt-ui-context-phase2.md` in this same commit. Guardrail `EXPECTED_PRODUCT_ACTION_FILES` appended with all three new action files. Ripple: `manage-products-page.test.tsx` needed a `getOfferingDetail` mock (the page now calls it); `manage-offering-table.test.tsx`'s no-op-loop test now also excludes Specifications, plus a new `describe("Specifications (pm21-spec §3.11)")` block. Full suite green (165 files / 1495 tests), `tsc`/`eslint`/`prettier`/`next build` all clean. No live-DB fixture walkthrough performed.

**pm22 — UI: Price management.** `actions/product/insert-price.action.ts` (new) — guard → `safeParse` (pm15's own `insertPriceSchema`, unchanged) → delegate to pm15's `insertPrice` → `revalidatePath` both product pages. `components/products/manage/price-form.tsx` (new, `PriceForm`) — six fields exactly (Price name/Price type/Currency/GL code/Pricing model/Start date, Amount or a new tier-row editor depending on pricing model), its own local `priceFormSchema` validating only flat-shape-meaningful checks (Design §2.4 — tier contiguity/XOR stay defined once, in `tieredPricingCharacteristicsSchema`, reused not re-declared). `components/products/manage/add-price-dialog.tsx` (new, `AddPriceDialog`) — unlike Edit, this **does** get its own dialog file (Design §2.1: Add Price has exactly one submit outcome, matching `CreateOfferingDialog`'s shape) and its own per-row instance (Design §2.2: no new shared table state, `ManageOfferingTable` needed no `addPriceRow` state at all). Fills every pm18 Add-price seam (primary and expanded-sibling rows alike) via one `AddPriceDialog` wrap per row, each row's own `onBranchPrice` closure calling the existing `setExpandedFamilies` adder — same auto-expand pattern pm20/pm21 established. Guardrail `EXPECTED_PRODUCT_ACTION_FILES` appended with `insert-price.action.ts`. Two implementation choices flagged, both spec-sanctioned "either is fine" calls: (1) `pricingModel`'s `RadioGroup` is wired via a `Controller`, not the manual `register().onChange` shim pm22-spec §3.3's own literal code sample showed — the spec explicitly calls this an acceptable substitution, first use of `RadioGroup` in the codebase either way; (2) the backdating-warning computation captures "now" once via a `useState(() => Date.now())` lazy initializer rather than calling `Date.now()` directly during render — required to satisfy this codebase's `react-hooks/purity` ESLint rule (React Compiler), which flags impure calls in the render body; a mount-time snapshot is indistinguishable from a live clock for a short-lived dialog. Tests use `vi.useFakeTimers({ toFake: ["Date"] })` pinned to local midnight to make the 3-day backdating boundary deterministic regardless of wall-clock time-of-day, avoiding an off-by-hours flake in the literal `Date.now() - startOfDayMidnight` comparison. Full suite green (168 files / 1525 tests), `tsc`/`eslint`/`prettier`/`next build` all clean. No live-DB fixture walkthrough performed (same gap as pm18–pm21). Rebased onto `54feb72` (an out-of-band TOCTOU/version-race fix to pm14–16 backend code, pushed to `origin/dev1` independently of this unit's own work) before pushing; re-ran the full verification baseline post-rebase, unchanged results — `insertPrice`'s exported signature and `InsertPriceResult` shape were untouched by that fix, so nothing in this unit needed rework.

**pm24 (Phase 2 ship gate) — Guardrails, authz matrix, full sweep.** Tests + CI only, same discipline as pm09. Pre-flight audit (spec §3.1) found a real gap before any pm24 code was written: the ledger's guardrails 8 (single-active-per-family), 9 (branch-not-mutate), and 14 (price immutability, behavioral) were marked "INHERIT — audit present & green," but no committed test anywhere in the repo exercised `branchOfferingAsDraft`/`activateOffering`/`retireOffering`/`addSpecification`/`updateSpecification`/`deleteSpecification`/`insertPrice` — pm12/13/14/15/16 had each only verified this behavior via disposable, uncommitted scripts (per their own notes above). Per the spec's own explicit instruction this is a defect in the owning units, not something pm24 should paper over with a workaround — flagged to the user, who chose to have the missing coverage added now rather than stop-and-report or ship with a known gap. Added 8 new tests to `tests/db/product-repositories.integration.test.ts` (not originally in pm24's own file list, but the correct home — same file, same describe-block style as the existing pm12–pm16 coverage): two for guardrail 8 (sequential sibling supersession; two near-simultaneous activations on siblings leave exactly one ACTIVE, proving the `FOR UPDATE` family lock actually serializes), five for guardrail 9 (offering-fields/add-spec/update-spec/delete-spec/insert-price branch-not-mutate, each asserting the ACTIVE source row and its children are byte-identical after the write and exactly one new sibling DRAFT exists), one for guardrail 14 (successor price insert on a DRAFT leaves the prior price row byte-identical and offering `version` untouched). Then pm24's own net-new work in `tests/guardrails/product-module-boundaries.test.ts`: rewrote assertion 1 to add the per-file `export async function <Name>(` grep pm19's incremental version hadn't added; added guardrail 11 (View-stays-read-only), guardrail 13 (schema-diff), the guardrail-12 route-manifest re-check (pm18 had already added the route itself); `PRODUCT_WRITE_SERVICE_FILES` already had all eight entries (pm14/15/16 self-extended it, per the recurring-pattern note above — verify only, no edit needed); added the guardrail-10 structural backstop. One deviation from the spec's own literal code, flagged: §3.7's suggested regex (`/\.deleteSpecification\(|(?<!\.)\bdeleteSpecification\(/`) false-positived on `actions/product/delete-specification.action.ts`, which legitimately calls the *service* function of the same name (not the repository method) — the bare-name alternation couldn't distinguish them. Fixed by scoping the check to the dot-qualified `productSpecificationRepository.deleteSpecification(` pattern only, since the repository is never destructured anywhere in this codebase. `tests/auth/guard.integration.test.ts` extended per §2.7/§3.8: new `productsManagerUserId` principal (products:EDIT only, no DELETE) mirroring the existing `customerManagerUserId` pattern; dynamic imports for all eight `actions/product/*.action` modules; two `it.each` loops (seven EDIT-gated actions don't reject the EDIT-only caller on permission grounds; all eight reject `noGrantsUserId`) plus one dedicated test proving `retireOfferingAction` rejects the EDIT-only caller — the concrete proof the EDIT/DELETE split is real. `tests/app/route-manifest.test.ts` needed no edit (pm18 had already added `/products/manage-products`). Verified against three separate ephemeral, disposable Postgres containers (never the dev stack's own `DATABASE_URL` — see `[[local-docker-dev-stack]]`): full integration suite (37 files / 351 tests, up from 335 pre-pm24) green across all three runs; full unit suite (172 files / 1579 tests) green; `tsc --noEmit`, `next build` clean; `eslint` shows only the two pre-existing, unrelated Customer-module issues already noted in pm19/pm20/pm23's own notes. `git status` diff scope: exactly `tests/db/product-repositories.integration.test.ts`, `tests/guardrails/product-module-boundaries.test.ts`, `tests/auth/guard.integration.test.ts` — no `app/**`/`services/**`/`db/**`/`actions/**`/`components/**` change, no doc edit (permission-map rows already correct per §3.9's audit). With pm24 verified, Phase 2 (15 units total — 14 implementation units pm10–pm23 plus the pm24 ship gate) is code-complete and CI-gated; CI SAST/DAST and the manual dev-server walkthrough (§3.10) remain outside this tooling's reach, same caveat pm09 carried for Phase 1.

**pm23 — UI: Lifecycle Actions (Activate/Retire/Discard).** `actions/product/activate-offering.action.ts` (new) — guard `products:EDIT` → `safeParse` (pm16's own `activateOfferingSchema`, unchanged) → delegate to pm16's `activateOffering` → `revalidatePath` both product pages. `actions/product/retire-offering.action.ts` (new) — guard `products:DELETE` → `safeParse` (`retireOfferingSchema`) → delegate to pm16's `retireOffering`, one action handling both Retire and Discard (code-standards-phase2 §1 rule 11, extended one layer up). `components/products/manage/activate-offering-dialog.tsx` (new, `ActivateOfferingDialog`) — plain `Dialog`, accent-filled confirm button, the two named precondition failures (`NO_PRICE_ROWS`/`SPECIFICATIONS_NOT_RESOLVED`) render as a persistent inline `Alert` rather than a toast (the one departure from pm19/pm20/pm22's toast-only failure handling), `onSuperseded` prop fires only when `supersededOfferingId` is non-null. `components/products/manage/retire-offering-dialog.tsx` (new, `RetireOfferingDialog`) — one `AlertDialog` component, copy/title/confirm-label switching on a `currentStatus` prop ("DRAFT" → Discard copy, "ACTIVE" → Retire copy), success toast driven by the server's own `eventType`, not the prop. Both dialogs use a plain `useState`-backed `Textarea` for the optional Reason field, no `react-hook-form`/`zodResolver` (grep-confirmed absent) — the two schemas are single-field and don't earn that weight. Filled every remaining pm18 seam (Activate/Discard/Retire, primary and expanded-sibling rows alike) in `manage-offering-table.tsx`; the auto-expand-on-supersession wiring reuses the same `setExpandedFamilies` adder pm20/pm21/pm22 already established, passed through as both dialogs' respective row-action props. This is the last of the five UI units (pm19–pm23) — every button `pm18` originally rendered inert now has real behavior; `EXPECTED_PRODUCT_ACTION_FILES` appended with `activate-offering.action.ts`/`retire-offering.action.ts`, completing the full eight-file set `code-standards-phase2.md` §7 names. `tests/components/manage-offering-table.test.tsx`'s prior "still wired to nothing" seam test was rewritten (§3.8's own instruction) into a real-behavior assertion — no remaining "no attached behavior" case exists anywhere in that file. Full suite green (172 files / 1575 tests), `tsc`/`prettier`/`next build` all clean; `eslint` shows only the two pre-existing, unrelated Customer-module issues already noted in pm19/pm20's notes (`organization-form.tsx` `react-hooks/set-state-in-effect` error, `new-customer-form.tsx` `react-hooks/incompatible-library` warning). No live-DB fixture walkthrough performed (same gap as pm18–pm22 — needs a throwaway script or live browser session to confirm the real `PRODUCT_OFFERING_ACTIVATED`/`_SUPERSEDED`/`_RETIRED`/`_DISCARDED` audit rows and the Manage-Products/View-Product visibility split end-to-end).

## Phase 3 (Product Ordering & Inventory Update)

**pm25 — Ordering & Inventory Data Layer.** Creates the `ordering` (TMF622) and `inventory` (TMF637) pg schemas — 5 tables, 2 enums, 5 sequences (prefix + 8-digit `lpad`, matching the repo-wide standard from commit `b27bb3e`, not the spec's stale `\d{6}` text — spec updated), cross-schema FKs into `customer`/`billing`/`product`/`core`, all constraints — plus the `product_orders`/`product_inventory` permission registry rows, the Zod validation layer (`validation/ordering/**` + `validation/inventory/**`), sample-story seeds, and DB-level guardrail tests. No change to `db/schema/product.ts` (schema-diff guardrail stays byte-identical). **Deviation flagged (spec §5):** the seed's "resolve seeded party/BAN/offering ids by lookup" assumes seeded customer/accounts data that does not exist anywhere in `db/seeds` — so the seed self-provisions its `TOREMOVE-Template-`-prefixed prerequisites (org → party_role → financial_account → billing_account + two template appusers for submitter ≠ reviewer) via idempotent lookup-or-create, and looks up the existing seeded product offering; every id is still resolved at runtime, never hardcoded. **Permission-map ripple avoided:** `product_orders`/`product_inventory` were added to the *optional* module-permission set in `types/permissions.ts` (accounts precedent), not the required-core set, so no `EffectivePermissionMap` fixture ripple across the ~25 test files. **new-pgSchema integration-test ripple** (progress-tracker recurring pattern): every integration test doing a `DROP SCHEMA … CASCADE` + migrate reset needed `DROP SCHEMA IF EXISTS "inventory"/"ordering" CASCADE` added before the `core` drop.

**pm26 — Repositories + Orders Read Services.** Five repositories: `db/repositories/ordering/{product-order,product-order-item,order-item-price-override}.repository.ts` + `db/repositories/inventory/{product-inventory,inventory-status-history}.repository.ts` (accounts-folder placement precedent). The two Inv. #16/#18 tables (`order_item_price_override`, `inventory_status_history`) export insert + finders only — no update/delete, ever; `product_order_item` is likewise write-once (Inv. #15); `product_order`/`product_inventory` carry a deliberately narrow write surface (`updateStatus`/`updateCharacteristics` — status-workflow / the one editable never-rated field only, never the billing core). Two orders read services (`services/ordering/list-orders.ts` → `OrderListPage` with a fixed `ORDERS_LIST_PAGE_SIZE = 20`; `get-order-detail.ts` → `OrderDetail`), returning new read models added to `types/ordering.ts` (`OrderListRow`/`OrderListPage`/`OrderPriceLine`/`OrderDetailItem`/`OrderDetail`). `getOrderDetail` computes the Inv. #16 override-else-catalog resolution once, reusing the catalog's existing `findByOfferingIdWithDerivedEnd` derived-effectivity query (never reimplemented; no ACTIVE filter, so a grandfathered RETIRED pinned version still reads — Inv. #17). **pm30 precondition (from pm26 code review):** `getOrderDetail` resolves each override against the catalog row effective on the read date, so an override whose `price_type` has no currently-effective catalog row would drop off the price sheet — unreachable via the ordering flow (an override only targets a flat type effective at order time, and catalog rows are immutable/insert-only, so that type always has a row with `start ≤ order date ≤ read date`), but pm30's approval service must enforce it as an explicit gate: every submitted override `price_type` has a currently-effective flat catalog row on the pinned version, denominated in the BAN currency. That currency gate is why `OrderPriceLine` carries one `currency` per line (from the catalog row): override currency = BAN currency = catalog price currency by construction, so a per-amount currency split in the read model would imply a mismatch the design forbids (also raised in pm26 review). The orders list join resolves customer name via `party_role → organization`, offering name/version, submitter/reviewer display names (`alias(appuser, …)` — first use of `alias()` in the codebase, needed for the two same-table joins), and a correlated `exists` override flag; both list `findList` count queries use the **same join set** as their data query, so `total` can't diverge from the row count under an orphan/multi-item state. Tests: `tests/db/ordering-repository-exports.test.ts` (structural no-mutation asserts, pm03 pattern), `tests/guardrails/ordering-module-boundaries.test.ts` (no `next/*` import + no raw SQL in `services/ordering`), `tests/db/ordering-read.integration.test.ts` (behavioral against the pm25 sample story rebuilt inline: override-else-catalog 420/list/tiered-null, currently-effective row selection, all-null on the standard order, PENDING-filter, RETIRED-version grandfathered read, repo filter/search/sort/pagination, inventory-repo smoke). **new-pgSchema integration-test ripple completion (pm25 gap):** pm25's ripple sweep added the `inventory`/`ordering` drops only to *some* files and, for 16 accounts `.integration.test.ts` files, only to their `afterAll` (not the `beforeAll` before `migrate()`); it also missed 2 accounts `.property.test.ts` files entirely and 2 more `.integration.test.ts` files (`document-detail`, `v09-bill-cycle-integrity`). Because the integration config runs files sequentially with non-deterministic *file order* (`fileParallelism: false`), whichever gap-file followed one that left the schemas behind failed its migrate with `schema "ordering" already exists` (flaky 4→3→1 across identical-code runs until every file was made self-sufficient). Fixed by adding `DROP SCHEMA IF EXISTS "inventory"/"ordering" CASCADE` to the `beforeAll` (before the `billing` drop) of all 20 affected files — pm25 never surfaced this because it never ran the integration suite. This confirms the recurring ripple must be applied to `beforeAll` of **every** DB-resetting file (both `.integration.test.ts` and `.property.test.ts`), not just its teardown.

**pm27 — Nav + Orders Page (Read Surface).** Nav diff (`components/admin-nav.tsx`, pm17 pattern): "Products" section gains "Orders" (lucide `ClipboardList`) and "Subscriptions" (lucide `Layers`) after "Manage Products," no `requiredPermission` (nav-renders-regardless convention) — `/products/subscriptions` 404s until pm33, same as pm17→pm18. New page: `app/(app)/products/orders/{page,loading,error}.tsx` — `requirePermission(PERMISSIONS.PRODUCT_ORDERS, LEVELS.READ)` → parse `validation/ordering/orders-list.schema.ts` (already existed from pm25/26, unmodified) → pm26's `listOrders` → compose `OrdersTable`; thin orchestrator, no DB/business rules. New components under `components/products/ordering/`: `OrderStatusBadge` (pill+icon over all nine `ORDER_STATUSES`; COMPLETED→success, PENDING→warning, REJECTED/FAILED→danger, the four not-yet-written states→neutral) and `OrdersTable` (offering-table.tsx's hand-rolled pattern — search/status-filter/sortable-headers/pagination/row-select-via-`?order=` — reused, not forked; ten columns per spec, negotiated-price pill via the shared **Accent** family — deliberately not the reserved AI/Iris-violet tokens the module-wide exclusion still forbids — plain muted "list" text otherwise, and the Reviewed column's `— (auto)` vs `—` distinction). Two inert seams, each with a grep-able `pmXX seam` comment: the "New order" header CTA (`--action-cta-bg`, pm29) and a per-row "Review" affordance on `PENDING` rows only (pm31; `stopPropagation` so it never fires the row's own selection). No detail/review panel built this unit — `?order=` only drives row highlighting (`aria-current`); an unknown id simply matches no row, which is why "empty-selection, not an error" holds without a dedicated not-found branch. Ripple: `tests/app/route-manifest.test.ts` gained `/products/orders`; `tests/components/admin-nav.test.tsx` extended for the two new links/labels (six→eight). New tests: `tests/app/orders-page.test.tsx` (guard-mocked page test, product-offering-page.test.tsx precedent — guard call, redirect propagation, deep-link/tampered-URL parsing, prop threading into `OrdersTable`), `tests/components/order-status-badge.test.tsx`, `tests/components/orders-table.test.tsx`. **Permission-separation proof, done differently from pm24's live-DB principal:** rather than extending the live-DB `guard.integration.test.ts` (no ephemeral Postgres available this session), the "a catalog `products`-only principal is blocked from Orders" invariant is proven as a fast, DB-free unit test directly against `hasLevel`/`EffectivePermissionMap` (`tests/types/permissions.test.ts`) — `product_orders` is an `OptionalPermissionName` (types/permissions.ts) absent from a catalog-only map entirely, so `hasLevel` rejects it by construction; combined with the guard-mocked proof that the page calls `requirePermission(PERMISSIONS.PRODUCT_ORDERS, ...)` (a distinct permission name from `PERMISSIONS.PRODUCTS`), this covers the checklist's "Inv. cross-check" bullet without a live database. The full `guard.integration.test.ts` behavioral version (a real `productsManagerUserId`-shaped principal hitting the real page) is left to pm34's authz-matrix ship gate, consistent with that unit owning the matrix entry per this spec's own Design section. `prodmgmt-ui-context.md` gained a new §8 (OrderStatusBadge wiring table, negotiated-price indicator, Review affordance, "New order" CTA, Reviewed-column convention) in this same change set, plus a §0 scope-note update. Verified: `tsc --noEmit`, ESLint, and Prettier all clean on the changed/new files; full unit suite green (200 files / 2138 tests); `next build` clean with `/products/orders` in the route list. No live-DB fixture walkthrough performed (same gap as every Phase-2/3 UI unit since pm18 — no live Postgres available in-session).

**pm28 — Order Submission Backend.** `services/ordering/order-preconditions.ts` — `checkOrderPreconditions(tx, input, now)`, the single source of the six-check submission validation set (party `ACTIVE`, BAN non-closed + owned by the party, offering `ACTIVE ∧ billing_only ∧ is_sellable`, ≥1 price row, override price-type/currency validity, 3-day backdating tolerance), every read locked/re-read on `tx` immediately before use — to be reused verbatim by pm30's approval, never duplicated. `services/ordering/create-order.ts` — `createOrder(input, actorId, now)`: standard path inserts order+item, then inventory+first status-history row, flips the order to `COMPLETED`, and writes `PRODUCT_ORDER_CREATED`/`PRODUCT_INVENTORY_CREATED`/`PRODUCT_ORDER_COMPLETED`, all in one transaction; override path commits the order directly as `PENDING` with the override row(s) and writes `PRODUCT_ORDER_CREATED`/`PRODUCT_ORDER_PENDING_APPROVAL`, no inventory. Two disclosed additive repository methods per the spec's own Design section (locked single-row finders only, exact names it specified): `partyRoleRepository.findStatusByIdForUpdate` (`db/repositories/party-role.ts`) and `billingAccountRepository.findStateByIdForUpdate` (`db/repositories/accounts/billing-account.repository.ts`, also returns `refPartyRoleId`/`currency` for the BAN-mismatch and override-currency checks). The BAN-mismatch check (Q15 "BAN belongs to the party's FA") is implemented as `billing_account.ref_party_role_id === input.customerPartyRoleId` directly — `billing_account` carries its own `ref_party_role_id`, set equal to its financial account's at onboarding time (confirmed against `onboard-customer-accounts.ts`), so this is equivalent to joining through the FA without the extra query. Audit ripple (pm28-spec §3): `AUDIT_EVENT_TYPES` gained all six order events (`_CREATED/_PENDING_APPROVAL/_APPROVED/_REJECTED/_COMPLETED/_FAILED`) plus `PRODUCT_INVENTORY_CREATED`, per the spec's explicit instruction, even though this unit only ever writes three of them (`_CREATED`, `_PENDING_APPROVAL`, `_COMPLETED`, plus `PRODUCT_INVENTORY_CREATED`) — matching the module's established "seed the full enum, use a subset" convention (TMF622 `ORDER_STATUSES` precedent). `AUDIT_EVENT_CATEGORY_MAP` categorized the seven as a judgment call absent explicit spec guidance (not `tsc`-caught either way, flagged here): `_CREATED`/`PRODUCT_INVENTORY_CREATED` → Additive, `_PENDING_APPROVAL`/`_APPROVED`/`_COMPLETED` → Change, `_REJECTED`/`_FAILED` → Removal (mirrors `PRODUCT_OFFERING_RETIRED`/`_DISCARDED`'s "terminal negative outcome" precedent). `audit-log-filters.test.tsx` bumped 51→58 options.

**Real deviation, flagged:** pm28-spec §2 point 5's second sentence describes a "compensating step" where an unexpected post-order-insert repo error leaves the order persisted as `FAILED` with `failure_reason`, rather than rolling back. This directly conflicts with two other authorities in the same spec/codebase: (1) §4's own crash-injection requirement — "crash injection between item insert and inventory insert leaves no committed partial state" — reads as a full-rollback proof, not a FAILED-row proof; (2) the Design section's own cited precedent, `onboard-customer-accounts.ts`'s ac04/V7 pattern (Module Inv. #5, `tests/accounts/v07-onboarding-atomicity.integration.test.ts`), which is exactly "inject a failure after early writes, assert the whole transaction rolls back, nothing persists." The referenced external decision doc (`_updatemodule-product-ordering-inventory-plan.md`, cited throughout `prodmgmt-architecture.md`/`prodmgmt-ai-workflow-rules.md` as authoritative for Q1–Q20) is not present anywhere in this repo, so the conflict couldn't be resolved by reading it. Resolved in favor of the stronger, doubly-corroborated signal: `createOrder` is one flat `db.transaction` — any failure at any point, precondition or unexpected write error, rolls back the entire attempt, no row persists. `FAILED` stays a seeded `OrderStatus`/`PRODUCT_ORDER_FAILED` an added `AuditEventType` (TMF622 full-enum-seeded-but-unused convention), but neither is written by this unit. A first implementation attempt used a nested `tx.transaction()` (real Postgres `SAVEPOINT`, confirmed via `drizzle-orm/postgres-js/session.js`'s `.savepoint()` call) plus a catch-and-compensate step to produce the FAILED-persists behavior; discarded once the V7 precedent and the crash-injection test wording were weighed together. If the missing plan doc turns up and says otherwise, a follow-up unit should revisit — the current behavior is the safer, precedent-matching default, not an oversight.

Live-DB verified against an ephemeral, disposable Postgres 16 container (never the dev stack's own `DATABASE_URL` — see `[[local-docker-dev-stack]]`), 13/13 new tests in `tests/db/create-order.integration.test.ts` green: standard-path atomicity (order+item+inventory+history+3 audit rows), override-path park (`PENDING`, override row, zero inventory, zero `COMPLETED` audit), all eight §1 error codes (including both `OFFERING_NOT_ORDERABLE` sub-cases — DRAFT and non-sellable), the TOCTOU re-check (party flipped `SUSPENDED` after a simulated form-read, before submit), and the crash-injection atomicity proof (`productInventoryRepository.insertInventory` spied to throw — whole transaction rolls back, zero order/item/inventory rows). pm26's `ordering-read.integration.test.ts` (11 tests) and the structural/guardrail suites (`ordering-repository-exports.test.ts`, `ordering-module-boundaries.test.ts`) re-verified with no regressions. Full unit suite green (200 files / 2138 tests, unchanged from pm27's count plus this unit's additions net of no new unit-test files — the new integration suite is excluded from the unit config by design); `tsc --noEmit`, `eslint .`, `prettier --check .` all clean. No `db/schema/product.ts` / action / UI change; `services/ordering` stays framework-agnostic with no raw SQL (guardrail-verified).

**pm29 — New Order Wizard UI + Action.** `actions/ordering/create-order.action.ts` (new, the only file in `actions/ordering/` — pinned there by this unit's own `EXPECTED_ORDERING_ACTION_FILES` guardrail) — the standard shape: `requirePermission(PERMISSIONS.PRODUCT_ORDERS, LEVELS.EDIT)` → `isRedirectError` catch → `createOrderSchema.safeParse` → pm28's `createOrder` → on success `revalidatePath('/products/orders')` + `/products/subscriptions` → typed `{ok,code}` result, every one of pm28's eight `OrderPreconditionErrorCode`s passed through unchanged. New boundary guardrail in `tests/guardrails/ordering-module-boundaries.test.ts`: `EXPECTED_ORDERING_ACTION_FILES = { "create-order.action.ts": "createOrderAction" }` (pm19 precedent), asserting `actions/ordering/` contains exactly this one file. `components/products/ordering/new-order-wizard.tsx` (new, `NewOrderWizard`) — the app's first full-screen `Dialog` (no existing size variant to reuse; sized via a `className` override on `DialogContent`) and first use of react-hook-form's `FormProvider`/`useFormContext` (every prior form passed `register`/`control` down as plain props — a 3-step wizard needed context instead to avoid prop-drilling through `WizardStepOffer`/`CharacteristicsEditor`/`OverridePriceFields`). Composes `WizardStepCustomer`, `WizardStepAccount`, `WizardStepOffer` (all new, `components/products/ordering/`), `CharacteristicsEditor` (new — reuses pm21's record↔list boundary-translation *pattern*, not the literal `specification-form.tsx` helpers, since those are typed against `ProductSpecCharacteristics`, not this module's own `CharacteristicsRecord`), and `OverridePriceFields` (new — one row per **flat** current-effective price, list amount struck through when overridden, tiered rows read-only "not overridable," any non-empty override renders the approval banner verbatim from the spec). Step selections (`customer`/`account`/`offering`) are plain `useState` in `NewOrderWizard`; step 3's `quantity`/`startDate`/`characteristicsList`/`overrides` are one `useForm<WizardFormValues>` with a **local** `zodResolver` schema (mirroring `createOrderSchema`'s rules — `inclusiveBilledDateSchema` reused directly for the 3-day backdating fast-fail, same money regex as the wire schema's override check) rather than the wire schema itself, translated to `CreateOrderInput` only at submit — the same local-schema-plus-translation call `price-form.tsx`/`specification-form.tsx` already made, for the same reason (`useFieldArray` needs array shapes the wire schema's `Record`/tuple shapes don't have). Filled pm27's "New order" seam in `orders-table.tsx` (one import + one JSX wrap, `locale` threaded through since it was already a prop on `OrdersTable`, nothing else touched).

**Two disclosed placement/design calls, both flagged:** (1) The wizard's cross-module *reads* (customer search, the selected customer's BAN list with state/currency/cycle/terms, the orderable-offer picker, offer detail) needed a Server Action entry point a client component could call directly (`OnboardAccountsWizard`/`getWizardOptionsAction` precedent), but couldn't live beside the wizard components (`components/**` may not depend on `services/**`/`auth/**` — ESLint `boundaries/dependencies`), in `actions/ordering/` (pinned to exactly `create-order.action.ts` by this same unit's own guardrail), or in `actions/customer/`/`actions/product/` (each pinned by an existing ship-gated allow-list this unit has no mandate to reopen). Landed in `actions/accounts/new-order-wizard-reads.ts` instead — the one action folder with no locked allow-list, already hosting `get-wizard-options.ts`'s own "cross-module wizard-read support" precedent. Its four exports (`wizardSearchCustomersAction`, `wizardGetCustomerAccountsAction`, `wizardSearchOfferingsAction`, `wizardGetOfferingDetailAction`) each re-check `product_orders:EDIT` independently — the same permission `createOrderAction` itself requires — rather than each read's own "native" permission, since a caller who can search through this wizard is by construction about to submit an order. `wizardGetCustomerAccountsAction` composes `searchAccounts(customerPartyRoleId, "customer")` (exact `ref_party_role_id` match — one FA per customer, ac04 precedent) → `getFinancialAccountDetail` for the non-closed BAN list → a per-BAN `getBillingAccountDetail` call for currency/cycle/resolved-terms (an accepted N+1 cost on a short list, the same disclosed trade pm21 made for `getOfferingDetail`); the offer picker filters `ACTIVE ∧ billing_only ∧ is_sellable` server-side inside `wizardSearchOfferingsAction` since `listOfferings` itself carries no such filter. (2) Step 2's BAN list and step 3's offer detail are fetched once by `NewOrderWizard` itself (not inside `WizardStepAccount`/`WizardStepOffer`) and passed down as props — `WizardStepAccount` ended up fully presentational — specifically so navigating back and forward between steps never re-fetches committed data (Design's own explicit requirement), which conditionally-mounted child-owned effects would have violated on every remount. This also drove every loading flag (`isLoadingAccounts`, `isLoadingOfferingDetail`, the two step components' own `isSearching`) to be **derived** state (`accountsLoadedFor !== customer.partyRoleId`, etc.) rather than a `useState` toggled synchronously inside the fetch effect — a plain "set loading, then fetch" effect body trips this codebase's `react-hooks/set-state-in-effect` rule (React Compiler), which the existing `OnboardAccountsWizard` precedent sidesteps by starting in a `"loading"` phase rather than setting one; this unit's four fetch-on-id-change effects instead derive "still loading" by comparing the current id against the id the last successful fetch resolved for, so the effect body never calls `setState` outside its own `.then()` continuation.

**Interpretive call, flagged:** `searchCustomers` (customer module) has no party-role-id lookup mode to toggle onto — unlike `searchAccounts`'s `customer`/`name` modes — so `WizardStepCustomer` offers one name/trading-name search box rather than the "party-role/name toggle" the spec's Design prose names; a non-functional toggle onto a search function that can't honor it seemed worse than one working box. Backdating: the client shows a non-blocking warning banner (reusing `price-form.tsx`'s copy pattern) inside the 3-day tolerance and a blocking `FieldError` beyond it (via `inclusiveBilledDateSchema`'s own message), matching the checklist's "≤3 days → warning, submits; >3 days → field error" exactly; `BACKDATED_START_TOO_FAR` from the server (forced past the client's own check) additionally calls `form.setError("startDate", ...)`, not just a form-level banner.

Ripple: `tests/components/orders-table.test.tsx` needed `@/actions/ordering/create-order.action` and `@/actions/accounts/new-order-wizard-reads` mocked — `OrdersTable` now renders `NewOrderWizard` at its seam, and importing the real (unmocked) action module transitively pulls in `db/client.ts`, which throws on the test env's missing `DATABASE_URL`/`BETTER_AUTH_*` vars; the existing 19 assertions in that file were otherwise unaffected. New tests: `tests/actions/create-order.action.test.ts` (7 tests — every pm28 precondition code passed through unchanged, `FORBIDDEN`/`VALIDATION_ERROR` reject with the service never called, pm20 precedent), `tests/components/characteristics-editor.test.tsx` (record↔list round-trip + component render/add/remove), `tests/components/new-order-wizard.test.tsx` (stepper navigation preserving the customer/account selection across Back/Next with the accounts read-action asserted called exactly once — proving the never-refetch requirement directly — override entry showing/hiding the approval banner, and a full standard-order submit assembling the exact `CreateOrderInput` shape). Verified: `tsc --noEmit`, `eslint .`, and `prettier --check .` all clean **repo-wide** (not just changed files); full unit suite green (203 files / 2158 tests, up from pm28's 200/2138); `next build` clean with `/products/orders` unchanged in the route list (no new route this unit — the wizard is a dialog, not a page). No live-DB fixture walkthrough or browser session performed (same gap as every UI unit since pm18) — the local Docker dev stack was running in-session, but exercising it would mean pointing a fresh `next dev`/rebuild at its Postgres rather than its own already-built `app` container (5 days stale, predates this unit's code), judged out of this unit's scope given the standard verification baseline above was already fully green.

**pm30 — Approval Backend.** `approveOrder` / `rejectOrder` for `PENDING` override orders (backend only — no Server Action or UI; the Review screen fills pm27's "Review" seam in a later unit, and `actions/ordering/` stays pinned to `create-order.action.ts` by pm29's guardrail). **Role helper:** additive finder `roleAssignRepository.findRoleNamesByUserId(db, userId)` (join `role_assign → roles.role_name`) + new `auth/roles.ts`'s `actorHasRole(userId, roleName)` — the codebase's **first role-*name* authority check** (vs `resolver.ts`'s permission-level checks), resolved live per request (platform Inv. #15, never cached/session-read). **Forced ESLint carve-out** (`auth-lockout` precedent): `services/**` can't import general `auth`, so `auth/roles.ts` got its own `auth-roles` boundary element + a `services → auth-roles` allow rule + an `auth-roles → db` rule (it reads the DB, unlike the pure `auth-lockout` helper) in `eslint.config.mjs`. **Shared instantiation:** pm28's standard-path inventory-creation block was factored into `services/ordering/instantiate-order.ts`'s `instantiateOrder(tx, order, item, actorId, now, review?)` — one definition of "create the subscription", two callers (`createOrder` standard path + `approveOrder`); the optional `review` bag stamps `reviewed_by`/`reviewed_at` onto the same COMPLETED update for the approval path only. `createOrder`'s standard path now calls it (behavior byte-identical — same audit ordering, same crash-injection rollback via the `productInventoryRepository.insertInventory` spy, which `instantiateOrder` still calls). **Service** `services/ordering/review-order.ts`: `approveOrder` = MANAGER gate → `db.transaction` [`findByIdForUpdate` lock + re-read → `ORDER_NOT_FOUND`/`ORDER_NOT_PENDING`/`SELF_REVIEW` → reconstruct the exact `CreateOrderInput` from the persisted write-once order/item/override rows → **re-run `checkOrderPreconditions` verbatim** (the single validation source, grep-confirmed: defined once in `order-preconditions.ts`, called only by submit + approve) → `PRODUCT_ORDER_APPROVED` audit → `instantiateOrder` with the reviewer stamp]; a precondition failure returns its concrete code and the order stays `PENDING` (empty tx). `rejectOrder` = same MANAGER gate + locked PENDING re-read, then `updateStatus` `REJECTED` + `failure_reason` + reviewed stamps + `PRODUCT_ORDER_REJECTED` audit; terminal, no inventory, no re-validation. Both share a `lockPendingOrderForReview` helper for steps 1–2. **Audit ripple was already satisfied by pm28** (it seeded all six order events + category-map entries + the 58-option `audit-log-filters.test.tsx` bump), so pm30 only *writes* `_APPROVED`/`_REJECTED` for the first time — no new `AUDIT_EVENT_TYPES`/map/filter-test change this unit. New live-DB tests `tests/db/review-order.integration.test.ts` (happy approve w/ reviewed stamps + 1 inventory + audit rows; reject terminal/no-inventory; `SELF_REVIEW` service guard **and** the `reviewed_by <> submitted_by` DB CHECK via a direct SQL write; `NOT_MANAGER` with grant→revoke proving live resolution both directions; `ORDER_NOT_FOUND`/`ORDER_NOT_PENDING`; the stale-approval `CUSTOMER_NOT_ACTIVE` re-check leaving the order PENDING w/ zero inventory; and the approve-vs-approve → one `COMPLETED` + one `ORDER_NOT_PENDING` + exactly one inventory row and approve-vs-reject → one winner/never-both races, each looped `RACE_RUNS = 4×`). Verified: `tsc --noEmit`, `eslint .`, `prettier --check` all clean **repo-wide**; full unit suite green (204 files / 2173 tests — the new integration file is excluded from the unit config by design, same as pm28's); `next build` clean, route list unchanged (backend-only). **No live-DB fixture walkthrough performed** — no Postgres available in-session (`DATABASE_URL` unset), same gap as pm25/pm27/pm29; the integration suite is `describe.skipIf(!DATABASE_URL)` and type-checks via `tsc` but was not executed against a live DB this session.

**pm31 — Review UI + Actions — implemented and verified (2026-08-12).** Filled pm27's "Review" seam. **Actions:** `actions/ordering/{approve-order,reject-order}.action.ts` (standard shape, pm29/create-order precedent): guard `product_orders:EDIT` → `isRedirectError` catch → `safeParse` (pm25's `approveOrderSchema`/`rejectOrderSchema`) → pm30's `approveOrder`/`rejectOrder` → `revalidatePath` (`/products/orders`, plus `/products/subscriptions` on approve only — reject never instantiates) → typed `{ok,code}` passthrough of every `ReviewOrderErrorCode`. Both appended to `EXPECTED_ORDERING_ACTION_FILES` (now `create`/`approve`/`reject`). `approveOrder` takes no reason, so `approveOrderSchema`'s optional `reason` is validated-but-unconsumed (only reject requires one). **Components (`components/products/ordering/`):** `OrderReviewPanel` (server, presentational) — context strip (customer · BAN · currency · cycle · terms), kv-grid summary (offer+version, qty, start date, characteristics as `key: value`), pricing table with a **Δ% column** (list vs negotiated, negative deltas in danger text), info banner (approval re-validates at approval time), submitted/reviewed metadata; renders read-only detail for every status, `ReviewActions` only when `PENDING`. `ReviewActions` (client) — Approve (accent, confirmation `Dialog` restating list-vs-negotiated for each overridden line) and Reject (danger `AlertDialog`, required reason `FieldError`), disabled + tooltip when `!canReview`. **Page:** `orders/page.tsx` now captures `{userId, permissionMap}` from the guard and, for the `?order=`-selected order, composes the panel via a `buildReviewPanel` helper — `getOrderDetail` (pm26) **unchanged** for the review data, layered with existing read services (`getCustomerDetail`, `getBillingAccountDetail` + `resolveTerm`, `getUserById`) for the summary's resolved names + cycle/terms (the **user-confirmed "reuse existing services" call**), and resolves `canReview` = `actorHasRole(userId,"MANAGER")` ∧ `hasLevel(EDIT)` ∧ `userId !== submittedBy`, with a per-case `reviewDisabledReason`. An unknown/ill-formed id → `null` → no panel (empty-selection). Filled pm27's Review seam in `orders-table.tsx` (button now `selectRow` + `stopPropagation`).

**Disclosed calls / forced companions, all flagged:** (1) **ESLint carve-out** — `app/**` couldn't import the `auth-roles` boundary element (carved out in pm30), so added `"auth-roles"` to the `app` allow rule in `eslint.config.mjs` (same shape pm30 added for `services`), needed for the page's `actorHasRole` call. (2) **Money-arithmetic gate carve-out (user-confirmed)** — the spec-mandated Δ% needs `Number(<amount>)` division, which trips `tests/accounts/grep-gates.test.ts`'s repo-wide "no `Number(...amount)` outside `money.ts`" gate (and product code-standards §2.4). Resolved by an explicit, commented allow-list entry for `order-review-panel.tsx` (+ a non-vacuous guard test) rather than gaming the regex — Δ% is display-only, never a rating/billing source (Inv. #7/#16), so the gate's money-precision rationale doesn't apply. (3) **Code→message mapping lives in `ReviewActions`, not the action** — the actions stay thin typed passthroughs (create-order precedent); the component maps every pm30 code to a user-readable toast, including the spec's exact "Cannot approve: `<reason>`. The order remains pending; reject it if no longer valid." for the eight stale-precondition codes. (4) **Native `title` tooltip** — no shared Tooltip primitive exists in `components/ui`; the disabled Approve/Reject buttons carry their explanatory tooltip on a wrapping `<span title=…>` (a disabled `<button>` doesn't emit hover events). (5) **pm27 seam tests updated** — the two `orders-table.test.tsx` tests that asserted the Review button was inert now assert it selects the order (`?order=`) exactly once without double-firing the row handler, matching the pm23 precedent of rewriting a seam's "wired to nothing" test when the seam is filled. **Tests:** `tests/actions/{approve,reject}-order.action.test.ts` (7 each — success/FORBIDDEN-service-never-called/VALIDATION_ERROR/code-passthrough-no-revalidate/SERVER_ERROR; reject additionally proves it revalidates orders **only**, never subscriptions), `tests/components/review-actions.test.tsx` (enabled/disabled+tooltip for submitter & non-manager, approve restates list-vs-negotiated + submits `{orderId}`, reject requires reason, reject with reason, stale-precondition message mapping), `tests/components/order-review-panel.test.tsx` (renders detail; Δ% display-side; ReviewActions only on PENDING; read-only on COMPLETED/REJECTED + rejection reason shown), plus three new `orders-page.test.tsx` cases (manager≠submitter → `canReview` true with resolved names; submitter → false + "You submitted this order"; non-manager → false + "Requires MANAGER role") and the page's new service imports mocked. Verified: `tsc --noEmit`, `eslint .`, `prettier --check` all clean **repo-wide**; full unit suite green (208 files / 2202 tests, up from pm30's 204/2173); `next build` clean with `/products/orders` unchanged in the route list (the review surface is the page's `?order=` selection panel, not a new route). No live-DB fixture walkthrough or browser session performed (same gap as every UI unit since pm18 — no live Postgres available in-session; the two Server Actions delegate to pm30's already-integration-tested `approveOrder`/`rejectOrder`).

### Not yet implemented (no spec written past pm31)

Only `pm25`–`pm31` are spec'd and built. The order write path (`createOrder` + inventory instantiation) and its audit events shipped in pm28; the New Order wizard UI + its Server Action shipped in pm29; the **manager approval backend** (`approveOrder`/`rejectOrder`) shipped in pm30; the **Review UI + approval Server Actions** (wiring pm30 into pm27's Review seam) shipped in pm31. Everything else `prodmgmt-update-overview.md` requires to hit its own success criteria has **no `specs/pmXX-spec.md` yet**:

- **Subscription lifecycle services** — suspend / resume / terminate (guarded transitions, append-only gap-free status history, 3-day backdating tolerance) and edit-instance-characteristics (never a rating input).
- **Remaining audit event types** — the inventory-lifecycle events `PRODUCT_INVENTORY_CHARACTERISTICS_UPDATED/_SUSPENDED/_RESUMED/_TERMINATED` (the six order events + `PRODUCT_INVENTORY_CREATED` were added in pm28; `_APPROVED`/`_REJECTED` are **written** by pm30's approval backend and now surfaced through pm31's Review UI; `_FAILED` remains seeded-but-unused).
- **Server Actions** for the subscription lifecycle — `actions/ordering/` now holds `create-order`/`approve-order`/`reject-order` (pm29's guardrail extended by pm31); everything under `actions/inventory/` is still unbuilt.
- **UI** — Subscriptions list page and lifecycle action dialogs. (Orders list shipped in pm27; the New Order wizard shipped in pm29; the Review Order panel — approve/reject — shipped in pm31.)
- **Nav** — done in pm27 (both items render; `/products/subscriptions` still 404s until pm33 builds the page).
- **Authz matrix entries** for `/products/orders` and `/products/subscriptions` (`product_orders`/`product_inventory` grants, no cross-grant from catalog `products`) — pm27 only covers the guard-mocked page-level proof + the DB-free permission-separation unit test (see pm27 note); the full live-DB matrix entry is still this bullet's job.
- **Phase 3 ship gate** — guardrails + full sweep, pm09/pm24 precedent.

## Per-unit specs

| Unit | Spec file            | Summary                                                                                                                                                                                                                                                                  |
| ---- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| pm01 | `specs/pm01-spec.md` | Rename `app/(admin)/**` → `app/(app)/**` (14 files, `git mv`); update 6 test imports + 2 path comments; add rename-invariance CI proof. No other change. |
| pm02 | `specs/pm02-spec.md` | `product` schema/migration/seeds in one unit: 3 tables + enum + 3 sequences + all constraints; permission registry wiring; `validation/product/`; `db/seeds/product.ts`; unit + integration guardrail tests. No `app/**`, `actions/product/`, or repository code. |
| pm03 | `specs/pm03-spec.md` | Read backend: 3 finder-only repositories (price repo permanently exports no mutation, Inv. #1), `services/product` (`listOfferings`, `getOfferingDetail`), data-only migration seeding page-size config, `types/product.ts` read models. No `app/**`, `actions/product/`, or UI. |
| pm04 | `specs/pm04-spec.md` | `components/admin-nav.tsx`: flat `NAV_ITEMS` → sectioned `NAV_SECTIONS`, new "Products" section; collapsed-rail divider between sections. No `app/**`, permission check, or dependency change. |
| pm05 | `specs/pm05-spec.md` | `app/(app)/products/product-offering/page.tsx` (guard + searchParams parse + fetch); `LifecycleBadge` + `OfferingTable` + `OfferingDetailRegion` (new). No `services/**`, `db/**`, `validation/**`, `actions/**`, `app/api/**`; no field-level rendering (pm06–08); no authz-matrix entry (pm09). |
| pm06 | `specs/pm06-spec.md` | `OfferingDetail` (new) — populated Details section. `offering-detail-region.tsx` + page threading (edit). No backend, `admin-nav.tsx`, or `offering-table.tsx` change; no specs/prices rendering; no authz-matrix entry. |
| pm07 | `specs/pm07-spec.md` | `CharacteristicChip` + `SpecificationsPanel` (new) — populated Specifications section. `offering-detail-region.tsx` edit (Specifications seam only). No backend or prior-unit UI change; no prices rendering; no authz-matrix entry. |
| pm08 | `specs/pm08-spec.md` | `formatCurrency` (edit) + `PriceTypeBadge` + `TierTable` + `PricesPanel` (new) — populated Prices section. `offering-detail-region.tsx` edit (Prices seam only). No backend or prior-unit UI change; no authz-matrix entry. |
| pm09 | `specs/pm09-spec.md` | `guard.integration.test.ts` edit (authz matrix) + `product-module-boundaries.test.ts` (new) — the module's ship gate: authz-matrix entry + 5-assertion negative-space sweep. Tests + CI only, no product code change; no doc edit needed. |
| pm10 | `specs/pm10-spec.md` | `db/schema/product.ts` edit — nullable, self-referencing `family_offering_id` on `product_offering` + index + self-reference CHECK. Migration `0010_product_offering_family.sql` (schema-diff generated). Schema-only, first Phase 2 unit. No repository/service/type/UI code. |
| pm11 | `specs/pm11-spec.md` | Backend create path: `create-offering.schema.ts` (no `isBundle` field, ever), `insertOffering` repository method (hardcodes `isBundle: false`/`familyOfferingId: null`/`version: 1`/`DRAFT`), `createOffering` service (insert + `PRODUCT_OFFERING_CREATED` audit row, one transaction, no uniqueness pre-check). No Server Action, UI, or page — those are pm19. |
| pm12 | `specs/pm12-spec.md` | Backend data-access primitive: `branchOfferingAsDraft(tx, sourceOfferingId, overrides?)` on `product-offering.ts` — clones an offering plus all its spec/price rows into a new `DRAFT` row, one-hop family resolution, `MAX(version)+1` numbering, `isBundle` copied unconditionally (no `overrides` key). No audit, service, action, or UI — consumed by pm13–pm16. |
| pm13 | `specs/pm13-spec.md` | Backend update path: `update-offering.schema.ts` (`name`/`isSellable`/`billingOnly`/`saveAsNew`, no `offeringId`/`isBundle`), `updateOfferingDraftInPlace` repository method (`DRAFT`-guarded in-place `UPDATE`), `updateOffering` service — four-way routing table (`ACTIVE`→branch, `DRAFT`+`saveAsNew`→branch, plain `DRAFT`→in-place with no-op guard, `RETIRED`→rejected), `PRODUCT_OFFERING_UPDATED`/`_BRANCHED` audit events. No Server Action, UI, or page — those are pm20. |
| pm14 | `specs/pm14-spec.md` | Backend specification CRUD: `create-specification.schema.ts`/`update-specification.schema.ts` (field-identical, no id keys), `product-specification.ts` gains `insertSpecification`/`updateSpecification`/`deleteSpecification` (no status backstop — table has no `lifecycle_status` column), `addSpecification`/`updateSpecification`/`deleteSpecification` services — uniform branch-first-when-`ACTIVE` routing, content-matching `findClonedCounterpart` helper to locate the branched clone's counterpart row, `PRODUCT_SPECIFICATION_CREATED`/`_UPDATED`/`_DELETED` audit events. No Server Action, UI, or page — those are pm21. |
| pm15 | `specs/pm15-spec.md` | Backend price management: `insert-price.schema.ts` (`name`/`priceType`/`currency`/`glCode`/`startDateTime`/`priceCharacteristics`, no id key, nests `priceCharacteristicsSchema` wholesale), `product-offering-price.ts` gains `insertPrice` — the repository's only write, permanently (Inv. #1 relaxes for `insert*` only, never update/delete), `insertPrice` service — two-row branch-first-when-`ACTIVE` routing (pure new content, no counterpart-locate step), 3-day backdating tolerance checked twice (fast-fail in the schema, authoritative against injectable `now` in the service), `PRODUCT_PRICE_ADDED` audit event. No Server Action, UI, or page — those are pm22. |
| pm16 | `specs/pm16-spec.md` | Backend lifecycle: `activate-offering.schema.ts`/`retire-offering.schema.ts` (both single-field `reason`), `product-offering.ts` gains `findActiveInFamily` (`.for("update")` row-locks the whole family by immutable identity, first use of locking in this codebase), `activateOffering` (transactional Inv. 13 re-check + retire-sibling + flip-to-ACTIVE), `retireOffering` (unconditional flip to `RETIRED`, one method backs both retire and discard). Services: `activateOffering` (the mandatory-spec precondition `SPECIFICATIONS_NOT_RESOLVED` is re-checked under a `FOR UPDATE` lock inside the transaction — specs can change (pm14); `NO_PRICE_ROWS` stays a pre-transaction-only check because prices are insert-only (Inv. #1) and the result can't regress), `retireOffering` (`PRODUCT_OFFERING_RETIRED` vs `_DISCARDED` derived from a locked re-read inside the transaction). Four audit events (`_ACTIVATED`/`_SUPERSEDED`/`_RETIRED`/`_DISCARDED`). No Server Action, UI, or page — those are pm23. |
| pm17 | `specs/pm17-spec.md` | Nav: `components/admin-nav.tsx`'s "Products" section relabeled ("Product Offering" → "View Product," same href/icon) + new sibling "Manage Products" item (`/products/manage-products`, `PackagePlus` icon, no `requiredPermission` — deliberately not `cm03`'s locked pattern). Data-only diff, zero render-loop/type changes. §2.6 page-heading rename folded in: `app/(app)/products/product-offering/page.tsx` `metadata.title`/`H1` → "View Product." No backend, Server Action, or new page — `/products/manage-products` 404s until `pm18`. |
| pm18 | `specs/pm18-spec.md` | Frontend: `app/(app)/products/manage-products/{page,loading,error}.tsx` (new) — guard `products:EDIT` → page-loops the existing `listOfferings` twice (`null`, `"RETIRED"`) via private helpers → groups rows into `OfferingFamilyRow`s (one row per family, `ACTIVE` else highest-version primary) → composes `ManageOfferingTable` (new, `components/products/manage/`) — expand/collapse version history, status-matrix row actions (Edit/Add price/Activate/Discard/Retire) and "New offering" CTA, all real UI, all inert seams for pm19–pm23. One flagged exception to "no new backend code": `familyOfferingId` added to `OfferingListRow` (`types/product.ts`) + `findList`'s `SELECT` (`db/repositories/product-offering.ts`) — one column, no new repository method or service export. |
| pm19 | `specs/pm19-spec.md` | UI + Server Action merged: `actions/product/create-offering.action.ts` (new, first file in `actions/product/`) — guard → `safeParse` → delegate to pm11's `createOffering` → `revalidatePath` both product pages, three failure codes only (`VALIDATION_ERROR`/`FORBIDDEN`/`SERVER_ERROR`, no `NAME_CONFLICT`). `offering-form.tsx` + `create-offering-dialog.tsx` (new, `components/products/manage/`) — create-mode-only form (Name, Sellable default-checked, Billing only default-unchecked, no `isBundle` control ever), dialog closes + toasts + `router.refresh()` on success (no URL-state navigation). Fills pm18's "New offering" CTA seam in `manage-offering-table.tsx`. Rewrites the guardrail's "no `actions/product/` folder" assertion into an incremental allow-list in this same commit (Design §2.5), not deferred to pm24. |
| pm20 | `specs/pm20-spec.md` | UI + Server Action merged: `actions/product/update-offering.action.ts` (new) — guard → `safeParse` → delegate to pm13's `updateOffering` → `revalidatePath` both product pages. `offering-form.tsx` gains its edit mode, composed inline in `manage-offering-table.tsx` (no separate dialog file); two independent `handleSubmit` calls, one per save outcome; button set depends on `currentStatus` (DRAFT: Save/Save as new draft; ACTIVE: Create new draft only, behind a warning banner). One flagged read-model exception (same shape as pm18 §2.2): `billingOnly` added to `OfferingListRow`/`findList`'s `SELECT`. Fills both pm18 Edit seams; a branched save auto-expands its family. |
| pm21 | `specs/pm21-spec.md` | UI + Server Actions merged: `actions/product/{create,update,delete}-specification.action.ts` (new) — guard → `safeParse` (create/update only) → delegate to pm14's three services → `revalidatePath` both product pages. `specification-form.tsx` (new) — first form editing a `Record<string, string>` field, via a UI-only `characteristicsList` array (`useFieldArray`) translated to/from the wire `Record` at the form boundary. `specifications-dialog.tsx` (new, `SpecificationsDialog`) — one `Dialog` swapping list/form views, plus a nested `AlertDialog` for delete (DRAFT-only). A direct success keeps the dialog open + refreshes (departs from pm19/pm20's "always close"); a branch-producing success closes/toasts/auto-expands like pm20. Read-model gap closed via a new page-local `fetchSpecificationsByOfferingId` helper (reuses `getOfferingDetail` unmodified) rather than widening `OfferingListRow` or adding a read action. Sixth row-action button (`ListChecks`) added to the table matrix and both phase-2 doc files. |
| pm22 | `specs/pm22-spec.md` | UI + Server Action merged: `actions/product/insert-price.action.ts` (new) — guard → `safeParse` (pm15's own `insertPriceSchema`, unchanged) → delegate to pm15's `insertPrice` → `revalidatePath` both product pages. `price-form.tsx` (new, `PriceForm`) — exactly six fields (name/price type/currency/GL code/pricing model/start date), a new input-only tier-row editor (`useFieldArray`) for the tiered branch, a local `priceFormSchema` that validates only flat-shape checks and duplicates the 3-day tolerance a third time, deliberately not re-declaring tier-contiguity/XOR (those stay defined once, in `tieredPricingCharacteristicsSchema`). `add-price-dialog.tsx` (new, `AddPriceDialog`) — unlike Edit, gets its own dialog file (one submit outcome, `CreateOfferingDialog`'s shape) and its own per-row instance (no new shared table state in `ManageOfferingTable`). Fills every pm18 Add-price seam, primary and expanded-sibling rows alike; a branched success auto-expands the family like pm20/pm21. |
| pm23 | `specs/pm23-spec.md` | UI + Server Actions merged: `actions/product/activate-offering.action.ts` (new, `products:EDIT`) and `retire-offering.action.ts` (new, `products:DELETE` — the permission split from Activate) — each guard → `safeParse` (pm16's own schemas, unchanged) → delegate to pm16's `activateOffering`/`retireOffering` → `revalidatePath` both product pages. `activate-offering-dialog.tsx` (new, `ActivateOfferingDialog`) — plain `Dialog`, accent confirm button, `NO_PRICE_ROWS`/`SPECIFICATIONS_NOT_RESOLVED` render as a persistent inline `Alert` (not a toast, the one departure from pm19/20/22's failure handling), `onSuperseded` prop auto-expands the family. `retire-offering-dialog.tsx` (new, `RetireOfferingDialog`) — one `AlertDialog` handling both Retire and Discard, copy keyed off a `currentStatus` prop, success toast keyed off the server's own `eventType`. Both dialogs use a plain `useState`-backed `Textarea` Reason field, no `react-hook-form` (single-field schemas don't earn it). Fills every remaining pm18 seam (Activate/Discard/Retire, primary and sibling rows) — the last of the five UI units; every button pm18 left inert now has real behavior. |
| pm24 | `specs/pm24-spec.md` | Tests + CI only, the phase's ship gate (pm09's structural precedent, "almost line-for-line"): rewrites `tests/guardrails/product-module-boundaries.test.ts`'s assertion 1 (folder existence + exact export-name grep, closed set), adds guardrail 11 (View stays read-only — `components/products/*.tsx` outside `manage/` and the View Product page tree import nothing from the write surface), guardrail 13 (schema-diff — `product_offering` differs from Phase 1 by exactly `family_offering_id` + its index, the other two tables byte-identical), verifies/extends the guardrail-12 route-manifest check and the `PRODUCT_WRITE_SERVICE_FILES` allow-list, adds an optional guardrail-10 structural backstop (exactly one call site for `deleteSpecification`). Extends `tests/auth/guard.integration.test.ts` with a `productsManagerUserId` (products:EDIT only) principal and direct-Server-Action-call proofs that `products:EDIT` and `products:DELETE` are two different gates. No `app/**`/`services/**`/`db/**`/`actions/**`/`components/**` change. |
| pm25 | `specs/pm25-spec.md` | Ordering & Inventory data layer (Phase 3, unit 1): `ordering`/`inventory` schemas (5 tables, 2 enums, 5 sequences @ 8-digit `lpad`, cross-schema FKs → `customer`/`billing`/`product`/`core`, all constraints), migrations `0022` (schema, drizzle-gen) + `0023` (permission seed DML), `product_orders`/`product_inventory` permission rows + typed constants + optional-map plumbing, `validation/ordering/**` + `validation/inventory/**`, `db/seeds/ordering-inventory.ts` (grants + self-provisioned sample story), bootstrap-db-roles grant blocks, DB guardrail integration tests + new-pgSchema reset ripple. No `db/schema/product.ts` / repository / service / action / UI change. |
| pm26 | `specs/pm26-spec.md` | Repositories + Orders read services (Phase 3, unit 2): five repositories under `db/repositories/ordering/**` + `db/repositories/inventory/**` (insert-only where Inv. #16/#18 mandate; write-once order item, Inv. #15; narrow status/characteristics writes on order/inventory), `types/ordering.ts` read models (`OrderListRow`/`OrderListPage`/`OrderPriceLine`/`OrderDetail`), `services/ordering/{list-orders,get-order-detail}.ts` (framework-agnostic; override-else-catalog resolved once via the catalog's existing derived-effectivity query, RETIRED-version-safe, Inv. #16/#17). Structural export guardrail + `services/ordering` no-`next`/no-SQL boundary guardrail + behavioral integration tests against the pm25 sample story. Completed the pm25 new-pgSchema `beforeAll` reset ripple across 20 DB-resetting test files. No `db/schema/product.ts` / action / UI change. |
| pm27 | `specs/pm27-spec.md` | Nav + Orders page — read surface (Phase 3, unit 3): `components/admin-nav.tsx` gains "Orders"/"Subscriptions" (data-only diff, pm17 pattern); new `app/(app)/products/orders/{page,loading,error}.tsx` (guard `product_orders:READ` → parse pm25's `orders-list.schema.ts` → pm26's `listOrders` → compose); new `components/products/ordering/{order-status-badge,orders-table}.tsx` (TMF622 status pill + the list table, offering-table.tsx's hand-rolled search/sort/filter/pagination pattern reused). Inert seams for New order (pm29) and Review (pm31), each grep-able. `prodmgmt-ui-context.md` gains the OrderStatusBadge/negotiated-price wiring section in the same change set. No `actions/ordering/**`, order detail/review UI, or Subscriptions page — those are later units. **Review fixes:** pagination range-label renders only when `rows.length > 0` (avoids inverted range when `page > totalPages`); Review button stops keydown propagation so keyboard Enter/Space no longer bubbles to the row's `selectRow`; `orders-table` filter labels reuse the exported `ORDER_STATUS_LABELS` map (single source of truth); UI-context doc no longer describes `ACKNOWLEDGED` as "not yet written by this phase"; tests added for empty-state no-data-rows, pagination boundary disabling, and keyboard row/Review activation (19 pass). |
| pm28 | `specs/pm28-spec.md` | Order submission backend (Phase 3, unit 4): `services/ordering/order-preconditions.ts` (`checkOrderPreconditions`, the shared six-check validation set, reused verbatim by pm30) + `services/ordering/create-order.ts` (`createOrder` — one transaction, TOCTOU-checked; standard path atomically instantiates inventory + completes the order, override path parks `PENDING`). Two disclosed additive locked finders (`partyRoleRepository.findStatusByIdForUpdate`, `billingAccountRepository.findStateByIdForUpdate`). Audit ripple: six order events + `PRODUCT_INVENTORY_CREATED`. Live-DB integration tests (13, new `tests/db/create-order.integration.test.ts`) prove atomicity, the override park, all eight §1 error codes, TOCTOU, and crash-injection rollback. No Server Action, UI, or page — those are later units. |
| pm29 | `specs/pm29-spec.md` | New Order wizard UI + Action (Phase 3, unit 5): `actions/ordering/create-order.action.ts` (new, the only file in `actions/ordering/`) — guard `product_orders:EDIT` → `safeParse` (pm28's `createOrderSchema`) → delegate to pm28's `createOrder` → `revalidatePath` both `/products/orders` and `/products/subscriptions`, every pm28 precondition code passed through unchanged. `components/products/ordering/new-order-wizard.tsx` (new, `NewOrderWizard`) — the app's first full-screen `Dialog` and first use of react-hook-form's `FormProvider`/`useFormContext`, composing `WizardStepCustomer`/`WizardStepAccount`/`WizardStepOffer`/`CharacteristicsEditor`/`OverridePriceFields` (all new). Cross-module reads needed by the wizard (customer search, BAN list w/ state·currency·cycle·terms, orderable-offer picker, offer detail) landed in `actions/accounts/new-order-wizard-reads.ts` — disclosed placement call: `components/**` can't depend on `services/**`/`auth/**` directly (ESLint boundary), and `actions/ordering/`/`actions/customer/`/`actions/product/` are each pinned to a fixed file set (this unit's own new `EXPECTED_ORDERING_ACTION_FILES` guardrail included). Fills pm27's "New order" seam in `orders-table.tsx` (one import + one JSX wrap). No Subscriptions page, Review screen, or `actions/inventory/**` — those are later units. |
| pm30 | `specs/pm30-spec.md` | Approval backend (Phase 3, unit 6): `services/ordering/review-order.ts`'s `approveOrder`/`rejectOrder` for `PENDING` override orders — MANAGER-role holder of `product_orders:EDIT` who is not the submitter approves (full re-validation under locks via the shared `checkOrderPreconditions`, then instantiation, `reviewed_by`/`reviewed_at` stamped) or rejects (terminal, `failure_reason`, no inventory). Additive `roleAssignRepository.findRoleNamesByUserId` + new `auth/roles.ts`'s `actorHasRole` (first role-name check, live per request) + its `auth-roles` ESLint carve-out (`services → auth-roles`, `auth-lockout` precedent). Shared `instantiateOrder` (`services/ordering/instantiate-order.ts`) factored out of pm28's `createOrder` — one definition of "create the subscription", two callers. Live-DB integration/concurrency tests (`tests/db/review-order.integration.test.ts`): happy approve, reject, `SELF_REVIEW` service guard + DB CHECK, `NOT_MANAGER` w/ live grant/revoke, stale-approval `CUSTOMER_NOT_ACTIVE`, and the approve-vs-approve / approve-vs-reject races (4× each). Backend only — no Server Action or UI (`actions/ordering/` stays pinned to `create-order.action.ts`); audit ripple pre-satisfied by pm28. |
