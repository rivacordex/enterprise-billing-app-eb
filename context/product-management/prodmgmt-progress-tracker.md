# Product Management — Progress Tracker

## Status

Phase 1 (all 9 units) implemented and committed. Module is ship-gate-verified (pm09) at the DB/test level; CI SAST/DAST baseline is a pipeline step outside this tooling.

Phase 2 (CRUD fast-follow) started: pm10, pm11, pm12, pm13, and pm14 implemented, verified, and committed.

| Unit | Name                                                                 | Commit    |
| ---- | --------------------------------------------------------------------- | --------- |
| pm01 | Route-group rename `(admin)` → `(app)` + rename-invariance CI proof   | `313a66e` |
| pm02 | DB foundation + validation schemas + seeds (`product` schema)         | `962a2e8` (fix `232d78d`) |
| pm03 | Repositories + `services/product`                                     | `a168465` |
| pm04 | Nav refactor (`NAV_ITEMS` → `NAV_SECTIONS`)                            | `2f5fd35` |
| pm05 | Page — one section per unit (table → detail → specs → prices)         | `e0d026f` |
| pm06 | Offering detail section (populated `OfferingDetail` fields)           | `bc33109` |
| pm07 | Specifications panel (populated `SpecificationsPanel`)                | `998ed1b` |
| pm08 | Prices panel (populated `PricesPanel`, `formatCurrency`)              | `9561d27` |
| pm09 | Authz-matrix entry + guardrail sweep (ship gate)                      | `e94e565` |
| pm10 | Schema: `family_offering_id` version-lineage column (Phase 2, unit 1) | `5884e76` |
| pm11 | Backend: Create offering (`insertOffering`, `createOffering`) (Phase 2, unit 2) | `a349a97` |
| pm12 | Backend: Branch-as-draft primitive (`branchOfferingAsDraft`) (Phase 2, unit 3) | `273a764` |
| pm13 | Backend: Update offering (`updateOfferingDraftInPlace`, `updateOffering`) (Phase 2, unit 4) | `2b44f21` |
| pm14 | Backend: Specification management (Phase 2, unit 5)                   | _pending commit_ |

**Renumbering note:** `pm02-spec.md` bundles DB foundation + `validation/product/` + seeds into one unit, superseding an earlier 3-way split. Numbers above match `pm00-build-plan.md`'s 9-unit count.

## Recurring patterns (apply across units — not repeated per-unit below)

- **Permission-name ripple:** adding `"products"` to `PERMISSION_NAMES` forced `products: null`/`"DELETE"` into every hardcoded `EffectivePermissionMap` object-literal fixture across ~25+ test files (`tsc`-caught for object literals, but a few hardcoded array-length assertions in `roles-read.service.test.ts` were not). See `[[permission-name-addition-ripple]]`.
- **Cross-schema integration-test ripple:** every pre-existing `tests/**/*.integration.test.ts` doing a fresh `DROP SCHEMA core CASCADE` + migrate cycle needed `DROP SCHEMA IF EXISTS "product" CASCADE` added first (26 files) — otherwise the next file's migration fails with "schema product already exists." See `[[new-pgschema-integration-test-ripple]]`.
- **`db → validation` ESLint boundary:** both `db/schema/product.ts` (type-only JSONB import) and `types/product.ts` (`SpecificationCard`/`PriceCard` embedding Zod-owned shapes) needed `"validation"` added to their allowed-import targets in `eslint.config.mjs` — a forced companion change in pm02 and pm03, not scope creep.
- **Region-seam pattern:** pm05 built `OfferingDetailRegion` with three placeholder frames (Details/Specifications/Prices) carrying `{/* pmXX */}` seam comments; pm06/pm07/pm08 each filled exactly one seam, leaving the other two frames byte-unchanged, and each left the `<h2>` section title owned by the region (not the child component).
- **Verification baseline per unit:** `tsc --noEmit` + `eslint .` + `prettier --check` clean, plus `npx vitest run` (unit config); most units left CI SAST/DAST and a live dev-server walkthrough unverified (no CI environment / browser available in-session) — noted per-unit below only where it diverges from this default.

## Per-unit notes

**pm01** — Renamed `app/(admin)/**` → `app/(app)/**` (14 files, `git mv`), updated 6 test imports + 2 path comments, added `tests/app/route-manifest.test.ts` as the rename-invariance guardrail (set-equal manifest, old group gone, no stale `(admin)` reference).

**pm02** — `db/schema/product.ts` (`product` schema: `lifecycle_status` enum, 3 sequences, 3 tables — `product_offering`/`product_specifications`/`product_offering_price` — full constraints), migration `0006_product.sql` + `products` permission row, permission registry wiring, `types/product.ts`, `validation/product/` (spec characteristics, pricing characteristics w/ tier-contiguity + amount-XOR-tiers discriminated union, offering-list searchParams), `db/seeds/product.ts` (2 offerings/3 specs/6 prices, ADMIN→products:DELETE). Follow-up fix (`232d78d`, new migration `0007`, not an edit to the applied `0006`): `product_offering.last_edited_by` FK changed to `ON DELETE SET NULL` (matching the "last actor" convention elsewhere); `CHECK (amount >= 0)` added to `product_offering_price` as DB-level defense-in-depth.

**pm03** — Read backend: three finder-only repositories (`product-offering`, `product-specification`, `product-offering-price` — the price repo permanently exports no mutation function, Inv. #1), `listOfferings` (configurable page size via `SYSTEM_CONFIG`, migration `0008_product_config.sql` seeding `'5'`) and `getOfferingDetail` (injectable-clock `resolveEffectivityStatus`, `[start, successorStart)` boundary rule, `LEAD() OVER (PARTITION BY ...)` window for derived `endDateTime`). Migration numbered `0008` not `0007` (pm02's follow-up fix already consumed `0007`).

**pm04** — `components/admin-nav.tsx`: flat `NAV_ITEMS` → sectioned `NAV_SECTIONS`, new "Products" section (Product Offering → `/products/product-offering`) above "Administration"; collapsed-rail hairline divider between sections; Administration items/active-state unchanged. No permission check (nav stays permission-blind by design). Manually verified against a live dev container: correct render order, active-state pill, collapsed-mode divider/tooltips, and a clean 404 on the not-yet-built route.

**pm05** — `app/(app)/products/product-offering/page.tsx` (guard-first, lenient searchParams parse via `.catch()`), `LifecycleBadge`, `OfferingTable` (search/filter/sort/paginate/select, all URL state via `useTransition`), `OfferingDetailRegion` (the three-seam shell with empty/not-found states only). Added `/products/product-offering` to the frozen route manifest.

**pm06** — `OfferingDetail` component fills the Details seam: mono ID eyebrow, name, `LifecycleBadge`, derived flag chips (`Bundle`/`Sellable`/`Billing only`, plus an ACTIVE-not-sellable warning chip), labeled Version/Last Modified/Last Edited By grid. User-verified live against a dev container ("looks good").

**pm07** — `CharacteristicChip` + `SpecificationsPanel` fill the Specifications seam: per-spec card with mono ID, name, `Mandatory`/`Default` badges (flag-true-only, independent of each other), Default-value row shown only when non-null, JSONB characteristics as chips in insertion order, muted empty state.

**pm08** — `formatCurrency` (`lib/formatters.ts`), `PriceTypeBadge`, `TierTable`, `PricesPanel` fill the Prices seam: per-price card with type badge, effectivity state tag (Starts.../Superseded/none), flat amount via `formatCurrency` or tier table for tiered pricing (never both), conditional field rows, derived Effective window ("Open-ended" not "Never"), current/future/superseded card styling. Sequencing note: pm08's component work started before pm07 was committed (explicit user direction); the combined diff was later split back into two clean, spec-matching commits (`998ed1b` then `9561d27`) before either was pushed.

**pm09 (ship gate)** — Tests + CI only, no product feature code touched. Confirmed all 6 inherited guardrails (price immutability structural half, overlap constraint, derived effectivity, JSONB validation, deep link, rename invariance) present and green — flagged that "price immutability bumps version" is not yet testable in v1 since no price-mutation path exists at all (correctly built that way by pm02/pm03; the CRUD fast-follow will exercise it). Added: `products:READ/EDIT/DELETE` to the authz matrix in `guard.integration.test.ts`, and `tests/guardrails/product-module-boundaries.test.ts` (new — no `actions/product/` folder, no `app/api/product*`, price repo write-export shape, no audit-write import from product read paths, route manifest sanity). Real deviation found by running the integration suite (not by reading the spec): the `products` permission is migration-seeded DML (`0006_product.sql`), so the spec's literal "insert it in the test's bulk seed array" snippet violates the unique constraint — fixed by `SELECT`-ing the migration-seeded row instead of re-inserting it. Doc audit (architecture §4, code-standards §8, permission constants) found no gap needing a fix.

**Post-ship polish (2026-07-09, `e2bb187`)** — UI density pass, no data-layer change: tightened page/section padding, moved the Details field grid to a wrapped flex row. Per explicit instruction, deleted `CharacteristicChip`/`TierTable` entirely — `SpecificationsPanel`/`PricesPanel` now inline JSONB characteristics/tiers as plain text instead of dedicated widgets. Docs (`prodmgmt-code-standards.md`, `prodmgmt-ui-context.md`) updated to match.

**Post-ship deployment bug (2026-07-09, `d5f7148`)** — deployed ACA instance 500'd on `/products/product-offering` with `42501: permission denied for schema product`. Root cause: `db/bootstrap/bootstrap-db-roles.sql` (from User Management) only ever granted `app_runtime`/`app_migrate` privileges on `core`; its own header comment flagged `product`/`customer`/`billing`/`accounting` as follow-ups that never happened when `product` shipped. Fixed by appending a `product` schema GRANT/REVOKE block (plus sequence grants, since `product` has ID columns using `nextval()` unlike `core`). **Any future schema (`customer`, `billing`, `accounting`, ...) must add its own grant block to this file in the same change that adds its migration, or this recurs.**

---

## Phase 2 (CRUD Fast-Follow)

**pm10 — Schema: `family_offering_id` version-lineage column.** `db/schema/product.ts`: added nullable, self-referencing `familyOfferingId` (`ON DELETE RESTRICT`) to `productOffering`, plus `product_offering_family_idx` and the `product_offering_family_not_self_check` CHECK guard, exactly as specced. Migration `0010_product_offering_family.sql` (schema-diff generated, not `--custom`), journal/snapshot regenerated. Diff hygiene confirmed: only `product.ts`, the new migration, and its meta files changed — `product_specifications`/`product_offering_price` byte-identical.

Two implementation notes beyond the literal spec text:
- **Self-referencing FK needed a type annotation Drizzle's inference can't derive on its own**: `text("family_offering_id").references(() => productOffering.productOfferingId, ...)` fails `tsc` with "implicitly has type 'any'" (circular initializer) — fixed per Drizzle's documented pattern by annotating the callback's return type: `(): AnyPgColumn => productOffering.productOfferingId`. Confirmed this is compile-time only (regenerating the migration afterward produced "No schema changes, nothing to migrate").
- **FK constraint name is truncated by Postgres's 63-char identifier limit**, as the spec flagged as a possibility: `product_offering_family_offering_id_product_offering_product_offering_id_fk` (75 chars) → truncated to `product_offering_family_offering_id_product_offering_product_of`. Functionally fine; noting the actual name here per the spec's instruction, for any future code that needs to reference it explicitly (e.g. a targeted `DROP CONSTRAINT`).

Verified against the running dev DB (`docker exec` + `psql`, not a throwaway DB — migration is additive `ALTER TABLE`, not the destructive integration-test reset path): `\d product.product_offering` shows the column/FK/CHECK/index exactly as specced; self-reference guard and FK-integrity both reject as expected; a hand-inserted root+branch pair round-trips through the `WHERE product_offering_id = :rootId OR family_offering_id = :rootId` lookup; both seeded Phase 1 offerings remain `family_offering_id IS NULL` post-migrate.

**One in-scope test fix**: `tests/db/product-schema.test.ts`'s "exact snake_case column set" assertion for `product_offering` needed `family_offering_id` added to its expected list — the spec's "existing tests pass unmodified" claim didn't hold for this one test, since it enumerates the exact column set by design. `product_specifications`/`product_offering_price` sections untouched. All other Phase 1 tests (1411 total) pass unmodified.

**Known unrelated pre-existing gap, not caused by or fixed in this unit**: `tests/guardrails/product-module-boundaries.test.ts`'s "has no actions/product/ folder" check currently fails — an empty, untracked `actions/product/` directory exists in the working tree that this unit did not create and does not touch (pm10 is schema-only; nothing in its diff goes near `actions/`). Flagged for the user rather than silently deleted, since its origin is unknown and pm10's scope doesn't cover it.

**pm11 — Backend: Create offering.** Implemented exactly per `pm11-spec.md`: `validation/product/create-offering.schema.ts` (new — `name`/`isSellable`/`billingOnly` only, no `isBundle` key at all), `db/repositories/product-offering.ts` (edit — added `insertOffering`, `findList`/`findDetailById` untouched, `isBundle: false` and `familyOfferingId: null` hardcoded literals at the insert call site), `types/audit.ts` (edit — appended `"PRODUCT_OFFERING_CREATED"` as the last entry), `services/product/create-offering.ts` (new — `createOffering`, no pre-transaction uniqueness check, insert + audit row atomic in one `db.transaction`, mirrors `createRole`). No `actions/`, `components/`, or `app/` changes; no `next/*` import or `"use server"` directive in either new file.

Three companion fixes were required for existing tests/types to stay green — none were in the spec's stated diff list, but each is a build-gate necessity, following the same "forced companion change, not scope creep" precedent as pm02's ESLint-boundary edit and pm10's `product-schema.test.ts` fix:
- **`types/audit-log.ts`** — `AUDIT_EVENT_CATEGORY_MAP` is a `Record<AuditEventType, AuditEventCategory>`; adding an `AUDIT_EVENT_TYPES` entry without a matching map entry fails `tsc`. Added `PRODUCT_OFFERING_CREATED: "Additive"` (same category as `ROLE_CREATED`/`USER_CREATED`/`CONTACT_CREATED`). Matches `[[audit-event-type-addition-ripple]]`.
- **`tests/components/audit-log-filters.test.tsx`** — the hardcoded "31 event types" / `toHaveLength(32)` assertions don't move with a new `AUDIT_EVENT_TYPES` entry (`tsc` doesn't catch this one, per the same memory). Bumped to 32/33, and added an explicit assertion that `PRODUCT_OFFERING_CREATED` renders under the "Additive" optgroup.
- **`tests/db/product-repository-exports.test.ts`** and **`tests/guardrails/product-module-boundaries.test.ts`** — two pm09 ship-gate guardrails were written when `services/product/`and the product repositories were 100% read-only (Phase 1), and both blanket-forbade any mutation-shaped export/import under those paths. Phase 2's architecture doc explicitly relaxes this for the *offering* repository only (write methods are expected going forward; the price repository's zero-mutation rule stays permanent, Inv. #1) and expects write services under `services/product/` to legitimately import the audit-write path (Inv. #7 only ever covered *read* paths). Fixed both to whitelist by name rather than deleting the guardrail outright: the repository-exports test now forbids every mutation-shaped export except `insertOffering`; the boundary test now excludes `create-offering.ts` from the "no audit import" scan of `services/product/`, so it still fully protects the two read services (`list-offerings.ts`, `get-offering-detail.ts`). Both will need one more name added to their allow-lists per future write unit (pm12 branch, pm16 activate, pm17 retire, etc.) — flagged here so that's expected, not a surprise.

**Resolved before commit:** the empty, untracked `actions/product/` directory both pm10 and pm11's first-pass note flagged (neither unit created it, origin never identified) was removed — it was fully empty, so deletion was lossless — and `tests/guardrails/product-module-boundaries.test.ts` now passes clean, including its "has no actions/product/ folder" assertion (pm11's own §5 boundary checklist requires exactly this).

**Verification:** `tsc --noEmit`, `eslint .` (one pre-existing, unrelated error in `components/customers/organization-form.tsx` — not touched by this diff), and `prettier --check .` all clean. Full `npx vitest run`: 1412/1412 passing after the `actions/product/` removal above. **Live-DB backend-correctness checks (spec §5) completed** against the running dev DB (`db.transaction`-wrapped script via `tsx`, mirroring pm10's `docker exec`/`psql` approach, cleaned up after): confirmed a fake/non-existent `actorId` causes the audit insert to fail on its FK constraint *and* rolls back the offering insert (proving the transaction is atomic, not just structurally wrapped); confirmed a real actor id produces `{ ok: true, offeringId: 'PRDOFR######' }` with the row showing `familyOfferingId: null`, `version: 1`, `lifecycleStatus: 'DRAFT'`, `isBundle: false`, and exactly one matching `PRODUCT_OFFERING_CREATED` audit row with the three input fields in `afterData`. Concurrent-same-name and crafted-`isBundle`-input checks were not separately exercised (the type system already makes the latter unrepresentable per the spec's Design section) but the core insert/audit/atomicity path is now confirmed against a live database, not just reviewed by inspection.

**pm12 — Backend: Branch-as-draft primitive.** Implemented exactly per `pm12-spec.md`: single-file edit to `db/repositories/product-offering.ts` — added `BranchOfferingOverrides` type (`name`/`isSellable`/`billingOnly` only, no `isBundle` key), private `resolveNextVersion` helper (`MAX(version)` scoped via `WHERE product_offering_id = :rootId OR family_offering_id = :rootId`), and `branchOfferingAsDraft(tx, sourceOfferingId, overrides?)` cloning the source offering plus all its specification and price rows into a new `DRAFT` row. `isBundle` copied unconditionally from `source.isBundle`, never read off `overrides` (grep-confirmed, no code path exists to read it even via an `as any` cast). Family root resolved inline (one hop, `source.familyOfferingId ?? source.productOfferingId`) — not extracted to a shared helper, per the spec's explicit "not yet" reasoning. Cloned price rows copy `created_at` from source (forensic content); the offering row's own `lastModified`/`lastEditedBy` are omitted and fall through to column defaults, matching `insertOffering`'s precedent. No audit write, no `actorId` parameter, no schema/service/action/UI change — `findList`, `findDetailById`, and `insertOffering` all byte-unchanged.

**Verification:** `tsc --noEmit`, `eslint .` (same two pre-existing unrelated warnings/error in `components/customers/*` — untouched by this diff), and `prettier --check .` (one formatting pass needed on the new code, applied) all clean. Full `npx vitest run`: 1412/1412 passing unmodified, including `tests/guardrails/product-module-boundaries.test.ts` (5/5, confirms no `audit.repository`/`insertAuditEvent`/`AUDIT_LOG` reference was introduced). **Live-DB backend-correctness checks (spec §5) completed** against the running dev DB via a `db.transaction`-wrapped `tsx` script that deliberately threw at the end to force a rollback (nothing persisted — confirmed by re-querying `product.product_offering` before/after and diffing byte-for-byte): root-branch family/version resolution (`family_offering_id` → source's own id, `version` 1→2), branch-of-a-branch one-hop resolution (still resolves to the original root, not the intermediate branch, `version` →3), `lifecycle_status` always `DRAFT` regardless of source status (tested against both an `ACTIVE` seed root and a `DRAFT` branch-of-that-root as source), no-override field copying, `isSellable: false` override correctly applied (proves `??` not a truthy check), `is_bundle: true` surviving cloning from a hand-inserted fixture row, a crafted `overrides` object with an `isBundle: true` key (via `as any`) still producing `is_bundle` from the source not the crafted value, full spec/price child-row cloning (2 specs/4 prices on the seeded "5G Nationwide Service Plan" root) with fresh distinct ids and byte-identical content field-by-field (including price `created_at`), zero-children branch producing zero new child rows, and the source offering plus its specs/prices completely unchanged after every branch operation above.

**pm13 — Backend: Update offering.** Implemented exactly per `pm13-spec.md`: `validation/product/update-offering.schema.ts` (new — `name`/`isSellable`/`billingOnly`/`saveAsNew`, no `offeringId` or `isBundle` key), `db/repositories/product-offering.ts` (edit — added `updateOfferingDraftInPlace`, a `WHERE lifecycle_status = 'DRAFT'`-guarded in-place `UPDATE` that never touches `version`/`familyOfferingId`/`isBundle`; `findList`/`findDetailById`/`insertOffering`/`branchOfferingAsDraft` untouched), `types/audit.ts` (edit — appended `PRODUCT_OFFERING_UPDATED` then `PRODUCT_OFFERING_BRANCHED`, in that order), `services/product/update-offering.ts` (new — `updateOffering`, implementing the four-way routing table: in-place edit for a plain `DRAFT` save with a no-op short-circuit, `branchAndAudit` shared by the `ACTIVE`-target and `saveAsNew`-on-`DRAFT` paths, `OFFERING_RETIRED`/`OFFERING_NOT_FOUND` rejections before any write). No `actions/`, `components/`, or `app/` changes; no `next/*` import or `"use server"` directive in either new file.

Two forced companion fixes, same "expected, not scope creep" precedent pm11 flagged for its own two guardrail updates (and explicitly predicted would recur here):
- **`types/audit-log.ts`** — `AUDIT_EVENT_CATEGORY_MAP` needed both new entries: `PRODUCT_OFFERING_UPDATED: "Change"` (matches `USER_UPDATED`/`ROLE_UPDATED`) and `PRODUCT_OFFERING_BRANCHED: "Additive"` (a branch always creates a new row, same family as `PRODUCT_OFFERING_CREATED`). `tsc`-caught, matches `[[audit-event-type-addition-ripple]]`.
- **`tests/components/audit-log-filters.test.tsx`** — bumped the hardcoded "32 event types" / `toHaveLength(33)` assertions to 34/35 (not `tsc`-caught, per the same memory) and added explicit assertions that `PRODUCT_OFFERING_BRANCHED` renders under "Additive" and `PRODUCT_OFFERING_UPDATED` under "Change".

One additional pair of forced fixes, not predicted by name in pm11's note but flagged there as inevitable ("one more name added to their allow-lists per future write unit"):
- **`tests/db/product-repository-exports.test.ts`** — the offering-repository mutation-name assert now whitelists `updateOfferingDraftInPlace` alongside `insertOffering` (via an explicit `ALLOWED_OFFERING_MUTATIONS` set, replacing the single `!== "insertOffering"` check) rather than disabling the assert.
- **`tests/guardrails/product-module-boundaries.test.ts`** — `PRODUCT_WRITE_SERVICE_FILES` now includes `"update-offering.ts"` alongside `"create-offering.ts"`, so the "no product read path imports the audit-log write path" sweep continues to check only the two genuinely read-only services.

**Verification:** `tsc --noEmit`, `eslint .` (same two pre-existing unrelated warnings/error in `components/customers/*` — untouched by this diff), and `prettier --check .` all clean. Full `npx vitest run`: 1412/1412 passing (including both updated guardrail tests) after the two companion fixes above. **Live-DB backend-correctness checks (spec §5) completed** against the running dev DB, via a `tsx` script (copied into the repo root to resolve `node_modules`/`@/` paths, deleted immediately after the run — never checked in) that exercised the real `updateOffering` service and `productOfferingRepository` methods end-to-end (no rollback trick — the service owns its own `db.transaction`, so this ran real commits) against offering rows carrying a distinctive `PM13VERIFY-` name prefix, then explicitly deleted every row it created (offerings, their audit rows) by that prefix and re-queried to confirm zero remained: in-place `DRAFT` edit (name/isSellable/billingOnly persisted, `lastModified` bumped, exactly one `PRODUCT_OFFERING_UPDATED` audit row matching before/after values); no-op guard on an identical-values save (`lastModified` unchanged, zero new audit rows); `saveAsNew` on a `DRAFT` with values identical to current (still branches — no no-op short-circuit on this path — original draft byte-identical afterward, new sibling `DRAFT`, exactly one `PRODUCT_OFFERING_BRANCHED` audit row); `ACTIVE`-target edit (source row byte-identical afterward, new sibling `DRAFT` with `version` = family max + 1, correct `familyOfferingId`, exactly one `PRODUCT_OFFERING_BRANCHED` audit row with `sourceOfferingId` on both `beforeData`/`afterData` matching the source's id); not-found guard (`OFFERING_NOT_FOUND`, no writes); `RETIRED` guard (`OFFERING_RETIRED`, zero audit rows written). Did not touch the shared `TOREMOVE-Template-*` seed fixtures at all — the `ACTIVE`/`RETIRED` test rows were freshly inserted and fully cleaned up, unlike pm12's verification which branched off the real seed row.

**pm14 — Backend: Specification management.** Implemented exactly per `pm14-spec.md`: `validation/product/create-specification.schema.ts` + `update-specification.schema.ts` (new, field-identical — `name`/`isMandatory`/`isDefault`/`defaultValue`/`productSpecCharacteristics`, no `offeringId`/`specId` key in either), `db/repositories/product-specification.ts` (edit — added `insertSpecification`/`updateSpecification`/`deleteSpecification`; `findByOfferingId` byte-unchanged), `types/audit.ts` (edit — appended `PRODUCT_SPECIFICATION_CREATED`/`_UPDATED`/`_DELETED`), `services/product/add-specification.ts`/`update-specification.ts`/`delete-specification.ts` (new — uniform branch-first-when-ACTIVE routing table across create/update/delete, `findByOfferingId` reused for both existence-check and pre-branch content snapshot, private `findClonedCounterpart` content-matching helper duplicated once between update/delete per the spec's explicit judgment call). No `actions/`, `components/`, `app/`, or `db/schema/` changes; no `next/*` import or `"use server"` directive in any of the three service files.

Two forced companion fixes, same "expected, not scope creep" precedent as pm11/pm13's own (and explicitly predicted by pm11's note — "one more name added to their allow-lists per future write unit"):
- **`types/audit-log.ts`** — `AUDIT_EVENT_CATEGORY_MAP` needed all three new entries: `PRODUCT_SPECIFICATION_CREATED: "Additive"`, `PRODUCT_SPECIFICATION_UPDATED: "Change"`, `PRODUCT_SPECIFICATION_DELETED: "Removal"` (mirrors `CONTACT_CREATED`/`_UPDATED`/`_DELETED`'s categories exactly). `tsc`-caught, matches `[[audit-event-type-addition-ripple]]`.
- **`tests/components/audit-log-filters.test.tsx`** — bumped the hardcoded "34 event types" / `toHaveLength(35)` assertions to 37/38 (not `tsc`-caught, per the same memory) and added explicit assertions that each new type renders under its correct optgroup.

One additional gap not named anywhere in `pm14-spec.md`'s own diff list, found by running the full suite rather than by reading the spec (same category as pm11's two undocumented guardrail fixes):
- **`tests/db/product-repository-exports.test.ts`** — its `"productSpecificationRepository exports no mutation function"` assertion is a hard-coded blanket prohibition pm09 wrote when the specification repository was still finder-only; this unit's three new write methods trip it immediately. Fixed the same way pm11 fixed the analogous offering-repository assertion: added an `ALLOWED_SPECIFICATION_MUTATIONS` allow-list (`insertSpecification`/`updateSpecification`/`deleteSpecification`) rather than deleting the guardrail. `pm14-spec.md` names `tests/guardrails/product-module-boundaries.test.ts`'s `PRODUCT_WRITE_SERVICE_FILES` extension explicitly but never mentions this second, older guardrail — flagged here so it isn't rediscovered as new by pm15/pm16.

**Verification:** `tsc --noEmit`, `eslint .` (same two pre-existing unrelated errors in `components/customers/*` — untouched by this diff), and `prettier --check .` all clean. Full `npx vitest run`: 1412/1412 passing (same count as pm13 — no new `it` blocks added, only existing assertions extended). **Live-DB backend-correctness checks (spec §5) completed** against the running dev DB, via a `tsx` script (`node --conditions=react-server --env-file=.env --import tsx`, repo-root, deleted immediately after — never checked in) that exercised all three real services end-to-end against a fresh `PM14VERIFY-`-prefixed `DRAFT` offering and the real seeded `ACTIVE` offering ("5G Nationwide Service Plan", which carries two specifications with different content — used to confirm `findClonedCounterpart` picks the correct row among multiple candidates, not just when there's only one). All 58 assertions passed: direct-`DRAFT` and branch-first paths for create/update/delete; `ACTIVE` source row and all its specification/price rows confirmed byte-identical (via `JSON.stringify` snapshot diff) after every branch operation; same-values `updateSpecification` resubmit confirmed to still write a new audit row (no no-op guard, as designed); `OFFERING_NOT_FOUND`/`OFFERING_RETIRED`/`SPECIFICATION_NOT_FOUND` guards (including the id-exists-but-wrong-offering case) all confirmed to write nothing. Cleanup deleted every row created (offerings, their cloned specification/price rows, and all `PRODUCT_SPECIFICATION_*` audit rows — this unit is the first to ever write that event-type family, so the delete-by-event-type is exact, not a blanket wipe) and re-confirmed zero remained; one real snag hit and fixed here rather than papered over: `product_offering_price`/`product_specifications` are both `ON DELETE RESTRICT` (not cascading) against `product_offering`, so the cleanup script had to delete price/spec rows for every branch-cloned offering before deleting the offering row itself, or the delete failed on the FK.

## Per-unit specs

| Unit | Spec file            | Summary                                                                                                                                                                                                                                                                  |
| ---- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| pm01 | `specs/pm01-spec.md` | Rename `app/(admin)/**` → `app/(app)/**` (14 files, `git mv`); update 6 test imports + 2 path comments; add rename-invariance CI proof. No other change. |
| pm02 | `specs/pm02-spec.md` | `product` schema/migration/seeds in one unit: 3 tables + enum + 3 sequences + all constraints; permission registry wiring; `validation/product/`; `db/seeds/product.ts`; unit + integration guardrail tests. No `app/**`, `actions/product/`, or repository code. |
| pm03 | `specs/pm03-spec.md` | Read backend: 3 finder-only repositories (price repo permanently exports no mutation, Inv. #1), `services/product` (`listOfferings`, `getOfferingDetail`), data-only migration seeding page-size config, `types/product.ts` read models. No `app/**`, `actions/product/`, or UI. |
| pm04 | `specs/pm04-spec.md` | `components/admin-nav.tsx`: flat `NAV_ITEMS` → sectioned `NAV_SECTIONS`, new "Products" section; collapsed-rail divider between sections. No `app/**`, permission check, or dependency change. |
| pm05 | `specs/pm05-spec.md` | `app/(app)/products/product-offering/page.tsx` (guard + searchParams parse + fetch); `LifecycleBadge` + `OfferingTable` + `OfferingDetailRegion` (new). No `services/**`, `db/**`, `validation/**`, `actions/**`, `app/api/**`; no field-level rendering (pm06–08); no authz-matrix entry (pm09). |
| pm06 | `specs/pm06-spec.md` | `OfferingDetail` (new) — populated Details section. `offering-detail-region.tsx` + page threading (edit). No backend, `admin-nav.tsx`, or `offering-table.tsx` change; no specs/prices rendering; no authz-matrix entry. |
| pm07 | `specs/pm07-spec.md` | `CharacteristicChip` + `SpecificationsPanel` (new) — populated Specifications section. `offering-detail-region.tsx` edit (Specifications seam only). No backend or prior-unit UI change; no prices rendering; no authz-matrix entry. |
| pm08 | `specs/pm08-spec.md` | `formatCurrency` (edit) + `PriceTypeBadge` + `TierTable` + `PricesPanel` (new) — populated Prices section. `offering-detail-region.tsx` edit (Prices seam only). No backend or prior-unit UI change; no authz-matrix entry. |
| pm09 | `specs/pm09-spec.md` | `guard.integration.test.ts` edit (authz matrix) + `product-module-boundaries.test.ts` (new) — the module's ship gate: authz-matrix entry + 5-assertion negative-space sweep. Tests + CI only, no product code change; no doc edit needed. |
| pm10 | `specs/pm10-spec.md` | `db/schema/product.ts` edit — nullable, self-referencing `family_offering_id` on `product_offering` + index + self-reference CHECK. Migration `0010_product_offering_family.sql` (schema-diff generated). Schema-only, first Phase 2 unit. No repository/service/type/UI code. |
| pm11 | `specs/pm11-spec.md` | Backend create path: `create-offering.schema.ts` (no `isBundle` field, ever), `insertOffering` repository method (hardcodes `isBundle: false`/`familyOfferingId: null`/`version: 1`/`DRAFT`), `createOffering` service (insert + `PRODUCT_OFFERING_CREATED` audit row, one transaction, no uniqueness pre-check). No Server Action, UI, or page — those are pm19. |
| pm12 | `specs/pm12-spec.md` | Backend data-access primitive: `branchOfferingAsDraft(tx, sourceOfferingId, overrides?)` on `product-offering.ts` — clones an offering plus all its spec/price rows into a new `DRAFT` row, one-hop family resolution, `MAX(version)+1` numbering, `isBundle` copied unconditionally (no `overrides` key). No audit, service, action, or UI — consumed by pm13–pm16. |
| pm13 | `specs/pm13-spec.md` | Backend update path: `update-offering.schema.ts` (`name`/`isSellable`/`billingOnly`/`saveAsNew`, no `offeringId`/`isBundle`), `updateOfferingDraftInPlace` repository method (`DRAFT`-guarded in-place `UPDATE`), `updateOffering` service — four-way routing table (`ACTIVE`→branch, `DRAFT`+`saveAsNew`→branch, plain `DRAFT`→in-place with no-op guard, `RETIRED`→rejected), `PRODUCT_OFFERING_UPDATED`/`_BRANCHED` audit events. No Server Action, UI, or page — those are pm20. |
| pm14 | `specs/pm14-spec.md` | Backend specification CRUD: `create-specification.schema.ts`/`update-specification.schema.ts` (field-identical, no id keys), `product-specification.ts` gains `insertSpecification`/`updateSpecification`/`deleteSpecification` (no status backstop — table has no `lifecycle_status` column), `addSpecification`/`updateSpecification`/`deleteSpecification` services — uniform branch-first-when-`ACTIVE` routing, content-matching `findClonedCounterpart` helper to locate the branched clone's counterpart row, `PRODUCT_SPECIFICATION_CREATED`/`_UPDATED`/`_DELETED` audit events. No Server Action, UI, or page — those are pm21. |
