# Product Management — Progress Tracker

## Status

Phase 1 (all 9 units) implemented and committed. Module is ship-gate-verified (pm09) at the DB/test level; CI SAST/DAST baseline is a pipeline step outside this tooling.

Phase 2 (CRUD fast-follow) implemented and ship-gate-verified: pm10–pm17 implemented and locally verified via local tests and live-DB checks. pm18 implemented, verified via local tests and build only (no live-DB fixture walkthrough — see pm18 note). pm19 implemented, verified via local tests and build only (no live-DB fixture walkthrough — see pm19 note). pm20 and pm21 implemented, verified via local tests and build only (no live-DB fixture walkthrough — see pm20/pm21 notes). pm22 implemented, verified via local tests and build only (no live-DB fixture walkthrough — see pm22 note). pm23 implemented, verified via local tests and build only (no live-DB fixture walkthrough — see pm23 note); this completed all five UI units (pm19–pm23). **pm24 (ship gate) now complete — see note below.** CI SAST/DAST and the manual dev-server walkthrough remain outside this tooling (same caveat as pm09).

**Commit references are no longer tracked in this file** (per-unit notes below stand on their own; use `git log` for commit history).

| Unit | Name                                                                 |
| ---- | --------------------------------------------------------------------- |
| pm01 | Route-group rename `(admin)` → `(app)` + rename-invariance CI proof   |
| pm02 | DB foundation + validation schemas + seeds (`product` schema)         |
| pm03 | Repositories + `services/product`                                     |
| pm04 | Nav refactor (`NAV_ITEMS` → `NAV_SECTIONS`)                            |
| pm05 | Page — one section per unit (table → detail → specs → prices)         |
| pm06 | Offering detail section (populated `OfferingDetail` fields)           |
| pm07 | Specifications panel (populated `SpecificationsPanel`)                |
| pm08 | Prices panel (populated `PricesPanel`, `formatCurrency`)              |
| pm09 | Authz-matrix entry + guardrail sweep (ship gate)                      |
| pm10 | Schema: `family_offering_id` version-lineage column (Phase 2, unit 1) |
| pm11 | Backend: Create offering (`insertOffering`, `createOffering`) (Phase 2, unit 2) |
| pm12 | Backend: Branch-as-draft primitive (`branchOfferingAsDraft`) (Phase 2, unit 3) |
| pm13 | Backend: Update offering (`updateOfferingDraftInPlace`, `updateOffering`) (Phase 2, unit 4) |
| pm14 | Backend: Specification management (Phase 2, unit 5)                   |
| pm15 | Backend: Price management (Phase 2, unit 6)                           |
| pm16 | Backend: Activation & Retirement/Discard (Phase 2, unit 7)            |
| pm17 | Nav: Relabel + "Manage Products" entry (Phase 2, unit 8)               |
| pm18 | Manage Products page shell (Phase 2, unit 9)                           |
| pm19 | UI: Create Offering (Phase 2, unit 10)                                 |
| pm20 | UI: Edit Offering (Phase 2, unit 11)                                    |
| pm21 | UI: Specification Management (Phase 2, unit 12)                        |
| pm22 | UI: Price management (Phase 2, unit 13)                                 |
| pm23 | UI: Lifecycle Actions — Activate/Retire/Discard (Phase 2, unit 14) |
| pm24 | Ship gate: Guardrails, authz matrix, full sweep (Phase 2, unit 24 of 24) |

**Renumbering note:** `pm02-spec.md` bundles DB foundation + `validation/product/` + seeds into one unit, superseding an earlier 3-way split. Numbers above match `pm00-build-plan.md`'s 9-unit count.

## Recurring patterns (apply across units — not repeated per-unit below)

- **Permission-name ripple:** adding `"products"` to `PERMISSION_NAMES` forced `products: null`/`"DELETE"` into every hardcoded `EffectivePermissionMap` object-literal fixture across ~25+ test files (`tsc`-caught for object literals, but a few hardcoded array-length assertions in `roles-read.service.test.ts` were not). See `[[permission-name-addition-ripple]]`.
- **Cross-schema integration-test ripple:** every pre-existing `tests/**/*.integration.test.ts` doing a fresh `DROP SCHEMA core CASCADE` + migrate cycle needed `DROP SCHEMA IF EXISTS "product" CASCADE` added first (26 files) — otherwise the next file's migration fails with "schema product already exists." See `[[new-pgschema-integration-test-ripple]]`.
- **`db → validation` ESLint boundary:** both `db/schema/product.ts` (type-only JSONB import) and `types/product.ts` (`SpecificationCard`/`PriceCard` embedding Zod-owned shapes) needed `"validation"` added to their allowed-import targets in `eslint.config.mjs` — a forced companion change in pm02 and pm03, not scope creep.
- **Region-seam pattern:** pm05 built `OfferingDetailRegion` with three placeholder frames (Details/Specifications/Prices) carrying `{/* pmXX */}` seam comments; pm06/pm07/pm08 each filled exactly one seam, leaving the other two frames byte-unchanged, and each left the `<h2>` section title owned by the region (not the child component).
- **Audit event-type ripple (every Phase 2 write unit):** each new `AUDIT_EVENT_TYPES` entry needs (1) an `AUDIT_EVENT_CATEGORY_MAP` entry in `types/audit-log.ts` (`tsc`-caught) and (2) a bumped event-type count + optgroup assertion in `tests/components/audit-log-filters.test.tsx` (**not** `tsc`-caught). See `[[audit-event-type-addition-ripple]]`.
- **Repository mutation allow-lists:** `tests/db/product-repository-exports.test.ts` and `tests/guardrails/product-module-boundaries.test.ts`'s `PRODUCT_WRITE_SERVICE_FILES` both need a new entry per write unit — expected, flagged once by pm11, not a surprise since.
- **Verification baseline per unit:** `tsc --noEmit` + `eslint .` + `prettier --check` clean, plus `npx vitest run` (unit config) and a live-DB backend-correctness pass via a disposable `tsx` script (real dev DB, distinctive name/id prefix, cleaned up and re-confirmed zero rows afterward — never checked in). CI SAST/DAST and a live dev-server browser walkthrough remain unverified (no CI/browser in-session) unless noted per-unit.
- **New-route ripple (any unit adding a page.tsx):** `tests/app/route-manifest.test.ts`'s `ROUTE_MANIFEST` does a strict set-equality check against every real `app/**/page.tsx` — a new page 404-fails that test until the literal URL is added to the array (pm18 first hit this since pm01–pm17 added no new route).

## Per-unit notes

**pm01** — Renamed `app/(admin)/**` → `app/(app)/**` (14 files, `git mv`), updated 6 test imports + 2 path comments, added `tests/app/route-manifest.test.ts` as the rename-invariance guardrail.

**pm02** — `db/schema/product.ts` (`product` schema: `lifecycle_status` enum, 3 sequences, 3 tables, full constraints), migration `0006_product.sql` + `products` permission row, permission registry wiring, `types/product.ts`, `validation/product/`, `db/seeds/product.ts`. Follow-up fix (`232d78d`, migration `0007`): `product_offering.last_edited_by` FK → `ON DELETE SET NULL`; `CHECK (amount >= 0)` added to `product_offering_price`.

**pm03** — Read backend: three finder-only repositories (price repo permanently exports no mutation, Inv. #1), `listOfferings` + `getOfferingDetail` (injectable-clock `resolveEffectivityStatus`, `LEAD() OVER` window for derived `endDateTime`). Migration `0008` (not `0007` — pm02's follow-up consumed it).

**pm04** — `components/admin-nav.tsx`: flat `NAV_ITEMS` → sectioned `NAV_SECTIONS`, new "Products" section above "Administration." Manually verified live: render order, active-state, collapsed-mode divider, clean 404 pre-route.

**pm05** — `app/(app)/products/product-offering/page.tsx`, `LifecycleBadge`, `OfferingTable`, `OfferingDetailRegion` (three-seam shell). Added route to the frozen manifest.

**pm06** — `OfferingDetail` fills the Details seam. User-verified live ("looks good").

**pm07** — `CharacteristicChip` + `SpecificationsPanel` fill the Specifications seam.

**pm08** — `formatCurrency`, `PriceTypeBadge`, `TierTable`, `PricesPanel` fill the Prices seam. Component work sequenced ahead of pm07's commit (explicit user direction); diff later split into two clean commits before push.

**pm09 (ship gate)** — Tests + CI only. Confirmed all 6 inherited guardrails green; flagged "price immutability bumps version" as untestable in v1 (no mutation path yet — correct for that phase). Added authz-matrix entries + `tests/guardrails/product-module-boundaries.test.ts`. Real deviation: `products` permission is migration-seeded DML, so the spec's "insert in bulk seed array" text would violate the unique constraint — fixed by `SELECT`-ing the seeded row instead.

**Post-ship polish (2026-07-09, `e2bb187`)** — UI density pass; deleted `CharacteristicChip`/`TierTable` per explicit instruction, inlined as plain text instead.

**Post-ship deployment bug (2026-07-09, `d5f7148`)** — deployed instance 500'd (`42501: permission denied for schema product`) because `bootstrap-db-roles.sql` only ever granted privileges on `core`. Fixed by appending a `product` grant/revoke block (incl. sequence grants). **Any future schema must add its own grant block in the same change that adds its migration, or this recurs.**

---

## Phase 2 (CRUD Fast-Follow)

**pm10 — Schema: `family_offering_id`.** Added nullable, self-referencing `familyOfferingId` (`ON DELETE RESTRICT`) + index + self-reference CHECK to `productOffering`. Migration `0010` (schema-diff generated). Two notes: self-referencing FK needed an explicit `(): AnyPgColumn =>` return-type annotation (Drizzle inference gap, compile-time only); FK constraint name truncated by Postgres's 63-char limit to `product_offering_family_offering_id_product_offering_product_of`. One in-scope test fix: `product-schema.test.ts`'s exact-column-set assertion needed the new column added.

**pm11 — Backend: Create offering.** `create-offering.schema.ts` (no `isBundle` field, ever), `insertOffering` (hardcodes `isBundle: false`/`familyOfferingId: null`/`version: 1`/`DRAFT`), `createOffering` service (insert + `PRODUCT_OFFERING_CREATED` audit, one transaction, no uniqueness pre-check). Forced companion fixes (first occurrence of the now-recurring pattern): `AUDIT_EVENT_CATEGORY_MAP` entry, `audit-log-filters.test.tsx` count bump, and both `product-repository-exports.test.ts`/`product-module-boundaries.test.ts` relaxed from blanket read-only prohibitions to named allow-lists (Phase 2 architecture explicitly permits offering-repo writes; price repo stays permanently finder-only sans `insert*`). Live-DB verified: FK-enforced atomicity (bad actorId rolls back the offering insert too), real insert produces correct defaults + exactly one audit row.

**pm12 — Backend: Branch-as-draft primitive.** `branchOfferingAsDraft(tx, sourceOfferingId, overrides?)` clones an offering + all spec/price rows into a new `DRAFT`; `isBundle` always copied from source, never `overrides` (grep-confirmed no read path, even via `as any`). One-hop family resolution inlined, not extracted (explicit "not yet" call). No audit write — caller composes it. Live-DB verified via a script that deliberately threw to force rollback (byte-diffed before/after): version/family resolution (root and branch-of-branch), status always `DRAFT` regardless of source, override `??` semantics, full child-row cloning, zero-children case, source untouched.

**pm13 — Backend: Update offering.** `update-offering.schema.ts` (`name`/`isSellable`/`billingOnly`/`saveAsNew`), `updateOfferingDraftInPlace` (DRAFT-guarded in-place update, never touches `version`/`familyOfferingId`/`isBundle`), `updateOffering` service (four-way routing: in-place DRAFT save w/ no-op short-circuit, `branchAndAudit` shared by ACTIVE-target and `saveAsNew`, `OFFERING_RETIRED`/`NOT_FOUND` guards). Two new audit types (`_UPDATED` Change, `_BRANCHED` Additive) + the two repository/guardrail allow-list bumps. Live-DB verified via real commits against `PM13VERIFY-`-prefixed rows, fully cleaned up after: in-place edit, no-op guard, `saveAsNew` still branches even on identical values, ACTIVE-target branch with correct family/version, not-found/retired guards.

**pm14 — Backend: Specification management.** `create-specification.schema.ts`/`update-specification.schema.ts` (field-identical), `product-specification.ts` gains `insertSpecification`/`updateSpecification`/`deleteSpecification`, three services with uniform branch-first-when-ACTIVE routing + a `findClonedCounterpart` content-matching helper (duplicated once between update/delete, per spec's explicit judgment call). Three new audit types (`_CREATED`/`_UPDATED`/`_DELETED`). One undiscovered-until-test-run gap: `product-repository-exports.test.ts`'s spec-repository assertion needed the same allow-list treatment pm11 gave the offering repository (not named in pm14-spec's own diff list). Live-DB verified against `PM14VERIFY-`-prefixed rows plus the real seeded ACTIVE offering (to test `findClonedCounterpart` picking the right row among multiple candidates); one real snag: price/spec rows are `ON DELETE RESTRICT` against offering, so cleanup had to delete children before parents. **Post-ship review fix (2026-07-22):** all three services read `offering.lifecycleStatus` (and, for update/delete, the target spec's current content) via `db` *before* opening the transaction, then used that stale snapshot to make the branch-first decision inside the transaction — a TOCTOU window. Fixed by moving both reads onto `tx`, immediately before the branch decision, inside the same transaction as the write. Also added a `refProductOfferingId` backstop condition to `updateSpecification`/`deleteSpecification`'s `WHERE` clause (defense-in-depth, same shape as `updateOfferingDraftInPlace`'s status backstop) — callers now pass the target offering id alongside the spec id. `product-repository-exports.test.ts` extended: `MUTATION_NAME_PATTERN` now recognizes a `branch` prefix and `branchOfferingAsDraft` (pm12, previously unguarded by this test) is in `ALLOWED_OFFERING_MUTATIONS`.

**pm15 — Backend: Price management.** `insert-price.schema.ts` + `product-offering-price.ts` gains `insertPrice` (repo's only-ever write, Inv. #1), `insertPrice` service (branch-first-when-ACTIVE, 3-day backdating tolerance checked twice — schema fast-fail + injectable-`now` service check, needed separate time sources since schema's `superRefine` uses real `Date.now()`). One new audit type (`PRODUCT_PRICE_ADDED`, Additive). Live-DB verified: direct/branch-first paths, backdating boundary, not-found/retired guards. **Post-ship review fix (2026-07-22):** `insertPrice` read `offering.lifecycleStatus` via `db` before the transaction opened, then used that stale snapshot for the branch-first decision inside the transaction — the same TOCTOU pattern fixed in pm14's services. Fixed by moving the `findDetailById` read onto `tx`, immediately before the branch decision.

**pm16 — Backend: Activation & Retirement/Discard.** `activate-offering.schema.ts`/`retire-offering.schema.ts` (both single-field `reason`), `product-offering.ts` gains `findActiveInFamily` (`.for("update")` row-locks the whole family by immutable identity, first use of locking in this codebase), `activateOffering` (transactional Inv. 13 re-check + retire-sibling + flip-to-ACTIVE), `retireOffering` (unconditional flip to `RETIRED`, one method backs both retire and discard per code-standards §1 rule 11). Four new audit types (`_ACTIVATED`/`_SUPERSEDED`/`_RETIRED`/`_DISCARDED`). Live-DB verified via disposable script: all precondition/guard rejections, automatic supersession, **Inv. 13 under real concurrency run 4× — exactly one family member ended up `ACTIVE` every time**, retire/discard confirmed to share the identical repository method. **Post-ship review fix (2026-07-22):** two related gaps closed. (1) `activateOffering`'s `SPECIFICATIONS_NOT_RESOLVED` precondition was only checked once, pre-transaction — a mandatory spec's `defaultValue` could be cleared (or the spec deleted, pm14) between that read and the write. Added `productSpecificationRepository.findByOfferingIdForUpdate` (`FOR UPDATE` variant of `findByOfferingId`) and re-check the precondition, locked, inside the transaction, immediately before calling `activateOffering`. `NO_PRICE_ROWS` was left as a pre-transaction-only check (prices are insert-only, Inv. #1 — that result can't regress). (2) `retireOffering` derived its `eventType` (`_RETIRED` vs `_DISCARDED`) from the same pre-transaction status read, which could go stale the same way (e.g. a concurrent activation) — `retireOffering`'s repository method has no status WHERE-backstop of its own. Added `productOfferingRepository.findLifecycleStatusForUpdate` (single-row, `FOR UPDATE`, no join — unlike `findDetailById`) and re-derive `eventType` from a locked re-read inside the transaction; an already-RETIRED result now aborts with `OFFERING_RETIRED` instead of writing a stale-eventType audit row. Separately, `db/repositories/product-offering.ts`'s `resolveNextVersion` (pm12) had no protection against two concurrent `branchOfferingAsDraft` calls on the same family both computing the same `MAX(version)` and allocating duplicate version numbers — row locking doesn't help here (the contended "row" is the next insert, which doesn't exist yet in either transaction's snapshot), so fixed with a `pg_advisory_xact_lock(hashtext(rootId))` acquired at the top of `resolveNextVersion`, serializing concurrent branches of the same family; auto-released at transaction end. Typecheck/lint/format clean and full mocked suite green (155 files / 1414 tests) after this round. Live-DB re-verified once the Tailscale dev DB became reachable again (2026-07-22, `PMRACEVERIFY-`-prefixed rows, cleaned up and re-confirmed zero rows afterward): two concurrent `branchOfferingAsDraft` calls on the same family produced three distinct, sequential versions (advisory lock holds); `activateOffering` succeeds with the locked spec re-check in place; `retireOffering` derives `RETIRED`/`DISCARDED` correctly from the locked re-read for ACTIVE/DRAFT sources respectively and rejects a second retire on an already-RETIRED offering; `insertPrice`'s tx-based re-fetch works on both the direct-DRAFT and branch-first-ACTIVE paths. (Dev DB also needed a `db:migrate` catch-up first — it was missing the pm10 `family_offering_id` migration.)

**pm17 — Nav: Relabel + "Manage Products" entry.** `components/admin-nav.tsx`'s "Products" section relabeled to "View Product" + new sibling "Manage Products" item, deliberately with no `requiredPermission` (nav-renders-regardless-of-permission convention, pm17-spec §2.2). Data-only diff, zero render-loop/type changes. §2.6 folded in: `product-offering/page.tsx`'s `metadata.title`/`H1` → "View Product," text only. Tests extended per §3.5, all pre-existing Customer-section assertions pass unmodified. `/products/manage-products` 404s until `pm18`. Full suite green (155 files / 1414 tests).

**pm18 — Manage Products page shell.** Confirmed pm17 already resolved §3.8's "View Product" heading rename (folded into pm17, not deferred) — this unit touches nothing under `product-offering/`. The one flagged read-model exception (§2.2): `familyOfferingId` added to `OfferingListRow` (`types/product.ts`) and to `findList`'s `SELECT` (`db/repositories/product-offering.ts`) — one column, no new repository method, no new service export. New: `app/(app)/products/manage-products/{page,loading,error}.tsx`, `components/products/manage/manage-offering-table.tsx`. Page-local private helpers (`fetchAllForStatus`/`fetchAllOfferingRows`/`resolveFamilyId`/`selectPrimary`/`groupIntoFamilies`) page-loop the existing `listOfferings` twice (`status: null`, `status: "RETIRED"`) and group by resolved family root, matching `firstValue`'s page-local-helper precedent. Every row action (Edit/Add price/Activate/Discard/Retire) and the "New offering" CTA render fully wired-looking but inert, each with a `pm19`–`pm23` seam comment, per the pm05→pm08 seam discipline. One deviation from the spec's literal code, flagged: ui-context-phase2's `--text-secondary`/`--text-danger` row-action color tokens don't exist in `app/globals.css` (grepped repo-wide — zero other references) and adding them was out of this unit's file scope, so quiet actions use the existing `text-muted-foreground` token and danger actions (Discard/Retire) use the existing `text-destructive` token instead — same semantic roles, no new CSS custom properties introduced. Ripple (additive, expected per pm18-spec §5): `tests/components/offering-table.test.tsx` and `tests/services/list-offerings.service.test.ts` fixtures needed `familyOfferingId: null` added to their `OfferingListRow` literals; `tests/app/route-manifest.test.ts`'s frozen `ROUTE_MANIFEST` needed `/products/manage-products` added (strict set-equality against real `page.tsx` files). Full suite green (157 files / 1426 tests), `tsc`/`eslint`/`prettier`/`next build` all clean. No live-DB fixture walkthrough performed (needs pm13/pm16 update/activate services or a throwaway multi-version fixture script to exercise the expand affordance and RETIRED-primary case per §5's fixture-data note).

**pm19 — UI: Create Offering.** `actions/product/create-offering.action.ts` (new, first file in `actions/product/`; guard → `safeParse` → delegate to pm11's `createOffering` → `revalidatePath` both product pages, no `NAME_CONFLICT`-style branch since pm11's service has none), `offering-form.tsx` (new, create-mode-only `OfferingForm`, no `isBundle` control, `Controller`+`Checkbox` pattern reused from `user-form.tsx`), `create-offering-dialog.tsx` (new, mirrors `create-role-dialog.tsx`; success path is `router.refresh()` not `router.push()` since Manage Products has no selected-row URL-state concept). Filled pm18's inert "New offering" CTA seam in `manage-offering-table.tsx` (one import + one JSX wrap, nothing else touched). Guardrail assertion updated in this same commit, not deferred to pm24 (Design §2.5): `product-module-boundaries.test.ts`'s "has no `actions/product/` folder" assertion replaced with an `EXPECTED_PRODUCT_ACTION_FILES` allow-list check — pm20–pm23 must each append their own action file to that array rather than assuming the rewrite is still pm24's job alone. One undocumented-in-spec ripple: `tests/setup.ts` needed a `ResizeObserver` polyfill added — jsdom doesn't implement it, and pm19 is the first suite to render a `Checkbox` with a non-empty parent (Radix's size-measuring indicator needs it to mount). Verified: `tsc`/`eslint`/`prettier`/`vitest run`/`next build` all clean on the pm19-specific files; the one pre-existing lint error (`organization-form.tsx`) and one pre-existing flaky test timeout (`customer-role-form.test.tsx`) are both unrelated Customer-module issues, unchanged by this unit. No live-DB fixture walkthrough performed (same gap as pm18 — needs a throwaway script or live browser session to confirm the real `PRODUCT_OFFERING_CREATED` audit row and table refresh end-to-end).

**pm20 — UI: Edit Offering.** `actions/product/update-offering.action.ts` (new) — guard → `safeParse` → delegate to pm13's `updateOffering` → `revalidatePath` both product pages. `offering-form.tsx` gains `EditOfferingForm`, composed inline in `manage-offering-table.tsx` (Design §2.1 — no separate `edit-offering-dialog.tsx` file); two independent `handleSubmit` calls, one per save outcome, instead of one native submit disambiguated after the fact. Footer buttons depend on `currentStatus`: DRAFT gets Cancel/"Save as new draft"/"Save"; ACTIVE gets Cancel/"Create new draft" only, behind the `--bg-warning` banner. Flagged read-model exception (same shape as pm18 §2.2): `billingOnly` added to `OfferingListRow`/`findList`'s `SELECT` to prefill the dialog. `editOfferingFieldsSchema` (`.omit`-derived) appended to pm13's `update-offering.schema.ts`. Fills both pm18 Edit seams; a branched save auto-expands its family. Guardrail `EXPECTED_PRODUCT_ACTION_FILES` appended with `update-offering.action.ts`. Ripple: `billingOnly: false` added to `OfferingListRow` fixtures across three test files; `manage-offering-table.test.tsx`'s no-op-loop test now excludes Edit; new `describe("Edit offering (pm20-spec §3.8)")` block. Full suite green (160 files / 1455 tests), `tsc`/`eslint`/`prettier`/`next build` all clean (one pre-existing unrelated `organization-form.tsx` lint error). No live-DB fixture walkthrough performed. **Post-ship review fix (2026-07-23):** `services/product/update-offering.ts`'s `updateOffering` read `offering.lifecycleStatus` via `db` *before* opening the transaction, then used that stale snapshot to route between the ACTIVE-branch/DRAFT-in-place/RETIRED-reject paths inside the transaction — the same TOCTOU pattern already fixed in pm14/pm15/pm16's services. Fixed by moving the read onto `tx` via `findDetailByIdForUpdate` (row-locked), wrapping the entire routing decision and both the branch and in-place-update paths in one `db.transaction`, immediately before any write. `manage-offering-table.test.tsx` gained a test confirming an expanded sibling row's own Edit button populates the dialog with the sibling's values (not the primary's) and submits the sibling's id; `create-offering.action.test.ts`/`update-offering.action.test.ts` each gained a test covering `requirePermission` rejecting with a non-redirect error (`SERVER_ERROR`, service never called). Full suite green (168 files / 1528 tests), `tsc`/`eslint`/`prettier` all clean.

**pm21 — UI: Specification Management.** `actions/product/{create,update,delete}-specification.action.ts` (new) — guard → `safeParse` (create/update only; delete has no schema, matching `update-offering.action.ts`'s plain-parameter precedent) → delegate to pm14's three services → `revalidatePath` both product pages. `specification-form.tsx` (new, `SpecificationForm`) — the first form editing a `Record<string, string>` field: `useFieldArray` needs an array, so a UI-only `characteristicsList: {key,value}[]` shape is translated to/from the wire `Record` at the form's own boundary (`recordToList`/`listToRecord`). `specifications-dialog.tsx` (new, `SpecificationsDialog`) — one `Dialog` swapping between a list view and a form view (not separate dialog files), plus a nested `AlertDialog` (reusing `delete-role-dialog.tsx`'s shape) for hard-delete, UI-gated to `DRAFT` targets only even though pm14's service also supports an `ACTIVE`-target branch-first delete (dead code from this unit's UI, deliberately). **Two design calls flagged for pm22/pm23:** (1) a **direct** success keeps the dialog **open** + refreshes rather than closing — a deliberate departure from pm19/pm20's "always close," fitted to this dialog's "add several specs in one sitting" shape; a **branch-producing** success still closes/toasts/auto-expands exactly like pm20. (2) The read-model gap is closed via a new page-local `fetchSpecificationsByOfferingId` helper (reuses `getOfferingDetail` unmodified, one call per displayed offering) passed as a sibling `specificationsByOfferingId` prop — not by widening `OfferingListRow` or adding a read Server Action. Disclosed cost: `getOfferingDetail` also computes prices, discarded here, doubling per-row query cost on page load — accepted for now, same trade pm18 made. Sixth row-action button (`ListChecks`, `--text-secondary`, `DRAFT`/`ACTIVE` only) added to the table matrix and to both `prodmgmt-code-standards-phase2.md` §4/§7 and `prodmgmt-ui-context-phase2.md` in this same commit. Guardrail `EXPECTED_PRODUCT_ACTION_FILES` appended with all three new action files. Ripple: `manage-products-page.test.tsx` needed a `getOfferingDetail` mock (the page now calls it); `manage-offering-table.test.tsx`'s no-op-loop test now also excludes Specifications, plus a new `describe("Specifications (pm21-spec §3.11)")` block. Full suite green (165 files / 1495 tests), `tsc`/`eslint`/`prettier`/`next build` all clean. No live-DB fixture walkthrough performed.

**pm22 — UI: Price management.** `actions/product/insert-price.action.ts` (new) — guard → `safeParse` (pm15's own `insertPriceSchema`, unchanged) → delegate to pm15's `insertPrice` → `revalidatePath` both product pages. `components/products/manage/price-form.tsx` (new, `PriceForm`) — six fields exactly (Price name/Price type/Currency/GL code/Pricing model/Start date, Amount or a new tier-row editor depending on pricing model), its own local `priceFormSchema` validating only flat-shape-meaningful checks (Design §2.4 — tier contiguity/XOR stay defined once, in `tieredPricingCharacteristicsSchema`, reused not re-declared). `components/products/manage/add-price-dialog.tsx` (new, `AddPriceDialog`) — unlike Edit, this **does** get its own dialog file (Design §2.1: Add Price has exactly one submit outcome, matching `CreateOfferingDialog`'s shape) and its own per-row instance (Design §2.2: no new shared table state, `ManageOfferingTable` needed no `addPriceRow` state at all). Fills every pm18 Add-price seam (primary and expanded-sibling rows alike) via one `AddPriceDialog` wrap per row, each row's own `onBranchPrice` closure calling the existing `setExpandedFamilies` adder — same auto-expand pattern pm20/pm21 established. Guardrail `EXPECTED_PRODUCT_ACTION_FILES` appended with `insert-price.action.ts`. Two implementation choices flagged, both spec-sanctioned "either is fine" calls: (1) `pricingModel`'s `RadioGroup` is wired via a `Controller`, not the manual `register().onChange` shim pm22-spec §3.3's own literal code sample showed — the spec explicitly calls this an acceptable substitution, first use of `RadioGroup` in the codebase either way; (2) the backdating-warning computation captures "now" once via a `useState(() => Date.now())` lazy initializer rather than calling `Date.now()` directly during render — required to satisfy this codebase's `react-hooks/purity` ESLint rule (React Compiler), which flags impure calls in the render body; a mount-time snapshot is indistinguishable from a live clock for a short-lived dialog. Tests use `vi.useFakeTimers({ toFake: ["Date"] })` pinned to local midnight to make the 3-day backdating boundary deterministic regardless of wall-clock time-of-day, avoiding an off-by-hours flake in the literal `Date.now() - startOfDayMidnight` comparison. Full suite green (168 files / 1525 tests), `tsc`/`eslint`/`prettier`/`next build` all clean. No live-DB fixture walkthrough performed (same gap as pm18–pm21). Rebased onto `54feb72` (an out-of-band TOCTOU/version-race fix to pm14–16 backend code, pushed to `origin/dev1` independently of this unit's own work) before pushing; re-ran the full verification baseline post-rebase, unchanged results — `insertPrice`'s exported signature and `InsertPriceResult` shape were untouched by that fix, so nothing in this unit needed rework.

**pm24 (Phase 2 ship gate) — Guardrails, authz matrix, full sweep.** Tests + CI only, same discipline as pm09. Pre-flight audit (spec §3.1) found a real gap before any pm24 code was written: the ledger's guardrails 8 (single-active-per-family), 9 (branch-not-mutate), and 14 (price immutability, behavioral) were marked "INHERIT — audit present & green," but no committed test anywhere in the repo exercised `branchOfferingAsDraft`/`activateOffering`/`retireOffering`/`addSpecification`/`updateSpecification`/`deleteSpecification`/`insertPrice` — pm12/13/14/15/16 had each only verified this behavior via disposable, uncommitted scripts (per their own notes above). Per the spec's own explicit instruction this is a defect in the owning units, not something pm24 should paper over with a workaround — flagged to the user, who chose to have the missing coverage added now rather than stop-and-report or ship with a known gap. Added 8 new tests to `tests/db/product-repositories.integration.test.ts` (not originally in pm24's own file list, but the correct home — same file, same describe-block style as the existing pm12–pm16 coverage): two for guardrail 8 (sequential sibling supersession; two near-simultaneous activations on siblings leave exactly one ACTIVE, proving the `FOR UPDATE` family lock actually serializes), five for guardrail 9 (offering-fields/add-spec/update-spec/delete-spec/insert-price branch-not-mutate, each asserting the ACTIVE source row and its children are byte-identical after the write and exactly one new sibling DRAFT exists), one for guardrail 14 (successor price insert on a DRAFT leaves the prior price row byte-identical and offering `version` untouched). Then pm24's own net-new work in `tests/guardrails/product-module-boundaries.test.ts`: rewrote assertion 1 to add the per-file `export async function <Name>(` grep pm19's incremental version hadn't added; added guardrail 11 (View-stays-read-only), guardrail 13 (schema-diff), the guardrail-12 route-manifest re-check (pm18 had already added the route itself); `PRODUCT_WRITE_SERVICE_FILES` already had all eight entries (pm14/15/16 self-extended it, per the recurring-pattern note above — verify only, no edit needed); added the guardrail-10 structural backstop. One deviation from the spec's own literal code, flagged: §3.7's suggested regex (`/\.deleteSpecification\(|(?<!\.)\bdeleteSpecification\(/`) false-positived on `actions/product/delete-specification.action.ts`, which legitimately calls the *service* function of the same name (not the repository method) — the bare-name alternation couldn't distinguish them. Fixed by scoping the check to the dot-qualified `productSpecificationRepository.deleteSpecification(` pattern only, since the repository is never destructured anywhere in this codebase. `tests/auth/guard.integration.test.ts` extended per §2.7/§3.8: new `productsManagerUserId` principal (products:EDIT only, no DELETE) mirroring the existing `customerManagerUserId` pattern; dynamic imports for all eight `actions/product/*.action` modules; two `it.each` loops (seven EDIT-gated actions don't reject the EDIT-only caller on permission grounds; all eight reject `noGrantsUserId`) plus one dedicated test proving `retireOfferingAction` rejects the EDIT-only caller — the concrete proof the EDIT/DELETE split is real. `tests/app/route-manifest.test.ts` needed no edit (pm18 had already added `/products/manage-products`). Verified against three separate ephemeral, disposable Postgres containers (never the dev stack's own `DATABASE_URL` — see `[[local-docker-dev-stack]]`): full integration suite (37 files / 351 tests, up from 335 pre-pm24) green across all three runs; full unit suite (172 files / 1579 tests) green; `tsc --noEmit`, `next build` clean; `eslint` shows only the two pre-existing, unrelated Customer-module issues already noted in pm19/pm20/pm23's own notes. `git status` diff scope: exactly `tests/db/product-repositories.integration.test.ts`, `tests/guardrails/product-module-boundaries.test.ts`, `tests/auth/guard.integration.test.ts` — no `app/**`/`services/**`/`db/**`/`actions/**`/`components/**` change, no doc edit (permission-map rows already correct per §3.9's audit). With pm24 verified, Phase 2 (all 14 units, pm10–pm24) is code-complete and CI-gated; CI SAST/DAST and the manual dev-server walkthrough (§3.10) remain outside this tooling's reach, same caveat pm09 carried for Phase 1.

**pm23 — UI: Lifecycle Actions (Activate/Retire/Discard).** `actions/product/activate-offering.action.ts` (new) — guard `products:EDIT` → `safeParse` (pm16's own `activateOfferingSchema`, unchanged) → delegate to pm16's `activateOffering` → `revalidatePath` both product pages. `actions/product/retire-offering.action.ts` (new) — guard `products:DELETE` → `safeParse` (`retireOfferingSchema`) → delegate to pm16's `retireOffering`, one action handling both Retire and Discard (code-standards-phase2 §1 rule 11, extended one layer up). `components/products/manage/activate-offering-dialog.tsx` (new, `ActivateOfferingDialog`) — plain `Dialog`, accent-filled confirm button, the two named precondition failures (`NO_PRICE_ROWS`/`SPECIFICATIONS_NOT_RESOLVED`) render as a persistent inline `Alert` rather than a toast (the one departure from pm19/pm20/pm22's toast-only failure handling), `onSuperseded` prop fires only when `supersededOfferingId` is non-null. `components/products/manage/retire-offering-dialog.tsx` (new, `RetireOfferingDialog`) — one `AlertDialog` component, copy/title/confirm-label switching on a `currentStatus` prop ("DRAFT" → Discard copy, "ACTIVE" → Retire copy), success toast driven by the server's own `eventType`, not the prop. Both dialogs use a plain `useState`-backed `Textarea` for the optional Reason field, no `react-hook-form`/`zodResolver` (grep-confirmed absent) — the two schemas are single-field and don't earn that weight. Filled every remaining pm18 seam (Activate/Discard/Retire, primary and expanded-sibling rows alike) in `manage-offering-table.tsx`; the auto-expand-on-supersession wiring reuses the same `setExpandedFamilies` adder pm20/pm21/pm22 already established, passed through as both dialogs' respective row-action props. This is the last of the five UI units (pm19–pm23) — every button `pm18` originally rendered inert now has real behavior; `EXPECTED_PRODUCT_ACTION_FILES` appended with `activate-offering.action.ts`/`retire-offering.action.ts`, completing the full eight-file set `code-standards-phase2.md` §7 names. `tests/components/manage-offering-table.test.tsx`'s prior "still wired to nothing" seam test was rewritten (§3.8's own instruction) into a real-behavior assertion — no remaining "no attached behavior" case exists anywhere in that file. Full suite green (172 files / 1575 tests), `tsc`/`prettier`/`next build` all clean; `eslint` shows only the two pre-existing, unrelated Customer-module issues already noted in pm19/pm20's notes (`organization-form.tsx` `react-hooks/set-state-in-effect` error, `new-customer-form.tsx` `react-hooks/incompatible-library` warning). No live-DB fixture walkthrough performed (same gap as pm18–pm22 — needs a throwaway script or live browser session to confirm the real `PRODUCT_OFFERING_ACTIVATED`/`_SUPERSEDED`/`_RETIRED`/`_DISCARDED` audit rows and the Manage-Products/View-Product visibility split end-to-end).

## Per-unit specs

| Unit | Spec file            | Summary                                                                                                                                                                                                                                                                  |
| ---- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| pm01 | `specs/pm01-spec.md` | Rename `app/(admin)/**` → `app/(app)/**` (14 files, `git mv`); update 6 test imports + 2 path comments; add rename-invariance CI proof. No other change. |
| pm02 | `specs/pm02-spec.md` | `product` schema/migration/seeds in one unit: 3 tables + enum + 3 sequences + all constraints; permission registry wiring; `validation/product/`; `db/seeds/product.ts`; unit + integration guardrail tests. No `app/**`, `actions/product/`, or repository code. |
| pm03 | `specs/pm03-spec.md` | Read backend: 3 finder-only repositories (price repo permanently exports no mutation, Inv. #1), `services/product` (`listOfferings`, `getOfferingDetail`), data-only migration seeding page-size config, `types/product.ts` read models. No `app/**`, `actions/product/`, or UI. |
| pm04 | `specs/pm04-spec.md` | `components/admin-nav.tsx`: flat `NAV_ITEMS` → sectioned `NAV_SECTIONS`, new "Products" section; collapsed-rail divider between sections. No `app/**`, permission check, or dependency change. |
| pm05 | `specs/pm05-spec.md` | `app/(app)/products/product-offering/page.tsx` (guard + searchParams parse + fetch); `LifecycleBadge` + `OfferingTable` + `OfferingDetailRegion` (new). No `services/**`, `db/**`, `validation/**`, `actions/**`, `app/api/**`; no field-level rendering (pm06–08); no authz-matrix entry (pm09). |
| pm06 | `specs/pm06-spec.md` | `OfferingDetail` (new) — populated Details section. `offering-detail-region.tsx` + page threading (edit). No backend, `admin-nav.tsx`, or `offering-table.tsx` change; no specs/prices rendering; no authz-matrix entry. |
| pm07 | `specs/pm07-spec.md` | `CharacteristicChip` + `SpecificationsPanel` (new) — populated Specifications section. `offering-detail-region.tsx` edit (Specifications seam only). No backend or prior-unit UI change; no prices rendering; no authz-matrix entry. |
| pm08 | `specs/pm08-spec.md` | `formatCurrency` (edit) + `PriceTypeBadge` + `TierTable` + `PricesPanel` (new) — populated Prices section. `offering-detail-region.tsx` edit (Prices seam only). No backend or prior-unit UI change; no authz-matrix entry. |
| pm09 | `specs/pm09-spec.md` | `guard.integration.test.ts` edit (authz matrix) + `product-module-boundaries.test.ts` (new) — the module's ship gate: authz-matrix entry + 5-assertion negative-space sweep. Tests + CI only, no product code change; no doc edit needed. |
| pm10 | `specs/pm10-spec.md` | `db/schema/product.ts` edit — nullable, self-referencing `family_offering_id` on `product_offering` + index + self-reference CHECK. Migration `0010_product_offering_family.sql` (schema-diff generated). Schema-only, first Phase 2 unit. No repository/service/type/UI code. |
| pm11 | `specs/pm11-spec.md` | Backend create path: `create-offering.schema.ts` (no `isBundle` field, ever), `insertOffering` repository method (hardcodes `isBundle: false`/`familyOfferingId: null`/`version: 1`/`DRAFT`), `createOffering` service (insert + `PRODUCT_OFFERING_CREATED` audit row, one transaction, no uniqueness pre-check). No Server Action, UI, or page — those are pm19. |
| pm12 | `specs/pm12-spec.md` | Backend data-access primitive: `branchOfferingAsDraft(tx, sourceOfferingId, overrides?)` on `product-offering.ts` — clones an offering plus all its spec/price rows into a new `DRAFT` row, one-hop family resolution, `MAX(version)+1` numbering, `isBundle` copied unconditionally (no `overrides` key). No audit, service, action, or UI — consumed by pm13–pm16. |
| pm13 | `specs/pm13-spec.md` | Backend update path: `update-offering.schema.ts` (`name`/`isSellable`/`billingOnly`/`saveAsNew`, no `offeringId`/`isBundle`), `updateOfferingDraftInPlace` repository method (`DRAFT`-guarded in-place `UPDATE`), `updateOffering` service — four-way routing table (`ACTIVE`→branch, `DRAFT`+`saveAsNew`→branch, plain `DRAFT`→in-place with no-op guard, `RETIRED`→rejected), `PRODUCT_OFFERING_UPDATED`/`_BRANCHED` audit events. No Server Action, UI, or page — those are pm20. |
| pm14 | `specs/pm14-spec.md` | Backend specification CRUD: `create-specification.schema.ts`/`update-specification.schema.ts` (field-identical, no id keys), `product-specification.ts` gains `insertSpecification`/`updateSpecification`/`deleteSpecification` (no status backstop — table has no `lifecycle_status` column), `addSpecification`/`updateSpecification`/`deleteSpecification` services — uniform branch-first-when-`ACTIVE` routing, content-matching `findClonedCounterpart` helper to locate the branched clone's counterpart row, `PRODUCT_SPECIFICATION_CREATED`/`_UPDATED`/`_DELETED` audit events. No Server Action, UI, or page — those are pm21. |
| pm15 | `specs/pm15-spec.md` | Backend price management: `insert-price.schema.ts` (`name`/`priceType`/`currency`/`glCode`/`startDateTime`/`priceCharacteristics`, no id key, nests `priceCharacteristicsSchema` wholesale), `product-offering-price.ts` gains `insertPrice` — the repository's only write, permanently (Inv. #1 relaxes for `insert*` only, never update/delete), `insertPrice` service — two-row branch-first-when-`ACTIVE` routing (pure new content, no counterpart-locate step), 3-day backdating tolerance checked twice (fast-fail in the schema, authoritative against injectable `now` in the service), `PRODUCT_PRICE_ADDED` audit event. No Server Action, UI, or page — those are pm22. |
| pm16 | `specs/pm16-spec.md` | Backend lifecycle: `activate-offering.schema.ts`/`retire-offering.schema.ts` (both single-field `reason`), `product-offering.ts` gains `findActiveInFamily` (`.for("update")` row-locks the whole family by immutable identity, first use of locking in this codebase), `activateOffering` (transactional Inv. 13 re-check + retire-sibling + flip-to-ACTIVE), `retireOffering` (unconditional flip to `RETIRED`, one method backs both retire and discard). Services: `activateOffering` (price/mandatory-spec preconditions read ahead of the transaction), `retireOffering` (`PRODUCT_OFFERING_RETIRED` vs `_DISCARDED` chosen from pre-transaction status). Four audit events (`_ACTIVATED`/`_SUPERSEDED`/`_RETIRED`/`_DISCARDED`). No Server Action, UI, or page — those are pm23. |
| pm17 | `specs/pm17-spec.md` | Nav: `components/admin-nav.tsx`'s "Products" section relabeled ("Product Offering" → "View Product," same href/icon) + new sibling "Manage Products" item (`/products/manage-products`, `PackagePlus` icon, no `requiredPermission` — deliberately not `cm03`'s locked pattern). Data-only diff, zero render-loop/type changes. §2.6 page-heading rename folded in: `app/(app)/products/product-offering/page.tsx` `metadata.title`/`H1` → "View Product." No backend, Server Action, or new page — `/products/manage-products` 404s until `pm18`. |
| pm18 | `specs/pm18-spec.md` | Frontend: `app/(app)/products/manage-products/{page,loading,error}.tsx` (new) — guard `products:EDIT` → page-loops the existing `listOfferings` twice (`null`, `"RETIRED"`) via private helpers → groups rows into `OfferingFamilyRow`s (one row per family, `ACTIVE` else highest-version primary) → composes `ManageOfferingTable` (new, `components/products/manage/`) — expand/collapse version history, status-matrix row actions (Edit/Add price/Activate/Discard/Retire) and "New offering" CTA, all real UI, all inert seams for pm19–pm23. One flagged exception to "no new backend code": `familyOfferingId` added to `OfferingListRow` (`types/product.ts`) + `findList`'s `SELECT` (`db/repositories/product-offering.ts`) — one column, no new repository method or service export. |
| pm19 | `specs/pm19-spec.md` | UI + Server Action merged: `actions/product/create-offering.action.ts` (new, first file in `actions/product/`) — guard → `safeParse` → delegate to pm11's `createOffering` → `revalidatePath` both product pages, three failure codes only (`VALIDATION_ERROR`/`FORBIDDEN`/`SERVER_ERROR`, no `NAME_CONFLICT`). `offering-form.tsx` + `create-offering-dialog.tsx` (new, `components/products/manage/`) — create-mode-only form (Name, Sellable default-checked, Billing only default-unchecked, no `isBundle` control ever), dialog closes + toasts + `router.refresh()` on success (no URL-state navigation). Fills pm18's "New offering" CTA seam in `manage-offering-table.tsx`. Rewrites the guardrail's "no `actions/product/` folder" assertion into an incremental allow-list in this same commit (Design §2.5), not deferred to pm24. |
| pm20 | `specs/pm20-spec.md` | UI + Server Action merged: `actions/product/update-offering.action.ts` (new) — guard → `safeParse` → delegate to pm13's `updateOffering` → `revalidatePath` both product pages. `offering-form.tsx` gains its edit mode, composed inline in `manage-offering-table.tsx` (no separate dialog file); two independent `handleSubmit` calls, one per save outcome; button set depends on `currentStatus` (DRAFT: Save/Save as new draft; ACTIVE: Create new draft only, behind a warning banner). One flagged read-model exception (same shape as pm18 §2.2): `billingOnly` added to `OfferingListRow`/`findList`'s `SELECT`. Fills both pm18 Edit seams; a branched save auto-expands its family. |
| pm21 | `specs/pm21-spec.md` | UI + Server Actions merged: `actions/product/{create,update,delete}-specification.action.ts` (new) — guard → `safeParse` (create/update only) → delegate to pm14's three services → `revalidatePath` both product pages. `specification-form.tsx` (new) — first form editing a `Record<string, string>` field, via a UI-only `characteristicsList` array (`useFieldArray`) translated to/from the wire `Record` at the form boundary. `specifications-dialog.tsx` (new, `SpecificationsDialog`) — one `Dialog` swapping list/form views, plus a nested `AlertDialog` for delete (DRAFT-only). A direct success keeps the dialog open + refreshes (departs from pm19/pm20's "always close"); a branch-producing success closes/toasts/auto-expands like pm20. Read-model gap closed via a new page-local `fetchSpecificationsByOfferingId` helper (reuses `getOfferingDetail` unmodified) rather than widening `OfferingListRow` or adding a read action. Sixth row-action button (`ListChecks`) added to the table matrix and both phase-2 doc files. |
| pm22 | `specs/pm22-spec.md` | UI + Server Action merged: `actions/product/insert-price.action.ts` (new) — guard → `safeParse` (pm15's own `insertPriceSchema`, unchanged) → delegate to pm15's `insertPrice` → `revalidatePath` both product pages. `price-form.tsx` (new, `PriceForm`) — exactly six fields (name/price type/currency/GL code/pricing model/start date), a new input-only tier-row editor (`useFieldArray`) for the tiered branch, a local `priceFormSchema` that validates only flat-shape checks and duplicates the 3-day tolerance a third time, deliberately not re-declaring tier-contiguity/XOR (those stay defined once, in `tieredPricingCharacteristicsSchema`). `add-price-dialog.tsx` (new, `AddPriceDialog`) — unlike Edit, gets its own dialog file (one submit outcome, `CreateOfferingDialog`'s shape) and its own per-row instance (no new shared table state in `ManageOfferingTable`). Fills every pm18 Add-price seam, primary and expanded-sibling rows alike; a branched success auto-expands the family like pm20/pm21. |
| pm23 | `specs/pm23-spec.md` | UI + Server Actions merged: `actions/product/activate-offering.action.ts` (new, `products:EDIT`) and `retire-offering.action.ts` (new, `products:DELETE` — the permission split from Activate) — each guard → `safeParse` (pm16's own schemas, unchanged) → delegate to pm16's `activateOffering`/`retireOffering` → `revalidatePath` both product pages. `activate-offering-dialog.tsx` (new, `ActivateOfferingDialog`) — plain `Dialog`, accent confirm button, `NO_PRICE_ROWS`/`SPECIFICATIONS_NOT_RESOLVED` render as a persistent inline `Alert` (not a toast, the one departure from pm19/20/22's failure handling), `onSuperseded` prop auto-expands the family. `retire-offering-dialog.tsx` (new, `RetireOfferingDialog`) — one `AlertDialog` handling both Retire and Discard, copy keyed off a `currentStatus` prop, success toast keyed off the server's own `eventType`. Both dialogs use a plain `useState`-backed `Textarea` Reason field, no `react-hook-form` (single-field schemas don't earn it). Fills every remaining pm18 seam (Activate/Discard/Retire, primary and sibling rows) — the last of the five UI units; every button pm18 left inert now has real behavior. |
| pm24 | `specs/pm24-spec.md` | Tests + CI only, the phase's ship gate (pm09's structural precedent, "almost line-for-line"): rewrites `tests/guardrails/product-module-boundaries.test.ts`'s assertion 1 (folder existence + exact export-name grep, closed set), adds guardrail 11 (View stays read-only — `components/products/*.tsx` outside `manage/` and the View Product page tree import nothing from the write surface), guardrail 13 (schema-diff — `product_offering` differs from Phase 1 by exactly `family_offering_id` + its index, the other two tables byte-identical), verifies/extends the guardrail-12 route-manifest check and the `PRODUCT_WRITE_SERVICE_FILES` allow-list, adds an optional guardrail-10 structural backstop (exactly one call site for `deleteSpecification`). Extends `tests/auth/guard.integration.test.ts` with a `productsManagerUserId` (products:EDIT only) principal and direct-Server-Action-call proofs that `products:EDIT` and `products:DELETE` are two different gates. No `app/**`/`services/**`/`db/**`/`actions/**`/`components/**` change. |
